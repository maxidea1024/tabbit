{{~## One constant module. `set` is the single set this file is for. ##~}}
{{ include 'ruby-file-head.sbn' ~}}
  # Generated from {{ set.location }}
{{~ for line in set.comment ~}}
  # {{ line }}
{{~ end ~}}
  module {{ set.name }}
{{~ for constant in set.constants ~}}
{{~ for line in constant.comment ~}}
    # {{ line }}
{{~ end ~}}
    {{ constant.name }} = {{ constant.value }}
{{~ end ~}}
  end
{{ include 'ruby-file-foot.sbn' ~}}
