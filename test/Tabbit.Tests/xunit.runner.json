{
  "$schema": "https://xunit.net/schema/current/xunit.runner.schema.json",

  "//": [
    "Off, and three attempts at turning it on are what keeps it off.",
    "",
    "The tests share scenario output trees: one converts and another reads what it wrote.",
    "That works because nothing runs beside anything else. Turning it on measures 3m20s",
    "against 13m08s serial, and fails - 52 tests, then 18 once the language harnesses stopped",
    "building inside the trees they were generated into, then 65 once a test that plants files",
    "in an output tree got a scenario of its own and two builds stopped sharing a directory.",
    "",
    "The set is different every run, which is what says the remainder is not a list of known",
    "races. What it would take is every test that shares a tree owning what it writes. The",
    "updater tests look like a second problem on top of that - they start HTTP servers, so a",
    "port rather than a directory.",
    "",
    "doc/roadmap.md, the not-yet list, carries the numbers so the next attempt starts from",
    "them rather than from the guess that a few places need fixing."
  ],
  "parallelizeTestCollections": false
}
