// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_A_TEMPLATE_H
#define TABBIT_GENERATED_A_TEMPLATE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "A_forward.h"

namespace x {
// Generated from test/fixtures/xlsx/reserved-words/reserved-words.xlsx : ReservedWords : B2
/// Named after a C++ keyword.
struct TemplateRecord {
  /// primary index
  std::int32_t index = 0;
  /// class: keyword in C++ and C#
  std::string tb_class;
  /// int: keyword in C++ and C#
  std::int32_t tb_int = 0;
  /// delete: keyword in C++
  bool tb_delete = false;
  /// operator: keyword in C++, and a secondary index
  std::string tb_operator;
  /// namespace: keyword in C++ and C#
  std::string tb_namespace;
  /// constructor: special member in TypeScript
  std::string constructor;
  /// function: keyword in TypeScript
  std::string function;
};

/// Named after a C++ keyword.
class TemplateTable {
 public:
  const std::vector<TemplateRecord>& records() const { return records_; }

  /// The row with this Index, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const TemplateRecord* find_by_index(std::int32_t key) const {
    const auto it = by_index_.find(key);
    return it == by_index_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_index(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const TemplateRecord& get_by_index_or_throw(std::int32_t key) const {
    const TemplateRecord* found = find_by_index(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Template` that corresponds to "
                    "field `Index` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Index.
  bool contains_index(std::int32_t key) const {
    return by_index_.find(key) != by_index_.end();
  }

  /// The row with this Operator, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const TemplateRecord* find_by_operator(const std::string& key) const {
    const auto it = by_operator_.find(key);
    return it == by_operator_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Operator, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_operator(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const TemplateRecord& get_by_operator_or_throw(const std::string& key) const {
    const TemplateRecord* found = find_by_operator(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Template` that corresponds to "
                    "field `Operator` value ") + key);
    }

    return *found;
  }

  /// Whether the table holds a row with this Operator.
  bool contains_operator(const std::string& key) const {
    return by_operator_.find(key) != by_operator_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<TemplateRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Template.Index", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Template.Index");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].index = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Template.Class", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Template.Class");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].tb_class = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Template.Int", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Template.Int");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].tb_int = value;
            }
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "Template.Delete", tabbit::kKindScalar, false, {tabbit::kElementBool});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Template.Delete");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.tb_delete = cursor.next_bool();
          }
          break;
        }
        case 5: {
          tabbit::check_column(column, "Template.Operator", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Template.Operator");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].tb_operator = value;
            }
          }
          break;
        }
        case 6: {
          tabbit::check_column(column, "Template.Namespace", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Template.Namespace");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].tb_namespace = value;
            }
          }
          break;
        }
        case 7: {
          tabbit::check_column(column, "Template.Constructor", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Template.Constructor");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].constructor = value;
            }
          }
          break;
        }
        case 8: {
          tabbit::check_column(column, "Template.Function", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Template.Function");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].function = value;
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class A;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<TemplateRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> index_map;
    index_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      index_map.emplace(records[i].index, i);
    }

    std::unordered_map<std::string, std::size_t> operator_map;
    operator_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      operator_map.emplace(records[i].tb_operator, i);
    }

    records_ = std::move(records);
    by_index_ = std::move(index_map);
    by_operator_ = std::move(operator_map);
  }

  std::vector<TemplateRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_index_;
  std::unordered_map<std::string, std::size_t> by_operator_;
};

}  // namespace x

#endif  // TABBIT_GENERATED_A_TEMPLATE_H
