// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_A_H
#define TABBIT_GENERATED_A_H

#include <cstddef>
#include <string>

#include "enums/A_enum_keyword.h"
#include "tables/A_template.h"
#include "tables/A_package.h"

namespace x {
/// Every table, loaded together so cross-table references can be resolved.
class A {
 public:
  const TemplateTable& tb_template() const { return tb_template_; }
  const PackageTable& package() const { return package_; }

  /// Reads every table from `base_path`, then links the references between them.
  ///
  /// Safe to call on a loaded accessor. Every file is read into a table of its own and the
  /// references are linked among those, so a read that throws part way through leaves every
  /// table holding the load it already had, and no row points at a row from it.
  void read_all(const std::string& base_path, const std::string& file_extension = ".tcb") {
    TemplateTable loaded_tb_template;
    loaded_tb_template.read(base_path + "/Template" + file_extension);
    PackageTable loaded_package;
    loaded_package.read(base_path + "/Package" + file_extension);

    solve_cross_references(loaded_tb_template, loaded_package);

    tb_template_ = std::move(loaded_tb_template);
    package_ = std::move(loaded_package);
  }

 private:
  /// The tables arrive as arguments, which is how this links the load being read rather
  /// than the one already published.
  ///
  /// Every table is passed whether or not it takes part in a reference, so the call site
  /// is the same shape for every model. `[[maybe_unused]]` is what says so to the
  /// compiler: the gate builds with `-Wextra -Werror`, and a model where nothing
  /// references anything - which is most of them - otherwise fails to compile on the
  /// unused parameters.
  void solve_cross_references([[maybe_unused]] TemplateTable& loaded_tb_template, [[maybe_unused]] PackageTable& loaded_package) {
    // No table references another.
  }

  TemplateTable tb_template_;
  PackageTable package_;
};

}  // namespace x

#endif  // TABBIT_GENERATED_A_H
