-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")

-- Generated from test/fixtures/xlsx/reserved-words/reserved-words.xlsx : ReservedWords : B2.
-- Named after a C++ keyword.
---@class TemplateRecord
---@field index integer
---@field class string
---@field int integer
---@field delete boolean
---@field operator string
---@field namespace string
---@field constructor string
---@field function string
local TemplateRecordMeta = tcb.strictType("a `Template` row", { "index", "class", "int", "delete", "operator", "namespace", "constructor", "function" })

---@return TemplateRecord
local function newTemplateRecord()
  return setmetatable({
    index = 0,
    class = "",
    int = 0,
    delete = false,
    operator = "",
    namespace = "",
    constructor = "",
    ["function"] = "",
  }, TemplateRecordMeta)
end

---@class TemplateTable
---@field records TemplateRecord[]
local TemplateTable = {}

local TemplateTableMeta = tcb.strictInstance("the `Template` table", TemplateTable, { "records", "byIndex", "byOperator" })

function TemplateTable.new()
  return setmetatable({
    records = {},
    byIndex = {},
    byOperator = {},
  }, TemplateTableMeta)
end

-- The row with this Index, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function TemplateTable:findByIndex(key)
  return self.byIndex[key]
end

-- The row with this Index, or a raised error naming what was missing -
-- for a key that has to be there.
function TemplateTable:getByIndexOrThrow(key)
  local record = self:findByIndex(key)

  if record == nil then
    tcb.fail("there is no record in table `Template` that corresponds to " ..
      "field `Index` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this Index.
function TemplateTable:containsIndex(key)
  return self:findByIndex(key) ~= nil
end

-- The row with this Operator, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function TemplateTable:findByOperator(key)
  return self.byOperator[key]
end

-- The row with this Operator, or a raised error naming what was missing -
-- for a key that has to be there.
function TemplateTable:getByOperatorOrThrow(key)
  local record = self:findByOperator(key)

  if record == nil then
    tcb.fail("there is no record in table `Template` that corresponds to " ..
      "field `Operator` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this Operator.
function TemplateTable:containsOperator(key)
  return self:findByOperator(key) ~= nil
end

-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function TemplateTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/wire/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newTemplateRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "Template.Index", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Template.Index")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].index = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "Template.Class", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Template.Class")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].class = value
        end

        at = at + n
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "Template.Int", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Template.Int")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].int = value
        end

        at = at + n
      end
    elseif column.tag == 4 then
      tcb.checkColumn(column, "Template.Delete", tcb.KIND_SCALAR, false, { tcb.ELEMENT_BOOL })
      local cursor = tcb.newCursor(reader, column, count, "Template.Delete")
      for i = 1, count do
        local record = records[i]
        record.delete = cursor:nextBool()
      end
    elseif column.tag == 5 then
      tcb.checkColumn(column, "Template.Operator", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Template.Operator")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].operator = value
        end

        at = at + n
      end
    elseif column.tag == 6 then
      tcb.checkColumn(column, "Template.Namespace", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Template.Namespace")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].namespace = value
        end

        at = at + n
      end
    elseif column.tag == 7 then
      tcb.checkColumn(column, "Template.Constructor", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Template.Constructor")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].constructor = value
        end

        at = at + n
      end
    elseif column.tag == 8 then
      tcb.checkColumn(column, "Template.Function", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Template.Function")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i]["function"] = value
        end

        at = at + n
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byIndex = {}

  for i = 1, #records do
    byIndex[records[i].index] = records[i]
  end

  local byOperator = {}

  for i = 1, #records do
    byOperator[records[i].operator] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byIndex = byIndex
  self.byOperator = byOperator
end

-- The same load from a file on disk.
function TemplateTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return TemplateTable
