// swift-tools-version:5.9
// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------
//
// One target over the files as they are, rather than the Sources/<Target> layout:
// the commoner use of this output is to drop it into a project that already has a
// build, and moving the files for the sake of a manifest that may not be wanted
// would make that use the awkward one.
//
// swift-crypto is here for one call: the table MAC's HMAC-SHA-256. On Apple
// platforms CryptoKit answers instead and this dependency is never fetched; without
// either, the reader still builds and reads every file but cannot verify a MAC.
// doc/languages/swift.md says what to do in each case.
import PackageDescription

let package = Package(
    name: "X",
    platforms: [.macOS(.v10_15), .iOS(.v13), .tvOS(.v13), .watchOS(.v6)],
    products: [
        .library(name: "X", targets: ["X"])
    ],
    dependencies: [
        .package(
            url: "https://github.com/apple/swift-crypto.git",
            from: "3.0.0")
    ],
    targets: [
        .target(
            name: "X",
            dependencies: [.product(name: "Crypto", package: "swift-crypto")],
            path: ".",
            sources: ["tabbit", "A.swift", "tables", "enums"])
    ]
)
