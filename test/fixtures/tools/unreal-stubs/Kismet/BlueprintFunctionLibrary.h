// UBlueprintFunctionLibrary, which the generated Blueprint library derives from.
//
// Empty. In the engine it is a UObject carrying reflection data; nothing the generated library
// does at runtime needs any of it, because every member is a static function the harness can
// call directly. That the functions are exposed to Blueprint at all is checked by
// UnrealTargetTests reading the generated text, which is where a macro can be checked.

#pragma once

#include "CoreMinimal.h"

class UBlueprintFunctionLibrary
{
};
