// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_RECORDTRIMACCESSOR_LOOT_H
#define TABBIT_GENERATED_RECORDTRIMACCESSOR_LOOT_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "RecordTrimAccessor_forward.h"
// Generated from test/fixtures/xlsx/record-trim/record-trim.xlsx : Trim : B2
/// One element of LootRecord::slot.
struct LootRecord_slot_entry {
  /// element 1
  std::int32_t id = 0;
  /// element 1
  std::int32_t count = 0;
  /// element 1 - a string, so the element factory has to fill it
  std::string label;
};

/// One element of LootRecord::pos.
struct LootRecord_pos_entry {
  /// single record, not an array
  std::int32_t x = 0;
  /// second member of it
  std::int32_t y = 0;
};

/// A record array trimmed at the last element the row filled in.
struct LootRecord {
  /// primary index
  std::int32_t index = 0;
  /// plain column, must not move
  std::string name;
  /// element 1
  std::vector<LootRecord_slot_entry> slot;
  /// single record, not an array
  LootRecord_pos_entry pos;
  /// scalar serial array, element 1 - required, so the array is never empty
  std::vector<std::string> tag;
};

/// A record array trimmed at the last element the row filled in.
class LootTable {
 public:
  const std::vector<LootRecord>& records() const { return records_; }

  /// The row with this Index, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const LootRecord* find_by_index(std::int32_t key) const {
    const auto it = by_index_.find(key);
    return it == by_index_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_index(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const LootRecord& get_by_index_or_throw(std::int32_t key) const {
    const LootRecord* found = find_by_index(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Loot` that corresponds to "
                    "field `Index` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Index.
  bool contains_index(std::int32_t key) const {
    return by_index_.find(key) != by_index_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<LootRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Loot.Index", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loot.Index");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].index = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Loot.Name", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loot.Name");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].name = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Loot.Slot.Id", tabbit::kKindArray, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loot.Slot.Id");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = cursor.next_length();
            record.slot.assign(static_cast<std::size_t>(element_count), LootRecord_slot_entry());
            for (std::int32_t j = 0; j < element_count; ++j) {
              record.slot[static_cast<std::size_t>(j)].id = cursor.next_i32();
            }
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "Loot.Slot.Count", tabbit::kKindArray, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loot.Slot.Count");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = cursor.next_length();
            if (record.slot.size() != static_cast<std::size_t>(element_count)) {
              throw tabbit::TcbError(
                  "Loot.slot: the file gives this row a different "
                  "element count for one member of the record than for another; every member of a "
                  "record carries the same count, so the file is damaged");
            }
            for (std::int32_t j = 0; j < element_count; ++j) {
              record.slot[static_cast<std::size_t>(j)].count = cursor.next_i32();
            }
          }
          break;
        }
        case 5: {
          tabbit::check_column(column, "Loot.Slot.Label", tabbit::kKindArray, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loot.Slot.Label");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = cursor.next_length();
            if (record.slot.size() != static_cast<std::size_t>(element_count)) {
              throw tabbit::TcbError(
                  "Loot.slot: the file gives this row a different "
                  "element count for one member of the record than for another; every member of a "
                  "record carries the same count, so the file is damaged");
            }
            for (std::int32_t j = 0; j < element_count; ++j) {
              record.slot[static_cast<std::size_t>(j)].label = cursor.next_string();
            }
          }
          break;
        }
        case 6: {
          tabbit::check_column(column, "Loot.Pos.X", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loot.Pos.X");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].pos.x = value;
            }
          }
          break;
        }
        case 7: {
          tabbit::check_column(column, "Loot.Pos.Y", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loot.Pos.Y");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].pos.y = value;
            }
          }
          break;
        }
        case 8: {
          tabbit::check_column(column, "Loot.Tag", tabbit::kKindArray, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loot.Tag");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = cursor.next_length();
            record.tag.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              record.tag[static_cast<std::size_t>(j)] = cursor.next_string();
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class RecordTrimAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<LootRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> index_map;
    index_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      index_map.emplace(records[i].index, i);
    }

    records_ = std::move(records);
    by_index_ = std::move(index_map);
  }

  std::vector<LootRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_index_;
};

#endif  // TABBIT_GENERATED_RECORDTRIMACCESSOR_LOOT_H
