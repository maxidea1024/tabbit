/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RECORD_TRIM_LOOT_H
#define RECORD_TRIM_LOOT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "RecordTrim_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/record-trim/record-trim.xlsx : Trim : B2
 *
 * A record array trimmed at the last element the row filled in.
 */
/* One element of RecordTrim_LootRecord_t::slot. */
struct RecordTrim_LootRecord_t_slot_entry {
  /* element 1 */
  int32_t id;
  /* element 1 */
  int32_t count;
  /* element 1 - a string, so the element factory has to fill it */
  const char* label;
};

/* One element of RecordTrim_LootRecord_t::pos. */
struct RecordTrim_LootRecord_t_pos_entry {
  /* single record, not an array */
  int32_t x;
  /* second member of it */
  int32_t y;
};

struct RecordTrim_LootRecord_t {
  /* primary index */
  int32_t index;
  /* plain column, must not move */
  const char* name;
  /* element 1 */
  struct RecordTrim_LootRecord_t_slot_entry* slot;
  int32_t slot_count;
  /* single record, not an array */
  struct RecordTrim_LootRecord_t_pos_entry pos;
  /* scalar serial array, element 1 - required, so the array is never empty */
  const char** tag;
  int32_t tag_count;
};

/* Every row of Loot.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct RecordTrim_LootTable_t {
  RecordTrim_LootRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} RecordTrim_LootTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool RecordTrim_LootLoad(RecordTrim_LootTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void RecordTrim_LootFree(RecordTrim_LootTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const RecordTrim_LootRecord_t* RecordTrim_LootFindByIndex(
  const RecordTrim_LootTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool RecordTrim_LootContainsIndex(
  const RecordTrim_LootTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RECORD_TRIM_LOOT_H */
