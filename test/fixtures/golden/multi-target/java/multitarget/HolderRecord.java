// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package multitarget;

import tabbit.TcbReader;

// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Holders : B2
/** One column per case the spec states. */
public final class HolderRecord {
    /** primary index */
    public int index;
    /** two targets - the ordinary case */
    public int pick;
    /** five targets, and the value picks one */
    public int wide;
    /** the same notation with one name */
    public WeaponRecord only;
    public int onlyIndex;
    /** two targets, and `-` is none of them */
    public int maybe;
    /**
     * Whether this row has a value for maybe. The value field keeps its type and
     * holds the type's empty value when the row had none; this says which it was.
     */
    public boolean hasMaybe;
    /**
     * The row pick names, as whichever of its target tables holds it. Read it
     * through the methods below rather than directly: they check the discriminator first.
     */
    public Object pickRow;

    /** Which table pick is a row of. */
    public HolderPickTarget pickTarget = HolderPickTarget.NONE;
    /**
     * The row wide names, as whichever of its target tables holds it. Read it
     * through the methods below rather than directly: they check the discriminator first.
     */
    public Object wideRow;

    /** Which table wide is a row of. */
    public HolderWideTarget wideTarget = HolderWideTarget.NONE;
    /**
     * The row maybe names, as whichever of its target tables holds it. Read it
     * through the methods below rather than directly: they check the discriminator first.
     */
    public Object maybeRow;

    /** Which table maybe is a row of. */
    public HolderMaybeTarget maybeTarget = HolderMaybeTarget.NONE;

    /**
     * The WeaponRecord row pick names, or null when it names a row of
     * one of the others.
     */
    public WeaponRecord weaponByPick() {
        if (pickTarget != HolderPickTarget.WEAPON) {
            return null;
        }

        return (WeaponRecord) pickRow;
    }

    /**
     * The ArmourRecord row pick names, or null when it names a row of
     * one of the others.
     */
    public ArmourRecord armourByPick() {
        if (pickTarget != HolderPickTarget.ARMOUR) {
            return null;
        }

        return (ArmourRecord) pickRow;
    }

    /**
     * The WeaponRecord row wide names, or null when it names a row of
     * one of the others.
     */
    public WeaponRecord weaponByWide() {
        if (wideTarget != HolderWideTarget.WEAPON) {
            return null;
        }

        return (WeaponRecord) wideRow;
    }

    /**
     * The ArmourRecord row wide names, or null when it names a row of
     * one of the others.
     */
    public ArmourRecord armourByWide() {
        if (wideTarget != HolderWideTarget.ARMOUR) {
            return null;
        }

        return (ArmourRecord) wideRow;
    }

    /**
     * The TrinketRecord row wide names, or null when it names a row of
     * one of the others.
     */
    public TrinketRecord trinketByWide() {
        if (wideTarget != HolderWideTarget.TRINKET) {
            return null;
        }

        return (TrinketRecord) wideRow;
    }

    /**
     * The MountRecord row wide names, or null when it names a row of
     * one of the others.
     */
    public MountRecord mountByWide() {
        if (wideTarget != HolderWideTarget.MOUNT) {
            return null;
        }

        return (MountRecord) wideRow;
    }

    /**
     * The BannerRecord row wide names, or null when it names a row of
     * one of the others.
     */
    public BannerRecord bannerByWide() {
        if (wideTarget != HolderWideTarget.BANNER) {
            return null;
        }

        return (BannerRecord) wideRow;
    }

    /**
     * The WeaponRecord row maybe names, or null when it names a row of
     * one of the others.
     */
    public WeaponRecord weaponByMaybe() {
        if (maybeTarget != HolderMaybeTarget.WEAPON) {
            return null;
        }

        return (WeaponRecord) maybeRow;
    }

    /**
     * The ArmourRecord row maybe names, or null when it names a row of
     * one of the others.
     */
    public ArmourRecord armourByMaybe() {
        if (maybeTarget != HolderMaybeTarget.ARMOUR) {
            return null;
        }

        return (ArmourRecord) maybeRow;
    }

}
