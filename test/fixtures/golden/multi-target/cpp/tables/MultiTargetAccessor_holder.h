// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_MULTITARGETACCESSOR_HOLDER_H
#define TABBIT_GENERATED_MULTITARGETACCESSOR_HOLDER_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "MultiTargetAccessor_forward.h"
#include "enums/MultiTargetAccessor_enum_holder_pick_target.h"
#include "enums/MultiTargetAccessor_enum_holder_wide_target.h"
#include "enums/MultiTargetAccessor_enum_holder_maybe_target.h"

namespace multi_target {
// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Holders : B2
/// One column per case the spec states.
struct HolderRecord {
  /// primary index
  std::int32_t index = 0;
  /// two targets - the ordinary case
  std::int32_t pick = 0;
  /// five targets, and the value picks one
  std::int32_t wide = 0;
  /// the same notation with one name
  const WeaponRecord* only = nullptr;
  std::int32_t only_index = 0;
  /// two targets, and `-` is none of them
  std::int32_t maybe = 0;
  /// Whether this row has a value for `maybe`.
  bool has_maybe = false;

  /// The row `pick` names, as whichever of its target tables holds it. Read it
  /// through the accessors below rather than directly: they check the discriminator first.
  const void* pick_row = nullptr;

  /// Which table `pick` is a row of.
  HolderPickTarget pick_target = HolderPickTarget::None;

  /// The `WeaponRecord` row `pick` names, or nullptr when it names a
  /// row of one of the others.
  const WeaponRecord* weapon_by_pick() const {
    return pick_target == HolderPickTarget::Weapon
        ? static_cast<const WeaponRecord*>(pick_row)
        : nullptr;
  }

  /// The `ArmourRecord` row `pick` names, or nullptr when it names a
  /// row of one of the others.
  const ArmourRecord* armour_by_pick() const {
    return pick_target == HolderPickTarget::Armour
        ? static_cast<const ArmourRecord*>(pick_row)
        : nullptr;
  }

  /// The row `wide` names, as whichever of its target tables holds it. Read it
  /// through the accessors below rather than directly: they check the discriminator first.
  const void* wide_row = nullptr;

  /// Which table `wide` is a row of.
  HolderWideTarget wide_target = HolderWideTarget::None;

  /// The `WeaponRecord` row `wide` names, or nullptr when it names a
  /// row of one of the others.
  const WeaponRecord* weapon_by_wide() const {
    return wide_target == HolderWideTarget::Weapon
        ? static_cast<const WeaponRecord*>(wide_row)
        : nullptr;
  }

  /// The `ArmourRecord` row `wide` names, or nullptr when it names a
  /// row of one of the others.
  const ArmourRecord* armour_by_wide() const {
    return wide_target == HolderWideTarget::Armour
        ? static_cast<const ArmourRecord*>(wide_row)
        : nullptr;
  }

  /// The `TrinketRecord` row `wide` names, or nullptr when it names a
  /// row of one of the others.
  const TrinketRecord* trinket_by_wide() const {
    return wide_target == HolderWideTarget::Trinket
        ? static_cast<const TrinketRecord*>(wide_row)
        : nullptr;
  }

  /// The `MountRecord` row `wide` names, or nullptr when it names a
  /// row of one of the others.
  const MountRecord* mount_by_wide() const {
    return wide_target == HolderWideTarget::Mount
        ? static_cast<const MountRecord*>(wide_row)
        : nullptr;
  }

  /// The `BannerRecord` row `wide` names, or nullptr when it names a
  /// row of one of the others.
  const BannerRecord* banner_by_wide() const {
    return wide_target == HolderWideTarget::Banner
        ? static_cast<const BannerRecord*>(wide_row)
        : nullptr;
  }

  /// The row `maybe` names, as whichever of its target tables holds it. Read it
  /// through the accessors below rather than directly: they check the discriminator first.
  const void* maybe_row = nullptr;

  /// Which table `maybe` is a row of.
  HolderMaybeTarget maybe_target = HolderMaybeTarget::None;

  /// The `WeaponRecord` row `maybe` names, or nullptr when it names a
  /// row of one of the others.
  const WeaponRecord* weapon_by_maybe() const {
    return maybe_target == HolderMaybeTarget::Weapon
        ? static_cast<const WeaponRecord*>(maybe_row)
        : nullptr;
  }

  /// The `ArmourRecord` row `maybe` names, or nullptr when it names a
  /// row of one of the others.
  const ArmourRecord* armour_by_maybe() const {
    return maybe_target == HolderMaybeTarget::Armour
        ? static_cast<const ArmourRecord*>(maybe_row)
        : nullptr;
  }
};

/// One column per case the spec states.
class HolderTable {
 public:
  const std::vector<HolderRecord>& records() const { return records_; }

  /// The row with this Index, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const HolderRecord* find_by_index(std::int32_t key) const {
    const auto it = by_index_.find(key);
    return it == by_index_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_index(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const HolderRecord& get_by_index_or_throw(std::int32_t key) const {
    const HolderRecord* found = find_by_index(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Holder` that corresponds to "
                    "field `Index` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Index.
  bool contains_index(std::int32_t key) const {
    return by_index_.find(key) != by_index_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<HolderRecord> records;
    records.resize(row_count);
    std::vector<std::uint8_t> presence;

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Holder.Index", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Holder.Index");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].index = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Holder.Pick", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Holder.Pick");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].pick = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Holder.Wide", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Holder.Wide");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].wide = value;
            }
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "Holder.Only", tabbit::kKindScalar, false, {tabbit::kElementI32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Holder.Only");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].only_index = value;
            }
          }
          break;
        }
        case 5: {
          tabbit::check_column(column, "Holder.Maybe", tabbit::kKindScalar, true, {tabbit::kElementI32, tabbit::kElementVarint});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Holder.Maybe");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].maybe = value;
            }
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_maybe = tabbit::is_present(presence, i);

            if (!records[i].has_maybe) {
              records[i].maybe = 0;
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class MultiTargetAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<HolderRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> index_map;
    index_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      index_map.emplace(records[i].index, i);
    }

    records_ = std::move(records);
    by_index_ = std::move(index_map);
  }

  std::vector<HolderRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_index_;
};

}  // namespace multi_target

#endif  // TABBIT_GENERATED_MULTITARGETACCESSOR_HOLDER_H
