// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Tabbit.Fixtures.MultiTarget
{
    /// <summary>
    /// One column per case the spec states.
    /// </summary>
    [System.Serializable]
    public partial class HolderTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// primary index
            /// </summary>
            public int Index => _index;

            /// <summary>
            /// two targets - the ordinary case
            /// </summary>
            public int Pick => _pick;

            /// <summary>
            /// five targets, and the value picks one
            /// </summary>
            public int Wide => _wide;

            /// <summary>
            /// the same notation with one name
            /// </summary>
            public WeaponTable.Record Only => _only;

            /// <summary>
            /// two targets, and `-` is none of them
            /// </summary>
            public int Maybe => _maybe;
            /// <summary>Whether this row has a value for <see cref="Maybe"/>.</summary>
            public bool HasMaybe => _maybeHasValue;

            /// <summary>Which table <see cref="Pick"/> is a row of.</summary>
            public HolderPickTarget PickTarget => _pickTarget;
            /// <summary>
            /// The <c>Weapon</c> row <see cref="Pick"/> names, or null
            /// when it names a row of one of the others.
            /// </summary>
            public WeaponTable.Record PickAsWeapon
                => _pickTarget == HolderPickTarget.Weapon
                    ? (WeaponTable.Record)_pickRow
                    : null;
            /// <summary>
            /// The <c>Armour</c> row <see cref="Pick"/> names, or null
            /// when it names a row of one of the others.
            /// </summary>
            public ArmourTable.Record PickAsArmour
                => _pickTarget == HolderPickTarget.Armour
                    ? (ArmourTable.Record)_pickRow
                    : null;

            /// <summary>Which table <see cref="Wide"/> is a row of.</summary>
            public HolderWideTarget WideTarget => _wideTarget;
            /// <summary>
            /// The <c>Weapon</c> row <see cref="Wide"/> names, or null
            /// when it names a row of one of the others.
            /// </summary>
            public WeaponTable.Record WideAsWeapon
                => _wideTarget == HolderWideTarget.Weapon
                    ? (WeaponTable.Record)_wideRow
                    : null;
            /// <summary>
            /// The <c>Armour</c> row <see cref="Wide"/> names, or null
            /// when it names a row of one of the others.
            /// </summary>
            public ArmourTable.Record WideAsArmour
                => _wideTarget == HolderWideTarget.Armour
                    ? (ArmourTable.Record)_wideRow
                    : null;
            /// <summary>
            /// The <c>Trinket</c> row <see cref="Wide"/> names, or null
            /// when it names a row of one of the others.
            /// </summary>
            public TrinketTable.Record WideAsTrinket
                => _wideTarget == HolderWideTarget.Trinket
                    ? (TrinketTable.Record)_wideRow
                    : null;
            /// <summary>
            /// The <c>Mount</c> row <see cref="Wide"/> names, or null
            /// when it names a row of one of the others.
            /// </summary>
            public MountTable.Record WideAsMount
                => _wideTarget == HolderWideTarget.Mount
                    ? (MountTable.Record)_wideRow
                    : null;
            /// <summary>
            /// The <c>Banner</c> row <see cref="Wide"/> names, or null
            /// when it names a row of one of the others.
            /// </summary>
            public BannerTable.Record WideAsBanner
                => _wideTarget == HolderWideTarget.Banner
                    ? (BannerTable.Record)_wideRow
                    : null;

            /// <summary>Which table <see cref="Maybe"/> is a row of.</summary>
            public HolderMaybeTarget MaybeTarget => _maybeTarget;
            /// <summary>
            /// The <c>Weapon</c> row <see cref="Maybe"/> names, or null
            /// when it names a row of one of the others.
            /// </summary>
            public WeaponTable.Record MaybeAsWeapon
                => _maybeTarget == HolderMaybeTarget.Weapon
                    ? (WeaponTable.Record)_maybeRow
                    : null;
            /// <summary>
            /// The <c>Armour</c> row <see cref="Maybe"/> names, or null
            /// when it names a row of one of the others.
            /// </summary>
            public ArmourTable.Record MaybeAsArmour
                => _maybeTarget == HolderMaybeTarget.Armour
                    ? (ArmourTable.Record)_maybeRow
                    : null;
            #endregion

            #region Reference wiring
            public void SetReference_Only_INTERNAL(WeaponTable.Record value) => _only = value;
            #endregion

            #region Multi-target reference wiring
            public void SetReference_Pick_INTERNAL(HolderPickTarget target, object row)
            {
                _pickTarget = target;
                _pickRow = row;
            }
            public void SetReference_Wide_INTERNAL(HolderWideTarget target, object row)
            {
                _wideTarget = target;
                _wideRow = row;
            }
            public void SetReference_Maybe_INTERNAL(HolderMaybeTarget target, object row)
            {
                _maybeTarget = target;
                _maybeRow = row;
            }
            #endregion

            #region Storage
            internal int _index;
            internal int _pick;
            internal int _wide;
            internal WeaponTable.Record _only;
            public int _only_Weapon_index;
            public bool _only_F = false;
            internal int _maybe;
            internal bool _maybeHasValue;
            internal object _pickRow;
            internal HolderPickTarget _pickTarget;
            internal object _wideRow;
            internal HolderWideTarget _wideTarget;
            internal object _maybeRow;
            internal HolderMaybeTarget _maybeTarget;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Index\":"); ToStringHelper.ToString(Index, sb);
                sb.Append(",\"Pick\":"); ToStringHelper.ToString(Pick, sb);
                sb.Append(",\"Wide\":"); ToStringHelper.ToString(Wide, sb);
                sb.Append(",\"Only\":"); ToStringHelper.ToString(Only, sb);
                sb.Append(",\"Maybe\":"); ToStringHelper.ToString(Maybe, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Index", "Pick", "Wide", "Only", "Maybe" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Index, r.Pick, r.Wide, r.Only, r.Maybe });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Index'
        public Dictionary<int, Record> RecordsByIndex => _recordsByIndex;
        private Dictionary<int, Record> _recordsByIndex = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Index`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByIndex(int key)
            => _recordsByIndex.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Index`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByIndex(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIndexOrThrow(int key)
        {
            if (!_recordsByIndex.TryGetValue(key, out Record record))
                throw new TabbitException($"There is no record in table `Holder` that corresponds to field `Index` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Index`.</summary>
        public bool ContainsIndex(int key) => _recordsByIndex.ContainsKey(key);
        #endregion // Indexing by `Index`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await MultiTargetAccessor.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `MultiTargetAccessor.EncryptionKey` and `MultiTargetAccessor.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, MultiTargetAccessor.EncryptionKey, MultiTargetAccessor.MacKey, MultiTargetAccessor.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "Holder.Index", TcbTable.KindScalar, 1, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Holder.Index");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._index = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "Holder.Pick", TcbTable.KindScalar, 1, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Holder.Pick");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._pick = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "Holder.Wide", TcbTable.KindScalar, 1, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Holder.Wide");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._wide = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "Holder.Only", TcbTable.KindScalar, 1, false, TcbTable.ElementI32);
                        cursor = new TcbColumnCursor(reader, column, count, "Holder.Only");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._only_Weapon_index = value;
                                record._only = default(WeaponTable.Record); // will be assigned.
                                record._only_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "Holder.Maybe", TcbTable.KindScalar, 1, true, TcbTable.ElementI32, TcbTable.ElementVarint);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Holder.Maybe");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._maybe = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._maybeHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._maybeHasValue)
                                records[i]._maybe = default(int);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByIndex = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsByIndex.Add(record.Index, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByIndex = recordsByIndex;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Tabbit.Fixtures.MultiTarget
