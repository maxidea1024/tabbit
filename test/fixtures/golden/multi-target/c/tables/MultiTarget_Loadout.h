/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef MULTI_TARGET_LOADOUT_H
#define MULTI_TARGET_LOADOUT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "MultiTarget_Forward.h"
#include "enums/MultiTarget_EnumLoadoutSlotPickTarget.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : B2
 *
 * A record array whose member reaches several tables.
 */
/* One element of MultiTarget_LoadoutRecord_t::slot. */
struct MultiTarget_LoadoutRecord_t_slot_entry {
  /* element 1 - two targets */
  int32_t pick; const void* pick_row; MultiTarget_LoadoutSlotPickTarget_t pick_target;
  /* element 1 - an ordinary member */
  int32_t count;
};

/* The MultiTarget_WeaponRecord_t row pick names, or NULL when it names a row of
 * one of the others. */
const MultiTarget_WeaponRecord_t* MultiTarget_LoadoutSlotWeaponByPick(
  const struct MultiTarget_LoadoutRecord_t_slot_entry* element);

/* The MultiTarget_ArmourRecord_t row pick names, or NULL when it names a row of
 * one of the others. */
const MultiTarget_ArmourRecord_t* MultiTarget_LoadoutSlotArmourByPick(
  const struct MultiTarget_LoadoutRecord_t_slot_entry* element);

struct MultiTarget_LoadoutRecord_t {
  /* primary index */
  int32_t index;
  /* element 1 - two targets */
  struct MultiTarget_LoadoutRecord_t_slot_entry* slot;
  int32_t slot_count;
};

/* Every row of Loadout.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct MultiTarget_LoadoutTable_t {
  MultiTarget_LoadoutRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} MultiTarget_LoadoutTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool MultiTarget_LoadoutLoad(MultiTarget_LoadoutTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void MultiTarget_LoadoutFree(MultiTarget_LoadoutTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const MultiTarget_LoadoutRecord_t* MultiTarget_LoadoutFindByIndex(
  const MultiTarget_LoadoutTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool MultiTarget_LoadoutContainsIndex(
  const MultiTarget_LoadoutTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* MULTI_TARGET_LOADOUT_H */
