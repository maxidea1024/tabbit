/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef MULTI_TARGET_FITTING_H
#define MULTI_TARGET_FITTING_H

#include "tabbit/tabbit_tcb_reader.h"

#include "MultiTarget_Forward.h"
#include "enums/MultiTarget_EnumFittingMainPickTarget.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : J2
 *
 * One record - no element number at all - whose member reaches several.
 */
/* One element of MultiTarget_FittingRecord_t::main. */
struct MultiTarget_FittingRecord_t_main_entry {
  /* the member, in a record of one */
  int32_t pick; const void* pick_row; MultiTarget_FittingMainPickTarget_t pick_target;
  /* an ordinary member beside it */
  int32_t count;
};

/* The MultiTarget_WeaponRecord_t row pick names, or NULL when it names a row of
 * one of the others. */
const MultiTarget_WeaponRecord_t* MultiTarget_FittingMainWeaponByPick(
  const struct MultiTarget_FittingRecord_t_main_entry* element);

/* The MultiTarget_ArmourRecord_t row pick names, or NULL when it names a row of
 * one of the others. */
const MultiTarget_ArmourRecord_t* MultiTarget_FittingMainArmourByPick(
  const struct MultiTarget_FittingRecord_t_main_entry* element);

struct MultiTarget_FittingRecord_t {
  /* primary index */
  int32_t index;
  /* the member, in a record of one */
  struct MultiTarget_FittingRecord_t_main_entry main;
  /* padding, so every table is one width */
  int32_t pad;
  /* padding, so every table is one width */
  int32_t pad2;
};

/* Every row of Fitting.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct MultiTarget_FittingTable_t {
  MultiTarget_FittingRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} MultiTarget_FittingTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool MultiTarget_FittingLoad(MultiTarget_FittingTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void MultiTarget_FittingFree(MultiTarget_FittingTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const MultiTarget_FittingRecord_t* MultiTarget_FittingFindByIndex(
  const MultiTarget_FittingTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool MultiTarget_FittingContainsIndex(
  const MultiTarget_FittingTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* MULTI_TARGET_FITTING_H */
