/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef MULTI_TARGET_RACK_H
#define MULTI_TARGET_RACK_H

#include "tabbit/tabbit_tcb_reader.h"

#include "MultiTarget_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : R2
 *
 * One record whose members are arrays, one of them reaching several.
 */
/* One element of MultiTarget_RackRecord_t::slots. */
struct MultiTarget_RackRecord_t_slots_entry {
  /* element 1 of the member */
  int32_t pick[2];
  /* element 1 beside it */
  int32_t count[2];
};

struct MultiTarget_RackRecord_t {
  /* primary index */
  int32_t index;
  /* element 1 of the member */
  struct MultiTarget_RackRecord_t_slots_entry slots;
};

/* Every row of Rack.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct MultiTarget_RackTable_t {
  MultiTarget_RackRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} MultiTarget_RackTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool MultiTarget_RackLoad(MultiTarget_RackTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void MultiTarget_RackFree(MultiTarget_RackTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const MultiTarget_RackRecord_t* MultiTarget_RackFindByIndex(
  const MultiTarget_RackTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool MultiTarget_RackContainsIndex(
  const MultiTarget_RackTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* MULTI_TARGET_RACK_H */
