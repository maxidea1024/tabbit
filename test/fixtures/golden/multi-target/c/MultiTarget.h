/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef MULTI_TARGET_H
#define MULTI_TARGET_H

#include "enums/MultiTarget_EnumHolderPickTarget.h"
#include "enums/MultiTarget_EnumHolderWideTarget.h"
#include "enums/MultiTarget_EnumHolderMaybeTarget.h"
#include "tables/MultiTarget_Weapon.h"
#include "tables/MultiTarget_Armour.h"
#include "tables/MultiTarget_Trinket.h"
#include "tables/MultiTarget_Mount.h"
#include "tables/MultiTarget_Banner.h"
#include "tables/MultiTarget_Holder.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Every table, loaded together so cross-table references can be resolved. */
typedef struct MultiTarget_t {
  MultiTarget_WeaponTable_t weapon;
  MultiTarget_ArmourTable_t armour;
  MultiTarget_TrinketTable_t trinket;
  MultiTarget_MountTable_t mount;
  MultiTarget_BannerTable_t banner;
  MultiTarget_HolderTable_t holder;
} MultiTarget_t;

/* Reads every table from base_path, then links the references between them.
 *
 * Declare the struct zeroed - `MultiTarget_t data = {0};` - and this is safe to
 * call again on it afterwards. That is what a reload is: every file is read beside what the
 * struct already holds and swapped in at the end, so the pointers a caller is holding stay
 * valid until this returns true, and stay valid for the previous load if it returns false.
 *
 * Returns false if any table could not be read, having released whatever had been loaded up
 * to that point and left `data` alone - a half-loaded model is not one to hand back. */
bool MultiTarget_LoadAll(MultiTarget_t* data, const char* base_path,
                 char* error, size_t error_size);

/* The same, for data files carrying some other extension than the one the recipe told
 * the exporter to write. `file_extension` includes the dot. */
bool MultiTarget_LoadAllWithExtension(MultiTarget_t* data, const char* base_path,
                 const char* file_extension, char* error, size_t error_size);

void MultiTarget_Free(MultiTarget_t* data);

#ifdef __cplusplus
}
#endif

#endif /* MULTI_TARGET_H */
