/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "MultiTarget.h"

#include <stdio.h>
#include <string.h>

/* The definitions for the declarations in the forward header. Beside the accessor's own
 * load because that is where a project answers the questions the generated code cannot. */
const uint8_t* MultiTarget_EncryptionKey = NULL;
int32_t MultiTarget_EncryptionKeyLength = 0;
const uint8_t* MultiTarget_MacKey = NULL;
int32_t MultiTarget_MacKeyLength = 0;
bool MultiTarget_VerifyMac = true;

/* Turns the stored indices into usable pointers, once every table is in memory.
 *
 * A reference into a row that is not there stays NULL rather than inventing one. */
static void MultiTarget_SolveCrossReferences(MultiTarget_t* data) {
  int32_t row;

  for (row = 0; row < data->holder.count; ++row) {
    MultiTarget_HolderRecord_t* record = &data->holder.records[row];

    {
      const MultiTarget_WeaponRecord_t* target = MultiTarget_WeaponFindByIndex(
        &data->weapon, record->only_index);

      if (target != NULL)
        record->only = target;
    }

    if (record->pick != 0) {

      if (record->pick_target == MULTI_TARGET_HOLDER_PICK_TARGET_NONE) {
        const MultiTarget_WeaponRecord_t* found = MultiTarget_WeaponFindByIndex(
          &data->weapon, record->pick);

        if (found != NULL) {
          record->pick_row = found;
          record->pick_target = MULTI_TARGET_HOLDER_PICK_TARGET_WEAPON;
        }
      }

      if (record->pick_target == MULTI_TARGET_HOLDER_PICK_TARGET_NONE) {
        const MultiTarget_ArmourRecord_t* found = MultiTarget_ArmourFindByIndex(
          &data->armour, record->pick);

        if (found != NULL) {
          record->pick_row = found;
          record->pick_target = MULTI_TARGET_HOLDER_PICK_TARGET_ARMOUR;
        }
      }
    }

    if (record->wide != 0) {

      if (record->wide_target == MULTI_TARGET_HOLDER_WIDE_TARGET_NONE) {
        const MultiTarget_WeaponRecord_t* found = MultiTarget_WeaponFindByIndex(
          &data->weapon, record->wide);

        if (found != NULL) {
          record->wide_row = found;
          record->wide_target = MULTI_TARGET_HOLDER_WIDE_TARGET_WEAPON;
        }
      }

      if (record->wide_target == MULTI_TARGET_HOLDER_WIDE_TARGET_NONE) {
        const MultiTarget_ArmourRecord_t* found = MultiTarget_ArmourFindByIndex(
          &data->armour, record->wide);

        if (found != NULL) {
          record->wide_row = found;
          record->wide_target = MULTI_TARGET_HOLDER_WIDE_TARGET_ARMOUR;
        }
      }

      if (record->wide_target == MULTI_TARGET_HOLDER_WIDE_TARGET_NONE) {
        const MultiTarget_TrinketRecord_t* found = MultiTarget_TrinketFindByIndex(
          &data->trinket, record->wide);

        if (found != NULL) {
          record->wide_row = found;
          record->wide_target = MULTI_TARGET_HOLDER_WIDE_TARGET_TRINKET;
        }
      }

      if (record->wide_target == MULTI_TARGET_HOLDER_WIDE_TARGET_NONE) {
        const MultiTarget_MountRecord_t* found = MultiTarget_MountFindByIndex(
          &data->mount, record->wide);

        if (found != NULL) {
          record->wide_row = found;
          record->wide_target = MULTI_TARGET_HOLDER_WIDE_TARGET_MOUNT;
        }
      }

      if (record->wide_target == MULTI_TARGET_HOLDER_WIDE_TARGET_NONE) {
        const MultiTarget_BannerRecord_t* found = MultiTarget_BannerFindByIndex(
          &data->banner, record->wide);

        if (found != NULL) {
          record->wide_row = found;
          record->wide_target = MULTI_TARGET_HOLDER_WIDE_TARGET_BANNER;
        }
      }
    }

    if (record->maybe != 0) {

      if (record->maybe_target == MULTI_TARGET_HOLDER_MAYBE_TARGET_NONE) {
        const MultiTarget_WeaponRecord_t* found = MultiTarget_WeaponFindByIndex(
          &data->weapon, record->maybe);

        if (found != NULL) {
          record->maybe_row = found;
          record->maybe_target = MULTI_TARGET_HOLDER_MAYBE_TARGET_WEAPON;
        }
      }

      if (record->maybe_target == MULTI_TARGET_HOLDER_MAYBE_TARGET_NONE) {
        const MultiTarget_ArmourRecord_t* found = MultiTarget_ArmourFindByIndex(
          &data->armour, record->maybe);

        if (found != NULL) {
          record->maybe_row = found;
          record->maybe_target = MULTI_TARGET_HOLDER_MAYBE_TARGET_ARMOUR;
        }
      }
    }
  }

  for (row = 0; row < data->loadout.count; ++row) {
    MultiTarget_LoadoutRecord_t* record = &data->loadout.records[row];

    {
      int32_t element;

      for (element = 0; element < record->slot_count; ++element) {
        if (record->slot[element].pick != 0) {

          if (record->slot[element].pick_target == MULTI_TARGET_LOADOUT_SLOT_PICK_TARGET_NONE) {
            const MultiTarget_WeaponRecord_t* found = MultiTarget_WeaponFindByIndex(
              &data->weapon, record->slot[element].pick);

            if (found != NULL) {
              record->slot[element].pick_row = found;
              record->slot[element].pick_target = MULTI_TARGET_LOADOUT_SLOT_PICK_TARGET_WEAPON;
            }
          }

          if (record->slot[element].pick_target == MULTI_TARGET_LOADOUT_SLOT_PICK_TARGET_NONE) {
            const MultiTarget_ArmourRecord_t* found = MultiTarget_ArmourFindByIndex(
              &data->armour, record->slot[element].pick);

            if (found != NULL) {
              record->slot[element].pick_row = found;
              record->slot[element].pick_target = MULTI_TARGET_LOADOUT_SLOT_PICK_TARGET_ARMOUR;
            }
          }
        }
      }
    }
  }

  for (row = 0; row < data->fitting.count; ++row) {
    MultiTarget_FittingRecord_t* record = &data->fitting.records[row];

    if (record->main.pick != 0) {

      if (record->main.pick_target == MULTI_TARGET_FITTING_MAIN_PICK_TARGET_NONE) {
        const MultiTarget_WeaponRecord_t* found = MultiTarget_WeaponFindByIndex(
          &data->weapon, record->main.pick);

        if (found != NULL) {
          record->main.pick_row = found;
          record->main.pick_target = MULTI_TARGET_FITTING_MAIN_PICK_TARGET_WEAPON;
        }
      }

      if (record->main.pick_target == MULTI_TARGET_FITTING_MAIN_PICK_TARGET_NONE) {
        const MultiTarget_ArmourRecord_t* found = MultiTarget_ArmourFindByIndex(
          &data->armour, record->main.pick);

        if (found != NULL) {
          record->main.pick_row = found;
          record->main.pick_target = MULTI_TARGET_FITTING_MAIN_PICK_TARGET_ARMOUR;
        }
      }
    }
  }

  for (row = 0; row < data->rack.count; ++row) {
    MultiTarget_RackRecord_t* record = &data->rack.records[row];

    {
      int32_t element;

      for (element = 0; element < record->slots.pick_count; ++element) {
        if (record->slots.pick[element] != 0) {

          if (record->slots.pick_target[element] == MULTI_TARGET_RACK_SLOTS_PICK_TARGET_NONE) {
            const MultiTarget_WeaponRecord_t* found = MultiTarget_WeaponFindByIndex(
              &data->weapon, record->slots.pick[element]);

            if (found != NULL) {
              record->slots.pick_row[element] = found;
              record->slots.pick_target[element] = MULTI_TARGET_RACK_SLOTS_PICK_TARGET_WEAPON;
            }
          }

          if (record->slots.pick_target[element] == MULTI_TARGET_RACK_SLOTS_PICK_TARGET_NONE) {
            const MultiTarget_ArmourRecord_t* found = MultiTarget_ArmourFindByIndex(
              &data->armour, record->slots.pick[element]);

            if (found != NULL) {
              record->slots.pick_row[element] = found;
              record->slots.pick_target[element] = MULTI_TARGET_RACK_SLOTS_PICK_TARGET_ARMOUR;
            }
          }
        }
      }
    }
  }
}

bool MultiTarget_LoadAll(MultiTarget_t* data, const char* base_path,
                 char* error, size_t error_size) {
  return MultiTarget_LoadAllWithExtension(
    data, base_path, ".tcb", error, error_size);
}

bool MultiTarget_LoadAllWithExtension(MultiTarget_t* data, const char* base_path,
                 const char* file_extension, char* error, size_t error_size) {
  char path[1024];

  /* Loaded beside whatever `data` is already holding and swapped in at the end. Reading
   * again is a reload, and one that fails has to leave the caller's pointers valid: they
   * point into the arenas of the previous load, which is why this cannot start by zeroing
   * `data` - doing that dropped those arenas without freeing them. */
  MultiTarget_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Weapon", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    MultiTarget_Free(&loaded);
    return false;
  }

  if (!MultiTarget_WeaponLoad(&loaded.weapon, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    MultiTarget_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Armour", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    MultiTarget_Free(&loaded);
    return false;
  }

  if (!MultiTarget_ArmourLoad(&loaded.armour, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    MultiTarget_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Trinket", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    MultiTarget_Free(&loaded);
    return false;
  }

  if (!MultiTarget_TrinketLoad(&loaded.trinket, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    MultiTarget_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Mount", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    MultiTarget_Free(&loaded);
    return false;
  }

  if (!MultiTarget_MountLoad(&loaded.mount, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    MultiTarget_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Banner", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    MultiTarget_Free(&loaded);
    return false;
  }

  if (!MultiTarget_BannerLoad(&loaded.banner, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    MultiTarget_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Holder", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    MultiTarget_Free(&loaded);
    return false;
  }

  if (!MultiTarget_HolderLoad(&loaded.holder, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    MultiTarget_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Loadout", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    MultiTarget_Free(&loaded);
    return false;
  }

  if (!MultiTarget_LoadoutLoad(&loaded.loadout, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    MultiTarget_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Fitting", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    MultiTarget_Free(&loaded);
    return false;
  }

  if (!MultiTarget_FittingLoad(&loaded.fitting, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    MultiTarget_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Rack", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    MultiTarget_Free(&loaded);
    return false;
  }

  if (!MultiTarget_RackLoad(&loaded.rack, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    MultiTarget_Free(&loaded);
    return false;
  }

  /* Linked among the tables of this load, so no row points into the previous one. */
  MultiTarget_SolveCrossReferences(&loaded);

  /* The previous load goes now, and not before. */
  MultiTarget_Free(data);
  *data = loaded;

  return true;
}

void MultiTarget_Free(MultiTarget_t* data) {
  MultiTarget_WeaponFree(&data->weapon);
  MultiTarget_ArmourFree(&data->armour);
  MultiTarget_TrinketFree(&data->trinket);
  MultiTarget_MountFree(&data->mount);
  MultiTarget_BannerFree(&data->banner);
  MultiTarget_HolderFree(&data->holder);
  MultiTarget_LoadoutFree(&data->loadout);
  MultiTarget_FittingFree(&data->fitting);
  MultiTarget_RackFree(&data->rack);
}
