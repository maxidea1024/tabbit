// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#pragma once

#include "CoreMinimal.h"
#include "UObject/ObjectMacros.h"
#include "Kismet/BlueprintFunctionLibrary.h"

#include "TabbitTcbReader.h"

// Must be the last include. UHT requires it, and moving it produces an error that
// names some other line entirely.
#include "FMultiTargetData.generated.h"


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Holders : C7
/** Which table `Holder.Pick` points at. The column carries one id and the tables it may be a row of take separate id bands, so exactly one of them answers. */
UENUM(BlueprintType)
enum class EHolderPickTarget : uint8
{
    /** No row of any of them. */
    None = 0 UMETA(DisplayName = "None"),
    /** A row of `Weapon`. */
    Weapon = 1 UMETA(DisplayName = "Weapon"),
    /** A row of `Armour`. */
    Armour = 2 UMETA(DisplayName = "Armour"),
};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Holders : D7
/** Which table `Holder.Wide` points at. The column carries one id and the tables it may be a row of take separate id bands, so exactly one of them answers. */
UENUM(BlueprintType)
enum class EHolderWideTarget : uint8
{
    /** No row of any of them. */
    None = 0 UMETA(DisplayName = "None"),
    /** A row of `Weapon`. */
    Weapon = 1 UMETA(DisplayName = "Weapon"),
    /** A row of `Armour`. */
    Armour = 2 UMETA(DisplayName = "Armour"),
    /** A row of `Trinket`. */
    Trinket = 3 UMETA(DisplayName = "Trinket"),
    /** A row of `Mount`. */
    Mount = 4 UMETA(DisplayName = "Mount"),
    /** A row of `Banner`. */
    Banner = 5 UMETA(DisplayName = "Banner"),
};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Holders : F7
/** Which table `Holder.Maybe` points at. The column carries one id and the tables it may be a row of take separate id bands, so exactly one of them answers. */
UENUM(BlueprintType)
enum class EHolderMaybeTarget : uint8
{
    /** No row of any of them. */
    None = 0 UMETA(DisplayName = "None"),
    /** A row of `Weapon`. */
    Weapon = 1 UMETA(DisplayName = "Weapon"),
    /** A row of `Armour`. */
    Armour = 2 UMETA(DisplayName = "Armour"),
};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : C7
/** Which table `Loadout.Slot1Pick` points at. The column carries one id and the tables it may be a row of take separate id bands, so exactly one of them answers. */
UENUM(BlueprintType)
enum class ELoadoutSlotPickTarget : uint8
{
    /** No row of any of them. */
    None = 0 UMETA(DisplayName = "None"),
    /** A row of `Weapon`. */
    Weapon = 1 UMETA(DisplayName = "Weapon"),
    /** A row of `Armour`. */
    Armour = 2 UMETA(DisplayName = "Armour"),
};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : K7
/** Which table `Fitting.MainPick` points at. The column carries one id and the tables it may be a row of take separate id bands, so exactly one of them answers. */
UENUM(BlueprintType)
enum class EFittingMainPickTarget : uint8
{
    /** No row of any of them. */
    None = 0 UMETA(DisplayName = "None"),
    /** A row of `Weapon`. */
    Weapon = 1 UMETA(DisplayName = "Weapon"),
    /** A row of `Armour`. */
    Armour = 2 UMETA(DisplayName = "Armour"),
};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : S7
/** Which table `Rack.SlotsPick1` points at. The column carries one id and the tables it may be a row of take separate id bands, so exactly one of them answers. */
UENUM(BlueprintType)
enum class ERackSlotsPickTarget : uint8
{
    /** No row of any of them. */
    None = 0 UMETA(DisplayName = "None"),
    /** A row of `Weapon`. */
    Weapon = 1 UMETA(DisplayName = "Weapon"),
    /** A row of `Armour`. */
    Armour = 2 UMETA(DisplayName = "Armour"),
};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Catalogues : B2
/** first target of every column below */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FWeaponRow
{
    GENERATED_BODY()

    /** primary index, in this table's own band */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Weapon")
    int32 Index = 0;

    /** so the resolved row has something to read */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Weapon")
    FString Name;

    /** a third column, so every table is one width */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Weapon")
    FString Note;

};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Catalogues : J2
/** second target */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FArmourRow
{
    GENERATED_BODY()

    /** primary index, in this table's own band */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Armour")
    int32 Index = 0;

    /** so the resolved row has something to read */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Armour")
    FString Name;

    /** a third column, so every table is one width */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Armour")
    FString Note;

};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Catalogues : R2
/** only the wide column names this */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FTrinketRow
{
    GENERATED_BODY()

    /** primary index, in this table's own band */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Trinket")
    int32 Index = 0;

    /** so the resolved row has something to read */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Trinket")
    FString Name;

    /** a third column, so every table is one width */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Trinket")
    FString Note;

};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Catalogues : Z2
/** only the wide column names this */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FMountRow
{
    GENERATED_BODY()

    /** primary index, in this table's own band */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Mount")
    int32 Index = 0;

    /** so the resolved row has something to read */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Mount")
    FString Name;

    /** a third column, so every table is one width */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Mount")
    FString Note;

};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Catalogues : AH2
/** the wide column's last target */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FBannerRow
{
    GENERATED_BODY()

    /** primary index, in this table's own band */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Banner")
    int32 Index = 0;

    /** so the resolved row has something to read */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Banner")
    FString Name;

    /** a third column, so every table is one width */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Banner")
    FString Note;

};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Holders : B2
/** One column per case the spec states. */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FHolderRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Holder")
    int32 Index = 0;

    /** two targets - the ordinary case */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Holder")
    int32 Pick = 0;

    /** five targets, and the value picks one */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Holder")
    int32 Wide = 0;

    /** the same notation with one name */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Holder")
    int32 OnlyIndex = 0;

    /** two targets, and `-` is none of them */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Holder")
    int32 Maybe = 0;

    /** Whether this row has a value for Maybe. */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Holder")
    bool bHasMaybe = false;
};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : B2
/** One element of FLoadoutRow::Slot. */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FLoadoutSlotEntry
{
    GENERATED_BODY()

    /** element 1 - two targets */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Loadout")
    int32 Pick = 0;

    /** element 1 - an ordinary member */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Loadout")
    int32 Count = 0;

};

/** A record array whose member reaches several tables. */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FLoadoutRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Loadout")
    int32 Index = 0;

    /** element 1 - two targets */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Loadout")
    TArray<FLoadoutSlotEntry> Slot;

};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : J2
/** One element of FFittingRow::Main. */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FFittingMainEntry
{
    GENERATED_BODY()

    /** the member, in a record of one */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Fitting")
    int32 Pick = 0;

    /** an ordinary member beside it */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Fitting")
    int32 Count = 0;

};

/** One record - no element number at all - whose member reaches several. */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FFittingRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Fitting")
    int32 Index = 0;

    /** the member, in a record of one */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Fitting")
    FFittingMainEntry Main;

    /** padding, so every table is one width */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Fitting")
    int32 Pad = 0;

    /** padding, so every table is one width */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Fitting")
    int32 Pad2 = 0;

};


// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : R2
/** One element of FRackRow::Slots. */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FRackSlotsEntry
{
    GENERATED_BODY()

    /** element 1 of the member */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Rack")
    TArray<int32> Pick;

    /** element 1 beside it */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Rack")
    TArray<int32> Count;

};

/** One record whose members are arrays, one of them reaching several. */
USTRUCT(BlueprintType)
struct MULTITARGETDATA_API FRackRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Rack")
    int32 Index = 0;

    /** element 1 of the member */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Rack")
    FRackSlotsEntry Slots;

};


/** Every row of Weapon. */
class MULTITARGETDATA_API FWeaponTable
{
public:
    const TArray<FWeaponRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FWeaponRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FWeaponRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Armour. */
class MULTITARGETDATA_API FArmourTable
{
public:
    const TArray<FArmourRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FArmourRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FArmourRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Trinket. */
class MULTITARGETDATA_API FTrinketTable
{
public:
    const TArray<FTrinketRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FTrinketRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FTrinketRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Mount. */
class MULTITARGETDATA_API FMountTable
{
public:
    const TArray<FMountRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FMountRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FMountRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Banner. */
class MULTITARGETDATA_API FBannerTable
{
public:
    const TArray<FBannerRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FBannerRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FBannerRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Holder. */
class MULTITARGETDATA_API FHolderTable
{
public:
    const TArray<FHolderRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FHolderRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FHolderRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Loadout. */
class MULTITARGETDATA_API FLoadoutTable
{
public:
    const TArray<FLoadoutRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FLoadoutRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FLoadoutRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Fitting. */
class MULTITARGETDATA_API FFittingTable
{
public:
    const TArray<FFittingRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FFittingRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FFittingRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Rack. */
class MULTITARGETDATA_API FRackTable
{
public:
    const TArray<FRackRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FRackRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FRackRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/**
 * Every table, loaded together.
 *
 * A static class rather than a UObject: the data outlives any world, is read once at
 * startup and never mutated, and nothing here needs reflection or garbage collection.
 */
class MULTITARGETDATA_API FMultiTargetData
{
public:
    static const FWeaponTable& Weapon() { return WeaponStorage; }
    static const FArmourTable& Armour() { return ArmourStorage; }
    static const FTrinketTable& Trinket() { return TrinketStorage; }
    static const FMountTable& Mount() { return MountStorage; }
    static const FBannerTable& Banner() { return BannerStorage; }
    static const FHolderTable& Holder() { return HolderStorage; }
    static const FLoadoutTable& Loadout() { return LoadoutStorage; }
    static const FFittingTable& Fitting() { return FittingStorage; }
    static const FRackTable& Rack() { return RackStorage; }

    /**
     * Reads every table from BasePath. Returns false if any of them could not be read.
     *
     * FileExtension defaults to what the recipe told the exporter to write. Pass a different
     * one when the data files were renamed on the way into the package - which they often are,
     * since some platforms only cook what carries an extension they recognise.
     */
    static bool ReadAll(const FString& BasePath,
                        const FString& FileExtension = TEXT(".tcb"));

    /**
     * The key the table files were sealed with, or empty when they were not sealed.
     *
     * Beside ReadAll because it is the same kind of thing: one decision the project makes
     * once, at start-up, before the first table is read. A key threaded through every Read
     * signature would be the same value written out at every call site, and every call site
     * is somewhere it could be wrong.
     *
     * Thirty-two bytes, the key the converter was given. Empty - the default - is a project
     * that does not encrypt, and it costs that project nothing: the load path opens the
     * envelope either way, and a file that is not encrypted comes back from that unchanged.
     * A project that does encrypt and leaves this empty gets the reader's own message saying
     * the file is encrypted and no key was given, rather than a failure somewhere inside the
     * format.
     *
     * Where the key comes from is the one thing about this layer worth being careful with.
     * It is a secret only until it reaches a repository, a build log or an issue, and it
     * cannot be taken back afterwards - so it is read at start-up from wherever this project
     * keeps secrets, not written into a source file or a .ini the packaging step will cook.
     * What the layer is for, and therefore what is and is not worth defending, is
     * spec/tcb-v104-composed-encodings.md section 4.
     */
    static TArray<uint8> EncryptionKey;

    /**
     * The key the table files were signed with, or empty when they were not signed.
     *
     * A different key from EncryptionKey and independent of it: a file can be signed without
     * being encrypted, and encrypted without being signed.
     *
     * What it buys is the thing encryption does not. The cipher is a keystream XOR, so a bit
     * flipped in the file is a bit flipped in the value it decrypts to - no key needed - and
     * every structural check the format makes still passes, because a four-byte float
     * accepts any four bytes. With this set, a file altered after export stops loading.
     *
     * Setting it makes the reader stricter in a second way: a file that carries no MAC is
     * refused, because otherwise the check could be removed by zeroing sixteen bytes. So the
     * order to turn this on in is data first, key second. spec/tcb-mac-and-signature.md.
     */
    static TArray<uint8> MacKey;

    /**
     * Whether to check the MAC at all. True by default.
     *
     * For tools that read exported files without the key, and for measuring what the check
     * costs at load. It is not a security boundary - anyone who can flip a flag in a shipped
     * build can read MacKey out of the same build.
     */
    static bool bVerifyMac;

private:
    static FWeaponTable WeaponStorage;
    static FArmourTable ArmourStorage;
    static FTrinketTable TrinketStorage;
    static FMountTable MountStorage;
    static FBannerTable BannerStorage;
    static FHolderTable HolderStorage;
    static FLoadoutTable LoadoutStorage;
    static FFittingTable FittingStorage;
    static FRackTable RackStorage;
};

/**
 * The same data, reachable from Blueprint.
 *
 * Every row above is a USTRUCT marked BlueprintType with BlueprintReadOnly properties,
 * which says the rows are meant to be used from Blueprint - and until this existed there
 * was no way to get one. A graph could declare a variable of a row type and had nothing to
 * obtain a value from, because the accessor is a plain C++ class and a static method on one
 * is not something Blueprint can call.
 *
 * A function library rather than making the accessor a UObject. The storage stays where it
 * is: this owns nothing, adds no garbage collection, and does not need a world. It is a
 * few UFUNCTIONs over the C++ that was already there.
 */
UCLASS()
class MULTITARGETDATA_API UMultiTargetDataLibrary : public UBlueprintFunctionLibrary
{
    GENERATED_BODY()

public:

    /**
     * The Weapon row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Weapon",
              meta = (DisplayName = "Get Weapon Row"))
    static FWeaponRow GetWeaponRow(int32 Key, bool& bFound);

    /** How many Weapon rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Weapon",
              meta = (DisplayName = "Get Weapon Row Count"))
    static int32 GetWeaponRowCount();

    /**
     * The Weapon row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Weapon",
              meta = (DisplayName = "Get Weapon Row At"))
    static FWeaponRow GetWeaponRowAt(int32 Position, bool& bFound);

    /**
     * The Armour row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Armour",
              meta = (DisplayName = "Get Armour Row"))
    static FArmourRow GetArmourRow(int32 Key, bool& bFound);

    /** How many Armour rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Armour",
              meta = (DisplayName = "Get Armour Row Count"))
    static int32 GetArmourRowCount();

    /**
     * The Armour row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Armour",
              meta = (DisplayName = "Get Armour Row At"))
    static FArmourRow GetArmourRowAt(int32 Position, bool& bFound);

    /**
     * The Trinket row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Trinket",
              meta = (DisplayName = "Get Trinket Row"))
    static FTrinketRow GetTrinketRow(int32 Key, bool& bFound);

    /** How many Trinket rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Trinket",
              meta = (DisplayName = "Get Trinket Row Count"))
    static int32 GetTrinketRowCount();

    /**
     * The Trinket row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Trinket",
              meta = (DisplayName = "Get Trinket Row At"))
    static FTrinketRow GetTrinketRowAt(int32 Position, bool& bFound);

    /**
     * The Mount row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Mount",
              meta = (DisplayName = "Get Mount Row"))
    static FMountRow GetMountRow(int32 Key, bool& bFound);

    /** How many Mount rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Mount",
              meta = (DisplayName = "Get Mount Row Count"))
    static int32 GetMountRowCount();

    /**
     * The Mount row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Mount",
              meta = (DisplayName = "Get Mount Row At"))
    static FMountRow GetMountRowAt(int32 Position, bool& bFound);

    /**
     * The Banner row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Banner",
              meta = (DisplayName = "Get Banner Row"))
    static FBannerRow GetBannerRow(int32 Key, bool& bFound);

    /** How many Banner rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Banner",
              meta = (DisplayName = "Get Banner Row Count"))
    static int32 GetBannerRowCount();

    /**
     * The Banner row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Banner",
              meta = (DisplayName = "Get Banner Row At"))
    static FBannerRow GetBannerRowAt(int32 Position, bool& bFound);

    /**
     * The Holder row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Holder",
              meta = (DisplayName = "Get Holder Row"))
    static FHolderRow GetHolderRow(int32 Key, bool& bFound);

    /** How many Holder rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Holder",
              meta = (DisplayName = "Get Holder Row Count"))
    static int32 GetHolderRowCount();

    /**
     * The Holder row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Holder",
              meta = (DisplayName = "Get Holder Row At"))
    static FHolderRow GetHolderRowAt(int32 Position, bool& bFound);

    /**
     * The Loadout row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Loadout",
              meta = (DisplayName = "Get Loadout Row"))
    static FLoadoutRow GetLoadoutRow(int32 Key, bool& bFound);

    /** How many Loadout rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Loadout",
              meta = (DisplayName = "Get Loadout Row Count"))
    static int32 GetLoadoutRowCount();

    /**
     * The Loadout row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Loadout",
              meta = (DisplayName = "Get Loadout Row At"))
    static FLoadoutRow GetLoadoutRowAt(int32 Position, bool& bFound);

    /**
     * The Fitting row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Fitting",
              meta = (DisplayName = "Get Fitting Row"))
    static FFittingRow GetFittingRow(int32 Key, bool& bFound);

    /** How many Fitting rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Fitting",
              meta = (DisplayName = "Get Fitting Row Count"))
    static int32 GetFittingRowCount();

    /**
     * The Fitting row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Fitting",
              meta = (DisplayName = "Get Fitting Row At"))
    static FFittingRow GetFittingRowAt(int32 Position, bool& bFound);

    /**
     * The Rack row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Rack",
              meta = (DisplayName = "Get Rack Row"))
    static FRackRow GetRackRow(int32 Key, bool& bFound);

    /** How many Rack rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Rack",
              meta = (DisplayName = "Get Rack Row Count"))
    static int32 GetRackRowCount();

    /**
     * The Rack row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Rack",
              meta = (DisplayName = "Get Rack Row At"))
    static FRackRow GetRackRowAt(int32 Position, bool& bFound);

    /**
     * Reads every table from BasePath, as FMultiTargetData::ReadAll does.
     *
     * BlueprintCallable rather than BlueprintPure: it does something.
     */
    UFUNCTION(BlueprintCallable, Category = "Tabbit",
              meta = (DisplayName = "Load All Tabbit Tables"))
    static bool ReadAll(const FString& BasePath,
                        const FString& FileExtension = TEXT(".tcb"));
};
