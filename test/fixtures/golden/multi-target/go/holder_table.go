// ------------------------------------------------------------------------------
// Code generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package multitarget

import (
	"fmt"

	"multitarget/tabbit"
)

// HolderRecord was generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Holders : B2.
// One column per case the spec states.
type HolderRecord struct {
	// primary index
	Index int32
	// two targets - the ordinary case
	Pick int32
	// five targets, and the value picks one
	Wide int32
	// the same notation with one name
	Only *WeaponRecord
	OnlyIndex int32
	// two targets, and `-` is none of them
	Maybe int32
	// Whether this row has a value for Maybe. The value member keeps its type
	// and holds the type's empty value when the row had none; this says which it was.
	HasMaybe bool
	// The row Pick names, as whichever of its target tables holds it. Read it
	// through the methods below rather than directly: they check the discriminator first.
	PickRow any
	// Which table Pick is a row of.
	PickTarget HolderPickTarget
	// The row Wide names, as whichever of its target tables holds it. Read it
	// through the methods below rather than directly: they check the discriminator first.
	WideRow any
	// Which table Wide is a row of.
	WideTarget HolderWideTarget
	// The row Maybe names, as whichever of its target tables holds it. Read it
	// through the methods below rather than directly: they check the discriminator first.
	MaybeRow any
	// Which table Maybe is a row of.
	MaybeTarget HolderMaybeTarget
}


// WeaponByPick returns the Weapon row Pick names, or nil when it
// names a row of one of the others.
func (r *HolderRecord) WeaponByPick() *WeaponRecord {
	if r.PickTarget != HolderPickTargetWeapon {
		return nil
	}

	return r.PickRow.(*WeaponRecord)
}

// ArmourByPick returns the Armour row Pick names, or nil when it
// names a row of one of the others.
func (r *HolderRecord) ArmourByPick() *ArmourRecord {
	if r.PickTarget != HolderPickTargetArmour {
		return nil
	}

	return r.PickRow.(*ArmourRecord)
}


// WeaponByWide returns the Weapon row Wide names, or nil when it
// names a row of one of the others.
func (r *HolderRecord) WeaponByWide() *WeaponRecord {
	if r.WideTarget != HolderWideTargetWeapon {
		return nil
	}

	return r.WideRow.(*WeaponRecord)
}

// ArmourByWide returns the Armour row Wide names, or nil when it
// names a row of one of the others.
func (r *HolderRecord) ArmourByWide() *ArmourRecord {
	if r.WideTarget != HolderWideTargetArmour {
		return nil
	}

	return r.WideRow.(*ArmourRecord)
}

// TrinketByWide returns the Trinket row Wide names, or nil when it
// names a row of one of the others.
func (r *HolderRecord) TrinketByWide() *TrinketRecord {
	if r.WideTarget != HolderWideTargetTrinket {
		return nil
	}

	return r.WideRow.(*TrinketRecord)
}

// MountByWide returns the Mount row Wide names, or nil when it
// names a row of one of the others.
func (r *HolderRecord) MountByWide() *MountRecord {
	if r.WideTarget != HolderWideTargetMount {
		return nil
	}

	return r.WideRow.(*MountRecord)
}

// BannerByWide returns the Banner row Wide names, or nil when it
// names a row of one of the others.
func (r *HolderRecord) BannerByWide() *BannerRecord {
	if r.WideTarget != HolderWideTargetBanner {
		return nil
	}

	return r.WideRow.(*BannerRecord)
}


// WeaponByMaybe returns the Weapon row Maybe names, or nil when it
// names a row of one of the others.
func (r *HolderRecord) WeaponByMaybe() *WeaponRecord {
	if r.MaybeTarget != HolderMaybeTargetWeapon {
		return nil
	}

	return r.MaybeRow.(*WeaponRecord)
}

// ArmourByMaybe returns the Armour row Maybe names, or nil when it
// names a row of one of the others.
func (r *HolderRecord) ArmourByMaybe() *ArmourRecord {
	if r.MaybeTarget != HolderMaybeTargetArmour {
		return nil
	}

	return r.MaybeRow.(*ArmourRecord)
}

// HolderTable holds every row of Holder.
type HolderTable struct {
	records []HolderRecord
	byIndex map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *HolderTable) Records() []HolderRecord { return t.records }

// FindByIndex returns the row with this Index, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *HolderTable) FindByIndex(key int32) *HolderRecord {
	position, found := t.byIndex[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIndexOrError returns the row with this Index, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *HolderTable) GetByIndexOrError(key int32) (*HolderRecord, error) {
	position, found := t.byIndex[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Holder` that corresponds to field `Index` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsIndex reports whether the table holds a row with this Index.
func (t *HolderTable) ContainsIndex(key int32) bool {
	_, found := t.byIndex[key]
	return found
}

// Read loads the table from a .tcb file written by Tabbit.
func (t *HolderTable) Read(filename string) error {
	data, err := tabbit.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	// Unconditionally, and with whatever EncryptionKey and MacKey hold - which is nil
	// unless the project set them. A file that is neither encrypted nor signed comes back
	// from this untouched, so the load path is the same either way and there is no
	// condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
	//
	// What comes back is a window onto data rather than a copy of it, which is why data
	// stays in scope for the whole read.
	opened, err := tabbit.Open(data, EncryptionKey, MacKey, VerifyMac)
	if err != nil {
		return err
	}

	reader := tabbit.NewReader(opened)
	count, columns := tabbit.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]HolderRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if tabbit.CheckColumn(reader, column, "Holder.Index", tabbit.KindScalar, 1, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Holder.Index")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Index = value
						i++
					}
				}
			}
		case 2:
			if tabbit.CheckColumn(reader, column, "Holder.Pick", tabbit.KindScalar, 1, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Holder.Pick")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Pick = value
						i++
					}
				}
			}
		case 3:
			if tabbit.CheckColumn(reader, column, "Holder.Wide", tabbit.KindScalar, 1, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Holder.Wide")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Wide = value
						i++
					}
				}
			}
		case 4:
			if tabbit.CheckColumn(reader, column, "Holder.Only", tabbit.KindScalar, 1, false, tabbit.ElementI32) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Holder.Only")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].OnlyIndex = value
						i++
					}
				}
			}
		case 5:
			if tabbit.CheckColumn(reader, column, "Holder.Maybe", tabbit.KindScalar, 1, true, tabbit.ElementI32, tabbit.ElementVarint) {
				// The bitmap is at the front of the block, so it is read before the values.
				// The values are written for every row either way, which is what lets the
				// read shapes below stay as they are.
				presence := tabbit.ReadPresence(reader, column, count)
				cursor := tabbit.NewColumnCursor(reader, column, count, "Holder.Maybe")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Maybe = value
						i++
					}
				}

				// A pass of its own rather than a line inside each read shape, so the row
				// index is worked out once. The block carries a value for every row, so an
				// absent one has just been given whatever was there; putting the empty
				// value back is what makes this path agree with the JSON one.
				for i := int32(0); i < count; i++ {
					records[i].HasMaybe = tabbit.IsPresent(presence, i)

					if !records[i].HasMaybe {
						records[i].Maybe = 0
					}
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		tabbit.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Holder: %w", err)
	}

	byIndex := make(map[int32]int, len(records))
	for i := range records {
		byIndex[records[i].Index] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byIndex = byIndex

	return nil
}
