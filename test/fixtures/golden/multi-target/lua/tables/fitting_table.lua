-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")
local FittingMainPickTarget = require(_root .. "enums.enum_fitting_main_pick_target")

---@class FittingMainEntry
---@field pick integer
---@field pickRow any
---@field pickTarget integer
---@field count integer
local FittingMainEntry = {}

-- The WeaponRecord row `pick` names, or nil when it names a row of
-- one of the others.
---@return WeaponRecord|nil
function FittingMainEntry:weaponByPick()
  if self.pickTarget ~= FittingMainPickTarget.weapon then
    return nil
  end

  return self.pickRow
end

-- The ArmourRecord row `pick` names, or nil when it names a row of
-- one of the others.
---@return ArmourRecord|nil
function FittingMainEntry:armourByPick()
  if self.pickTarget ~= FittingMainPickTarget.armour then
    return nil
  end

  return self.pickRow
end

local FittingMainEntryMeta = tcb.strictInstance("an element of FittingRecord.main", FittingMainEntry, { "pick", "pickRow", "pickTarget", "count" })

---@return FittingMainEntry
local function newFittingMainEntry()
  return setmetatable({
    pick = 0,
    pickRow = nil,
    pickTarget = FittingMainPickTarget.none,
    count = 0,
  }, FittingMainEntryMeta)
end

-- Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Groups : J2.
-- One record - no element number at all - whose member reaches several.
---@class FittingRecord
---@field index integer
---@field main FittingMainEntry
---@field pad integer
---@field pad2 integer
local FittingRecordMeta = tcb.strictType("a `Fitting` row", { "index", "main", "pad", "pad2" })

---@return FittingRecord
local function newFittingRecord()
  return setmetatable({
    index = 0,
    main = newFittingMainEntry(),
    pad = 0,
    pad2 = 0,
  }, FittingRecordMeta)
end

---@class FittingTable
---@field records FittingRecord[]
local FittingTable = {}

local FittingTableMeta = tcb.strictInstance("the `Fitting` table", FittingTable, { "records", "byIndex" })

function FittingTable.new()
  return setmetatable({
    records = {},
    byIndex = {},
  }, FittingTableMeta)
end

-- The row with this Index, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function FittingTable:findByIndex(key)
  return self.byIndex[key]
end

-- The row with this Index, or a raised error naming what was missing -
-- for a key that has to be there.
function FittingTable:getByIndexOrThrow(key)
  local record = self:findByIndex(key)

  if record == nil then
    tcb.fail("there is no record in table `Fitting` that corresponds to " ..
      "field `Index` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this Index.
function FittingTable:containsIndex(key)
  return self:findByIndex(key) ~= nil
end

-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function FittingTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newFittingRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "Fitting.Index", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Fitting.Index")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].index = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "Fitting.Main.Pick", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Fitting.Main.Pick")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].main.pick = value
        end

        at = at + n
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "Fitting.Main.Count", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Fitting.Main.Count")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].main.count = value
        end

        at = at + n
      end
    elseif column.tag == 4 then
      tcb.checkColumn(column, "Fitting.Pad", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Fitting.Pad")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].pad = value
        end

        at = at + n
      end
    elseif column.tag == 5 then
      tcb.checkColumn(column, "Fitting.Pad2", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Fitting.Pad2")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].pad2 = value
        end

        at = at + n
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byIndex = {}

  for i = 1, #records do
    byIndex[records[i].index] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byIndex = byIndex
end

-- The same load from a file on disk.
function FittingTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return FittingTable
