// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { Tables } from '../tables'

// Automatically import to handle external type references.
import { WeaponRecord } from './weapon'
import { HolderPickTarget } from '../enums/holder-pick-target'
import { ArmourRecord } from './armour'
import { HolderWideTarget } from '../enums/holder-wide-target'
import { TrinketRecord } from './trinket'
import { MountRecord } from './mount'
import { BannerRecord } from './banner'
import { HolderMaybeTarget } from '../enums/holder-maybe-target'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  index: number
  pick: number
  wide: number
  only: number
  maybe: number
}

// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Holders : B2
/** One column per case the spec states. */
export class HolderRecord {
  /** Default constructor */
  constructor() {
  }

  /** primary index */
  public get index(): number { return this._index }

  /** two targets - the ordinary case */
  public get pick(): number { return this._pick }

  /** five targets, and the value picks one */
  public get wide(): number { return this._wide }

  /** the same notation with one name */
  public get only(): WeaponRecord { return this._only }

  /** two targets, and `-` is none of them */
  public get maybe(): number { return this._maybe }
  /** Whether this row has a value for `maybe`. */
  public get hasMaybe(): boolean { return this._maybeHasValue }

  /** Which table `pick` is a row of. */
  public get pickTarget(): HolderPickTarget { return this._pickTarget }
  /** The `weapon` row `pick` names, or undefined when it names a row of one of the others. */
  public get pickAsWeapon(): WeaponRecord | undefined {
    return this._pickTarget === HolderPickTarget.Weapon
      ? this._pickRow as WeaponRecord
      : undefined
  }
  /** The `armour` row `pick` names, or undefined when it names a row of one of the others. */
  public get pickAsArmour(): ArmourRecord | undefined {
    return this._pickTarget === HolderPickTarget.Armour
      ? this._pickRow as ArmourRecord
      : undefined
  }

  /** Which table `wide` is a row of. */
  public get wideTarget(): HolderWideTarget { return this._wideTarget }
  /** The `weapon` row `wide` names, or undefined when it names a row of one of the others. */
  public get wideAsWeapon(): WeaponRecord | undefined {
    return this._wideTarget === HolderWideTarget.Weapon
      ? this._wideRow as WeaponRecord
      : undefined
  }
  /** The `armour` row `wide` names, or undefined when it names a row of one of the others. */
  public get wideAsArmour(): ArmourRecord | undefined {
    return this._wideTarget === HolderWideTarget.Armour
      ? this._wideRow as ArmourRecord
      : undefined
  }
  /** The `trinket` row `wide` names, or undefined when it names a row of one of the others. */
  public get wideAsTrinket(): TrinketRecord | undefined {
    return this._wideTarget === HolderWideTarget.Trinket
      ? this._wideRow as TrinketRecord
      : undefined
  }
  /** The `mount` row `wide` names, or undefined when it names a row of one of the others. */
  public get wideAsMount(): MountRecord | undefined {
    return this._wideTarget === HolderWideTarget.Mount
      ? this._wideRow as MountRecord
      : undefined
  }
  /** The `banner` row `wide` names, or undefined when it names a row of one of the others. */
  public get wideAsBanner(): BannerRecord | undefined {
    return this._wideTarget === HolderWideTarget.Banner
      ? this._wideRow as BannerRecord
      : undefined
  }

  /** Which table `maybe` is a row of. */
  public get maybeTarget(): HolderMaybeTarget { return this._maybeTarget }
  /** The `weapon` row `maybe` names, or undefined when it names a row of one of the others. */
  public get maybeAsWeapon(): WeaponRecord | undefined {
    return this._maybeTarget === HolderMaybeTarget.Weapon
      ? this._maybeRow as WeaponRecord
      : undefined
  }
  /** The `armour` row `maybe` names, or undefined when it names a row of one of the others. */
  public get maybeAsArmour(): ArmourRecord | undefined {
    return this._maybeTarget === HolderMaybeTarget.Armour
      ? this._maybeRow as ArmourRecord
      : undefined
  }

  public setReference_pick_INTERNAL(target: HolderPickTarget, row: WeaponRecord | ArmourRecord | undefined): void {
    this._pickTarget = target
    this._pickRow = row
  }

  public setReference_wide_INTERNAL(target: HolderWideTarget, row: WeaponRecord | ArmourRecord | TrinketRecord | MountRecord | BannerRecord | undefined): void {
    this._wideTarget = target
    this._wideRow = row
  }

  public setReference_maybe_INTERNAL(target: HolderMaybeTarget, row: WeaponRecord | ArmourRecord | undefined): void {
    this._maybeTarget = target
    this._maybeRow = row
  }

  public setReference_only_INTERNAL(value: WeaponRecord) { this._only = value; }

  public _index: number = 0
  public _pick: number = 0
  public _wide: number = 0
  public _only: WeaponRecord
  public _only_Weapon_index: number = 0
  public _only_F: boolean = false
  public _maybe: number = 0
  public _maybeHasValue: boolean = false
  public _pickRow: WeaponRecord | ArmourRecord | undefined = undefined
  public _pickTarget: HolderPickTarget = HolderPickTarget.None
  public _wideRow: WeaponRecord | ArmourRecord | TrinketRecord | MountRecord | BannerRecord | undefined = undefined
  public _wideTarget: HolderWideTarget = HolderWideTarget.None
  public _maybeRow: WeaponRecord | ArmourRecord | undefined = undefined
  public _maybeTarget: HolderMaybeTarget = HolderMaybeTarget.None

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._index = dataRow.index
    this._pick = dataRow.pick
    this._wide = dataRow.wide
    this._only_Weapon_index = dataRow.only
    this._maybeHasValue = dataRow.maybe !== null && dataRow.maybe !== undefined; if (this._maybeHasValue) this._maybe = dataRow.maybe
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._index = dataRow[offset++]
    this._pick = dataRow[offset++]
    this._wide = dataRow[offset++]
    this._only_Weapon_index = dataRow[offset++]
    const _maybe_raw = dataRow[offset++]
    this._maybeHasValue = _maybe_raw !== null && _maybe_raw !== undefined
    if (this._maybeHasValue) this._maybe = _maybe_raw
  }
}

// Generated from test/fixtures/xlsx/multi-target/multi-target.xlsx : Holders : B2
/** One column per case the spec states. */
export class HolderTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): HolderRecord[] { return this._records }
  private _records: HolderRecord[] = []

  // Indexing by 'index'
  public get recordsByIndex(): Map<number, HolderRecord> { return this._recordsByIndex }
  private _recordsByIndex: Map<number, HolderRecord> = new Map<number, HolderRecord>()

  /**
   * The row with this index, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByIndex(key: number): HolderRecord | undefined {
    return this._recordsByIndex.get(key)
  }

  /**
   * The row with this index, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByIndex(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIndexOrThrow(key: number): HolderRecord {
    const found = this._recordsByIndex.get(key)
    if (!found)
      throw new Error(`There is no record in table "Holder" that corresponds to field "index" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this index. */
  public containsIndex(key: number): boolean {
    return this._recordsByIndex.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: HolderRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new HolderRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new HolderRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `Tables.encryptionKey` and `Tables.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      Tables.encryptionKey, Tables.macKey, Tables.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor
    let presence: Uint8Array | null

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: HolderRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new HolderRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'Holder.Index', tabbit.KIND_SCALAR, 1, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Holder.Index')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._index = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'Holder.Pick', tabbit.KIND_SCALAR, 1, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Holder.Pick')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._pick = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'Holder.Wide', tabbit.KIND_SCALAR, 1, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Holder.Wide')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._wide = value
          }
          break
        case 4:
          tabbit.checkColumn(column, 'Holder.Only', tabbit.KIND_SCALAR, 1, false, [tabbit.ELEMENT_I32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Holder.Only')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._only_Weapon_index = value
          }
          break
        case 5:
          tabbit.checkColumn(column, 'Holder.Maybe', tabbit.KIND_SCALAR, 1, true, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Holder.Maybe')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._maybe = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._maybeHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._maybeHasValue)
              records[i]._maybe = 0
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: HolderRecord[]): void {
    const recordsByIndex = new Map<number, HolderRecord>()

    for (const record of records) {
      recordsByIndex.set(record.index, record)
    }

    this._records = records
    this._recordsByIndex = recordsByIndex
  }
}
