// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as path from 'path'

import { WeaponTable } from './tables/weapon'
import { ArmourTable } from './tables/armour'
import { TrinketTable } from './tables/trinket'
import { MountTable } from './tables/mount'
import { BannerTable } from './tables/banner'
import { HolderTable } from './tables/holder'
import { LoadoutTable } from './tables/loadout'
import { FittingTable } from './tables/fitting'
import { RackTable } from './tables/rack'
import { HolderPickTarget } from './enums/holder-pick-target'
import { HolderWideTarget } from './enums/holder-wide-target'
import { HolderMaybeTarget } from './enums/holder-maybe-target'
import { LoadoutSlotPickTarget } from './enums/loadout-slot-pick-target'
import { FittingMainPickTarget } from './enums/fitting-main-pick-target'
import { RackSlotsPickTarget } from './enums/rack-slots-pick-target'

/** Tables */
export class Tables {
  /**
   * The key the table files were sealed with, or null when they were not sealed.
   *
   * Beside the load calls because it is the same kind of thing: one decision the consuming
   * project makes once, at start-up, before the first table is read. A key threaded through
   * every read signature would be the same value written out at every call site, and every
   * call site is somewhere it could be wrong.
   *
   * Thirty-two bytes, the key the converter was given. Null - the default - is a project
   * that does not encrypt, and it costs that project nothing: the load path opens the
   * envelope either way, and a file that is not encrypted comes back from that unchanged. A
   * project that does encrypt and leaves this null gets the reader's own message saying the
   * file is encrypted and no key was given, rather than a failure somewhere inside the
   * format.
   *
   * Where the key comes from is the one thing about this layer worth being careful with. It
   * is a secret only until it reaches a repository, a build log or an issue, and it cannot
   * be taken back afterwards - so it is read at start-up from wherever this project keeps
   * secrets, not written into a source file and not bundled into anything a browser can
   * fetch. What the layer is for, and therefore what is and is not worth defending, is
   * spec/tcb-v104-composed-encodings.md section 4.
   */
  public static encryptionKey: Uint8Array | null = null

  /**
   * The key the table files were signed with, or null when they were not signed.
   *
   * A different key from `encryptionKey` and independent of it: a file can be signed without
   * being encrypted, and encrypted without being signed.
   *
   * What it buys is the thing encryption does not. The cipher is a keystream XOR, so a bit
   * flipped in the file is a bit flipped in the value it decrypts to - no key needed - and
   * every structural check the format makes still passes, because a four-byte float accepts
   * any four bytes. With this set, a file altered after export stops loading instead.
   *
   * Setting it makes the reader stricter in a second way: a file that carries no MAC is
   * refused, because otherwise the check could be removed by zeroing sixteen bytes. So the
   * order to turn this on in is data first, key second.
   * spec/tcb-mac-and-signature.md.
   */
  public static macKey: Uint8Array | null = null

  /**
   * Whether to check the MAC at all. True by default.
   *
   * For tools that read exported files without the key, and for measuring what the check
   * costs at load. It is not a security boundary - anyone who can flip a flag in a shipped
   * bundle can read `macKey` out of the same bundle.
   */
  public static verifyMac = true

  /** Peroperty for table Weapon */
  public get weapon(): WeaponTable { return this._weapon }
  private _weapon: WeaponTable = new WeaponTable()

  /** Peroperty for table Armour */
  public get armour(): ArmourTable { return this._armour }
  private _armour: ArmourTable = new ArmourTable()

  /** Peroperty for table Trinket */
  public get trinket(): TrinketTable { return this._trinket }
  private _trinket: TrinketTable = new TrinketTable()

  /** Peroperty for table Mount */
  public get mount(): MountTable { return this._mount }
  private _mount: MountTable = new MountTable()

  /** Peroperty for table Banner */
  public get banner(): BannerTable { return this._banner }
  private _banner: BannerTable = new BannerTable()

  /** Peroperty for table Holder */
  public get holder(): HolderTable { return this._holder }
  private _holder: HolderTable = new HolderTable()

  /** Peroperty for table Loadout */
  public get loadout(): LoadoutTable { return this._loadout }
  private _loadout: LoadoutTable = new LoadoutTable()

  /** Peroperty for table Fitting */
  public get fitting(): FittingTable { return this._fitting }
  private _fitting: FittingTable = new FittingTable()

  /** Peroperty for table Rack */
  public get rack(): RackTable { return this._rack }
  private _rack: RackTable = new RackTable()

  /**
   * Read all tables asynchronously.
   *
   * `fileExtension` defaults to what the JSON exporter writes. Pass a different one when the
   * data files were renamed after export.
   */
  public async readAll(basePath: string, fileExtension: string = '.json'): Promise<void> {
    const weapon = new WeaponTable()
    await weapon.read(path.join(basePath, `Weapon${fileExtension}`))
    const armour = new ArmourTable()
    await armour.read(path.join(basePath, `Armour${fileExtension}`))
    const trinket = new TrinketTable()
    await trinket.read(path.join(basePath, `Trinket${fileExtension}`))
    const mount = new MountTable()
    await mount.read(path.join(basePath, `Mount${fileExtension}`))
    const banner = new BannerTable()
    await banner.read(path.join(basePath, `Banner${fileExtension}`))
    const holder = new HolderTable()
    await holder.read(path.join(basePath, `Holder${fileExtension}`))
    const loadout = new LoadoutTable()
    await loadout.read(path.join(basePath, `Loadout${fileExtension}`))
    const fitting = new FittingTable()
    await fitting.read(path.join(basePath, `Fitting${fileExtension}`))
    const rack = new RackTable()
    await rack.read(path.join(basePath, `Rack${fileExtension}`))

    this.publish(weapon, armour, trinket, mount, banner, holder, loadout, fitting, rack)
  }

  /** Read all tables synchronously. */
  public readAllSync(basePath: string, fileExtension: string = '.json'): void {
    const weapon = new WeaponTable()
    weapon.readSync(path.join(basePath, `Weapon${fileExtension}`))
    const armour = new ArmourTable()
    armour.readSync(path.join(basePath, `Armour${fileExtension}`))
    const trinket = new TrinketTable()
    trinket.readSync(path.join(basePath, `Trinket${fileExtension}`))
    const mount = new MountTable()
    mount.readSync(path.join(basePath, `Mount${fileExtension}`))
    const banner = new BannerTable()
    banner.readSync(path.join(basePath, `Banner${fileExtension}`))
    const holder = new HolderTable()
    holder.readSync(path.join(basePath, `Holder${fileExtension}`))
    const loadout = new LoadoutTable()
    loadout.readSync(path.join(basePath, `Loadout${fileExtension}`))
    const fitting = new FittingTable()
    fitting.readSync(path.join(basePath, `Fitting${fileExtension}`))
    const rack = new RackTable()
    rack.readSync(path.join(basePath, `Rack${fileExtension}`))

    this.publish(weapon, armour, trinket, mount, banner, holder, loadout, fitting, rack)
  }

  /**
   * Read all tables from the binary export.
   *
   * The counterpart of `readAllSync`, and the one to use when the data is binary: reading a
   * table on its own with `readBinarySync` leaves its references unlinked, because linking
   * needs every table. Both formats produce the same values.
   */
  public readAllBinarySync(basePath: string, fileExtension: string = '.tcb'): void {
    const weapon = new WeaponTable()
    weapon.readBinarySync(path.join(basePath, `Weapon${fileExtension}`))
    const armour = new ArmourTable()
    armour.readBinarySync(path.join(basePath, `Armour${fileExtension}`))
    const trinket = new TrinketTable()
    trinket.readBinarySync(path.join(basePath, `Trinket${fileExtension}`))
    const mount = new MountTable()
    mount.readBinarySync(path.join(basePath, `Mount${fileExtension}`))
    const banner = new BannerTable()
    banner.readBinarySync(path.join(basePath, `Banner${fileExtension}`))
    const holder = new HolderTable()
    holder.readBinarySync(path.join(basePath, `Holder${fileExtension}`))
    const loadout = new LoadoutTable()
    loadout.readBinarySync(path.join(basePath, `Loadout${fileExtension}`))
    const fitting = new FittingTable()
    fitting.readBinarySync(path.join(basePath, `Fitting${fileExtension}`))
    const rack = new RackTable()
    rack.readBinarySync(path.join(basePath, `Rack${fileExtension}`))

    this.publish(weapon, armour, trinket, mount, banner, holder, loadout, fitting, rack)
  }

  /**
   * Publishes one whole load.
   *
   * Reading again - a refresh, a downloaded patch - loads into tables of its own and gets
   * here only once every file has been read. A failure anywhere leaves every table holding
   * what it held, which is the answer a running program wants: the data it already had, and
   * an exception saying why the new data was not taken.
   */
  private publish(weapon: WeaponTable, armour: ArmourTable, trinket: TrinketTable, mount: MountTable, banner: BannerTable, holder: HolderTable, loadout: LoadoutTable, fitting: FittingTable, rack: RackTable): void {
    this._weapon = weapon
    this._armour = armour
    this._trinket = trinket
    this._mount = mount
    this._banner = banner
    this._holder = holder
    this._loadout = loadout
    this._fitting = fitting
    this._rack = rack

    this.solveCrossReferences()
  }

  /**
   * Turns the stored keys into the rows they name, once every table is in memory.
   *
   * A `foreign` column travels as the target row's primary key, so a table read on its own
   * has the key and not the row - which is why this runs from `publish` and not from a
   * table's own read. A zero means the sheet left the cell empty and is left unresolved.
   */
  private solveCrossReferences(): void {
    for (const record of this._holder.records) {
      if (record._only_Weapon_index > 0) {
        const target = this._weapon.getByIndexOrThrow(
          record._only_Weapon_index)
        record.setReference_only_INTERNAL(target)
        record._only_F = true
      }
      if (record.pick > 0) {
        if (record.pickTarget === HolderPickTarget.None) {
          const found = this._weapon.findByIndex(record.pick)
          if (found !== undefined)
            record.setReference_pick_INTERNAL(HolderPickTarget.Weapon, found)
        }
        if (record.pickTarget === HolderPickTarget.None) {
          const found = this._armour.findByIndex(record.pick)
          if (found !== undefined)
            record.setReference_pick_INTERNAL(HolderPickTarget.Armour, found)
        }
      }
      if (record.wide > 0) {
        if (record.wideTarget === HolderWideTarget.None) {
          const found = this._weapon.findByIndex(record.wide)
          if (found !== undefined)
            record.setReference_wide_INTERNAL(HolderWideTarget.Weapon, found)
        }
        if (record.wideTarget === HolderWideTarget.None) {
          const found = this._armour.findByIndex(record.wide)
          if (found !== undefined)
            record.setReference_wide_INTERNAL(HolderWideTarget.Armour, found)
        }
        if (record.wideTarget === HolderWideTarget.None) {
          const found = this._trinket.findByIndex(record.wide)
          if (found !== undefined)
            record.setReference_wide_INTERNAL(HolderWideTarget.Trinket, found)
        }
        if (record.wideTarget === HolderWideTarget.None) {
          const found = this._mount.findByIndex(record.wide)
          if (found !== undefined)
            record.setReference_wide_INTERNAL(HolderWideTarget.Mount, found)
        }
        if (record.wideTarget === HolderWideTarget.None) {
          const found = this._banner.findByIndex(record.wide)
          if (found !== undefined)
            record.setReference_wide_INTERNAL(HolderWideTarget.Banner, found)
        }
      }
      if (record.maybe > 0) {
        if (record.maybeTarget === HolderMaybeTarget.None) {
          const found = this._weapon.findByIndex(record.maybe)
          if (found !== undefined)
            record.setReference_maybe_INTERNAL(HolderMaybeTarget.Weapon, found)
        }
        if (record.maybeTarget === HolderMaybeTarget.None) {
          const found = this._armour.findByIndex(record.maybe)
          if (found !== undefined)
            record.setReference_maybe_INTERNAL(HolderMaybeTarget.Armour, found)
        }
      }
    }

    for (const record of this._loadout.records) {
      for (let i = 0; i < record._slot.length; i++) {
        if (record._slot[i].pick > 0) {
          if (record._slot[i].pickTarget === LoadoutSlotPickTarget.None) {
            const found = this._weapon.findByIndex(record._slot[i].pick)
            if (found !== undefined) {
              record._slot[i].pickRow = found
              record._slot[i].pickTarget = LoadoutSlotPickTarget.Weapon
            }
          }
          if (record._slot[i].pickTarget === LoadoutSlotPickTarget.None) {
            const found = this._armour.findByIndex(record._slot[i].pick)
            if (found !== undefined) {
              record._slot[i].pickRow = found
              record._slot[i].pickTarget = LoadoutSlotPickTarget.Armour
            }
          }
        }
      }
    }

    for (const record of this._fitting.records) {
      if (record._main.pick > 0) {
        if (record._main.pickTarget === FittingMainPickTarget.None) {
          const found = this._weapon.findByIndex(record._main.pick)
          if (found !== undefined) {
            record._main.pickRow = found
            record._main.pickTarget = FittingMainPickTarget.Weapon
          }
        }
        if (record._main.pickTarget === FittingMainPickTarget.None) {
          const found = this._armour.findByIndex(record._main.pick)
          if (found !== undefined) {
            record._main.pickRow = found
            record._main.pickTarget = FittingMainPickTarget.Armour
          }
        }
      }
    }

    for (const record of this._rack.records) {
      for (let i = 0; i < record._slots.pick.length; i++) {
        if (record._slots.pick[i] > 0) {
          if (record._slots.pickTarget[i] === RackSlotsPickTarget.None) {
            const found = this._weapon.findByIndex(record._slots.pick[i])
            if (found !== undefined) {
              record._slots.pickRow[i] = found
              record._slots.pickTarget[i] = RackSlotsPickTarget.Weapon
            }
          }
          if (record._slots.pickTarget[i] === RackSlotsPickTarget.None) {
            const found = this._armour.findByIndex(record._slots.pick[i])
            if (found !== undefined) {
              record._slots.pickRow[i] = found
              record._slots.pickTarget[i] = RackSlotsPickTarget.Armour
            }
          }
        }
      }
    }
  }
}
