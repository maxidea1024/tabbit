// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_DECLAREDACCESSOR_H
#define TABBIT_GENERATED_DECLAREDACCESSOR_H

#include <cstddef>
#include <string>

#include "enums/DeclaredAccessor_enum_affinity.h"
#include "tables/DeclaredAccessor_loadout.h"
#include "tables/DeclaredAccessor_bonuses.h"
#include "tables/DeclaredAccessor_chest.h"

namespace tabbit {
namespace fixtures {
namespace declared {
/// Every table, loaded together so cross-table references can be resolved.
class DeclaredAccessor {
 public:
  const LoadoutTable& loadout() const { return loadout_; }
  const BonusesTable& bonuses() const { return bonuses_; }
  const ChestTable& chest() const { return chest_; }

  /// Reads every table from `base_path`, then links the references between them.
  ///
  /// Safe to call on a loaded accessor. Every file is read into a table of its own and the
  /// references are linked among those, so a read that throws part way through leaves every
  /// table holding the load it already had, and no row points at a row from it.
  void read_all(const std::string& base_path, const std::string& file_extension = ".tcb") {
    LoadoutTable loaded_loadout;
    loaded_loadout.read(base_path + "/Loadout" + file_extension);
    BonusesTable loaded_bonuses;
    loaded_bonuses.read(base_path + "/Bonuses" + file_extension);
    ChestTable loaded_chest;
    loaded_chest.read(base_path + "/Chest" + file_extension);

    solve_cross_references(loaded_loadout, loaded_bonuses, loaded_chest);

    loadout_ = std::move(loaded_loadout);
    bonuses_ = std::move(loaded_bonuses);
    chest_ = std::move(loaded_chest);
  }

 private:
  /// The tables arrive as arguments, which is how this links the load being read rather
  /// than the one already published.
  ///
  /// Every table is passed whether or not it takes part in a reference, so the call site
  /// is the same shape for every model. `[[maybe_unused]]` is what says so to the
  /// compiler: the gate builds with `-Wextra -Werror`, and a model where nothing
  /// references anything - which is most of them - otherwise fails to compile on the
  /// unused parameters.
  void solve_cross_references([[maybe_unused]] LoadoutTable& loaded_loadout, [[maybe_unused]] BonusesTable& loaded_bonuses, [[maybe_unused]] ChestTable& loaded_chest) {
    // No table references another.
  }

  LoadoutTable loadout_;
  BonusesTable bonuses_;
  ChestTable chest_;
};

}  // namespace declared
}  // namespace fixtures
}  // namespace tabbit

#endif  // TABBIT_GENERATED_DECLAREDACCESSOR_H
