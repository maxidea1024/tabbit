/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DECLARED_LOADOUT_H
#define DECLARED_LOADOUT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "Declared_Forward.h"
#include "enums/Declared_EnumAffinity.h"
#include "structs/Declared_StructReward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/declared/declared.xlsx : Declared : B2
 *
 * A record group whose members a schema file declares.
 */

struct Declared_LoadoutRecord_t {
  /* primary index */
  int32_t index;
  /* plain column, outside the group */
  const char* name;
  /* Which item, as that table's key. */
  struct Declared_Reward* slot;
  int32_t slot_count;
  /* an enum the schema file declares */
  Declared_Affinity_t grade;
};

/* Every row of Loadout.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Declared_LoadoutTable_t {
  Declared_LoadoutRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} Declared_LoadoutTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Declared_LoadoutLoad(Declared_LoadoutTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Declared_LoadoutFree(Declared_LoadoutTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Declared_LoadoutRecord_t* Declared_LoadoutFindByIndex(
  const Declared_LoadoutTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool Declared_LoadoutContainsIndex(
  const Declared_LoadoutTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* DECLARED_LOADOUT_H */
