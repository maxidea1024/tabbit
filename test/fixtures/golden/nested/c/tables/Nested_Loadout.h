/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef NESTED_LOADOUT_H
#define NESTED_LOADOUT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "Nested_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/nested/nested.xlsx : Nested : B2
 *
 * Columns folded into records by the Group.Member notation.
 */
/* One element of Nested_LoadoutRecord_t::pos. */
struct Nested_LoadoutRecord_t_pos_entry {
  /* record with no number */
  float x;
  /* second member of the same record */
  float y;
};

/* One element of Nested_LoadoutRecord_t::slot. */
struct Nested_LoadoutRecord_t_slot_entry {
  /* element 1, first member */
  int32_t id;
  /* element 1, second member */
  const char* label;
};

struct Nested_LoadoutRecord_t {
  /* primary index */
  int32_t index;
  /* plain column, between two groups */
  const char* name;
  /* record with no number */
  struct Nested_LoadoutRecord_t_pos_entry pos;
  /* element 1, first member */
  struct Nested_LoadoutRecord_t_slot_entry* slot;
  int32_t slot_count;
  /* plain column inside the group's span */
  const char* note;
  /* scalar serial field */
  const char** tag_array;
  int32_t tag_array_count;
};

/* Every row of Loadout.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Nested_LoadoutTable_t {
  Nested_LoadoutRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} Nested_LoadoutTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Nested_LoadoutLoad(Nested_LoadoutTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Nested_LoadoutFree(Nested_LoadoutTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Nested_LoadoutRecord_t* Nested_LoadoutFindByIndex(
  const Nested_LoadoutTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool Nested_LoadoutContainsIndex(
  const Nested_LoadoutTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* NESTED_LOADOUT_H */
