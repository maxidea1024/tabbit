// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package tabbit.fixtures.nested

import java.io.File
import tabbit.TcbReader
import tabbit.RecordNotFoundException
import tabbit.Uuid
import tabbit.readAllBytes
import tabbit.open
import tabbit.readTableHeader
import tabbit.checkColumn
import tabbit.checkBlockEnd
import tabbit.readPresence
import tabbit.isPresent
import tabbit.TcbException
import tabbit.ColumnCursor
import tabbit.ELEMENT_VARINT
import tabbit.ELEMENT_BOOL
import tabbit.ELEMENT_I32
import tabbit.ELEMENT_I64
import tabbit.ELEMENT_F32
import tabbit.ELEMENT_F64
import tabbit.ELEMENT_STRING
import tabbit.ELEMENT_UUID
import tabbit.KIND_SCALAR
import tabbit.KIND_FIXED_ARRAY
import tabbit.KIND_VAR_ARRAY

// Generated from test/fixtures/xlsx/nested/nested.xlsx : Nested : B2
/** Columns folded into records by the Group.Member notation. */
class LoadoutRecord {
    /** primary index */
    var index: Int = 0
    /** plain column, between two groups */
    var name: String = ""
    /** record with no number */
    var pos: PosEntry = PosEntry()
    /** element 1, first member */
    var slot: MutableList<SlotEntry> = MutableList(2) { SlotEntry() }
    /** plain column inside the group's span */
    var note: String = ""
    /** scalar serial field */
    var tagArray: MutableList<String> = ArrayList()

    /** One element of pos. */
    class PosEntry {
        /** record with no number */
        var x: Float = 0.0f
        /** second member of the same record */
        var y: Float = 0.0f
    }

    /** One element of slot. */
    class SlotEntry {
        /** element 1, first member */
        var id: Int = 0
        /** element 1, second member */
        var label: String = ""
    }

}

/** Every row of Loadout. */
class LoadoutTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<LoadoutRecord> = ArrayList()
        private set

    private var byIndex: HashMap<Int, LoadoutRecord> = HashMap()

    /**
     * The row with this Index, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findByIndex(key: Int): LoadoutRecord? = byIndex[key]

    /**
     * The row with this Index, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByIndex(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIndexOrThrow(key: Int): LoadoutRecord =
        byIndex[key] ?: throw RecordNotFoundException(
            "there is no record in table `Loadout` that corresponds to " +
                "field `Index` value $key")

    /** Whether the table holds a row with this Index. */
    fun containsIndex(key: Int): Boolean = byIndex.containsKey(key)

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        // Opened unconditionally, and with whatever NestedAccessor.encryptionKey and
        // NestedAccessor.macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from this untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/tcb-mac-and-signature.md.
        val reader = TcbReader(open(
            readAllBytes(filename), NestedAccessor.encryptionKey,
            NestedAccessor.macKey, NestedAccessor.verifyMac))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<LoadoutRecord>(count)
        val loadedByIndex = HashMap<Int, LoadoutRecord>(count * 2)

        repeat(count) { loaded.add(LoadoutRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "Loadout.Index", KIND_SCALAR, 1, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Index")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].index = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                2 -> {
                    checkColumn(column, "Loadout.Name", KIND_SCALAR, 1, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Name")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].name = cursor.runSameText
                            at++
                            n--
                        }
                    }
                }
                3 -> {
                    checkColumn(column, "Loadout.Pos.X", KIND_SCALAR, 1, false, ELEMENT_F32)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Pos.X")
                    for (record in loaded) {
                        record.pos.x = cursor.nextF32()
                    }
                }
                4 -> {
                    checkColumn(column, "Loadout.Pos.Y", KIND_SCALAR, 1, false, ELEMENT_F32)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Pos.Y")
                    for (record in loaded) {
                        record.pos.y = cursor.nextF32()
                    }
                }
                5 -> {
                    checkColumn(column, "Loadout.Slot.Id", KIND_FIXED_ARRAY, 2, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Slot.Id")
                    for (record in loaded) {
                        for (element in 0 until 2) {
                            record.slot[element].id = cursor.nextI32()
                        }
                    }
                }
                6 -> {
                    checkColumn(column, "Loadout.Slot.Label", KIND_FIXED_ARRAY, 2, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Slot.Label")
                    for (record in loaded) {
                        for (element in 0 until 2) {
                            record.slot[element].label = cursor.nextString()
                        }
                    }
                }
                7 -> {
                    checkColumn(column, "Loadout.Note", KIND_SCALAR, 1, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Note")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].note = cursor.runSameText
                            at++
                            n--
                        }
                    }
                }
                8 -> {
                    checkColumn(column, "Loadout.Tag_array", KIND_FIXED_ARRAY, 2, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Tag_array")
                    for (record in loaded) {
                        record.tagArray = ArrayList(2)
                        repeat(2) { record.tagArray.add(cursor.nextString()) }
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedByIndex[record.index] = record
        }

        // Published, now that every column read.
        records = loaded
        byIndex = loadedByIndex
    }
}
