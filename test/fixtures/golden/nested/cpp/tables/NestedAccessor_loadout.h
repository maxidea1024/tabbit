// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_NESTEDACCESSOR_LOADOUT_H
#define TABBIT_GENERATED_NESTEDACCESSOR_LOADOUT_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "NestedAccessor_forward.h"
// Generated from test/fixtures/xlsx/nested/nested.xlsx : Nested : B2
/// One element of LoadoutRecord::pos.
struct LoadoutRecord_pos_entry {
  /// record with no number
  float x = 0.0f;
  /// second member of the same record
  float y = 0.0f;
};

/// One element of LoadoutRecord::slot.
struct LoadoutRecord_slot_entry {
  /// element 1, first member
  std::int32_t id = 0;
  /// element 1, second member
  std::string label;
};

/// Columns folded into records by the Group.Member notation.
struct LoadoutRecord {
  /// primary index
  std::int32_t index = 0;
  /// plain column, between two groups
  std::string name;
  /// record with no number
  LoadoutRecord_pos_entry pos;
  /// element 1, first member
  std::vector<LoadoutRecord_slot_entry> slot;
  /// plain column inside the group's span
  std::string note;
  /// scalar serial field
  std::vector<std::string> tag_array;
};

/// Columns folded into records by the Group.Member notation.
class LoadoutTable {
 public:
  const std::vector<LoadoutRecord>& records() const { return records_; }

  /// The row with this Index, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const LoadoutRecord* find_by_index(std::int32_t key) const {
    const auto it = by_index_.find(key);
    return it == by_index_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_index(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const LoadoutRecord& get_by_index_or_throw(std::int32_t key) const {
    const LoadoutRecord* found = find_by_index(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Loadout` that corresponds to "
                    "field `Index` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Index.
  bool contains_index(std::int32_t key) const {
    return by_index_.find(key) != by_index_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<LoadoutRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Loadout.Index", tabbit::kKindScalar, 1, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loadout.Index");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].index = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Loadout.Name", tabbit::kKindScalar, 1, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loadout.Name");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].name = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Loadout.Pos.X", tabbit::kKindScalar, 1, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loadout.Pos.X");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.pos.x = cursor.next_f32();
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "Loadout.Pos.Y", tabbit::kKindScalar, 1, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loadout.Pos.Y");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.pos.y = cursor.next_f32();
          }
          break;
        }
        case 5: {
          tabbit::check_column(column, "Loadout.Slot.Id", tabbit::kKindFixedArray, 2, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loadout.Slot.Id");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.slot.resize(2);
            for (std::size_t j = 0; j < 2; ++j) {
              record.slot[j].id = cursor.next_i32();
            }
          }
          break;
        }
        case 6: {
          tabbit::check_column(column, "Loadout.Slot.Label", tabbit::kKindFixedArray, 2, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loadout.Slot.Label");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.slot.resize(2);
            for (std::size_t j = 0; j < 2; ++j) {
              record.slot[j].label = cursor.next_string();
            }
          }
          break;
        }
        case 7: {
          tabbit::check_column(column, "Loadout.Note", tabbit::kKindScalar, 1, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loadout.Note");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].note = value;
            }
          }
          break;
        }
        case 8: {
          tabbit::check_column(column, "Loadout.Tag_array", tabbit::kKindFixedArray, -1, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Loadout.Tag_array");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.tag_array.resize(static_cast<std::size_t>(column.count));
            for (std::size_t j = 0; j < static_cast<std::size_t>(column.count); ++j) {
              record.tag_array[j] = cursor.next_string();
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class NestedAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<LoadoutRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> index_map;
    index_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      index_map.emplace(records[i].index, i);
    }

    records_ = std::move(records);
    by_index_ = std::move(index_map);
  }

  std::vector<LoadoutRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_index_;
};

#endif  // TABBIT_GENERATED_NESTEDACCESSOR_LOADOUT_H
