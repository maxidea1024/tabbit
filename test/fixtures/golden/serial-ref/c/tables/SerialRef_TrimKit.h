/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef SERIAL_REF_TRIM_KIT_H
#define SERIAL_REF_TRIM_KIT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "SerialRef_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/serial-ref-trim/serial-ref-trim.xlsx : Refs : J2
 *
 * Numbered reference columns folded into an array the row's length.
 */
struct SerialRef_TrimKitRecord_t {
  /* primary index */
  int32_t index;
  /* element 1 - the row it points at */
  const SerialRef_BitRecord_t** slot;
  int32_t* slot_index;
  int32_t slot_count;
  /* Which of slot's elements have a value, one bool per element, or NULL where
   * the file did not carry the column. spec/nullable-array-elements.md. */
  const bool* has_slot_at;
  /* element 1 - the target's own value */
  int32_t* tier;
  int32_t* tier_index;
  int32_t tier_count;
  /* Which of tier's elements have a value, one bool per element, or NULL where
   * the file did not carry the column. spec/nullable-array-elements.md. */
  const bool* has_tier_at;
};

/* Every row of TrimKit.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct SerialRef_TrimKitTable_t {
  SerialRef_TrimKitRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} SerialRef_TrimKitTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool SerialRef_TrimKitLoad(SerialRef_TrimKitTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void SerialRef_TrimKitFree(SerialRef_TrimKitTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const SerialRef_TrimKitRecord_t* SerialRef_TrimKitFindByIndex(
  const SerialRef_TrimKitTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool SerialRef_TrimKitContainsIndex(
  const SerialRef_TrimKitTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* SERIAL_REF_TRIM_KIT_H */
