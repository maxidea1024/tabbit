-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")

---@class GuideSkillEntry
---@field step integer[]
---@field order string[]
local GuideSkillEntryMeta = tcb.strictType("an element of GuideRecord.skill", { "step", "order" })

---@return GuideSkillEntry
local function newGuideSkillEntry()
  return setmetatable({
    step = tcb.repeated(2, 0),
    order = tcb.repeated(2, ""),
  }, GuideSkillEntryMeta)
end

---@class GuidePosEntry
---@field x number
---@field y number
local GuidePosEntryMeta = tcb.strictType("an element of GuideRecord.pos", { "x", "y" })

---@return GuidePosEntry
local function newGuidePosEntry()
  return setmetatable({
    x = 0.0,
    y = 0.0,
  }, GuidePosEntryMeta)
end

-- Generated from test/fixtures/xlsx/member-array/member-array.xlsx : MemberArray : B3.
-- A record whose members are arrays, and an array of arrays beside it.
---@class GuideRecord
---@field index integer
---@field name string
---@field skill GuideSkillEntry
---@field pos GuidePosEntry
---@field tagArray string[]
---@field grid integer[][]
local GuideRecordMeta = tcb.strictType("a `Guide` row", { "index", "name", "skill", "pos", "tagArray", "grid" })

---@return GuideRecord
local function newGuideRecord()
  return setmetatable({
    index = 0,
    name = "",
    skill = newGuideSkillEntry(),
    pos = newGuidePosEntry(),
    tagArray = {},
    grid = { tcb.repeated(3, 0), tcb.repeated(3, 0) },
  }, GuideRecordMeta)
end

---@class GuideTable
---@field records GuideRecord[]
local GuideTable = {}

local GuideTableMeta = tcb.strictInstance("the `Guide` table", GuideTable, { "records", "byIndex" })

function GuideTable.new()
  return setmetatable({
    records = {},
    byIndex = {},
  }, GuideTableMeta)
end

-- The row with this Index, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function GuideTable:findByIndex(key)
  return self.byIndex[key]
end

-- The row with this Index, or a raised error naming what was missing -
-- for a key that has to be there.
function GuideTable:getByIndexOrThrow(key)
  local record = self:findByIndex(key)

  if record == nil then
    tcb.fail("there is no record in table `Guide` that corresponds to " ..
      "field `Index` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this Index.
function GuideTable:containsIndex(key)
  return self:findByIndex(key) ~= nil
end

-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function GuideTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newGuideRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "Guide.Index", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Guide.Index")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].index = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "Guide.Name", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Guide.Name")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].name = value
        end

        at = at + n
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "Guide.Skill.Step", tcb.KIND_ARRAY, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Guide.Skill.Step")
      for i = 1, count do
        local record = records[i]
        local elementCount = cursor:nextLength()
        record.skill.step = {}
        for element = 1, elementCount do
          record.skill.step[element] = cursor:nextI32()
        end
      end
    elseif column.tag == 4 then
      tcb.checkColumn(column, "Guide.Skill.Order", tcb.KIND_ARRAY, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Guide.Skill.Order")
      for i = 1, count do
        local record = records[i]
        local elementCount = cursor:nextLength()
        record.skill.order = {}
        for element = 1, elementCount do
          record.skill.order[element] = cursor:nextString()
        end
      end
    elseif column.tag == 5 then
      tcb.checkColumn(column, "Guide.Pos.X", tcb.KIND_SCALAR, false, { tcb.ELEMENT_F32 })
      local cursor = tcb.newCursor(reader, column, count, "Guide.Pos.X")
      for i = 1, count do
        local record = records[i]
        record.pos.x = cursor:nextF32()
      end
    elseif column.tag == 6 then
      tcb.checkColumn(column, "Guide.Pos.Y", tcb.KIND_SCALAR, false, { tcb.ELEMENT_F32 })
      local cursor = tcb.newCursor(reader, column, count, "Guide.Pos.Y")
      for i = 1, count do
        local record = records[i]
        record.pos.y = cursor:nextF32()
      end
    elseif column.tag == 7 then
      tcb.checkColumn(column, "Guide.Tag_array", tcb.KIND_ARRAY, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Guide.Tag_array")
      for i = 1, count do
        local record = records[i]
        local elementCount = cursor:nextLength()
        local values = {}

        for element = 1, elementCount do
          values[element] = cursor:nextString()
        end

        record.tagArray = values
      end
    elseif column.tag == 8 then
      tcb.checkColumn(column, "Guide.Grid.1", tcb.KIND_ARRAY, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Guide.Grid.1")
      for i = 1, count do
        local record = records[i]
        local elementCount = cursor:nextLength()
        record.grid[1] = {}
        for element = 1, elementCount do
          record.grid[1][element] = cursor:nextI32()
        end
      end
    elseif column.tag == 9 then
      tcb.checkColumn(column, "Guide.Grid.2", tcb.KIND_ARRAY, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Guide.Grid.2")
      for i = 1, count do
        local record = records[i]
        local elementCount = cursor:nextLength()
        record.grid[2] = {}
        for element = 1, elementCount do
          record.grid[2][element] = cursor:nextI32()
        end
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byIndex = {}

  for i = 1, #records do
    byIndex[records[i].index] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byIndex = byIndex
end

-- The same load from a file on disk.
function GuideTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return GuideTable
