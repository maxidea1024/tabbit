/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef MEMBER_ARRAY_GUIDE_H
#define MEMBER_ARRAY_GUIDE_H

#include "tabbit/tabbit_tcb_reader.h"

#include "MemberArray_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/member-array/member-array.xlsx : MemberArray : B2
 *
 * A record whose members are arrays, and an array of arrays beside it.
 */
/* One element of MemberArray_GuideRecord_t::skill. */
struct MemberArray_GuideRecord_t_skill_entry {
  /* member array, element 1 */
  int32_t* step; int32_t step_count;
  /* second member, element 1 */
  const char** order; int32_t order_count;
};

/* One element of MemberArray_GuideRecord_t::pos. */
struct MemberArray_GuideRecord_t_pos_entry {
  /* a record with no number at all - still one record, members not arrays */
  float x;
  /* second member of it */
  float y;
};

struct MemberArray_GuideRecord_t {
  /* primary index */
  int32_t index;
  /* plain column, between the groups */
  const char* name;
  /* member array, element 1 */
  struct MemberArray_GuideRecord_t_skill_entry skill;
  /* a record with no number at all - still one record, members not arrays */
  struct MemberArray_GuideRecord_t_pos_entry pos;
  /* scalar serial array, so both array kinds sit in one table */
  const char** tag;
  int32_t tag_count;
  /* array of arrays: outer 1, inner 1 - neither level has a name */
  int32_t* grid[2];
  int32_t grid_count[2];
};

/* Every row of Guide.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct MemberArray_GuideTable_t {
  MemberArray_GuideRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} MemberArray_GuideTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool MemberArray_GuideLoad(MemberArray_GuideTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void MemberArray_GuideFree(MemberArray_GuideTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const MemberArray_GuideRecord_t* MemberArray_GuideFindByIndex(
  const MemberArray_GuideTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool MemberArray_GuideContainsIndex(
  const MemberArray_GuideTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* MEMBER_ARRAY_GUIDE_H */
