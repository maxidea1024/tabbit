// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Grids : B11
/// How much an element does to another.
class ElementChartRecord {
  Element attacker = Element.of(0);
  List<double> rate = [];

}

/// Every row of ElementChart.
class ElementChartTable extends Iterable<ElementChartRecord> {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<ElementChartRecord> get records => _records;
  List<ElementChartRecord> _records = [];

  /// How many rows the table holds.
  ///
  /// Overridden rather than inherited: `Iterable.length` counts by walking, and the rows
  /// are a list that already knows.
  @override
  int get length => _records.length;

  /// The rows, in the order the file wrote them. Extending `Iterable` on top of this is
  /// what gives the table `map` - `where` - `firstWhere` without any of them being
  /// written here.
  ///
  /// The list reference is read once, here. A refresh replaces the reference rather than
  /// its contents, so a loop already running keeps the rows it started with.
  @override
  Iterator<ElementChartRecord> get iterator => _records.iterator;

  Map<Element, ElementChartRecord> _byAttacker = {};

  ElementChartColumnTable? _columnAxis;

  /// Hands this table the column axis its cells are positioned by.
  ///
  /// Called by the accessor once both files are read, and the length check is here rather than
  /// in the read. A grid is two files, either can be deployed without the other, and a row
  /// whose array is a different length from the axis is a row whose cells have moved.
  void linkColumnAxis(ElementChartColumnTable columns) {
    for (final record in _records) {
      if (record.rate.length != columns.records.length) {
        throw StateError(
            'table `ElementChart` holds ${record.rate.length} cells for '
            '`${record.attacker}` and its column axis `ElementChartColumn` holds '
            '${columns.records.length} keys; the two files are from different builds');
      }
    }

    _columnAxis = columns;
  }

  /// The row axis keys, in the order the file wrote them.
  List<Element> get rowKeys =>
      _records.map((record) => record.attacker).toList();

  /// The column axis keys, in the order a row holds its cells.
  List<Element> get colKeys =>
      _columnAxis?.records.map((record) => record.defender).toList()
      ?? <Element>[];

  /// One row of cells, or null when the grid has no such row.
  ///
  /// In the order [colKeys] is in, which is what makes a position mean something. The list is
  /// the record's own, so it is not a copy.
  List<double>? row(Element attacker) =>
      findByAttacker(attacker)?.rate;

  /// The cell where the two keys meet, or a thrown exception naming the one that missed.
  ///
  /// It throws rather than answering with the type's empty value, because a grid's empty value
  /// is one the sheet can write - 0 in a modifier table is a modifier of zero.
  double at(Element attacker, Element defender) =>
      _cellRecord(attacker).rate[_cellAt(defender)];


  /// The row half of a cell lookup.
  ElementChartRecord _cellRecord(Element attacker) {
    if (_columnAxis == null) {
      throw StateError(
          'table `ElementChart` has no column axis yet; '
          'read `ElementChartColumn` before reading a cell');
    }

    final record = findByAttacker(attacker);

    if (record == null) {
      throw RecordNotFoundException(
          'table `ElementChart` has no row `${attacker}`');
    }

    return record;
  }

  /// The column half of a cell lookup.
  int _cellAt(Element defender) {
    final column = _columnAxis!.findByDefender(defender);

    if (column == null) {
      throw RecordNotFoundException(
          'table `ElementChart` has no column `${defender}`');
    }

    return column.at;
  }

  /// The row with this Attacker, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  ElementChartRecord? findByAttacker(Element key) => _byAttacker[key];

  /// The row with this Attacker, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getByAttacker(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  ElementChartRecord getByAttackerOrThrow(Element key) {
    final record = _byAttacker[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `ElementChart` that corresponds to '
          'field `Attacker` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Attacker.
  bool containsAttacker(Element key) => _byAttacker.containsKey(key);


  /// The row with this Attacker, or null when the table has none.
  ///
  /// `findByAttacker` under the spelling Dart uses for a keyed collection. It
  /// answers with null because that is what `Map` does here, and the type says so - a
  /// language whose own map throws generates a subscript that throws.
  /// spec/targets/table-collection-surface.md section 5.7.
  ///
  /// This is the key, not the row's position. `table[0]` is the row whose
  /// Attacker is 0, not the first row - the rows in order are `records`.
  ///
  /// Only a key of one column has this: Dart's subscript takes one argument.
  ElementChartRecord? operator [](Element key) => findByAttacker(key);


  /// Each row with the Attacker it is keyed by -
  /// `for (final entry in table.entries)`.
  ///
  /// What this saves a caller is not the key value - the row carries it - but having to
  /// know which column the key is: Attacker here, something else in the next
  /// table.
  ///
  /// The rows come in the order the file wrote them rather than the order the map holds
  /// them, which makes this and iterating the table agree. Only the primary key has this:
  /// a table keyed by several columns together has no single key value to pair a row with.
  Iterable<MapEntry<Element, ElementChartRecord>> get entries =>
      _records.map((record) => MapEntry(record.attacker, record));

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <ElementChartRecord>[];
    final loadedByAttacker = <Element, ElementChartRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(ElementChartRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'ElementChart.Attacker', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'ElementChart.Attacker');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].attacker = Element.of(value);
            }
          }
          break;
        case 2:
          checkColumn(column, 'ElementChart.Rate', kindArray, false, [elementF32]);
          cursor = TcbColumnCursor(reader, column, count, 'ElementChart.Rate');
          for (final record in loaded) {
            final elementCount = cursor.nextLength();
            record.rate = List.generate(elementCount, (_) => cursor.nextF32());
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedByAttacker[record.attacker] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byAttacker = loadedByAttacker;
  }
}
