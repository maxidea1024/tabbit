// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#pragma once

#include "CoreMinimal.h"
#include "UObject/ObjectMacros.h"
#include "Kismet/BlueprintFunctionLibrary.h"

#include "TabbitTcbReader.h"

// Must be the last include. UHT requires it, and moving it produces an error that
// names some other line entirely.
#include "FMatrixData.generated.h"


// Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Enums : B2
/** What an attack is made of. */
UENUM(BlueprintType)
enum class EAffinity : uint8
{
    /** no element */
    None = 0 UMETA(DisplayName = "None"),
    /** burns */
    Fire = 1 UMETA(DisplayName = "Fire"),
    /** soaks */
    Water = 2 UMETA(DisplayName = "Water"),
    /** cuts */
    Wind = 3 UMETA(DisplayName = "Wind"),
};


// Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Goods : B2
/** What a town trades in. */
USTRUCT(BlueprintType)
struct MATRIXDATA_API FGoodsRow
{
    GENERATED_BODY()

    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Goods")
    int32 Id = 0;

    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Goods")
    FString Name;

};


// Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Grids : B2
/** Price modifier by town and goods. */
USTRUCT(BlueprintType)
struct MATRIXDATA_API FTownPriceRow
{
    GENERATED_BODY()

    /** which town */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "TownPrice")
    int32 Town = 0;

    /** the modifier */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "TownPrice")
    TArray<int32> Price;

    /** Which of Price's elements have a value. Empty where the file did not carry
     * the column, and then every index is out of range anyway.
     * spec/types/nullable-array-elements.md. */
    TArray<bool> bHasPriceAt;

};


// Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Grids : B2
/** Price modifier by town and goods. */
USTRUCT(BlueprintType)
struct MATRIXDATA_API FTownPriceColumnRow
{
    GENERATED_BODY()

    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "TownPriceColumn")
    int32 Goods = 0;

    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "TownPriceColumn")
    int32 At = 0;

};


// Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Grids : B11
/** How much an element does to another. */
USTRUCT(BlueprintType)
struct MATRIXDATA_API FElementChartRow
{
    GENERATED_BODY()

    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ElementChart")
    EAffinity Attacker = static_cast<EAffinity>(0);

    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ElementChart")
    TArray<float> Rate;

};


// Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Grids : B11
/** How much an element does to another. */
USTRUCT(BlueprintType)
struct MATRIXDATA_API FElementChartColumnRow
{
    GENERATED_BODY()

    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ElementChartColumn")
    EAffinity Defender = static_cast<EAffinity>(0);

    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ElementChartColumn")
    int32 At = 0;

};


/** Every row of Goods. */
class MATRIXDATA_API FGoodsTable
{
public:
    const TArray<FGoodsRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FGoodsRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FGoodsRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of TownPrice. */
class MATRIXDATA_API FTownPriceTable
{
public:
    const TArray<FTownPriceRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Town, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FTownPriceRow* FindByTown(int32 Key) const;

    /** Whether the table holds a row with this Town. */
    bool ContainsTown(int32 Key) const;



    /**
     * Hands this table the column axis its cells are positioned by.
     *
     * Called by the accessor once both files are read. The length check is here rather than in
     * the read: a grid is two files, either can be deployed without the other, and a row whose
     * array is a different length from the axis is a row whose cells have moved.
     */
    bool LinkColumnAxis(const FTownPriceColumnTable& Columns);

    /** The row axis keys, in the order the file wrote them. */
    TArray<int32> RowKeys() const;

    /** The column axis keys, in the order a row holds its cells. */
    const TArray<int32>& ColKeys() const { return ColumnKeys; }

    /**
     * One row of cells, or nullptr when the grid has no such row.
     *
     * In the order ColKeys is in, which is what makes a position mean something.
     */
    const TArray<int32>* Row(int32 Town) const;

    /**
     * The cell where the two keys meet, and whether both keys were there.
     *
     * It answers false rather than the type's empty value, because a grid's empty value is one
     * the sheet can write - 0 in a modifier table is a modifier of zero. Unreal builds without
     * exceptions, so a miss is a return rather than a throw.
     */
    bool At(int32 Town, int32 Goods, int32& OutCell) const;



    /**
     * Whether the sheet gave the cell where the two keys meet a value, and whether both keys
     * were there.
     *
     * A grid whose cells are optional reads a blank as the type's empty value, and that value
     * is one the sheet could also have written - so this is the only way to tell them apart.
     */
    bool HasAt(int32 Town, int32 Goods, bool& OutHasValue) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:

    bool CellAt(int32 Town, int32 Goods, const FTownPriceRow*& OutRecord, int32& OutPosition) const;

    TArray<FTownPriceRow> RecordsStorage;
    TMap<int32, int32> ByTown;
    TMap<int32, int32> ColumnAt;
    TArray<int32> ColumnKeys;
    bool bColumnAxisLinked = false;
};


/** Every row of TownPriceColumn. */
class MATRIXDATA_API FTownPriceColumnTable
{
public:
    const TArray<FTownPriceColumnRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Goods, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FTownPriceColumnRow* FindByGoods(int32 Key) const;

    /** Whether the table holds a row with this Goods. */
    bool ContainsGoods(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FTownPriceColumnRow> RecordsStorage;
    TMap<int32, int32> ByGoods;
};


/** Every row of ElementChart. */
class MATRIXDATA_API FElementChartTable
{
public:
    const TArray<FElementChartRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Attacker, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FElementChartRow* FindByAttacker(EAffinity Key) const;

    /** Whether the table holds a row with this Attacker. */
    bool ContainsAttacker(EAffinity Key) const;



    /**
     * Hands this table the column axis its cells are positioned by.
     *
     * Called by the accessor once both files are read. The length check is here rather than in
     * the read: a grid is two files, either can be deployed without the other, and a row whose
     * array is a different length from the axis is a row whose cells have moved.
     */
    bool LinkColumnAxis(const FElementChartColumnTable& Columns);

    /** The row axis keys, in the order the file wrote them. */
    TArray<EAffinity> RowKeys() const;

    /** The column axis keys, in the order a row holds its cells. */
    const TArray<EAffinity>& ColKeys() const { return ColumnKeys; }

    /**
     * One row of cells, or nullptr when the grid has no such row.
     *
     * In the order ColKeys is in, which is what makes a position mean something.
     */
    const TArray<float>* Row(EAffinity Attacker) const;

    /**
     * The cell where the two keys meet, and whether both keys were there.
     *
     * It answers false rather than the type's empty value, because a grid's empty value is one
     * the sheet can write - 0 in a modifier table is a modifier of zero. Unreal builds without
     * exceptions, so a miss is a return rather than a throw.
     */
    bool At(EAffinity Attacker, EAffinity Defender, float& OutCell) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:

    bool CellAt(EAffinity Attacker, EAffinity Defender, const FElementChartRow*& OutRecord, int32& OutPosition) const;

    TArray<FElementChartRow> RecordsStorage;
    TMap<EAffinity, int32> ByAttacker;
    TMap<EAffinity, int32> ColumnAt;
    TArray<EAffinity> ColumnKeys;
    bool bColumnAxisLinked = false;
};


/** Every row of ElementChartColumn. */
class MATRIXDATA_API FElementChartColumnTable
{
public:
    const TArray<FElementChartColumnRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Defender, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FElementChartColumnRow* FindByDefender(EAffinity Key) const;

    /** Whether the table holds a row with this Defender. */
    bool ContainsDefender(EAffinity Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FElementChartColumnRow> RecordsStorage;
    TMap<EAffinity, int32> ByDefender;
};


/**
 * Every table, loaded together.
 *
 * A static class rather than a UObject: the data outlives any world, is read once at
 * startup and never mutated, and nothing here needs reflection or garbage collection.
 */
class MATRIXDATA_API FMatrixData
{
public:
    static const FGoodsTable& Goods() { return GoodsStorage; }
    static const FTownPriceTable& TownPrice() { return TownPriceStorage; }
    static const FTownPriceColumnTable& TownPriceColumn() { return TownPriceColumnStorage; }
    static const FElementChartTable& ElementChart() { return ElementChartStorage; }
    static const FElementChartColumnTable& ElementChartColumn() { return ElementChartColumnStorage; }

    /**
     * Reads every table from BasePath. Returns false if any of them could not be read.
     *
     * FileExtension defaults to what the recipe told the exporter to write. Pass a different
     * one when the data files were renamed on the way into the package - which they often are,
     * since some platforms only cook what carries an extension they recognise.
     */
    static bool ReadAll(const FString& BasePath,
                        const FString& FileExtension = TEXT(".tcb"));

    /**
     * The key the table files were sealed with, or empty when they were not sealed.
     *
     * Beside ReadAll because it is the same kind of thing: one decision the project makes
     * once, at start-up, before the first table is read. A key threaded through every Read
     * signature would be the same value written out at every call site, and every call site
     * is somewhere it could be wrong.
     *
     * Thirty-two bytes, the key the converter was given. Empty - the default - is a project
     * that does not encrypt, and it costs that project nothing: the load path opens the
     * envelope either way, and a file that is not encrypted comes back from that unchanged.
     * A project that does encrypt and leaves this empty gets the reader's own message saying
     * the file is encrypted and no key was given, rather than a failure somewhere inside the
     * format.
     *
     * Where the key comes from is the one thing about this layer worth being careful with.
     * It is a secret only until it reaches a repository, a build log or an issue, and it
     * cannot be taken back afterwards - so it is read at start-up from wherever this project
     * keeps secrets, not written into a source file or a .ini the packaging step will cook.
     * What the layer is for, and therefore what is and is not worth defending, is
     * spec/wire/tcb-v104-composed-encodings.md section 4.
     */
    static TArray<uint8> EncryptionKey;

    /**
     * The key the table files were signed with, or empty when they were not signed.
     *
     * A different key from EncryptionKey and independent of it: a file can be signed without
     * being encrypted, and encrypted without being signed.
     *
     * What it buys is the thing encryption does not. The cipher is a keystream XOR, so a bit
     * flipped in the file is a bit flipped in the value it decrypts to - no key needed - and
     * every structural check the format makes still passes, because a four-byte float
     * accepts any four bytes. With this set, a file altered after export stops loading.
     *
     * Setting it makes the reader stricter in a second way: a file that carries no MAC is
     * refused, because otherwise the check could be removed by zeroing sixteen bytes. So the
     * order to turn this on in is data first, key second. spec/wire/tcb-mac-and-signature.md.
     */
    static TArray<uint8> MacKey;

    /**
     * Whether to check the MAC at all. True by default.
     *
     * For tools that read exported files without the key, and for measuring what the check
     * costs at load. It is not a security boundary - anyone who can flip a flag in a shipped
     * build can read MacKey out of the same build.
     */
    static bool bVerifyMac;

private:
    static FGoodsTable GoodsStorage;
    static FTownPriceTable TownPriceStorage;
    static FTownPriceColumnTable TownPriceColumnStorage;
    static FElementChartTable ElementChartStorage;
    static FElementChartColumnTable ElementChartColumnStorage;
};

/**
 * The same data, reachable from Blueprint.
 *
 * Every row above is a USTRUCT marked BlueprintType with BlueprintReadOnly properties,
 * which says the rows are meant to be used from Blueprint - and until this existed there
 * was no way to get one. A graph could declare a variable of a row type and had nothing to
 * obtain a value from, because the accessor is a plain C++ class and a static method on one
 * is not something Blueprint can call.
 *
 * A function library rather than making the accessor a UObject. The storage stays where it
 * is: this owns nothing, adds no garbage collection, and does not need a world. It is a
 * few UFUNCTIONs over the C++ that was already there.
 */
UCLASS()
class MATRIXDATA_API UMatrixDataLibrary : public UBlueprintFunctionLibrary
{
    GENERATED_BODY()

public:

    /**
     * The Goods row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Goods",
              meta = (DisplayName = "Get Goods Row"))
    static FGoodsRow GetGoodsRow(int32 Key, bool& bFound);

    /** How many Goods rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Goods",
              meta = (DisplayName = "Get Goods Row Count"))
    static int32 GetGoodsRowCount();

    /**
     * The Goods row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Goods",
              meta = (DisplayName = "Get Goods Row At"))
    static FGoodsRow GetGoodsRowAt(int32 Position, bool& bFound);

    /**
     * The TownPrice row with the given Town.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|TownPrice",
              meta = (DisplayName = "Get TownPrice Row"))
    static FTownPriceRow GetTownPriceRow(int32 Key, bool& bFound);

    /** How many TownPrice rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|TownPrice",
              meta = (DisplayName = "Get TownPrice Row Count"))
    static int32 GetTownPriceRowCount();

    /**
     * The TownPrice row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|TownPrice",
              meta = (DisplayName = "Get TownPrice Row At"))
    static FTownPriceRow GetTownPriceRowAt(int32 Position, bool& bFound);

    /**
     * The TownPriceColumn row with the given Goods.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|TownPriceColumn",
              meta = (DisplayName = "Get TownPriceColumn Row"))
    static FTownPriceColumnRow GetTownPriceColumnRow(int32 Key, bool& bFound);

    /** How many TownPriceColumn rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|TownPriceColumn",
              meta = (DisplayName = "Get TownPriceColumn Row Count"))
    static int32 GetTownPriceColumnRowCount();

    /**
     * The TownPriceColumn row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|TownPriceColumn",
              meta = (DisplayName = "Get TownPriceColumn Row At"))
    static FTownPriceColumnRow GetTownPriceColumnRowAt(int32 Position, bool& bFound);

    /**
     * The ElementChart row with the given Attacker.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ElementChart",
              meta = (DisplayName = "Get ElementChart Row"))
    static FElementChartRow GetElementChartRow(EAffinity Key, bool& bFound);

    /** How many ElementChart rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ElementChart",
              meta = (DisplayName = "Get ElementChart Row Count"))
    static int32 GetElementChartRowCount();

    /**
     * The ElementChart row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ElementChart",
              meta = (DisplayName = "Get ElementChart Row At"))
    static FElementChartRow GetElementChartRowAt(int32 Position, bool& bFound);

    /**
     * The ElementChartColumn row with the given Defender.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ElementChartColumn",
              meta = (DisplayName = "Get ElementChartColumn Row"))
    static FElementChartColumnRow GetElementChartColumnRow(EAffinity Key, bool& bFound);

    /** How many ElementChartColumn rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ElementChartColumn",
              meta = (DisplayName = "Get ElementChartColumn Row Count"))
    static int32 GetElementChartColumnRowCount();

    /**
     * The ElementChartColumn row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ElementChartColumn",
              meta = (DisplayName = "Get ElementChartColumn Row At"))
    static FElementChartColumnRow GetElementChartColumnRowAt(int32 Position, bool& bFound);

    /**
     * Reads every table from BasePath, as FMatrixData::ReadAll does.
     *
     * BlueprintCallable rather than BlueprintPure: it does something.
     */
    UFUNCTION(BlueprintCallable, Category = "Tabbit",
              meta = (DisplayName = "Load All Tabbit Tables"))
    static bool ReadAll(const FString& BasePath,
                        const FString& FileExtension = TEXT(".tcb"));
};
