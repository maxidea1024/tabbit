/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef MATRIX_TOWN_PRICE_COLUMN_H
#define MATRIX_TOWN_PRICE_COLUMN_H

#include "tabbit/tabbit_tcb_reader.h"

#include "Matrix_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Grids : B2
 *
 * Price modifier by town and goods.
 */
struct Matrix_TownPriceColumnRecord_t {
  int32_t goods;
  const Matrix_GoodsRecord_t* goods_by_goods;
  int32_t at;
};

/* Every row of TownPriceColumn.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Matrix_TownPriceColumnTable_t {
  Matrix_TownPriceColumnRecord_t* records;
  int32_t count;

  tb_index_entry* by_goods;
  tb_arena arena;
} Matrix_TownPriceColumnTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Matrix_TownPriceColumnLoad(Matrix_TownPriceColumnTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Matrix_TownPriceColumnFree(Matrix_TownPriceColumnTable_t* table);

/* The row with the given Goods, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Matrix_TownPriceColumnRecord_t* Matrix_TownPriceColumnFindByGoods(
  const Matrix_TownPriceColumnTable_t* table, int32_t key);

/* Whether the table holds a row with this Goods. */
bool Matrix_TownPriceColumnContainsGoods(
  const Matrix_TownPriceColumnTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* MATRIX_TOWN_PRICE_COLUMN_H */
