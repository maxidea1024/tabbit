/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef MATRIX_ELEMENT_CHART_COLUMN_H
#define MATRIX_ELEMENT_CHART_COLUMN_H

#include "tabbit/tabbit_tcb_reader.h"

#include "Matrix_Forward.h"
#include "enums/Matrix_EnumAffinity.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Grids : B11
 *
 * How much an element does to another.
 */
struct Matrix_ElementChartColumnRecord_t {
  Matrix_Affinity_t defender;
  int32_t at;
};

/* Every row of ElementChartColumn.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Matrix_ElementChartColumnTable_t {
  Matrix_ElementChartColumnRecord_t* records;
  int32_t count;

  tb_index_entry* by_defender;
  tb_arena arena;
} Matrix_ElementChartColumnTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Matrix_ElementChartColumnLoad(Matrix_ElementChartColumnTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Matrix_ElementChartColumnFree(Matrix_ElementChartColumnTable_t* table);

/* The row with the given Defender, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Matrix_ElementChartColumnRecord_t* Matrix_ElementChartColumnFindByDefender(
  const Matrix_ElementChartColumnTable_t* table, Matrix_Affinity_t key);

/* Whether the table holds a row with this Defender. */
bool Matrix_ElementChartColumnContainsDefender(
  const Matrix_ElementChartColumnTable_t* table, Matrix_Affinity_t key);

#ifdef __cplusplus
}
#endif

#endif /* MATRIX_ELEMENT_CHART_COLUMN_H */
