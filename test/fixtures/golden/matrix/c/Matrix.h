/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef MATRIX_H
#define MATRIX_H

#include "enums/Matrix_EnumAffinity.h"
#include "tables/Matrix_Goods.h"
#include "tables/Matrix_TownPrice.h"
#include "tables/Matrix_TownPriceColumn.h"
#include "tables/Matrix_ElementChart.h"
#include "tables/Matrix_ElementChartColumn.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Every table, loaded together so cross-table references can be resolved. */
typedef struct Matrix_t {
  Matrix_GoodsTable_t goods;
  Matrix_TownPriceTable_t town_price;
  Matrix_TownPriceColumnTable_t town_price_column;
  Matrix_ElementChartTable_t element_chart;
  Matrix_ElementChartColumnTable_t element_chart_column;
} Matrix_t;

/* Reads every table from base_path, then links the references between them.
 *
 * Declare the struct zeroed - `Matrix_t data = {0};` - and this is safe to
 * call again on it afterwards. That is what a reload is: every file is read beside what the
 * struct already holds and swapped in at the end, so the pointers a caller is holding stay
 * valid until this returns true, and stay valid for the previous load if it returns false.
 *
 * Returns false if any table could not be read, having released whatever had been loaded up
 * to that point and left `data` alone - a half-loaded model is not one to hand back. */
bool Matrix_LoadAll(Matrix_t* data, const char* base_path,
                 char* error, size_t error_size);

/* The same, for data files carrying some other extension than the one the recipe told
 * the exporter to write. `file_extension` includes the dot. */
bool Matrix_LoadAllWithExtension(Matrix_t* data, const char* base_path,
                 const char* file_extension, char* error, size_t error_size);

void Matrix_Free(Matrix_t* data);

#ifdef __cplusplus
}
#endif

#endif /* MATRIX_H */
