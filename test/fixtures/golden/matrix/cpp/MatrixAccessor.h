// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_MATRIXACCESSOR_H
#define TABBIT_GENERATED_MATRIXACCESSOR_H

#include <cstddef>
#include <string>

#include "enums/MatrixAccessor_enum_affinity.h"
#include "tables/MatrixAccessor_goods.h"
#include "tables/MatrixAccessor_town_price.h"
#include "tables/MatrixAccessor_town_price_column.h"
#include "tables/MatrixAccessor_element_chart.h"
#include "tables/MatrixAccessor_element_chart_column.h"
/// Every table, loaded together so cross-table references can be resolved.
class MatrixAccessor {
 public:
  const GoodsTable& goods() const { return goods_; }
  const TownPriceTable& town_price() const { return town_price_; }
  const TownPriceColumnTable& town_price_column() const { return town_price_column_; }
  const ElementChartTable& element_chart() const { return element_chart_; }
  const ElementChartColumnTable& element_chart_column() const { return element_chart_column_; }

  /// Reads every table from `base_path`, then links the references between them.
  ///
  /// Safe to call on a loaded accessor. Every file is read into a table of its own and the
  /// references are linked among those, so a read that throws part way through leaves every
  /// table holding the load it already had, and no row points at a row from it.
  void read_all(const std::string& base_path, const std::string& file_extension = ".tcb") {
    GoodsTable loaded_goods;
    loaded_goods.read(base_path + "/Goods" + file_extension);
    TownPriceTable loaded_town_price;
    loaded_town_price.read(base_path + "/TownPrice" + file_extension);
    TownPriceColumnTable loaded_town_price_column;
    loaded_town_price_column.read(base_path + "/TownPriceColumn" + file_extension);
    ElementChartTable loaded_element_chart;
    loaded_element_chart.read(base_path + "/ElementChart" + file_extension);
    ElementChartColumnTable loaded_element_chart_column;
    loaded_element_chart_column.read(base_path + "/ElementChartColumn" + file_extension);

    solve_cross_references(loaded_goods, loaded_town_price, loaded_town_price_column, loaded_element_chart, loaded_element_chart_column);
    loaded_town_price.link_column_axis(loaded_town_price_column);
    loaded_element_chart.link_column_axis(loaded_element_chart_column);

    goods_ = std::move(loaded_goods);
    town_price_ = std::move(loaded_town_price);
    town_price_column_ = std::move(loaded_town_price_column);
    element_chart_ = std::move(loaded_element_chart);
    element_chart_column_ = std::move(loaded_element_chart_column);
  }

 private:
  /// The tables arrive as arguments, which is how this links the load being read rather
  /// than the one already published.
  ///
  /// Every table is passed whether or not it takes part in a reference, so the call site
  /// is the same shape for every model. `[[maybe_unused]]` is what says so to the
  /// compiler: the gate builds with `-Wextra -Werror`, and a model where nothing
  /// references anything - which is most of them - otherwise fails to compile on the
  /// unused parameters.
  void solve_cross_references([[maybe_unused]] GoodsTable& loaded_goods, [[maybe_unused]] TownPriceTable& loaded_town_price, [[maybe_unused]] TownPriceColumnTable& loaded_town_price_column, [[maybe_unused]] ElementChartTable& loaded_element_chart, [[maybe_unused]] ElementChartColumnTable& loaded_element_chart_column) {
    for (auto& record : loaded_town_price_column.records_) {
      {
        const auto* target = loaded_goods.find_by_id(record.goods);
        if (target != nullptr) record.goods_by_goods = target;
      }
    }
  }

  GoodsTable goods_;
  TownPriceTable town_price_;
  TownPriceColumnTable town_price_column_;
  ElementChartTable element_chart_;
  ElementChartColumnTable element_chart_column_;
};

#endif  // TABBIT_GENERATED_MATRIXACCESSOR_H
