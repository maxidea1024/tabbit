// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Tabbit.Fixtures.Matrix
{
    [System.Serializable]
    public partial class TownPriceRecord
    {
        #region Values
        /// <summary>
        /// which town
        /// </summary>
        public int Town => _town;

        /// <summary>
        /// the modifier
        /// </summary>
        public int[] Price => _price;
        /// <summary>Whether element <paramref name="index"/> of <see cref="Price"/> has a value.</summary>
        public bool HasPriceAt(int index)
            => _priceHasValueAt == null
                || index < 0 || index >= _priceHasValueAt.Length
                || _priceHasValueAt[index];
        #endregion

        #region Storage
        internal int _town;
        internal int[] _price = System.Array.Empty<int>();
        internal bool[] _priceHasValueAt;
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"Town\":"); ToStringHelper.ToString(Town, sb);
            sb.Append(",\"Price\":"); ToStringHelper.ToString(Price, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// Price modifier by town and goods.
    /// </summary>
    [System.Serializable]
    public partial class TownPriceTable : IEnumerable<TownPriceRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Town", "Price" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Town, r.Price });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<TownPriceRecord> Records => _records;
        private List<TownPriceRecord> _records = new List<TownPriceRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<TownPriceRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<TownPriceRecord> IEnumerable<TownPriceRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'Town'
        public Dictionary<int, TownPriceRecord> RecordsByTown => _recordsByTown;
        private Dictionary<int, TownPriceRecord> _recordsByTown = new Dictionary<int, TownPriceRecord>();

        /// <summary>
        /// The row with this `Town`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public TownPriceRecord FindByTown(int key)
            => _recordsByTown.TryGetValue(key, out TownPriceRecord record) ? record : null;

        /// <summary>
        /// The row with this `Town`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByTown(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public TownPriceRecord GetByTownOrThrow(int key)
        {
            if (!_recordsByTown.TryGetValue(key, out TownPriceRecord record))
                throw new TabbitException($"There is no record in table `TownPrice` that corresponds to field `Town` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Town`.</summary>
        public bool ContainsTown(int key) => _recordsByTown.ContainsKey(key);
        #endregion // Indexing by `Town`

        /// <summary>Each row with the `Town` it is keyed by.</summary>
        /// <remarks>
        /// `foreach (var (key, row) in table.Entries)`. What this saves a caller is not the
        /// key value - the row carries it - but having to know which column the key is:
        /// `Town` here, something else in the next table.
        ///
        /// A struct that enumerates itself, so the loop allocates nothing, and the rows in
        /// the order the file wrote them rather than the order a dictionary happens to hold
        /// them - which makes this and a plain `foreach` over the table agree.
        ///
        /// Only the primary key gets this. A table keyed by several columns together has no
        /// single key value to pair a row with.
        /// </remarks>
        public struct EntryEnumerator
        {
            private readonly List<TownPriceRecord> _rows;
            private int _at;

            internal EntryEnumerator(List<TownPriceRecord> rows)
            {
                _rows = rows;
                _at = -1;
            }

            public EntryEnumerator GetEnumerator() => this;

            public bool MoveNext() => ++_at < _rows.Count;

            public (int Key, TownPriceRecord Row) Current
                => (_rows[_at].Town, _rows[_at]);
        }

        /// <summary>Each row with the `Town` it is keyed by.</summary>
        public EntryEnumerator Entries => new EntryEnumerator(_records);

        /// <summary>
        /// The row with this `Town`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// `GetByTownOrThrow` under the spelling the language uses for a
        /// keyed collection. It throws because that is what `Dictionary` does here; a
        /// language whose own map answers with null generates a subscript that answers with
        /// null. spec/targets/table-collection-surface.md section 5.7.
        ///
        /// **This is the key, not the row's position.** `table[0]` is the row whose
        /// `Town` is 0, not the first row - the rows in order are
        /// `Records`. Nothing in the type says which, which is why the table itself is not
        /// subscriptable by position anywhere.
        ///
        /// It does not replace `FindByTown`: a key that may be absent
        /// wants the one whose name says a miss is an ordinary answer.
        /// </remarks>
        public TownPriceRecord this[int key] => GetByTownOrThrow(key);

        #region Grid of 'Town' by 'Goods'
        private TownPriceColumnTable _columnAxis;

        /// <summary>
        /// Hands this table the column axis its cells are positioned by.
        /// </summary>
        /// <remarks>
        /// Called by the accessor once both files are read, and **the length check is here**
        /// rather than in the read. A grid is two files, either can be deployed without the
        /// other, and a row whose array is a different length from the axis is a row whose
        /// cells have moved - which reads the wrong cell and reports nothing. This is what
        /// turns that into a failure at load.
        /// </remarks>
        public void LinkColumnAxis(TownPriceColumnTable columns)
        {
            foreach (var record in _records)
            {
                if (record.Price.Length == columns.Records.Count)
                    continue;

                throw new TabbitException(
                    $"Table `TownPrice` holds {record.Price.Length} cells for `{record.Town}` and its column axis `TownPriceColumn` holds {columns.Records.Count} keys. The two files are from different builds.");
            }

            _columnAxis = columns;
        }

        /// <summary>The row axis keys, in the order the file wrote them.</summary>
        public IEnumerable<int> RowKeys
        {
            get
            {
                foreach (var record in _records)
                    yield return record.Town;
            }
        }

        /// <summary>The column axis keys, in the order a row holds its cells.</summary>
        public IEnumerable<int> ColKeys
        {
            get
            {
                if (_columnAxis == null)
                    yield break;

                foreach (var record in _columnAxis.Records)
                    yield return record.Goods;
            }
        }

        /// <summary>
        /// One row of cells, or null when the grid has no such row.
        /// </summary>
        /// <remarks>
        /// In the order <see cref="ColKeys"/> is in, which is what makes a position mean
        /// something. The array is the record's own, so it is not a copy.
        /// </remarks>
        public int[] Row(int town)
        {
            var record = FindByTown(town);
            return record == null ? null : record.Price;
        }

        /// <summary>
        /// The cell where the two keys meet, or a thrown exception naming the one that missed.
        /// </summary>
        /// <remarks>
        /// It throws rather than answering with the type's empty value, because a grid's empty
        /// value is a value the sheet can write - `0` in a modifier table is a modifier of
        /// zero, and a key nobody declared is not that.
        /// </remarks>
        public int At(int town, int goods)
        {
            if (_columnAxis == null)
                throw new TabbitException("Table `TownPrice` has no column axis yet. Read `TownPriceColumn` before reading a cell.");

            var record = FindByTown(town);

            if (record == null)
                throw new TabbitException($"Table `TownPrice` has no row `{town}`.");

            var column = _columnAxis.FindByGoods(goods);

            if (column == null)
                throw new TabbitException($"Table `TownPrice` has no column `{goods}`.");

            return record.Price[column.At];
        }

        /// <summary>
        /// Whether the sheet gave the cell where the two keys meet a value.
        /// </summary>
        /// <remarks>
        /// A grid whose cells are optional reads a blank as the type's empty value, and that
        /// value is one the sheet could also have written - so this is the only way to tell
        /// "nobody filled it in" from "somebody wrote zero". Both keys have to be there;
        /// a key that is not is the same report `At` makes.
        /// </remarks>
        public bool HasAt(int town, int goods)
        {
            if (_columnAxis == null)
                throw new TabbitException("Table `TownPrice` has no column axis yet. Read `TownPriceColumn` before reading a cell.");

            var record = FindByTown(town);

            if (record == null)
                throw new TabbitException($"Table `TownPrice` has no row `{town}`.");

            var column = _columnAxis.FindByGoods(goods);

            if (column == null)
                throw new TabbitException($"Table `TownPrice` has no column `{goods}`.");

            return record.HasPriceAt(column.At);
        }
        #endregion

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await MatrixAccessor.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `MatrixAccessor.EncryptionKey` and `MatrixAccessor.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, MatrixAccessor.EncryptionKey, MatrixAccessor.MacKey, MatrixAccessor.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] elementPresence;
            int elementAt;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<TownPriceRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new TownPriceRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "TownPrice.Town", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "TownPrice.Town");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._town = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "TownPrice.Price", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint, elementNullable: true);
                        elementPresence = TcbTable.ReadElementPresence(reader, column);
                        elementAt = 0;
                        cursor = new TcbColumnCursor(reader, column, count, "TownPrice.Price");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._price = new int[elementCount];
                            record._priceHasValueAt = new bool[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._priceHasValueAt[j] =
                                    TcbTable.IsPresent(elementPresence, elementAt++);
                                record._price[j] = cursor.NextI32();
                            }
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByTown = new Dictionary<int, TownPriceRecord>(count);
            foreach (var record in records)
                recordsByTown.Add(record.Town, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByTown = recordsByTown;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Tabbit.Fixtures.Matrix
