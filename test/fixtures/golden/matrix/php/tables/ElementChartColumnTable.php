<?php

// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

declare(strict_types=1);

namespace Tabbit\Fixtures\Matrix;

require_once __DIR__ . '/../tabbit/TcbReader.php';
require_once __DIR__ . '/../enums/Affinity.php';
require_once __DIR__ . '/../MatrixAccessor.php';

use Tabbit\TcbReader;
use Tabbit\TcbColumnCursor;
use Tabbit\RecordNotFoundException;
use Tabbit\Uuid;

/**
 * Generated from test/fixtures/xlsx/matrix/matrix.xlsx : Grids : B11
 *
 * How much an element does to another.
 */
final class ElementChartColumnRecord
{
    public Affinity $defender = Affinity::None;
    public int $at = 0;
}

/** Every row of ElementChartColumn. */
final class ElementChartColumnTable implements \Countable, \IteratorAggregate, \ArrayAccess
{
    /** @var list<ElementChartColumnRecord> */
    public array $records = [];

    /** How many rows the table holds. */
    public function count(): int
    {
        return count($this->records);
    }

    /**
     * The rows, in the order the file wrote them.
     *
     * The array is read once, here. A refresh replaces it rather than its contents, so a
     * loop already running keeps the rows it started with.
     *
     * @return \Traversable<int, ElementChartColumnRecord>
     */
    public function getIterator(): \Traversable
    {
        return new \ArrayIterator($this->records);
    }

    /** @var array<int, ElementChartColumnRecord> */
    private array $byDefender = [];

    /**
     * The row with this Defender, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findByDefender(Affinity $key): ?ElementChartColumnRecord
    {
        return $this->byDefender[$key->value] ?? null;
    }

    /**
     * The row with this Defender, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByDefender($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getByDefenderOrThrow(Affinity $key): ElementChartColumnRecord
    {
        $record = $this->byDefender[$key->value] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `ElementChartColumn` that corresponds to " .
                "field `Defender` value " . $key->value . ".");
        }

        return $record;
    }

    /** Whether the table holds a row with this Defender. */
    public function containsDefender(Affinity $key): bool
    {
        return isset($this->byDefender[$key->value]);
    }


    /**
     * Whether the table holds a row with this Defender - `isset($table[$key])`.
     */
    public function offsetExists(mixed $offset): bool
    {
        return $this->containsDefender($offset);
    }

    /**
     * The row with this Defender, or null when the table has none.
     *
     * findByDefender under the spelling PHP uses for a keyed collection. It
     * answers with null because that is what array access does here; a language whose own
     * map throws generates a subscript that throws.
     * spec/targets/table-collection-surface.md section 5.7.
     *
     * This is the key, not the row's position. `$table[0]` is the row whose
     * Defender is 0, not the first row - the rows in order are ->records.
     *
     * Only a key of one column has this: PHP's array access takes one offset.
     */
    public function offsetGet(mixed $offset): ?ElementChartColumnRecord
    {
        return $this->findByDefender($offset);
    }

    /** The table is read-only: a row comes from the file, not from an assignment. */
    public function offsetSet(mixed $offset, mixed $value): void
    {
        throw new \LogicException('ElementChartColumnTable is read-only.');
    }

    /** The table is read-only: a row comes from the file, not from an assignment. */
    public function offsetUnset(mixed $offset): void
    {
        throw new \LogicException('ElementChartColumnTable is read-only.');
    }


    /**
     * Each row with the Defender it is keyed by -
     * `foreach ($table->entries() as $key => $row)`.
     *
     * What this saves a caller is not the key value - the row carries it - but having to
     * know which column the key is: Defender here, something else in the next
     * table.
     *
     * The rows come in the order the file wrote them rather than the order the map holds
     * them, which makes this and iterating the table agree. Only the primary key has this:
     * a table keyed by several columns together has no single key value to pair a row with.
     *
     * @return \Generator<int, ElementChartColumnRecord>
     */
    public function entries(): \Generator
    {
        foreach ($this->records as $record) {
            yield $record->defender => $record;
        }
    }

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    public function read(string $filename): void
    {
        // The keys go to fromFile because reading and opening are one call here: the
        // reader's constructor takes the whole file and opens the envelope itself. It does
        // so unconditionally, and with whatever MatrixAccessor::$encryptionKey and
        // MatrixAccessor::$macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from that untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/wire/tcb-mac-and-signature.md.
        $reader = TcbReader::fromFile(
            $filename, MatrixAccessor::$encryptionKey,
            MatrixAccessor::$macKey, MatrixAccessor::$verifyMac);
        [$count, $columns] = $reader->readTableHeader();

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        $records = [];
        $byDefender = [];

        for ($i = 0; $i < $count; $i++) {
            $records[] = new ElementChartColumnRecord();
        }

        foreach ($columns as $column) {
            $blockEnd = $reader->position() + $column['byteLength'];

            switch ($column['tag']) {
                case 1:
                    TcbReader::checkColumn($column, 'ElementChartColumn.Defender', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_VARINT]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'ElementChartColumn.Defender');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameI32($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->defender = Affinity::tryFrom($value) ?? Affinity::None;
                        }
                    }
                    break;

                case 2:
                    TcbReader::checkColumn($column, 'ElementChartColumn.At', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_I32, TcbReader::ELEMENT_VARINT]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'ElementChartColumn.At');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameI32($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->at = $value;
                        }
                    }
                    break;

                default:
                    // A column added after this code was generated.
                    $reader->skip($column['byteLength']);
                    break;
            }

            $reader->checkBlockEnd($column, $blockEnd);
        }

        foreach ($records as $record) {
            $byDefender[$record->defender->value] = $record;
        }

        // Published, now that every column read.
        $this->records = $records;
        $this->byDefender = $byDefender;
    }
}
