// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { Tables } from '../tables'

/** One element of VectorsRecord.pos. */
export interface PosEntry {
  /** where it is */
  x: number
  /** where it is */
  y: number
  /** where it is */
  z: number
}

/** PosEntry as the .json export carries it. */
interface PosEntryJson {
  x: number
  y: number
  z: number
}

/** One element of VectorsRecord.cell. */
export interface CellEntry {
  /** which tile */
  x: number
  /** which tile */
  y: number
}

/** CellEntry as the .json export carries it. */
interface CellEntryJson {
  x: number
  y: number
}

/** One element of VectorsRecord.rot. */
export interface RotEntry {
  /** how it is turned */
  x: number
  /** how it is turned */
  y: number
  /** how it is turned */
  z: number
  /** how it is turned */
  w: number
}

/** RotEntry as the .json export carries it. */
interface RotEntryJson {
  x: number
  y: number
  z: number
  w: number
}

/** One element of VectorsRecord.tint. */
export interface TintEntry {
  /** 8-bit colour */
  r: number
  /** 8-bit colour */
  g: number
  /** 8-bit colour */
  b: number
  /** 8-bit colour */
  a: number
}

/** TintEntry as the .json export carries it. */
interface TintEntryJson {
  r: number
  g: number
  b: number
  a: number
}

/** One element of VectorsRecord.glow. */
export interface GlowEntry {
  /** float colour */
  r: number
  /** float colour */
  g: number
  /** float colour */
  b: number
  /** float colour */
  a: number
}

/** GlowEntry as the .json export carries it. */
interface GlowEntryJson {
  r: number
  g: number
  b: number
  a: number
}

/** A type for handling rows when parsing .json. */
interface IDataRow {
  index: number
  pos: PosEntryJson
  cell: CellEntryJson
  rot: RotEntryJson
  tint: TintEntryJson
  glow: GlowEntryJson
  label: string
}

// Generated from test/fixtures/xlsx/composite/composite.xlsx : Vectors : B2
/** Vectors, a rotation and two colours, in one notation or the other. */
export class VectorsRecord {
  /** Default constructor */
  constructor() {
  }

  /** primary index */
  public get index(): number { return this._index }

  /** where it is */
  public get pos(): PosEntry { return this._pos }

  /** which tile */
  public get cell(): CellEntry { return this._cell }

  /** how it is turned */
  public get rot(): RotEntry { return this._rot }

  /** 8-bit colour */
  public get tint(): TintEntry { return this._tint }

  /** float colour */
  public get glow(): GlowEntry { return this._glow }

  /** a plain column after the components */
  public get label(): string { return this._label }

  public _index: number = 0
  public _pos: PosEntry = { x: 0, y: 0, z: 0 }
  public _cell: CellEntry = { x: 0, y: 0 }
  public _rot: RotEntry = { x: 0, y: 0, z: 0, w: 0 }
  public _tint: TintEntry = { r: 0, g: 0, b: 0, a: 0 }
  public _glow: GlowEntry = { r: 0, g: 0, b: 0, a: 0 }
  public _label: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._index = dataRow.index
    this._pos = ((e: any) => ({ x: Math.fround(e.x), y: Math.fround(e.y), z: Math.fround(e.z) }))(dataRow.pos)
    this._cell = ((e: any) => ({ x: e.x, y: e.y }))(dataRow.cell)
    this._rot = ((e: any) => ({ x: Math.fround(e.x), y: Math.fround(e.y), z: Math.fround(e.z), w: Math.fround(e.w) }))(dataRow.rot)
    this._tint = ((e: any) => ({ r: e.r, g: e.g, b: e.b, a: e.a }))(dataRow.tint)
    this._glow = ((e: any) => ({ r: Math.fround(e.r), g: Math.fround(e.g), b: Math.fround(e.b), a: Math.fround(e.a) }))(dataRow.glow)
    this._label = dataRow.label
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._index = dataRow[offset++]
    this._pos = { x: Math.fround(dataRow[offset++]), y: Math.fround(dataRow[offset++]), z: Math.fround(dataRow[offset++]) }
    this._cell = { x: dataRow[offset++], y: dataRow[offset++] }
    this._rot = { x: Math.fround(dataRow[offset++]), y: Math.fround(dataRow[offset++]), z: Math.fround(dataRow[offset++]), w: Math.fround(dataRow[offset++]) }
    this._tint = { r: dataRow[offset++], g: dataRow[offset++], b: dataRow[offset++], a: dataRow[offset++] }
    this._glow = { r: Math.fround(dataRow[offset++]), g: Math.fround(dataRow[offset++]), b: Math.fround(dataRow[offset++]), a: Math.fround(dataRow[offset++]) }
    this._label = dataRow[offset++]
  }
}

// Generated from test/fixtures/xlsx/composite/composite.xlsx : Vectors : B2
/** Vectors, a rotation and two colours, in one notation or the other. */
export class VectorsTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): VectorsRecord[] { return this._records }
  private _records: VectorsRecord[] = []

  // Indexing by 'index'
  public get recordsByIndex(): Map<number, VectorsRecord> { return this._recordsByIndex }
  private _recordsByIndex: Map<number, VectorsRecord> = new Map<number, VectorsRecord>()

  /**
   * The row with this index, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByIndex(key: number): VectorsRecord | undefined {
    return this._recordsByIndex.get(key)
  }

  /**
   * The row with this index, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByIndex(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIndexOrThrow(key: number): VectorsRecord {
    const found = this._recordsByIndex.get(key)
    if (!found)
      throw new Error(`There is no record in table "Vectors" that corresponds to field "index" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this index. */
  public containsIndex(key: number): boolean {
    return this._recordsByIndex.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: VectorsRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new VectorsRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new VectorsRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `Tables.encryptionKey` and `Tables.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      Tables.encryptionKey, Tables.macKey, Tables.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: VectorsRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new VectorsRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'Vectors.Index', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Index')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._index = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'Vectors.Pos.X', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Pos.X')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._pos.x = cursor.nextF32()
          }
          break
        case 3:
          tabbit.checkColumn(column, 'Vectors.Pos.Y', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Pos.Y')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._pos.y = cursor.nextF32()
          }
          break
        case 4:
          tabbit.checkColumn(column, 'Vectors.Pos.Z', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Pos.Z')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._pos.z = cursor.nextF32()
          }
          break
        case 5:
          tabbit.checkColumn(column, 'Vectors.Cell.X', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Cell.X')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._cell.x = value
          }
          break
        case 6:
          tabbit.checkColumn(column, 'Vectors.Cell.Y', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Cell.Y')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._cell.y = value
          }
          break
        case 7:
          tabbit.checkColumn(column, 'Vectors.Rot.X', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Rot.X')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rot.x = cursor.nextF32()
          }
          break
        case 8:
          tabbit.checkColumn(column, 'Vectors.Rot.Y', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Rot.Y')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rot.y = cursor.nextF32()
          }
          break
        case 9:
          tabbit.checkColumn(column, 'Vectors.Rot.Z', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Rot.Z')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rot.z = cursor.nextF32()
          }
          break
        case 10:
          tabbit.checkColumn(column, 'Vectors.Rot.W', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Rot.W')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rot.w = cursor.nextF32()
          }
          break
        case 11:
          tabbit.checkColumn(column, 'Vectors.Tint.R', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Tint.R')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._tint.r = value
          }
          break
        case 12:
          tabbit.checkColumn(column, 'Vectors.Tint.G', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Tint.G')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._tint.g = value
          }
          break
        case 13:
          tabbit.checkColumn(column, 'Vectors.Tint.B', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Tint.B')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._tint.b = value
          }
          break
        case 14:
          tabbit.checkColumn(column, 'Vectors.Tint.A', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Tint.A')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._tint.a = value
          }
          break
        case 15:
          tabbit.checkColumn(column, 'Vectors.Glow.R', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Glow.R')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._glow.r = cursor.nextF32()
          }
          break
        case 16:
          tabbit.checkColumn(column, 'Vectors.Glow.G', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Glow.G')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._glow.g = cursor.nextF32()
          }
          break
        case 17:
          tabbit.checkColumn(column, 'Vectors.Glow.B', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Glow.B')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._glow.b = cursor.nextF32()
          }
          break
        case 18:
          tabbit.checkColumn(column, 'Vectors.Glow.A', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Glow.A')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._glow.a = cursor.nextF32()
          }
          break
        case 19:
          tabbit.checkColumn(column, 'Vectors.Label', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Vectors.Label')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._label = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: VectorsRecord[]): void {
    const recordsByIndex = new Map<number, VectorsRecord>()

    for (const record of records) {
      recordsByIndex.set(record.index, record)
    }

    this._records = records
    this._recordsByIndex = recordsByIndex
  }
}
