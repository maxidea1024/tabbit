// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_COMPOSITEACCESSOR_VECTORS_H
#define TABBIT_GENERATED_COMPOSITEACCESSOR_VECTORS_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "CompositeAccessor_forward.h"

namespace tabbit_fixtures {
namespace composite {
// Generated from test/fixtures/xlsx/composite/composite.xlsx : Vectors : B2
/// One element of VectorsRecord::pos.
struct VectorsRecord_pos_entry {
  /// where it is
  float x = 0.0f;
  /// where it is
  float y = 0.0f;
  /// where it is
  float z = 0.0f;
};

/// One element of VectorsRecord::cell.
struct VectorsRecord_cell_entry {
  /// which tile
  std::int32_t x = 0;
  /// which tile
  std::int32_t y = 0;
};

/// One element of VectorsRecord::rot.
struct VectorsRecord_rot_entry {
  /// how it is turned
  float x = 0.0f;
  /// how it is turned
  float y = 0.0f;
  /// how it is turned
  float z = 0.0f;
  /// how it is turned
  float w = 0.0f;
};

/// One element of VectorsRecord::tint.
struct VectorsRecord_tint_entry {
  /// 8-bit colour
  std::int32_t r = 0;
  /// 8-bit colour
  std::int32_t g = 0;
  /// 8-bit colour
  std::int32_t b = 0;
  /// 8-bit colour
  std::int32_t a = 0;
};

/// One element of VectorsRecord::glow.
struct VectorsRecord_glow_entry {
  /// float colour
  float r = 0.0f;
  /// float colour
  float g = 0.0f;
  /// float colour
  float b = 0.0f;
  /// float colour
  float a = 0.0f;
};

/// Vectors, a rotation and two colours, in one notation or the other.
struct VectorsRecord {
  /// primary index
  std::int32_t index = 0;
  /// where it is
  VectorsRecord_pos_entry pos;
  /// which tile
  VectorsRecord_cell_entry cell;
  /// how it is turned
  VectorsRecord_rot_entry rot;
  /// 8-bit colour
  VectorsRecord_tint_entry tint;
  /// float colour
  VectorsRecord_glow_entry glow;
  /// a plain column after the components
  std::string label;
};

/// Vectors, a rotation and two colours, in one notation or the other.
class VectorsTable {
 public:
  const std::vector<VectorsRecord>& records() const { return records_; }

  /// The row with this Index, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const VectorsRecord* find_by_index(std::int32_t key) const {
    const auto it = by_index_.find(key);
    return it == by_index_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_index(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const VectorsRecord& get_by_index_or_throw(std::int32_t key) const {
    const VectorsRecord* found = find_by_index(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Vectors` that corresponds to "
                    "field `Index` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Index.
  bool contains_index(std::int32_t key) const {
    return by_index_.find(key) != by_index_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<VectorsRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Vectors.Index", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Index");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].index = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Vectors.Pos.X", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Pos.X");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.pos.x = cursor.next_f32();
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Vectors.Pos.Y", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Pos.Y");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.pos.y = cursor.next_f32();
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "Vectors.Pos.Z", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Pos.Z");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.pos.z = cursor.next_f32();
          }
          break;
        }
        case 5: {
          tabbit::check_column(column, "Vectors.Cell.X", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Cell.X");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].cell.x = value;
            }
          }
          break;
        }
        case 6: {
          tabbit::check_column(column, "Vectors.Cell.Y", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Cell.Y");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].cell.y = value;
            }
          }
          break;
        }
        case 7: {
          tabbit::check_column(column, "Vectors.Rot.X", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Rot.X");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.rot.x = cursor.next_f32();
          }
          break;
        }
        case 8: {
          tabbit::check_column(column, "Vectors.Rot.Y", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Rot.Y");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.rot.y = cursor.next_f32();
          }
          break;
        }
        case 9: {
          tabbit::check_column(column, "Vectors.Rot.Z", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Rot.Z");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.rot.z = cursor.next_f32();
          }
          break;
        }
        case 10: {
          tabbit::check_column(column, "Vectors.Rot.W", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Rot.W");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.rot.w = cursor.next_f32();
          }
          break;
        }
        case 11: {
          tabbit::check_column(column, "Vectors.Tint.R", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Tint.R");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].tint.r = value;
            }
          }
          break;
        }
        case 12: {
          tabbit::check_column(column, "Vectors.Tint.G", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Tint.G");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].tint.g = value;
            }
          }
          break;
        }
        case 13: {
          tabbit::check_column(column, "Vectors.Tint.B", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Tint.B");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].tint.b = value;
            }
          }
          break;
        }
        case 14: {
          tabbit::check_column(column, "Vectors.Tint.A", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Tint.A");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].tint.a = value;
            }
          }
          break;
        }
        case 15: {
          tabbit::check_column(column, "Vectors.Glow.R", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Glow.R");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.glow.r = cursor.next_f32();
          }
          break;
        }
        case 16: {
          tabbit::check_column(column, "Vectors.Glow.G", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Glow.G");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.glow.g = cursor.next_f32();
          }
          break;
        }
        case 17: {
          tabbit::check_column(column, "Vectors.Glow.B", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Glow.B");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.glow.b = cursor.next_f32();
          }
          break;
        }
        case 18: {
          tabbit::check_column(column, "Vectors.Glow.A", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Glow.A");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.glow.a = cursor.next_f32();
          }
          break;
        }
        case 19: {
          tabbit::check_column(column, "Vectors.Label", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Vectors.Label");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].label = value;
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class CompositeAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<VectorsRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> index_map;
    index_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      index_map.emplace(records[i].index, i);
    }

    records_ = std::move(records);
    by_index_ = std::move(index_map);
  }

  std::vector<VectorsRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_index_;
};

}  // namespace composite
}  // namespace tabbit_fixtures

#endif  // TABBIT_GENERATED_COMPOSITEACCESSOR_VECTORS_H
