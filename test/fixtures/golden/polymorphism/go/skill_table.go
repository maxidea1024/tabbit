// ------------------------------------------------------------------------------
// Code generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package gamedata

import (
	"fmt"

	"gamedata/tabbit"
)

// SkillEffectEntry is one element of SkillRecord.Effect.
type SkillEffectEntry struct {
	// which shape this row's effect is
	Type int32
	// How likely it is to land, in percent. Every variant carries it, so it is one column and
	// every row fills it.
	Chance int32
	// How much it takes.
	Damage int32
	// Whether it ignores armour.
	Pierces bool
	// Which element it deals, as a row of that catalogue.
	//
	// **A reference on a variant member is the shape a real project reaches for first** - "the
	// reward is an item, or a currency, or a monster" is that shape - and it is a different
	// path twice over: the blank cells of the other variants go through the reference
	// conversion, and the built variant has to carry the resolved row rather than the key.
	ElementId int32
	ElementByElementId *ElementRecord
	// How much it gives.
	Amount int32
	// How often it lands, as a band rather than a number.
	Band Band
}

// SkillRecord was generated from test/fixtures/xlsx/polymorphism/polymorphism.xlsx : Polymorphism : F2.
// Skills whose effect is one of several shapes.
type SkillRecord struct {
	// primary index
	Index int32
	// plain column, outside the group
	Name string
	// which shape this row's effect is
	effect SkillEffectEntry
}


// Effect is what this row's Effect is. Narrow it with a type
// switch: the interface is sealed, so the variants in this package are the whole list.
func (r *SkillRecord) Effect() Effect {
	return r.effectElement(&r.effect)
}

// effectElement builds one value from the entry the read filled.
func (r *SkillRecord) effectElement(
	entry *SkillEffectEntry) Effect {
	switch entry.Type {
	case 1:
		return DamageEffect{
			EffectBase: EffectBase{
				Chance: entry.Chance,
			},
			Damage: entry.Damage,
			Pierces: entry.Pierces,
			ElementId: entry.ElementId,
			ElementByElementId: entry.ElementByElementId,
		}
	case 2:
		return HealEffect{
			EffectBase: EffectBase{
				Chance: entry.Chance,
			},
			Amount: entry.Amount,
			Band: entry.Band,
		}
	case 3:
		return NoEffect{
			EffectBase: EffectBase{
				Chance: entry.Chance,
			},
		}
	}

	panic(fmt.Sprintf(
		"Effect: no variant is numbered %d", entry.Type))
}

// SkillTable holds every row of Skill.
type SkillTable struct {
	records []SkillRecord
	byIndex map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *SkillTable) Records() []SkillRecord { return t.records }

// FindByIndex returns the row with this Index, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *SkillTable) FindByIndex(key int32) *SkillRecord {
	position, found := t.byIndex[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIndexOrError returns the row with this Index, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *SkillTable) GetByIndexOrError(key int32) (*SkillRecord, error) {
	position, found := t.byIndex[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Skill` that corresponds to field `Index` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsIndex reports whether the table holds a row with this Index.
func (t *SkillTable) ContainsIndex(key int32) bool {
	_, found := t.byIndex[key]
	return found
}

// Read loads the table from a .tcb file written by Tabbit.
func (t *SkillTable) Read(filename string) error {
	data, err := tabbit.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	// Unconditionally, and with whatever EncryptionKey and MacKey hold - which is nil
	// unless the project set them. A file that is neither encrypted nor signed comes back
	// from this untouched, so the load path is the same either way and there is no
	// condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
	//
	// What comes back is a window onto data rather than a copy of it, which is why data
	// stays in scope for the whole read.
	opened, err := tabbit.Open(data, EncryptionKey, MacKey, VerifyMac)
	if err != nil {
		return err
	}

	reader := tabbit.NewReader(opened)
	count, columns := tabbit.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]SkillRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if tabbit.CheckColumn(reader, column, "Skill.Index", tabbit.KindScalar, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Skill.Index")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Index = value
						i++
					}
				}
			}
		case 2:
			if tabbit.CheckColumn(reader, column, "Skill.Name", tabbit.KindScalar, false, tabbit.ElementString) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Skill.Name")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameString(count - i)
					for ; n > 0; n-- {
						records[i].Name = value
						i++
					}
				}
			}
		case 3:
			if tabbit.CheckColumn(reader, column, "Skill.Effect.Type", tabbit.KindScalar, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Skill.Effect.Type")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].effect.Type = value
						i++
					}
				}
			}
		case 4:
			if tabbit.CheckColumn(reader, column, "Skill.Effect.Chance", tabbit.KindScalar, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Skill.Effect.Chance")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].effect.Chance = value
						i++
					}
				}
			}
		case 5:
			if tabbit.CheckColumn(reader, column, "Skill.Effect.Damage", tabbit.KindScalar, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Skill.Effect.Damage")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].effect.Damage = value
						i++
					}
				}
			}
		case 6:
			if tabbit.CheckColumn(reader, column, "Skill.Effect.Pierces", tabbit.KindScalar, false, tabbit.ElementBool) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Skill.Effect.Pierces")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.effect.Pierces = cursor.NextBool()
				}
			}
		case 7:
			if tabbit.CheckColumn(reader, column, "Skill.Effect.ElementId", tabbit.KindScalar, false, tabbit.ElementI32) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Skill.Effect.ElementId")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].effect.ElementId = value
						i++
					}
				}
			}
		case 8:
			if tabbit.CheckColumn(reader, column, "Skill.Effect.Amount", tabbit.KindScalar, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Skill.Effect.Amount")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].effect.Amount = value
						i++
					}
				}
			}
		case 9:
			if tabbit.CheckColumn(reader, column, "Skill.Effect.Band", tabbit.KindScalar, false, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Skill.Effect.Band")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].effect.Band = Band(value)
						i++
					}
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		tabbit.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Skill: %w", err)
	}

	byIndex := make(map[int32]int, len(records))
	for i := range records {
		byIndex[records[i].Index] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byIndex = byIndex

	return nil
}
