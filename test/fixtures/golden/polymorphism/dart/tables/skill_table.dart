// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
/// One element of [SkillRecord.effect].
class SkillEffectEntry {
  /// which shape this row's effect is
  int type = 0;
  /// How likely it is to land, in percent. Every variant carries it, so it is one column and
  /// every row fills it.
  int chance = 0;
  /// How much it takes.
  int damage = 0;
  /// Whether it ignores armour.
  bool pierces = false;
  /// Which element it deals, as a row of that catalogue.
  ///
  /// **A reference on a variant member is the shape a real project reaches for first** - "the
  /// reward is an item, or a currency, or a monster" is that shape - and it is a different
  /// path twice over: the blank cells of the other variants go through the reference
  /// conversion, and the built variant has to carry the resolved row rather than the key.
  int elementId = 0;
  ElementRecord? elementByElementId;
  /// How much it gives.
  int amount = 0;
  /// How often it lands, as a band rather than a number.
  Band band = Band.of(0);
}

// Generated from test/fixtures/xlsx/polymorphism/polymorphism.xlsx : Polymorphism : F2
/// Skills whose effect is one of several shapes.
class SkillRecord {
  /// primary index
  int index = 0;
  /// plain column, outside the group
  String name = '';
  /// which shape this row's effect is
  SkillEffectEntry effect = SkillEffectEntry();


  Effect? _effectValue;

  /// What this row's [Effect] is. `switch` over it is exhaustive.
  Effect get effectOf {
    final cached = _effectValue;
    if (cached != null) return cached;

    final built = _effectElement(effect);

    _effectValue = built;
    return built;
  }

  /// Builds one value from the entry the read filled.
  static Effect _effectElement(
      SkillEffectEntry entry) {
    final built = switch (entry.type) {
      1 => DamageEffect(
        chance: entry.chance,
        damage: entry.damage,
        pierces: entry.pierces,
        elementId: entry.elementId,
        elementByElementId: entry.elementByElementId,
      ),
      2 => HealEffect(
        chance: entry.chance,
        amount: entry.amount,
        band: entry.band,
      ),
      3 => NoEffect(
        chance: entry.chance,
      ),
      _ => throw StateError(
          'Effect: no variant is numbered ${ entry.type }'),
    };

    return built;
  }

}

/// Every row of Skill.
class SkillTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<SkillRecord> get records => _records;
  List<SkillRecord> _records = [];

  Map<int, SkillRecord> _byIndex = {};

  /// The row with this Index, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  SkillRecord? findByIndex(int key) => _byIndex[key];

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getByIndex(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  SkillRecord getByIndexOrThrow(int key) {
    final record = _byIndex[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `Skill` that corresponds to '
          'field `Index` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Index.
  bool containsIndex(int key) => _byIndex.containsKey(key);

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <SkillRecord>[];
    final loadedByIndex = <int, SkillRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(SkillRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'Skill.Index', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Skill.Index');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].index = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'Skill.Name', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'Skill.Name');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].name = value;
            }
          }
          break;
        case 3:
          checkColumn(column, 'Skill.Effect.Type', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Skill.Effect.Type');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].effect.type = value;
            }
          }
          break;
        case 4:
          checkColumn(column, 'Skill.Effect.Chance', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Skill.Effect.Chance');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].effect.chance = value;
            }
          }
          break;
        case 5:
          checkColumn(column, 'Skill.Effect.Damage', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Skill.Effect.Damage');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].effect.damage = value;
            }
          }
          break;
        case 6:
          checkColumn(column, 'Skill.Effect.Pierces', kindScalar, false, [elementBool]);
          cursor = TcbColumnCursor(reader, column, count, 'Skill.Effect.Pierces');
          for (final record in loaded) {
            record.effect.pierces = cursor.nextBool();
          }
          break;
        case 7:
          checkColumn(column, 'Skill.Effect.ElementId', kindScalar, false, [elementI32]);
          cursor = TcbColumnCursor(reader, column, count, 'Skill.Effect.ElementId');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].effect.elementId = value;
            }
          }
          break;
        case 8:
          checkColumn(column, 'Skill.Effect.Amount', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Skill.Effect.Amount');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].effect.amount = value;
            }
          }
          break;
        case 9:
          checkColumn(column, 'Skill.Effect.Band', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Skill.Effect.Band');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].effect.band = Band.of(value);
            }
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedByIndex[record.index] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byIndex = loadedByIndex;
  }
}
