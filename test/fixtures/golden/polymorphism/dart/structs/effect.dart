// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
/// One value of [Effect], whichever shape it took.
///
/// Narrow it with `switch`, which is exhaustive over a sealed class, or with `is`.
sealed class Effect {
  /// How likely it is to land, in percent. Every variant carries it, so it is one column and
  /// every row fills it.
  final int chance;

  const Effect({
    required this.chance,
  });
}

/// One shape of [Effect].
///
/// The file carries `1` for it. That number rather than the name is
/// what lets this variant be renamed without a deployed reader reading a different one.
final class DamageEffect extends Effect {
  /// How much it takes.
  final int damage;
  /// Whether it ignores armour.
  final bool pierces;

  const DamageEffect({
    required super.chance,
    required this.damage,
    required this.pierces,
  });
}

/// One shape of [Effect].
///
/// The file carries `2` for it. That number rather than the name is
/// what lets this variant be renamed without a deployed reader reading a different one.
final class HealEffect extends Effect {
  /// How much it gives.
  final int amount;

  const HealEffect({
    required super.chance,
    required this.amount,
  });
}

/// One shape of [Effect].
///
/// The file carries `3` for it. That number rather than the name is
/// what lets this variant be renamed without a deployed reader reading a different one.
final class NoEffect extends Effect {

  const NoEffect({
    required super.chance,
  });
}
