// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package gamedata;

import tabbit.TcbReader;

// Generated from test/fixtures/xlsx/polymorphism/polymorphism.xlsx : PolymorphicArray : B2
/** Skills whose effects are a list, each element its own shape. */
public final class ComboRecord {
    /** primary index */
    public int index;
    /** plain column, outside the group */
    public String name = "";
    /** which shape this element's effect is */
    public EffectsEntry[] effects = new EffectsEntry[0];

    /** One element of effects. */
    public static final class EffectsEntry {
        /** which shape this element's effect is */
        public int type;
        /** How likely it is to land, in percent. Every variant carries it, so it is one column and */
        /** every row fills it. */
        public int chance;
        /** How much it takes. */
        public int damage;
        /** Whether it ignores armour. */
        public boolean pierces;
        /** Which element it deals, as a row of that catalogue. */
        /**  */
        /** **A reference on a variant member is the shape a real project reaches for first** - "the */
        /** reward is an item, or a currency, or a monster" is that shape - and it is a different */
        /** path twice over: the blank cells of the other variants go through the reference */
        /** conversion, and the built variant has to carry the resolved row rather than the key. */
        public int elementId;
        public ElementRecord elementByElementId;
        /** How much it gives. */
        public int amount;
        /** How often it lands, as a band rather than a number. */
        public Band band = Band.of(0);
    }



    private java.util.List<Effect> effectsValue;

    /**
     * What this row's Effects are, one per element. Narrow each with
     * {@code instanceof}.
     */
    public java.util.List<Effect> effects() {
        if (effectsValue != null) {
            return effectsValue;
        }

        java.util.List<Effect> all =
            new java.util.ArrayList<>(effects.length);

        for (EffectsEntry element : effects) {
            all.add(effectsElement(element));
        }

        effectsValue = all;
        return all;
    }

    /** Builds one value from the entry the read filled. */
    private static Effect effectsElement(
            EffectsEntry entry) {
        switch (entry.type) {
            case 1: {
                Effect.DamageEffect built =
                    new Effect.DamageEffect();
                built.chance = entry.chance;
                built.damage = entry.damage;
                built.pierces = entry.pierces;
                built.elementId = entry.elementId;
                built.elementByElementId = entry.elementByElementId;
                return built;
            }
            case 2: {
                Effect.HealEffect built =
                    new Effect.HealEffect();
                built.chance = entry.chance;
                built.amount = entry.amount;
                built.band = entry.band;
                return built;
            }
            case 3: {
                Effect.NoEffect built =
                    new Effect.NoEffect();
                built.chance = entry.chance;
                return built;
            }
        }

        throw new IllegalStateException(
            "Effect: no variant is numbered " + entry.type);
    }
}
