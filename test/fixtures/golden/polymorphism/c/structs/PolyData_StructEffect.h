/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef POLY_DATA_STRUCT_EFFECT_H
#define POLY_DATA_STRUCT_EFFECT_H

#include <stdbool.h>
#include <stdint.h>
#include "enums/PolyData_EnumBand.h"
#include "PolyData_Forward.h"

/** Which shape a PolyData_StructEffect value took. */
typedef enum PolyData_StructEffectKind {
  /** The file carries 1 for this one. */
  POLY_DATA_STRUCT_EFFECT_KIND_DAMAGE_EFFECT = 1,
  /** The file carries 2 for this one. */
  POLY_DATA_STRUCT_EFFECT_KIND_HEAL_EFFECT = 2,
  /** The file carries 3 for this one. */
  POLY_DATA_STRUCT_EFFECT_KIND_NO_EFFECT = 3,
} PolyData_StructEffectKind;

/** One shape of PolyData_StructEffect. */
typedef struct PolyData_StructDamageEffect {
  /* How likely it is to land, in percent. Every variant carries it, so it is one column and */
  /* every row fills it. */
  int32_t chance;
  /* How much it takes. */
  int32_t damage;
  /* Whether it ignores armour. */
  bool pierces;
  /* Which element it deals, as a row of that catalogue. */
  /*  */
  /* **A reference on a variant member is the shape a real project reaches for first** - "the */
  /* reward is an item, or a currency, or a monster" is that shape - and it is a different */
  /* path twice over: the blank cells of the other variants go through the reference */
  /* conversion, and the built variant has to carry the resolved row rather than the key. */
  int32_t element_id;
  const PolyData_ElementRecord_t* element_by_element_id;
} PolyData_StructDamageEffect;

/** One shape of PolyData_StructEffect. */
typedef struct PolyData_StructHealEffect {
  /* How likely it is to land, in percent. Every variant carries it, so it is one column and */
  /* every row fills it. */
  int32_t chance;
  /* How much it gives. */
  int32_t amount;
  /* How often it lands, as a band rather than a number. */
  PolyData_Band_t band;
} PolyData_StructHealEffect;

/** One shape of PolyData_StructEffect. */
typedef struct PolyData_StructNoEffect {
  /* How likely it is to land, in percent. Every variant carries it, so it is one column and */
  /* every row fills it. */
  int32_t chance;
} PolyData_StructNoEffect;

#endif /* POLY_DATA_STRUCT_EFFECT_H */
