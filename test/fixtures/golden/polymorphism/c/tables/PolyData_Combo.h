/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef POLY_DATA_COMBO_H
#define POLY_DATA_COMBO_H

#include "tabbit/tabbit_tcb_reader.h"

#include "PolyData_Forward.h"
#include "enums/PolyData_EnumBand.h"
#include "structs/PolyData_StructEffect.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/polymorphism/polymorphism.xlsx : PolymorphicArray : B2
 *
 * Skills whose effects are a list, each element its own shape.
 */
/* One element of PolyData_ComboRecord_t::effects. */
struct PolyData_ComboRecord_t_effects_entry {
  /* which shape this element's effect is */
  int32_t type;
  /* How likely it is to land, in percent. Every variant carries it, so it is one column and */
  /* every row fills it. */
  int32_t chance;
  /* How much it takes. */
  int32_t damage;
  /* Whether it ignores armour. */
  bool pierces;
  /* Which element it deals, as a row of that catalogue. */
  /*  */
  /* **A reference on a variant member is the shape a real project reaches for first** - "the */
  /* reward is an item, or a currency, or a monster" is that shape - and it is a different */
  /* path twice over: the blank cells of the other variants go through the reference */
  /* conversion, and the built variant has to carry the resolved row rather than the key. */
  int32_t element_id; const PolyData_ElementRecord_t* element_by_element_id;
  /* How much it gives. */
  int32_t amount;
  /* How often it lands, as a band rather than a number. */
  PolyData_Band_t band;
};

struct PolyData_ComboRecord_t {
  /* primary index */
  int32_t index;
  /* plain column, outside the group */
  const char* name;
  /* which shape this element's effect is */
  struct PolyData_ComboRecord_t_effects_entry* effects;
  int32_t effects_count;
};

/* Every row of Combo.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct PolyData_ComboTable_t {
  PolyData_ComboRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} PolyData_ComboTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool PolyData_ComboLoad(PolyData_ComboTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void PolyData_ComboFree(PolyData_ComboTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const PolyData_ComboRecord_t* PolyData_ComboFindByIndex(
  const PolyData_ComboTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool PolyData_ComboContainsIndex(
  const PolyData_ComboTable_t* table, int32_t key);


/* How many PolyData_StructEffects this row has. */
size_t PolyData_ComboEffectsCount(
  const PolyData_ComboRecord_t* record);

/* Which shape one element's PolyData_StructEffect took. */
PolyData_StructEffectKind PolyData_ComboEffectsKind(
  const PolyData_ComboRecord_t* record, size_t at);

/* Fills `out` when that element is a PolyData_StructDamageEffect, and answers whether it was.
 *
 * The file carries 1 for that variant. */
bool PolyData_ComboEffectsAsDamageEffect(
  const PolyData_ComboRecord_t* record, size_t at, PolyData_StructDamageEffect* out);

/* Fills `out` when that element is a PolyData_StructHealEffect, and answers whether it was.
 *
 * The file carries 2 for that variant. */
bool PolyData_ComboEffectsAsHealEffect(
  const PolyData_ComboRecord_t* record, size_t at, PolyData_StructHealEffect* out);

/* Fills `out` when that element is a PolyData_StructNoEffect, and answers whether it was.
 *
 * The file carries 3 for that variant. */
bool PolyData_ComboEffectsAsNoEffect(
  const PolyData_ComboRecord_t* record, size_t at, PolyData_StructNoEffect* out);

#ifdef __cplusplus
}
#endif

#endif /* POLY_DATA_COMBO_H */
