/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef POLY_DATA_ELEMENT_H
#define POLY_DATA_ELEMENT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "PolyData_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/polymorphism/polymorphism.xlsx : Polymorphism : B2
 *
 * What a damaging effect is made of.
 */
struct PolyData_ElementRecord_t {
  /* primary index */
  int32_t code;
  /* what it is called */
  const char* name;
};

/* Every row of Element.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct PolyData_ElementTable_t {
  PolyData_ElementRecord_t* records;
  int32_t count;

  tb_index_entry* by_code;
  tb_arena arena;
} PolyData_ElementTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool PolyData_ElementLoad(PolyData_ElementTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void PolyData_ElementFree(PolyData_ElementTable_t* table);

/* The row with the given Code, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const PolyData_ElementRecord_t* PolyData_ElementFindByCode(
  const PolyData_ElementTable_t* table, int32_t key);

/* Whether the table holds a row with this Code. */
bool PolyData_ElementContainsCode(
  const PolyData_ElementTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* POLY_DATA_ELEMENT_H */
