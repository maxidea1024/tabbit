/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef POLY_DATA_BOON_H
#define POLY_DATA_BOON_H

#include "tabbit/tabbit_tcb_reader.h"

#include "PolyData_Forward.h"
#include "enums/PolyData_EnumBand.h"
#include "structs/PolyData_StructEffect.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/polymorphism/polymorphism.xlsx : PolymorphicSubset : B2
 *
 * Blessings whose effect is one of several shapes, written without the columns no row uses.
 */
/* One element of PolyData_BoonRecord_t::effect. */
struct PolyData_BoonRecord_t_effect_entry {
  /* which shape this row's effect is */
  int32_t type;
  /* How likely it is to land, in percent. Every variant carries it, so it is one column and */
  /* every row fills it. */
  int32_t chance;
  /* How much it takes. */
  int32_t damage;
  /* Whether it ignores armour. */
  bool pierces;
  /* How much it gives. */
  int32_t amount;
  /* Which element it deals, as a row of that catalogue. */
  /*  */
  /* **A reference on a variant member is the shape a real project reaches for first** - "the */
  /* reward is an item, or a currency, or a monster" is that shape - and it is a different */
  /* path twice over: the blank cells of the other variants go through the reference */
  /* conversion, and the built variant has to carry the resolved row rather than the key. */
  int32_t element_id; const PolyData_ElementRecord_t* element_by_element_id;
  /* How often it lands, as a band rather than a number. */
  PolyData_Band_t band;
};

struct PolyData_BoonRecord_t {
  /* primary index */
  int32_t index;
  /* plain column, outside the group */
  const char* name;
  /* which shape this row's effect is */
  struct PolyData_BoonRecord_t_effect_entry effect;
};

/* Every row of Boon.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct PolyData_BoonTable_t {
  PolyData_BoonRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} PolyData_BoonTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool PolyData_BoonLoad(PolyData_BoonTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void PolyData_BoonFree(PolyData_BoonTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const PolyData_BoonRecord_t* PolyData_BoonFindByIndex(
  const PolyData_BoonTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool PolyData_BoonContainsIndex(
  const PolyData_BoonTable_t* table, int32_t key);


/* Which shape this row's PolyData_StructEffect took. */
PolyData_StructEffectKind PolyData_BoonEffectKind(
  const PolyData_BoonRecord_t* record);

/* Fills `out` when this row is a PolyData_StructDamageEffect, and answers whether it was.
 *
 * The file carries 1 for that variant. */
bool PolyData_BoonEffectAsDamageEffect(
  const PolyData_BoonRecord_t* record, PolyData_StructDamageEffect* out);

/* Fills `out` when this row is a PolyData_StructHealEffect, and answers whether it was.
 *
 * The file carries 2 for that variant. */
bool PolyData_BoonEffectAsHealEffect(
  const PolyData_BoonRecord_t* record, PolyData_StructHealEffect* out);

/* Fills `out` when this row is a PolyData_StructNoEffect, and answers whether it was.
 *
 * The file carries 3 for that variant. */
bool PolyData_BoonEffectAsNoEffect(
  const PolyData_BoonRecord_t* record, PolyData_StructNoEffect* out);

#ifdef __cplusplus
}
#endif

#endif /* POLY_DATA_BOON_H */
