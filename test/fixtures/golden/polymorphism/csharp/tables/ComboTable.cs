// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;
[System.Serializable]
public partial class ComboRecord
{
    #region Values
    /// <summary>
    /// primary index
    /// </summary>
    public int Index => _index;

    /// <summary>
    /// plain column, outside the group
    /// </summary>
    public string Name => _name;

    /// <summary>
    /// which shape this element's effect is
    /// </summary>
    public Effect[] Effects
        => _effects_value ?? (_effects_value = BuildEffects());

    private Effect[] _effects_value;

    private Effect[] BuildEffects()
    {
        var built = new Effect[_effects.Length];

        for (int at = 0; at < _effects.Length; at++)
            built[at] = BuildEffectsElement(_effects[at]);

        return built;
    }

    private static Effect BuildEffectsElement(
        EffectsEntry entry)
    {
        switch (entry.Type)
        {
            case 1:
                return new DamageEffect
                {
                    Chance = entry.Chance,
                    Damage = entry.Damage,
                    Pierces = entry.Pierces,
                    ElementId = entry.ElementId,
                    ElementByElementId = entry.ElementByElementId,
                };
            case 2:
                return new HealEffect
                {
                    Chance = entry.Chance,
                    Amount = entry.Amount,
                    Band = entry.Band,
                };
            case 3:
                return new NoEffect
                {
                    Chance = entry.Chance,
                };
        }

        // A number no variant claims - a file written by a build that had one this code
        // does not. Unlike a column added later, which the reader skips, that makes the
        // element uninterpretable.
        throw new TcbException(
            "Effect: no variant is numbered " + entry.Type);
    }
    #endregion

    /// <summary>One element of <see cref="Effects"/>.</summary>
    [System.Serializable]
    public struct EffectsEntry
    {
        /// which shape this element's effect is
        public int Type;
        /// How likely it is to land, in percent. Every variant carries it, so it is one column and
        /// every row fills it.
        public int Chance;
        /// How much it takes.
        public int Damage;
        /// Whether it ignores armour.
        public bool Pierces;
        /// Which element it deals, as a row of that catalogue.
        ///
        /// **A reference on a variant member is the shape a real project reaches for first** - "the
        /// reward is an item, or a currency, or a monster" is that shape - and it is a different
        /// path twice over: the blank cells of the other variants go through the reference
        /// conversion, and the built variant has to carry the resolved row rather than the key.
        public int ElementId;
        public ElementRecord ElementByElementId;
        public bool ElementId_F;
        /// How much it gives.
        public int Amount;
        /// How often it lands, as a band rather than a number.
        public Band Band;

        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"Type\":"); ToStringHelper.ToString(Type, sb);
            sb.Append(",\"Chance\":"); ToStringHelper.ToString(Chance, sb);
            sb.Append(",\"Damage\":"); ToStringHelper.ToString(Damage, sb);
            sb.Append(",\"Pierces\":"); ToStringHelper.ToString(Pierces, sb);
            sb.Append(",\"ElementId\":"); ToStringHelper.ToString(ElementId, sb);
            sb.Append(",\"Amount\":"); ToStringHelper.ToString(Amount, sb);
            sb.Append(",\"Band\":"); ToStringHelper.ToString(Band, sb);
            sb.Append("}");
            return sb.ToString();
        }
    }

    #region Storage
    internal int _index;
    internal string _name = "";
    internal EffectsEntry[] _effects = System.Array.Empty<EffectsEntry>();
    #endregion

    #region ToString
    public override string ToString()
    {
        var sb = new StringBuilder("{");
        sb.Append("\"Index\":"); ToStringHelper.ToString(Index, sb);
        sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
        sb.Append(",\"Effects\":"); ToStringHelper.ToString(Effects, sb);
        sb.Append("}");
        return sb.ToString();
    }
    #endregion
}

/// <summary>
/// Skills whose effects are a list, each element its own shape.
/// </summary>
[System.Serializable]
public partial class ComboTable : IEnumerable<ComboRecord>
{
    /// <summary>
    /// Field names.
    /// </summary>
    public static readonly string[] FieldNames = { "Index", "Name", "Effects" };

    /// <summary>
    /// Build object value map.
    /// </summary>
    public List<object[]> BuildObjectValueMap()
    {
        var result = new List<object[]>();
        foreach (var r in _records)
            result.Add(new object[] { r.Index, r.Name, r.Effects });

        return result;
    }

    /// <summary>
    /// All records.
    /// </summary>
    /// <remarks>
    /// The list a read publishes, not one a read fills. Whoever holds this reference holds
    /// the rows as they were when they took it, and a refresh underneath them replaces the
    /// reference rather than the contents - so an iteration in progress neither tears nor
    /// throws, and a read that fails leaves the previous rows exactly where they were.
    /// </remarks>
    public List<ComboRecord> Records => _records;
    private List<ComboRecord> _records = new List<ComboRecord>();

    /// <summary>How many rows the table holds.</summary>
    public int Count => _records.Count;

    /// <summary>The rows, in the order the file wrote them.</summary>
    /// <remarks>
    /// A struct enumerator rather than the interface one, because `foreach` binds to this
    /// by name and a boxed enumerator allocates once per loop - which in a project that
    /// walks a table every frame is an allocation every frame. LINQ and anything holding
    /// the table as `IEnumerable` reach the explicit implementations below instead, and
    /// those box exactly as they always would have.
    ///
    /// The list reference is read once, here. A refresh replaces the reference rather than
    /// its contents, so a loop already running keeps the rows it started with - the same
    /// property `Records` documents above, reached without naming the list.
    /// </remarks>
    public List<ComboRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

    IEnumerator<ComboRecord> IEnumerable<ComboRecord>.GetEnumerator() => _records.GetEnumerator();

    System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
        => _records.GetEnumerator();

    #region Indexing by 'Index'
    public Dictionary<int, ComboRecord> RecordsByIndex => _recordsByIndex;
    private Dictionary<int, ComboRecord> _recordsByIndex = new Dictionary<int, ComboRecord>();

    /// <summary>
    /// The row with this `Index`, or null when the table has none.
    /// </summary>
    /// <remarks>
    /// The lookup to reach for when a missing row is an ordinary answer - an optional
    /// reference, a key that came from user input. Every language Tabbit generates has
    /// this one under the same name.
    /// </remarks>
    public ComboRecord FindByIndex(int key)
        => _recordsByIndex.TryGetValue(key, out ComboRecord record) ? record : null;

    /// <summary>
    /// The row with this `Index`, or a thrown exception naming what was
    /// missing.
    /// </summary>
    /// <remarks>
    /// For a key that has to be there - one from another table, or a constant. The name
    /// says it throws, because a caller reading `GetByIndex(id).Name` at
    /// a glance cannot otherwise tell whether the next line is a null check or a catch.
    /// </remarks>
    public ComboRecord GetByIndexOrThrow(int key)
    {
        if (!_recordsByIndex.TryGetValue(key, out ComboRecord record))
            throw new TabbitException($"There is no record in table `Combo` that corresponds to field `Index` value {key}");

        return record;
    }

    /// <summary>Whether the table holds a row with this `Index`.</summary>
    public bool ContainsIndex(int key) => _recordsByIndex.ContainsKey(key);
    #endregion // Indexing by `Index`

    /// <summary>Each row with the `Index` it is keyed by.</summary>
    /// <remarks>
    /// `foreach (var (key, row) in table.Entries)`. What this saves a caller is not the
    /// key value - the row carries it - but having to know which column the key is:
    /// `Index` here, something else in the next table.
    ///
    /// A struct that enumerates itself, so the loop allocates nothing, and the rows in
    /// the order the file wrote them rather than the order a dictionary happens to hold
    /// them - which makes this and a plain `foreach` over the table agree.
    ///
    /// Only the primary key gets this. A table keyed by several columns together has no
    /// single key value to pair a row with.
    /// </remarks>
    public struct EntryEnumerator
    {
        private readonly List<ComboRecord> _rows;
        private int _at;

        internal EntryEnumerator(List<ComboRecord> rows)
        {
            _rows = rows;
            _at = -1;
        }

        public EntryEnumerator GetEnumerator() => this;

        public bool MoveNext() => ++_at < _rows.Count;

        public (int Key, ComboRecord Row) Current
            => (_rows[_at].Index, _rows[_at]);
    }

    /// <summary>Each row with the `Index` it is keyed by.</summary>
    public EntryEnumerator Entries => new EntryEnumerator(_records);

    /// <summary>
    /// The row with this `Index`, or a thrown exception naming what was
    /// missing.
    /// </summary>
    /// <remarks>
    /// `GetByIndexOrThrow` under the spelling the language uses for a
    /// keyed collection. It throws because that is what `Dictionary` does here; a
    /// language whose own map answers with null generates a subscript that answers with
    /// null. spec/targets/table-collection-surface.md section 5.7.
    ///
    /// **This is the key, not the row's position.** `table[0]` is the row whose
    /// `Index` is 0, not the first row - the rows in order are
    /// `Records`. Nothing in the type says which, which is why the table itself is not
    /// subscriptable by position anywhere.
    ///
    /// It does not replace `FindByIndex`: a key that may be absent
    /// wants the one whose name says a miss is an ordinary answer.
    /// </remarks>
    public ComboRecord this[int key] => GetByIndexOrThrow(key);

    /// <summary>
    /// Read a table from specified file.
    /// </summary>
    public async Task ReadAsync(string filename)
    {
        var bytes = await PolyAccessor.ReadAllBytesAsync(filename);

        // Unconditionally, and with whatever `PolyAccessor.EncryptionKey` and `PolyAccessor.MacKey`
        // hold - which is null unless the project set them. A file that is neither
        // encrypted nor signed comes back from this untouched, so the load path is the
        // same either way and there is no condition here that could be the wrong way
        // round. spec/wire/tcb-mac-and-signature.md.
        var reader = new TcbReader(TcbTable.Open(
            bytes, PolyAccessor.EncryptionKey, PolyAccessor.MacKey, PolyAccessor.VerifyMac));

        await ReadAsync(reader);
    }

    /// <summary>
    /// Read a table from specified reader.
    /// </summary>
    /// <remarks>
    /// Column by column, matched by tag rather than position. A column this build does
    /// not know is skipped by its block length; one it knows but cannot read - the type
    /// changed incompatibly - fails naming the field. Order, names and columns added or
    /// removed on either side are therefore all survivable.
    /// </remarks>
    public Task ReadAsync(TcbReader reader)
    {
        var columns = TcbTable.ReadHeader(reader, out int count);
        TcbColumnCursor cursor;

        // Read into storage of its own and published at the end, which is what makes a
        // refresh atomic: nothing here touches what the table is currently holding, so a
        // file that turns out to be truncated - or a column this build cannot read -
        // leaves the previous rows in place and reports why.
        //
        // Sized once. The row count was checked against what the columns declare before
        // this point, so it is a number the file could actually hold rows for - and a
        // list that grows into twenty thousand rows reallocates fifteen times to get
        // there, copying everything each time.
        var records = new List<ComboRecord>(count);

        for (int i = 0; i < count; i++)
            records.Add(new ComboRecord());

        foreach (var column in columns)
        {
            int blockEnd = reader.Position + column.ByteLength;

            switch (column.Tag)
            {
                case 1:
                    TcbTable.CheckColumn(column, "Combo.Index", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                    cursor = new TcbColumnCursor(reader, column, count, "Combo.Index");
                    for (int i = 0; i < count; )
                    {
                        // One call per run of equal values, not one per row - over a
                        // run-length encoded column this is most of the decode.
                        int n = cursor.NextSameI32(count - i, out var value);
                        do
                        {
                            var record = records[i++];
                            record._index = value;
                        } while (--n > 0);
                    }
                    break;

                case 2:
                    TcbTable.CheckColumn(column, "Combo.Name", TcbTable.KindScalar, false, TcbTable.ElementString);
                    cursor = new TcbColumnCursor(reader, column, count, "Combo.Name");
                    for (int i = 0; i < count; )
                    {
                        // One call per run of equal values, not one per row - over a
                        // run-length encoded column this is most of the decode.
                        int n = cursor.NextSameString(count - i, out var value);
                        do
                        {
                            var record = records[i++];
                            record._name = value;
                        } while (--n > 0);
                    }
                    break;

                case 3:
                    TcbTable.CheckColumn(column, "Combo.Effects.Type", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                    cursor = new TcbColumnCursor(reader, column, count, "Combo.Effects.Type");
                    for (int i = 0; i < count; i++)
                    {
                        var record = records[i];
                        int elementCount;
                        elementCount = cursor.NextLength();
                        record._effects = new ComboRecord.EffectsEntry[elementCount];
                        for (int j = 0; j < elementCount; ++j)
                        {
                            record._effects[j].Type = cursor.NextI32();
                        }
                    }
                    break;

                case 4:
                    TcbTable.CheckColumn(column, "Combo.Effects.Chance", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                    cursor = new TcbColumnCursor(reader, column, count, "Combo.Effects.Chance");
                    for (int i = 0; i < count; i++)
                    {
                        var record = records[i];
                        int elementCount;
                        elementCount = cursor.NextLength();
                        if (record._effects.Length != elementCount)
                        {
                            throw new TcbException(
                                "Combo.Effects: the file gives this row "
                                + elementCount + " elements for one member of the record and "
                                + record._effects.Length + " for another. Every member of a "
                                + "record carries the same element count, so the file is damaged.");
                        }
                        for (int j = 0; j < elementCount; ++j)
                        {
                            record._effects[j].Chance = cursor.NextI32();
                        }
                    }
                    break;

                case 5:
                    TcbTable.CheckColumn(column, "Combo.Effects.Damage", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                    cursor = new TcbColumnCursor(reader, column, count, "Combo.Effects.Damage");
                    for (int i = 0; i < count; i++)
                    {
                        var record = records[i];
                        int elementCount;
                        elementCount = cursor.NextLength();
                        if (record._effects.Length != elementCount)
                        {
                            throw new TcbException(
                                "Combo.Effects: the file gives this row "
                                + elementCount + " elements for one member of the record and "
                                + record._effects.Length + " for another. Every member of a "
                                + "record carries the same element count, so the file is damaged.");
                        }
                        for (int j = 0; j < elementCount; ++j)
                        {
                            record._effects[j].Damage = cursor.NextI32();
                        }
                    }
                    break;

                case 6:
                    TcbTable.CheckColumn(column, "Combo.Effects.Pierces", TcbTable.KindArray, false, TcbTable.ElementBool);
                    cursor = new TcbColumnCursor(reader, column, count, "Combo.Effects.Pierces");
                    for (int i = 0; i < count; i++)
                    {
                        var record = records[i];
                        int elementCount;
                        elementCount = cursor.NextLength();
                        if (record._effects.Length != elementCount)
                        {
                            throw new TcbException(
                                "Combo.Effects: the file gives this row "
                                + elementCount + " elements for one member of the record and "
                                + record._effects.Length + " for another. Every member of a "
                                + "record carries the same element count, so the file is damaged.");
                        }
                        for (int j = 0; j < elementCount; ++j)
                        {
                            record._effects[j].Pierces = cursor.NextBool();
                        }
                    }
                    break;

                case 7:
                    TcbTable.CheckColumn(column, "Combo.Effects.ElementId", TcbTable.KindArray, false, TcbTable.ElementI32);
                    cursor = new TcbColumnCursor(reader, column, count, "Combo.Effects.ElementId");
                    for (int i = 0; i < count; i++)
                    {
                        var record = records[i];
                        int elementCount;
                        elementCount = cursor.NextLength();
                        if (record._effects.Length != elementCount)
                        {
                            throw new TcbException(
                                "Combo.Effects: the file gives this row "
                                + elementCount + " elements for one member of the record and "
                                + record._effects.Length + " for another. Every member of a "
                                + "record carries the same element count, so the file is damaged.");
                        }
                        for (int j = 0; j < elementCount; ++j)
                        {
                            record._effects[j].ElementId = cursor.NextI32();
                            record._effects[j].ElementByElementId = default(ElementRecord); // will be assigned.
                            record._effects[j].ElementId_F = false;
                        }
                    }
                    break;

                case 8:
                    TcbTable.CheckColumn(column, "Combo.Effects.Amount", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                    cursor = new TcbColumnCursor(reader, column, count, "Combo.Effects.Amount");
                    for (int i = 0; i < count; i++)
                    {
                        var record = records[i];
                        int elementCount;
                        elementCount = cursor.NextLength();
                        if (record._effects.Length != elementCount)
                        {
                            throw new TcbException(
                                "Combo.Effects: the file gives this row "
                                + elementCount + " elements for one member of the record and "
                                + record._effects.Length + " for another. Every member of a "
                                + "record carries the same element count, so the file is damaged.");
                        }
                        for (int j = 0; j < elementCount; ++j)
                        {
                            record._effects[j].Amount = cursor.NextI32();
                        }
                    }
                    break;

                case 9:
                    TcbTable.CheckColumn(column, "Combo.Effects.Band", TcbTable.KindArray, false, TcbTable.ElementVarint);
                    cursor = new TcbColumnCursor(reader, column, count, "Combo.Effects.Band");
                    for (int i = 0; i < count; i++)
                    {
                        var record = records[i];
                        int elementCount;
                        elementCount = cursor.NextLength();
                        if (record._effects.Length != elementCount)
                        {
                            throw new TcbException(
                                "Combo.Effects: the file gives this row "
                                + elementCount + " elements for one member of the record and "
                                + record._effects.Length + " for another. Every member of a "
                                + "record carries the same element count, so the file is damaged.");
                        }
                        for (int j = 0; j < elementCount; ++j)
                        {
                            record._effects[j].Band = (Band)cursor.NextI32();
                        }
                    }
                    break;

                default:
                    // A column added after this code was generated. Its block length
                    // says how far to move on.
                    reader.Skip(column.ByteLength);
                    break;
            }

            TcbTable.CheckBlockEnd(reader, column, blockEnd);
        }

        // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
        // duplicate key throws here - before any of this is visible.
        var recordsByIndex = new Dictionary<int, ComboRecord>(count);
        foreach (var record in records)
            recordsByIndex.Add(record.Index, record);

        // Published. Everything above succeeded, and the barrier is what keeps the writes
        // that built these from being seen after the references to them - a reader on
        // another thread gets the whole table or the previous one, never half of either.
        System.Threading.Thread.MemoryBarrier();

        _records = records;
        _recordsByIndex = recordsByIndex;

        return Task.CompletedTask;
    }

    public override string ToString()
    {
        var sb = new StringBuilder();
        ToStringHelper.ToString(_records, sb);
        return sb.ToString();
    }
}
