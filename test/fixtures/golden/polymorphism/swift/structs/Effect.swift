// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import Foundation
/// One value of `Effect`, whichever shape it took.
///
/// Narrow it with `switch`, which is exhaustive over an enum.
public enum Effect {
    /// One shape of `Effect`.
    ///
    /// The file carries `1` for it. That number rather than the name
    /// is what lets this variant be renamed without a deployed reader reading a different one.
    case damageEffect(DamageEffect)
    /// One shape of `Effect`.
    ///
    /// The file carries `2` for it. That number rather than the name
    /// is what lets this variant be renamed without a deployed reader reading a different one.
    case healEffect(HealEffect)
    /// One shape of `Effect`.
    ///
    /// The file carries `3` for it. That number rather than the name
    /// is what lets this variant be renamed without a deployed reader reading a different one.
    case noEffect(NoEffect)

    /// How likely it is to land, in percent. Every variant carries it, so it is one column and
    /// every row fills it.
    public var chance: Int32 {
        switch self {
        case .damageEffect(let it): return it.chance
        case .healEffect(let it): return it.chance
        case .noEffect(let it): return it.chance
        }
    }
}

/// The payload of `Effect.damageEffect`.
public struct DamageEffect {
    /// How likely it is to land, in percent. Every variant carries it, so it is one column and
    /// every row fills it.
    public var chance: Int32
    /// How much it takes.
    public var damage: Int32
    /// Whether it ignores armour.
    public var pierces: Bool
    /// Which element it deals, as a row of that catalogue.
    ///
    /// **A reference on a variant member is the shape a real project reaches for first** - "the
    /// reward is an item, or a currency, or a monster" is that shape - and it is a different
    /// path twice over: the blank cells of the other variants go through the reference
    /// conversion, and the built variant has to carry the resolved row rather than the key.
    public var elementId: Int32
    public var elementByElementId: ElementRecord?
}

/// The payload of `Effect.healEffect`.
public struct HealEffect {
    /// How likely it is to land, in percent. Every variant carries it, so it is one column and
    /// every row fills it.
    public var chance: Int32
    /// How much it gives.
    public var amount: Int32
    /// How often it lands, as a band rather than a number.
    public var band: Band
}

/// The payload of `Effect.noEffect`.
public struct NoEffect {
    /// How likely it is to land, in percent. Every variant carries it, so it is one column and
    /// every row fills it.
    public var chance: Int32
}
