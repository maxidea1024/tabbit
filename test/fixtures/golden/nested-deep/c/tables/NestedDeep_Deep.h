/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef NESTED_DEEP_DEEP_H
#define NESTED_DEEP_DEEP_H

#include "tabbit/tabbit_tcb_reader.h"

#include "NestedDeep_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/nested-deep/nested-deep.xlsx : Deep : B2
 *
 * A record whose member is a record.
 */
/* A record inside NestedDeep_DeepRecord_t::star. */
struct NestedDeep_DeepRecord_t_star_entryPosition {
  /* element 1, one level further in */
  int32_t x;
  /* its sibling */
  int32_t y;
};

/* One element of NestedDeep_DeepRecord_t::star. */
struct NestedDeep_DeepRecord_t_star_entry {
  /* element 1, a value beside a record */
  int32_t id;
  /* element 1, one level further in */
  struct NestedDeep_DeepRecord_t_star_entryPosition position;
};

struct NestedDeep_DeepRecord_t {
  /* primary index */
  int32_t index;
  /* element 1, a value beside a record */
  struct NestedDeep_DeepRecord_t_star_entry* star;
  int32_t star_count;
};

/* Every row of Deep.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct NestedDeep_DeepTable_t {
  NestedDeep_DeepRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} NestedDeep_DeepTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool NestedDeep_DeepLoad(NestedDeep_DeepTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void NestedDeep_DeepFree(NestedDeep_DeepTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const NestedDeep_DeepRecord_t* NestedDeep_DeepFindByIndex(
  const NestedDeep_DeepTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool NestedDeep_DeepContainsIndex(
  const NestedDeep_DeepTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* NESTED_DEEP_DEEP_H */
