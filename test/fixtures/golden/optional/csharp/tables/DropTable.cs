// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Tabbit.Fixtures.Optional
{
    /// <summary>
    /// Optional columns, each beside the same type left required.
    /// </summary>
    [System.Serializable]
    public partial class DropTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// primary index, never optional
            /// </summary>
            public int Index => _index;

            /// <summary>
            /// required, so a blank here would be an error
            /// </summary>
            public int Hp => _hp;

            /// <summary>
            /// optional int
            /// </summary>
            public int Bonus => _bonus;
            /// <summary>Whether this row has a value for <see cref="Bonus"/>.</summary>
            public bool HasBonus => _bonusHasValue;

            /// <summary>
            /// optional double
            /// </summary>
            public double Weight => _weight;
            /// <summary>Whether this row has a value for <see cref="Weight"/>.</summary>
            public bool HasWeight => _weightHasValue;

            /// <summary>
            /// optional float
            /// </summary>
            public float Ratio => _ratio;
            /// <summary>Whether this row has a value for <see cref="Ratio"/>.</summary>
            public bool HasRatio => _ratioHasValue;

            /// <summary>
            /// optional bigint
            /// </summary>
            public long Count => _count;
            /// <summary>Whether this row has a value for <see cref="Count"/>.</summary>
            public bool HasCount => _countHasValue;

            /// <summary>
            /// optional datetime
            /// </summary>
            public System.DateTime OpenAt => _openAt;
            /// <summary>Whether this row has a value for <see cref="OpenAt"/>.</summary>
            public bool HasOpenAt => _openAtHasValue;

            /// <summary>
            /// optional timespan
            /// </summary>
            public System.TimeSpan Cooldown => _cooldown;
            /// <summary>Whether this row has a value for <see cref="Cooldown"/>.</summary>
            public bool HasCooldown => _cooldownHasValue;

            /// <summary>
            /// optional uuid
            /// </summary>
            public System.Guid Batch => _batch;
            /// <summary>Whether this row has a value for <see cref="Batch"/>.</summary>
            public bool HasBatch => _batchHasValue;

            /// <summary>
            /// optional enum label
            /// </summary>
            public global::Tabbit.Fixtures.Optional.Rarity Grade => _grade;
            /// <summary>Whether this row has a value for <see cref="Grade"/>.</summary>
            public bool HasGrade => _gradeHasValue;

            /// <summary>
            /// optional array
            /// </summary>
            public int[] Costs => _costs;
            /// <summary>Whether this row has a value for <see cref="Costs"/>.</summary>
            public bool HasCosts => _costsHasValue;

            /// <summary>
            /// optional string
            /// </summary>
            public string Label => _label;
            /// <summary>Whether this row has a value for <see cref="Label"/>.</summary>
            public bool HasLabel => _labelHasValue;

            /// <summary>
            /// optional bool
            /// </summary>
            public bool Hidden => _hidden;
            /// <summary>Whether this row has a value for <see cref="Hidden"/>.</summary>
            public bool HasHidden => _hiddenHasValue;
            #endregion

            #region Storage
            internal int _index;
            internal int _hp;
            internal int _bonus;
            internal bool _bonusHasValue;
            internal double _weight;
            internal bool _weightHasValue;
            internal float _ratio;
            internal bool _ratioHasValue;
            internal long _count;
            internal bool _countHasValue;
            internal System.DateTime _openAt;
            internal bool _openAtHasValue;
            internal System.TimeSpan _cooldown;
            internal bool _cooldownHasValue;
            internal System.Guid _batch;
            internal bool _batchHasValue;
            internal global::Tabbit.Fixtures.Optional.Rarity _grade;
            internal bool _gradeHasValue;
            internal int[] _costs = System.Array.Empty<int>();
            internal bool _costsHasValue;
            internal string _label = "";
            internal bool _labelHasValue;
            internal bool _hidden;
            internal bool _hiddenHasValue;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Index\":"); ToStringHelper.ToString(Index, sb);
                sb.Append(",\"Hp\":"); ToStringHelper.ToString(Hp, sb);
                sb.Append(",\"Bonus\":"); ToStringHelper.ToString(Bonus, sb);
                sb.Append(",\"Weight\":"); ToStringHelper.ToString(Weight, sb);
                sb.Append(",\"Ratio\":"); ToStringHelper.ToString(Ratio, sb);
                sb.Append(",\"Count\":"); ToStringHelper.ToString(Count, sb);
                sb.Append(",\"OpenAt\":"); ToStringHelper.ToString(OpenAt, sb);
                sb.Append(",\"Cooldown\":"); ToStringHelper.ToString(Cooldown, sb);
                sb.Append(",\"Batch\":"); ToStringHelper.ToString(Batch, sb);
                sb.Append(",\"Grade\":"); ToStringHelper.ToString(Grade, sb);
                sb.Append(",\"Costs\":"); ToStringHelper.ToString(Costs, sb);
                sb.Append(",\"Label\":"); ToStringHelper.ToString(Label, sb);
                sb.Append(",\"Hidden\":"); ToStringHelper.ToString(Hidden, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Index", "Hp", "Bonus", "Weight", "Ratio", "Count", "OpenAt", "Cooldown", "Batch", "Grade", "Costs", "Label", "Hidden" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Index, r.Hp, r.Bonus, r.Weight, r.Ratio, r.Count, r.OpenAt, r.Cooldown, r.Batch, r.Grade, r.Costs, r.Label, r.Hidden });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Index'
        public Dictionary<int, Record> RecordsByIndex => _recordsByIndex;
        private Dictionary<int, Record> _recordsByIndex = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Index`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByIndex(int key)
            => _recordsByIndex.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Index`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByIndex(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIndexOrThrow(int key)
        {
            if (!_recordsByIndex.TryGetValue(key, out Record record))
                throw new TabbitException($"There is no record in table `Drop` that corresponds to field `Index` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Index`.</summary>
        public bool ContainsIndex(int key) => _recordsByIndex.ContainsKey(key);
        #endregion // Indexing by `Index`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await OptionalAccessor.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `OptionalAccessor.EncryptionKey` and `OptionalAccessor.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, OptionalAccessor.EncryptionKey, OptionalAccessor.MacKey, OptionalAccessor.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "Drop.Index", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Index");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._index = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "Drop.Hp", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Hp");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._hp = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "Drop.Bonus", TcbTable.KindScalar, true, TcbTable.ElementI32, TcbTable.ElementVarint);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Bonus");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._bonus = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._bonusHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._bonusHasValue)
                                records[i]._bonus = default(int);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "Drop.Weight", TcbTable.KindScalar, true, TcbTable.ElementF64, TcbTable.ElementF32, TcbTable.ElementI32);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Weight");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._weight = cursor.NextF64();
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._weightHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._weightHasValue)
                                records[i]._weight = default(double);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "Drop.Ratio", TcbTable.KindScalar, true, TcbTable.ElementF32);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Ratio");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._ratio = cursor.NextF32();
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._ratioHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._ratioHasValue)
                                records[i]._ratio = default(float);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "Drop.Count", TcbTable.KindScalar, true, TcbTable.ElementI64, TcbTable.ElementI32, TcbTable.ElementVarint);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Count");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._count = cursor.NextI64();
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._countHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._countHasValue)
                                records[i]._count = default(long);
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "Drop.OpenAt", TcbTable.KindScalar, true, TcbTable.ElementI64);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.OpenAt");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._openAt = new System.DateTime(cursor.NextI64());
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._openAtHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._openAtHasValue)
                                records[i]._openAt = default(System.DateTime);
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "Drop.Cooldown", TcbTable.KindScalar, true, TcbTable.ElementI64);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Cooldown");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._cooldown = new System.TimeSpan(cursor.NextI64());
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._cooldownHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._cooldownHasValue)
                                records[i]._cooldown = default(System.TimeSpan);
                        }
                        break;

                    case 9:
                        TcbTable.CheckColumn(column, "Drop.Batch", TcbTable.KindScalar, true, TcbTable.ElementUuid);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._batch);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._batchHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._batchHasValue)
                                records[i]._batch = default(System.Guid);
                        }
                        break;

                    case 10:
                        TcbTable.CheckColumn(column, "Drop.Grade", TcbTable.KindScalar, true, TcbTable.ElementVarint);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Grade");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._grade = (global::Tabbit.Fixtures.Optional.Rarity)value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._gradeHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._gradeHasValue)
                                records[i]._grade = default(global::Tabbit.Fixtures.Optional.Rarity);
                        }
                        break;

                    case 11:
                        TcbTable.CheckColumn(column, "Drop.Costs", TcbTable.KindArray, true, TcbTable.ElementI32, TcbTable.ElementVarint);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Costs");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._costs = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._costs[j] = cursor.NextI32();
                            }
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._costsHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._costsHasValue)
                                records[i]._costs = System.Array.Empty<int>();
                        }
                        break;

                    case 12:
                        TcbTable.CheckColumn(column, "Drop.Label", TcbTable.KindScalar, true, TcbTable.ElementString);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Label");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._label = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._labelHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._labelHasValue)
                                records[i]._label = "";
                        }
                        break;

                    case 13:
                        TcbTable.CheckColumn(column, "Drop.Hidden", TcbTable.KindScalar, true, TcbTable.ElementBool);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Drop.Hidden");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._hidden = cursor.NextBool();
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._hiddenHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._hiddenHasValue)
                                records[i]._hidden = default(bool);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByIndex = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsByIndex.Add(record.Index, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByIndex = recordsByIndex;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Tabbit.Fixtures.Optional
