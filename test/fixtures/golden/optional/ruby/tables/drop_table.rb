# ------------------------------------------------------------------------------
# Generated by Tabbit. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

require_relative '../tabbit/tcb_reader'


module Optional
  # Generated from test/fixtures/xlsx/optional/optional.xlsx : Optional : F2
  # Optional columns, each beside the same type left required.
  class DropRecord
    attr_accessor :index, :hp, :bonus, :has_bonus, :weight, :has_weight, :ratio, :has_ratio, :count, :has_count, :open_at, :has_open_at, :cooldown, :has_cooldown, :batch, :has_batch, :grade, :has_grade, :costs, :has_costs, :label, :has_label, :hidden, :has_hidden

    def initialize
      @index = 0
      @hp = 0
      @bonus = 0
      @has_bonus = false
      @weight = 0.0
      @has_weight = false
      @ratio = 0.0
      @has_ratio = false
      @count = 0
      @has_count = false
      @open_at = 0
      @has_open_at = false
      @cooldown = 0
      @has_cooldown = false
      @batch = Tabbit::Uuid.new
      @has_batch = false
      @grade = 0
      @has_grade = false
      @costs = []
      @has_costs = false
      @label = ''
      @has_label = false
      @hidden = false
      @has_hidden = false
    end

  end

  # Every row of Drop.
  class DropTable
    attr_accessor :records

    def initialize
      @records = []
      @by_index = {}
    end

    # The row with this Index, or nil when the table has none.
    #
    # The lookup to reach for when a missing row is an ordinary answer - an optional
    # reference, a key that came from user input.
    def find_by_index(key)
      @by_index[key]
    end

    # The row with this Index, or a raised error naming what was missing.
    #
    # For a key that has to be there - one from another table, or a constant. The name
    # says it raises, because a caller reading `get_by_index(key).name` at a
    # glance cannot otherwise tell whether the next line is a check or a rescue.
    def get_by_index_or_throw(key)
      record = @by_index[key]

      if record.nil?
        raise Tabbit::RecordNotFoundError,
              "there is no record in table `Drop` that corresponds to " \
              "field `Index` value #{key.inspect}"
      end

      record
    end

    # Whether the table holds a row with this Index.
    def contains_index?(key)
      @by_index.key?(key)
    end

    # Loads the table from a .tcb file written by Tabbit.
    # Column by column, matched by tag rather than position: a column this build does
    # not know is skipped by its block length, and one whose type changed incompatibly
    # fails naming the field.
    def read(filename)
      # Opened unconditionally, and with whatever Tables.encryption_key and Tables.mac_key
      # hold - which is nil unless the project set them. A file that is neither encrypted
      # nor signed comes back from this untouched, so the load path is the same either way
      # and there is no condition here that could be the wrong way round.
      # spec/tcb-mac-and-signature.md.
      reader = Tabbit::Reader.new(
        Tabbit.open(Tabbit.read_all_bytes(filename), Tables.encryption_key,
                      Tables.mac_key, verify_mac: Tables.verify_mac)
      )
      count, columns = Tabbit.read_table_header(reader)

      # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
      records = Array.new(count) { DropRecord.new }

      columns.each do |column|
        block_end = reader.position + column.byte_length

        case column.tag
        when 1
          Tabbit.check_column(column, 'Drop.Index', Tabbit::KIND_SCALAR, 1, false, [Tabbit::ELEMENT_I32, Tabbit::ELEMENT_VARINT])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Index')
          at = 0
          while at < count
            n, value = cursor.next_same_i32(count - at)
            (at...(at + n)).each do |i|
              records[i].index = value
            end
            at += n
          end
        when 2
          Tabbit.check_column(column, 'Drop.Hp', Tabbit::KIND_SCALAR, 1, false, [Tabbit::ELEMENT_I32, Tabbit::ELEMENT_VARINT])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Hp')
          at = 0
          while at < count
            n, value = cursor.next_same_i32(count - at)
            (at...(at + n)).each do |i|
              records[i].hp = value
            end
            at += n
          end
        when 3
          Tabbit.check_column(column, 'Drop.Bonus', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_I32, Tabbit::ELEMENT_VARINT])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Bonus')
          at = 0
          while at < count
            n, value = cursor.next_same_i32(count - at)
            (at...(at + n)).each do |i|
              records[i].bonus = value
            end
            at += n
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_bonus = Tabbit.present?(presence, row)
            record.bonus = 0 unless record.has_bonus
          end
        when 4
          Tabbit.check_column(column, 'Drop.Weight', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_F64, Tabbit::ELEMENT_F32, Tabbit::ELEMENT_I32])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Weight')
          records.each do |record|
            record.weight = cursor.next_f64
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_weight = Tabbit.present?(presence, row)
            record.weight = 0.0 unless record.has_weight
          end
        when 5
          Tabbit.check_column(column, 'Drop.Ratio', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_F32])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Ratio')
          records.each do |record|
            record.ratio = cursor.next_f32
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_ratio = Tabbit.present?(presence, row)
            record.ratio = 0.0 unless record.has_ratio
          end
        when 6
          Tabbit.check_column(column, 'Drop.Count', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_I64, Tabbit::ELEMENT_I32, Tabbit::ELEMENT_VARINT])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Count')
          records.each do |record|
            record.count = cursor.next_i64
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_count = Tabbit.present?(presence, row)
            record.count = 0 unless record.has_count
          end
        when 7
          Tabbit.check_column(column, 'Drop.OpenAt', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_I64])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.OpenAt')
          records.each do |record|
            record.open_at = cursor.next_i64
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_open_at = Tabbit.present?(presence, row)
            record.open_at = 0 unless record.has_open_at
          end
        when 8
          Tabbit.check_column(column, 'Drop.Cooldown', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_I64])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Cooldown')
          records.each do |record|
            record.cooldown = cursor.next_i64
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_cooldown = Tabbit.present?(presence, row)
            record.cooldown = 0 unless record.has_cooldown
          end
        when 9
          Tabbit.check_column(column, 'Drop.Batch', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_UUID])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          records.each do |record|
            record.batch = reader.read_uuid
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_batch = Tabbit.present?(presence, row)
            record.batch = Tabbit::Uuid.new unless record.has_batch
          end
        when 10
          Tabbit.check_column(column, 'Drop.Grade', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_VARINT])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Grade')
          at = 0
          while at < count
            n, value = cursor.next_same_i32(count - at)
            (at...(at + n)).each do |i|
              records[i].grade = value
            end
            at += n
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_grade = Tabbit.present?(presence, row)
            record.grade = 0 unless record.has_grade
          end
        when 11
          Tabbit.check_column(column, 'Drop.Costs', Tabbit::KIND_VAR_ARRAY, 0, true, [Tabbit::ELEMENT_I32, Tabbit::ELEMENT_VARINT])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Costs')
          records.each do |record|
            element_count = cursor.next_length
            record.costs = Array.new(element_count) { cursor.next_i32 }
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_costs = Tabbit.present?(presence, row)
            record.costs = [] unless record.has_costs
          end
        when 12
          Tabbit.check_column(column, 'Drop.Label', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_STRING])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Label')
          at = 0
          while at < count
            n, value = cursor.next_same_string(count - at)
            (at...(at + n)).each do |i|
              records[i].label = value
            end
            at += n
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_label = Tabbit.present?(presence, row)
            record.label = '' unless record.has_label
          end
        when 13
          Tabbit.check_column(column, 'Drop.Hidden', Tabbit::KIND_SCALAR, 1, true, [Tabbit::ELEMENT_BOOL])
          # The bitmap is at the front of the block, so it is read before the values. The
          # values are written for every row either way, which is what lets the read shapes
          # below stay as they are.
          presence = Tabbit.read_presence(reader, column, count)
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Drop.Hidden')
          records.each do |record|
            record.hidden = cursor.next_bool
          end

          # A pass of its own rather than a line inside each read shape, so the row index is
          # worked out once. The block carries a value for every row, so an absent one has
          # just been given whatever was there; putting the empty value back is what makes
          # this path agree with the JSON one.
          records.each_with_index do |record, row|
            record.has_hidden = Tabbit.present?(presence, row)
            record.hidden = false unless record.has_hidden
          end
        else
          # A column added after this code was generated.
          reader.skip(column.byte_length)
        end

        Tabbit.check_block_end(reader, column, block_end)
      end

      by_index = records.to_h { |record| [record.index, record] }

      # Published, now that every column read.
      @records = records
      @by_index = by_index
    end
  end
end
