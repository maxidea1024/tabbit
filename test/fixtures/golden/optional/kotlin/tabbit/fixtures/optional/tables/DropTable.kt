// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package tabbit.fixtures.optional

import java.io.File
import tabbit.TcbReader
import tabbit.RecordNotFoundException
import tabbit.Uuid
import tabbit.readAllBytes
import tabbit.open
import tabbit.readTableHeader
import tabbit.checkColumn
import tabbit.checkColumnWithElements
import tabbit.checkBlockEnd
import tabbit.readPresence
import tabbit.readElementPresence
import tabbit.isPresent
import tabbit.TcbException
import tabbit.ColumnCursor
import tabbit.ELEMENT_VARINT
import tabbit.ELEMENT_BOOL
import tabbit.ELEMENT_I32
import tabbit.ELEMENT_I64
import tabbit.ELEMENT_F32
import tabbit.ELEMENT_F64
import tabbit.ELEMENT_STRING
import tabbit.ELEMENT_UUID
import tabbit.KIND_SCALAR
import tabbit.KIND_ARRAY

// Generated from test/fixtures/xlsx/optional/optional.xlsx : Optional : F2
/** Optional columns, each beside the same type left required. */
class DropRecord {
    /** primary index, never optional */
    var index: Int = 0
    /** required, so a blank here would be an error */
    var hp: Int = 0
    /** optional int */
    var bonus: Int = 0
    /**
     * Whether this row has a value for bonus. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasBonus: Boolean = false
    /** optional double */
    var weight: Double = 0.0
    /**
     * Whether this row has a value for weight. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasWeight: Boolean = false
    /** optional float */
    var ratio: Float = 0.0f
    /**
     * Whether this row has a value for ratio. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasRatio: Boolean = false
    /** optional bigint */
    var count: Long = 0L
    /**
     * Whether this row has a value for count. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasCount: Boolean = false
    /** optional datetime */
    var openAt: Long = 0L
    /**
     * Whether this row has a value for openAt. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasOpenAt: Boolean = false
    /** optional timespan */
    var cooldown: Long = 0L
    /**
     * Whether this row has a value for cooldown. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasCooldown: Boolean = false
    /** optional uuid */
    var batch: Uuid = Uuid()
    /**
     * Whether this row has a value for batch. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasBatch: Boolean = false
    /** optional enum label */
    var grade: Rarity = Rarity.of(0)
    /**
     * Whether this row has a value for grade. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasGrade: Boolean = false
    /** optional array */
    var costs: MutableList<Int> = ArrayList()
    /**
     * Whether this row has a value for costs. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasCosts: Boolean = false
    /** optional string */
    var label: String = ""
    /**
     * Whether this row has a value for label. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasLabel: Boolean = false
    /** optional bool */
    var hidden: Boolean = false
    /**
     * Whether this row has a value for hidden. The value property keeps its type
     * and holds the type's empty value when the row had none; this says which it was.
     */
    var hasHidden: Boolean = false

}

/** Every row of Drop. */
class DropTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<DropRecord> = ArrayList()
        private set

    private var byIndex: HashMap<Int, DropRecord> = HashMap()

    /**
     * The row with this Index, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findByIndex(key: Int): DropRecord? = byIndex[key]

    /**
     * The row with this Index, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByIndex(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIndexOrThrow(key: Int): DropRecord =
        byIndex[key] ?: throw RecordNotFoundException(
            "there is no record in table `Drop` that corresponds to " +
                "field `Index` value $key")

    /** Whether the table holds a row with this Index. */
    fun containsIndex(key: Int): Boolean = byIndex.containsKey(key)

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        // Opened unconditionally, and with whatever OptionalAccessor.encryptionKey and
        // OptionalAccessor.macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from this untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/tcb-mac-and-signature.md.
        val reader = TcbReader(open(
            readAllBytes(filename), OptionalAccessor.encryptionKey,
            OptionalAccessor.macKey, OptionalAccessor.verifyMac))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<DropRecord>(count)
        val loadedByIndex = HashMap<Int, DropRecord>(count * 2)

        repeat(count) { loaded.add(DropRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "Drop.Index", KIND_SCALAR, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Index")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].index = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                2 -> {
                    checkColumn(column, "Drop.Hp", KIND_SCALAR, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Hp")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].hp = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                3 -> {
                    checkColumn(column, "Drop.Bonus", KIND_SCALAR, true, ELEMENT_I32, ELEMENT_VARINT)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Bonus")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].bonus = cursor.runSameValue
                            at++
                            n--
                        }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasBonus = isPresent(presence, row)

                        if (!record.hasBonus) {
                            record.bonus = 0
                        }
                    }
                }
                4 -> {
                    checkColumn(column, "Drop.Weight", KIND_SCALAR, true, ELEMENT_F64, ELEMENT_F32, ELEMENT_I32)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Weight")
                    for (record in loaded) {
                        record.weight = cursor.nextF64()
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasWeight = isPresent(presence, row)

                        if (!record.hasWeight) {
                            record.weight = 0.0
                        }
                    }
                }
                5 -> {
                    checkColumn(column, "Drop.Ratio", KIND_SCALAR, true, ELEMENT_F32)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Ratio")
                    for (record in loaded) {
                        record.ratio = cursor.nextF32()
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasRatio = isPresent(presence, row)

                        if (!record.hasRatio) {
                            record.ratio = 0.0f
                        }
                    }
                }
                6 -> {
                    checkColumn(column, "Drop.Count", KIND_SCALAR, true, ELEMENT_I64, ELEMENT_I32, ELEMENT_VARINT)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Count")
                    for (record in loaded) {
                        record.count = cursor.nextI64()
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasCount = isPresent(presence, row)

                        if (!record.hasCount) {
                            record.count = 0L
                        }
                    }
                }
                7 -> {
                    checkColumn(column, "Drop.OpenAt", KIND_SCALAR, true, ELEMENT_I64)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.OpenAt")
                    for (record in loaded) {
                        record.openAt = cursor.nextI64()
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasOpenAt = isPresent(presence, row)

                        if (!record.hasOpenAt) {
                            record.openAt = 0L
                        }
                    }
                }
                8 -> {
                    checkColumn(column, "Drop.Cooldown", KIND_SCALAR, true, ELEMENT_I64)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Cooldown")
                    for (record in loaded) {
                        record.cooldown = cursor.nextI64()
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasCooldown = isPresent(presence, row)

                        if (!record.hasCooldown) {
                            record.cooldown = 0L
                        }
                    }
                }
                9 -> {
                    checkColumn(column, "Drop.Batch", KIND_SCALAR, true, ELEMENT_UUID)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    for (record in loaded) {
                        record.batch = reader.readUuid()
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasBatch = isPresent(presence, row)

                        if (!record.hasBatch) {
                            record.batch = Uuid()
                        }
                    }
                }
                10 -> {
                    checkColumn(column, "Drop.Grade", KIND_SCALAR, true, ELEMENT_VARINT)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Grade")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].grade = Rarity.of(cursor.runSameValue)
                            at++
                            n--
                        }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasGrade = isPresent(presence, row)

                        if (!record.hasGrade) {
                            record.grade = Rarity.of(0)
                        }
                    }
                }
                11 -> {
                    checkColumn(column, "Drop.Costs", KIND_ARRAY, true, ELEMENT_I32, ELEMENT_VARINT)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Costs")
                    for (record in loaded) {
                        val elementCount = cursor.nextLength()
                        record.costs = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.costs.add(cursor.nextI32()) }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasCosts = isPresent(presence, row)

                        if (!record.hasCosts) {
                            record.costs = ArrayList()
                        }
                    }
                }
                12 -> {
                    checkColumn(column, "Drop.Label", KIND_SCALAR, true, ELEMENT_STRING)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Label")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].label = cursor.runSameText
                            at++
                            n--
                        }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasLabel = isPresent(presence, row)

                        if (!record.hasLabel) {
                            record.label = ""
                        }
                    }
                }
                13 -> {
                    checkColumn(column, "Drop.Hidden", KIND_SCALAR, true, ELEMENT_BOOL)
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    val presence = readPresence(reader, column, count)
                    val cursor = ColumnCursor(reader, column, count, "Drop.Hidden")
                    for (record in loaded) {
                        record.hidden = cursor.nextBool()
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row in 0 until count) {
                        val record = loaded[row]
                        record.hasHidden = isPresent(presence, row)

                        if (!record.hasHidden) {
                            record.hidden = false
                        }
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedByIndex[record.index] = record
        }

        // Published, now that every column read.
        records = loaded
        byIndex = loadedByIndex
    }
}
