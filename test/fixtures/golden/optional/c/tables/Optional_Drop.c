/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "tables/Optional_Drop.h"

#include <string.h>

static bool Optional_DropParse(Optional_DropTable_t* table, tb_reader* reader) {
  int32_t row;
  int32_t at;
  tb_column* columns = NULL;
  int32_t column_count = 0;
  tb_cursor cursor;
  /* Declared with the other locals: C89 wants them at the top of the function, and the
   * switch's cases share its scope either way. */
  const uint8_t* presence = NULL;

  if (!tb_read_table_header(reader, &table->count, &columns, &column_count))
    return false;

  if (table->count > 0) {
    table->records = (Optional_DropRecord_t*)tb_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->records);

    if (table->records == NULL)
      return tb_fail_with(reader, "out of memory allocating the rows");

    table->by_index = (tb_index_entry*)tb_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->by_index);

    if (table->by_index == NULL)
      return tb_fail_with(reader, "out of memory allocating the Index index");
  }

  /* The arena hands back zeroed memory, so every member starts at its zero - which for
   * a string is NULL, and NULL is what reaches printf when a column the file does not
   * carry is read. An empty string instead: a literal, so there is nothing to free and
   * nothing to allocate. */
  for (row = 0; row < table->count; ++row) {
    Optional_DropRecord_t* record = &table->records[row];

    record->label = "";
  }

  /* Column by column, matched by tag rather than by position: a column this build has
   * no member for is skipped by its declared length, and one whose type no longer fits
   * the member stops the read naming the field. Rows arrive with every member at an
   * empty value, so one the file has nothing for keeps it. */
  for (at = 0; at < column_count && !tb_failed(reader); ++at) {
    const tb_column* column = &columns[at];
    int32_t block_end = reader->position + column->byte_length;

    switch (column->tag) {

    case 1:
      (void)tb_check_column(reader, column, "Drop.Index", TB_KIND_SCALAR, 1, false, TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT));

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Index");

      {
        int32_t run_length = 0;
        int32_t value = 0;

        row = 0;

        while (row < table->count && !tb_failed(reader)) {
          if (!tb_cursor_next_same_i32(&cursor, table->count - row, &run_length, &value))
            break;

          for (; run_length > 0; --run_length, ++row)
            table->records[row].index = value;
        }
      }
      break;

    case 2:
      (void)tb_check_column(reader, column, "Drop.Hp", TB_KIND_SCALAR, 1, false, TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT));

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Hp");

      {
        int32_t run_length = 0;
        int32_t value = 0;

        row = 0;

        while (row < table->count && !tb_failed(reader)) {
          if (!tb_cursor_next_same_i32(&cursor, table->count - row, &run_length, &value))
            break;

          for (; run_length > 0; --run_length, ++row)
            table->records[row].hp = value;
        }
      }
      break;

    case 3:
      (void)tb_check_column(reader, column, "Drop.Bonus", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Bonus");

      {
        int32_t run_length = 0;
        int32_t value = 0;

        row = 0;

        while (row < table->count && !tb_failed(reader)) {
          if (!tb_cursor_next_same_i32(&cursor, table->count - row, &run_length, &value))
            break;

          for (; run_length > 0; --run_length, ++row)
            table->records[row].bonus = value;
        }
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_bonus = tb_is_present(presence, row);

        if (!table->records[row].has_bonus)
          table->records[row].bonus = 0;
      }
      break;

    case 4:
      (void)tb_check_column(reader, column, "Drop.Weight", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_F64) | TB_ELEMENT_MASK(TB_ELEMENT_F32) | TB_ELEMENT_MASK(TB_ELEMENT_I32));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Weight");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        Optional_DropRecord_t* record = &table->records[row];

        (void)tb_cursor_next_f64(&cursor, &record->weight);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_weight = tb_is_present(presence, row);

        if (!table->records[row].has_weight)
          table->records[row].weight = 0;
      }
      break;

    case 5:
      (void)tb_check_column(reader, column, "Drop.Ratio", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_F32));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Ratio");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        Optional_DropRecord_t* record = &table->records[row];

        (void)tb_cursor_next_f32(&cursor, &record->ratio);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_ratio = tb_is_present(presence, row);

        if (!table->records[row].has_ratio)
          table->records[row].ratio = 0;
      }
      break;

    case 6:
      (void)tb_check_column(reader, column, "Drop.Count", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_I64) | TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Count");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        Optional_DropRecord_t* record = &table->records[row];

        (void)tb_cursor_next_i64(&cursor, &record->count);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_count = tb_is_present(presence, row);

        if (!table->records[row].has_count)
          table->records[row].count = 0;
      }
      break;

    case 7:
      (void)tb_check_column(reader, column, "Drop.OpenAt", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_I64));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.OpenAt");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        Optional_DropRecord_t* record = &table->records[row];

        (void)tb_cursor_next_i64(&cursor, &record->open_at);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_open_at = tb_is_present(presence, row);

        if (!table->records[row].has_open_at)
          table->records[row].open_at = 0;
      }
      break;

    case 8:
      (void)tb_check_column(reader, column, "Drop.Cooldown", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_I64));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Cooldown");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        Optional_DropRecord_t* record = &table->records[row];

        (void)tb_cursor_next_i64(&cursor, &record->cooldown);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_cooldown = tb_is_present(presence, row);

        if (!table->records[row].has_cooldown)
          table->records[row].cooldown = 0;
      }
      break;

    case 9:
      (void)tb_check_column(reader, column, "Drop.Batch", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_UUID));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        Optional_DropRecord_t* record = &table->records[row];

        (void)tb_read_uuid(reader, &record->batch);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_batch = tb_is_present(presence, row);

        if (!table->records[row].has_batch)
          memset(&table->records[row].batch, 0, sizeof table->records[row].batch);
      }
      break;

    case 10:
      (void)tb_check_column(reader, column, "Drop.Grade", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_VARINT));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Grade");

      {
        int32_t run_length = 0;
        int32_t value = 0;

        row = 0;

        while (row < table->count && !tb_failed(reader)) {
          if (!tb_cursor_next_same_i32(&cursor, table->count - row, &run_length, &value))
            break;

          for (; run_length > 0; --run_length, ++row)
            table->records[row].grade = (Optional_Rarity_t)value;
        }
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_grade = tb_is_present(presence, row);

        if (!table->records[row].has_grade)
          table->records[row].grade = (Optional_Rarity_t)0;
      }
      break;

    case 11:
      (void)tb_check_column(reader, column, "Drop.Costs", TB_KIND_VAR_ARRAY, 0, true, TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Costs");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        Optional_DropRecord_t* record = &table->records[row];
        int32_t element_count = 0;
        int32_t element;

        (void)tb_cursor_next_length(&cursor, &element_count);

        record->costs_count = element_count;
        record->costs = (int32_t*)tb_arena_alloc(
          &table->arena, (size_t)element_count * sizeof *record->costs);

        if (element_count > 0 && record->costs == NULL)
          return tb_fail_with(reader, "out of memory allocating an array");

        for (element = 0; element < element_count && !tb_failed(reader); ++element)
          (void)tb_cursor_next_i32(&cursor, &record->costs[element]);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_costs = tb_is_present(presence, row);

        if (!table->records[row].has_costs)
          memset(&table->records[row].costs, 0, sizeof table->records[row].costs);
      }
      break;

    case 12:
      (void)tb_check_column(reader, column, "Drop.Label", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_STRING));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Label");

      {
        int32_t run_length = 0;
        const char* value = NULL;

        row = 0;

        while (row < table->count && !tb_failed(reader)) {
          if (!tb_cursor_next_same_string(&cursor, table->count - row, &run_length, &value))
            break;

          for (; run_length > 0; --run_length, ++row)
            table->records[row].label = value;
        }
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_label = tb_is_present(presence, row);

        if (!table->records[row].has_label)
          table->records[row].label = "";
      }
      break;

    case 13:
      (void)tb_check_column(reader, column, "Drop.Hidden", TB_KIND_SCALAR, 1, true, TB_ELEMENT_MASK(TB_ELEMENT_BOOL));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Drop.Hidden");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        Optional_DropRecord_t* record = &table->records[row];

        (void)tb_cursor_next_bool(&cursor, &record->hidden);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_hidden = tb_is_present(presence, row);

        if (!table->records[row].has_hidden)
          table->records[row].hidden = false;
      }
      break;

    default:
      /* A column this build has no member for: added to the schema after the code
       * was generated, or removed from the code while the data still carries it.
       * The block's declared length is the whole of what skipping needs to know. */
      (void)tb_skip(reader, column->byte_length);
      break;
    }

    if (!tb_check_block_end(reader, column, block_end))
      return false;
  }

  if (tb_failed(reader))
    return false;

  /* The keys can only be gathered once every column has been read: the column that
   * carries one arrives wherever the file happens to put it. */
  for (row = 0; row < table->count; ++row) {
    table->by_index[row].key = table->records[row].index;
    table->by_index[row].position = row;
  }

  tb_index_sort(table->by_index, table->count);

  return true;
}

bool Optional_DropLoad(Optional_DropTable_t* table, const char* filename,
                   char* error, size_t error_size) {
  uint8_t* bytes = NULL;
  int32_t length = 0;
  const uint8_t* opened = NULL;
  int32_t opened_length = 0;
  tb_reader reader;
  bool ok;

  /* Loaded beside whatever the table is already holding, arena and all, and swapped in
   * only if it worked. That is what makes a reload safe: reading a patched file that
   * turns out to be truncated - or to carry a column this build cannot read - leaves the
   * rows the caller has pointers to exactly where they were.
   *
   * It also has to be this way round for the memory. The table owns its arena, and
   * zeroing the table before reading - which is what this did - dropped that arena
   * without freeing it, so every reload leaked the whole of the previous load. */
  Optional_DropTable_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (!tb_read_all_bytes(filename, &bytes, &length)) {
    tb_copy_error(error, error_size, filename, "cannot be read");
    return false;
  }

  /* Opened unconditionally, with whatever Optional_EncryptionKey and
   * Optional_MacKey hold - NULL unless the project set them. A file that is
   * neither encrypted nor signed comes back from this untouched, so the load path is the
   * same either way and there is no condition here that could be the wrong way round.
   * spec/tcb-mac-and-signature.md.
   *
   * What comes back is a window onto `bytes` rather than a copy of it, which is why the
   * buffer is still what tb_free_bytes is given below. */
  if (!tb_open(bytes, length, Optional_EncryptionKey,
        Optional_EncryptionKeyLength, Optional_MacKey,
        Optional_MacKeyLength, Optional_VerifyMac,
        &opened, &opened_length, error, error_size)) {
    tb_free_bytes(bytes);
    return false;
  }

  tb_reader_init(&reader, opened, opened_length, &loaded.arena);

  ok = Optional_DropParse(&loaded, &reader);

  if (ok) {
    /* The previous load goes now, and not before: its arena holds the strings and
     * arrays the caller was reading up to this line. */
    Optional_DropFree(table);
    *table = loaded;
  } else {
    tb_copy_error(error, error_size, filename, tb_error(&reader));

    /* Nothing half-read is kept, and the table is untouched. */
    Optional_DropFree(&loaded);
  }

  /* The strings were copied into the arena, so the file buffer goes either way. */
  tb_free_bytes(bytes);

  return ok;
}

void Optional_DropFree(Optional_DropTable_t* table) {
  tb_arena_free(&table->arena);

  memset(table, 0, sizeof *table);
}

const Optional_DropRecord_t* Optional_DropFindByIndex(
  const Optional_DropTable_t* table, int32_t key) {
  int32_t position = tb_index_find(table->by_index, table->count, key);

  return position < 0 ? NULL : &table->records[position];
}

bool Optional_DropContainsIndex(
  const Optional_DropTable_t* table, int32_t key) {
  return tb_index_find(table->by_index, table->count, key) >= 0;
}
