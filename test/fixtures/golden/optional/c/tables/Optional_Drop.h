/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef OPTIONAL_DROP_H
#define OPTIONAL_DROP_H

#include "tabbit/tabbit_tcb_reader.h"

#include "Optional_Forward.h"
#include "enums/Optional_EnumRarity.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/optional/optional.xlsx : Optional : F2
 *
 * Optional columns, each beside the same type left required.
 */
struct Optional_DropRecord_t {
  /* primary index, never optional */
  int32_t index;
  /* required, so a blank here would be an error */
  int32_t hp;
  /* optional int */
  int32_t bonus;
  /* Whether this row has a value for bonus. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_bonus;
  /* optional double */
  double weight;
  /* Whether this row has a value for weight. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_weight;
  /* optional float */
  float ratio;
  /* Whether this row has a value for ratio. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_ratio;
  /* optional bigint */
  int64_t count;
  /* Whether this row has a value for count. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_count;
  /* optional datetime */
  int64_t open_at;
  /* Whether this row has a value for open_at. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_open_at;
  /* optional timespan */
  int64_t cooldown;
  /* Whether this row has a value for cooldown. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_cooldown;
  /* optional uuid */
  tb_uuid batch;
  /* Whether this row has a value for batch. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_batch;
  /* optional enum label */
  Optional_Rarity_t grade;
  /* Whether this row has a value for grade. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_grade;
  /* optional array */
  int32_t* costs;
  int32_t costs_count;
  /* Whether this row has a value for costs. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_costs;
  /* optional string */
  const char* label;
  /* Whether this row has a value for label. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_label;
  /* optional bool */
  bool hidden;
  /* Whether this row has a value for hidden. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_hidden;
};

/* Every row of Drop.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Optional_DropTable_t {
  Optional_DropRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} Optional_DropTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Optional_DropLoad(Optional_DropTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Optional_DropFree(Optional_DropTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Optional_DropRecord_t* Optional_DropFindByIndex(
  const Optional_DropTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool Optional_DropContainsIndex(
  const Optional_DropTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* OPTIONAL_DROP_H */
