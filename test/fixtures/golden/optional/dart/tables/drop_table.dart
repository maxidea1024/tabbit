// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from test/fixtures/xlsx/optional/optional.xlsx : Optional : F2
/// Optional columns, each beside the same type left required.
class DropRecord {
  /// primary index, never optional
  int index = 0;
  /// required, so a blank here would be an error
  int hp = 0;
  /// optional int
  int bonus = 0;
  bool hasBonus = false;
  /// optional double
  double weight = 0.0;
  bool hasWeight = false;
  /// optional float
  double ratio = 0.0;
  bool hasRatio = false;
  /// optional bigint
  BigInt count = BigInt.zero;
  bool hasCount = false;
  /// optional datetime
  BigInt openAt = BigInt.zero;
  bool hasOpenAt = false;
  /// optional timespan
  BigInt cooldown = BigInt.zero;
  bool hasCooldown = false;
  /// optional uuid
  Uuid batch = Uuid.empty();
  bool hasBatch = false;
  /// optional enum label
  Rarity grade = Rarity.of(0);
  bool hasGrade = false;
  /// optional array
  List<int> costs = [];
  bool hasCosts = false;
  /// optional string
  String label = '';
  bool hasLabel = false;
  /// optional bool
  bool hidden = false;
  bool hasHidden = false;

}

/// Every row of Drop.
class DropTable extends Iterable<DropRecord> {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<DropRecord> get records => _records;
  List<DropRecord> _records = [];

  /// How many rows the table holds.
  ///
  /// Overridden rather than inherited: `Iterable.length` counts by walking, and the rows
  /// are a list that already knows.
  @override
  int get length => _records.length;

  /// The rows, in the order the file wrote them. Extending `Iterable` on top of this is
  /// what gives the table `map` - `where` - `firstWhere` without any of them being
  /// written here.
  ///
  /// The list reference is read once, here. A refresh replaces the reference rather than
  /// its contents, so a loop already running keeps the rows it started with.
  @override
  Iterator<DropRecord> get iterator => _records.iterator;

  Map<int, DropRecord> _byIndex = {};

  /// The row with this Index, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  DropRecord? findByIndex(int key) => _byIndex[key];

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getByIndex(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  DropRecord getByIndexOrThrow(int key) {
    final record = _byIndex[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `Drop` that corresponds to '
          'field `Index` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Index.
  bool containsIndex(int key) => _byIndex.containsKey(key);


  /// The row with this Index, or null when the table has none.
  ///
  /// `findByIndex` under the spelling Dart uses for a keyed collection. It
  /// answers with null because that is what `Map` does here, and the type says so - a
  /// language whose own map throws generates a subscript that throws.
  /// spec/targets/table-collection-surface.md section 5.7.
  ///
  /// This is the key, not the row's position. `table[0]` is the row whose
  /// Index is 0, not the first row - the rows in order are `records`.
  ///
  /// Only a key of one column has this: Dart's subscript takes one argument.
  DropRecord? operator [](int key) => findByIndex(key);


  /// Each row with the Index it is keyed by -
  /// `for (final entry in table.entries)`.
  ///
  /// What this saves a caller is not the key value - the row carries it - but having to
  /// know which column the key is: Index here, something else in the next
  /// table.
  ///
  /// The rows come in the order the file wrote them rather than the order the map holds
  /// them, which makes this and iterating the table agree. Only the primary key has this:
  /// a table keyed by several columns together has no single key value to pair a row with.
  Iterable<MapEntry<int, DropRecord>> get entries =>
      _records.map((record) => MapEntry(record.index, record));

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;
    late List<int> presence;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <DropRecord>[];
    final loadedByIndex = <int, DropRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(DropRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'Drop.Index', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Index');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].index = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'Drop.Hp', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Hp');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].hp = value;
            }
          }
          break;
        case 3:
          checkColumn(column, 'Drop.Bonus', kindScalar, true, [elementI32, elementVarint]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Bonus');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].bonus = value;
            }
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasBonus = isPresent(presence, row);

            if (!record.hasBonus) {
              record.bonus = 0;
            }
          }
          break;
        case 4:
          checkColumn(column, 'Drop.Weight', kindScalar, true, [elementF64, elementF32, elementI32]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Weight');
          for (final record in loaded) {
            record.weight = cursor.nextF64();
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasWeight = isPresent(presence, row);

            if (!record.hasWeight) {
              record.weight = 0.0;
            }
          }
          break;
        case 5:
          checkColumn(column, 'Drop.Ratio', kindScalar, true, [elementF32]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Ratio');
          for (final record in loaded) {
            record.ratio = cursor.nextF32();
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasRatio = isPresent(presence, row);

            if (!record.hasRatio) {
              record.ratio = 0.0;
            }
          }
          break;
        case 6:
          checkColumn(column, 'Drop.Count', kindScalar, true, [elementI64, elementI32, elementVarint]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Count');
          for (final record in loaded) {
            record.count = cursor.nextI64();
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasCount = isPresent(presence, row);

            if (!record.hasCount) {
              record.count = BigInt.zero;
            }
          }
          break;
        case 7:
          checkColumn(column, 'Drop.OpenAt', kindScalar, true, [elementI64]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.OpenAt');
          for (final record in loaded) {
            record.openAt = cursor.nextI64();
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasOpenAt = isPresent(presence, row);

            if (!record.hasOpenAt) {
              record.openAt = BigInt.zero;
            }
          }
          break;
        case 8:
          checkColumn(column, 'Drop.Cooldown', kindScalar, true, [elementI64]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Cooldown');
          for (final record in loaded) {
            record.cooldown = cursor.nextI64();
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasCooldown = isPresent(presence, row);

            if (!record.hasCooldown) {
              record.cooldown = BigInt.zero;
            }
          }
          break;
        case 9:
          checkColumn(column, 'Drop.Batch', kindScalar, true, [elementUuid]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          for (final record in loaded) {
            record.batch = reader.readUuid();
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasBatch = isPresent(presence, row);

            if (!record.hasBatch) {
              record.batch = Uuid.empty();
            }
          }
          break;
        case 10:
          checkColumn(column, 'Drop.Grade', kindScalar, true, [elementVarint]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Grade');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].grade = Rarity.of(value);
            }
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasGrade = isPresent(presence, row);

            if (!record.hasGrade) {
              record.grade = Rarity.of(0);
            }
          }
          break;
        case 11:
          checkColumn(column, 'Drop.Costs', kindArray, true, [elementI32, elementVarint]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Costs');
          for (final record in loaded) {
            final elementCount = cursor.nextLength();
            record.costs = List.generate(elementCount, (_) => cursor.nextI32());
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasCosts = isPresent(presence, row);

            if (!record.hasCosts) {
              record.costs = [];
            }
          }
          break;
        case 12:
          checkColumn(column, 'Drop.Label', kindScalar, true, [elementString]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Label');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].label = value;
            }
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasLabel = isPresent(presence, row);

            if (!record.hasLabel) {
              record.label = '';
            }
          }
          break;
        case 13:
          checkColumn(column, 'Drop.Hidden', kindScalar, true, [elementBool]);
          // The bitmap is at the front of the block, so it is read before the values. The
          // values are written for every row either way, which is what lets the read shapes
          // below stay as they are.
          presence = readPresence(reader, column, count);
          cursor = TcbColumnCursor(reader, column, count, 'Drop.Hidden');
          for (final record in loaded) {
            record.hidden = cursor.nextBool();
          }

          // A pass of its own rather than a line inside each read shape, so the row index is
          // worked out once. The block carries a value for every row, so an absent one has
          // just been given whatever was there; putting the empty value back is what makes
          // this path agree with the JSON one.
          for (var row = 0; row < count; row++) {
            final record = loaded[row];
            record.hasHidden = isPresent(presence, row);

            if (!record.hasHidden) {
              record.hidden = false;
            }
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedByIndex[record.index] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byIndex = loadedByIndex;
  }
}
