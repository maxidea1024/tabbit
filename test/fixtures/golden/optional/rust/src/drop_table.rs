// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

use std::collections::HashMap;
use std::path::Path;
use crate::tabbit;
use crate::enum_rarity::Rarity;

// Generated from test/fixtures/xlsx/optional/optional.xlsx : Optional : F2
/// Optional columns, each beside the same type left required.
#[derive(Clone, Debug, Default)]
pub struct DropRecord {
    /// primary index, never optional
    pub index: i32,
    /// required, so a blank here would be an error
    pub hp: i32,
    /// optional int
    pub bonus: i32,
    /// Whether this row has a value for `bonus`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_bonus: bool,
    /// optional double
    pub weight: f64,
    /// Whether this row has a value for `weight`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_weight: bool,
    /// optional float
    pub ratio: f32,
    /// Whether this row has a value for `ratio`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_ratio: bool,
    /// optional bigint
    pub count: i64,
    /// Whether this row has a value for `count`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_count: bool,
    /// optional datetime
    pub open_at: i64,
    /// Whether this row has a value for `open_at`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_open_at: bool,
    /// optional timespan
    pub cooldown: i64,
    /// Whether this row has a value for `cooldown`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_cooldown: bool,
    /// optional uuid
    pub batch: tabbit::Uuid,
    /// Whether this row has a value for `batch`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_batch: bool,
    /// optional enum label
    pub grade: Rarity,
    /// Whether this row has a value for `grade`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_grade: bool,
    /// optional array
    pub costs: Vec<i32>,
    /// Whether this row has a value for `costs`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_costs: bool,
    /// optional string
    pub label: String,
    /// Whether this row has a value for `label`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_label: bool,
    /// optional bool
    pub hidden: bool,
    /// Whether this row has a value for `hidden`. The value member keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    pub has_hidden: bool,
}

impl DropRecord {
}

/// Every row of Drop.
#[derive(Clone, Debug, Default)]
pub struct DropTable {
    records: Vec<DropRecord>,
    by_index: HashMap<i32, usize>,
}

impl DropTable {
    /// Every row, in the order the sheet declared them.
    pub fn records(&self) -> &[DropRecord] {
        &self.records
    }

    /// The row with this Index, or None when the table has none.
    ///
    /// The lookup to reach for when a missing row is an ordinary answer - an optional
    /// reference, a key that came from user input.
    pub fn find_by_index(&self, key: i32) -> Option<&DropRecord> {
        self.by_index.get(&key).map(|position| &self.records[*position])
    }

    /// The row with this Index, or an error naming what was missing.
    ///
    /// For a key that has to be there - one from another table, or a constant. Rust has no
    /// exception to throw, so where the other languages name a throw this one names the
    /// error it returns.
    pub fn get_by_index_or_error(&self, key: i32) -> tabbit::Result<&DropRecord> {
        self.by_index
            .get(&key)
            .map(|position| &self.records[*position])
            .ok_or_else(|| tabbit::Error::RecordNotFound {
                table: "Drop",
                field: "Index",
                key: format!("{:?}", key),
            })
    }

    /// Whether the table holds a row with this Index.
    pub fn contains_index(&self, key: i32) -> bool {
        self.by_index.contains_key(&key)
    }

    /// Loads the table from a .tcb file written by Tabbit.
    pub fn read(&mut self, filename: &Path) -> tabbit::Result<()> {
        // Mutable, and it outlives the reader on purpose: opening the envelope decrypts in
        // place and hands back a borrow of these same bytes rather than a copy of them.
        let mut data = tabbit::read_all_bytes(filename)?;

        // Unconditionally, and with whatever `ENCRYPTION_KEY` and `MAC_KEY` hold - which is
        // unset unless the project set them. A file that is neither encrypted nor signed
        // comes back from this untouched, so the load path is the same either way and there
        // is no condition here that could be the wrong way round.
        // spec/wire/tcb-mac-and-signature.md.
        let key: &[u8] = crate::ENCRYPTION_KEY.get().map(Vec::as_slice).unwrap_or(&[]);
        let mac_key: &[u8] = crate::MAC_KEY.get().map(Vec::as_slice).unwrap_or(&[]);

        let mut reader = tabbit::Reader::new(tabbit::open(
            &mut data,
            key,
            mac_key,
            crate::VERIFY_MAC.load(std::sync::atomic::Ordering::Relaxed),
        )?);

        // Column by column, matched by tag rather than position: a column this build
        // does not know is skipped by its block length, and one whose type changed
        // incompatibly fails naming the field.
        let header = tabbit::read_table_header(&mut reader)?;
        let count = header.row_count.max(0) as usize;

        // Read into rows of its own and assigned at the end. Reading a table that is
        // already loaded is a refresh, and one that turns out to be unreadable has to
        // leave the rows already there alone - `?` returns before anything is replaced.
        let mut records = vec![DropRecord::default(); count];

        for column in &header.columns {
            let block_end = reader.position() + column.byte_length.max(0) as usize;

            match column.tag {
                1 => {
                    tabbit::check_column(column, "Drop.Index", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_I32, tabbit::ELEMENT_VARINT])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Index")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_i32((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].index = value;
                            at += 1;
                        }
                    }
                }
                2 => {
                    tabbit::check_column(column, "Drop.Hp", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_I32, tabbit::ELEMENT_VARINT])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Hp")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_i32((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].hp = value;
                            at += 1;
                        }
                    }
                }
                3 => {
                    tabbit::check_column(column, "Drop.Bonus", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_I32, tabbit::ELEMENT_VARINT])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Bonus")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_i32((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].bonus = value;
                            at += 1;
                        }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_bonus = tabbit::is_present(&presence, row);

                        if !record.has_bonus {
                            record.bonus = Default::default();
                        }
                    }
                }
                4 => {
                    tabbit::check_column(column, "Drop.Weight", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_F64, tabbit::ELEMENT_F32, tabbit::ELEMENT_I32])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Weight")?;
                    for record in records.iter_mut() {
                        record.weight = cursor.next_f64()?;
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_weight = tabbit::is_present(&presence, row);

                        if !record.has_weight {
                            record.weight = Default::default();
                        }
                    }
                }
                5 => {
                    tabbit::check_column(column, "Drop.Ratio", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_F32])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Ratio")?;
                    for record in records.iter_mut() {
                        record.ratio = cursor.next_f32()?;
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_ratio = tabbit::is_present(&presence, row);

                        if !record.has_ratio {
                            record.ratio = Default::default();
                        }
                    }
                }
                6 => {
                    tabbit::check_column(column, "Drop.Count", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_I64, tabbit::ELEMENT_I32, tabbit::ELEMENT_VARINT])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Count")?;
                    for record in records.iter_mut() {
                        record.count = cursor.next_i64()?;
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_count = tabbit::is_present(&presence, row);

                        if !record.has_count {
                            record.count = Default::default();
                        }
                    }
                }
                7 => {
                    tabbit::check_column(column, "Drop.OpenAt", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_I64])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.OpenAt")?;
                    for record in records.iter_mut() {
                        record.open_at = cursor.next_i64()?;
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_open_at = tabbit::is_present(&presence, row);

                        if !record.has_open_at {
                            record.open_at = Default::default();
                        }
                    }
                }
                8 => {
                    tabbit::check_column(column, "Drop.Cooldown", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_I64])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Cooldown")?;
                    for record in records.iter_mut() {
                        record.cooldown = cursor.next_i64()?;
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_cooldown = tabbit::is_present(&presence, row);

                        if !record.has_cooldown {
                            record.cooldown = Default::default();
                        }
                    }
                }
                9 => {
                    tabbit::check_column(column, "Drop.Batch", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_UUID])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    for record in records.iter_mut() {
                        record.batch = reader.read_uuid()?;
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_batch = tabbit::is_present(&presence, row);

                        if !record.has_batch {
                            record.batch = Default::default();
                        }
                    }
                }
                10 => {
                    tabbit::check_column(column, "Drop.Grade", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_VARINT])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Grade")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_i32((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].grade = Rarity::from_value(value).unwrap_or_default();
                            at += 1;
                        }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_grade = tabbit::is_present(&presence, row);

                        if !record.has_grade {
                            record.grade = Default::default();
                        }
                    }
                }
                11 => {
                    tabbit::check_column(column, "Drop.Costs", tabbit::KIND_ARRAY, true, &[tabbit::ELEMENT_I32, tabbit::ELEMENT_VARINT])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Costs")?;
                    for record in records.iter_mut() {
                        let element_count = cursor.next_length()?.max(0) as usize;
                        record.costs = Vec::with_capacity(element_count.min(65536));
                        for _ in 0..element_count {
                            record.costs.push(cursor.next_i32()?);
                        }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_costs = tabbit::is_present(&presence, row);

                        if !record.has_costs {
                            record.costs = Default::default();
                        }
                    }
                }
                12 => {
                    tabbit::check_column(column, "Drop.Label", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_STRING])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Label")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_string((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].label = value.clone();
                            at += 1;
                        }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_label = tabbit::is_present(&presence, row);

                        if !record.has_label {
                            record.label = Default::default();
                        }
                    }
                }
                13 => {
                    tabbit::check_column(column, "Drop.Hidden", tabbit::KIND_SCALAR, true, &[tabbit::ELEMENT_BOOL])?;
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    let presence = tabbit::read_presence(&mut reader, column, header.row_count)?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Drop.Hidden")?;
                    for record in records.iter_mut() {
                        record.hidden = cursor.next_bool()?;
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (row, record) in records.iter_mut().enumerate() {
                        record.has_hidden = tabbit::is_present(&presence, row);

                        if !record.has_hidden {
                            record.hidden = Default::default();
                        }
                    }
                }
                _ => {
                    // A column added after this code was generated.
                    reader.skip(column.byte_length)?;
                }
            }

            tabbit::check_block_end(&reader, column, block_end)?;
        }

        let by_index = records
            .iter()
            .enumerate()
            .map(|(position, record)| (record.index.clone(), position))
            .collect();

        // Published, now that every column read.
        self.records = records;
        self.by_index = by_index;

        Ok(())
    }
}
