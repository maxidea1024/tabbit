// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_OPTIONALACCESSOR_DROP_H
#define TABBIT_GENERATED_OPTIONALACCESSOR_DROP_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "OptionalAccessor_forward.h"
#include "enums/OptionalAccessor_enum_rarity.h"
// Generated from test/fixtures/xlsx/optional/optional.xlsx : Optional : F2
/// Optional columns, each beside the same type left required.
struct DropRecord {
  /// primary index, never optional
  std::int32_t index = 0;
  /// required, so a blank here would be an error
  std::int32_t hp = 0;
  /// optional int
  std::int32_t bonus = 0;
  /// Whether this row has a value for `bonus`.
  bool has_bonus = false;
  /// optional double
  double weight = 0.0;
  /// Whether this row has a value for `weight`.
  bool has_weight = false;
  /// optional float
  float ratio = 0.0f;
  /// Whether this row has a value for `ratio`.
  bool has_ratio = false;
  /// optional bigint
  std::int64_t count = 0;
  /// Whether this row has a value for `count`.
  bool has_count = false;
  /// optional datetime
  tabbit::DateTime open_at;
  /// Whether this row has a value for `open_at`.
  bool has_open_at = false;
  /// optional timespan
  tabbit::TimeSpan cooldown;
  /// Whether this row has a value for `cooldown`.
  bool has_cooldown = false;
  /// optional uuid
  tabbit::Uuid batch;
  /// Whether this row has a value for `batch`.
  bool has_batch = false;
  /// optional enum label
  Rarity grade = static_cast<Rarity>(0);
  /// Whether this row has a value for `grade`.
  bool has_grade = false;
  /// optional array
  std::vector<std::int32_t> costs;
  /// Whether this row has a value for `costs`.
  bool has_costs = false;
  /// optional string
  std::string label;
  /// Whether this row has a value for `label`.
  bool has_label = false;
  /// optional bool
  bool hidden = false;
  /// Whether this row has a value for `hidden`.
  bool has_hidden = false;
};

/// Optional columns, each beside the same type left required.
class DropTable {
 public:
  const std::vector<DropRecord>& records() const { return records_; }

  /// The row with this Index, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const DropRecord* find_by_index(std::int32_t key) const {
    const auto it = by_index_.find(key);
    return it == by_index_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_index(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const DropRecord& get_by_index_or_throw(std::int32_t key) const {
    const DropRecord* found = find_by_index(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Drop` that corresponds to "
                    "field `Index` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Index.
  bool contains_index(std::int32_t key) const {
    return by_index_.find(key) != by_index_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<DropRecord> records;
    records.resize(row_count);
    std::vector<std::uint8_t> presence;

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Drop.Index", tabbit::kKindScalar, 1, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Index");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].index = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Drop.Hp", tabbit::kKindScalar, 1, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Hp");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].hp = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Drop.Bonus", tabbit::kKindScalar, 1, true, {tabbit::kElementI32, tabbit::kElementVarint});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Bonus");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].bonus = value;
            }
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_bonus = tabbit::is_present(presence, i);

            if (!records[i].has_bonus) {
              records[i].bonus = 0;
            }
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "Drop.Weight", tabbit::kKindScalar, 1, true, {tabbit::kElementF64, tabbit::kElementF32, tabbit::kElementI32});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Weight");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.weight = cursor.next_f64();
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_weight = tabbit::is_present(presence, i);

            if (!records[i].has_weight) {
              records[i].weight = 0;
            }
          }
          break;
        }
        case 5: {
          tabbit::check_column(column, "Drop.Ratio", tabbit::kKindScalar, 1, true, {tabbit::kElementF32});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Ratio");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.ratio = cursor.next_f32();
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_ratio = tabbit::is_present(presence, i);

            if (!records[i].has_ratio) {
              records[i].ratio = 0;
            }
          }
          break;
        }
        case 6: {
          tabbit::check_column(column, "Drop.Count", tabbit::kKindScalar, 1, true, {tabbit::kElementI64, tabbit::kElementI32, tabbit::kElementVarint});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Count");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.count = cursor.next_i64();
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_count = tabbit::is_present(presence, i);

            if (!records[i].has_count) {
              records[i].count = 0;
            }
          }
          break;
        }
        case 7: {
          tabbit::check_column(column, "Drop.OpenAt", tabbit::kKindScalar, 1, true, {tabbit::kElementI64});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.OpenAt");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.open_at = tabbit::from_net_ticks(cursor.next_i64());
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_open_at = tabbit::is_present(presence, i);

            if (!records[i].has_open_at) {
              records[i].open_at = tabbit::DateTime();
            }
          }
          break;
        }
        case 8: {
          tabbit::check_column(column, "Drop.Cooldown", tabbit::kKindScalar, 1, true, {tabbit::kElementI64});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Cooldown");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.cooldown = tabbit::TimeSpan(cursor.next_i64());
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_cooldown = tabbit::is_present(presence, i);

            if (!records[i].has_cooldown) {
              records[i].cooldown = tabbit::TimeSpan();
            }
          }
          break;
        }
        case 9: {
          tabbit::check_column(column, "Drop.Batch", tabbit::kKindScalar, 1, true, {tabbit::kElementUuid});
          presence = tabbit::read_presence(reader, column, row_count);
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.batch);
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_batch = tabbit::is_present(presence, i);

            if (!records[i].has_batch) {
              records[i].batch = tabbit::Uuid();
            }
          }
          break;
        }
        case 10: {
          tabbit::check_column(column, "Drop.Grade", tabbit::kKindScalar, 1, true, {tabbit::kElementVarint});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Grade");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].grade = static_cast<Rarity>(value);
            }
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_grade = tabbit::is_present(presence, i);

            if (!records[i].has_grade) {
              records[i].grade = static_cast<Rarity>(0);
            }
          }
          break;
        }
        case 11: {
          tabbit::check_column(column, "Drop.Costs", tabbit::kKindVarArray, 0, true, {tabbit::kElementI32, tabbit::kElementVarint});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Costs");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = cursor.next_length();
            record.costs.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              record.costs[static_cast<std::size_t>(j)] = cursor.next_i32();
            }
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_costs = tabbit::is_present(presence, i);

            if (!records[i].has_costs) {
              records[i].costs = {};
            }
          }
          break;
        }
        case 12: {
          tabbit::check_column(column, "Drop.Label", tabbit::kKindScalar, 1, true, {tabbit::kElementString});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Label");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].label = value;
            }
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_label = tabbit::is_present(presence, i);

            if (!records[i].has_label) {
              records[i].label = std::string();
            }
          }
          break;
        }
        case 13: {
          tabbit::check_column(column, "Drop.Hidden", tabbit::kKindScalar, 1, true, {tabbit::kElementBool});
          presence = tabbit::read_presence(reader, column, row_count);
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Drop.Hidden");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.hidden = cursor.next_bool();
          }
          for (std::size_t i = 0; i < row_count; ++i) {
            records[i].has_hidden = tabbit::is_present(presence, i);

            if (!records[i].has_hidden) {
              records[i].hidden = false;
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class OptionalAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<DropRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> index_map;
    index_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      index_map.emplace(records[i].index, i);
    }

    records_ = std::move(records);
    by_index_ = std::move(index_map);
  }

  std::vector<DropRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_index_;
};

#endif  // TABBIT_GENERATED_OPTIONALACCESSOR_DROP_H
