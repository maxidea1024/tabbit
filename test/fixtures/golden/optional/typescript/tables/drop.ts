// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { Tables } from '../tables'

// Automatically import to handle external type references.
import { Rarity } from '../enums/rarity'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  index: number
  hp: number
  bonus: number
  weight: number
  ratio: number
  count: string
  openAt: string
  cooldown: string
  batch: string
  grade: Rarity
  costs: number[]
  label: string
  hidden: boolean
}

// Generated from test/fixtures/xlsx/optional/optional.xlsx : Optional : F2
/** Optional columns, each beside the same type left required. */
export class DropRecord {
  /** Default constructor */
  constructor() {
  }

  /** primary index, never optional */
  public get index(): number { return this._index }

  /** required, so a blank here would be an error */
  public get hp(): number { return this._hp }

  /** optional int */
  public get bonus(): number { return this._bonus }
  /** Whether this row has a value for `bonus`. */
  public get hasBonus(): boolean { return this._bonusHasValue }

  /** optional double */
  public get weight(): number { return this._weight }
  /** Whether this row has a value for `weight`. */
  public get hasWeight(): boolean { return this._weightHasValue }

  /** optional float */
  public get ratio(): number { return this._ratio }
  /** Whether this row has a value for `ratio`. */
  public get hasRatio(): boolean { return this._ratioHasValue }

  /** optional bigint */
  public get count(): bigint { return this._count }
  /** Whether this row has a value for `count`. */
  public get hasCount(): boolean { return this._countHasValue }

  /** optional datetime */
  public get openAt(): string { return this._openAt }
  /** Whether this row has a value for `openAt`. */
  public get hasOpenAt(): boolean { return this._openAtHasValue }

  /** optional timespan */
  public get cooldown(): string { return this._cooldown }
  /** Whether this row has a value for `cooldown`. */
  public get hasCooldown(): boolean { return this._cooldownHasValue }

  /** optional uuid */
  public get batch(): string { return this._batch }
  /** Whether this row has a value for `batch`. */
  public get hasBatch(): boolean { return this._batchHasValue }

  /** optional enum label */
  public get grade(): Rarity { return this._grade }
  /** Whether this row has a value for `grade`. */
  public get hasGrade(): boolean { return this._gradeHasValue }

  /** optional array */
  public get costs(): number[] { return this._costs }
  /** Whether this row has a value for `costs`. */
  public get hasCosts(): boolean { return this._costsHasValue }

  /** optional string */
  public get label(): string { return this._label }
  /** Whether this row has a value for `label`. */
  public get hasLabel(): boolean { return this._labelHasValue }

  /** optional bool */
  public get hidden(): boolean { return this._hidden }
  /** Whether this row has a value for `hidden`. */
  public get hasHidden(): boolean { return this._hiddenHasValue }

  public _index: number = 0
  public _hp: number = 0
  public _bonus: number = 0
  public _bonusHasValue: boolean = false
  public _weight: number = 0
  public _weightHasValue: boolean = false
  public _ratio: number = 0
  public _ratioHasValue: boolean = false
  public _count: bigint = 0n
  public _countHasValue: boolean = false
  public _openAt: string = ''
  public _openAtHasValue: boolean = false
  public _cooldown: string = ''
  public _cooldownHasValue: boolean = false
  public _batch: string = ''
  public _batchHasValue: boolean = false
  public _grade: Rarity = 0 as Rarity
  public _gradeHasValue: boolean = false
  public _costs: number[] = []
  public _costsHasValue: boolean = false
  public _label: string = ''
  public _labelHasValue: boolean = false
  public _hidden: boolean = false
  public _hiddenHasValue: boolean = false

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._index = dataRow.index
    this._hp = dataRow.hp
    this._bonusHasValue = dataRow.bonus !== null && dataRow.bonus !== undefined; if (this._bonusHasValue) this._bonus = dataRow.bonus
    this._weightHasValue = dataRow.weight !== null && dataRow.weight !== undefined; if (this._weightHasValue) this._weight = dataRow.weight
    this._ratioHasValue = dataRow.ratio !== null && dataRow.ratio !== undefined; if (this._ratioHasValue) this._ratio = Math.fround(dataRow.ratio)
    this._countHasValue = dataRow.count !== null && dataRow.count !== undefined; if (this._countHasValue) this._count = BigInt(dataRow.count)
    this._openAtHasValue = dataRow.openAt !== null && dataRow.openAt !== undefined; if (this._openAtHasValue) this._openAt = dataRow.openAt
    this._cooldownHasValue = dataRow.cooldown !== null && dataRow.cooldown !== undefined; if (this._cooldownHasValue) this._cooldown = dataRow.cooldown
    this._batchHasValue = dataRow.batch !== null && dataRow.batch !== undefined; if (this._batchHasValue) this._batch = dataRow.batch
    this._gradeHasValue = dataRow.grade !== null && dataRow.grade !== undefined; if (this._gradeHasValue) this._grade = dataRow.grade
    this._costsHasValue = dataRow.costs !== null && dataRow.costs !== undefined; if (this._costsHasValue) this._costs = dataRow.costs
    this._labelHasValue = dataRow.label !== null && dataRow.label !== undefined; if (this._labelHasValue) this._label = dataRow.label
    this._hiddenHasValue = dataRow.hidden !== null && dataRow.hidden !== undefined; if (this._hiddenHasValue) this._hidden = dataRow.hidden
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._index = dataRow[offset++]
    this._hp = dataRow[offset++]
    const _bonus_raw = dataRow[offset++]
    this._bonusHasValue = _bonus_raw !== null && _bonus_raw !== undefined
    if (this._bonusHasValue) this._bonus = _bonus_raw
    const _weight_raw = dataRow[offset++]
    this._weightHasValue = _weight_raw !== null && _weight_raw !== undefined
    if (this._weightHasValue) this._weight = _weight_raw
    const _ratio_raw = dataRow[offset++]
    this._ratioHasValue = _ratio_raw !== null && _ratio_raw !== undefined
    if (this._ratioHasValue) this._ratio = Math.fround(_ratio_raw)
    const _count_raw = dataRow[offset++]
    this._countHasValue = _count_raw !== null && _count_raw !== undefined
    if (this._countHasValue) this._count = BigInt(_count_raw)
    const _openAt_raw = dataRow[offset++]
    this._openAtHasValue = _openAt_raw !== null && _openAt_raw !== undefined
    if (this._openAtHasValue) this._openAt = _openAt_raw
    const _cooldown_raw = dataRow[offset++]
    this._cooldownHasValue = _cooldown_raw !== null && _cooldown_raw !== undefined
    if (this._cooldownHasValue) this._cooldown = _cooldown_raw
    const _batch_raw = dataRow[offset++]
    this._batchHasValue = _batch_raw !== null && _batch_raw !== undefined
    if (this._batchHasValue) this._batch = _batch_raw
    const _grade_raw = dataRow[offset++]
    this._gradeHasValue = _grade_raw !== null && _grade_raw !== undefined
    if (this._gradeHasValue) this._grade = _grade_raw
    this._costs = dataRow[offset++]
    const _label_raw = dataRow[offset++]
    this._labelHasValue = _label_raw !== null && _label_raw !== undefined
    if (this._labelHasValue) this._label = _label_raw
    const _hidden_raw = dataRow[offset++]
    this._hiddenHasValue = _hidden_raw !== null && _hidden_raw !== undefined
    if (this._hiddenHasValue) this._hidden = _hidden_raw
  }
}

// Generated from test/fixtures/xlsx/optional/optional.xlsx : Optional : F2
/** Optional columns, each beside the same type left required. */
export class DropTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): DropRecord[] { return this._records }
  private _records: DropRecord[] = []

  // Indexing by 'index'
  public get recordsByIndex(): Map<number, DropRecord> { return this._recordsByIndex }
  private _recordsByIndex: Map<number, DropRecord> = new Map<number, DropRecord>()

  /**
   * The row with this index, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByIndex(key: number): DropRecord | undefined {
    return this._recordsByIndex.get(key)
  }

  /**
   * The row with this index, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByIndex(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIndexOrThrow(key: number): DropRecord {
    const found = this._recordsByIndex.get(key)
    if (!found)
      throw new Error(`There is no record in table "Drop" that corresponds to field "index" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this index. */
  public containsIndex(key: number): boolean {
    return this._recordsByIndex.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: DropRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new DropRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new DropRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `Tables.encryptionKey` and `Tables.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      Tables.encryptionKey, Tables.macKey, Tables.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor
    let presence: Uint8Array | null

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: DropRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new DropRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'Drop.Index', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Index')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._index = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'Drop.Hp', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Hp')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._hp = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'Drop.Bonus', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Bonus')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._bonus = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._bonusHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._bonusHasValue)
              records[i]._bonus = 0
          }
          break
        case 4:
          tabbit.checkColumn(column, 'Drop.Weight', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_F64, tabbit.ELEMENT_F32, tabbit.ELEMENT_I32])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Weight')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._weight = cursor.nextF64()
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._weightHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._weightHasValue)
              records[i]._weight = 0
          }
          break
        case 5:
          tabbit.checkColumn(column, 'Drop.Ratio', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_F32])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Ratio')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._ratio = cursor.nextF32()
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._ratioHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._ratioHasValue)
              records[i]._ratio = 0
          }
          break
        case 6:
          tabbit.checkColumn(column, 'Drop.Count', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_I64, tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Count')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._count = cursor.nextI64()
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._countHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._countHasValue)
              records[i]._count = 0n
          }
          break
        case 7:
          tabbit.checkColumn(column, 'Drop.OpenAt', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_I64])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.OpenAt')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._openAt = tabbit.formatDateTimeTicks(cursor.nextI64())
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._openAtHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._openAtHasValue)
              records[i]._openAt = ''
          }
          break
        case 8:
          tabbit.checkColumn(column, 'Drop.Cooldown', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_I64])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Cooldown')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._cooldown = tabbit.formatTimeSpanTicks(cursor.nextI64())
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._cooldownHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._cooldownHasValue)
              records[i]._cooldown = ''
          }
          break
        case 9:
          tabbit.checkColumn(column, 'Drop.Batch', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_UUID])
          presence = tabbit.readPresence(reader, column, rowCount)
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._batch = reader.readUuid()
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._batchHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._batchHasValue)
              records[i]._batch = ''
          }
          break
        case 10:
          tabbit.checkColumn(column, 'Drop.Grade', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Grade')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._grade = value as Rarity
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._gradeHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._gradeHasValue)
              records[i]._grade = 0 as Rarity
          }
          break
        case 11:
          tabbit.checkColumn(column, 'Drop.Costs', tabbit.KIND_ARRAY, true, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Costs')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = cursor.nextLength()
            record._costs = []
            for (let j = 0; j < elementCount; ++j) {
              record._costs.push(cursor.nextI32())
            }
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._costsHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._costsHasValue)
              records[i]._costs = []
          }
          break
        case 12:
          tabbit.checkColumn(column, 'Drop.Label', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_STRING])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Label')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._label = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._labelHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._labelHasValue)
              records[i]._label = ''
          }
          break
        case 13:
          tabbit.checkColumn(column, 'Drop.Hidden', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_BOOL])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Drop.Hidden')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._hidden = cursor.nextBool()
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._hiddenHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._hiddenHasValue)
              records[i]._hidden = false
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: DropRecord[]): void {
    const recordsByIndex = new Map<number, DropRecord>()

    for (const record of records) {
      recordsByIndex.set(record.index, record)
    }

    this._records = records
    this._recordsByIndex = recordsByIndex
  }
}
