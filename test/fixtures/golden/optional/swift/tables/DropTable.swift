// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import Foundation

// Generated from test/fixtures/xlsx/optional/optional.xlsx : Optional : F2
/// Optional columns, each beside the same type left required.
public final class DropRecord {

    public init() {}

    /// primary index, never optional
    public var index: Int32 = 0

    /// required, so a blank here would be an error
    public var hp: Int32 = 0

    /// optional int
    public var bonus: Int32 = 0
    /// Whether this row has a value for bonus. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasBonus: Bool = false

    /// optional double
    public var weight: Double = 0
    /// Whether this row has a value for weight. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasWeight: Bool = false

    /// optional float
    public var ratio: Float = 0
    /// Whether this row has a value for ratio. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasRatio: Bool = false

    /// optional bigint
    public var count: Int64 = 0
    /// Whether this row has a value for count. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasCount: Bool = false

    /// optional datetime
    public var openAt: Int64 = 0
    /// Whether this row has a value for openAt. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasOpenAt: Bool = false

    /// optional timespan
    public var cooldown: Int64 = 0
    /// Whether this row has a value for cooldown. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasCooldown: Bool = false

    /// optional uuid
    public var batch: Tcb.Uuid = Tcb.Uuid()
    /// Whether this row has a value for batch. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasBatch: Bool = false

    /// optional enum label
    public var grade: Rarity = Rarity.of(0)
    /// Whether this row has a value for grade. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasGrade: Bool = false

    /// optional array
    public var costs: [Int32] = []
    /// Whether this row has a value for costs. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasCosts: Bool = false

    /// optional string
    public var label: String = ""
    /// Whether this row has a value for label. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasLabel: Bool = false

    /// optional bool
    public var hidden: Bool = false
    /// Whether this row has a value for hidden. The value property keeps its type
    /// and holds the type's empty value when the row had none; this says which it was.
    public var hasHidden: Bool = false
}

/// Every row of Drop.
public final class DropTable {

    public init() {}

    /// Every row, in the order the sheet declared them.
    ///
    /// The array a read published, not one a read fills. A refresh replaces the value rather
    /// than appending to it, so whoever holds this holds one whole load.
    public private(set) var records: [DropRecord] = []

    private var byIndex: [Int32: DropRecord] = [:]

    /// The row with this Index, or nil when the table has none.
    ///
    /// The lookup to reach for when a missing row is an ordinary answer - an optional
    /// reference, a key that came from user input.
    public func findByIndex(_ key: Int32) -> DropRecord? {
        byIndex[key]
    }

    /// The row with this Index, or a thrown error naming what was missing.
    ///
    /// For a key that has to be there - one from another table, or a constant. The name says
    /// it throws, because a caller reading getByIndexOrThrow(key).name at a
    /// glance cannot otherwise tell whether the next line is a nil check or a catch.
    public func getByIndexOrThrow(_ key: Int32) throws -> DropRecord {
        guard let record = byIndex[key] else {
            throw RecordNotFoundError(
                "there is no record in table `Drop` that corresponds to "
                + "field `Index` value \(key)")
        }

        return record
    }

    /// Whether the table holds a row with this Index.
    public func containsIndex(_ key: Int32) -> Bool {
        byIndex[key] != nil
    }

    /// Loads the table from a .tcb file written by Tabbit.
    ///
    /// Column by column, matched by tag rather than position: a column this build does not
    /// know is skipped by its block length, and one whose type changed incompatibly throws
    /// naming the field.
    ///
    /// The keys are parameters rather than properties of a shared object, because Swift's
    /// concurrency checking makes mutable global state an error and because the accessor
    /// holding them is an instance - which is what lets two loads of two data sets exist at
    /// once. OptionalAccessor passes its own. spec/accessor-instances.md.
    public func read(
        _ filename: String,
        key: [UInt8]? = nil,
        macKey: [UInt8]? = nil,
        verifyMac: Bool = true
    ) throws {
        // Opened unconditionally, and with whatever the caller holds - which is nil unless
        // the project set it. A file that is neither encrypted nor signed comes back from
        // this untouched, so the load path is the same either way and there is no condition
        // here that could be the wrong way round. spec/tcb-mac-and-signature.md.
        let bytes = try Tcb.openTable(
            try Tcb.readAllBytes(filename), key: key, macKey: macKey, verifyMac: verifyMac)

        let reader = Tcb.Reader(bytes)
        let header = try Tcb.readTableHeader(reader)
        let count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is
        // already loaded is a refresh, and one that turns out to be unreadable has to leave
        // the rows already there alone.
        var loaded = [DropRecord]()
        loaded.reserveCapacity(count)

        for _ in 0 ..< count { loaded.append(DropRecord()) }
        var loadedByIndex = [Int32: DropRecord](
            minimumCapacity: count)

        for column in header.columns {
            let blockEnd = reader.position + column.byteLength

            switch column.tag {
            case 1:
                try Tcb.checkColumn(column, "Drop.Index", Tcb.kindScalar, false, Tcb.elementI32, Tcb.elementVarint)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Index")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].index = cursor.runSameValue
                        at += 1
                        n -= 1
                    }
                }
            case 2:
                try Tcb.checkColumn(column, "Drop.Hp", Tcb.kindScalar, false, Tcb.elementI32, Tcb.elementVarint)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Hp")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].hp = cursor.runSameValue
                        at += 1
                        n -= 1
                    }
                }
            case 3:
                try Tcb.checkColumn(column, "Drop.Bonus", Tcb.kindScalar, true, Tcb.elementI32, Tcb.elementVarint)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Bonus")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].bonus = cursor.runSameValue
                        at += 1
                        n -= 1
                    }
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasBonus = Tcb.isPresent(presence, row)

                    if !record.hasBonus {
                        record.bonus = 0
                    }
                }
            case 4:
                try Tcb.checkColumn(column, "Drop.Weight", Tcb.kindScalar, true, Tcb.elementF64, Tcb.elementF32, Tcb.elementI32)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Weight")
                for record in loaded {
                    record.weight = try cursor.nextF64()
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasWeight = Tcb.isPresent(presence, row)

                    if !record.hasWeight {
                        record.weight = 0
                    }
                }
            case 5:
                try Tcb.checkColumn(column, "Drop.Ratio", Tcb.kindScalar, true, Tcb.elementF32)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Ratio")
                for record in loaded {
                    record.ratio = try cursor.nextF32()
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasRatio = Tcb.isPresent(presence, row)

                    if !record.hasRatio {
                        record.ratio = 0
                    }
                }
            case 6:
                try Tcb.checkColumn(column, "Drop.Count", Tcb.kindScalar, true, Tcb.elementI64, Tcb.elementI32, Tcb.elementVarint)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Count")
                for record in loaded {
                    record.count = try cursor.nextI64()
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasCount = Tcb.isPresent(presence, row)

                    if !record.hasCount {
                        record.count = 0
                    }
                }
            case 7:
                try Tcb.checkColumn(column, "Drop.OpenAt", Tcb.kindScalar, true, Tcb.elementI64)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.OpenAt")
                for record in loaded {
                    record.openAt = try cursor.nextI64()
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasOpenAt = Tcb.isPresent(presence, row)

                    if !record.hasOpenAt {
                        record.openAt = 0
                    }
                }
            case 8:
                try Tcb.checkColumn(column, "Drop.Cooldown", Tcb.kindScalar, true, Tcb.elementI64)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Cooldown")
                for record in loaded {
                    record.cooldown = try cursor.nextI64()
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasCooldown = Tcb.isPresent(presence, row)

                    if !record.hasCooldown {
                        record.cooldown = 0
                    }
                }
            case 9:
                try Tcb.checkColumn(column, "Drop.Batch", Tcb.kindScalar, true, Tcb.elementUuid)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                for record in loaded {
                    record.batch = try reader.readUuid()
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasBatch = Tcb.isPresent(presence, row)

                    if !record.hasBatch {
                        record.batch = Tcb.Uuid()
                    }
                }
            case 10:
                try Tcb.checkColumn(column, "Drop.Grade", Tcb.kindScalar, true, Tcb.elementVarint)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Grade")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].grade = Rarity.of(cursor.runSameValue)
                        at += 1
                        n -= 1
                    }
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasGrade = Tcb.isPresent(presence, row)

                    if !record.hasGrade {
                        record.grade = Rarity.of(0)
                    }
                }
            case 11:
                try Tcb.checkColumn(column, "Drop.Costs", Tcb.kindArray, true, Tcb.elementI32, Tcb.elementVarint)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Costs")
                for record in loaded {
                    let elementCount = max(0, try cursor.nextLength())
                    record.costs = []
                    record.costs.reserveCapacity(elementCount)

                    for _ in 0 ..< elementCount {
                        record.costs.append(try cursor.nextI32())
                    }
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasCosts = Tcb.isPresent(presence, row)

                    if !record.hasCosts {
                        record.costs = []
                    }
                }
            case 12:
                try Tcb.checkColumn(column, "Drop.Label", Tcb.kindScalar, true, Tcb.elementString)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Label")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameString(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].label = cursor.runSameText
                        at += 1
                        n -= 1
                    }
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasLabel = Tcb.isPresent(presence, row)

                    if !record.hasLabel {
                        record.label = ""
                    }
                }
            case 13:
                try Tcb.checkColumn(column, "Drop.Hidden", Tcb.kindScalar, true, Tcb.elementBool)
                // The bitmap is at the front of the block, so it is read before the values.
                // The values are written for every row either way, which is what lets the
                // read shapes below stay as they are.
                let presence = try Tcb.readPresence(reader, column, count)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Drop.Hidden")
                for record in loaded {
                    record.hidden = try cursor.nextBool()
                }

                // A pass of its own rather than a line inside each read shape, so the row
                // index is worked out once. The block carries a value for every row, so an
                // absent one has just been given whatever was there; putting the empty value
                // back is what makes this path agree with the JSON one.
                for row in 0 ..< count {
                    let record = loaded[row]
                    record.hasHidden = Tcb.isPresent(presence, row)

                    if !record.hasHidden {
                        record.hidden = false
                    }
                }
            default:
                // A column added after this code was generated.
                try reader.skip(column.byteLength)
            }

            try Tcb.checkBlockEnd(reader, column, blockEnd)
        }

        for record in loaded {
            loadedByIndex[record.index] = record
        }

        // Published, now that every column read.
        records = loaded
        byIndex = loadedByIndex
    }
}
