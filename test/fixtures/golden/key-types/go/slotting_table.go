// ------------------------------------------------------------------------------
// Code generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package keytypes

import (
	"fmt"

	"keytypes/tabbit"
)

// SlottingRecord was generated from test/fixtures/xlsx/key-types/key-types.xlsx : Slotting : B2.
// Keyed by an enum: one row per label, which is a shape sheets have.
type SlottingRecord struct {
	// primary index, an enum label
	Index Slot
	// anything
	Capacity int32
	// secondary index, past 32 bits
	Serial int64
}

// SlottingTable holds every row of Slotting.
type SlottingTable struct {
	records []SlottingRecord
	byIndex map[Slot]int
	bySerial map[int64]int
}

// Records returns every row, in the order the sheet declared them.
func (t *SlottingTable) Records() []SlottingRecord { return t.records }

// FindByIndex returns the row with this Index, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *SlottingTable) FindByIndex(key Slot) *SlottingRecord {
	position, found := t.byIndex[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIndexOrError returns the row with this Index, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *SlottingTable) GetByIndexOrError(key Slot) (*SlottingRecord, error) {
	position, found := t.byIndex[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Slotting` that corresponds to field `Index` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsIndex reports whether the table holds a row with this Index.
func (t *SlottingTable) ContainsIndex(key Slot) bool {
	_, found := t.byIndex[key]
	return found
}

// FindBySerial returns the row with this Serial, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *SlottingTable) FindBySerial(key int64) *SlottingRecord {
	position, found := t.bySerial[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetBySerialOrError returns the row with this Serial, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *SlottingTable) GetBySerialOrError(key int64) (*SlottingRecord, error) {
	position, found := t.bySerial[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Slotting` that corresponds to field `Serial` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsSerial reports whether the table holds a row with this Serial.
func (t *SlottingTable) ContainsSerial(key int64) bool {
	_, found := t.bySerial[key]
	return found
}

// Read loads the table from a .tcb file written by Tabbit.
func (t *SlottingTable) Read(filename string) error {
	data, err := tabbit.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	// Unconditionally, and with whatever EncryptionKey and MacKey hold - which is nil
	// unless the project set them. A file that is neither encrypted nor signed comes back
	// from this untouched, so the load path is the same either way and there is no
	// condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
	//
	// What comes back is a window onto data rather than a copy of it, which is why data
	// stays in scope for the whole read.
	opened, err := tabbit.Open(data, EncryptionKey, MacKey, VerifyMac)
	if err != nil {
		return err
	}

	reader := tabbit.NewReader(opened)
	count, columns := tabbit.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]SlottingRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if tabbit.CheckColumn(reader, column, "Slotting.Index", tabbit.KindScalar, 1, false, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Slotting.Index")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Index = Slot(value)
						i++
					}
				}
			}
		case 2:
			if tabbit.CheckColumn(reader, column, "Slotting.Capacity", tabbit.KindScalar, 1, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Slotting.Capacity")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Capacity = value
						i++
					}
				}
			}
		case 3:
			if tabbit.CheckColumn(reader, column, "Slotting.Serial", tabbit.KindScalar, 1, false, tabbit.ElementI64, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Slotting.Serial")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Serial = cursor.NextI64()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		tabbit.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Slotting: %w", err)
	}

	byIndex := make(map[Slot]int, len(records))
	for i := range records {
		byIndex[records[i].Index] = i
	}

	bySerial := make(map[int64]int, len(records))
	for i := range records {
		bySerial[records[i].Serial] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byIndex = byIndex
	t.bySerial = bySerial

	return nil
}
