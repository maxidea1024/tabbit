/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef KEY_TYPES_LEDGER_H
#define KEY_TYPES_LEDGER_H

#include "tabbit/tabbit_tcb_reader.h"

#include "KeyTypes_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/key-types/key-types.xlsx : Ledger : B2
 *
 * Keyed by a bigint, for ids that outgrew 32 bits.
 */
struct KeyTypes_LedgerRecord_t {
  /* primary index, past 32 bits */
  int64_t index;
  /* anything */
  int32_t amount;
  /* secondary index, a uuid */
  tb_uuid batch;
};

/* Every row of Ledger.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct KeyTypes_LedgerTable_t {
  KeyTypes_LedgerRecord_t* records;
  int32_t count;

  tb_index64_entry* by_index;
  tb_uuid_index_entry* by_batch;
  tb_arena arena;
} KeyTypes_LedgerTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool KeyTypes_LedgerLoad(KeyTypes_LedgerTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void KeyTypes_LedgerFree(KeyTypes_LedgerTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const KeyTypes_LedgerRecord_t* KeyTypes_LedgerFindByIndex(
  const KeyTypes_LedgerTable_t* table, int64_t key);

/* Whether the table holds a row with this Index. */
bool KeyTypes_LedgerContainsIndex(
  const KeyTypes_LedgerTable_t* table, int64_t key);
/* The row with the given Batch, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const KeyTypes_LedgerRecord_t* KeyTypes_LedgerFindByBatch(
  const KeyTypes_LedgerTable_t* table, tb_uuid key);

/* Whether the table holds a row with this Batch. */
bool KeyTypes_LedgerContainsBatch(
  const KeyTypes_LedgerTable_t* table, tb_uuid key);

#ifdef __cplusplus
}
#endif

#endif /* KEY_TYPES_LEDGER_H */
