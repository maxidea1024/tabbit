/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef KEY_TYPES_SLOTTING_H
#define KEY_TYPES_SLOTTING_H

#include "tabbit/tabbit_tcb_reader.h"

#include "KeyTypes_Forward.h"
#include "enums/KeyTypes_EnumSlot.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/key-types/key-types.xlsx : Slotting : B2
 *
 * Keyed by an enum: one row per label, which is a shape sheets have.
 */
struct KeyTypes_SlottingRecord_t {
  /* primary index, an enum label */
  KeyTypes_Slot_t index;
  /* anything */
  int32_t capacity;
  /* secondary index, past 32 bits */
  int64_t serial;
};

/* Every row of Slotting.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct KeyTypes_SlottingTable_t {
  KeyTypes_SlottingRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_index64_entry* by_serial;
  tb_arena arena;
} KeyTypes_SlottingTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool KeyTypes_SlottingLoad(KeyTypes_SlottingTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void KeyTypes_SlottingFree(KeyTypes_SlottingTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const KeyTypes_SlottingRecord_t* KeyTypes_SlottingFindByIndex(
  const KeyTypes_SlottingTable_t* table, KeyTypes_Slot_t key);

/* Whether the table holds a row with this Index. */
bool KeyTypes_SlottingContainsIndex(
  const KeyTypes_SlottingTable_t* table, KeyTypes_Slot_t key);
/* The row with the given Serial, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const KeyTypes_SlottingRecord_t* KeyTypes_SlottingFindBySerial(
  const KeyTypes_SlottingTable_t* table, int64_t key);

/* Whether the table holds a row with this Serial. */
bool KeyTypes_SlottingContainsSerial(
  const KeyTypes_SlottingTable_t* table, int64_t key);

#ifdef __cplusplus
}
#endif

#endif /* KEY_TYPES_SLOTTING_H */
