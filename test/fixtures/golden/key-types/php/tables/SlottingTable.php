<?php

// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

declare(strict_types=1);

namespace Tabbit\Fixtures\KeyTypes;

require_once __DIR__ . '/../tabbit/TcbReader.php';
require_once __DIR__ . '/../enums/Slot.php';
require_once __DIR__ . '/../KeyTypesAccessor.php';

use Tabbit\TcbReader;
use Tabbit\TcbColumnCursor;
use Tabbit\RecordNotFoundException;
use Tabbit\Uuid;

/**
 * Generated from test/fixtures/xlsx/key-types/key-types.xlsx : Slotting : B2
 *
 * Keyed by an enum: one row per label, which is a shape sheets have.
 */
final class SlottingRecord
{
    /** primary index, an enum label */
    public Slot $index = Slot::None;
    /** anything */
    public int $capacity = 0;
    /** secondary index, past 32 bits */
    public int $serial = 0;
}

/** Every row of Slotting. */
final class SlottingTable
{
    /** @var list<SlottingRecord> */
    public array $records = [];

    /** @var array<int, SlottingRecord> */
    private array $byIndex = [];

    /** @var array<int, SlottingRecord> */
    private array $bySerial = [];

    /**
     * The row with this Index, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findByIndex(Slot $key): ?SlottingRecord
    {
        return $this->byIndex[$key->value] ?? null;
    }

    /**
     * The row with this Index, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByIndex($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getByIndexOrThrow(Slot $key): SlottingRecord
    {
        $record = $this->byIndex[$key->value] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `Slotting` that corresponds to " .
                "field `Index` value " . $key->value . ".");
        }

        return $record;
    }

    /** Whether the table holds a row with this Index. */
    public function containsIndex(Slot $key): bool
    {
        return isset($this->byIndex[$key->value]);
    }

    /**
     * The row with this Serial, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findBySerial(int $key): ?SlottingRecord
    {
        return $this->bySerial[$key] ?? null;
    }

    /**
     * The row with this Serial, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getBySerial($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getBySerialOrThrow(int $key): SlottingRecord
    {
        $record = $this->bySerial[$key] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `Slotting` that corresponds to " .
                "field `Serial` value " . $key . ".");
        }

        return $record;
    }

    /** Whether the table holds a row with this Serial. */
    public function containsSerial(int $key): bool
    {
        return isset($this->bySerial[$key]);
    }

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    public function read(string $filename): void
    {
        // The keys go to fromFile because reading and opening are one call here: the
        // reader's constructor takes the whole file and opens the envelope itself. It does
        // so unconditionally, and with whatever KeyTypesAccessor::$encryptionKey and
        // KeyTypesAccessor::$macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from that untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/tcb-mac-and-signature.md.
        $reader = TcbReader::fromFile(
            $filename, KeyTypesAccessor::$encryptionKey,
            KeyTypesAccessor::$macKey, KeyTypesAccessor::$verifyMac);
        [$count, $columns] = $reader->readTableHeader();

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        $records = [];
        $byIndex = [];
        $bySerial = [];

        for ($i = 0; $i < $count; $i++) {
            $records[] = new SlottingRecord();
        }

        foreach ($columns as $column) {
            $blockEnd = $reader->position() + $column['byteLength'];

            switch ($column['tag']) {
                case 1:
                    TcbReader::checkColumn($column, 'Slotting.Index', TcbReader::KIND_SCALAR, 1, false, [TcbReader::ELEMENT_VARINT]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Slotting.Index');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameI32($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->index = Slot::tryFrom($value) ?? Slot::None;
                        }
                    }
                    break;

                case 2:
                    TcbReader::checkColumn($column, 'Slotting.Capacity', TcbReader::KIND_SCALAR, 1, false, [TcbReader::ELEMENT_I32, TcbReader::ELEMENT_VARINT]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Slotting.Capacity');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameI32($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->capacity = $value;
                        }
                    }
                    break;

                case 3:
                    TcbReader::checkColumn($column, 'Slotting.Serial', TcbReader::KIND_SCALAR, 1, false, [TcbReader::ELEMENT_I64, TcbReader::ELEMENT_I32, TcbReader::ELEMENT_VARINT]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Slotting.Serial');
                    foreach ($records as $record) {
                        $record->serial = $cursor->nextI64();
                    }
                    break;

                default:
                    // A column added after this code was generated.
                    $reader->skip($column['byteLength']);
                    break;
            }

            $reader->checkBlockEnd($column, $blockEnd);
        }

        foreach ($records as $record) {
            $byIndex[$record->index->value] = $record;
            $bySerial[$record->serial] = $record;
        }

        // Published, now that every column read.
        $this->records = $records;
        $this->byIndex = $byIndex;
        $this->bySerial = $bySerial;
    }
}
