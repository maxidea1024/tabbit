-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")
local Slot = require(_root .. "enums.enum_slot")

-- Generated from test/fixtures/xlsx/key-types/key-types.xlsx : Slotting : B2.
-- Keyed by an enum: one row per label, which is a shape sheets have.
---@class SlottingRecord
---@field index integer
---@field capacity integer
---@field serial integer
local SlottingRecordMeta = tcb.strictType("a `Slotting` row", { "index", "capacity", "serial" })

---@return SlottingRecord
local function newSlottingRecord()
  return setmetatable({
    index = 0,
    capacity = 0,
    serial = 0,
  }, SlottingRecordMeta)
end

---@class SlottingTable
---@field records SlottingRecord[]
local SlottingTable = {}

local SlottingTableMeta = tcb.strictInstance("the `Slotting` table", SlottingTable, { "records", "byIndex", "bySerial" })

function SlottingTable.new()
  return setmetatable({
    records = {},
    byIndex = {},
    bySerial = {},
  }, SlottingTableMeta)
end

-- The row with this Index, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function SlottingTable:findByIndex(key)
  return self.byIndex[key]
end

-- The row with this Index, or a raised error naming what was missing -
-- for a key that has to be there.
function SlottingTable:getByIndexOrThrow(key)
  local record = self:findByIndex(key)

  if record == nil then
    tcb.fail("there is no record in table `Slotting` that corresponds to " ..
      "field `Index` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this Index.
function SlottingTable:containsIndex(key)
  return self:findByIndex(key) ~= nil
end

-- The row with this Serial, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function SlottingTable:findBySerial(key)
  return self.bySerial[tcb.int64String(key)]
end

-- The row with this Serial, or a raised error naming what was missing -
-- for a key that has to be there.
function SlottingTable:getBySerialOrThrow(key)
  local record = self:findBySerial(key)

  if record == nil then
    tcb.fail("there is no record in table `Slotting` that corresponds to " ..
      "field `Serial` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this Serial.
function SlottingTable:containsSerial(key)
  return self:findBySerial(key) ~= nil
end

-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function SlottingTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newSlottingRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "Slotting.Index", tcb.KIND_SCALAR, 1, false, { tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Slotting.Index")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].index = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "Slotting.Capacity", tcb.KIND_SCALAR, 1, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Slotting.Capacity")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].capacity = value
        end

        at = at + n
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "Slotting.Serial", tcb.KIND_SCALAR, 1, false, { tcb.ELEMENT_I64, tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Slotting.Serial")
      for i = 1, count do
        local record = records[i]
        record.serial = cursor:nextI64()
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byIndex = {}

  for i = 1, #records do
    byIndex[records[i].index] = records[i]
  end

  local bySerial = {}

  for i = 1, #records do
    bySerial[tcb.int64String(records[i].serial)] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byIndex = byIndex
  self.bySerial = bySerial
end

-- The same load from a file on disk.
function SlottingTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return SlottingTable
