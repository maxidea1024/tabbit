// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_KEYTYPESACCESSOR_ASSET_H
#define TABBIT_GENERATED_KEYTYPESACCESSOR_ASSET_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "KeyTypesAccessor_forward.h"
#include "enums/KeyTypesAccessor_enum_slot.h"
// Generated from test/fixtures/xlsx/key-types/key-types.xlsx : Asset : B2
/// Keyed by a uuid, which is what a pipeline that mints ids hands over.
struct AssetRecord {
  /// primary index, a uuid
  tabbit::Uuid index;
  /// anything
  std::string path;
  /// secondary index, an enum label
  Slot slot = static_cast<Slot>(0);
};

/// Keyed by a uuid, which is what a pipeline that mints ids hands over.
class AssetTable {
 public:
  const std::vector<AssetRecord>& records() const { return records_; }

  /// The row with this Index, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const AssetRecord* find_by_index(tabbit::Uuid key) const {
    const auto it = by_index_.find(key);
    return it == by_index_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_index(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const AssetRecord& get_by_index_or_throw(tabbit::Uuid key) const {
    const AssetRecord* found = find_by_index(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Asset` that corresponds to "
                    "field `Index` value ") + key.to_string());
    }

    return *found;
  }

  /// Whether the table holds a row with this Index.
  bool contains_index(tabbit::Uuid key) const {
    return by_index_.find(key) != by_index_.end();
  }

  /// The row with this Slot, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const AssetRecord* find_by_slot(Slot key) const {
    const auto it = by_slot_.find(key);
    return it == by_slot_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Slot, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_slot(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const AssetRecord& get_by_slot_or_throw(Slot key) const {
    const AssetRecord* found = find_by_slot(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Asset` that corresponds to "
                    "field `Slot` value ") + std::to_string(static_cast<std::int64_t>(key)));
    }

    return *found;
  }

  /// Whether the table holds a row with this Slot.
  bool contains_slot(Slot key) const {
    return by_slot_.find(key) != by_slot_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<AssetRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Asset.Index", tabbit::kKindScalar, false, {tabbit::kElementUuid});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.index);
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Asset.Path", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Asset.Path");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].path = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Asset.Slot", tabbit::kKindScalar, false, {tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Asset.Slot");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].slot = static_cast<Slot>(value);
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class KeyTypesAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<AssetRecord>&& records) {
    std::unordered_map<tabbit::Uuid, std::size_t> index_map;
    index_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      index_map.emplace(records[i].index, i);
    }

    std::unordered_map<Slot, std::size_t> slot_map;
    slot_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      slot_map.emplace(records[i].slot, i);
    }

    records_ = std::move(records);
    by_index_ = std::move(index_map);
    by_slot_ = std::move(slot_map);
  }

  std::vector<AssetRecord> records_;
  std::unordered_map<tabbit::Uuid, std::size_t> by_index_;
  std::unordered_map<Slot, std::size_t> by_slot_;
};

#endif  // TABBIT_GENERATED_KEYTYPESACCESSOR_ASSET_H
