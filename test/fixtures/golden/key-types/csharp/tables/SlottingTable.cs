// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Tabbit.Fixtures.KeyTypes
{
    /// <summary>
    /// Keyed by an enum: one row per label, which is a shape sheets have.
    /// </summary>
    [System.Serializable]
    public partial class SlottingTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// primary index, an enum label
            /// </summary>
            public global::Tabbit.Fixtures.KeyTypes.Slot Index => _index;

            /// <summary>
            /// anything
            /// </summary>
            public int Capacity => _capacity;

            /// <summary>
            /// secondary index, past 32 bits
            /// </summary>
            public long Serial => _serial;
            #endregion

            #region Storage
            internal global::Tabbit.Fixtures.KeyTypes.Slot _index;
            internal int _capacity;
            internal long _serial;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Index\":"); ToStringHelper.ToString(Index, sb);
                sb.Append(",\"Capacity\":"); ToStringHelper.ToString(Capacity, sb);
                sb.Append(",\"Serial\":"); ToStringHelper.ToString(Serial, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Index", "Capacity", "Serial" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Index, r.Capacity, r.Serial });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Index'
        public Dictionary<global::Tabbit.Fixtures.KeyTypes.Slot, Record> RecordsByIndex => _recordsByIndex;
        private Dictionary<global::Tabbit.Fixtures.KeyTypes.Slot, Record> _recordsByIndex = new Dictionary<global::Tabbit.Fixtures.KeyTypes.Slot, Record>();

        /// <summary>
        /// The row with this `Index`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByIndex(global::Tabbit.Fixtures.KeyTypes.Slot key)
            => _recordsByIndex.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Index`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByIndex(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIndexOrThrow(global::Tabbit.Fixtures.KeyTypes.Slot key)
        {
            if (!_recordsByIndex.TryGetValue(key, out Record record))
                throw new TabbitException($"There is no record in table `Slotting` that corresponds to field `Index` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Index`.</summary>
        public bool ContainsIndex(global::Tabbit.Fixtures.KeyTypes.Slot key) => _recordsByIndex.ContainsKey(key);
        #endregion // Indexing by `Index`

        #region Indexing by 'Serial'
        public Dictionary<long, Record> RecordsBySerial => _recordsBySerial;
        private Dictionary<long, Record> _recordsBySerial = new Dictionary<long, Record>();

        /// <summary>
        /// The row with this `Serial`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindBySerial(long key)
            => _recordsBySerial.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Serial`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetBySerial(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetBySerialOrThrow(long key)
        {
            if (!_recordsBySerial.TryGetValue(key, out Record record))
                throw new TabbitException($"There is no record in table `Slotting` that corresponds to field `Serial` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Serial`.</summary>
        public bool ContainsSerial(long key) => _recordsBySerial.ContainsKey(key);
        #endregion // Indexing by `Serial`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await KeyTypesAccessor.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `KeyTypesAccessor.EncryptionKey` and `KeyTypesAccessor.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, KeyTypesAccessor.EncryptionKey, KeyTypesAccessor.MacKey, KeyTypesAccessor.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "Slotting.Index", TcbTable.KindScalar, 1, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Slotting.Index");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._index = (global::Tabbit.Fixtures.KeyTypes.Slot)value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "Slotting.Capacity", TcbTable.KindScalar, 1, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Slotting.Capacity");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._capacity = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "Slotting.Serial", TcbTable.KindScalar, 1, false, TcbTable.ElementI64, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Slotting.Serial");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._serial = cursor.NextI64();
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByIndex = new Dictionary<global::Tabbit.Fixtures.KeyTypes.Slot, Record>(count);
            foreach (var record in records)
                recordsByIndex.Add(record.Index, record);
            var recordsBySerial = new Dictionary<long, Record>(count);
            foreach (var record in records)
                recordsBySerial.Add(record.Serial, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByIndex = recordsByIndex;
            _recordsBySerial = recordsBySerial;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Tabbit.Fixtures.KeyTypes
