/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "CompositeKey.h"

#include <stdio.h>
#include <string.h>

/* The definitions for the declarations in the forward header. Beside the accessor's own
 * load because that is where a project answers the questions the generated code cannot. */
const uint8_t* CompositeKey_EncryptionKey = NULL;
int32_t CompositeKey_EncryptionKeyLength = 0;
const uint8_t* CompositeKey_MacKey = NULL;
int32_t CompositeKey_MacKeyLength = 0;
bool CompositeKey_VerifyMac = true;

/* Turns the stored indices into usable pointers, once every table is in memory.
 *
 * A reference into a row that is not there stays NULL rather than inventing one. */
static void CompositeKey_SolveCrossReferences(CompositeKey_t* data) {
  int32_t row;

  for (row = 0; row < data->beast_move.count; ++row) {
    CompositeKey_BeastMoveRecord_t* record = &data->beast_move.records[row];

    {
      const CompositeKey_BeastRecord_t* target = CompositeKey_BeastFindByIndex(
        &data->beast, record->beast_id);

      if (target != NULL)
        record->beast_by_beast_id = target;
    }

    {
      const CompositeKey_MoveRecord_t* target = CompositeKey_MoveFindByIndex(
        &data->move, record->move_id);

      if (target != NULL)
        record->move_by_move_id = target;
    }
  }

  for (row = 0; row < data->beast_note.count; ++row) {
    CompositeKey_BeastNoteRecord_t* record = &data->beast_note.records[row];

    {
      const CompositeKey_BeastRecord_t* target = CompositeKey_BeastFindByIndex(
        &data->beast, record->beast_id);

      if (target != NULL)
        record->beast_by_beast_id = target;
    }
  }

  for (row = 0; row < data->move_note.count; ++row) {
    CompositeKey_MoveNoteRecord_t* record = &data->move_note.records[row];

    {
      const CompositeKey_MoveRecord_t* target = CompositeKey_MoveFindByIndex(
        &data->move, record->move_id);

      if (target != NULL)
        record->move_by_move_id = target;
    }
  }
}

bool CompositeKey_LoadAll(CompositeKey_t* data, const char* base_path,
                 char* error, size_t error_size) {
  return CompositeKey_LoadAllWithExtension(
    data, base_path, ".tcb", error, error_size);
}

bool CompositeKey_LoadAllWithExtension(CompositeKey_t* data, const char* base_path,
                 const char* file_extension, char* error, size_t error_size) {
  char path[1024];

  /* Loaded beside whatever `data` is already holding and swapped in at the end. Reading
   * again is a reload, and one that fails has to leave the caller's pointers valid: they
   * point into the arenas of the previous load, which is why this cannot start by zeroing
   * `data` - doing that dropped those arenas without freeing them. */
  CompositeKey_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Loadout", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    CompositeKey_Free(&loaded);
    return false;
  }

  if (!CompositeKey_LoadoutLoad(&loaded.loadout, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    CompositeKey_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Route", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    CompositeKey_Free(&loaded);
    return false;
  }

  if (!CompositeKey_RouteLoad(&loaded.route, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    CompositeKey_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Grid", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    CompositeKey_Free(&loaded);
    return false;
  }

  if (!CompositeKey_GridLoad(&loaded.grid, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    CompositeKey_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Beast", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    CompositeKey_Free(&loaded);
    return false;
  }

  if (!CompositeKey_BeastLoad(&loaded.beast, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    CompositeKey_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Move", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    CompositeKey_Free(&loaded);
    return false;
  }

  if (!CompositeKey_MoveLoad(&loaded.move, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    CompositeKey_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "BeastMove", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    CompositeKey_Free(&loaded);
    return false;
  }

  if (!CompositeKey_BeastMoveLoad(&loaded.beast_move, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    CompositeKey_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "BeastNote", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    CompositeKey_Free(&loaded);
    return false;
  }

  if (!CompositeKey_BeastNoteLoad(&loaded.beast_note, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    CompositeKey_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "MoveNote", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    CompositeKey_Free(&loaded);
    return false;
  }

  if (!CompositeKey_MoveNoteLoad(&loaded.move_note, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    CompositeKey_Free(&loaded);
    return false;
  }

  /* Linked among the tables of this load, so no row points into the previous one. */
  CompositeKey_SolveCrossReferences(&loaded);

  /* The previous load goes now, and not before. */
  CompositeKey_Free(data);
  *data = loaded;

  return true;
}

void CompositeKey_Free(CompositeKey_t* data) {
  CompositeKey_LoadoutFree(&data->loadout);
  CompositeKey_RouteFree(&data->route);
  CompositeKey_GridFree(&data->grid);
  CompositeKey_BeastFree(&data->beast);
  CompositeKey_MoveFree(&data->move);
  CompositeKey_BeastMoveFree(&data->beast_move);
  CompositeKey_BeastNoteFree(&data->beast_note);
  CompositeKey_MoveNoteFree(&data->move_note);
}
