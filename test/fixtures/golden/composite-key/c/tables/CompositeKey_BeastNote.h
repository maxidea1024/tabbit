/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef COMPOSITE_KEY_BEAST_NOTE_H
#define COMPOSITE_KEY_BEAST_NOTE_H

#include "tabbit/tabbit_tcb_reader.h"

#include "CompositeKey_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : T2
 *
 * Indexed by a reference to a string-keyed table, named with `key=`.
 */
struct CompositeKey_BeastNoteRecord_t {
  /* not the index, so `key=` has something to move */
  int32_t seq;
  /* the index, and a reference */
  const char* beast_id;
  const CompositeKey_BeastRecord_t* beast_by_beast_id;
  /* anything */
  const char* note;
};

/* Every row of BeastNote.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct CompositeKey_BeastNoteTable_t {
  CompositeKey_BeastNoteRecord_t* records;
  int32_t count;

  tb_string_index_entry* by_beast_id;
  tb_arena arena;
} CompositeKey_BeastNoteTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool CompositeKey_BeastNoteLoad(CompositeKey_BeastNoteTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void CompositeKey_BeastNoteFree(CompositeKey_BeastNoteTable_t* table);

/* The row with the given BeastId, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const CompositeKey_BeastNoteRecord_t* CompositeKey_BeastNoteFindByBeastId(
  const CompositeKey_BeastNoteTable_t* table, const char* key);

/* Whether the table holds a row with this BeastId. */
bool CompositeKey_BeastNoteContainsBeastId(
  const CompositeKey_BeastNoteTable_t* table, const char* key);

#ifdef __cplusplus
}
#endif

#endif /* COMPOSITE_KEY_BEAST_NOTE_H */
