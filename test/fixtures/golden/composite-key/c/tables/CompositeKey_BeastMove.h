/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef COMPOSITE_KEY_BEAST_MOVE_H
#define COMPOSITE_KEY_BEAST_MOVE_H

#include "tabbit/tabbit_tcb_reader.h"

#include "CompositeKey_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : N2
 *
 * One row per pair; the key is both references taken together.
 */
struct CompositeKey_BeastMoveRecord_t {
  /* a string-keyed target */
  const char* beast_id;
  const CompositeKey_BeastRecord_t* beast_by_beast_id;
  /* an int-keyed target */
  int32_t move_id;
  const CompositeKey_MoveRecord_t* move_by_move_id;
  /* anything */
  int32_t power;
};

/* Every row of BeastMove.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct CompositeKey_BeastMoveTable_t {
  CompositeKey_BeastMoveRecord_t* records;
  int32_t count;

  tb_string_index_entry* by_beast_id_and_move_id;
  tb_arena arena;
} CompositeKey_BeastMoveTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool CompositeKey_BeastMoveLoad(CompositeKey_BeastMoveTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void CompositeKey_BeastMoveFree(CompositeKey_BeastMoveTable_t* table);

/* The row with the given BeastId and MoveId, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const CompositeKey_BeastMoveRecord_t* CompositeKey_BeastMoveFindByBeastIdAndMoveId(
  const CompositeKey_BeastMoveTable_t* table, const char* beast_id_key, int32_t move_id_key);

/* Whether the table holds a row with this BeastId and MoveId. */
bool CompositeKey_BeastMoveContainsBeastIdAndMoveId(
  const CompositeKey_BeastMoveTable_t* table, const char* beast_id_key, int32_t move_id_key);

#ifdef __cplusplus
}
#endif

#endif /* COMPOSITE_KEY_BEAST_MOVE_H */
