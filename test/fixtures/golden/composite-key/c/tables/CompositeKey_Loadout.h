/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef COMPOSITE_KEY_LOADOUT_H
#define COMPOSITE_KEY_LOADOUT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "CompositeKey_Forward.h"
#include "enums/CompositeKey_EnumSlot.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Loadout : B2
 *
 * One row per stage and slot; neither column is unique on its own.
 */
struct CompositeKey_LoadoutRecord_t {
  /* which stage */
  int32_t stage;
  /* which slot */
  CompositeKey_Slot_t slot;
  /* anything */
  int32_t power;
  /* anything */
  const char* label;
};

/* Every row of Loadout.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct CompositeKey_LoadoutTable_t {
  CompositeKey_LoadoutRecord_t* records;
  int32_t count;

  tb_string_index_entry* by_stage_and_slot;
  tb_arena arena;
} CompositeKey_LoadoutTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool CompositeKey_LoadoutLoad(CompositeKey_LoadoutTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void CompositeKey_LoadoutFree(CompositeKey_LoadoutTable_t* table);

/* The row with the given Stage and Slot, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const CompositeKey_LoadoutRecord_t* CompositeKey_LoadoutFindByStageAndSlot(
  const CompositeKey_LoadoutTable_t* table, int32_t stage_key, CompositeKey_Slot_t slot_key);

/* Whether the table holds a row with this Stage and Slot. */
bool CompositeKey_LoadoutContainsStageAndSlot(
  const CompositeKey_LoadoutTable_t* table, int32_t stage_key, CompositeKey_Slot_t slot_key);

#ifdef __cplusplus
}
#endif

#endif /* COMPOSITE_KEY_LOADOUT_H */
