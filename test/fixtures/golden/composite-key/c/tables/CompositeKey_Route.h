/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef COMPOSITE_KEY_ROUTE_H
#define COMPOSITE_KEY_ROUTE_H

#include "tabbit/tabbit_tcb_reader.h"

#include "CompositeKey_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2
 *
 * A composite primary key of strings, beside a single-column secondary one.
 */
struct CompositeKey_RouteRecord_t {
  /* where it starts */
  const char* from;
  /* where it ends */
  const char* to;
  /* unique on its own */
  const char* code;
  /* anything */
  int32_t distance;
};

/* Every row of Route.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct CompositeKey_RouteTable_t {
  CompositeKey_RouteRecord_t* records;
  int32_t count;

  tb_string_index_entry* by_code;
  tb_string_index_entry* by_from_and_to;
  tb_arena arena;
} CompositeKey_RouteTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool CompositeKey_RouteLoad(CompositeKey_RouteTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void CompositeKey_RouteFree(CompositeKey_RouteTable_t* table);

/* The row with the given Code, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const CompositeKey_RouteRecord_t* CompositeKey_RouteFindByCode(
  const CompositeKey_RouteTable_t* table, const char* key);

/* Whether the table holds a row with this Code. */
bool CompositeKey_RouteContainsCode(
  const CompositeKey_RouteTable_t* table, const char* key);
/* The row with the given From and To, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const CompositeKey_RouteRecord_t* CompositeKey_RouteFindByFromAndTo(
  const CompositeKey_RouteTable_t* table, const char* from_key, const char* to_key);

/* Whether the table holds a row with this From and To. */
bool CompositeKey_RouteContainsFromAndTo(
  const CompositeKey_RouteTable_t* table, const char* from_key, const char* to_key);

#ifdef __cplusplus
}
#endif

#endif /* COMPOSITE_KEY_ROUTE_H */
