/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef COMPOSITE_KEY_MOVE_NOTE_H
#define COMPOSITE_KEY_MOVE_NOTE_H

#include "tabbit/tabbit_tcb_reader.h"

#include "CompositeKey_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : Z2
 *
 * First column is the index and it is a reference to an int-keyed table.
 */
struct CompositeKey_MoveNoteRecord_t {
  /* the index, and a reference */
  int32_t move_id;
  const CompositeKey_MoveRecord_t* move_by_move_id;
  /* anything */
  const char* note;
  /* padding, so every table is one width */
  int32_t pad1;
};

/* Every row of MoveNote.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct CompositeKey_MoveNoteTable_t {
  CompositeKey_MoveNoteRecord_t* records;
  int32_t count;

  tb_index_entry* by_move_id;
  tb_arena arena;
} CompositeKey_MoveNoteTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool CompositeKey_MoveNoteLoad(CompositeKey_MoveNoteTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void CompositeKey_MoveNoteFree(CompositeKey_MoveNoteTable_t* table);

/* The row with the given MoveId, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const CompositeKey_MoveNoteRecord_t* CompositeKey_MoveNoteFindByMoveId(
  const CompositeKey_MoveNoteTable_t* table, int32_t key);

/* Whether the table holds a row with this MoveId. */
bool CompositeKey_MoveNoteContainsMoveId(
  const CompositeKey_MoveNoteTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* COMPOSITE_KEY_MOVE_NOTE_H */
