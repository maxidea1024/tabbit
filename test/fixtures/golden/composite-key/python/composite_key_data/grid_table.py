# ------------------------------------------------------------------------------
# Generated by Tabbit. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

import enum
import os

from . import tabbit


class GridRecord:
    """Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Grid : B2.

    Three columns taken together, so two is not the only width that works.
    """

    __slots__ = ("x", "y", "z", "name")

    def __init__(self):
        self.x = 0
        self.y = 0
        self.z = ""
        self.name = ""

    def __repr__(self):
        return "GridRecord(x=%r, y=%r, z=%r, name=%r)" % (self.x, self.y, self.z, self.name)


class GridTable:
    """Every row of Grid."""

    __slots__ = ("records", "by_x_and_y_and_z")

    def __init__(self):
        self.records = []
        self.by_x_and_y_and_z = {}

    @staticmethod
    def _key_of_x_and_y_and_z(x_key, y_key, z_key):
        """Joins the columns of the X and Y and Z key into the text the map is keyed by."""
        parts = [
            str(int(x_key)),
            str(int(y_key)),
            z_key,
        ]

        return "".join("%d:%s" % (len(part), part) for part in parts)

    def find_by_x_and_y_and_z(self, x_key, y_key, z_key):
        """The row with this X and Y and Z, or None when the table has none.

        The lookup to reach for when a missing row is an ordinary answer - an optional
        reference, a key that came from user input.
        """
        return self.by_x_and_y_and_z.get(self._key_of_x_and_y_and_z(x_key, y_key, z_key))

    def get_by_x_and_y_and_z_or_throw(self, x_key, y_key, z_key):
        """The row with this X and Y and Z, or a raised error naming what was missing.

        For a key that has to be there - one from another table, or a constant. The name
        says it raises, because a caller reading `get_by_x_and_y_and_z(key).name` at a
        glance cannot otherwise tell whether the next line is a check or a try.
        """
        record = self.by_x_and_y_and_z.get(self._key_of_x_and_y_and_z(x_key, y_key, z_key))

        if record is None:
            raise tabbit.RecordNotFoundError(
                "there is no record in table `Grid` that corresponds to "
                "field `X and Y and Z` value (%r, %r, %r)" % (x_key, y_key, z_key,))

        return record

    def contains_x_and_y_and_z(self, x_key, y_key, z_key):
        """Whether the table holds a row with this X and Y and Z."""
        return self._key_of_x_and_y_and_z(x_key, y_key, z_key) in self.by_x_and_y_and_z

    def read(self, filename):
        """Loads the table from a .tcb file written by Tabbit.

        Column by column, matched by tag rather than position: a column this build does
        not know is skipped by its block length, and one whose type changed incompatibly
        fails naming the field.
        """
        # Imported here rather than at the top: `tables` imports this module,
        # so at import time it is half built and at call time it is not.
        from .tables import Tables

        # Opened unconditionally, and with whatever `Tables.encryption_key` and
        # `Tables.mac_key` hold - which is None unless the project set them. A file that is
        # neither encrypted nor signed comes back from this untouched, so the load path is
        # the same either way and there is no condition here that could be the wrong way
        # round. spec/wire/tcb-mac-and-signature.md.
        reader = tabbit.Reader(tabbit.open(
            tabbit.read_all_bytes(filename), Tables.encryption_key,
            Tables.mac_key, Tables.verify_mac))
        count, columns = tabbit.read_table_header(reader)

        # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        records = [GridRecord() for _ in range(count)]

        for column in columns:
            block_end = reader.position + column.byte_length
            if column.tag == 1:
                tabbit.check_column(column, "Grid.X", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT))
                cursor = tabbit.ColumnCursor(reader, column, count, "Grid.X")
                at = 0
                while at < count:
                    n, value = cursor.next_same_i32(count - at)
                    for i in range(at, at + n):
                        records[i].x = value
                    at += n
            elif column.tag == 2:
                tabbit.check_column(column, "Grid.Y", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT))
                cursor = tabbit.ColumnCursor(reader, column, count, "Grid.Y")
                at = 0
                while at < count:
                    n, value = cursor.next_same_i32(count - at)
                    for i in range(at, at + n):
                        records[i].y = value
                    at += n
            elif column.tag == 3:
                tabbit.check_column(column, "Grid.Z", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_STRING,))
                cursor = tabbit.ColumnCursor(reader, column, count, "Grid.Z")
                at = 0
                while at < count:
                    n, value = cursor.next_same_string(count - at)
                    for i in range(at, at + n):
                        records[i].z = value
                    at += n
            elif column.tag == 4:
                tabbit.check_column(column, "Grid.Name", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_STRING,))
                cursor = tabbit.ColumnCursor(reader, column, count, "Grid.Name")
                at = 0
                while at < count:
                    n, value = cursor.next_same_string(count - at)
                    for i in range(at, at + n):
                        records[i].name = value
                    at += n
            else:
                # A column added after this code was generated.
                reader.skip(column.byte_length)

            tabbit.check_block_end(reader, column, block_end)

        by_x_and_y_and_z = {
            self._key_of_x_and_y_and_z(record.x, record.y, record.z): record
            for record in records
        }

        # Published, now that every column read.
        self.records = records
        self.by_x_and_y_and_z = by_x_and_y_and_z
