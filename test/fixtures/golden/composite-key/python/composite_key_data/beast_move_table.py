# ------------------------------------------------------------------------------
# Generated by Tabbit. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

import enum
import os

from . import tabbit


class BeastMoveRecord:
    """Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : N2.

    One row per pair; the key is both references taken together.
    """

    __slots__ = ("beast_id", "beast_by_beast_id", "move_id", "move_by_move_id", "power")

    def __init__(self):
        self.beast_id = 0
        self.beast_by_beast_id = None
        self.move_id = 0
        self.move_by_move_id = None
        self.power = 0

    def __repr__(self):
        return "BeastMoveRecord(beast_id=%r, move_id=%r, power=%r)" % (self.beast_id, self.move_id, self.power)


class BeastMoveTable:
    """Every row of BeastMove."""

    __slots__ = ("records", "by_beast_id_and_move_id")

    def __init__(self):
        self.records = []
        self.by_beast_id_and_move_id = {}

    def __len__(self):
        """How many rows the table holds."""
        return len(self.records)

    def __iter__(self):
        """The rows, in the order the file wrote them.

        The list reference is read once, here. A refresh replaces the reference rather
        than its contents, so a loop already running keeps the rows it started with.
        """
        return iter(self.records)

    @staticmethod
    def _key_of_beast_id_and_move_id(beast_id_key, move_id_key):
        """Joins the columns of the BeastId and MoveId key into the text the map is keyed by."""
        parts = [
            beast_id_key,
            str(int(move_id_key)),
        ]

        return "".join("%d:%s" % (len(part), part) for part in parts)

    def find_by_beast_id_and_move_id(self, beast_id_key, move_id_key):
        """The row with this BeastId and MoveId, or None when the table has none.

        The lookup to reach for when a missing row is an ordinary answer - an optional
        reference, a key that came from user input.
        """
        return self.by_beast_id_and_move_id.get(self._key_of_beast_id_and_move_id(beast_id_key, move_id_key))

    def get_by_beast_id_and_move_id_or_throw(self, beast_id_key, move_id_key):
        """The row with this BeastId and MoveId, or a raised error naming what was missing.

        For a key that has to be there - one from another table, or a constant. The name
        says it raises, because a caller reading `get_by_beast_id_and_move_id(key).name` at a
        glance cannot otherwise tell whether the next line is a check or a try.
        """
        record = self.by_beast_id_and_move_id.get(self._key_of_beast_id_and_move_id(beast_id_key, move_id_key))

        if record is None:
            raise tabbit.RecordNotFoundError(
                "there is no record in table `BeastMove` that corresponds to "
                "field `BeastId and MoveId` value (%r, %r)" % (beast_id_key, move_id_key,))

        return record

    def contains_beast_id_and_move_id(self, beast_id_key, move_id_key):
        """Whether the table holds a row with this BeastId and MoveId."""
        return self._key_of_beast_id_and_move_id(beast_id_key, move_id_key) in self.by_beast_id_and_move_id


    def __getitem__(self, key):
        """The row with this BeastId and MoveId, or a raised error naming what was missing.

        get_by_beast_id_and_move_id_or_throw under the spelling Python uses for a keyed
        collection. It raises because that is what dict does; a language whose own map
        answers with null generates a subscript that answers with null.
        spec/targets/table-collection-surface.md section 5.7.

        This is the key, not the row's position. table[0] is the row whose
        BeastId and MoveId is 0, not the first row - the rows in order are .records.


        A key of several columns is passed as a tuple, in the order the sheet wrote them.
        """

        return self.get_by_beast_id_and_move_id_or_throw(*key)

    def read(self, filename):
        """Loads the table from a .tcb file written by Tabbit.

        Column by column, matched by tag rather than position: a column this build does
        not know is skipped by its block length, and one whose type changed incompatibly
        fails naming the field.
        """
        # Imported here rather than at the top: `tables` imports this module,
        # so at import time it is half built and at call time it is not.
        from .tables import Tables

        # Opened unconditionally, and with whatever `Tables.encryption_key` and
        # `Tables.mac_key` hold - which is None unless the project set them. A file that is
        # neither encrypted nor signed comes back from this untouched, so the load path is
        # the same either way and there is no condition here that could be the wrong way
        # round. spec/wire/tcb-mac-and-signature.md.
        reader = tabbit.Reader(tabbit.open(
            tabbit.read_all_bytes(filename), Tables.encryption_key,
            Tables.mac_key, Tables.verify_mac))
        count, columns = tabbit.read_table_header(reader)

        # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        records = [BeastMoveRecord() for _ in range(count)]

        for column in columns:
            block_end = reader.position + column.byte_length
            if column.tag == 1:
                tabbit.check_column(column, "BeastMove.BeastId", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_STRING,))
                cursor = tabbit.ColumnCursor(reader, column, count, "BeastMove.BeastId")
                at = 0
                while at < count:
                    n, value = cursor.next_same_string(count - at)
                    for i in range(at, at + n):
                        records[i].beast_id = value
                    at += n
            elif column.tag == 2:
                tabbit.check_column(column, "BeastMove.MoveId", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_I32,))
                cursor = tabbit.ColumnCursor(reader, column, count, "BeastMove.MoveId")
                at = 0
                while at < count:
                    n, value = cursor.next_same_i32(count - at)
                    for i in range(at, at + n):
                        records[i].move_id = value
                    at += n
            elif column.tag == 3:
                tabbit.check_column(column, "BeastMove.Power", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT))
                cursor = tabbit.ColumnCursor(reader, column, count, "BeastMove.Power")
                at = 0
                while at < count:
                    n, value = cursor.next_same_i32(count - at)
                    for i in range(at, at + n):
                        records[i].power = value
                    at += n
            else:
                # A column added after this code was generated.
                reader.skip(column.byte_length)

            tabbit.check_block_end(reader, column, block_end)

        by_beast_id_and_move_id = {
            self._key_of_beast_id_and_move_id(record.beast_id, record.move_id): record
            for record in records
        }

        # Published, now that every column read.
        self.records = records
        self.by_beast_id_and_move_id = by_beast_id_and_move_id
