# ------------------------------------------------------------------------------
# Generated by Tabbit. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

import enum
import os

from . import tabbit


class RouteRecord:
    """Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2.

    A composite primary key of strings, beside a single-column secondary one.
    """

    __slots__ = ("from_", "to", "code", "distance")

    def __init__(self):
        self.from_ = ""
        self.to = ""
        self.code = ""
        self.distance = 0

    def __repr__(self):
        return "RouteRecord(from_=%r, to=%r, code=%r, distance=%r)" % (self.from_, self.to, self.code, self.distance)


class RouteTable:
    """Every row of Route."""

    __slots__ = ("records", "by_code", "by_from_and_to")

    def __init__(self):
        self.records = []
        self.by_code = {}
        self.by_from_and_to = {}

    def find_by_code(self, key):
        """The row with this Code, or None when the table has none.

        The lookup to reach for when a missing row is an ordinary answer - an optional
        reference, a key that came from user input.
        """
        return self.by_code.get(key)

    def get_by_code_or_throw(self, key):
        """The row with this Code, or a raised error naming what was missing.

        For a key that has to be there - one from another table, or a constant. The name
        says it raises, because a caller reading `get_by_code(key).name` at a
        glance cannot otherwise tell whether the next line is a check or a try.
        """
        record = self.by_code.get(key)

        if record is None:
            raise tabbit.RecordNotFoundError(
                "there is no record in table `Route` that corresponds to "
                "field `Code` value %r" % (key,))

        return record

    def contains_code(self, key):
        """Whether the table holds a row with this Code."""
        return key in self.by_code

    @staticmethod
    def _key_of_from_and_to(from_key, to_key):
        """Joins the columns of the From and To key into the text the map is keyed by."""
        parts = [
            from_key,
            to_key,
        ]

        return "".join("%d:%s" % (len(part), part) for part in parts)

    def find_by_from_and_to(self, from_key, to_key):
        """The row with this From and To, or None when the table has none.

        The lookup to reach for when a missing row is an ordinary answer - an optional
        reference, a key that came from user input.
        """
        return self.by_from_and_to.get(self._key_of_from_and_to(from_key, to_key))

    def get_by_from_and_to_or_throw(self, from_key, to_key):
        """The row with this From and To, or a raised error naming what was missing.

        For a key that has to be there - one from another table, or a constant. The name
        says it raises, because a caller reading `get_by_from_and_to(key).name` at a
        glance cannot otherwise tell whether the next line is a check or a try.
        """
        record = self.by_from_and_to.get(self._key_of_from_and_to(from_key, to_key))

        if record is None:
            raise tabbit.RecordNotFoundError(
                "there is no record in table `Route` that corresponds to "
                "field `From and To` value (%r, %r)" % (from_key, to_key,))

        return record

    def contains_from_and_to(self, from_key, to_key):
        """Whether the table holds a row with this From and To."""
        return self._key_of_from_and_to(from_key, to_key) in self.by_from_and_to

    def read(self, filename):
        """Loads the table from a .tcb file written by Tabbit.

        Column by column, matched by tag rather than position: a column this build does
        not know is skipped by its block length, and one whose type changed incompatibly
        fails naming the field.
        """
        # Imported here rather than at the top: `tables` imports this module,
        # so at import time it is half built and at call time it is not.
        from .tables import Tables

        # Opened unconditionally, and with whatever `Tables.encryption_key` and
        # `Tables.mac_key` hold - which is None unless the project set them. A file that is
        # neither encrypted nor signed comes back from this untouched, so the load path is
        # the same either way and there is no condition here that could be the wrong way
        # round. spec/tcb-mac-and-signature.md.
        reader = tabbit.Reader(tabbit.open(
            tabbit.read_all_bytes(filename), Tables.encryption_key,
            Tables.mac_key, Tables.verify_mac))
        count, columns = tabbit.read_table_header(reader)

        # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        records = [RouteRecord() for _ in range(count)]

        for column in columns:
            block_end = reader.position + column.byte_length
            if column.tag == 1:
                tabbit.check_column(column, "Route.From", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_STRING,))
                cursor = tabbit.ColumnCursor(reader, column, count, "Route.From")
                at = 0
                while at < count:
                    n, value = cursor.next_same_string(count - at)
                    for i in range(at, at + n):
                        records[i].from_ = value
                    at += n
            elif column.tag == 2:
                tabbit.check_column(column, "Route.To", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_STRING,))
                cursor = tabbit.ColumnCursor(reader, column, count, "Route.To")
                at = 0
                while at < count:
                    n, value = cursor.next_same_string(count - at)
                    for i in range(at, at + n):
                        records[i].to = value
                    at += n
            elif column.tag == 3:
                tabbit.check_column(column, "Route.Code", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_STRING,))
                cursor = tabbit.ColumnCursor(reader, column, count, "Route.Code")
                at = 0
                while at < count:
                    n, value = cursor.next_same_string(count - at)
                    for i in range(at, at + n):
                        records[i].code = value
                    at += n
            elif column.tag == 4:
                tabbit.check_column(column, "Route.Distance", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT))
                cursor = tabbit.ColumnCursor(reader, column, count, "Route.Distance")
                at = 0
                while at < count:
                    n, value = cursor.next_same_i32(count - at)
                    for i in range(at, at + n):
                        records[i].distance = value
                    at += n
            else:
                # A column added after this code was generated.
                reader.skip(column.byte_length)

            tabbit.check_block_end(reader, column, block_end)

        by_code = {record.code: record for record in records}
        by_from_and_to = {
            self._key_of_from_and_to(record.from_, record.to): record
            for record in records
        }

        # Published, now that every column read.
        self.records = records
        self.by_code = by_code
        self.by_from_and_to = by_from_and_to
