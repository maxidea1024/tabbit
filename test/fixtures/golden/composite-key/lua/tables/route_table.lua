-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")

-- Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2.
-- A composite primary key of strings, beside a single-column secondary one.
---@class RouteRecord
---@field from string
---@field to string
---@field code string
---@field distance integer
local RouteRecordMeta = tcb.strictType("a `Route` row", { "from", "to", "code", "distance" })

---@return RouteRecord
local function newRouteRecord()
  return setmetatable({
    from = "",
    to = "",
    code = "",
    distance = 0,
  }, RouteRecordMeta)
end

---@class RouteTable
---@field records RouteRecord[]
local RouteTable = {}

local RouteTableMeta = tcb.strictInstance("the `Route` table", RouteTable, { "records", "byCode", "byFromAndTo" })

function RouteTable.new()
  return setmetatable({
    records = {},
    byCode = {},
    byFromAndTo = {},
  }, RouteTableMeta)
end

-- The row with this Code, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function RouteTable:findByCode(key)
  return self.byCode[key]
end

-- The row with this Code, or a raised error naming what was missing -
-- for a key that has to be there.
function RouteTable:getByCodeOrThrow(key)
  local record = self:findByCode(key)

  if record == nil then
    tcb.fail("there is no record in table `Route` that corresponds to " ..
      "field `Code` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this Code.
function RouteTable:containsCode(key)
  return self:findByCode(key) ~= nil
end

-- Joins the columns of the From and To key into the text the map is keyed by.
local function keyOfFromAndTo(fromKey, toKey)
  local parts = {
    fromKey,
    toKey,
  }

  local text = {}

  for i = 1, #parts do
    text[i] = #parts[i] .. ":" .. parts[i]
  end

  return table.concat(text)
end

-- The row with this From and To, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function RouteTable:findByFromAndTo(fromKey, toKey)
  return self.byFromAndTo[keyOfFromAndTo(fromKey, toKey)]
end

-- The row with this From and To, or a raised error naming what was missing -
-- for a key that has to be there.
function RouteTable:getByFromAndToOrThrow(fromKey, toKey)
  local record = self:findByFromAndTo(fromKey, toKey)

  if record == nil then
    tcb.fail("there is no record in table `Route` that corresponds to " ..
      "field `From and To` value (%s, %s)", tostring(fromKey), tostring(toKey))
  end

  return record
end

-- Whether the table holds a row with this From and To.
function RouteTable:containsFromAndTo(fromKey, toKey)
  return self:findByFromAndTo(fromKey, toKey) ~= nil
end

-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function RouteTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newRouteRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "Route.From", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Route.From")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].from = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "Route.To", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Route.To")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].to = value
        end

        at = at + n
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "Route.Code", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Route.Code")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].code = value
        end

        at = at + n
      end
    elseif column.tag == 4 then
      tcb.checkColumn(column, "Route.Distance", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Route.Distance")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].distance = value
        end

        at = at + n
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byCode = {}

  for i = 1, #records do
    byCode[records[i].code] = records[i]
  end

  local byFromAndTo = {}

  for i = 1, #records do
    byFromAndTo[keyOfFromAndTo(records[i].from, records[i].to)] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byCode = byCode
  self.byFromAndTo = byFromAndTo
end

-- The same load from a file on disk.
function RouteTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return RouteTable
