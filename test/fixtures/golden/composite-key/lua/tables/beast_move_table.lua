-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")

-- Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : N2.
-- One row per pair; the key is both references taken together.
---@class BeastMoveRecord
---@field beastId string
---@field beastByBeastId BeastRecord|nil
---@field moveId integer
---@field moveByMoveId MoveRecord|nil
---@field power integer
local BeastMoveRecordMeta = tcb.strictType("a `BeastMove` row", { "beastId", "beastByBeastId", "moveId", "moveByMoveId", "power" })

---@return BeastMoveRecord
local function newBeastMoveRecord()
  return setmetatable({
    beastId = 0,
    moveId = 0,
    power = 0,
  }, BeastMoveRecordMeta)
end

---@class BeastMoveTable
---@field records BeastMoveRecord[]
local BeastMoveTable = {}

local BeastMoveTableMeta = tcb.strictInstance("the `BeastMove` table", BeastMoveTable, { "records", "byBeastIdAndMoveId" })

function BeastMoveTable.new()
  return setmetatable({
    records = {},
    byBeastIdAndMoveId = {},
  }, BeastMoveTableMeta)
end

-- Joins the columns of the BeastId and MoveId key into the text the map is keyed by.
local function keyOfBeastIdAndMoveId(beastIdKey, moveIdKey)
  local parts = {
    beastIdKey,
    tostring(moveIdKey),
  }

  local text = {}

  for i = 1, #parts do
    text[i] = #parts[i] .. ":" .. parts[i]
  end

  return table.concat(text)
end

-- The row with this BeastId and MoveId, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function BeastMoveTable:findByBeastIdAndMoveId(beastIdKey, moveIdKey)
  return self.byBeastIdAndMoveId[keyOfBeastIdAndMoveId(beastIdKey, moveIdKey)]
end

-- The row with this BeastId and MoveId, or a raised error naming what was missing -
-- for a key that has to be there.
function BeastMoveTable:getByBeastIdAndMoveIdOrThrow(beastIdKey, moveIdKey)
  local record = self:findByBeastIdAndMoveId(beastIdKey, moveIdKey)

  if record == nil then
    tcb.fail("there is no record in table `BeastMove` that corresponds to " ..
      "field `BeastId and MoveId` value (%s, %s)", tostring(beastIdKey), tostring(moveIdKey))
  end

  return record
end

-- Whether the table holds a row with this BeastId and MoveId.
function BeastMoveTable:containsBeastIdAndMoveId(beastIdKey, moveIdKey)
  return self:findByBeastIdAndMoveId(beastIdKey, moveIdKey) ~= nil
end

-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function BeastMoveTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newBeastMoveRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "BeastMove.BeastId", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "BeastMove.BeastId")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].beastId = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "BeastMove.MoveId", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32 })
      local cursor = tcb.newCursor(reader, column, count, "BeastMove.MoveId")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].moveId = value
        end

        at = at + n
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "BeastMove.Power", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "BeastMove.Power")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].power = value
        end

        at = at + n
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byBeastIdAndMoveId = {}

  for i = 1, #records do
    byBeastIdAndMoveId[keyOfBeastIdAndMoveId(records[i].beastId, records[i].moveId)] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byBeastIdAndMoveId = byBeastIdAndMoveId
end

-- The same load from a file on disk.
function BeastMoveTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return BeastMoveTable
