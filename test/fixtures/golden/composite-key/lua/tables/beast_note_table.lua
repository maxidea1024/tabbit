-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")

-- Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : T2.
-- Indexed by a reference to a string-keyed table, named with `key=`.
---@class BeastNoteRecord
---@field seq integer
---@field beastId string
---@field beastByBeastId BeastRecord|nil
---@field note string
local BeastNoteRecordMeta = tcb.strictType("a `BeastNote` row", { "seq", "beastId", "beastByBeastId", "note" })

---@return BeastNoteRecord
local function newBeastNoteRecord()
  return setmetatable({
    seq = 0,
    beastId = 0,
    note = "",
  }, BeastNoteRecordMeta)
end

---@class BeastNoteTable
---@field records BeastNoteRecord[]
local BeastNoteTable = {}

local BeastNoteTableMeta = tcb.strictInstance("the `BeastNote` table", BeastNoteTable, { "records", "byBeastId" })

function BeastNoteTable.new()
  return setmetatable({
    records = {},
    byBeastId = {},
  }, BeastNoteTableMeta)
end

-- The row with this BeastId, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function BeastNoteTable:findByBeastId(key)
  return self.byBeastId[key]
end

-- The row with this BeastId, or a raised error naming what was missing -
-- for a key that has to be there.
function BeastNoteTable:getByBeastIdOrThrow(key)
  local record = self:findByBeastId(key)

  if record == nil then
    tcb.fail("there is no record in table `BeastNote` that corresponds to " ..
      "field `BeastId` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this BeastId.
function BeastNoteTable:containsBeastId(key)
  return self:findByBeastId(key) ~= nil
end

-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function BeastNoteTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newBeastNoteRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "BeastNote.Seq", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "BeastNote.Seq")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].seq = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "BeastNote.BeastId", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "BeastNote.BeastId")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].beastId = value
        end

        at = at + n
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "BeastNote.Note", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "BeastNote.Note")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].note = value
        end

        at = at + n
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byBeastId = {}

  for i = 1, #records do
    byBeastId[records[i].beastId] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byBeastId = byBeastId
end

-- The same load from a file on disk.
function BeastNoteTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return BeastNoteTable
