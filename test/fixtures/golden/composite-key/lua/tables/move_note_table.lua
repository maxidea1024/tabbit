-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")

-- Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : Z2.
-- First column is the index and it is a reference to an int-keyed table.
---@class MoveNoteRecord
---@field moveId integer
---@field moveByMoveId MoveRecord|nil
---@field note string
---@field pad1 integer
local MoveNoteRecordMeta = tcb.strictType("a `MoveNote` row", { "moveId", "moveByMoveId", "note", "pad1" })

---@return MoveNoteRecord
local function newMoveNoteRecord()
  return setmetatable({
    moveId = 0,
    note = "",
    pad1 = 0,
  }, MoveNoteRecordMeta)
end

---@class MoveNoteTable
---@field records MoveNoteRecord[]
local MoveNoteTable = {}

local MoveNoteTableMeta = tcb.strictInstance("the `MoveNote` table", MoveNoteTable, { "records", "byMoveId" })

function MoveNoteTable.new()
  return setmetatable({
    records = {},
    byMoveId = {},
  }, MoveNoteTableMeta)
end

-- The row with this MoveId, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function MoveNoteTable:findByMoveId(key)
  return self.byMoveId[key]
end

-- The row with this MoveId, or a raised error naming what was missing -
-- for a key that has to be there.
function MoveNoteTable:getByMoveIdOrThrow(key)
  local record = self:findByMoveId(key)

  if record == nil then
    tcb.fail("there is no record in table `MoveNote` that corresponds to " ..
      "field `MoveId` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this MoveId.
function MoveNoteTable:containsMoveId(key)
  return self:findByMoveId(key) ~= nil
end

-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function MoveNoteTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/wire/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newMoveNoteRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "MoveNote.MoveId", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32 })
      local cursor = tcb.newCursor(reader, column, count, "MoveNote.MoveId")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].moveId = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "MoveNote.Note", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "MoveNote.Note")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].note = value
        end

        at = at + n
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "MoveNote.Pad1", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "MoveNote.Pad1")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].pad1 = value
        end

        at = at + n
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byMoveId = {}

  for i = 1, #records do
    byMoveId[records[i].moveId] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byMoveId = byMoveId
end

-- The same load from a file on disk.
function MoveNoteTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return MoveNoteTable
