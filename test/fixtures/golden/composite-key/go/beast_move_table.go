// ------------------------------------------------------------------------------
// Code generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package compositekey

import (
	"fmt"
	"iter"
	"strconv"

	"compositekey/tabbit"
)

// BeastMoveRecord was generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : N2.
// One row per pair; the key is both references taken together.
type BeastMoveRecord struct {
	// a string-keyed target
	BeastId        string
	BeastByBeastId *BeastRecord
	// an int-keyed target
	MoveId       int32
	MoveByMoveId *MoveRecord
	// anything
	Power int32
}

// BeastMoveTable holds every row of BeastMove.
type BeastMoveTable struct {
	records            []BeastMoveRecord
	byBeastIdAndMoveId map[string]int
}

// Records returns every row, in the order the sheet declared them.
func (t *BeastMoveTable) Records() []BeastMoveRecord { return t.records }

// Count returns how many rows the table holds.
func (t *BeastMoveTable) Count() int { return len(t.records) }

// All returns the rows, in the order the file wrote them - `for row := range t.All()`.
//
// The slice header is read once, here. A refresh replaces it rather than its contents, so a
// loop already running keeps the rows it started with.
func (t *BeastMoveTable) All() iter.Seq[*BeastMoveRecord] {
	records := t.records

	return func(yield func(*BeastMoveRecord) bool) {
		for i := range records {
			if !yield(&records[i]) {
				return
			}
		}
	}
}

// keyOfBeastIdAndMoveId joins the columns of the BeastId and MoveId key into the text the map is keyed by.
func (t *BeastMoveTable) keyOfBeastIdAndMoveId(beastIdKey string, moveIdKey int32) string {
	part0 := beastIdKey
	part1 := strconv.FormatInt(int64(moveIdKey), 10)
	return strconv.Itoa(len(part0)) + ":" + part0 + strconv.Itoa(len(part1)) + ":" + part1
}

// FindByBeastIdAndMoveId returns the row with this BeastId and MoveId, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *BeastMoveTable) FindByBeastIdAndMoveId(beastIdKey string, moveIdKey int32) *BeastMoveRecord {
	position, found := t.byBeastIdAndMoveId[t.keyOfBeastIdAndMoveId(beastIdKey, moveIdKey)]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByBeastIdAndMoveIdOrError returns the row with this BeastId and MoveId, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *BeastMoveTable) GetByBeastIdAndMoveIdOrError(beastIdKey string, moveIdKey int32) (*BeastMoveRecord, error) {
	position, found := t.byBeastIdAndMoveId[t.keyOfBeastIdAndMoveId(beastIdKey, moveIdKey)]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `BeastMove` that corresponds to field `BeastId and MoveId` value (%v, %v)", beastIdKey, moveIdKey)
	}

	return &t.records[position], nil
}

// ContainsBeastIdAndMoveId reports whether the table holds a row with this BeastId and MoveId.
func (t *BeastMoveTable) ContainsBeastIdAndMoveId(beastIdKey string, moveIdKey int32) bool {
	_, found := t.byBeastIdAndMoveId[t.keyOfBeastIdAndMoveId(beastIdKey, moveIdKey)]
	return found
}

// Read loads the table from a .tcb file written by Tabbit.
func (t *BeastMoveTable) Read(filename string) error {
	data, err := tabbit.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	// Unconditionally, and with whatever EncryptionKey and MacKey hold - which is nil
	// unless the project set them. A file that is neither encrypted nor signed comes back
	// from this untouched, so the load path is the same either way and there is no
	// condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
	//
	// What comes back is a window onto data rather than a copy of it, which is why data
	// stays in scope for the whole read.
	opened, err := tabbit.Open(data, EncryptionKey, MacKey, VerifyMac)
	if err != nil {
		return err
	}

	reader := tabbit.NewReader(opened)
	count, columns := tabbit.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]BeastMoveRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if tabbit.CheckColumn(reader, column, "BeastMove.BeastId", tabbit.KindScalar, false, tabbit.ElementString) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "BeastMove.BeastId")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameString(count - i)
					for ; n > 0; n-- {
						records[i].BeastId = value
						i++
					}
				}
			}
		case 2:
			if tabbit.CheckColumn(reader, column, "BeastMove.MoveId", tabbit.KindScalar, false, tabbit.ElementI32) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "BeastMove.MoveId")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].MoveId = value
						i++
					}
				}
			}
		case 3:
			if tabbit.CheckColumn(reader, column, "BeastMove.Power", tabbit.KindScalar, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "BeastMove.Power")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Power = value
						i++
					}
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		tabbit.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("BeastMove: %w", err)
	}

	byBeastIdAndMoveId := make(map[string]int, len(records))
	for i := range records {
		byBeastIdAndMoveId[t.keyOfBeastIdAndMoveId(records[i].BeastId, records[i].MoveId)] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byBeastIdAndMoveId = byBeastIdAndMoveId

	return nil
}
