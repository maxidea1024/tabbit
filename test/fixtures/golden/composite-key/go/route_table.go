// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package compositekey

import (
	"fmt"
	"strconv"

	"compositekey/tabbit"
)

// RouteRecord was generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2.
// A composite primary key of strings, beside a single-column secondary one.
type RouteRecord struct {
	// where it starts
	From string
	// where it ends
	To string
	// unique on its own
	Code string
	// anything
	Distance int32
}

// RouteTable holds every row of Route.
type RouteTable struct {
	records []RouteRecord
	byCode map[string]int
	byFromAndTo map[string]int
}

// Records returns every row, in the order the sheet declared them.
func (t *RouteTable) Records() []RouteRecord { return t.records }

// FindByCode returns the row with this Code, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *RouteTable) FindByCode(key string) *RouteRecord {
	position, found := t.byCode[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByCodeOrError returns the row with this Code, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *RouteTable) GetByCodeOrError(key string) (*RouteRecord, error) {
	position, found := t.byCode[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Route` that corresponds to field `Code` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsCode reports whether the table holds a row with this Code.
func (t *RouteTable) ContainsCode(key string) bool {
	_, found := t.byCode[key]
	return found
}

// keyOfFromAndTo joins the columns of the From and To key into the text the map is keyed by.
func (t *RouteTable) keyOfFromAndTo(fromKey string, toKey string) string {
	part0 := fromKey
	part1 := toKey
	return strconv.Itoa(len(part0)) + ":" + part0 + strconv.Itoa(len(part1)) + ":" + part1
}

// FindByFromAndTo returns the row with this From and To, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *RouteTable) FindByFromAndTo(fromKey string, toKey string) *RouteRecord {
	position, found := t.byFromAndTo[t.keyOfFromAndTo(fromKey, toKey)]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByFromAndToOrError returns the row with this From and To, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *RouteTable) GetByFromAndToOrError(fromKey string, toKey string) (*RouteRecord, error) {
	position, found := t.byFromAndTo[t.keyOfFromAndTo(fromKey, toKey)]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Route` that corresponds to field `From and To` value (%v, %v)", fromKey, toKey)
	}

	return &t.records[position], nil
}

// ContainsFromAndTo reports whether the table holds a row with this From and To.
func (t *RouteTable) ContainsFromAndTo(fromKey string, toKey string) bool {
	_, found := t.byFromAndTo[t.keyOfFromAndTo(fromKey, toKey)]
	return found
}

// Read loads the table from a .tcb file written by Tabbit.
func (t *RouteTable) Read(filename string) error {
	data, err := tabbit.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	// Unconditionally, and with whatever EncryptionKey and MacKey hold - which is nil
	// unless the project set them. A file that is neither encrypted nor signed comes back
	// from this untouched, so the load path is the same either way and there is no
	// condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
	//
	// What comes back is a window onto data rather than a copy of it, which is why data
	// stays in scope for the whole read.
	opened, err := tabbit.Open(data, EncryptionKey, MacKey, VerifyMac)
	if err != nil {
		return err
	}

	reader := tabbit.NewReader(opened)
	count, columns := tabbit.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]RouteRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if tabbit.CheckColumn(reader, column, "Route.From", tabbit.KindScalar, false, tabbit.ElementString) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Route.From")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameString(count - i)
					for ; n > 0; n-- {
						records[i].From = value
						i++
					}
				}
			}
		case 2:
			if tabbit.CheckColumn(reader, column, "Route.To", tabbit.KindScalar, false, tabbit.ElementString) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Route.To")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameString(count - i)
					for ; n > 0; n-- {
						records[i].To = value
						i++
					}
				}
			}
		case 3:
			if tabbit.CheckColumn(reader, column, "Route.Code", tabbit.KindScalar, false, tabbit.ElementString) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Route.Code")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameString(count - i)
					for ; n > 0; n-- {
						records[i].Code = value
						i++
					}
				}
			}
		case 4:
			if tabbit.CheckColumn(reader, column, "Route.Distance", tabbit.KindScalar, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Route.Distance")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Distance = value
						i++
					}
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		tabbit.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Route: %w", err)
	}

	byCode := make(map[string]int, len(records))
	for i := range records {
		byCode[records[i].Code] = i
	}

	byFromAndTo := make(map[string]int, len(records))
	for i := range records {
		byFromAndTo[t.keyOfFromAndTo(records[i].From, records[i].To)] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byCode = byCode
	t.byFromAndTo = byFromAndTo

	return nil
}
