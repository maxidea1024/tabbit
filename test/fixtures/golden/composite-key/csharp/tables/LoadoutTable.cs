// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Tabbit.Fixtures.CompositeKey
{
    /// <summary>
    /// One row per stage and slot; neither column is unique on its own.
    /// </summary>
    [System.Serializable]
    public partial class LoadoutTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// which stage
            /// </summary>
            public int Stage => _stage;

            /// <summary>
            /// which slot
            /// </summary>
            public global::Tabbit.Fixtures.CompositeKey.Slot Slot => _slot;

            /// <summary>
            /// anything
            /// </summary>
            public int Power => _power;

            /// <summary>
            /// anything
            /// </summary>
            public string Label => _label;
            #endregion

            #region Storage
            internal int _stage;
            internal global::Tabbit.Fixtures.CompositeKey.Slot _slot;
            internal int _power;
            internal string _label = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Stage\":"); ToStringHelper.ToString(Stage, sb);
                sb.Append(",\"Slot\":"); ToStringHelper.ToString(Slot, sb);
                sb.Append(",\"Power\":"); ToStringHelper.ToString(Power, sb);
                sb.Append(",\"Label\":"); ToStringHelper.ToString(Label, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Stage", "Slot", "Power", "Label" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Stage, r.Slot, r.Power, r.Label });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Stage and Slot'
        private Dictionary<string, Record> _recordsByStageAndSlot = new Dictionary<string, Record>();

        /// <summary>Joins the columns of the `Stage and Slot` key into the text the map is keyed by.</summary>
        private static string KeyOfStageAndSlot(int stageKey, global::Tabbit.Fixtures.CompositeKey.Slot slotKey)
        {
            var parts = new string[]
            {
                stageKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
                ((long)slotKey).ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `Stage and Slot`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByStageAndSlot(int stageKey, global::Tabbit.Fixtures.CompositeKey.Slot slotKey)
            => _recordsByStageAndSlot.TryGetValue(KeyOfStageAndSlot(stageKey, slotKey), out Record record) ? record : null;

        /// <summary>
        /// The row with this `Stage and Slot`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public Record GetByStageAndSlotOrThrow(int stageKey, global::Tabbit.Fixtures.CompositeKey.Slot slotKey)
        {
            if (!_recordsByStageAndSlot.TryGetValue(KeyOfStageAndSlot(stageKey, slotKey), out Record record))
                throw new TabbitException($"There is no record in table `Loadout` that corresponds to field `Stage and Slot` value ({stageKey}, {slotKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Stage and Slot`.</summary>
        public bool ContainsStageAndSlot(int stageKey, global::Tabbit.Fixtures.CompositeKey.Slot slotKey) => _recordsByStageAndSlot.ContainsKey(KeyOfStageAndSlot(stageKey, slotKey));
        #endregion // Indexing by `Stage and Slot`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await CompositeKeyAccessor.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `CompositeKeyAccessor.EncryptionKey` and `CompositeKeyAccessor.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, CompositeKeyAccessor.EncryptionKey, CompositeKeyAccessor.MacKey, CompositeKeyAccessor.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "Loadout.Stage", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Loadout.Stage");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._stage = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "Loadout.Slot", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Loadout.Slot");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._slot = (global::Tabbit.Fixtures.CompositeKey.Slot)value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "Loadout.Power", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Loadout.Power");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._power = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "Loadout.Label", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Loadout.Label");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._label = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByStageAndSlot = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsByStageAndSlot.Add(KeyOfStageAndSlot(record.Stage, record.Slot), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByStageAndSlot = recordsByStageAndSlot;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Tabbit.Fixtures.CompositeKey
