// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_COMPOSITEKEYACCESSOR_GRID_H
#define TABBIT_GENERATED_COMPOSITEKEYACCESSOR_GRID_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "CompositeKeyAccessor_forward.h"
// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Grid : B2
/// Three columns taken together, so two is not the only width that works.
struct GridRecord {
  /// column
  std::int32_t x = 0;
  /// row
  std::int32_t y = 0;
  /// layer
  std::string z;
  /// anything
  std::string name;
};

/// Three columns taken together, so two is not the only width that works.
class GridTable {
 public:
  const std::vector<GridRecord>& records() const { return records_; }

  /// Joins the columns of the X and Y and Z key into the text the map is keyed by.
  static std::string key_of_x_and_y_and_z(std::int32_t x_key, std::int32_t y_key, const std::string& z_key) {
    const std::string parts[] = {
      std::to_string(x_key),
      std::to_string(y_key),
      z_key,
    };

    std::string text;

    for (const std::string& part : parts) {
      text += std::to_string(part.size());
      text += ':';
      text += part;
    }

    return text;
  }

  /// The row with this X and Y and Z, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const GridRecord* find_by_x_and_y_and_z(std::int32_t x_key, std::int32_t y_key, const std::string& z_key) const {
    const auto it = by_x_and_y_and_z_.find(key_of_x_and_y_and_z(x_key, y_key, z_key));
    return it == by_x_and_y_and_z_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this X and Y and Z, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_x_and_y_and_z(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const GridRecord& get_by_x_and_y_and_z_or_throw(std::int32_t x_key, std::int32_t y_key, const std::string& z_key) const {
    const GridRecord* found = find_by_x_and_y_and_z(x_key, y_key, z_key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Grid` that corresponds to "
                    "field `X and Y and Z` value ") + key_of_x_and_y_and_z(x_key, y_key, z_key));
    }

    return *found;
  }

  /// Whether the table holds a row with this X and Y and Z.
  bool contains_x_and_y_and_z(std::int32_t x_key, std::int32_t y_key, const std::string& z_key) const {
    return by_x_and_y_and_z_.find(key_of_x_and_y_and_z(x_key, y_key, z_key)) != by_x_and_y_and_z_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<GridRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Grid.X", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Grid.X");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].x = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Grid.Y", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Grid.Y");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].y = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Grid.Z", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Grid.Z");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].z = value;
            }
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "Grid.Name", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Grid.Name");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].name = value;
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class CompositeKeyAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<GridRecord>&& records) {
    std::unordered_map<std::string, std::size_t> x_and_y_and_z_map;
    x_and_y_and_z_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      x_and_y_and_z_map.emplace(
        key_of_x_and_y_and_z(records[i].x, records[i].y, records[i].z), i);
    }

    records_ = std::move(records);
    by_x_and_y_and_z_ = std::move(x_and_y_and_z_map);
  }

  std::vector<GridRecord> records_;
  std::unordered_map<std::string, std::size_t> by_x_and_y_and_z_;
};

#endif  // TABBIT_GENERATED_COMPOSITEKEYACCESSOR_GRID_H
