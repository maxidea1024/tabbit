// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_COMPOSITEKEYACCESSOR_ROUTE_H
#define TABBIT_GENERATED_COMPOSITEKEYACCESSOR_ROUTE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "CompositeKeyAccessor_forward.h"
// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2
/// A composite primary key of strings, beside a single-column secondary one.
struct RouteRecord {
  /// where it starts
  std::string from;
  /// where it ends
  std::string to;
  /// unique on its own
  std::string code;
  /// anything
  std::int32_t distance = 0;
};

/// A composite primary key of strings, beside a single-column secondary one.
class RouteTable {
 public:
  const std::vector<RouteRecord>& records() const { return records_; }

  /// The row with this Code, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const RouteRecord* find_by_code(const std::string& key) const {
    const auto it = by_code_.find(key);
    return it == by_code_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Code, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_code(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const RouteRecord& get_by_code_or_throw(const std::string& key) const {
    const RouteRecord* found = find_by_code(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Route` that corresponds to "
                    "field `Code` value ") + key);
    }

    return *found;
  }

  /// Whether the table holds a row with this Code.
  bool contains_code(const std::string& key) const {
    return by_code_.find(key) != by_code_.end();
  }

  /// Joins the columns of the From and To key into the text the map is keyed by.
  static std::string key_of_from_and_to(const std::string& from_key, const std::string& to_key) {
    const std::string parts[] = {
      from_key,
      to_key,
    };

    std::string text;

    for (const std::string& part : parts) {
      text += std::to_string(part.size());
      text += ':';
      text += part;
    }

    return text;
  }

  /// The row with this From and To, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const RouteRecord* find_by_from_and_to(const std::string& from_key, const std::string& to_key) const {
    const auto it = by_from_and_to_.find(key_of_from_and_to(from_key, to_key));
    return it == by_from_and_to_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this From and To, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_from_and_to(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const RouteRecord& get_by_from_and_to_or_throw(const std::string& from_key, const std::string& to_key) const {
    const RouteRecord* found = find_by_from_and_to(from_key, to_key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Route` that corresponds to "
                    "field `From and To` value ") + key_of_from_and_to(from_key, to_key));
    }

    return *found;
  }

  /// Whether the table holds a row with this From and To.
  bool contains_from_and_to(const std::string& from_key, const std::string& to_key) const {
    return by_from_and_to_.find(key_of_from_and_to(from_key, to_key)) != by_from_and_to_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<RouteRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Route.From", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Route.From");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].from = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Route.To", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Route.To");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].to = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Route.Code", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Route.Code");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].code = value;
            }
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "Route.Distance", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Route.Distance");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].distance = value;
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class CompositeKeyAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<RouteRecord>&& records) {
    std::unordered_map<std::string, std::size_t> code_map;
    code_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      code_map.emplace(records[i].code, i);
    }

    std::unordered_map<std::string, std::size_t> from_and_to_map;
    from_and_to_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      from_and_to_map.emplace(
        key_of_from_and_to(records[i].from, records[i].to), i);
    }

    records_ = std::move(records);
    by_code_ = std::move(code_map);
    by_from_and_to_ = std::move(from_and_to_map);
  }

  std::vector<RouteRecord> records_;
  std::unordered_map<std::string, std::size_t> by_code_;
  std::unordered_map<std::string, std::size_t> by_from_and_to_;
};

#endif  // TABBIT_GENERATED_COMPOSITEKEYACCESSOR_ROUTE_H
