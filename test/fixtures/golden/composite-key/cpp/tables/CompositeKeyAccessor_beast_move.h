// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_COMPOSITEKEYACCESSOR_BEAST_MOVE_H
#define TABBIT_GENERATED_COMPOSITEKEYACCESSOR_BEAST_MOVE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "CompositeKeyAccessor_forward.h"
// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : N2
/// One row per pair; the key is both references taken together.
struct BeastMoveRecord {
  /// a string-keyed target
  std::string beast_id = std::string();
  const BeastRecord* beast_by_beast_id = nullptr;
  /// an int-keyed target
  std::int32_t move_id = 0;
  const MoveRecord* move_by_move_id = nullptr;
  /// anything
  std::int32_t power = 0;
};

/// One row per pair; the key is both references taken together.
class BeastMoveTable {
 public:
  const std::vector<BeastMoveRecord>& records() const { return records_; }

  /// How many rows the table holds.
  std::size_t size() const { return records_.size(); }

  /// Whether the table holds no rows.
  bool empty() const { return records_.empty(); }

  /// The rows, in the order the file wrote them - what a range-for binds to.
  ///
  /// A refresh replaces the vector rather than appending to it, so these are the ordinary
  /// vector invalidation rules: a loop running across a reload is reading a container that
  /// has been assigned to.
  typename std::vector<BeastMoveRecord>::const_iterator begin() const {
    return records_.begin();
  }

  typename std::vector<BeastMoveRecord>::const_iterator end() const {
    return records_.end();
  }

  /// Joins the columns of the BeastId and MoveId key into the text the map is keyed by.
  static std::string key_of_beast_id_and_move_id(const std::string& beast_id_key, std::int32_t move_id_key) {
    const std::string parts[] = {
      beast_id_key,
      std::to_string(move_id_key),
    };

    std::string text;

    for (const std::string& part : parts) {
      text += std::to_string(part.size());
      text += ':';
      text += part;
    }

    return text;
  }

  /// The row with this BeastId and MoveId, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const BeastMoveRecord* find_by_beast_id_and_move_id(const std::string& beast_id_key, std::int32_t move_id_key) const {
    const auto it = by_beast_id_and_move_id_.find(key_of_beast_id_and_move_id(beast_id_key, move_id_key));
    return it == by_beast_id_and_move_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this BeastId and MoveId, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_beast_id_and_move_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const BeastMoveRecord& get_by_beast_id_and_move_id_or_throw(const std::string& beast_id_key, std::int32_t move_id_key) const {
    const BeastMoveRecord* found = find_by_beast_id_and_move_id(beast_id_key, move_id_key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `BeastMove` that corresponds to "
                    "field `BeastId and MoveId` value ") + key_of_beast_id_and_move_id(beast_id_key, move_id_key));
    }

    return *found;
  }

  /// Whether the table holds a row with this BeastId and MoveId.
  bool contains_beast_id_and_move_id(const std::string& beast_id_key, std::int32_t move_id_key) const {
    return by_beast_id_and_move_id_.find(key_of_beast_id_and_move_id(beast_id_key, move_id_key)) != by_beast_id_and_move_id_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<BeastMoveRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "BeastMove.BeastId", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "BeastMove.BeastId");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].beast_id = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "BeastMove.MoveId", tabbit::kKindScalar, false, {tabbit::kElementI32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "BeastMove.MoveId");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].move_id = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "BeastMove.Power", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "BeastMove.Power");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].power = value;
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class CompositeKeyAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<BeastMoveRecord>&& records) {
    std::unordered_map<std::string, std::size_t> beast_id_and_move_id_map;
    beast_id_and_move_id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      beast_id_and_move_id_map.emplace(
        key_of_beast_id_and_move_id(records[i].beast_id, records[i].move_id), i);
    }

    records_ = std::move(records);
    by_beast_id_and_move_id_ = std::move(beast_id_and_move_id_map);
  }

  std::vector<BeastMoveRecord> records_;
  std::unordered_map<std::string, std::size_t> by_beast_id_and_move_id_;
};

#endif  // TABBIT_GENERATED_COMPOSITEKEYACCESSOR_BEAST_MOVE_H
