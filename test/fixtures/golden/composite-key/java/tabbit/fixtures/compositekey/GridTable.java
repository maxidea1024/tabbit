// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package tabbit.fixtures.compositekey;

import java.nio.file.Path;
import java.util.AbstractMap;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;

import tabbit.TcbReader;

/** Every row of Grid. */
public final class GridTable implements Iterable<GridRecord> {
    private List<GridRecord> records = new ArrayList<>();
    private Map<String, GridRecord> byXAndYAndZ = new HashMap<>();

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    public List<GridRecord> records() {
        return records;
    }

    /** How many rows the table holds. */
    public int size() {
        return records.size();
    }

    /**
     * The rows, in the order the file wrote them.
     *
     * The list reference is read once, here. A refresh replaces the reference rather than
     * its contents, so a loop already running keeps the rows it started with.
     */
    @Override
    public Iterator<GridRecord> iterator() {
        return records.iterator();
    }

    /** Joins the columns of the X and Y and Z key into the text the map is keyed by. */
    private static String keyOfXAndYAndZ(int xKey, int yKey, String zKey) {
        String[] parts = {
            Long.toString(xKey),
            Long.toString(yKey),
            zKey,
        };

        StringBuilder text = new StringBuilder();

        for (String part : parts) {
            text.append(part.length()).append(':').append(part);
        }

        return text.toString();
    }

    /**
     * The row with this X and Y and Z, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public GridRecord findByXAndYAndZ(int xKey, int yKey, String zKey) {
        return byXAndYAndZ.get(keyOfXAndYAndZ(xKey, yKey, zKey));
    }

    /**
     * The row with this X and Y and Z, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByXAndYAndZ(key).name() at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public GridRecord getByXAndYAndZOrThrow(int xKey, int yKey, String zKey) {
        GridRecord record = byXAndYAndZ.get(keyOfXAndYAndZ(xKey, yKey, zKey));

        if (record == null) {
            throw new TcbReader.RecordNotFoundException(
                "there is no record in table `Grid` that corresponds to field "
                + String.format("`X and Y and Z` value (%s, %s, %s)", xKey, yKey, zKey));
        }

        return record;
    }

    /** Whether the table holds a row with this X and Y and Z. */
    public boolean containsXAndYAndZ(int xKey, int yKey, String zKey) {
        return byXAndYAndZ.containsKey(keyOfXAndYAndZ(xKey, yKey, zKey));
    }

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    public void read(Path filename) {
        // Opened unconditionally, and with whatever CompositeKeyAccessor.encryptionKey and
        // CompositeKeyAccessor.macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from this untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/wire/tcb-mac-and-signature.md.
        TcbReader reader = new TcbReader(TcbReader.open(
            TcbReader.readAllBytes(filename), CompositeKeyAccessor.encryptionKey,
            CompositeKeyAccessor.macKey, CompositeKeyAccessor.verifyMac));
        TcbReader.Header header = TcbReader.readTableHeader(reader);
        int count = header.rowCount;
        TcbReader.ColumnCursor cursor;

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        List<GridRecord> loaded = new ArrayList<>(count);
        Map<String, GridRecord> loadedByXAndYAndZ = new HashMap<>(count * 2);

        for (int i = 0; i < count; i++) {
            loaded.add(new GridRecord());
        }

        for (TcbReader.Column column : header.columns) {
            int blockEnd = reader.position() + column.byteLength;

            switch (column.tag) {
                case 1: {
                    TcbReader.checkColumn(column, "Grid.X", TcbReader.KIND_SCALAR, false, TcbReader.ELEMENT_I32, TcbReader.ELEMENT_VARINT);
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Grid.X");
                    for (int i = 0; i < count; ) {
                        int n = cursor.nextSameI32(count - i);
                        for (; n > 0; n--, i++) {
                            loaded.get(i).x = cursor.runSameValue;
                        }
                    }
                    break;
                }
                case 2: {
                    TcbReader.checkColumn(column, "Grid.Y", TcbReader.KIND_SCALAR, false, TcbReader.ELEMENT_I32, TcbReader.ELEMENT_VARINT);
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Grid.Y");
                    for (int i = 0; i < count; ) {
                        int n = cursor.nextSameI32(count - i);
                        for (; n > 0; n--, i++) {
                            loaded.get(i).y = cursor.runSameValue;
                        }
                    }
                    break;
                }
                case 3: {
                    TcbReader.checkColumn(column, "Grid.Z", TcbReader.KIND_SCALAR, false, TcbReader.ELEMENT_STRING);
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Grid.Z");
                    for (int i = 0; i < count; ) {
                        int n = cursor.nextSameString(count - i);
                        for (; n > 0; n--, i++) {
                            loaded.get(i).z = cursor.runSameText;
                        }
                    }
                    break;
                }
                case 4: {
                    TcbReader.checkColumn(column, "Grid.Name", TcbReader.KIND_SCALAR, false, TcbReader.ELEMENT_STRING);
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Grid.Name");
                    for (int i = 0; i < count; ) {
                        int n = cursor.nextSameString(count - i);
                        for (; n > 0; n--, i++) {
                            loaded.get(i).name = cursor.runSameText;
                        }
                    }
                    break;
                }
                default:
                    // A column added after this code was generated.
                    reader.skip(column.byteLength);
                    break;
            }

            TcbReader.checkBlockEnd(reader, column, blockEnd);
        }

        for (GridRecord record : loaded) {
            loadedByXAndYAndZ.put(
                keyOfXAndYAndZ(record.x, record.y, record.z), record);
        }

        // Published, now that every column read.
        records = loaded;
        byXAndYAndZ = loadedByXAndYAndZ;
    }
}
