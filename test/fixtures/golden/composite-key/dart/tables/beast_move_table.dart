// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : N2
/// One row per pair; the key is both references taken together.
class BeastMoveRecord {
  /// a string-keyed target
  String beastId = '';
  BeastRecord? beastByBeastId;
  /// an int-keyed target
  int moveId = 0;
  MoveRecord? moveByMoveId;
  /// anything
  int power = 0;

}

/// Every row of BeastMove.
class BeastMoveTable extends Iterable<BeastMoveRecord> {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<BeastMoveRecord> get records => _records;
  List<BeastMoveRecord> _records = [];

  /// How many rows the table holds.
  ///
  /// Overridden rather than inherited: `Iterable.length` counts by walking, and the rows
  /// are a list that already knows.
  @override
  int get length => _records.length;

  /// The rows, in the order the file wrote them. Extending `Iterable` on top of this is
  /// what gives the table `map` - `where` - `firstWhere` without any of them being
  /// written here.
  ///
  /// The list reference is read once, here. A refresh replaces the reference rather than
  /// its contents, so a loop already running keeps the rows it started with.
  @override
  Iterator<BeastMoveRecord> get iterator => _records.iterator;

  Map<String, BeastMoveRecord> _byBeastIdAndMoveId = {};

  /// Joins the columns of the BeastId and MoveId key into the text the map is keyed by.
  static String _keyOfBeastIdAndMoveId(String beastIdKey, int moveIdKey) {
    final parts = <String>[
      beastIdKey,
      moveIdKey.toString(),
    ];

    final text = StringBuffer();

    for (final part in parts) {
      text.write('${part.length}:$part');
    }

    return text.toString();
  }

  /// The row with this BeastId and MoveId, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  BeastMoveRecord? findByBeastIdAndMoveId(String beastIdKey, int moveIdKey) => _byBeastIdAndMoveId[_keyOfBeastIdAndMoveId(beastIdKey, moveIdKey)];

  /// The row with this BeastId and MoveId, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getByBeastIdAndMoveId(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  BeastMoveRecord getByBeastIdAndMoveIdOrThrow(String beastIdKey, int moveIdKey) {
    final record = _byBeastIdAndMoveId[_keyOfBeastIdAndMoveId(beastIdKey, moveIdKey)];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `BeastMove` that corresponds to '
          'field `BeastId and MoveId` value ($beastIdKey, $moveIdKey)');
    }

    return record;
  }

  /// Whether the table holds a row with this BeastId and MoveId.
  bool containsBeastIdAndMoveId(String beastIdKey, int moveIdKey) => _byBeastIdAndMoveId.containsKey(_keyOfBeastIdAndMoveId(beastIdKey, moveIdKey));

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <BeastMoveRecord>[];
    final loadedByBeastIdAndMoveId = <String, BeastMoveRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(BeastMoveRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'BeastMove.BeastId', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'BeastMove.BeastId');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].beastId = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'BeastMove.MoveId', kindScalar, false, [elementI32]);
          cursor = TcbColumnCursor(reader, column, count, 'BeastMove.MoveId');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].moveId = value;
            }
          }
          break;
        case 3:
          checkColumn(column, 'BeastMove.Power', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'BeastMove.Power');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].power = value;
            }
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedByBeastIdAndMoveId[_keyOfBeastIdAndMoveId(record.beastId, record.moveId)] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byBeastIdAndMoveId = loadedByBeastIdAndMoveId;
  }
}
