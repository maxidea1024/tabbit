// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2
/// A composite primary key of strings, beside a single-column secondary one.
class RouteRecord {
  /// where it starts
  String from = '';
  /// where it ends
  String to = '';
  /// unique on its own
  String code = '';
  /// anything
  int distance = 0;

}

/// Every row of Route.
class RouteTable extends Iterable<RouteRecord> {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<RouteRecord> get records => _records;
  List<RouteRecord> _records = [];

  /// How many rows the table holds.
  ///
  /// Overridden rather than inherited: `Iterable.length` counts by walking, and the rows
  /// are a list that already knows.
  @override
  int get length => _records.length;

  /// The rows, in the order the file wrote them. Extending `Iterable` on top of this is
  /// what gives the table `map` - `where` - `firstWhere` without any of them being
  /// written here.
  ///
  /// The list reference is read once, here. A refresh replaces the reference rather than
  /// its contents, so a loop already running keeps the rows it started with.
  @override
  Iterator<RouteRecord> get iterator => _records.iterator;

  Map<String, RouteRecord> _byCode = {};
  Map<String, RouteRecord> _byFromAndTo = {};

  /// The row with this Code, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  RouteRecord? findByCode(String key) => _byCode[key];

  /// The row with this Code, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getByCode(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  RouteRecord getByCodeOrThrow(String key) {
    final record = _byCode[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `Route` that corresponds to '
          'field `Code` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Code.
  bool containsCode(String key) => _byCode.containsKey(key);

  /// Joins the columns of the From and To key into the text the map is keyed by.
  static String _keyOfFromAndTo(String fromKey, String toKey) {
    final parts = <String>[
      fromKey,
      toKey,
    ];

    final text = StringBuffer();

    for (final part in parts) {
      text.write('${part.length}:$part');
    }

    return text.toString();
  }

  /// The row with this From and To, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  RouteRecord? findByFromAndTo(String fromKey, String toKey) => _byFromAndTo[_keyOfFromAndTo(fromKey, toKey)];

  /// The row with this From and To, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getByFromAndTo(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  RouteRecord getByFromAndToOrThrow(String fromKey, String toKey) {
    final record = _byFromAndTo[_keyOfFromAndTo(fromKey, toKey)];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `Route` that corresponds to '
          'field `From and To` value ($fromKey, $toKey)');
    }

    return record;
  }

  /// Whether the table holds a row with this From and To.
  bool containsFromAndTo(String fromKey, String toKey) => _byFromAndTo.containsKey(_keyOfFromAndTo(fromKey, toKey));

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <RouteRecord>[];
    final loadedByCode = <String, RouteRecord>{};
    final loadedByFromAndTo = <String, RouteRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(RouteRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'Route.From', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'Route.From');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].from = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'Route.To', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'Route.To');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].to = value;
            }
          }
          break;
        case 3:
          checkColumn(column, 'Route.Code', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'Route.Code');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].code = value;
            }
          }
          break;
        case 4:
          checkColumn(column, 'Route.Distance', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Route.Distance');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].distance = value;
            }
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedByCode[record.code] = record;
      loadedByFromAndTo[_keyOfFromAndTo(record.from, record.to)] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byCode = loadedByCode;
    _byFromAndTo = loadedByFromAndTo;
  }
}
