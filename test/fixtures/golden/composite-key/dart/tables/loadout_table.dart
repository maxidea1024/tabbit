// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Loadout : B2
/// One row per stage and slot; neither column is unique on its own.
class LoadoutRecord {
  /// which stage
  int stage = 0;
  /// which slot
  Slot slot = Slot.of(0);
  /// anything
  int power = 0;
  /// anything
  String label = '';

}

/// Every row of Loadout.
class LoadoutTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<LoadoutRecord> get records => _records;
  List<LoadoutRecord> _records = [];

  Map<String, LoadoutRecord> _byStageAndSlot = {};

  /// Joins the columns of the Stage and Slot key into the text the map is keyed by.
  static String _keyOfStageAndSlot(int stageKey, Slot slotKey) {
    final parts = <String>[
      stageKey.toString(),
      slotKey.value.toString(),
    ];

    final text = StringBuffer();

    for (final part in parts) {
      text.write('${part.length}:$part');
    }

    return text.toString();
  }

  /// The row with this Stage and Slot, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  LoadoutRecord? findByStageAndSlot(int stageKey, Slot slotKey) => _byStageAndSlot[_keyOfStageAndSlot(stageKey, slotKey)];

  /// The row with this Stage and Slot, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getByStageAndSlot(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  LoadoutRecord getByStageAndSlotOrThrow(int stageKey, Slot slotKey) {
    final record = _byStageAndSlot[_keyOfStageAndSlot(stageKey, slotKey)];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `Loadout` that corresponds to '
          'field `Stage and Slot` value ($stageKey, $slotKey)');
    }

    return record;
  }

  /// Whether the table holds a row with this Stage and Slot.
  bool containsStageAndSlot(int stageKey, Slot slotKey) => _byStageAndSlot.containsKey(_keyOfStageAndSlot(stageKey, slotKey));

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <LoadoutRecord>[];
    final loadedByStageAndSlot = <String, LoadoutRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(LoadoutRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'Loadout.Stage', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Loadout.Stage');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].stage = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'Loadout.Slot', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Loadout.Slot');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].slot = Slot.of(value);
            }
          }
          break;
        case 3:
          checkColumn(column, 'Loadout.Power', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Loadout.Power');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].power = value;
            }
          }
          break;
        case 4:
          checkColumn(column, 'Loadout.Label', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'Loadout.Label');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].label = value;
            }
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedByStageAndSlot[_keyOfStageAndSlot(record.stage, record.slot)] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byStageAndSlot = loadedByStageAndSlot;
  }
}
