// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

use std::collections::HashMap;
use std::path::Path;
use crate::tabbit;

// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2
/// A composite primary key of strings, beside a single-column secondary one.
#[derive(Clone, Debug, Default)]
pub struct RouteRecord {
    /// where it starts
    pub from: String,
    /// where it ends
    pub to: String,
    /// unique on its own
    pub code: String,
    /// anything
    pub distance: i32,
}

impl RouteRecord {
}

/// Every row of Route.
#[derive(Clone, Debug, Default)]
pub struct RouteTable {
    records: Vec<RouteRecord>,
    by_code: HashMap<String, usize>,
    by_from_and_to: HashMap<String, usize>,
}

impl RouteTable {
    /// Every row, in the order the sheet declared them.
    pub fn records(&self) -> &[RouteRecord] {
        &self.records
    }

    /// The row with this Code, or None when the table has none.
    ///
    /// The lookup to reach for when a missing row is an ordinary answer - an optional
    /// reference, a key that came from user input.
    pub fn find_by_code(&self, key: &str) -> Option<&RouteRecord> {
        self.by_code.get(key).map(|position| &self.records[*position])
    }

    /// The row with this Code, or an error naming what was missing.
    ///
    /// For a key that has to be there - one from another table, or a constant. Rust has no
    /// exception to throw, so where the other languages name a throw this one names the
    /// error it returns.
    pub fn get_by_code_or_error(&self, key: &str) -> tabbit::Result<&RouteRecord> {
        self.by_code
            .get(key)
            .map(|position| &self.records[*position])
            .ok_or_else(|| tabbit::Error::RecordNotFound {
                table: "Route",
                field: "Code",
                key: format!("{:?}", key),
            })
    }

    /// Whether the table holds a row with this Code.
    pub fn contains_code(&self, key: &str) -> bool {
        self.by_code.contains_key(key)
    }

    /// Joins the columns of the From and To key into the text the map is keyed by.
    fn key_of_from_and_to(from_key: &str, to_key: &str) -> String {
        let parts: [String; 2] = [
            from_key.to_string(),
            to_key.to_string(),
        ];

        let mut text = String::new();

        for part in parts.iter() {
            text.push_str(&part.len().to_string());
            text.push(':');
            text.push_str(part);
        }

        text
    }

    /// The row with this From and To, or None when the table has none.
    ///
    /// The lookup to reach for when a missing row is an ordinary answer - an optional
    /// reference, a key that came from user input.
    pub fn find_by_from_and_to(&self, from_key: &str, to_key: &str) -> Option<&RouteRecord> {
        self.by_from_and_to.get(&Self::key_of_from_and_to(from_key, to_key)).map(|position| &self.records[*position])
    }

    /// The row with this From and To, or an error naming what was missing.
    ///
    /// For a key that has to be there - one from another table, or a constant. Rust has no
    /// exception to throw, so where the other languages name a throw this one names the
    /// error it returns.
    pub fn get_by_from_and_to_or_error(&self, from_key: &str, to_key: &str) -> tabbit::Result<&RouteRecord> {
        self.by_from_and_to
            .get(&Self::key_of_from_and_to(from_key, to_key))
            .map(|position| &self.records[*position])
            .ok_or_else(|| tabbit::Error::RecordNotFound {
                table: "Route",
                field: "From and To",
                key: format!("({:?}, {:?})", from_key, to_key),
            })
    }

    /// Whether the table holds a row with this From and To.
    pub fn contains_from_and_to(&self, from_key: &str, to_key: &str) -> bool {
        self.by_from_and_to.contains_key(&Self::key_of_from_and_to(from_key, to_key))
    }

    /// Loads the table from a .tcb file written by Tabbit.
    pub fn read(&mut self, filename: &Path) -> tabbit::Result<()> {
        // Mutable, and it outlives the reader on purpose: opening the envelope decrypts in
        // place and hands back a borrow of these same bytes rather than a copy of them.
        let mut data = tabbit::read_all_bytes(filename)?;

        // Unconditionally, and with whatever `ENCRYPTION_KEY` and `MAC_KEY` hold - which is
        // unset unless the project set them. A file that is neither encrypted nor signed
        // comes back from this untouched, so the load path is the same either way and there
        // is no condition here that could be the wrong way round.
        // spec/tcb-mac-and-signature.md.
        let key: &[u8] = crate::ENCRYPTION_KEY.get().map(Vec::as_slice).unwrap_or(&[]);
        let mac_key: &[u8] = crate::MAC_KEY.get().map(Vec::as_slice).unwrap_or(&[]);

        let mut reader = tabbit::Reader::new(tabbit::open(
            &mut data,
            key,
            mac_key,
            crate::VERIFY_MAC.load(std::sync::atomic::Ordering::Relaxed),
        )?);

        // Column by column, matched by tag rather than position: a column this build
        // does not know is skipped by its block length, and one whose type changed
        // incompatibly fails naming the field.
        let header = tabbit::read_table_header(&mut reader)?;
        let count = header.row_count.max(0) as usize;

        // Read into rows of its own and assigned at the end. Reading a table that is
        // already loaded is a refresh, and one that turns out to be unreadable has to
        // leave the rows already there alone - `?` returns before anything is replaced.
        let mut records = vec![RouteRecord::default(); count];

        for column in &header.columns {
            let block_end = reader.position() + column.byte_length.max(0) as usize;

            match column.tag {
                1 => {
                    tabbit::check_column(column, "Route.From", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_STRING])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Route.From")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_string((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].from = value.clone();
                            at += 1;
                        }
                    }
                }
                2 => {
                    tabbit::check_column(column, "Route.To", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_STRING])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Route.To")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_string((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].to = value.clone();
                            at += 1;
                        }
                    }
                }
                3 => {
                    tabbit::check_column(column, "Route.Code", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_STRING])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Route.Code")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_string((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].code = value.clone();
                            at += 1;
                        }
                    }
                }
                4 => {
                    tabbit::check_column(column, "Route.Distance", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_I32, tabbit::ELEMENT_VARINT])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "Route.Distance")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_i32((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].distance = value;
                            at += 1;
                        }
                    }
                }
                _ => {
                    // A column added after this code was generated.
                    reader.skip(column.byte_length)?;
                }
            }

            tabbit::check_block_end(&reader, column, block_end)?;
        }

        let by_code = records
            .iter()
            .enumerate()
            .map(|(position, record)| (record.code.clone(), position))
            .collect();

        let by_from_and_to = records
            .iter()
            .enumerate()
            .map(|(position, record)| (
                Self::key_of_from_and_to(&record.from, &record.to),
                position))
            .collect();

        // Published, now that every column read.
        self.records = records;
        self.by_code = by_code;
        self.by_from_and_to = by_from_and_to;

        Ok(())
    }
}
