<?php

// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

declare(strict_types=1);

namespace Tabbit\Fixtures\CompositeKey;

require_once __DIR__ . '/../tabbit/TcbReader.php';
require_once __DIR__ . '/../CompositeKeyAccessor.php';

use Tabbit\TcbReader;
use Tabbit\TcbColumnCursor;
use Tabbit\RecordNotFoundException;
use Tabbit\Uuid;

/**
 * Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2
 *
 * A composite primary key of strings, beside a single-column secondary one.
 */
final class RouteRecord
{
    /** where it starts */
    public string $from = '';
    /** where it ends */
    public string $to = '';
    /** unique on its own */
    public string $code = '';
    /** anything */
    public int $distance = 0;
}

/** Every row of Route. */
final class RouteTable
{
    /** @var list<RouteRecord> */
    public array $records = [];

    /** @var array<string, RouteRecord> */
    private array $byCode = [];

    /** @var array<string, RouteRecord> */
    private array $byFromAndTo = [];

    /**
     * The row with this Code, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findByCode(string $key): ?RouteRecord
    {
        return $this->byCode[$key] ?? null;
    }

    /**
     * The row with this Code, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByCode($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getByCodeOrThrow(string $key): RouteRecord
    {
        $record = $this->byCode[$key] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `Route` that corresponds to " .
                "field `Code` value " . $key . ".");
        }

        return $record;
    }

    /** Whether the table holds a row with this Code. */
    public function containsCode(string $key): bool
    {
        return isset($this->byCode[$key]);
    }

    /** Joins the columns of the From and To key into the text the map is keyed by. */
    private static function keyOfFromAndTo(string $fromKey, string $toKey): string
    {
        $parts = [
            $fromKey,
            $toKey,
        ];

        $text = "";

        foreach ($parts as $part) {
            $text .= strlen($part) . ":" . $part;
        }

        return $text;
    }

    /**
     * The row with this From and To, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findByFromAndTo(string $fromKey, string $toKey): ?RouteRecord
    {
        return $this->byFromAndTo[self::keyOfFromAndTo($fromKey, $toKey)] ?? null;
    }

    /**
     * The row with this From and To, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByFromAndTo($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getByFromAndToOrThrow(string $fromKey, string $toKey): RouteRecord
    {
        $record = $this->byFromAndTo[self::keyOfFromAndTo($fromKey, $toKey)] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `Route` that corresponds to " .
                sprintf("field `From and To` value (%s, %s).", $fromKey, $toKey));
        }

        return $record;
    }

    /** Whether the table holds a row with this From and To. */
    public function containsFromAndTo(string $fromKey, string $toKey): bool
    {
        return isset($this->byFromAndTo[self::keyOfFromAndTo($fromKey, $toKey)]);
    }

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    public function read(string $filename): void
    {
        // The keys go to fromFile because reading and opening are one call here: the
        // reader's constructor takes the whole file and opens the envelope itself. It does
        // so unconditionally, and with whatever CompositeKeyAccessor::$encryptionKey and
        // CompositeKeyAccessor::$macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from that untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/tcb-mac-and-signature.md.
        $reader = TcbReader::fromFile(
            $filename, CompositeKeyAccessor::$encryptionKey,
            CompositeKeyAccessor::$macKey, CompositeKeyAccessor::$verifyMac);
        [$count, $columns] = $reader->readTableHeader();

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        $records = [];
        $byCode = [];
        $byFromAndTo = [];

        for ($i = 0; $i < $count; $i++) {
            $records[] = new RouteRecord();
        }

        foreach ($columns as $column) {
            $blockEnd = $reader->position() + $column['byteLength'];

            switch ($column['tag']) {
                case 1:
                    TcbReader::checkColumn($column, 'Route.From', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_STRING]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Route.From');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameString($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->from = $value;
                        }
                    }
                    break;

                case 2:
                    TcbReader::checkColumn($column, 'Route.To', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_STRING]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Route.To');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameString($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->to = $value;
                        }
                    }
                    break;

                case 3:
                    TcbReader::checkColumn($column, 'Route.Code', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_STRING]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Route.Code');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameString($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->code = $value;
                        }
                    }
                    break;

                case 4:
                    TcbReader::checkColumn($column, 'Route.Distance', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_I32, TcbReader::ELEMENT_VARINT]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Route.Distance');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameI32($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->distance = $value;
                        }
                    }
                    break;

                default:
                    // A column added after this code was generated.
                    $reader->skip($column['byteLength']);
                    break;
            }

            $reader->checkBlockEnd($column, $blockEnd);
        }

        foreach ($records as $record) {
            $byCode[$record->code] = $record;
            $byFromAndTo[self::keyOfFromAndTo($record->from, $record->to)] = $record;
        }

        // Published, now that every column read.
        $this->records = $records;
        $this->byCode = $byCode;
        $this->byFromAndTo = $byFromAndTo;
    }
}
