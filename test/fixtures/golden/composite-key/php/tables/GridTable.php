<?php

// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

declare(strict_types=1);

namespace Tabbit\Fixtures\CompositeKey;

require_once __DIR__ . '/../tabbit/TcbReader.php';
require_once __DIR__ . '/../CompositeKeyAccessor.php';

use Tabbit\TcbReader;
use Tabbit\TcbColumnCursor;
use Tabbit\RecordNotFoundException;
use Tabbit\Uuid;

/**
 * Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Grid : B2
 *
 * Three columns taken together, so two is not the only width that works.
 */
final class GridRecord
{
    /** column */
    public int $x = 0;
    /** row */
    public int $y = 0;
    /** layer */
    public string $z = '';
    /** anything */
    public string $name = '';
}

/** Every row of Grid. */
final class GridTable
{
    /** @var list<GridRecord> */
    public array $records = [];

    /** @var array<string, GridRecord> */
    private array $byXAndYAndZ = [];

    /** Joins the columns of the X and Y and Z key into the text the map is keyed by. */
    private static function keyOfXAndYAndZ(int $xKey, int $yKey, string $zKey): string
    {
        $parts = [
            (string)(int)$xKey,
            (string)(int)$yKey,
            $zKey,
        ];

        $text = "";

        foreach ($parts as $part) {
            $text .= strlen($part) . ":" . $part;
        }

        return $text;
    }

    /**
     * The row with this X and Y and Z, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findByXAndYAndZ(int $xKey, int $yKey, string $zKey): ?GridRecord
    {
        return $this->byXAndYAndZ[self::keyOfXAndYAndZ($xKey, $yKey, $zKey)] ?? null;
    }

    /**
     * The row with this X and Y and Z, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByXAndYAndZ($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getByXAndYAndZOrThrow(int $xKey, int $yKey, string $zKey): GridRecord
    {
        $record = $this->byXAndYAndZ[self::keyOfXAndYAndZ($xKey, $yKey, $zKey)] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `Grid` that corresponds to " .
                sprintf("field `X and Y and Z` value (%s, %s, %s).", $xKey, $yKey, $zKey));
        }

        return $record;
    }

    /** Whether the table holds a row with this X and Y and Z. */
    public function containsXAndYAndZ(int $xKey, int $yKey, string $zKey): bool
    {
        return isset($this->byXAndYAndZ[self::keyOfXAndYAndZ($xKey, $yKey, $zKey)]);
    }

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    public function read(string $filename): void
    {
        // The keys go to fromFile because reading and opening are one call here: the
        // reader's constructor takes the whole file and opens the envelope itself. It does
        // so unconditionally, and with whatever CompositeKeyAccessor::$encryptionKey and
        // CompositeKeyAccessor::$macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from that untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/wire/tcb-mac-and-signature.md.
        $reader = TcbReader::fromFile(
            $filename, CompositeKeyAccessor::$encryptionKey,
            CompositeKeyAccessor::$macKey, CompositeKeyAccessor::$verifyMac);
        [$count, $columns] = $reader->readTableHeader();

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        $records = [];
        $byXAndYAndZ = [];

        for ($i = 0; $i < $count; $i++) {
            $records[] = new GridRecord();
        }

        foreach ($columns as $column) {
            $blockEnd = $reader->position() + $column['byteLength'];

            switch ($column['tag']) {
                case 1:
                    TcbReader::checkColumn($column, 'Grid.X', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_I32, TcbReader::ELEMENT_VARINT]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Grid.X');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameI32($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->x = $value;
                        }
                    }
                    break;

                case 2:
                    TcbReader::checkColumn($column, 'Grid.Y', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_I32, TcbReader::ELEMENT_VARINT]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Grid.Y');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameI32($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->y = $value;
                        }
                    }
                    break;

                case 3:
                    TcbReader::checkColumn($column, 'Grid.Z', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_STRING]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Grid.Z');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameString($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->z = $value;
                        }
                    }
                    break;

                case 4:
                    TcbReader::checkColumn($column, 'Grid.Name', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_STRING]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'Grid.Name');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameString($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->name = $value;
                        }
                    }
                    break;

                default:
                    // A column added after this code was generated.
                    $reader->skip($column['byteLength']);
                    break;
            }

            $reader->checkBlockEnd($column, $blockEnd);
        }

        foreach ($records as $record) {
            $byXAndYAndZ[self::keyOfXAndYAndZ($record->x, $record->y, $record->z)] = $record;
        }

        // Published, now that every column read.
        $this->records = $records;
        $this->byXAndYAndZ = $byXAndYAndZ;
    }
}
