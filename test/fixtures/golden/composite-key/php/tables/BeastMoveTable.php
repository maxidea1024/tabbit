<?php

// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

declare(strict_types=1);

namespace Tabbit\Fixtures\CompositeKey;

require_once __DIR__ . '/../tabbit/TcbReader.php';
require_once __DIR__ . '/../CompositeKeyAccessor.php';

use Tabbit\TcbReader;
use Tabbit\TcbColumnCursor;
use Tabbit\RecordNotFoundException;
use Tabbit\Uuid;

/**
 * Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : N2
 *
 * One row per pair; the key is both references taken together.
 */
final class BeastMoveRecord
{
    /** a string-keyed target */
    public string $beastId = '';

    public ?BeastRecord $beastByBeastId = null;
    /** an int-keyed target */
    public int $moveId = 0;

    public ?MoveRecord $moveByMoveId = null;
    /** anything */
    public int $power = 0;
}

/** Every row of BeastMove. */
final class BeastMoveTable
{
    /** @var list<BeastMoveRecord> */
    public array $records = [];

    /** @var array<string, BeastMoveRecord> */
    private array $byBeastIdAndMoveId = [];

    /** Joins the columns of the BeastId and MoveId key into the text the map is keyed by. */
    private static function keyOfBeastIdAndMoveId(string $beastIdKey, int $moveIdKey): string
    {
        $parts = [
            $beastIdKey,
            (string)(int)$moveIdKey,
        ];

        $text = "";

        foreach ($parts as $part) {
            $text .= strlen($part) . ":" . $part;
        }

        return $text;
    }

    /**
     * The row with this BeastId and MoveId, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findByBeastIdAndMoveId(string $beastIdKey, int $moveIdKey): ?BeastMoveRecord
    {
        return $this->byBeastIdAndMoveId[self::keyOfBeastIdAndMoveId($beastIdKey, $moveIdKey)] ?? null;
    }

    /**
     * The row with this BeastId and MoveId, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByBeastIdAndMoveId($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getByBeastIdAndMoveIdOrThrow(string $beastIdKey, int $moveIdKey): BeastMoveRecord
    {
        $record = $this->byBeastIdAndMoveId[self::keyOfBeastIdAndMoveId($beastIdKey, $moveIdKey)] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `BeastMove` that corresponds to " .
                sprintf("field `BeastId and MoveId` value (%s, %s).", $beastIdKey, $moveIdKey));
        }

        return $record;
    }

    /** Whether the table holds a row with this BeastId and MoveId. */
    public function containsBeastIdAndMoveId(string $beastIdKey, int $moveIdKey): bool
    {
        return isset($this->byBeastIdAndMoveId[self::keyOfBeastIdAndMoveId($beastIdKey, $moveIdKey)]);
    }

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    public function read(string $filename): void
    {
        // The keys go to fromFile because reading and opening are one call here: the
        // reader's constructor takes the whole file and opens the envelope itself. It does
        // so unconditionally, and with whatever CompositeKeyAccessor::$encryptionKey and
        // CompositeKeyAccessor::$macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from that untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/tcb-mac-and-signature.md.
        $reader = TcbReader::fromFile(
            $filename, CompositeKeyAccessor::$encryptionKey,
            CompositeKeyAccessor::$macKey, CompositeKeyAccessor::$verifyMac);
        [$count, $columns] = $reader->readTableHeader();

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        $records = [];
        $byBeastIdAndMoveId = [];

        for ($i = 0; $i < $count; $i++) {
            $records[] = new BeastMoveRecord();
        }

        foreach ($columns as $column) {
            $blockEnd = $reader->position() + $column['byteLength'];

            switch ($column['tag']) {
                case 1:
                    TcbReader::checkColumn($column, 'BeastMove.BeastId', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_STRING]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'BeastMove.BeastId');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameString($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->beastId = $value;
                        }
                    }
                    break;

                case 2:
                    TcbReader::checkColumn($column, 'BeastMove.MoveId', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_I32]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'BeastMove.MoveId');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameI32($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->moveId = $value;
                        }
                    }
                    break;

                case 3:
                    TcbReader::checkColumn($column, 'BeastMove.Power', TcbReader::KIND_SCALAR, false, [TcbReader::ELEMENT_I32, TcbReader::ELEMENT_VARINT]);
                    $cursor = new TcbColumnCursor($reader, $column, $count, 'BeastMove.Power');
                    for ($i = 0; $i < $count; ) {
                        [$n, $value] = $cursor->nextSameI32($count - $i);
                        for (; $n > 0; $n--, $i++) {
                            $records[$i]->power = $value;
                        }
                    }
                    break;

                default:
                    // A column added after this code was generated.
                    $reader->skip($column['byteLength']);
                    break;
            }

            $reader->checkBlockEnd($column, $blockEnd);
        }

        foreach ($records as $record) {
            $byBeastIdAndMoveId[self::keyOfBeastIdAndMoveId($record->beastId, $record->moveId)] = $record;
        }

        // Published, now that every column read.
        $this->records = $records;
        $this->byBeastIdAndMoveId = $byBeastIdAndMoveId;
    }
}
