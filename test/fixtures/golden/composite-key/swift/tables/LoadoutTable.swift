// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import Foundation

// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Loadout : B2
/// One row per stage and slot; neither column is unique on its own.
public final class LoadoutRecord {

    public init() {}

    /// which stage
    public var stage: Int32 = 0

    /// which slot
    public var slot: Slot = Slot.of(0)

    /// anything
    public var power: Int32 = 0

    /// anything
    public var label: String = ""
}

/// Every row of Loadout.
public final class LoadoutTable {

    public init() {}

    /// Every row, in the order the sheet declared them.
    ///
    /// The array a read published, not one a read fills. A refresh replaces the value rather
    /// than appending to it, so whoever holds this holds one whole load.
    public private(set) var records: [LoadoutRecord] = []

    private var byStageAndSlot: [String: LoadoutRecord] = [:]

    /// Joins the columns of the Stage and Slot key into the text the map is keyed by.
    private static func keyOfStageAndSlot(_ stageKey: Int32, _ slotKey: Slot) -> String {
        let parts: [String] = [
            String(stageKey),
            String(slotKey.rawValue),
        ]

        var text = ""

        for part in parts {
            text += "\(part.count):\(part)"
        }

        return text
    }

    /// The row with this Stage and Slot, or nil when the table has none.
    ///
    /// The lookup to reach for when a missing row is an ordinary answer - an optional
    /// reference, a key that came from user input.
    public func findByStageAndSlot(_ stageKey: Int32, _ slotKey: Slot) -> LoadoutRecord? {
        byStageAndSlot[Self.keyOfStageAndSlot(stageKey, slotKey)]
    }

    /// The row with this Stage and Slot, or a thrown error naming what was missing.
    ///
    /// For a key that has to be there - one from another table, or a constant. The name says
    /// it throws, because a caller reading getByStageAndSlotOrThrow(key).name at a
    /// glance cannot otherwise tell whether the next line is a nil check or a catch.
    public func getByStageAndSlotOrThrow(_ stageKey: Int32, _ slotKey: Slot) throws -> LoadoutRecord {
        guard let record = byStageAndSlot[Self.keyOfStageAndSlot(stageKey, slotKey)] else {
            throw RecordNotFoundError(
                "there is no record in table `Loadout` that corresponds to "
                + "field `Stage and Slot` value (\(stageKey), \(slotKey))")
        }

        return record
    }

    /// Whether the table holds a row with this Stage and Slot.
    public func containsStageAndSlot(_ stageKey: Int32, _ slotKey: Slot) -> Bool {
        byStageAndSlot[Self.keyOfStageAndSlot(stageKey, slotKey)] != nil
    }

    /// Loads the table from a .tcb file written by Tabbit.
    ///
    /// Column by column, matched by tag rather than position: a column this build does not
    /// know is skipped by its block length, and one whose type changed incompatibly throws
    /// naming the field.
    ///
    /// The keys are parameters rather than properties of a shared object, because Swift's
    /// concurrency checking makes mutable global state an error and because the accessor
    /// holding them is an instance - which is what lets two loads of two data sets exist at
    /// once. CompositeKeyAccessor passes its own. spec/accessor-instances.md.
    public func read(
        _ filename: String,
        key: [UInt8]? = nil,
        macKey: [UInt8]? = nil,
        verifyMac: Bool = true
    ) throws {
        // Opened unconditionally, and with whatever the caller holds - which is nil unless
        // the project set it. A file that is neither encrypted nor signed comes back from
        // this untouched, so the load path is the same either way and there is no condition
        // here that could be the wrong way round. spec/tcb-mac-and-signature.md.
        let bytes = try Tcb.openTable(
            try Tcb.readAllBytes(filename), key: key, macKey: macKey, verifyMac: verifyMac)

        let reader = Tcb.Reader(bytes)
        let header = try Tcb.readTableHeader(reader)
        let count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is
        // already loaded is a refresh, and one that turns out to be unreadable has to leave
        // the rows already there alone.
        var loaded = [LoadoutRecord]()
        loaded.reserveCapacity(count)

        for _ in 0 ..< count { loaded.append(LoadoutRecord()) }
        var loadedByStageAndSlot = [String: LoadoutRecord](
            minimumCapacity: count)

        for column in header.columns {
            let blockEnd = reader.position + column.byteLength

            switch column.tag {
            case 1:
                try Tcb.checkColumn(column, "Loadout.Stage", Tcb.kindScalar, false, Tcb.elementI32, Tcb.elementVarint)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Loadout.Stage")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].stage = cursor.runSameValue
                        at += 1
                        n -= 1
                    }
                }
            case 2:
                try Tcb.checkColumn(column, "Loadout.Slot", Tcb.kindScalar, false, Tcb.elementVarint)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Loadout.Slot")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].slot = Slot.of(cursor.runSameValue)
                        at += 1
                        n -= 1
                    }
                }
            case 3:
                try Tcb.checkColumn(column, "Loadout.Power", Tcb.kindScalar, false, Tcb.elementI32, Tcb.elementVarint)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Loadout.Power")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].power = cursor.runSameValue
                        at += 1
                        n -= 1
                    }
                }
            case 4:
                try Tcb.checkColumn(column, "Loadout.Label", Tcb.kindScalar, false, Tcb.elementString)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "Loadout.Label")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameString(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].label = cursor.runSameText
                        at += 1
                        n -= 1
                    }
                }
            default:
                // A column added after this code was generated.
                try reader.skip(column.byteLength)
            }

            try Tcb.checkBlockEnd(reader, column, blockEnd)
        }

        for record in loaded {
            loadedByStageAndSlot[Self.keyOfStageAndSlot(record.stage, record.slot)] = record
        }

        // Published, now that every column read.
        records = loaded
        byStageAndSlot = loadedByStageAndSlot
    }
}
