// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package tabbit.fixtures.compositekey

import java.io.File
import tabbit.TcbReader
import tabbit.RecordNotFoundException
import tabbit.Uuid
import tabbit.readAllBytes
import tabbit.open
import tabbit.readTableHeader
import tabbit.checkColumn
import tabbit.checkColumnWithElements
import tabbit.checkBlockEnd
import tabbit.readPresence
import tabbit.readElementPresence
import tabbit.isPresent
import tabbit.TcbException
import tabbit.ColumnCursor
import tabbit.ELEMENT_VARINT
import tabbit.ELEMENT_BOOL
import tabbit.ELEMENT_I32
import tabbit.ELEMENT_I64
import tabbit.ELEMENT_F32
import tabbit.ELEMENT_F64
import tabbit.ELEMENT_STRING
import tabbit.ELEMENT_UUID
import tabbit.KIND_SCALAR
import tabbit.KIND_ARRAY

// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2
/** A composite primary key of strings, beside a single-column secondary one. */
class RouteRecord {
    /** where it starts */
    var from: String = ""
    /** where it ends */
    var to: String = ""
    /** unique on its own */
    var code: String = ""
    /** anything */
    var distance: Int = 0

}

/** Every row of Route. */
class RouteTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<RouteRecord> = ArrayList()
        private set

    private var byCode: HashMap<String, RouteRecord> = HashMap()
    private var byFromAndTo: HashMap<String, RouteRecord> = HashMap()

    /**
     * The row with this Code, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findByCode(key: String): RouteRecord? = byCode[key]

    /**
     * The row with this Code, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByCode(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByCodeOrThrow(key: String): RouteRecord =
        byCode[key] ?: throw RecordNotFoundException(
            "there is no record in table `Route` that corresponds to " +
                "field `Code` value $key")

    /** Whether the table holds a row with this Code. */
    fun containsCode(key: String): Boolean = byCode.containsKey(key)

    /** Joins the columns of the From and To key into the text the map is keyed by. */
    private fun keyOfFromAndTo(fromKey: String, toKey: String): String {
        val parts = arrayOf(
            fromKey,
            toKey,
        )

        val text = StringBuilder()

        for (part in parts) {
            text.append(part.length).append(':').append(part)
        }

        return text.toString()
    }

    /**
     * The row with this From and To, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findByFromAndTo(fromKey: String, toKey: String): RouteRecord? = byFromAndTo[keyOfFromAndTo(fromKey, toKey)]

    /**
     * The row with this From and To, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByFromAndTo(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByFromAndToOrThrow(fromKey: String, toKey: String): RouteRecord =
        byFromAndTo[keyOfFromAndTo(fromKey, toKey)] ?: throw RecordNotFoundException(
            "there is no record in table `Route` that corresponds to " +
                "field `From and To` value ($fromKey, $toKey)")

    /** Whether the table holds a row with this From and To. */
    fun containsFromAndTo(fromKey: String, toKey: String): Boolean = byFromAndTo.containsKey(keyOfFromAndTo(fromKey, toKey))

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        // Opened unconditionally, and with whatever CompositeKeyAccessor.encryptionKey and
        // CompositeKeyAccessor.macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from this untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/tcb-mac-and-signature.md.
        val reader = TcbReader(open(
            readAllBytes(filename), CompositeKeyAccessor.encryptionKey,
            CompositeKeyAccessor.macKey, CompositeKeyAccessor.verifyMac))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<RouteRecord>(count)
        val loadedByCode = HashMap<String, RouteRecord>(count * 2)
        val loadedByFromAndTo = HashMap<String, RouteRecord>(count * 2)

        repeat(count) { loaded.add(RouteRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "Route.From", KIND_SCALAR, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Route.From")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].from = cursor.runSameText
                            at++
                            n--
                        }
                    }
                }
                2 -> {
                    checkColumn(column, "Route.To", KIND_SCALAR, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Route.To")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].to = cursor.runSameText
                            at++
                            n--
                        }
                    }
                }
                3 -> {
                    checkColumn(column, "Route.Code", KIND_SCALAR, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Route.Code")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].code = cursor.runSameText
                            at++
                            n--
                        }
                    }
                }
                4 -> {
                    checkColumn(column, "Route.Distance", KIND_SCALAR, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Route.Distance")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].distance = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedByCode[record.code] = record
            loadedByFromAndTo[keyOfFromAndTo(record.from, record.to)] = record
        }

        // Published, now that every column read.
        records = loaded
        byCode = loadedByCode
        byFromAndTo = loadedByFromAndTo
    }
}
