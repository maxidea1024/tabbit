// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package tabbit.fixtures.compositekey

import java.io.File
import tabbit.TcbReader
import tabbit.RecordNotFoundException
import tabbit.Uuid
import tabbit.readAllBytes
import tabbit.open
import tabbit.readTableHeader
import tabbit.checkColumn
import tabbit.checkColumnWithElements
import tabbit.checkBlockEnd
import tabbit.readPresence
import tabbit.readElementPresence
import tabbit.isPresent
import tabbit.TcbException
import tabbit.ColumnCursor
import tabbit.ELEMENT_VARINT
import tabbit.ELEMENT_BOOL
import tabbit.ELEMENT_I32
import tabbit.ELEMENT_I64
import tabbit.ELEMENT_F32
import tabbit.ELEMENT_F64
import tabbit.ELEMENT_STRING
import tabbit.ELEMENT_UUID
import tabbit.KIND_SCALAR
import tabbit.KIND_ARRAY

// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Loadout : B2
/** One row per stage and slot; neither column is unique on its own. */
class LoadoutRecord {
    /** which stage */
    var stage: Int = 0
    /** which slot */
    var slot: Slot = Slot.of(0)
    /** anything */
    var power: Int = 0
    /** anything */
    var label: String = ""

}

/** Every row of Loadout. */
class LoadoutTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<LoadoutRecord> = ArrayList()
        private set

    private var byStageAndSlot: HashMap<String, LoadoutRecord> = HashMap()

    /** Joins the columns of the Stage and Slot key into the text the map is keyed by. */
    private fun keyOfStageAndSlot(stageKey: Int, slotKey: Slot): String {
        val parts = arrayOf(
            stageKey.toString(),
            slotKey.value.toString(),
        )

        val text = StringBuilder()

        for (part in parts) {
            text.append(part.length).append(':').append(part)
        }

        return text.toString()
    }

    /**
     * The row with this Stage and Slot, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findByStageAndSlot(stageKey: Int, slotKey: Slot): LoadoutRecord? = byStageAndSlot[keyOfStageAndSlot(stageKey, slotKey)]

    /**
     * The row with this Stage and Slot, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByStageAndSlot(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByStageAndSlotOrThrow(stageKey: Int, slotKey: Slot): LoadoutRecord =
        byStageAndSlot[keyOfStageAndSlot(stageKey, slotKey)] ?: throw RecordNotFoundException(
            "there is no record in table `Loadout` that corresponds to " +
                "field `Stage and Slot` value ($stageKey, $slotKey)")

    /** Whether the table holds a row with this Stage and Slot. */
    fun containsStageAndSlot(stageKey: Int, slotKey: Slot): Boolean = byStageAndSlot.containsKey(keyOfStageAndSlot(stageKey, slotKey))

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        // Opened unconditionally, and with whatever CompositeKeyAccessor.encryptionKey and
        // CompositeKeyAccessor.macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from this untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/wire/tcb-mac-and-signature.md.
        val reader = TcbReader(open(
            readAllBytes(filename), CompositeKeyAccessor.encryptionKey,
            CompositeKeyAccessor.macKey, CompositeKeyAccessor.verifyMac))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<LoadoutRecord>(count)
        val loadedByStageAndSlot = HashMap<String, LoadoutRecord>(count * 2)

        repeat(count) { loaded.add(LoadoutRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "Loadout.Stage", KIND_SCALAR, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Stage")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].stage = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                2 -> {
                    checkColumn(column, "Loadout.Slot", KIND_SCALAR, false, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Slot")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].slot = Slot.of(cursor.runSameValue)
                            at++
                            n--
                        }
                    }
                }
                3 -> {
                    checkColumn(column, "Loadout.Power", KIND_SCALAR, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Power")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].power = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                4 -> {
                    checkColumn(column, "Loadout.Label", KIND_SCALAR, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Loadout.Label")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].label = cursor.runSameText
                            at++
                            n--
                        }
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedByStageAndSlot[keyOfStageAndSlot(record.stage, record.slot)] = record
        }

        // Published, now that every column read.
        records = loaded
        byStageAndSlot = loadedByStageAndSlot
    }
}
