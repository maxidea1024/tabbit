// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package tabbit.fixtures.compositekey

import java.io.File
import tabbit.TcbReader
import tabbit.RecordNotFoundException
import tabbit.Uuid
import tabbit.readAllBytes
import tabbit.open
import tabbit.readTableHeader
import tabbit.checkColumn
import tabbit.checkColumnWithElements
import tabbit.checkBlockEnd
import tabbit.readPresence
import tabbit.readElementPresence
import tabbit.isPresent
import tabbit.TcbException
import tabbit.ColumnCursor
import tabbit.ELEMENT_VARINT
import tabbit.ELEMENT_BOOL
import tabbit.ELEMENT_I32
import tabbit.ELEMENT_I64
import tabbit.ELEMENT_F32
import tabbit.ELEMENT_F64
import tabbit.ELEMENT_STRING
import tabbit.ELEMENT_UUID
import tabbit.KIND_SCALAR
import tabbit.KIND_ARRAY

// Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : N2
/** One row per pair; the key is both references taken together. */
class BeastMoveRecord {
    /** a string-keyed target */
    var beastId: String = ""
    var beastByBeastId: BeastRecord? = null
    /** an int-keyed target */
    var moveId: Int = 0
    var moveByMoveId: MoveRecord? = null
    /** anything */
    var power: Int = 0

}

/** Every row of BeastMove. */
class BeastMoveTable : Iterable<BeastMoveRecord> {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<BeastMoveRecord> = ArrayList()
        private set

    /** How many rows the table holds. */
    val size: Int
        get() = records.size

    /**
     * The rows, in the order the file wrote them.
     *
     * The list reference is read once, here. A refresh replaces the reference rather than
     * its contents, so a loop already running keeps the rows it started with.
     */
    override fun iterator(): Iterator<BeastMoveRecord> = records.iterator()

    private var byBeastIdAndMoveId: HashMap<String, BeastMoveRecord> = HashMap()

    /** Joins the columns of the BeastId and MoveId key into the text the map is keyed by. */
    private fun keyOfBeastIdAndMoveId(beastIdKey: String, moveIdKey: Int): String {
        val parts = arrayOf(
            beastIdKey,
            moveIdKey.toString(),
        )

        val text = StringBuilder()

        for (part in parts) {
            text.append(part.length).append(':').append(part)
        }

        return text.toString()
    }

    /**
     * The row with this BeastId and MoveId, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findByBeastIdAndMoveId(beastIdKey: String, moveIdKey: Int): BeastMoveRecord? = byBeastIdAndMoveId[keyOfBeastIdAndMoveId(beastIdKey, moveIdKey)]

    /**
     * The row with this BeastId and MoveId, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByBeastIdAndMoveId(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByBeastIdAndMoveIdOrThrow(beastIdKey: String, moveIdKey: Int): BeastMoveRecord =
        byBeastIdAndMoveId[keyOfBeastIdAndMoveId(beastIdKey, moveIdKey)] ?: throw RecordNotFoundException(
            "there is no record in table `BeastMove` that corresponds to " +
                "field `BeastId and MoveId` value ($beastIdKey, $moveIdKey)")

    /** Whether the table holds a row with this BeastId and MoveId. */
    fun containsBeastIdAndMoveId(beastIdKey: String, moveIdKey: Int): Boolean = byBeastIdAndMoveId.containsKey(keyOfBeastIdAndMoveId(beastIdKey, moveIdKey))


    /**
     * The row with this BeastId and MoveId, or null when the table has none.
     *
     * findByBeastIdAndMoveId under the spelling Kotlin uses for a keyed collection. It
     * answers with null because that is what Map does here, and the type says so - a
     * language whose own map throws generates a subscript that throws.
     * spec/targets/table-collection-surface.md section 5.7.
     *
     * This is the key, not the row's position. `table[0]` is the row whose
     * BeastId and MoveId is 0, not the first row - the rows in order are `records`.
     */
    operator fun get(beastIdKey: String, moveIdKey: Int): BeastMoveRecord? = findByBeastIdAndMoveId(beastIdKey, moveIdKey)

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        // Opened unconditionally, and with whatever CompositeKeyAccessor.encryptionKey and
        // CompositeKeyAccessor.macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from this untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/wire/tcb-mac-and-signature.md.
        val reader = TcbReader(open(
            readAllBytes(filename), CompositeKeyAccessor.encryptionKey,
            CompositeKeyAccessor.macKey, CompositeKeyAccessor.verifyMac))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<BeastMoveRecord>(count)
        val loadedByBeastIdAndMoveId = HashMap<String, BeastMoveRecord>(count * 2)

        repeat(count) { loaded.add(BeastMoveRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "BeastMove.BeastId", KIND_SCALAR, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "BeastMove.BeastId")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].beastId = cursor.runSameText
                            at++
                            n--
                        }
                    }
                }
                2 -> {
                    checkColumn(column, "BeastMove.MoveId", KIND_SCALAR, false, ELEMENT_I32)
                    val cursor = ColumnCursor(reader, column, count, "BeastMove.MoveId")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].moveId = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                3 -> {
                    checkColumn(column, "BeastMove.Power", KIND_SCALAR, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "BeastMove.Power")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].power = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedByBeastIdAndMoveId[keyOfBeastIdAndMoveId(record.beastId, record.moveId)] = record
        }

        // Published, now that every column read.
        records = loaded
        byBeastIdAndMoveId = loadedByBeastIdAndMoveId
    }
}
