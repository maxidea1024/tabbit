# ------------------------------------------------------------------------------
# Generated by Tabbit. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

require_relative '../tabbit/tcb_reader'


module CompositeKey
  # Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Route : B2
  # A composite primary key of strings, beside a single-column secondary one.
  class RouteRecord
    attr_accessor :from, :to, :code, :distance

    def initialize
      @from = ''
      @to = ''
      @code = ''
      @distance = 0
    end

  end

  # Every row of Route.
  class RouteTable
    attr_accessor :records

    def initialize
      @records = []
      @by_code = {}
      @by_from_and_to = {}
    end

    # The row with this Code, or nil when the table has none.
    #
    # The lookup to reach for when a missing row is an ordinary answer - an optional
    # reference, a key that came from user input.
    def find_by_code(key)
      @by_code[key]
    end

    # The row with this Code, or a raised error naming what was missing.
    #
    # For a key that has to be there - one from another table, or a constant. The name
    # says it raises, because a caller reading `get_by_code(key).name` at a
    # glance cannot otherwise tell whether the next line is a check or a rescue.
    def get_by_code_or_throw(key)
      record = @by_code[key]

      if record.nil?
        raise Tabbit::RecordNotFoundError,
              "there is no record in table `Route` that corresponds to " \
              "field `Code` value #{key.inspect}"
      end

      record
    end

    # Whether the table holds a row with this Code.
    def contains_code?(key)
      @by_code.key?(key)
    end

    # Joins the columns of the From and To key into the text the map is keyed by.
    def self.key_of_from_and_to(from_key, to_key)
      parts = [
        from_key,
        to_key,
      ]

      parts.map { |part| "#{part.length}:#{part}" }.join
    end

    # The row with this From and To, or nil when the table has none.
    #
    # The lookup to reach for when a missing row is an ordinary answer - an optional
    # reference, a key that came from user input.
    def find_by_from_and_to(from_key, to_key)
      @by_from_and_to[self.class.key_of_from_and_to(from_key, to_key)]
    end

    # The row with this From and To, or a raised error naming what was missing.
    #
    # For a key that has to be there - one from another table, or a constant. The name
    # says it raises, because a caller reading `get_by_from_and_to(key).name` at a
    # glance cannot otherwise tell whether the next line is a check or a rescue.
    def get_by_from_and_to_or_throw(from_key, to_key)
      record = @by_from_and_to[self.class.key_of_from_and_to(from_key, to_key)]

      if record.nil?
        raise Tabbit::RecordNotFoundError,
              "there is no record in table `Route` that corresponds to " \
              "field `From and To` value (#{from_key.inspect}, #{to_key.inspect})"
      end

      record
    end

    # Whether the table holds a row with this From and To.
    def contains_from_and_to?(from_key, to_key)
      @by_from_and_to.key?(self.class.key_of_from_and_to(from_key, to_key))
    end

    # Loads the table from a .tcb file written by Tabbit.
    # Column by column, matched by tag rather than position: a column this build does
    # not know is skipped by its block length, and one whose type changed incompatibly
    # fails naming the field.
    def read(filename)
      # Opened unconditionally, and with whatever Tables.encryption_key and Tables.mac_key
      # hold - which is nil unless the project set them. A file that is neither encrypted
      # nor signed comes back from this untouched, so the load path is the same either way
      # and there is no condition here that could be the wrong way round.
      # spec/wire/tcb-mac-and-signature.md.
      reader = Tabbit::Reader.new(
        Tabbit.open(Tabbit.read_all_bytes(filename), Tables.encryption_key,
                      Tables.mac_key, verify_mac: Tables.verify_mac)
      )
      count, columns = Tabbit.read_table_header(reader)

      # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
      records = Array.new(count) { RouteRecord.new }

      columns.each do |column|
        block_end = reader.position + column.byte_length

        case column.tag
        when 1
          Tabbit.check_column(column, 'Route.From', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_STRING])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Route.From')
          at = 0
          while at < count
            n, value = cursor.next_same_string(count - at)
            (at...(at + n)).each do |i|
              records[i].from = value
            end
            at += n
          end
        when 2
          Tabbit.check_column(column, 'Route.To', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_STRING])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Route.To')
          at = 0
          while at < count
            n, value = cursor.next_same_string(count - at)
            (at...(at + n)).each do |i|
              records[i].to = value
            end
            at += n
          end
        when 3
          Tabbit.check_column(column, 'Route.Code', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_STRING])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Route.Code')
          at = 0
          while at < count
            n, value = cursor.next_same_string(count - at)
            (at...(at + n)).each do |i|
              records[i].code = value
            end
            at += n
          end
        when 4
          Tabbit.check_column(column, 'Route.Distance', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_I32, Tabbit::ELEMENT_VARINT])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Route.Distance')
          at = 0
          while at < count
            n, value = cursor.next_same_i32(count - at)
            (at...(at + n)).each do |i|
              records[i].distance = value
            end
            at += n
          end
        else
          # A column added after this code was generated.
          reader.skip(column.byte_length)
        end

        Tabbit.check_block_end(reader, column, block_end)
      end

      by_code = records.to_h { |record| [record.code, record] }
      by_from_and_to = records.to_h do |record|
        [self.class.key_of_from_and_to(record.from, record.to), record]
      end

      # Published, now that every column read.
      @records = records
      @by_code = by_code
      @by_from_and_to = by_from_and_to
    end
  end
end
