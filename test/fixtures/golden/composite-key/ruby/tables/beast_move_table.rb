# ------------------------------------------------------------------------------
# Generated by Tabbit. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

require_relative '../tabbit/tcb_reader'


module CompositeKey
  # Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Link : N2
  # One row per pair; the key is both references taken together.
  class BeastMoveRecord
    attr_accessor :beast_id, :beast_by_beast_id, :move_id, :move_by_move_id, :power

    def initialize
      @beast_id = 0
      @beast_by_beast_id = nil
      @move_id = 0
      @move_by_move_id = nil
      @power = 0
    end

  end

  # Every row of BeastMove.
  class BeastMoveTable
    attr_accessor :records

    def initialize
      @records = []
      @by_beast_id_and_move_id = {}
    end

    # Joins the columns of the BeastId and MoveId key into the text the map is keyed by.
    def self.key_of_beast_id_and_move_id(beast_id_key, move_id_key)
      parts = [
        beast_id_key,
        move_id_key.to_i.to_s,
      ]

      parts.map { |part| "#{part.length}:#{part}" }.join
    end

    # The row with this BeastId and MoveId, or nil when the table has none.
    #
    # The lookup to reach for when a missing row is an ordinary answer - an optional
    # reference, a key that came from user input.
    def find_by_beast_id_and_move_id(beast_id_key, move_id_key)
      @by_beast_id_and_move_id[self.class.key_of_beast_id_and_move_id(beast_id_key, move_id_key)]
    end

    # The row with this BeastId and MoveId, or a raised error naming what was missing.
    #
    # For a key that has to be there - one from another table, or a constant. The name
    # says it raises, because a caller reading `get_by_beast_id_and_move_id(key).name` at a
    # glance cannot otherwise tell whether the next line is a check or a rescue.
    def get_by_beast_id_and_move_id_or_throw(beast_id_key, move_id_key)
      record = @by_beast_id_and_move_id[self.class.key_of_beast_id_and_move_id(beast_id_key, move_id_key)]

      if record.nil?
        raise Tabbit::RecordNotFoundError,
              "there is no record in table `BeastMove` that corresponds to " \
              "field `BeastId and MoveId` value (#{beast_id_key.inspect}, #{move_id_key.inspect})"
      end

      record
    end

    # Whether the table holds a row with this BeastId and MoveId.
    def contains_beast_id_and_move_id?(beast_id_key, move_id_key)
      @by_beast_id_and_move_id.key?(self.class.key_of_beast_id_and_move_id(beast_id_key, move_id_key))
    end

    # Loads the table from a .tcb file written by Tabbit.
    # Column by column, matched by tag rather than position: a column this build does
    # not know is skipped by its block length, and one whose type changed incompatibly
    # fails naming the field.
    def read(filename)
      # Opened unconditionally, and with whatever Tables.encryption_key and Tables.mac_key
      # hold - which is nil unless the project set them. A file that is neither encrypted
      # nor signed comes back from this untouched, so the load path is the same either way
      # and there is no condition here that could be the wrong way round.
      # spec/wire/tcb-mac-and-signature.md.
      reader = Tabbit::Reader.new(
        Tabbit.open(Tabbit.read_all_bytes(filename), Tables.encryption_key,
                      Tables.mac_key, verify_mac: Tables.verify_mac)
      )
      count, columns = Tabbit.read_table_header(reader)

      # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
      records = Array.new(count) { BeastMoveRecord.new }

      columns.each do |column|
        block_end = reader.position + column.byte_length

        case column.tag
        when 1
          Tabbit.check_column(column, 'BeastMove.BeastId', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_STRING])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'BeastMove.BeastId')
          at = 0
          while at < count
            n, value = cursor.next_same_string(count - at)
            (at...(at + n)).each do |i|
              records[i].beast_id = value
            end
            at += n
          end
        when 2
          Tabbit.check_column(column, 'BeastMove.MoveId', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_I32])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'BeastMove.MoveId')
          at = 0
          while at < count
            n, value = cursor.next_same_i32(count - at)
            (at...(at + n)).each do |i|
              records[i].move_id = value
            end
            at += n
          end
        when 3
          Tabbit.check_column(column, 'BeastMove.Power', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_I32, Tabbit::ELEMENT_VARINT])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'BeastMove.Power')
          at = 0
          while at < count
            n, value = cursor.next_same_i32(count - at)
            (at...(at + n)).each do |i|
              records[i].power = value
            end
            at += n
          end
        else
          # A column added after this code was generated.
          reader.skip(column.byte_length)
        end

        Tabbit.check_block_end(reader, column, block_end)
      end

      by_beast_id_and_move_id = records.to_h do |record|
        [self.class.key_of_beast_id_and_move_id(record.beast_id, record.move_id), record]
      end

      # Published, now that every column read.
      @records = records
      @by_beast_id_and_move_id = by_beast_id_and_move_id
    end
  end
end
