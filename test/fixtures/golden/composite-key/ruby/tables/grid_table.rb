# ------------------------------------------------------------------------------
# Generated by Tabbit. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

require_relative '../tabbit/tcb_reader'


module CompositeKey
  # Generated from test/fixtures/xlsx/composite-key/composite-key.xlsx : Grid : B2
  # Three columns taken together, so two is not the only width that works.
  class GridRecord
    attr_accessor :x, :y, :z, :name

    def initialize
      @x = 0
      @y = 0
      @z = ''
      @name = ''
    end

  end

  # Every row of Grid.
  class GridTable
    include Enumerable

    attr_accessor :records

    # How many rows the table holds.
    def size
      @records.size
    end

    # The rows, in the order the file wrote them. Including Enumerable on top of this
    # is what gives the table `map` - `select` - `find` - `sort_by` without any of them
    # being written here.
    #
    # The array reference is read once, here. A refresh replaces the reference rather
    # than its contents, so a loop already running keeps the rows it started with.
    def each(&block)
      @records.each(&block)
    end

    def initialize
      @records = []
      @by_x_and_y_and_z = {}
    end

    # Joins the columns of the X and Y and Z key into the text the map is keyed by.
    def self.key_of_x_and_y_and_z(x_key, y_key, z_key)
      parts = [
        x_key.to_i.to_s,
        y_key.to_i.to_s,
        z_key,
      ]

      parts.map { |part| "#{part.length}:#{part}" }.join
    end

    # The row with this X and Y and Z, or nil when the table has none.
    #
    # The lookup to reach for when a missing row is an ordinary answer - an optional
    # reference, a key that came from user input.
    def find_by_x_and_y_and_z(x_key, y_key, z_key)
      @by_x_and_y_and_z[self.class.key_of_x_and_y_and_z(x_key, y_key, z_key)]
    end

    # The row with this X and Y and Z, or a raised error naming what was missing.
    #
    # For a key that has to be there - one from another table, or a constant. The name
    # says it raises, because a caller reading `get_by_x_and_y_and_z(key).name` at a
    # glance cannot otherwise tell whether the next line is a check or a rescue.
    def get_by_x_and_y_and_z_or_throw(x_key, y_key, z_key)
      record = @by_x_and_y_and_z[self.class.key_of_x_and_y_and_z(x_key, y_key, z_key)]

      if record.nil?
        raise Tabbit::RecordNotFoundError,
              "there is no record in table `Grid` that corresponds to " \
              "field `X and Y and Z` value (#{x_key.inspect}, #{y_key.inspect}, #{z_key.inspect})"
      end

      record
    end

    # Whether the table holds a row with this X and Y and Z.
    def contains_x_and_y_and_z?(x_key, y_key, z_key)
      @by_x_and_y_and_z.key?(self.class.key_of_x_and_y_and_z(x_key, y_key, z_key))
    end


    # The row with this X and Y and Z, or nil when the table has none.
    #
    # find_by_x_and_y_and_z under the spelling Ruby uses for a keyed collection. It
    # answers with nil because that is what Hash does here; a language whose own map raises
    # generates a subscript that raises.
    # spec/targets/table-collection-surface.md section 5.7.
    #
    # This is the key, not the row's position. `table[0]` is the row whose
    # X and Y and Z is 0, not the first row - the rows in order are `records`.
    def [](x_key, y_key, z_key)
      find_by_x_and_y_and_z(x_key, y_key, z_key)
    end

    # Loads the table from a .tcb file written by Tabbit.
    # Column by column, matched by tag rather than position: a column this build does
    # not know is skipped by its block length, and one whose type changed incompatibly
    # fails naming the field.
    def read(filename)
      # Opened unconditionally, and with whatever Tables.encryption_key and Tables.mac_key
      # hold - which is nil unless the project set them. A file that is neither encrypted
      # nor signed comes back from this untouched, so the load path is the same either way
      # and there is no condition here that could be the wrong way round.
      # spec/wire/tcb-mac-and-signature.md.
      reader = Tabbit::Reader.new(
        Tabbit.open(Tabbit.read_all_bytes(filename), Tables.encryption_key,
                      Tables.mac_key, verify_mac: Tables.verify_mac)
      )
      count, columns = Tabbit.read_table_header(reader)

      # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
      records = Array.new(count) { GridRecord.new }

      columns.each do |column|
        block_end = reader.position + column.byte_length

        case column.tag
        when 1
          Tabbit.check_column(column, 'Grid.X', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_I32, Tabbit::ELEMENT_VARINT])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Grid.X')
          at = 0
          while at < count
            n, value = cursor.next_same_i32(count - at)
            (at...(at + n)).each do |i|
              records[i].x = value
            end
            at += n
          end
        when 2
          Tabbit.check_column(column, 'Grid.Y', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_I32, Tabbit::ELEMENT_VARINT])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Grid.Y')
          at = 0
          while at < count
            n, value = cursor.next_same_i32(count - at)
            (at...(at + n)).each do |i|
              records[i].y = value
            end
            at += n
          end
        when 3
          Tabbit.check_column(column, 'Grid.Z', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_STRING])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Grid.Z')
          at = 0
          while at < count
            n, value = cursor.next_same_string(count - at)
            (at...(at + n)).each do |i|
              records[i].z = value
            end
            at += n
          end
        when 4
          Tabbit.check_column(column, 'Grid.Name', Tabbit::KIND_SCALAR, false, [Tabbit::ELEMENT_STRING])
          cursor = Tabbit::ColumnCursor.new(reader, column, count, 'Grid.Name')
          at = 0
          while at < count
            n, value = cursor.next_same_string(count - at)
            (at...(at + n)).each do |i|
              records[i].name = value
            end
            at += n
          end
        else
          # A column added after this code was generated.
          reader.skip(column.byte_length)
        end

        Tabbit.check_block_end(reader, column, block_end)
      end

      by_x_and_y_and_z = records.to_h do |record|
        [self.class.key_of_x_and_y_and_z(record.x, record.y, record.z), record]
      end

      # Published, now that every column read.
      @records = records
      @by_x_and_y_and_z = by_x_and_y_and_z
    end
  end
end
