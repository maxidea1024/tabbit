/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RECORD_REF_KIT_H
#define RECORD_REF_KIT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "RecordRef_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/record-ref-trim/record-ref-trim.xlsx : Records : B2
 *
 * A trimmed record array whose first member is a reference.
 */
/* One element of RecordRef_KitRecord_t::part. */
struct RecordRef_KitRecord_t_part_entry {
  /* element 1, the reference */
  const RecordRef_ItemRecord_t* item_id; int32_t item_id_index;
  /* element 1, an ordinary member */
  int32_t count;
};

struct RecordRef_KitRecord_t {
  /* primary index */
  int32_t index;
  /* element 1, the reference */
  struct RecordRef_KitRecord_t_part_entry* part;
  int32_t part_count;
};

/* Every row of Kit.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct RecordRef_KitTable_t {
  RecordRef_KitRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} RecordRef_KitTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool RecordRef_KitLoad(RecordRef_KitTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void RecordRef_KitFree(RecordRef_KitTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const RecordRef_KitRecord_t* RecordRef_KitFindByIndex(
  const RecordRef_KitTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool RecordRef_KitContainsIndex(
  const RecordRef_KitTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RECORD_REF_KIT_H */
