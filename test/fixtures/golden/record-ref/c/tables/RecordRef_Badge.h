/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RECORD_REF_BADGE_H
#define RECORD_REF_BADGE_H

#include "tabbit/tabbit_tcb_reader.h"

#include "RecordRef_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/record-ref/record-ref.xlsx : Records : BN2
 *
 * A record of one whose members' keys are not numbers.
 */
/* One element of RecordRef_BadgeRecord_t::mark. */
struct RecordRef_BadgeRecord_t_mark_entry {
  /* a string key, on the run path */
  const char* clip_id; const RecordRef_ClipRecord_t* clip_by_clip_id;
  /* a uuid key, on the run path */
  tb_uuid seal_id; const RecordRef_SealRecord_t* seal_by_seal_id;
  /* an ordinary member beside them */
  int32_t rank;
};

struct RecordRef_BadgeRecord_t {
  /* primary index */
  int32_t index;
  /* a string key, on the run path */
  struct RecordRef_BadgeRecord_t_mark_entry mark;
  /* padding, so every table is one width */
  int32_t pad;
};

/* Every row of Badge.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct RecordRef_BadgeTable_t {
  RecordRef_BadgeRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} RecordRef_BadgeTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool RecordRef_BadgeLoad(RecordRef_BadgeTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void RecordRef_BadgeFree(RecordRef_BadgeTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const RecordRef_BadgeRecord_t* RecordRef_BadgeFindByIndex(
  const RecordRef_BadgeTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool RecordRef_BadgeContainsIndex(
  const RecordRef_BadgeTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RECORD_REF_BADGE_H */
