/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RECORD_REF_MOUNT_H
#define RECORD_REF_MOUNT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "RecordRef_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/record-ref/record-ref.xlsx : Records : AH2
 *
 * A reference inside a record inside a record.
 */
/* A record inside RecordRef_MountRecord_t::rig. */
struct RecordRef_MountRecord_t_rig_entryCore {
  /* element 1, two levels in */
  const RecordRef_ItemRecord_t* item_id; int32_t item_id_index;
  /* its sibling at that level */
  int32_t count;
};

/* One element of RecordRef_MountRecord_t::rig. */
struct RecordRef_MountRecord_t_rig_entry {
  /* element 1, two levels in */
  struct RecordRef_MountRecord_t_rig_entryCore core;
};

struct RecordRef_MountRecord_t {
  /* primary index */
  int32_t index;
  /* element 1, two levels in */
  struct RecordRef_MountRecord_t_rig_entry rig[2];
};

/* Every row of Mount.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct RecordRef_MountTable_t {
  RecordRef_MountRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} RecordRef_MountTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool RecordRef_MountLoad(RecordRef_MountTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void RecordRef_MountFree(RecordRef_MountTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const RecordRef_MountRecord_t* RecordRef_MountFindByIndex(
  const RecordRef_MountTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool RecordRef_MountContainsIndex(
  const RecordRef_MountTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RECORD_REF_MOUNT_H */
