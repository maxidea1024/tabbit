/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RECORD_REF_H
#define RECORD_REF_H

#include "tables/RecordRef_Item.h"
#include "tables/RecordRef_Loadout.h"
#include "tables/RecordRef_Holder.h"
#include "tables/RecordRef_Bag.h"
#include "tables/RecordRef_Mount.h"
#include "tables/RecordRef_Clip.h"
#include "tables/RecordRef_Pose.h"
#include "tables/RecordRef_Seal.h"
#include "tables/RecordRef_Badge.h"
#include "tables/RecordRef_Kit.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Every table, loaded together so cross-table references can be resolved. */
typedef struct RecordRef_t {
  RecordRef_ItemTable_t item;
  RecordRef_LoadoutTable_t loadout;
  RecordRef_HolderTable_t holder;
  RecordRef_BagTable_t bag;
  RecordRef_MountTable_t mount;
  RecordRef_ClipTable_t clip;
  RecordRef_PoseTable_t pose;
  RecordRef_SealTable_t seal;
  RecordRef_BadgeTable_t badge;
  RecordRef_KitTable_t kit;
} RecordRef_t;

/* Reads every table from base_path, then links the references between them.
 *
 * Declare the struct zeroed - `RecordRef_t data = {0};` - and this is safe to
 * call again on it afterwards. That is what a reload is: every file is read beside what the
 * struct already holds and swapped in at the end, so the pointers a caller is holding stay
 * valid until this returns true, and stay valid for the previous load if it returns false.
 *
 * Returns false if any table could not be read, having released whatever had been loaded up
 * to that point and left `data` alone - a half-loaded model is not one to hand back. */
bool RecordRef_LoadAll(RecordRef_t* data, const char* base_path,
                 char* error, size_t error_size);

/* The same, for data files carrying some other extension than the one the recipe told
 * the exporter to write. `file_extension` includes the dot. */
bool RecordRef_LoadAllWithExtension(RecordRef_t* data, const char* base_path,
                 const char* file_extension, char* error, size_t error_size);

void RecordRef_Free(RecordRef_t* data);

#ifdef __cplusplus
}
#endif

#endif /* RECORD_REF_H */
