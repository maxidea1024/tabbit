-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")

---@class PoseStepEntry
---@field clipId string
---@field clipByClipId ClipRecord|nil
---@field weight integer
local PoseStepEntryMeta = tcb.strictType("an element of PoseRecord.step", { "clipId", "clipByClipId", "weight" })

---@return PoseStepEntry
local function newPoseStepEntry()
  return setmetatable({
    clipId = "",
    weight = 0,
  }, PoseStepEntryMeta)
end

-- Generated from test/fixtures/xlsx/record-ref/record-ref.xlsx : Records : AX2.
-- A record member pointing at a table keyed by a string.
---@class PoseRecord
---@field index integer
---@field step PoseStepEntry[]
local PoseRecordMeta = tcb.strictType("a `Pose` row", { "index", "step" })

---@return PoseRecord
local function newPoseRecord()
  return setmetatable({
    index = 0,
    step = tcb.filledArray(2, newPoseStepEntry),
  }, PoseRecordMeta)
end

---@class PoseTable
---@field records PoseRecord[]
local PoseTable = {}

local PoseTableMeta = tcb.strictInstance("the `Pose` table", PoseTable, { "records", "byIndex" })

function PoseTable.new()
  return setmetatable({
    records = {},
    byIndex = {},
  }, PoseTableMeta)
end

-- The row with this Index, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function PoseTable:findByIndex(key)
  return self.byIndex[key]
end

-- The row with this Index, or a raised error naming what was missing -
-- for a key that has to be there.
function PoseTable:getByIndexOrThrow(key)
  local record = self:findByIndex(key)

  if record == nil then
    tcb.fail("there is no record in table `Pose` that corresponds to " ..
      "field `Index` value %s", tostring(key))
  end

  return record
end

-- Whether the table holds a row with this Index.
function PoseTable:containsIndex(key)
  return self:findByIndex(key) ~= nil
end

-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function PoseTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newPoseRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "Pose.Index", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Pose.Index")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].index = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "Pose.Step.ClipId", tcb.KIND_ARRAY, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "Pose.Step.ClipId")
      for i = 1, count do
        local record = records[i]
        local elementCount = cursor:nextLength()
        -- The first member builds the list; the rest check. Building it again would
        -- discard what the members before it wrote, and taking the shorter of two
        -- counts would shift every value after it.
        record.step = tcb.filledArray(elementCount, newPoseStepEntry)

        for element = 1, elementCount do
          record.step[element].clipId = cursor:nextString()
        end
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "Pose.Step.Weight", tcb.KIND_ARRAY, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "Pose.Step.Weight")
      for i = 1, count do
        local record = records[i]
        local elementCount = cursor:nextLength()
        if #record.step ~= elementCount then
          tcb.fail("Pose: the file gives one member of this record a " ..
            "different element count than another")
        end

        for element = 1, elementCount do
          record.step[element].weight = cursor:nextI32()
        end
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byIndex = {}

  for i = 1, #records do
    byIndex[records[i].index] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byIndex = byIndex
end

-- The same load from a file on disk.
function PoseTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return PoseTable
