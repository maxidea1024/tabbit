// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package tabbit.fixtures.docshowcase;

/**
 * One value of {@code Effect}, whichever shape it took.
 *
 * <p>Narrow it with {@code instanceof}. Every variant is nested here, so the set is what this
 * file holds.
 */
public abstract class Effect {
    /** How likely it is to land, in percent. Every variant carries it, so it is one column */
    /** and every row fills it. */
    public int chance;

    /**
     * One shape of {@code Effect}.
     *
     * <p>The file carries {@code 1} for it. That number rather than
     * the name is what lets this variant be renamed without a deployed reader reading a
     * different one.
     */
    public static final class DamageEffect extends Effect {
        /** How much it takes. */
    public int damage;
    }

    /**
     * One shape of {@code Effect}.
     *
     * <p>The file carries {@code 2} for it. That number rather than
     * the name is what lets this variant be renamed without a deployed reader reading a
     * different one.
     */
    public static final class HealEffect extends Effect {
        /** How much it gives. */
    public int amount;
    }

    /**
     * One shape of {@code Effect}.
     *
     * <p>The file carries {@code 3} for it. That number rather than
     * the name is what lets this variant be renamed without a deployed reader reading a
     * different one.
     */
    public static final class NoEffect extends Effect {
    }
}
