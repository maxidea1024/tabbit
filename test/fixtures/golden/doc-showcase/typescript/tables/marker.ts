// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { Tables } from '../tables'

/** One element of MarkerRecord.pos. */
export interface PosEntry {
  /** three floats */
  x: number
  /** three floats */
  y: number
  /** three floats */
  z: number
}

/** PosEntry as the .json export carries it. */
interface PosEntryJson {
  x: number
  y: number
  z: number
}

/** One element of MarkerRecord.cell. */
export interface CellEntry {
  /** two ints */
  x: number
  /** two ints */
  y: number
}

/** CellEntry as the .json export carries it. */
interface CellEntryJson {
  x: number
  y: number
}

/** One element of MarkerRecord.tint. */
export interface TintEntry {
  /** four bytes */
  r: number
  /** four bytes */
  g: number
  /** four bytes */
  b: number
  /** four bytes */
  a: number
}

/** TintEntry as the .json export carries it. */
interface TintEntryJson {
  r: number
  g: number
  b: number
  a: number
}

/** A type for handling rows when parsing .json. */
interface IDataRow {
  index: number
  pos: PosEntryJson
  cell: CellEntryJson
  tint: TintEntryJson
}

// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Types : M2
/** Values written as one cell and read as several. */
export class MarkerRecord {
  /** Default constructor */
  constructor() {
  }

  /** primary index */
  public get index(): number { return this._index }

  /** three floats */
  public get pos(): PosEntry { return this._pos }

  /** two ints */
  public get cell(): CellEntry { return this._cell }

  /** four bytes */
  public get tint(): TintEntry { return this._tint }

  public _index: number = 0
  public _pos: PosEntry = { x: 0, y: 0, z: 0 }
  public _cell: CellEntry = { x: 0, y: 0 }
  public _tint: TintEntry = { r: 0, g: 0, b: 0, a: 0 }

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._index = dataRow.index
    this._pos = ((e: any) => ({ x: Math.fround(e.x), y: Math.fround(e.y), z: Math.fround(e.z) }))(dataRow.pos)
    this._cell = ((e: any) => ({ x: e.x, y: e.y }))(dataRow.cell)
    this._tint = ((e: any) => ({ r: e.r, g: e.g, b: e.b, a: e.a }))(dataRow.tint)
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._index = dataRow[offset++]
    this._pos = { x: Math.fround(dataRow[offset++]), y: Math.fround(dataRow[offset++]), z: Math.fround(dataRow[offset++]) }
    this._cell = { x: dataRow[offset++], y: dataRow[offset++] }
    this._tint = { r: dataRow[offset++], g: dataRow[offset++], b: dataRow[offset++], a: dataRow[offset++] }
  }
}

// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Types : M2
/** Values written as one cell and read as several. */
export class MarkerTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): MarkerRecord[] { return this._records }
  private _records: MarkerRecord[] = []

  /** How many rows the table holds. */
  public get count(): number { return this._records.length }

  /**
   * The rows, in the order the file wrote them.
   *
   * The array reference is read once, here. A refresh replaces the reference rather
   * than its contents, so a loop already running keeps the rows it started with.
   */
  public [Symbol.iterator](): IterableIterator<MarkerRecord> {
    return this._records[Symbol.iterator]()
  }

  // Indexing by 'index'
  public get recordsByIndex(): Map<number, MarkerRecord> { return this._recordsByIndex }
  private _recordsByIndex: Map<number, MarkerRecord> = new Map<number, MarkerRecord>()

  /**
   * The row with this index, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByIndex(key: number): MarkerRecord | undefined {
    return this._recordsByIndex.get(key)
  }

  /**
   * The row with this index, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByIndex(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIndexOrThrow(key: number): MarkerRecord {
    const found = this._recordsByIndex.get(key)
    if (!found)
      throw new Error(`There is no record in table "Marker" that corresponds to field "index" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this index. */
  public containsIndex(key: number): boolean {
    return this._recordsByIndex.has(key)
  }


  /**
   * Each row with the index it is keyed by -
   * `for (const [key, row] of table.entries())`.
   *
   * What this saves a caller is not the key value - the row carries it - but having to know
   * which column the key is: index here, something else in the next table.
   *
   * The rows come in the order the file wrote them rather than the order the map holds
   * them, which makes this and iterating the table agree. Only the primary key has this: a
   * table keyed by several columns together has no single key value to pair a row with.
   */
  public *entries(): IterableIterator<[number, MarkerRecord]> {
    for (const record of this._records) {
      yield [record.index, record]
    }
  }

  /**
   * Read a table from the text of its JSON export.
   *
   * The text, not a path: where it comes from - a file, a fetch, an asset bundle - is the
   * caller's, so this module names no runtime module and loads wherever TypeScript runs.
   * Either row shape the exporter writes is accepted.
   */
  public readJsonFrom(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: MarkerRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new MarkerRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new MarkerRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /**
   * Read a table from the bytes of its binary export.
   *
   * The bytes as the file holds them; the envelope is opened here. Unconditionally, and with
   * whatever `Tables.encryptionKey` and `Tables.macKey` hold - which is null
   * unless the project set them. A file that is neither encrypted nor signed comes back from
   * that untouched, so the load path is the same either way and there is no condition here
   * that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
   *
   * Then column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(fileBytes: Uint8Array): void {
    const data = tabbit.open(
      fileBytes, Tables.encryptionKey, Tables.macKey, Tables.verifyMac)
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: MarkerRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new MarkerRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'Marker.Index', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Index')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._index = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'Marker.Pos.X', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Pos.X')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._pos.x = cursor.nextF32()
          }
          break
        case 3:
          tabbit.checkColumn(column, 'Marker.Pos.Y', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Pos.Y')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._pos.y = cursor.nextF32()
          }
          break
        case 4:
          tabbit.checkColumn(column, 'Marker.Pos.Z', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Pos.Z')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._pos.z = cursor.nextF32()
          }
          break
        case 5:
          tabbit.checkColumn(column, 'Marker.Cell.X', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Cell.X')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._cell.x = value
          }
          break
        case 6:
          tabbit.checkColumn(column, 'Marker.Cell.Y', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Cell.Y')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._cell.y = value
          }
          break
        case 7:
          tabbit.checkColumn(column, 'Marker.Tint.R', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Tint.R')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._tint.r = value
          }
          break
        case 8:
          tabbit.checkColumn(column, 'Marker.Tint.G', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Tint.G')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._tint.g = value
          }
          break
        case 9:
          tabbit.checkColumn(column, 'Marker.Tint.B', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Tint.B')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._tint.b = value
          }
          break
        case 10:
          tabbit.checkColumn(column, 'Marker.Tint.A', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Marker.Tint.A')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._tint.a = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: MarkerRecord[]): void {
    const recordsByIndex = new Map<number, MarkerRecord>()

    for (const record of records) {
      recordsByIndex.set(record.index, record)
    }

    this._records = records
    this._recordsByIndex = recordsByIndex
  }
}
