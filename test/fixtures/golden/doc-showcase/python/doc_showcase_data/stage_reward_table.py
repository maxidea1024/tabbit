# ------------------------------------------------------------------------------
# Generated by Tabbit. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

import enum
import os

from . import tabbit
from .enum_rarity import Rarity


class StageRewardRecord:
    """Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : StageReward : B2.

    Neither key column is unique alone.
    """

    __slots__ = ("stage", "rank", "gold", "drop_table")

    def __init__(self):
        self.stage = 0
        self.rank = Rarity(0)
        self.gold = 0
        self.drop_table = ""

    def __repr__(self):
        return "StageRewardRecord(stage=%r, rank=%r, gold=%r, drop_table=%r)" % (self.stage, self.rank, self.gold, self.drop_table)


class StageRewardTable:
    """Every row of StageReward."""

    __slots__ = ("records", "by_stage_and_rank")

    def __init__(self):
        self.records = []
        self.by_stage_and_rank = {}

    def __len__(self):
        """How many rows the table holds."""
        return len(self.records)

    def __iter__(self):
        """The rows, in the order the file wrote them.

        The list reference is read once, here. A refresh replaces the reference rather
        than its contents, so a loop already running keeps the rows it started with.
        """
        return iter(self.records)

    @staticmethod
    def _key_of_stage_and_rank(stage_key, rank_key):
        """Joins the columns of the Stage and Rank key into the text the map is keyed by."""
        parts = [
            str(int(stage_key)),
            str(int(rank_key)),
        ]

        return "".join("%d:%s" % (len(part), part) for part in parts)

    def find_by_stage_and_rank(self, stage_key, rank_key):
        """The row with this Stage and Rank, or None when the table has none.

        The lookup to reach for when a missing row is an ordinary answer - an optional
        reference, a key that came from user input.
        """
        return self.by_stage_and_rank.get(self._key_of_stage_and_rank(stage_key, rank_key))

    def get_by_stage_and_rank_or_throw(self, stage_key, rank_key):
        """The row with this Stage and Rank, or a raised error naming what was missing.

        For a key that has to be there - one from another table, or a constant. The name
        says it raises, because a caller reading `get_by_stage_and_rank(key).name` at a
        glance cannot otherwise tell whether the next line is a check or a try.
        """
        record = self.by_stage_and_rank.get(self._key_of_stage_and_rank(stage_key, rank_key))

        if record is None:
            raise tabbit.RecordNotFoundError(
                "there is no record in table `StageReward` that corresponds to "
                "field `Stage and Rank` value (%r, %r)" % (stage_key, rank_key,))

        return record

    def contains_stage_and_rank(self, stage_key, rank_key):
        """Whether the table holds a row with this Stage and Rank."""
        return self._key_of_stage_and_rank(stage_key, rank_key) in self.by_stage_and_rank


    def __getitem__(self, key):
        """The row with this Stage and Rank, or a raised error naming what was missing.

        get_by_stage_and_rank_or_throw under the spelling Python uses for a keyed
        collection. It raises because that is what dict does; a language whose own map
        answers with null generates a subscript that answers with null.
        spec/targets/table-collection-surface.md section 5.7.

        This is the key, not the row's position. table[0] is the row whose
        Stage and Rank is 0, not the first row - the rows in order are .records.


        A key of several columns is passed as a tuple, in the order the sheet wrote them.
        """

        return self.get_by_stage_and_rank_or_throw(*key)

    def read(self, filename):
        """Loads the table from a .tcb file written by Tabbit.

        Column by column, matched by tag rather than position: a column this build does
        not know is skipped by its block length, and one whose type changed incompatibly
        fails naming the field.
        """
        # Imported here rather than at the top: `tables` imports this module,
        # so at import time it is half built and at call time it is not.
        from .tables import Tables

        # Opened unconditionally, and with whatever `Tables.encryption_key` and
        # `Tables.mac_key` hold - which is None unless the project set them. A file that is
        # neither encrypted nor signed comes back from this untouched, so the load path is
        # the same either way and there is no condition here that could be the wrong way
        # round. spec/wire/tcb-mac-and-signature.md.
        reader = tabbit.Reader(tabbit.open(
            tabbit.read_all_bytes(filename), Tables.encryption_key,
            Tables.mac_key, Tables.verify_mac))
        count, columns = tabbit.read_table_header(reader)

        # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        records = [StageRewardRecord() for _ in range(count)]

        for column in columns:
            block_end = reader.position + column.byte_length
            if column.tag == 1:
                tabbit.check_column(column, "StageReward.Stage", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT))
                cursor = tabbit.ColumnCursor(reader, column, count, "StageReward.Stage")
                at = 0
                while at < count:
                    n, value = cursor.next_same_i32(count - at)
                    for i in range(at, at + n):
                        records[i].stage = value
                    at += n
            elif column.tag == 2:
                tabbit.check_column(column, "StageReward.Rank", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_VARINT,))
                cursor = tabbit.ColumnCursor(reader, column, count, "StageReward.Rank")
                at = 0
                while at < count:
                    n, value = cursor.next_same_i32(count - at)
                    for i in range(at, at + n):
                        records[i].rank = Rarity(value)
                    at += n
            elif column.tag == 3:
                tabbit.check_column(column, "StageReward.Gold", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT))
                cursor = tabbit.ColumnCursor(reader, column, count, "StageReward.Gold")
                at = 0
                while at < count:
                    n, value = cursor.next_same_i32(count - at)
                    for i in range(at, at + n):
                        records[i].gold = value
                    at += n
            elif column.tag == 4:
                tabbit.check_column(column, "StageReward.DropTable", tabbit.KIND_SCALAR, False, (tabbit.ELEMENT_STRING,))
                cursor = tabbit.ColumnCursor(reader, column, count, "StageReward.DropTable")
                at = 0
                while at < count:
                    n, value = cursor.next_same_string(count - at)
                    for i in range(at, at + n):
                        records[i].drop_table = value
                    at += n
            else:
                # A column added after this code was generated.
                reader.skip(column.byte_length)

            tabbit.check_block_end(reader, column, block_end)

        by_stage_and_rank = {
            self._key_of_stage_and_rank(record.stage, record.rank): record
            for record in records
        }

        # Published, now that every column read.
        self.records = records
        self.by_stage_and_rank = by_stage_and_rank
