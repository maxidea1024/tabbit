// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Sides : B2
/// Neither key column is unique alone.
class StageRewardRecord {
  /// which stage
  int stage = 0;
  /// finishing rank
  Rarity rank = Rarity.of(0);
  /// gold paid
  int gold = 0;
  /// drop table
  String dropTable = '';

}

/// Every row of StageReward.
class StageRewardTable extends Iterable<StageRewardRecord> {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<StageRewardRecord> get records => _records;
  List<StageRewardRecord> _records = [];

  /// How many rows the table holds.
  ///
  /// Overridden rather than inherited: `Iterable.length` counts by walking, and the rows
  /// are a list that already knows.
  @override
  int get length => _records.length;

  /// The rows, in the order the file wrote them. Extending `Iterable` on top of this is
  /// what gives the table `map` - `where` - `firstWhere` without any of them being
  /// written here.
  ///
  /// The list reference is read once, here. A refresh replaces the reference rather than
  /// its contents, so a loop already running keeps the rows it started with.
  @override
  Iterator<StageRewardRecord> get iterator => _records.iterator;

  Map<String, StageRewardRecord> _byStageAndRank = {};

  /// Joins the columns of the Stage and Rank key into the text the map is keyed by.
  static String _keyOfStageAndRank(int stageKey, Rarity rankKey) {
    final parts = <String>[
      stageKey.toString(),
      rankKey.value.toString(),
    ];

    final text = StringBuffer();

    for (final part in parts) {
      text.write('${part.length}:$part');
    }

    return text.toString();
  }

  /// The row with this Stage and Rank, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  StageRewardRecord? findByStageAndRank(int stageKey, Rarity rankKey) => _byStageAndRank[_keyOfStageAndRank(stageKey, rankKey)];

  /// The row with this Stage and Rank, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getByStageAndRank(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  StageRewardRecord getByStageAndRankOrThrow(int stageKey, Rarity rankKey) {
    final record = _byStageAndRank[_keyOfStageAndRank(stageKey, rankKey)];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `StageReward` that corresponds to '
          'field `Stage and Rank` value ($stageKey, $rankKey)');
    }

    return record;
  }

  /// Whether the table holds a row with this Stage and Rank.
  bool containsStageAndRank(int stageKey, Rarity rankKey) => _byStageAndRank.containsKey(_keyOfStageAndRank(stageKey, rankKey));

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <StageRewardRecord>[];
    final loadedByStageAndRank = <String, StageRewardRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(StageRewardRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'StageReward.Stage', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'StageReward.Stage');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].stage = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'StageReward.Rank', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'StageReward.Rank');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].rank = Rarity.of(value);
            }
          }
          break;
        case 3:
          checkColumn(column, 'StageReward.Gold', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'StageReward.Gold');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].gold = value;
            }
          }
          break;
        case 4:
          checkColumn(column, 'StageReward.DropTable', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'StageReward.DropTable');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].dropTable = value;
            }
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedByStageAndRank[_keyOfStageAndRank(record.stage, record.rank)] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byStageAndRank = loadedByStageAndRank;
  }
}
