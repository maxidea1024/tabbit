// ------------------------------------------------------------------------------
// Code generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package docshowcase

import (
	"fmt"
	"iter"
	"strconv"

	"docshowcase/tabbit"
)

// StageRewardRecord was generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : StageReward : B2.
// Neither key column is unique alone.
type StageRewardRecord struct {
	// which stage
	Stage int32
	// finishing rank
	Rank Rarity
	// gold paid
	Gold int32
	// drop table
	DropTable string
}

// StageRewardTable holds every row of StageReward.
type StageRewardTable struct {
	records        []StageRewardRecord
	byStageAndRank map[string]int
}

// Records returns every row, in the order the sheet declared them.
func (t *StageRewardTable) Records() []StageRewardRecord { return t.records }

// Count returns how many rows the table holds.
func (t *StageRewardTable) Count() int { return len(t.records) }

// All returns the rows, in the order the file wrote them - `for row := range t.All()`.
//
// The slice header is read once, here. A refresh replaces it rather than its contents, so a
// loop already running keeps the rows it started with.
func (t *StageRewardTable) All() iter.Seq[*StageRewardRecord] {
	records := t.records

	return func(yield func(*StageRewardRecord) bool) {
		for i := range records {
			if !yield(&records[i]) {
				return
			}
		}
	}
}

// keyOfStageAndRank joins the columns of the Stage and Rank key into the text the map is keyed by.
func (t *StageRewardTable) keyOfStageAndRank(stageKey int32, rankKey Rarity) string {
	part0 := strconv.FormatInt(int64(stageKey), 10)
	part1 := strconv.FormatInt(int64(rankKey), 10)
	return strconv.Itoa(len(part0)) + ":" + part0 + strconv.Itoa(len(part1)) + ":" + part1
}

// FindByStageAndRank returns the row with this Stage and Rank, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *StageRewardTable) FindByStageAndRank(stageKey int32, rankKey Rarity) *StageRewardRecord {
	position, found := t.byStageAndRank[t.keyOfStageAndRank(stageKey, rankKey)]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByStageAndRankOrError returns the row with this Stage and Rank, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *StageRewardTable) GetByStageAndRankOrError(stageKey int32, rankKey Rarity) (*StageRewardRecord, error) {
	position, found := t.byStageAndRank[t.keyOfStageAndRank(stageKey, rankKey)]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `StageReward` that corresponds to field `Stage and Rank` value (%v, %v)", stageKey, rankKey)
	}

	return &t.records[position], nil
}

// ContainsStageAndRank reports whether the table holds a row with this Stage and Rank.
func (t *StageRewardTable) ContainsStageAndRank(stageKey int32, rankKey Rarity) bool {
	_, found := t.byStageAndRank[t.keyOfStageAndRank(stageKey, rankKey)]
	return found
}

// Read loads the table from a .tcb file written by Tabbit.
func (t *StageRewardTable) Read(filename string) error {
	data, err := tabbit.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	// Unconditionally, and with whatever EncryptionKey and MacKey hold - which is nil
	// unless the project set them. A file that is neither encrypted nor signed comes back
	// from this untouched, so the load path is the same either way and there is no
	// condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
	//
	// What comes back is a window onto data rather than a copy of it, which is why data
	// stays in scope for the whole read.
	opened, err := tabbit.Open(data, EncryptionKey, MacKey, VerifyMac)
	if err != nil {
		return err
	}

	reader := tabbit.NewReader(opened)
	count, columns := tabbit.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]StageRewardRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if tabbit.CheckColumn(reader, column, "StageReward.Stage", tabbit.KindScalar, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "StageReward.Stage")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Stage = value
						i++
					}
				}
			}
		case 2:
			if tabbit.CheckColumn(reader, column, "StageReward.Rank", tabbit.KindScalar, false, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "StageReward.Rank")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Rank = Rarity(value)
						i++
					}
				}
			}
		case 3:
			if tabbit.CheckColumn(reader, column, "StageReward.Gold", tabbit.KindScalar, false, tabbit.ElementI32, tabbit.ElementVarint) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "StageReward.Gold")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameI32(count - i)
					for ; n > 0; n-- {
						records[i].Gold = value
						i++
					}
				}
			}
		case 4:
			if tabbit.CheckColumn(reader, column, "StageReward.DropTable", tabbit.KindScalar, false, tabbit.ElementString) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "StageReward.DropTable")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameString(count - i)
					for ; n > 0; n-- {
						records[i].DropTable = value
						i++
					}
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		tabbit.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("StageReward: %w", err)
	}

	byStageAndRank := make(map[string]int, len(records))
	for i := range records {
		byStageAndRank[t.keyOfStageAndRank(records[i].Stage, records[i].Rank)] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byStageAndRank = byStageAndRank

	return nil
}
