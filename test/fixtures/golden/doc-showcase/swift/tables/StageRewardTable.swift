// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import Foundation

// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Sides : B2
/// Neither key column is unique alone.
public final class StageRewardRecord {

    public init() {}

    /// which stage
    public var stage: Int32 = 0

    /// finishing rank
    public var rank: Rarity = Rarity.of(0)

    /// gold paid
    public var gold: Int32 = 0

    /// drop table
    public var dropTable: String = ""
}

/// Every row of StageReward.
public final class StageRewardTable: Sequence {

    public init() {}

    /// How many rows the table holds.
    public var count: Int { records.count }

    /// The rows, in the order the file wrote them. Conforming to `Sequence` on top of this
    /// is what gives the table `map` - `filter` - `first(where:)` without any of them being
    /// written here.
    ///
    /// The array is read once, here. A refresh replaces it rather than appending to it, so a
    /// loop already running keeps the rows it started with.
    public func makeIterator() -> Array<StageRewardRecord>.Iterator {
        records.makeIterator()
    }

    /// Every row, in the order the sheet declared them.
    ///
    /// The array a read published, not one a read fills. A refresh replaces the value rather
    /// than appending to it, so whoever holds this holds one whole load.
    public private(set) var records: [StageRewardRecord] = []

    private var byStageAndRank: [String: StageRewardRecord] = [:]

    /// Joins the columns of the Stage and Rank key into the text the map is keyed by.
    private static func keyOfStageAndRank(_ stageKey: Int32, _ rankKey: Rarity) -> String {
        let parts: [String] = [
            String(stageKey),
            String(rankKey.rawValue),
        ]

        var text = ""

        for part in parts {
            text += "\(part.count):\(part)"
        }

        return text
    }

    /// The row with this Stage and Rank, or nil when the table has none.
    ///
    /// The lookup to reach for when a missing row is an ordinary answer - an optional
    /// reference, a key that came from user input.
    public func findByStageAndRank(_ stageKey: Int32, _ rankKey: Rarity) -> StageRewardRecord? {
        byStageAndRank[Self.keyOfStageAndRank(stageKey, rankKey)]
    }

    /// The row with this Stage and Rank, or a thrown error naming what was missing.
    ///
    /// For a key that has to be there - one from another table, or a constant. The name says
    /// it throws, because a caller reading getByStageAndRankOrThrow(key).name at a
    /// glance cannot otherwise tell whether the next line is a nil check or a catch.
    public func getByStageAndRankOrThrow(_ stageKey: Int32, _ rankKey: Rarity) throws -> StageRewardRecord {
        guard let record = byStageAndRank[Self.keyOfStageAndRank(stageKey, rankKey)] else {
            throw RecordNotFoundError(
                "there is no record in table `StageReward` that corresponds to "
                + "field `Stage and Rank` value (\(stageKey), \(rankKey))")
        }

        return record
    }

    /// Whether the table holds a row with this Stage and Rank.
    public func containsStageAndRank(_ stageKey: Int32, _ rankKey: Rarity) -> Bool {
        byStageAndRank[Self.keyOfStageAndRank(stageKey, rankKey)] != nil
    }


    /// The row with this Stage and Rank, or nil when the table has none.
    ///
    /// `findByStageAndRank` under the spelling Swift uses for a keyed collection. It
    /// answers with an optional because that is what `Dictionary` does here, and the type
    /// says so - a language whose own map throws generates a subscript that throws.
    /// spec/targets/table-collection-surface.md section 5.7.
    ///
    /// This is the key, not the row's position. `table[0]` is the row whose
    /// Stage and Rank is 0, not the first row - the rows in order are `records`.
    public subscript(_ stageKey: Int32, _ rankKey: Rarity) -> StageRewardRecord? {
        findByStageAndRank(stageKey, rankKey)
    }

    /// Loads the table from a .tcb file written by Tabbit.
    ///
    /// Column by column, matched by tag rather than position: a column this build does not
    /// know is skipped by its block length, and one whose type changed incompatibly throws
    /// naming the field.
    ///
    /// The keys are parameters rather than properties of a shared object, because Swift's
    /// concurrency checking makes mutable global state an error and because the accessor
    /// holding them is an instance - which is what lets two loads of two data sets exist at
    /// once. DocShowcaseAccessor passes its own. spec/targets/accessor-instances.md.
    public func read(
        _ filename: String,
        key: [UInt8]? = nil,
        macKey: [UInt8]? = nil,
        verifyMac: Bool = true
    ) throws {
        // Opened unconditionally, and with whatever the caller holds - which is nil unless
        // the project set it. A file that is neither encrypted nor signed comes back from
        // this untouched, so the load path is the same either way and there is no condition
        // here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
        let bytes = try Tcb.openTable(
            try Tcb.readAllBytes(filename), key: key, macKey: macKey, verifyMac: verifyMac)

        let reader = Tcb.Reader(bytes)
        let header = try Tcb.readTableHeader(reader)
        let count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is
        // already loaded is a refresh, and one that turns out to be unreadable has to leave
        // the rows already there alone.
        var loaded = [StageRewardRecord]()
        loaded.reserveCapacity(count)

        for _ in 0 ..< count { loaded.append(StageRewardRecord()) }
        var loadedByStageAndRank = [String: StageRewardRecord](
            minimumCapacity: count)

        for column in header.columns {
            let blockEnd = reader.position + column.byteLength

            switch column.tag {
            case 1:
                try Tcb.checkColumn(column, "StageReward.Stage", Tcb.kindScalar, false, Tcb.elementI32, Tcb.elementVarint)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "StageReward.Stage")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].stage = cursor.runSameValue
                        at += 1
                        n -= 1
                    }
                }
            case 2:
                try Tcb.checkColumn(column, "StageReward.Rank", Tcb.kindScalar, false, Tcb.elementVarint)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "StageReward.Rank")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].rank = Rarity.of(cursor.runSameValue)
                        at += 1
                        n -= 1
                    }
                }
            case 3:
                try Tcb.checkColumn(column, "StageReward.Gold", Tcb.kindScalar, false, Tcb.elementI32, Tcb.elementVarint)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "StageReward.Gold")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameI32(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].gold = cursor.runSameValue
                        at += 1
                        n -= 1
                    }
                }
            case 4:
                try Tcb.checkColumn(column, "StageReward.DropTable", Tcb.kindScalar, false, Tcb.elementString)
                let cursor = try Tcb.ColumnCursor(reader, column, count, "StageReward.DropTable")
                var at = 0
                while at < count {
                    var n = try cursor.nextSameString(count - at)
                    while n > 0 {
                        let i = at
                        loaded[i].dropTable = cursor.runSameText
                        at += 1
                        n -= 1
                    }
                }
            default:
                // A column added after this code was generated.
                try reader.skip(column.byteLength)
            }

            try Tcb.checkBlockEnd(reader, column, blockEnd)
        }

        for record in loaded {
            loadedByStageAndRank[Self.keyOfStageAndRank(record.stage, record.rank)] = record
        }

        // Published, now that every column read.
        records = loaded
        byStageAndRank = loadedByStageAndRank
    }
}
