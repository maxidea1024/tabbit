// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#pragma once

#include "CoreMinimal.h"
#include "UObject/ObjectMacros.h"
#include "Kismet/BlueprintFunctionLibrary.h"

#include "TabbitTcbReader.h"

// Must be the last include. UHT requires it, and moving it produces an error that
// names some other line entirely.
#include "FDocShowcase.generated.h"


// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Basics : B2
/** How rare a potion is. */
UENUM(BlueprintType)
enum class ERarity : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    /** everywhere */
    Common = 1 UMETA(DisplayName = "Common"),
    /** in chests */
    Rare = 2 UMETA(DisplayName = "Rare"),
    /** from bosses */
    Epic = 3 UMETA(DisplayName = "Epic"),
};


/* Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Basics : G2 */
/** Values with no rows. */
struct FBalance
{
    /** stack limit */
    static inline const int32 MaxStack = 99;
    /** sale refund */
    static inline const float RefundRate = 0.5f;
};


// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Potion : B2
/** A key, two values and an enum. */
USTRUCT(BlueprintType)
struct DOCSHOWCASE_API FPotionRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Potion")
    int32 Index = 0;

    /** display name */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Potion")
    FString Name;

    /** how rare */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Potion")
    ERarity Rarity = static_cast<ERarity>(0);

    /** shop price */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Potion")
    int32 Price = 0;

};


// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Shop : B2
/** Pointed at by ShopEntry. */
USTRUCT(BlueprintType)
struct DOCSHOWCASE_API FShopRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Shop")
    int32 Index = 0;

    /** shop name */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Shop")
    FString Name;

};


// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Shop : F2
/** What each shop sells. */
USTRUCT(BlueprintType)
struct DOCSHOWCASE_API FShopEntryRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopEntry")
    int32 Index = 0;

    /** which shop */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopEntry")
    int32 ShopId = 0;

    /** which potion */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopEntry")
    int32 PotionId = 0;

    /** how many */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopEntry")
    int32 Stock = 0;

};


// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Loot : B2
/** Two ways to write an array. */
USTRUCT(BlueprintType)
struct DOCSHOWCASE_API FLootRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Loot")
    int32 Index = 0;

    /** search tags */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Loot")
    TArray<FString> Tags;

    /** weight 1 */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Loot")
    TArray<int32> Weight;

};


// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Spawn : B2
/** One element of FSpawnRow::At. */
USTRUCT(BlueprintType)
struct DOCSHOWCASE_API FSpawnAtEntry
{
    GENERATED_BODY()

    /** x position */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Spawn")
    float X = 0.0f;

    /** y position */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Spawn")
    float Y = 0.0f;

};

/** Two columns as one record. */
USTRUCT(BlueprintType)
struct DOCSHOWCASE_API FSpawnRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Spawn")
    int32 Index = 0;

    /** x position */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Spawn")
    FSpawnAtEntry At;

    /** radius, or blank */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Spawn")
    float Radius = 0.0f;

    /** Whether this row has a value for Radius. */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Spawn")
    bool bHasRadius = false;
};


// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : StageReward : B2
/** Neither key column is unique alone. */
USTRUCT(BlueprintType)
struct DOCSHOWCASE_API FStageRewardRow
{
    GENERATED_BODY()

    /** which stage */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    int32 Stage = 0;

    /** finishing rank */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    ERarity Rank = static_cast<ERarity>(0);

    /** gold paid */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    int32 Gold = 0;

    /** drop table */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    FString DropTable;

};


/** Every row of Potion. */
class DOCSHOWCASE_API FPotionTable
{
public:
    const TArray<FPotionRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FPotionRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FPotionRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Shop. */
class DOCSHOWCASE_API FShopTable
{
public:
    const TArray<FShopRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FShopRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FShopRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of ShopEntry. */
class DOCSHOWCASE_API FShopEntryTable
{
public:
    const TArray<FShopEntryRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FShopEntryRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FShopEntryRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Loot. */
class DOCSHOWCASE_API FLootTable
{
public:
    const TArray<FLootRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FLootRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FLootRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of Spawn. */
class DOCSHOWCASE_API FSpawnTable
{
public:
    const TArray<FSpawnRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Index, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FSpawnRow* FindByIndex(int32 Key) const;

    /** Whether the table holds a row with this Index. */
    bool ContainsIndex(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FSpawnRow> RecordsStorage;
    TMap<int32, int32> ByIndex;
};


/** Every row of StageReward. */
class DOCSHOWCASE_API FStageRewardTable
{
public:
    const TArray<FStageRewardRow>& Records() const { return RecordsStorage; }

    /** How many rows the table holds. */
    int32 Num() const { return RecordsStorage.Num(); }

    /**
     * Whether the table holds no rows.
     *
     * Asked of the count rather than through TArray::IsEmpty, which the engine only grew
     * in 4.27 - this way the generated module builds against older engines too.
     */
    bool IsEmpty() const { return RecordsStorage.Num() == 0; }

    /** The rows, in the order the file wrote them - what a range-for binds to. */
    auto begin() const { return RecordsStorage.begin(); }

    auto end() const { return RecordsStorage.end(); }

    /**
     * The row with this Stage and Rank, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FStageRewardRow* FindByStageAndRank(int32 StageKey, ERarity RankKey) const;

    /** Whether the table holds a row with this Stage and Rank. */
    bool ContainsStageAndRank(int32 StageKey, ERarity RankKey) const;

    /** Joins the columns of the Stage and Rank key into the text the map is keyed by. */
    static FString KeyOfStageAndRank(int32 StageKey, ERarity RankKey);

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FStageRewardRow> RecordsStorage;
    TMap<FString, int32> ByStageAndRank;
};


/**
 * Every table, loaded together.
 *
 * A static class rather than a UObject: the data outlives any world, is read once at
 * startup and never mutated, and nothing here needs reflection or garbage collection.
 */
class DOCSHOWCASE_API FDocShowcase
{
public:
    static const FPotionTable& Potion() { return PotionStorage; }
    static const FShopTable& Shop() { return ShopStorage; }
    static const FShopEntryTable& ShopEntry() { return ShopEntryStorage; }
    static const FLootTable& Loot() { return LootStorage; }
    static const FSpawnTable& Spawn() { return SpawnStorage; }
    static const FStageRewardTable& StageReward() { return StageRewardStorage; }

    /**
     * Reads every table from BasePath. Returns false if any of them could not be read.
     *
     * FileExtension defaults to what the recipe told the exporter to write. Pass a different
     * one when the data files were renamed on the way into the package - which they often are,
     * since some platforms only cook what carries an extension they recognise.
     */
    static bool ReadAll(const FString& BasePath,
                        const FString& FileExtension = TEXT(".tcb"));

    /**
     * The key the table files were sealed with, or empty when they were not sealed.
     *
     * Beside ReadAll because it is the same kind of thing: one decision the project makes
     * once, at start-up, before the first table is read. A key threaded through every Read
     * signature would be the same value written out at every call site, and every call site
     * is somewhere it could be wrong.
     *
     * Thirty-two bytes, the key the converter was given. Empty - the default - is a project
     * that does not encrypt, and it costs that project nothing: the load path opens the
     * envelope either way, and a file that is not encrypted comes back from that unchanged.
     * A project that does encrypt and leaves this empty gets the reader's own message saying
     * the file is encrypted and no key was given, rather than a failure somewhere inside the
     * format.
     *
     * Where the key comes from is the one thing about this layer worth being careful with.
     * It is a secret only until it reaches a repository, a build log or an issue, and it
     * cannot be taken back afterwards - so it is read at start-up from wherever this project
     * keeps secrets, not written into a source file or a .ini the packaging step will cook.
     * What the layer is for, and therefore what is and is not worth defending, is
     * spec/wire/tcb-v104-composed-encodings.md section 4.
     */
    static TArray<uint8> EncryptionKey;

    /**
     * The key the table files were signed with, or empty when they were not signed.
     *
     * A different key from EncryptionKey and independent of it: a file can be signed without
     * being encrypted, and encrypted without being signed.
     *
     * What it buys is the thing encryption does not. The cipher is a keystream XOR, so a bit
     * flipped in the file is a bit flipped in the value it decrypts to - no key needed - and
     * every structural check the format makes still passes, because a four-byte float
     * accepts any four bytes. With this set, a file altered after export stops loading.
     *
     * Setting it makes the reader stricter in a second way: a file that carries no MAC is
     * refused, because otherwise the check could be removed by zeroing sixteen bytes. So the
     * order to turn this on in is data first, key second. spec/wire/tcb-mac-and-signature.md.
     */
    static TArray<uint8> MacKey;

    /**
     * Whether to check the MAC at all. True by default.
     *
     * For tools that read exported files without the key, and for measuring what the check
     * costs at load. It is not a security boundary - anyone who can flip a flag in a shipped
     * build can read MacKey out of the same build.
     */
    static bool bVerifyMac;

private:
    static FPotionTable PotionStorage;
    static FShopTable ShopStorage;
    static FShopEntryTable ShopEntryStorage;
    static FLootTable LootStorage;
    static FSpawnTable SpawnStorage;
    static FStageRewardTable StageRewardStorage;
};

/**
 * The same data, reachable from Blueprint.
 *
 * Every row above is a USTRUCT marked BlueprintType with BlueprintReadOnly properties,
 * which says the rows are meant to be used from Blueprint - and until this existed there
 * was no way to get one. A graph could declare a variable of a row type and had nothing to
 * obtain a value from, because the accessor is a plain C++ class and a static method on one
 * is not something Blueprint can call.
 *
 * A function library rather than making the accessor a UObject. The storage stays where it
 * is: this owns nothing, adds no garbage collection, and does not need a world. It is a
 * few UFUNCTIONs over the C++ that was already there.
 */
UCLASS()
class DOCSHOWCASE_API UDocShowcaseLibrary : public UBlueprintFunctionLibrary
{
    GENERATED_BODY()

public:

    /**
     * The Potion row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Potion",
              meta = (DisplayName = "Get Potion Row"))
    static FPotionRow GetPotionRow(int32 Key, bool& bFound);

    /** How many Potion rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Potion",
              meta = (DisplayName = "Get Potion Row Count"))
    static int32 GetPotionRowCount();

    /**
     * The Potion row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Potion",
              meta = (DisplayName = "Get Potion Row At"))
    static FPotionRow GetPotionRowAt(int32 Position, bool& bFound);

    /**
     * The Shop row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Shop",
              meta = (DisplayName = "Get Shop Row"))
    static FShopRow GetShopRow(int32 Key, bool& bFound);

    /** How many Shop rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Shop",
              meta = (DisplayName = "Get Shop Row Count"))
    static int32 GetShopRowCount();

    /**
     * The Shop row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Shop",
              meta = (DisplayName = "Get Shop Row At"))
    static FShopRow GetShopRowAt(int32 Position, bool& bFound);

    /**
     * The ShopEntry row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ShopEntry",
              meta = (DisplayName = "Get ShopEntry Row"))
    static FShopEntryRow GetShopEntryRow(int32 Key, bool& bFound);

    /** How many ShopEntry rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ShopEntry",
              meta = (DisplayName = "Get ShopEntry Row Count"))
    static int32 GetShopEntryRowCount();

    /**
     * The ShopEntry row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ShopEntry",
              meta = (DisplayName = "Get ShopEntry Row At"))
    static FShopEntryRow GetShopEntryRowAt(int32 Position, bool& bFound);

    /**
     * The Loot row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Loot",
              meta = (DisplayName = "Get Loot Row"))
    static FLootRow GetLootRow(int32 Key, bool& bFound);

    /** How many Loot rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Loot",
              meta = (DisplayName = "Get Loot Row Count"))
    static int32 GetLootRowCount();

    /**
     * The Loot row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Loot",
              meta = (DisplayName = "Get Loot Row At"))
    static FLootRow GetLootRowAt(int32 Position, bool& bFound);

    /**
     * The Spawn row with the given Index.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Spawn",
              meta = (DisplayName = "Get Spawn Row"))
    static FSpawnRow GetSpawnRow(int32 Key, bool& bFound);

    /** How many Spawn rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Spawn",
              meta = (DisplayName = "Get Spawn Row Count"))
    static int32 GetSpawnRowCount();

    /**
     * The Spawn row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Spawn",
              meta = (DisplayName = "Get Spawn Row At"))
    static FSpawnRow GetSpawnRowAt(int32 Position, bool& bFound);

    /**
     * The StageReward row with the given Stage and Rank.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageReward",
              meta = (DisplayName = "Get StageReward Row"))
    static FStageRewardRow GetStageRewardRow(int32 StageKey, ERarity RankKey, bool& bFound);

    /** How many StageReward rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageReward",
              meta = (DisplayName = "Get StageReward Row Count"))
    static int32 GetStageRewardRowCount();

    /**
     * The StageReward row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageReward",
              meta = (DisplayName = "Get StageReward Row At"))
    static FStageRewardRow GetStageRewardRowAt(int32 Position, bool& bFound);

    /**
     * Reads every table from BasePath, as FDocShowcase::ReadAll does.
     *
     * BlueprintCallable rather than BlueprintPure: it does something.
     */
    UFUNCTION(BlueprintCallable, Category = "Tabbit",
              meta = (DisplayName = "Load All Tabbit Tables"))
    static bool ReadAll(const FString& BasePath,
                        const FString& FileExtension = TEXT(".tcb"));
};
