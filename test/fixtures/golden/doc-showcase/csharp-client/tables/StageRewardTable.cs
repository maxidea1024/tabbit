// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Tabbit.Fixtures.DocShowcase
{
    [System.Serializable]
    public partial class StageRewardRecord
    {
        #region Values
        /// <summary>
        /// which stage
        /// </summary>
        public int Stage => _stage;

        /// <summary>
        /// finishing rank
        /// </summary>
        public global::Tabbit.Fixtures.DocShowcase.Rarity Rank => _rank;

        /// <summary>
        /// gold paid
        /// </summary>
        public int Gold => _gold;
        #endregion

        #region Storage
        internal int _stage;
        internal global::Tabbit.Fixtures.DocShowcase.Rarity _rank;
        internal int _gold;
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"Stage\":"); ToStringHelper.ToString(Stage, sb);
            sb.Append(",\"Rank\":"); ToStringHelper.ToString(Rank, sb);
            sb.Append(",\"Gold\":"); ToStringHelper.ToString(Gold, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// Neither key column is unique alone.
    /// </summary>
    [System.Serializable]
    public partial class StageRewardTable : IEnumerable<StageRewardRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Stage", "Rank", "Gold" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Stage, r.Rank, r.Gold });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<StageRewardRecord> Records => _records;
        private List<StageRewardRecord> _records = new List<StageRewardRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<StageRewardRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<StageRewardRecord> IEnumerable<StageRewardRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'Stage and Rank'
        private Dictionary<string, StageRewardRecord> _recordsByStageAndRank = new Dictionary<string, StageRewardRecord>();

        /// <summary>Joins the columns of the `Stage and Rank` key into the text the map is keyed by.</summary>
        private static string KeyOfStageAndRank(int stageKey, global::Tabbit.Fixtures.DocShowcase.Rarity rankKey)
        {
            var parts = new string[]
            {
                stageKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
                ((long)rankKey).ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `Stage and Rank`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public StageRewardRecord FindByStageAndRank(int stageKey, global::Tabbit.Fixtures.DocShowcase.Rarity rankKey)
            => _recordsByStageAndRank.TryGetValue(KeyOfStageAndRank(stageKey, rankKey), out StageRewardRecord record) ? record : null;

        /// <summary>
        /// The row with this `Stage and Rank`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public StageRewardRecord GetByStageAndRankOrThrow(int stageKey, global::Tabbit.Fixtures.DocShowcase.Rarity rankKey)
        {
            if (!_recordsByStageAndRank.TryGetValue(KeyOfStageAndRank(stageKey, rankKey), out StageRewardRecord record))
                throw new TabbitException($"There is no record in table `StageReward` that corresponds to field `Stage and Rank` value ({stageKey}, {rankKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Stage and Rank`.</summary>
        public bool ContainsStageAndRank(int stageKey, global::Tabbit.Fixtures.DocShowcase.Rarity rankKey) => _recordsByStageAndRank.ContainsKey(KeyOfStageAndRank(stageKey, rankKey));

        /// <summary>
        /// The row with this `Stage and Rank`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// A key of several columns takes them in the order the sheet wrote them, which is
        /// the order `GetByStageAndRankOrThrow` takes them in too.
        /// spec/targets/table-collection-surface.md section 5.4.
        /// </remarks>
        public StageRewardRecord this[int stageKey, global::Tabbit.Fixtures.DocShowcase.Rarity rankKey]
            => GetByStageAndRankOrThrow(stageKey, rankKey);
        #endregion // Indexing by `Stage and Rank`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await DocShowcaseAccessor.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `DocShowcaseAccessor.EncryptionKey` and `DocShowcaseAccessor.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, DocShowcaseAccessor.EncryptionKey, DocShowcaseAccessor.MacKey, DocShowcaseAccessor.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<StageRewardRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new StageRewardRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "StageReward.Stage", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "StageReward.Stage");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._stage = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "StageReward.Rank", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "StageReward.Rank");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rank = (global::Tabbit.Fixtures.DocShowcase.Rarity)value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "StageReward.Gold", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "StageReward.Gold");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._gold = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByStageAndRank = new Dictionary<string, StageRewardRecord>(count);
            foreach (var record in records)
                recordsByStageAndRank.Add(KeyOfStageAndRank(record.Stage, record.Rank), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByStageAndRank = recordsByStageAndRank;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Tabbit.Fixtures.DocShowcase
