-- ------------------------------------------------------------------------------
-- Generated by Tabbit. DO NOT EDIT.
--
-- Changes to this file may cause incorrect behavior and will be lost if the code is
-- regenerated.
-- ------------------------------------------------------------------------------

local _root = (...):match("^(.-)[^%.]+%.[^%.]+$")
local tcb = require(_root .. "tabbit.tcb_reader")
local Rarity = require(_root .. "enums.enum_rarity")

-- Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Sides : B2.
-- Neither key column is unique alone.
---@class StageRewardRecord
---@field stage integer
---@field rank integer
---@field gold integer
---@field dropTable string
local StageRewardRecordMeta = tcb.strictType("a `StageReward` row", { "stage", "rank", "gold", "dropTable" })

---@return StageRewardRecord
local function newStageRewardRecord()
  return setmetatable({
    stage = 0,
    rank = 0,
    gold = 0,
    dropTable = "",
  }, StageRewardRecordMeta)
end

---@class StageRewardTable
---@field records StageRewardRecord[]
local StageRewardTable = {}

local StageRewardTableMeta = tcb.strictInstance("the `StageReward` table", StageRewardTable, { "records", "byStageAndRank" })

function StageRewardTable.new()
  return setmetatable({
    records = {},
    byStageAndRank = {},
  }, StageRewardTableMeta)
end

-- How many rows the table holds.
--
-- A method rather than `#table`: LuaJIT 2.1 does not call `__len` on a table, so the
-- operator would answer on 5.3+ and not on the other runtime this output has to run on.
function StageRewardTable:count()
  return #self.records
end

-- The rows, in the order the file wrote them - `for row in table:iter() do`.
--
-- The list is read once, here. A refresh replaces it rather than its contents, so a loop
-- already running keeps the rows it started with.
function StageRewardTable:iter()
  local records = self.records
  local i = 0

  return function()
    i = i + 1
    return records[i]
  end
end

-- Joins the columns of the Stage and Rank key into the text the map is keyed by.
local function keyOfStageAndRank(stageKey, rankKey)
  local parts = {
    tostring(stageKey),
    tostring(rankKey),
  }

  local text = {}

  for i = 1, #parts do
    text[i] = #parts[i] .. ":" .. parts[i]
  end

  return table.concat(text)
end

-- The row with this Stage and Rank, or nil when the table has none: the lookup to
-- reach for when a missing row is an ordinary answer.
function StageRewardTable:findByStageAndRank(stageKey, rankKey)
  return self.byStageAndRank[keyOfStageAndRank(stageKey, rankKey)]
end

-- The row with this Stage and Rank, or a raised error naming what was missing -
-- for a key that has to be there.
function StageRewardTable:getByStageAndRankOrThrow(stageKey, rankKey)
  local record = self:findByStageAndRank(stageKey, rankKey)

  if record == nil then
    tcb.fail("there is no record in table `StageReward` that corresponds to " ..
      "field `Stage and Rank` value (%s, %s)", tostring(stageKey), tostring(rankKey))
  end

  return record
end

-- Whether the table holds a row with this Stage and Rank.
function StageRewardTable:containsStageAndRank(stageKey, rankKey)
  return self:findByStageAndRank(stageKey, rankKey) ~= nil
end


-- Loads the table from the bytes of a .tcb file written by Tabbit. Column by column,
-- matched by tag rather than position: a column this build does not know is skipped by
-- its block length, and one whose type changed incompatibly fails naming the field.
function StageRewardTable:readBytes(data)
  -- Required here rather than at the top: the accessor requires this module, so at load
  -- time it is half built and at call time it is not.
  local accessor = require(_root .. "tables")

  -- Opened unconditionally, with whatever keys the accessor holds - nil unless the
  -- project set them. A file that is neither encrypted nor signed comes back untouched,
  -- so the load path is the same either way. spec/wire/tcb-mac-and-signature.md.
  local reader = tcb.newReader(tcb.open(
    data, accessor.encryptionKey, accessor.macKey, accessor.verifyMac))
  local count, columns = tcb.readTableHeader(reader)

  -- Read into storage of its own and published at the end: reading a table that is
  -- already loaded is a refresh, and one that turns out to be unreadable has to leave
  -- the rows already there alone.
  local records = {}

  for i = 1, count do
    records[i] = newStageRewardRecord()
  end

  for c = 1, #columns do
    local column = columns[c]
    local blockEnd = reader.position + column.byteLength

    if column.tag == 1 then
      tcb.checkColumn(column, "StageReward.Stage", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "StageReward.Stage")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].stage = value
        end

        at = at + n
      end
    elseif column.tag == 2 then
      tcb.checkColumn(column, "StageReward.Rank", tcb.KIND_SCALAR, false, { tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "StageReward.Rank")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].rank = value
        end

        at = at + n
      end
    elseif column.tag == 3 then
      tcb.checkColumn(column, "StageReward.Gold", tcb.KIND_SCALAR, false, { tcb.ELEMENT_I32, tcb.ELEMENT_VARINT })
      local cursor = tcb.newCursor(reader, column, count, "StageReward.Gold")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameI32(count - at)

        for i = at + 1, at + n do
          records[i].gold = value
        end

        at = at + n
      end
    elseif column.tag == 4 then
      tcb.checkColumn(column, "StageReward.DropTable", tcb.KIND_SCALAR, false, { tcb.ELEMENT_STRING })
      local cursor = tcb.newCursor(reader, column, count, "StageReward.DropTable")
      local at = 0

      while at < count do
        local n, value = cursor:nextSameString(count - at)

        for i = at + 1, at + n do
          records[i].dropTable = value
        end

        at = at + n
      end
    else
      -- A column added after this code was generated.
      reader:skip(column.byteLength)
    end

    tcb.checkBlockEnd(reader, column, blockEnd)
  end

  local byStageAndRank = {}

  for i = 1, #records do
    byStageAndRank[keyOfStageAndRank(records[i].stage, records[i].rank)] = records[i]
  end

  -- Published, now that every column read.
  self.records = records
  self.byStageAndRank = byStageAndRank
end

-- The same load from a file on disk.
function StageRewardTable:read(filename)
  self:readBytes(tcb.readAllBytes(filename))
end

return StageRewardTable
