/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "DocShowcase.h"

#include <stdio.h>
#include <string.h>

/* The definitions for the declarations in the forward header. Beside the accessor's own
 * load because that is where a project answers the questions the generated code cannot. */
const uint8_t* DocShowcase_EncryptionKey = NULL;
int32_t DocShowcase_EncryptionKeyLength = 0;
const uint8_t* DocShowcase_MacKey = NULL;
int32_t DocShowcase_MacKeyLength = 0;
bool DocShowcase_VerifyMac = true;

/* Turns the stored indices into usable pointers, once every table is in memory.
 *
 * A reference into a row that is not there stays NULL rather than inventing one. */
static void DocShowcase_SolveCrossReferences(DocShowcase_t* data) {
  int32_t row;

  for (row = 0; row < data->shop_entry.count; ++row) {
    DocShowcase_ShopEntryRecord_t* record = &data->shop_entry.records[row];

    {
      const DocShowcase_ShopRecord_t* target = DocShowcase_ShopFindByIndex(
        &data->shop, record->shop_id);

      if (target != NULL)
        record->shop_by_shop_id = target;
    }

    {
      const DocShowcase_PotionRecord_t* target = DocShowcase_PotionFindByIndex(
        &data->potion, record->potion_id);

      if (target != NULL)
        record->potion_by_potion_id = target;
    }
  }
}

bool DocShowcase_LoadAll(DocShowcase_t* data, const char* base_path,
                 char* error, size_t error_size) {
  return DocShowcase_LoadAllWithExtension(
    data, base_path, ".tcb", error, error_size);
}

bool DocShowcase_LoadAllWithExtension(DocShowcase_t* data, const char* base_path,
                 const char* file_extension, char* error, size_t error_size) {
  char path[1024];

  /* Loaded beside whatever `data` is already holding and swapped in at the end. Reading
   * again is a reload, and one that fails has to leave the caller's pointers valid: they
   * point into the arenas of the previous load, which is why this cannot start by zeroing
   * `data` - doing that dropped those arenas without freeing them. */
  DocShowcase_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Potion", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    DocShowcase_Free(&loaded);
    return false;
  }

  if (!DocShowcase_PotionLoad(&loaded.potion, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    DocShowcase_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Shop", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    DocShowcase_Free(&loaded);
    return false;
  }

  if (!DocShowcase_ShopLoad(&loaded.shop, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    DocShowcase_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "ShopEntry", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    DocShowcase_Free(&loaded);
    return false;
  }

  if (!DocShowcase_ShopEntryLoad(&loaded.shop_entry, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    DocShowcase_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Loot", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    DocShowcase_Free(&loaded);
    return false;
  }

  if (!DocShowcase_LootLoad(&loaded.loot, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    DocShowcase_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Spawn", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    DocShowcase_Free(&loaded);
    return false;
  }

  if (!DocShowcase_SpawnLoad(&loaded.spawn, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    DocShowcase_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "StageReward", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    DocShowcase_Free(&loaded);
    return false;
  }

  if (!DocShowcase_StageRewardLoad(&loaded.stage_reward, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    DocShowcase_Free(&loaded);
    return false;
  }

  /* Linked among the tables of this load, so no row points into the previous one. */
  DocShowcase_SolveCrossReferences(&loaded);

  /* The previous load goes now, and not before. */
  DocShowcase_Free(data);
  *data = loaded;

  return true;
}

void DocShowcase_Free(DocShowcase_t* data) {
  DocShowcase_PotionFree(&data->potion);
  DocShowcase_ShopFree(&data->shop);
  DocShowcase_ShopEntryFree(&data->shop_entry);
  DocShowcase_LootFree(&data->loot);
  DocShowcase_SpawnFree(&data->spawn);
  DocShowcase_StageRewardFree(&data->stage_reward);
}
