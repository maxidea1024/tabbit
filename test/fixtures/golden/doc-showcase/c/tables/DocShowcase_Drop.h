/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DOC_SHOWCASE_DROP_H
#define DOC_SHOWCASE_DROP_H

#include "tabbit/tabbit_tcb_reader.h"

#include "DocShowcase_Forward.h"
#include "enums/DocShowcase_EnumRarity.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Loot : G2
 *
 * An array of enums, and one whose elements may be absent.
 */
struct DocShowcase_DropRecord_t {
  /* primary index */
  int32_t index;
  /* which grades */
  DocShowcase_Rarity_t* grades;
  int32_t grades_count;
  /* how many of each */
  int32_t* counts;
  int32_t counts_count;
  /* Which of counts's elements have a value, one bool per element, or NULL where
   * the file did not carry the column. spec/types/nullable-array-elements.md. */
  const bool* has_counts_at;
};

/* Every row of Drop.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct DocShowcase_DropTable_t {
  DocShowcase_DropRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} DocShowcase_DropTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool DocShowcase_DropLoad(DocShowcase_DropTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void DocShowcase_DropFree(DocShowcase_DropTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const DocShowcase_DropRecord_t* DocShowcase_DropFindByIndex(
  const DocShowcase_DropTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool DocShowcase_DropContainsIndex(
  const DocShowcase_DropTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* DOC_SHOWCASE_DROP_H */
