/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DOC_SHOWCASE_SKILL_H
#define DOC_SHOWCASE_SKILL_H

#include "tabbit/tabbit_tcb_reader.h"

#include "DocShowcase_Forward.h"
#include "structs/DocShowcase_StructEffect.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Skill : B2
 *
 * Every variant's members side by side; a row fills its own.
 */
/* One element of DocShowcase_SkillRecord_t::effect. */
struct DocShowcase_SkillRecord_t_effect_entry {
  /* which shape */
  int32_t type;
  /* How likely it is to land, in percent. Every variant carries it, so it is one column */
  /* and every row fills it. */
  int32_t chance;
  /* How much it takes. */
  int32_t damage;
  /* How much it gives. */
  int32_t amount;
};

struct DocShowcase_SkillRecord_t {
  /* primary index */
  int32_t index;
  /* display name */
  const char* name;
  /* which shape */
  struct DocShowcase_SkillRecord_t_effect_entry effect;
};

/* Every row of Skill.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct DocShowcase_SkillTable_t {
  DocShowcase_SkillRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} DocShowcase_SkillTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool DocShowcase_SkillLoad(DocShowcase_SkillTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void DocShowcase_SkillFree(DocShowcase_SkillTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const DocShowcase_SkillRecord_t* DocShowcase_SkillFindByIndex(
  const DocShowcase_SkillTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool DocShowcase_SkillContainsIndex(
  const DocShowcase_SkillTable_t* table, int32_t key);


/* Which shape this row's DocShowcase_StructEffect took. */
DocShowcase_StructEffectKind DocShowcase_SkillEffectKind(
  const DocShowcase_SkillRecord_t* record);

/* Fills `out` when this row is a DocShowcase_StructDamageEffect, and answers whether it was.
 *
 * The file carries 1 for that variant. */
bool DocShowcase_SkillEffectAsDamageEffect(
  const DocShowcase_SkillRecord_t* record, DocShowcase_StructDamageEffect* out);

/* Fills `out` when this row is a DocShowcase_StructHealEffect, and answers whether it was.
 *
 * The file carries 2 for that variant. */
bool DocShowcase_SkillEffectAsHealEffect(
  const DocShowcase_SkillRecord_t* record, DocShowcase_StructHealEffect* out);

/* Fills `out` when this row is a DocShowcase_StructNoEffect, and answers whether it was.
 *
 * The file carries 3 for that variant. */
bool DocShowcase_SkillEffectAsNoEffect(
  const DocShowcase_SkillRecord_t* record, DocShowcase_StructNoEffect* out);

#ifdef __cplusplus
}
#endif

#endif /* DOC_SHOWCASE_SKILL_H */
