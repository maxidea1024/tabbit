/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DOC_SHOWCASE_DECK_H
#define DOC_SHOWCASE_DECK_H

#include "tabbit/tabbit_tcb_reader.h"

#include "DocShowcase_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Spawn : G2
 *
 * An array of records, and a record inside a record.
 */
/* One element of DocShowcase_DeckRecord_t::slot. */
struct DocShowcase_DeckRecord_t_slot_entry {
  /* element 1 */
  int32_t id;
  /* element 1 */
  const char* label;
};

/* A record inside DocShowcase_DeckRecord_t::home. */
struct DocShowcase_DeckRecord_t_home_entryAt {
  /* two levels in */
  float x;
  /* two levels in */
  float y;
};

/* One element of DocShowcase_DeckRecord_t::home. */
struct DocShowcase_DeckRecord_t_home_entry {
  /* two levels in */
  struct DocShowcase_DeckRecord_t_home_entryAt at;
};

struct DocShowcase_DeckRecord_t {
  /* primary index */
  int32_t index;
  /* element 1 */
  struct DocShowcase_DeckRecord_t_slot_entry* slot;
  int32_t slot_count;
  /* two levels in */
  struct DocShowcase_DeckRecord_t_home_entry home;
};

/* Every row of Deck.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct DocShowcase_DeckTable_t {
  DocShowcase_DeckRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} DocShowcase_DeckTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool DocShowcase_DeckLoad(DocShowcase_DeckTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void DocShowcase_DeckFree(DocShowcase_DeckTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const DocShowcase_DeckRecord_t* DocShowcase_DeckFindByIndex(
  const DocShowcase_DeckTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool DocShowcase_DeckContainsIndex(
  const DocShowcase_DeckTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* DOC_SHOWCASE_DECK_H */
