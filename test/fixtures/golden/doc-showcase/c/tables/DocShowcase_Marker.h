/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DOC_SHOWCASE_MARKER_H
#define DOC_SHOWCASE_MARKER_H

#include "tabbit/tabbit_tcb_reader.h"

#include "DocShowcase_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Types : M2
 *
 * Values written as one cell and read as several.
 */
/* One element of DocShowcase_MarkerRecord_t::pos. */
struct DocShowcase_MarkerRecord_t_pos_entry {
  /* three floats */
  float x;
  /* three floats */
  float y;
  /* three floats */
  float z;
};

/* One element of DocShowcase_MarkerRecord_t::cell. */
struct DocShowcase_MarkerRecord_t_cell_entry {
  /* two ints */
  int32_t x;
  /* two ints */
  int32_t y;
};

/* One element of DocShowcase_MarkerRecord_t::tint. */
struct DocShowcase_MarkerRecord_t_tint_entry {
  /* four bytes */
  int32_t r;
  /* four bytes */
  int32_t g;
  /* four bytes */
  int32_t b;
  /* four bytes */
  int32_t a;
};

struct DocShowcase_MarkerRecord_t {
  /* primary index */
  int32_t index;
  /* three floats */
  struct DocShowcase_MarkerRecord_t_pos_entry pos;
  /* two ints */
  struct DocShowcase_MarkerRecord_t_cell_entry cell;
  /* four bytes */
  struct DocShowcase_MarkerRecord_t_tint_entry tint;
};

/* Every row of Marker.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct DocShowcase_MarkerTable_t {
  DocShowcase_MarkerRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} DocShowcase_MarkerTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool DocShowcase_MarkerLoad(DocShowcase_MarkerTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void DocShowcase_MarkerFree(DocShowcase_MarkerTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const DocShowcase_MarkerRecord_t* DocShowcase_MarkerFindByIndex(
  const DocShowcase_MarkerTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool DocShowcase_MarkerContainsIndex(
  const DocShowcase_MarkerTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* DOC_SHOWCASE_MARKER_H */
