/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DOC_SHOWCASE_STAGE_REWARD_H
#define DOC_SHOWCASE_STAGE_REWARD_H

#include "tabbit/tabbit_tcb_reader.h"

#include "DocShowcase_Forward.h"
#include "enums/DocShowcase_EnumRarity.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : StageReward : B2
 *
 * Neither key column is unique alone.
 */
struct DocShowcase_StageRewardRecord_t {
  /* which stage */
  int32_t stage;
  /* finishing rank */
  DocShowcase_Rarity_t rank;
  /* gold paid */
  int32_t gold;
  /* drop table */
  const char* drop_table;
};

/* Every row of StageReward.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct DocShowcase_StageRewardTable_t {
  DocShowcase_StageRewardRecord_t* records;
  int32_t count;

  tb_string_index_entry* by_stage_and_rank;
  tb_arena arena;
} DocShowcase_StageRewardTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool DocShowcase_StageRewardLoad(DocShowcase_StageRewardTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void DocShowcase_StageRewardFree(DocShowcase_StageRewardTable_t* table);

/* The row with the given Stage and Rank, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const DocShowcase_StageRewardRecord_t* DocShowcase_StageRewardFindByStageAndRank(
  const DocShowcase_StageRewardTable_t* table, int32_t stage_key, DocShowcase_Rarity_t rank_key);

/* Whether the table holds a row with this Stage and Rank. */
bool DocShowcase_StageRewardContainsStageAndRank(
  const DocShowcase_StageRewardTable_t* table, int32_t stage_key, DocShowcase_Rarity_t rank_key);

#ifdef __cplusplus
}
#endif

#endif /* DOC_SHOWCASE_STAGE_REWARD_H */
