/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DOC_SHOWCASE_ANIMATION_H
#define DOC_SHOWCASE_ANIMATION_H

#include "tabbit/tabbit_tcb_reader.h"

#include "DocShowcase_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Index : B2
 *
 * Keyed by name, with a second key beside it.
 */
struct DocShowcase_AnimationRecord_t {
  /* primary index */
  const char* index;
  /* secondary index */
  int32_t slot;
  /* anything */
  float blend;
};

/* Every row of Animation.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct DocShowcase_AnimationTable_t {
  DocShowcase_AnimationRecord_t* records;
  int32_t count;

  tb_string_index_entry* by_index;
  tb_index_entry* by_slot;
  tb_arena arena;
} DocShowcase_AnimationTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool DocShowcase_AnimationLoad(DocShowcase_AnimationTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void DocShowcase_AnimationFree(DocShowcase_AnimationTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const DocShowcase_AnimationRecord_t* DocShowcase_AnimationFindByIndex(
  const DocShowcase_AnimationTable_t* table, const char* key);

/* Whether the table holds a row with this Index. */
bool DocShowcase_AnimationContainsIndex(
  const DocShowcase_AnimationTable_t* table, const char* key);
/* The row with the given Slot, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const DocShowcase_AnimationRecord_t* DocShowcase_AnimationFindBySlot(
  const DocShowcase_AnimationTable_t* table, int32_t key);

/* Whether the table holds a row with this Slot. */
bool DocShowcase_AnimationContainsSlot(
  const DocShowcase_AnimationTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* DOC_SHOWCASE_ANIMATION_H */
