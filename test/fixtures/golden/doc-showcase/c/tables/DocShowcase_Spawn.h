/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DOC_SHOWCASE_SPAWN_H
#define DOC_SHOWCASE_SPAWN_H

#include "tabbit/tabbit_tcb_reader.h"

#include "DocShowcase_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Spawn : B2
 *
 * Two columns as one record.
 */
/* One element of DocShowcase_SpawnRecord_t::at. */
struct DocShowcase_SpawnRecord_t_at_entry {
  /* x position */
  float x;
  /* y position */
  float y;
};

struct DocShowcase_SpawnRecord_t {
  /* primary index */
  int32_t index;
  /* x position */
  struct DocShowcase_SpawnRecord_t_at_entry at;
  /* radius, or blank */
  float radius;
  /* Whether this row has a value for radius. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_radius;
};

/* Every row of Spawn.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct DocShowcase_SpawnTable_t {
  DocShowcase_SpawnRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} DocShowcase_SpawnTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool DocShowcase_SpawnLoad(DocShowcase_SpawnTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void DocShowcase_SpawnFree(DocShowcase_SpawnTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const DocShowcase_SpawnRecord_t* DocShowcase_SpawnFindByIndex(
  const DocShowcase_SpawnTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool DocShowcase_SpawnContainsIndex(
  const DocShowcase_SpawnTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* DOC_SHOWCASE_SPAWN_H */
