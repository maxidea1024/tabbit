/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DOC_SHOWCASE_CRAFT_H
#define DOC_SHOWCASE_CRAFT_H

#include "tabbit/tabbit_tcb_reader.h"

#include "DocShowcase_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Shop : K2
 *
 * The four shapes a reference takes.
 */
struct DocShowcase_CraftRecord_t {
  /* primary index */
  int32_t index;
  /* a whole row */
  int32_t result;
  const DocShowcase_PotionRecord_t* potion_by_result;
  /* one of its values */
  int32_t result_name_index;
  const char* result_name;
  /* several rows in one cell */
  int32_t* parts;
  const DocShowcase_PotionRecord_t** potion_by_parts;
  int32_t parts_count;
  /* a row, or none */
  int32_t substitute;
  const DocShowcase_PotionRecord_t* potion_by_substitute;
  /* Whether this row has a value for substitute. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_substitute;
};

/* Every row of Craft.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct DocShowcase_CraftTable_t {
  DocShowcase_CraftRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} DocShowcase_CraftTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool DocShowcase_CraftLoad(DocShowcase_CraftTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void DocShowcase_CraftFree(DocShowcase_CraftTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const DocShowcase_CraftRecord_t* DocShowcase_CraftFindByIndex(
  const DocShowcase_CraftTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool DocShowcase_CraftContainsIndex(
  const DocShowcase_CraftTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* DOC_SHOWCASE_CRAFT_H */
