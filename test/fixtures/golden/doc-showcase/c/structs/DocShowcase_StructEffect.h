/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef DOC_SHOWCASE_STRUCT_EFFECT_H
#define DOC_SHOWCASE_STRUCT_EFFECT_H

#include <stdbool.h>
#include <stdint.h>

/** Which shape a DocShowcase_StructEffect value took. */
typedef enum DocShowcase_StructEffectKind {
  /** The file carries 1 for this one. */
  DOC_SHOWCASE_STRUCT_EFFECT_KIND_DAMAGE_EFFECT = 1,
  /** The file carries 2 for this one. */
  DOC_SHOWCASE_STRUCT_EFFECT_KIND_HEAL_EFFECT = 2,
  /** The file carries 3 for this one. */
  DOC_SHOWCASE_STRUCT_EFFECT_KIND_NO_EFFECT = 3,
} DocShowcase_StructEffectKind;

/** One shape of DocShowcase_StructEffect. */
typedef struct DocShowcase_StructDamageEffect {
  /* How likely it is to land, in percent. Every variant carries it, so it is one column */
  /* and every row fills it. */
  int32_t chance;
  /* How much it takes. */
  int32_t damage;
} DocShowcase_StructDamageEffect;

/** One shape of DocShowcase_StructEffect. */
typedef struct DocShowcase_StructHealEffect {
  /* How likely it is to land, in percent. Every variant carries it, so it is one column */
  /* and every row fills it. */
  int32_t chance;
  /* How much it gives. */
  int32_t amount;
} DocShowcase_StructHealEffect;

/** One shape of DocShowcase_StructEffect. */
typedef struct DocShowcase_StructNoEffect {
  /* How likely it is to land, in percent. Every variant carries it, so it is one column */
  /* and every row fills it. */
  int32_t chance;
} DocShowcase_StructNoEffect;

#endif /* DOC_SHOWCASE_STRUCT_EFFECT_H */
