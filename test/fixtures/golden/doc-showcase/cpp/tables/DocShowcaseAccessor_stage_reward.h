// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_DOCSHOWCASEACCESSOR_STAGE_REWARD_H
#define TABBIT_GENERATED_DOCSHOWCASEACCESSOR_STAGE_REWARD_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "DocShowcaseAccessor_forward.h"
#include "enums/DocShowcaseAccessor_enum_rarity.h"
// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : StageReward : B2
/// Neither key column is unique alone.
struct StageRewardRecord {
  /// which stage
  std::int32_t stage = 0;
  /// finishing rank
  Rarity rank = static_cast<Rarity>(0);
  /// gold paid
  std::int32_t gold = 0;
  /// drop table
  std::string drop_table;
};

/// Neither key column is unique alone.
class StageRewardTable {
 public:
  const std::vector<StageRewardRecord>& records() const { return records_; }

  /// How many rows the table holds.
  std::size_t size() const { return records_.size(); }

  /// Whether the table holds no rows.
  bool empty() const { return records_.empty(); }

  /// The rows, in the order the file wrote them - what a range-for binds to.
  ///
  /// A refresh replaces the vector rather than appending to it, so these are the ordinary
  /// vector invalidation rules: a loop running across a reload is reading a container that
  /// has been assigned to.
  typename std::vector<StageRewardRecord>::const_iterator begin() const {
    return records_.begin();
  }

  typename std::vector<StageRewardRecord>::const_iterator end() const {
    return records_.end();
  }

  /// Joins the columns of the Stage and Rank key into the text the map is keyed by.
  static std::string key_of_stage_and_rank(std::int32_t stage_key, Rarity rank_key) {
    const std::string parts[] = {
      std::to_string(stage_key),
      std::to_string(static_cast<std::int64_t>(rank_key)),
    };

    std::string text;

    for (const std::string& part : parts) {
      text += std::to_string(part.size());
      text += ':';
      text += part;
    }

    return text;
  }

  /// The row with this Stage and Rank, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const StageRewardRecord* find_by_stage_and_rank(std::int32_t stage_key, Rarity rank_key) const {
    const auto it = by_stage_and_rank_.find(key_of_stage_and_rank(stage_key, rank_key));
    return it == by_stage_and_rank_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Stage and Rank, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_stage_and_rank(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const StageRewardRecord& get_by_stage_and_rank_or_throw(std::int32_t stage_key, Rarity rank_key) const {
    const StageRewardRecord* found = find_by_stage_and_rank(stage_key, rank_key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `StageReward` that corresponds to "
                    "field `Stage and Rank` value ") + key_of_stage_and_rank(stage_key, rank_key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Stage and Rank.
  bool contains_stage_and_rank(std::int32_t stage_key, Rarity rank_key) const {
    return by_stage_and_rank_.find(key_of_stage_and_rank(stage_key, rank_key)) != by_stage_and_rank_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<StageRewardRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "StageReward.Stage", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "StageReward.Stage");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].stage = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "StageReward.Rank", tabbit::kKindScalar, false, {tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "StageReward.Rank");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].rank = static_cast<Rarity>(value);
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "StageReward.Gold", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "StageReward.Gold");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].gold = value;
            }
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "StageReward.DropTable", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "StageReward.DropTable");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].drop_table = value;
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class DocShowcaseAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<StageRewardRecord>&& records) {
    std::unordered_map<std::string, std::size_t> stage_and_rank_map;
    stage_and_rank_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      stage_and_rank_map.emplace(
        key_of_stage_and_rank(records[i].stage, records[i].rank), i);
    }

    records_ = std::move(records);
    by_stage_and_rank_ = std::move(stage_and_rank_map);
  }

  std::vector<StageRewardRecord> records_;
  std::unordered_map<std::string, std::size_t> by_stage_and_rank_;
};

#endif  // TABBIT_GENERATED_DOCSHOWCASEACCESSOR_STAGE_REWARD_H
