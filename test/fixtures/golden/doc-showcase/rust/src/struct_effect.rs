// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

/// One value of `Effect`, whichever shape it took.
///
/// Narrow it with `match`, which is exhaustive over an enum.
#[derive(Debug, Clone, PartialEq)]
pub enum Effect {
    /// One shape of `Effect`.
    ///
    /// The file carries `1` for it. That number rather than the name
    /// is what lets this variant be renamed without a deployed reader reading a different one.
    DamageEffect(DamageEffect),
    /// One shape of `Effect`.
    ///
    /// The file carries `2` for it. That number rather than the name
    /// is what lets this variant be renamed without a deployed reader reading a different one.
    HealEffect(HealEffect),
    /// One shape of `Effect`.
    ///
    /// The file carries `3` for it. That number rather than the name
    /// is what lets this variant be renamed without a deployed reader reading a different one.
    NoEffect(NoEffect),
}

impl Effect {
    /// How likely it is to land, in percent. Every variant carries it, so it is one column
    /// and every row fills it.
    pub fn chance(&self) -> &i32 {
        match self {
            Effect::DamageEffect(it) => &it.chance,
            Effect::HealEffect(it) => &it.chance,
            Effect::NoEffect(it) => &it.chance,
        }
    }
}

/// The payload of [`Effect::DamageEffect`].
#[derive(Debug, Clone, PartialEq, Default)]
pub struct DamageEffect {
    /// How likely it is to land, in percent. Every variant carries it, so it is one column
    /// and every row fills it.
    pub chance: i32,
    /// How much it takes.
    pub damage: i32,
}

/// The payload of [`Effect::HealEffect`].
#[derive(Debug, Clone, PartialEq, Default)]
pub struct HealEffect {
    /// How likely it is to land, in percent. Every variant carries it, so it is one column
    /// and every row fills it.
    pub chance: i32,
    /// How much it gives.
    pub amount: i32,
}

/// The payload of [`Effect::NoEffect`].
#[derive(Debug, Clone, PartialEq, Default)]
pub struct NoEffect {
    /// How likely it is to land, in percent. Every variant carries it, so it is one column
    /// and every row fills it.
    pub chance: i32,
}
