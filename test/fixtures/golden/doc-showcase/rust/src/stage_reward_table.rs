// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

use std::collections::HashMap;
use std::path::Path;
use crate::tabbit;
use crate::enum_rarity::Rarity;

// Generated from test/fixtures/xlsx/doc-showcase/doc-showcase.xlsx : Sides : B2
/// Neither key column is unique alone.
#[derive(Clone, Debug, Default)]
pub struct StageRewardRecord {
    /// which stage
    pub stage: i32,
    /// finishing rank
    pub rank: Rarity,
    /// gold paid
    pub gold: i32,
    /// drop table
    pub drop_table: String,
}

impl StageRewardRecord {
}

/// Every row of StageReward.
#[derive(Clone, Debug, Default)]
pub struct StageRewardTable {
    records: Vec<StageRewardRecord>,
    by_stage_and_rank: HashMap<String, usize>,
}

impl StageRewardTable {
    /// Every row, in the order the sheet declared them.
    pub fn records(&self) -> &[StageRewardRecord] {
        &self.records
    }

    /// How many rows the table holds.
    pub fn len(&self) -> usize {
        self.records.len()
    }

    /// Whether the table holds no rows.
    ///
    /// Paired with `len` because clippy refuses one without the other, and the generated
    /// crate is built with warnings denied.
    pub fn is_empty(&self) -> bool {
        self.records.is_empty()
    }

    /// The rows, in the order the file wrote them.
    pub fn iter(&self) -> std::slice::Iter<'_, StageRewardRecord> {
        self.records.iter()
    }

    /// Joins the columns of the Stage and Rank key into the text the map is keyed by.
    fn key_of_stage_and_rank(stage_key: i32, rank_key: Rarity) -> String {
        let parts: [String; 2] = [
            stage_key.to_string(),
            rank_key.value().to_string(),
        ];

        let mut text = String::new();

        for part in parts.iter() {
            text.push_str(&part.len().to_string());
            text.push(':');
            text.push_str(part);
        }

        text
    }

    /// The row with this Stage and Rank, or None when the table has none.
    ///
    /// The lookup to reach for when a missing row is an ordinary answer - an optional
    /// reference, a key that came from user input.
    pub fn find_by_stage_and_rank(&self, stage_key: i32, rank_key: Rarity) -> Option<&StageRewardRecord> {
        self.by_stage_and_rank.get(&Self::key_of_stage_and_rank(stage_key, rank_key)).map(|position| &self.records[*position])
    }

    /// The row with this Stage and Rank, or an error naming what was missing.
    ///
    /// For a key that has to be there - one from another table, or a constant. Rust has no
    /// exception to throw, so where the other languages name a throw this one names the
    /// error it returns.
    pub fn get_by_stage_and_rank_or_error(&self, stage_key: i32, rank_key: Rarity) -> tabbit::Result<&StageRewardRecord> {
        self.by_stage_and_rank
            .get(&Self::key_of_stage_and_rank(stage_key, rank_key))
            .map(|position| &self.records[*position])
            .ok_or_else(|| tabbit::Error::RecordNotFound {
                table: "StageReward",
                field: "Stage and Rank",
                key: format!("({:?}, {:?})", stage_key, rank_key),
            })
    }

    /// Whether the table holds a row with this Stage and Rank.
    pub fn contains_stage_and_rank(&self, stage_key: i32, rank_key: Rarity) -> bool {
        self.by_stage_and_rank.contains_key(&Self::key_of_stage_and_rank(stage_key, rank_key))
    }

    /// Loads the table from a .tcb file written by Tabbit.
    pub fn read(&mut self, filename: &Path) -> tabbit::Result<()> {
        // Mutable, and it outlives the reader on purpose: opening the envelope decrypts in
        // place and hands back a borrow of these same bytes rather than a copy of them.
        let mut data = tabbit::read_all_bytes(filename)?;

        // Unconditionally, and with whatever `ENCRYPTION_KEY` and `MAC_KEY` hold - which is
        // unset unless the project set them. A file that is neither encrypted nor signed
        // comes back from this untouched, so the load path is the same either way and there
        // is no condition here that could be the wrong way round.
        // spec/wire/tcb-mac-and-signature.md.
        let key: &[u8] = crate::ENCRYPTION_KEY.get().map(Vec::as_slice).unwrap_or(&[]);
        let mac_key: &[u8] = crate::MAC_KEY.get().map(Vec::as_slice).unwrap_or(&[]);

        let mut reader = tabbit::Reader::new(tabbit::open(
            &mut data,
            key,
            mac_key,
            crate::VERIFY_MAC.load(std::sync::atomic::Ordering::Relaxed),
        )?);

        // Column by column, matched by tag rather than position: a column this build
        // does not know is skipped by its block length, and one whose type changed
        // incompatibly fails naming the field.
        let header = tabbit::read_table_header(&mut reader)?;
        let count = header.row_count.max(0) as usize;

        // Read into rows of its own and assigned at the end. Reading a table that is
        // already loaded is a refresh, and one that turns out to be unreadable has to
        // leave the rows already there alone - `?` returns before anything is replaced.
        let mut records = vec![StageRewardRecord::default(); count];

        for column in &header.columns {
            let block_end = reader.position() + column.byte_length.max(0) as usize;

            match column.tag {
                1 => {
                    tabbit::check_column(column, "StageReward.Stage", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_I32, tabbit::ELEMENT_VARINT])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "StageReward.Stage")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_i32((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].stage = value;
                            at += 1;
                        }
                    }
                }
                2 => {
                    tabbit::check_column(column, "StageReward.Rank", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_VARINT])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "StageReward.Rank")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_i32((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].rank = Rarity::from_value(value).unwrap_or_default();
                            at += 1;
                        }
                    }
                }
                3 => {
                    tabbit::check_column(column, "StageReward.Gold", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_I32, tabbit::ELEMENT_VARINT])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "StageReward.Gold")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_i32((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].gold = value;
                            at += 1;
                        }
                    }
                }
                4 => {
                    tabbit::check_column(column, "StageReward.DropTable", tabbit::KIND_SCALAR, false, &[tabbit::ELEMENT_STRING])?;
                    let mut cursor = tabbit::TcbColumnCursor::new(&mut reader, column, header.row_count, "StageReward.DropTable")?;
                    let mut at = 0usize;
                    while at < records.len() {
                        let (n, value) = cursor.next_same_string((records.len() - at) as i32)?;
                        for _ in 0..n {
                            records[at].drop_table = value.clone();
                            at += 1;
                        }
                    }
                }
                _ => {
                    // A column added after this code was generated.
                    reader.skip(column.byte_length)?;
                }
            }

            tabbit::check_block_end(&reader, column, block_end)?;
        }

        let by_stage_and_rank = records
            .iter()
            .enumerate()
            .map(|(position, record)| (
                Self::key_of_stage_and_rank(record.stage, record.rank),
                position))
            .collect();

        // Published, now that every column read.
        self.records = records;
        self.by_stage_and_rank = by_stage_and_rank;

        Ok(())
    }
}

/// Lets a borrow of the table be iterated directly - `for record in &table`.
///
/// Separate from `iter` rather than instead of it, because a method is what an iterator
/// chain starts from and the trait is what a `for` reaches for; a crate that has one and
/// not the other reads as unfinished from whichever side the caller came in on.
impl<'a> IntoIterator for &'a StageRewardTable {
    type Item = &'a StageRewardRecord;
    type IntoIter = std::slice::Iter<'a, StageRewardRecord>;

    fn into_iter(self) -> Self::IntoIter {
        self.records.iter()
    }
}
