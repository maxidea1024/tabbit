// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package tabbit.fixtures.nullableelements;

import java.nio.file.Path;
import java.util.AbstractMap;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;

import tabbit.TcbReader;

/** Every row of Listing. */
public final class ListingTable implements Iterable<ListingRecord> {
    private List<ListingRecord> records = new ArrayList<>();
    private Map<Integer, ListingRecord> byIndex = new HashMap<>();

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    public List<ListingRecord> records() {
        return records;
    }

    /** How many rows the table holds. */
    public int size() {
        return records.size();
    }

    /**
     * The rows, in the order the file wrote them.
     *
     * The list reference is read once, here. A refresh replaces the reference rather than
     * its contents, so a loop already running keeps the rows it started with.
     */
    @Override
    public Iterator<ListingRecord> iterator() {
        return records.iterator();
    }

    /**
     * The row with this Index, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public ListingRecord findByIndex(int key) {
        return byIndex.get(key);
    }

    /**
     * The row with this Index, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByIndex(key).name() at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public ListingRecord getByIndexOrThrow(int key) {
        ListingRecord record = byIndex.get(key);

        if (record == null) {
            throw new TcbReader.RecordNotFoundException(
                "there is no record in table `Listing` that corresponds to field "
                + "`Index` value " + key);
        }

        return record;
    }

    /** Whether the table holds a row with this Index. */
    public boolean containsIndex(int key) {
        return byIndex.containsKey(key);
    }


    /**
     * Each row with the Index it is keyed by -
     * `for (var entry : table.entries())`.
     *
     * What this saves a caller is not the key value - the row carries it - but having to
     * know which column the key is: Index here, something else in the next
     * table.
     *
     * The rows come in the order the file wrote them rather than the order the map holds
     * them, which makes this and iterating the table agree. Only the primary key has this:
     * a table keyed by several columns together has no single key value to pair a row with.
     */
    public Iterable<Map.Entry<Integer, ListingRecord>> entries() {
        final List<ListingRecord> rows = records;

        return () -> new Iterator<Map.Entry<Integer, ListingRecord>>() {
            private final Iterator<ListingRecord> source = rows.iterator();

            @Override
            public boolean hasNext() {
                return source.hasNext();
            }

            @Override
            public Map.Entry<Integer, ListingRecord> next() {
                final ListingRecord row = source.next();

                return new AbstractMap.SimpleImmutableEntry<>(row.index, row);
            }
        };
    }

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    public void read(Path filename) {
        // Opened unconditionally, and with whatever NullableElementsAccessor.encryptionKey and
        // NullableElementsAccessor.macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from this untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/wire/tcb-mac-and-signature.md.
        TcbReader reader = new TcbReader(TcbReader.open(
            TcbReader.readAllBytes(filename), NullableElementsAccessor.encryptionKey,
            NullableElementsAccessor.macKey, NullableElementsAccessor.verifyMac));
        TcbReader.Header header = TcbReader.readTableHeader(reader);
        int count = header.rowCount;
        TcbReader.ColumnCursor cursor;
        byte[] presence;
        byte[] elementPresence;
        int elementAt;

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        List<ListingRecord> loaded = new ArrayList<>(count);
        Map<Integer, ListingRecord> loadedByIndex = new HashMap<>(count * 2);

        for (int i = 0; i < count; i++) {
            loaded.add(new ListingRecord());
        }

        for (TcbReader.Column column : header.columns) {
            int blockEnd = reader.position() + column.byteLength;

            switch (column.tag) {
                case 1: {
                    TcbReader.checkColumn(column, "Listing.Index", TcbReader.KIND_SCALAR, false, TcbReader.ELEMENT_I32, TcbReader.ELEMENT_VARINT);
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Listing.Index");
                    for (int i = 0; i < count; ) {
                        int n = cursor.nextSameI32(count - i);
                        for (; n > 0; n--, i++) {
                            loaded.get(i).index = cursor.runSameValue;
                        }
                    }
                    break;
                }
                case 2: {
                    TcbReader.checkColumn(column, "Listing.Name", TcbReader.KIND_SCALAR, false, TcbReader.ELEMENT_STRING);
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Listing.Name");
                    for (int i = 0; i < count; ) {
                        int n = cursor.nextSameString(count - i);
                        for (; n > 0; n--, i++) {
                            loaded.get(i).name = cursor.runSameText;
                        }
                    }
                    break;
                }
                case 3: {
                    TcbReader.checkColumn(column, "Listing.Plain", TcbReader.KIND_ARRAY, false, TcbReader.ELEMENT_I32, TcbReader.ELEMENT_VARINT);
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Listing.Plain");
                    for (ListingRecord record : loaded) {
                        int elementCount;
                        elementCount = cursor.nextLength();
                        record.plain = new int[elementCount];
                        for (int j = 0; j < elementCount; j++) {
                            record.plain[j] = cursor.nextI32();
                        }
                    }
                    break;
                }
                case 4: {
                    TcbReader.checkColumn(column, "Listing.Maybe", TcbReader.KIND_ARRAY, true, TcbReader.ELEMENT_I32, TcbReader.ELEMENT_VARINT);
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    presence = TcbReader.readPresence(reader, column, count);
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Listing.Maybe");
                    for (ListingRecord record : loaded) {
                        int elementCount;
                        elementCount = cursor.nextLength();
                        record.maybe = new int[elementCount];
                        for (int j = 0; j < elementCount; j++) {
                            record.maybe[j] = cursor.nextI32();
                        }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (int i = 0; i < count; i++) {
                        ListingRecord record = loaded.get(i);
                        record.hasMaybe = TcbReader.isPresent(presence, i);

                        if (!record.hasMaybe) {
                            record.maybe = new int[0];
                        }
                    }
                    break;
                }
                case 5: {
                    TcbReader.checkColumnWithElements(column, "Listing.Holes", TcbReader.KIND_ARRAY, false, TcbReader.ELEMENT_I32, TcbReader.ELEMENT_VARINT);
                    // Behind the row bitmap and in front of the values, walked with a counter
                    // that steps once per element of every row.
                    // spec/types/nullable-array-elements.md.
                    elementPresence = TcbReader.readElementPresence(reader, column);
                    elementAt = 0;
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Listing.Holes");
                    for (ListingRecord record : loaded) {
                        int elementCount;
                        elementCount = cursor.nextLength();
                        record.holes = new int[elementCount];
                        record.hasHolesAt = new boolean[elementCount];
                        for (int j = 0; j < elementCount; j++) {
                            record.holes[j] = cursor.nextI32();
                            record.hasHolesAt[j] =
                                TcbReader.isPresent(elementPresence, elementAt++);
                        }
                    }
                    break;
                }
                case 6: {
                    TcbReader.checkColumnWithElements(column, "Listing.Both", TcbReader.KIND_ARRAY, true, TcbReader.ELEMENT_I32, TcbReader.ELEMENT_VARINT);
                    // The bitmap is at the front of the block, so it is read before the
                    // values. The values are written for every row either way, which is what
                    // lets the read shapes below stay as they are.
                    presence = TcbReader.readPresence(reader, column, count);
                    // Behind the row bitmap and in front of the values, walked with a counter
                    // that steps once per element of every row.
                    // spec/types/nullable-array-elements.md.
                    elementPresence = TcbReader.readElementPresence(reader, column);
                    elementAt = 0;
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Listing.Both");
                    for (ListingRecord record : loaded) {
                        int elementCount;
                        elementCount = cursor.nextLength();
                        record.both = new int[elementCount];
                        record.hasBothAt = new boolean[elementCount];
                        for (int j = 0; j < elementCount; j++) {
                            record.both[j] = cursor.nextI32();
                            record.hasBothAt[j] =
                                TcbReader.isPresent(elementPresence, elementAt++);
                        }
                    }

                    // A pass of its own rather than a line inside each read shape, so the row
                    // index is worked out once. The block carries a value for every row, so
                    // an absent one has just been given whatever was there; putting the empty
                    // value back is what makes this path agree with the JSON one.
                    for (int i = 0; i < count; i++) {
                        ListingRecord record = loaded.get(i);
                        record.hasBoth = TcbReader.isPresent(presence, i);

                        if (!record.hasBoth) {
                            record.both = new int[0];
                        }
                    }
                    break;
                }
                case 7: {
                    TcbReader.checkColumnWithElements(column, "Listing.Words", TcbReader.KIND_ARRAY, false, TcbReader.ELEMENT_STRING);
                    // Behind the row bitmap and in front of the values, walked with a counter
                    // that steps once per element of every row.
                    // spec/types/nullable-array-elements.md.
                    elementPresence = TcbReader.readElementPresence(reader, column);
                    elementAt = 0;
                    cursor = new TcbReader.ColumnCursor(reader, column, count, "Listing.Words");
                    for (ListingRecord record : loaded) {
                        int elementCount;
                        elementCount = cursor.nextLength();
                        record.words = new String[elementCount];
                        record.hasWordsAt = new boolean[elementCount];
                        for (int j = 0; j < elementCount; j++) {
                            record.words[j] = cursor.nextString();
                            record.hasWordsAt[j] =
                                TcbReader.isPresent(elementPresence, elementAt++);
                        }
                    }
                    break;
                }
                default:
                    // A column added after this code was generated.
                    reader.skip(column.byteLength);
                    break;
            }

            TcbReader.checkBlockEnd(reader, column, blockEnd);
        }

        for (ListingRecord record : loaded) {
            loadedByIndex.put(record.index, record);
        }

        // Published, now that every column read.
        records = loaded;
        byIndex = loadedByIndex;
    }
}
