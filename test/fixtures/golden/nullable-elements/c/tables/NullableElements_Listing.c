/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "tables/NullableElements_Listing.h"

#include <string.h>

static bool NullableElements_ListingParse(NullableElements_ListingTable_t* table, tb_reader* reader) {
  int32_t row;
  int32_t at;
  tb_column* columns = NULL;
  int32_t column_count = 0;
  tb_cursor cursor;
  /* Declared with the other locals: C89 wants them at the top of the function, and the
   * switch's cases share its scope either way. */
  const uint8_t* presence = NULL;
  const uint8_t* element_presence = NULL;
  int32_t element_at = 0;

  if (!tb_read_table_header(reader, &table->count, &columns, &column_count))
    return false;

  if (table->count > 0) {
    table->records = (NullableElements_ListingRecord_t*)tb_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->records);

    if (table->records == NULL)
      return tb_fail_with(reader, "out of memory allocating the rows");

    table->by_index = (tb_index_entry*)tb_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->by_index);

    if (table->by_index == NULL)
      return tb_fail_with(reader, "out of memory allocating the Index index");
  }

  /* The arena hands back zeroed memory, so every member starts at its zero - which for
   * a string is NULL, and NULL is what reaches printf when a column the file does not
   * carry is read. An empty string instead: a literal, so there is nothing to free and
   * nothing to allocate. */
  for (row = 0; row < table->count; ++row) {
    NullableElements_ListingRecord_t* record = &table->records[row];

    record->name = "";
  }

  /* Column by column, matched by tag rather than by position: a column this build has
   * no member for is skipped by its declared length, and one whose type no longer fits
   * the member stops the read naming the field. Rows arrive with every member at an
   * empty value, so one the file has nothing for keeps it. */
  for (at = 0; at < column_count && !tb_failed(reader); ++at) {
    const tb_column* column = &columns[at];
    int32_t block_end = reader->position + column->byte_length;

    switch (column->tag) {

    case 1:
      (void)tb_check_column(reader, column, "Listing.Index", TB_KIND_SCALAR, 1, false, TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT));

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Listing.Index");

      {
        int32_t run_length = 0;
        int32_t value = 0;

        row = 0;

        while (row < table->count && !tb_failed(reader)) {
          if (!tb_cursor_next_same_i32(&cursor, table->count - row, &run_length, &value))
            break;

          for (; run_length > 0; --run_length, ++row)
            table->records[row].index = value;
        }
      }
      break;

    case 2:
      (void)tb_check_column(reader, column, "Listing.Name", TB_KIND_SCALAR, 1, false, TB_ELEMENT_MASK(TB_ELEMENT_STRING));

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Listing.Name");

      {
        int32_t run_length = 0;
        const char* value = NULL;

        row = 0;

        while (row < table->count && !tb_failed(reader)) {
          if (!tb_cursor_next_same_string(&cursor, table->count - row, &run_length, &value))
            break;

          for (; run_length > 0; --run_length, ++row)
            table->records[row].name = value;
        }
      }
      break;

    case 3:
      (void)tb_check_column(reader, column, "Listing.Plain", TB_KIND_VAR_ARRAY, 0, false, TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT));

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Listing.Plain");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        NullableElements_ListingRecord_t* record = &table->records[row];
        int32_t element_count = 0;
        int32_t element;

        (void)tb_cursor_next_length(&cursor, &element_count);

        record->plain_count = element_count;
        record->plain = (int32_t*)tb_arena_alloc(
          &table->arena, (size_t)element_count * sizeof *record->plain);

        if (element_count > 0 && record->plain == NULL)
          return tb_fail_with(reader, "out of memory allocating an array");

        for (element = 0; element < element_count && !tb_failed(reader); ++element)
          (void)tb_cursor_next_i32(&cursor, &record->plain[element]);
      }
      break;

    case 4:
      (void)tb_check_column(reader, column, "Listing.Maybe", TB_KIND_VAR_ARRAY, 0, true, TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT));

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Listing.Maybe");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        NullableElements_ListingRecord_t* record = &table->records[row];
        int32_t element_count = 0;
        int32_t element;

        (void)tb_cursor_next_length(&cursor, &element_count);

        record->maybe_count = element_count;
        record->maybe = (int32_t*)tb_arena_alloc(
          &table->arena, (size_t)element_count * sizeof *record->maybe);

        if (element_count > 0 && record->maybe == NULL)
          return tb_fail_with(reader, "out of memory allocating an array");

        for (element = 0; element < element_count && !tb_failed(reader); ++element)
          (void)tb_cursor_next_i32(&cursor, &record->maybe[element]);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_maybe = tb_is_present(presence, row);

        if (!table->records[row].has_maybe)
          { table->records[row].maybe = NULL; table->records[row].maybe_count = 0; }
      }
      break;

    case 5:
      (void)tb_check_column_elements(reader, column, "Listing.Holes", TB_KIND_VAR_ARRAY, 0, false, TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT), true);
      /* Behind the row bitmap and in front of the values, walked with a counter that steps
       * once per element of every row. spec/nullable-array-elements.md. */
      (void)tb_read_element_presence(reader, column, &element_presence);
      element_at = 0;

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Listing.Holes");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        NullableElements_ListingRecord_t* record = &table->records[row];
        int32_t element_count = 0;
        int32_t element;

        (void)tb_cursor_next_length(&cursor, &element_count);

        record->holes_count = element_count;
        record->holes = (int32_t*)tb_arena_alloc(
          &table->arena, (size_t)element_count * sizeof *record->holes);

        if (element_count > 0 && record->holes == NULL)
          return tb_fail_with(reader, "out of memory allocating an array");

        {
          bool* element_presence_out = (bool*)tb_arena_alloc(
            &table->arena, (size_t)element_count * sizeof(bool));

          if (element_count > 0 && element_presence_out == NULL)
            return tb_fail_with(reader, "out of memory allocating an element presence array");

          record->has_holes_at = element_presence_out;

          for (element = 0; element < element_count; ++element)
            element_presence_out[element] = tb_is_present(element_presence, element_at + element);
        }

        element_at += element_count;

        for (element = 0; element < element_count && !tb_failed(reader); ++element)
          (void)tb_cursor_next_i32(&cursor, &record->holes[element]);
      }
      break;

    case 6:
      (void)tb_check_column_elements(reader, column, "Listing.Both", TB_KIND_VAR_ARRAY, 0, true, TB_ELEMENT_MASK(TB_ELEMENT_I32) | TB_ELEMENT_MASK(TB_ELEMENT_VARINT), true);

      /* The bitmap is at the front of the block, so it is read before the values. The
       * values are written for every row either way, which is what lets the read shapes
       * below stay as they are. */
      (void)tb_read_presence(reader, column, table->count, &presence);
      /* Behind the row bitmap and in front of the values, walked with a counter that steps
       * once per element of every row. spec/nullable-array-elements.md. */
      (void)tb_read_element_presence(reader, column, &element_presence);
      element_at = 0;

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Listing.Both");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        NullableElements_ListingRecord_t* record = &table->records[row];
        int32_t element_count = 0;
        int32_t element;

        (void)tb_cursor_next_length(&cursor, &element_count);

        record->both_count = element_count;
        record->both = (int32_t*)tb_arena_alloc(
          &table->arena, (size_t)element_count * sizeof *record->both);

        if (element_count > 0 && record->both == NULL)
          return tb_fail_with(reader, "out of memory allocating an array");

        {
          bool* element_presence_out = (bool*)tb_arena_alloc(
            &table->arena, (size_t)element_count * sizeof(bool));

          if (element_count > 0 && element_presence_out == NULL)
            return tb_fail_with(reader, "out of memory allocating an element presence array");

          record->has_both_at = element_presence_out;

          for (element = 0; element < element_count; ++element)
            element_presence_out[element] = tb_is_present(element_presence, element_at + element);
        }

        element_at += element_count;

        for (element = 0; element < element_count && !tb_failed(reader); ++element)
          (void)tb_cursor_next_i32(&cursor, &record->both[element]);
      }

      /* A pass of its own rather than a line inside each read shape, so the row index is
       * worked out once. The block carries a value for every row, so an absent one has
       * just been given whatever was there. */
      for (row = 0; row < table->count; ++row) {
        table->records[row].has_both = tb_is_present(presence, row);

        if (!table->records[row].has_both)
          { table->records[row].both = NULL; table->records[row].both_count = 0; }
      }
      break;

    case 7:
      (void)tb_check_column_elements(reader, column, "Listing.Words", TB_KIND_VAR_ARRAY, 0, false, TB_ELEMENT_MASK(TB_ELEMENT_STRING), true);
      /* Behind the row bitmap and in front of the values, walked with a counter that steps
       * once per element of every row. spec/nullable-array-elements.md. */
      (void)tb_read_element_presence(reader, column, &element_presence);
      element_at = 0;

      (void)tb_cursor_init(&cursor, reader, column, table->count, "Listing.Words");

      for (row = 0; row < table->count && !tb_failed(reader); ++row) {
        NullableElements_ListingRecord_t* record = &table->records[row];
        int32_t element_count = 0;
        int32_t element;

        (void)tb_cursor_next_length(&cursor, &element_count);

        record->words_count = element_count;
        record->words = (const char**)tb_arena_alloc(
          &table->arena, (size_t)element_count * sizeof *record->words);

        if (element_count > 0 && record->words == NULL)
          return tb_fail_with(reader, "out of memory allocating an array");

        {
          bool* element_presence_out = (bool*)tb_arena_alloc(
            &table->arena, (size_t)element_count * sizeof(bool));

          if (element_count > 0 && element_presence_out == NULL)
            return tb_fail_with(reader, "out of memory allocating an element presence array");

          record->has_words_at = element_presence_out;

          for (element = 0; element < element_count; ++element)
            element_presence_out[element] = tb_is_present(element_presence, element_at + element);
        }

        element_at += element_count;

        for (element = 0; element < element_count && !tb_failed(reader); ++element)
          (void)tb_cursor_next_string(&cursor, &record->words[element]);
      }
      break;

    default:
      /* A column this build has no member for: added to the schema after the code
       * was generated, or removed from the code while the data still carries it.
       * The block's declared length is the whole of what skipping needs to know. */
      (void)tb_skip(reader, column->byte_length);
      break;
    }

    if (!tb_check_block_end(reader, column, block_end))
      return false;
  }

  if (tb_failed(reader))
    return false;

  /* The keys can only be gathered once every column has been read: the column that
   * carries one arrives wherever the file happens to put it. */
  for (row = 0; row < table->count; ++row) {
    table->by_index[row].key = table->records[row].index;
    table->by_index[row].position = row;
  }

  tb_index_sort(table->by_index, table->count);

  return true;
}

bool NullableElements_ListingLoad(NullableElements_ListingTable_t* table, const char* filename,
                   char* error, size_t error_size) {
  uint8_t* bytes = NULL;
  int32_t length = 0;
  const uint8_t* opened = NULL;
  int32_t opened_length = 0;
  tb_reader reader;
  bool ok;

  /* Loaded beside whatever the table is already holding, arena and all, and swapped in
   * only if it worked. That is what makes a reload safe: reading a patched file that
   * turns out to be truncated - or to carry a column this build cannot read - leaves the
   * rows the caller has pointers to exactly where they were.
   *
   * It also has to be this way round for the memory. The table owns its arena, and
   * zeroing the table before reading - which is what this did - dropped that arena
   * without freeing it, so every reload leaked the whole of the previous load. */
  NullableElements_ListingTable_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (!tb_read_all_bytes(filename, &bytes, &length)) {
    tb_copy_error(error, error_size, filename, "cannot be read");
    return false;
  }

  /* Opened unconditionally, with whatever NullableElements_EncryptionKey and
   * NullableElements_MacKey hold - NULL unless the project set them. A file that is
   * neither encrypted nor signed comes back from this untouched, so the load path is the
   * same either way and there is no condition here that could be the wrong way round.
   * spec/tcb-mac-and-signature.md.
   *
   * What comes back is a window onto `bytes` rather than a copy of it, which is why the
   * buffer is still what tb_free_bytes is given below. */
  if (!tb_open(bytes, length, NullableElements_EncryptionKey,
        NullableElements_EncryptionKeyLength, NullableElements_MacKey,
        NullableElements_MacKeyLength, NullableElements_VerifyMac,
        &opened, &opened_length, error, error_size)) {
    tb_free_bytes(bytes);
    return false;
  }

  tb_reader_init(&reader, opened, opened_length, &loaded.arena);

  ok = NullableElements_ListingParse(&loaded, &reader);

  if (ok) {
    /* The previous load goes now, and not before: its arena holds the strings and
     * arrays the caller was reading up to this line. */
    NullableElements_ListingFree(table);
    *table = loaded;
  } else {
    tb_copy_error(error, error_size, filename, tb_error(&reader));

    /* Nothing half-read is kept, and the table is untouched. */
    NullableElements_ListingFree(&loaded);
  }

  /* The strings were copied into the arena, so the file buffer goes either way. */
  tb_free_bytes(bytes);

  return ok;
}

void NullableElements_ListingFree(NullableElements_ListingTable_t* table) {
  tb_arena_free(&table->arena);

  memset(table, 0, sizeof *table);
}

const NullableElements_ListingRecord_t* NullableElements_ListingFindByIndex(
  const NullableElements_ListingTable_t* table, int32_t key) {
  int32_t position = tb_index_find(table->by_index, table->count, key);

  return position < 0 ? NULL : &table->records[position];
}

bool NullableElements_ListingContainsIndex(
  const NullableElements_ListingTable_t* table, int32_t key) {
  return tb_index_find(table->by_index, table->count, key) >= 0;
}
