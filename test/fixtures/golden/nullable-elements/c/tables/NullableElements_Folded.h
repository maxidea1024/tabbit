/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef NULLABLE_ELEMENTS_FOLDED_H
#define NULLABLE_ELEMENTS_FOLDED_H

#include "tabbit/tabbit_tcb_reader.h"

#include "NullableElements_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/nullable-elements/nullable-elements.xlsx : Arrays : J2
 *
 * Numbered columns that fold into one array whose elements may be absent.
 */
struct NullableElements_FoldedRecord_t {
  /* primary index */
  int32_t index;
  /* element 1 - and the group's answer */
  const char** tag;
  int32_t tag_count;
  /* Which of tag's elements have a value, one bool per element, or NULL where
   * the file did not carry the column. spec/nullable-array-elements.md. */
  const bool* has_tag_at;
};

/* Every row of Folded.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct NullableElements_FoldedTable_t {
  NullableElements_FoldedRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} NullableElements_FoldedTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool NullableElements_FoldedLoad(NullableElements_FoldedTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void NullableElements_FoldedFree(NullableElements_FoldedTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const NullableElements_FoldedRecord_t* NullableElements_FoldedFindByIndex(
  const NullableElements_FoldedTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool NullableElements_FoldedContainsIndex(
  const NullableElements_FoldedTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* NULLABLE_ELEMENTS_FOLDED_H */
