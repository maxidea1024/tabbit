/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef NULLABLE_ELEMENTS_LISTING_H
#define NULLABLE_ELEMENTS_LISTING_H

#include "tabbit/tabbit_tcb_reader.h"

#include "NullableElements_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/nullable-elements/nullable-elements.xlsx : Arrays : B2
 *
 * Every combination of an array and its elements being optional.
 */
struct NullableElements_ListingRecord_t {
  /* primary index */
  int32_t index;
  /* what the row is about */
  const char* name;
  /* both required */
  int32_t* plain;
  int32_t plain_count;
  /* the array may be absent */
  int32_t* maybe;
  int32_t maybe_count;
  /* Whether this row has a value for maybe. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_maybe;
  /* an element may be absent */
  int32_t* holes;
  int32_t holes_count;
  /* Which of holes's elements have a value, one bool per element, or NULL where
   * the file did not carry the column. spec/types/nullable-array-elements.md. */
  const bool* has_holes_at;
  /* either may be */
  int32_t* both;
  int32_t both_count;
  /* Whether this row has a value for both. The value member keeps its type and
   * holds the type's empty value when the row had none; this says which it was. */
  bool has_both;
  /* Which of both's elements have a value, one bool per element, or NULL where
   * the file did not carry the column. spec/types/nullable-array-elements.md. */
  const bool* has_both_at;
  /* where a value comparison cannot see it */
  const char** words;
  int32_t words_count;
  /* Which of words's elements have a value, one bool per element, or NULL where
   * the file did not carry the column. spec/types/nullable-array-elements.md. */
  const bool* has_words_at;
};

/* Every row of Listing.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct NullableElements_ListingTable_t {
  NullableElements_ListingRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_arena arena;
} NullableElements_ListingTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool NullableElements_ListingLoad(NullableElements_ListingTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void NullableElements_ListingFree(NullableElements_ListingTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const NullableElements_ListingRecord_t* NullableElements_ListingFindByIndex(
  const NullableElements_ListingTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool NullableElements_ListingContainsIndex(
  const NullableElements_ListingTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* NULLABLE_ELEMENTS_LISTING_H */
