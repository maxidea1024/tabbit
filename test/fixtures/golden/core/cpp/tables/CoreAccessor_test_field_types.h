// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_COREACCESSOR_TEST_FIELD_TYPES_H
#define TABBIT_GENERATED_COREACCESSOR_TEST_FIELD_TYPES_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "CoreAccessor_forward.h"
#include "enums/CoreAccessor_enum_value_type.h"

namespace tabbit_fixtures {
namespace core {
// Generated from test/fixtures/xlsx/core/core.xlsx : Types : B2
/// One column per supported primitive type.
struct TestFieldTypesRecord {
  /// primary index
  std::int32_t index = 0;
  /// utf8 text
  std::string string_field;
  /// logical flag
  bool bool_field = false;
  /// 32 bit integer
  std::int32_t int_field = 0;
  /// 64 bit integer
  std::int64_t big_int_field = 0;
  /// single precision
  float float_field = 0.0f;
  /// double precision
  double double_field = 0.0;
  /// date and time
  tabbit::DateTime datetime_field;
  /// time interval
  tabbit::TimeSpan timespan_field;
  /// globally unique id
  tabbit::Uuid uuid_field;
  /// enum reference
  ValueType value_type_field = static_cast<ValueType>(0);
};

/// One column per supported primitive type.
class TestFieldTypesTable {
 public:
  const std::vector<TestFieldTypesRecord>& records() const { return records_; }

  /// The row with this Index, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const TestFieldTypesRecord* find_by_index(std::int32_t key) const {
    const auto it = by_index_.find(key);
    return it == by_index_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_index(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const TestFieldTypesRecord& get_by_index_or_throw(std::int32_t key) const {
    const TestFieldTypesRecord* found = find_by_index(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `TestFieldTypes` that corresponds to "
                    "field `Index` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Index.
  bool contains_index(std::int32_t key) const {
    return by_index_.find(key) != by_index_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<TestFieldTypesRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "TestFieldTypes.Index", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.Index");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].index = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "TestFieldTypes.StringField", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.StringField");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].string_field = value;
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "TestFieldTypes.BoolField", tabbit::kKindScalar, false, {tabbit::kElementBool});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.BoolField");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.bool_field = cursor.next_bool();
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "TestFieldTypes.IntField", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.IntField");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].int_field = value;
            }
          }
          break;
        }
        case 5: {
          tabbit::check_column(column, "TestFieldTypes.BigIntField", tabbit::kKindScalar, false, {tabbit::kElementI64, tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.BigIntField");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.big_int_field = cursor.next_i64();
          }
          break;
        }
        case 6: {
          tabbit::check_column(column, "TestFieldTypes.FloatField", tabbit::kKindScalar, false, {tabbit::kElementF32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.FloatField");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.float_field = cursor.next_f32();
          }
          break;
        }
        case 7: {
          tabbit::check_column(column, "TestFieldTypes.DoubleField", tabbit::kKindScalar, false, {tabbit::kElementF64, tabbit::kElementF32, tabbit::kElementI32});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.DoubleField");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.double_field = cursor.next_f64();
          }
          break;
        }
        case 8: {
          tabbit::check_column(column, "TestFieldTypes.DatetimeField", tabbit::kKindScalar, false, {tabbit::kElementI64});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.DatetimeField");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.datetime_field = tabbit::from_net_ticks(cursor.next_i64());
          }
          break;
        }
        case 9: {
          tabbit::check_column(column, "TestFieldTypes.TimespanField", tabbit::kKindScalar, false, {tabbit::kElementI64});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.TimespanField");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.timespan_field = tabbit::TimeSpan(cursor.next_i64());
          }
          break;
        }
        case 10: {
          tabbit::check_column(column, "TestFieldTypes.UuidField", tabbit::kKindScalar, false, {tabbit::kElementUuid});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.uuid_field);
          }
          break;
        }
        case 11: {
          tabbit::check_column(column, "TestFieldTypes.ValueTypeField", tabbit::kKindScalar, false, {tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "TestFieldTypes.ValueTypeField");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].value_type_field = static_cast<ValueType>(value);
            }
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class CoreAccessor;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<TestFieldTypesRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> index_map;
    index_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      index_map.emplace(records[i].index, i);
    }

    records_ = std::move(records);
    by_index_ = std::move(index_map);
  }

  std::vector<TestFieldTypesRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_index_;
};

}  // namespace core
}  // namespace tabbit_fixtures

#endif  // TABBIT_GENERATED_COREACCESSOR_TEST_FIELD_TYPES_H
