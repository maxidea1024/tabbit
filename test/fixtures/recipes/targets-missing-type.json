{
  // A `Targets` entry with no `Type`, so nothing says which target it configures.

  "Sources": {
    "Xlsx": [
      {
        "Path": "test/fixtures/xlsx/core"
      }
    ]
  },

  "Targets": [
    {
      "Path": "test/fixtures/output/targets-missing-type/data"
    }
  ]
}
