{
  // spec/reference-optionality.md. The refused half: a blank in a reference column that is
  // declared required. This recipe is expected to fail, so it exports nothing anybody reads -
  // the export entry is here because a recipe needs one.
  "Sources": { "Xlsx": [ { "Path": "test/fixtures/xlsx/reference-required-blank" } ], "GoogleSheets": [] },
  "Targets": [
    { "Type": "json", "Path": "test/fixtures/output/reference-required-blank/json-named", "UseCompactRowFormat": false, "Indented": true }
  ]
}
