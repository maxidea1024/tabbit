{
  // `-` in a required column and in an index, both refused by validation. Expected to fail
  // during cooking, so the output below is never reached.
  // spec/blank-and-null-cells.md.

  "Sources": {
    "Xlsx": [
      {
        "Path": "test/fixtures/xlsx/no-value-refused"
      }
    ],
    "GoogleSheets": []
  },

  "Targets": [
    {
      "Type": "json",
      "Path": "test/fixtures/output/no-value-refused/json",
      "Indented": true
    }
  ]
}
