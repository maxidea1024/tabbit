// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    /// <summary>
    /// 기록부 완성률 보상이다.
    /// </summary>
    [System.Serializable]
    public partial class CodexRewardTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 식별자
            /// </summary>
            public string CodexRewardId => _codexRewardId;

            /// <summary>
            /// 지역별인지 전체인지
            /// </summary>
            public global::Wildling.Data.CodexScope CodexScope => _codexScope;

            /// <summary>
            /// 전체 보상은 비운다
            /// </summary>
            public string RegionId => _regionId_Region_index;
            public RegionTable.Record RegionByRegionId => _regionId;
            /// <summary>Whether this row has a value for <see cref="RegionId"/>.</summary>
            public bool HasRegionId => _regionIdHasValue;

            /// <summary>
            /// 완성률
            /// </summary>
            public int Threshold => _threshold;

            /// <summary>
            /// 보상
            /// </summary>
            public string RewardGroupId => _rewardGroupId_RewardGroup_index;
            public RewardGroupTable.Record RewardGroupByRewardGroupId => _rewardGroupId;
            #endregion

            #region Reference wiring
            public void SetReference_RegionId_INTERNAL(RegionTable.Record value) => _regionId = value;
            public void SetReference_RewardGroupId_INTERNAL(RewardGroupTable.Record value) => _rewardGroupId = value;
            #endregion

            #region Storage
            internal string _codexRewardId = "";
            internal global::Wildling.Data.CodexScope _codexScope;
            internal RegionTable.Record _regionId;
            internal string _regionId_Region_index;
            public bool _regionId_F = false;
            internal bool _regionIdHasValue;
            internal int _threshold;
            internal RewardGroupTable.Record _rewardGroupId;
            internal string _rewardGroupId_RewardGroup_index;
            public bool _rewardGroupId_F = false;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"CodexRewardId\":"); ToStringHelper.ToString(CodexRewardId, sb);
                sb.Append(",\"CodexScope\":"); ToStringHelper.ToString(CodexScope, sb);
                sb.Append(",\"RegionId\":"); ToStringHelper.ToString(RegionId, sb);
                sb.Append(",\"Threshold\":"); ToStringHelper.ToString(Threshold, sb);
                sb.Append(",\"RewardGroupId\":"); ToStringHelper.ToString(RewardGroupId, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "CodexRewardId", "CodexScope", "RegionId", "Threshold", "RewardGroupId" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.CodexRewardId, r.CodexScope, r.RegionId, r.Threshold, r.RewardGroupId });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'CodexRewardId'
        public Dictionary<string, Record> RecordsByCodexRewardId => _recordsByCodexRewardId;
        private Dictionary<string, Record> _recordsByCodexRewardId = new Dictionary<string, Record>();

        /// <summary>
        /// The row with this `CodexRewardId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByCodexRewardId(string key)
            => _recordsByCodexRewardId.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `CodexRewardId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByCodexRewardId(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByCodexRewardIdOrThrow(string key)
        {
            if (!_recordsByCodexRewardId.TryGetValue(key, out Record record))
                throw new TabbitException($"There is no record in table `CodexReward` that corresponds to field `CodexRewardId` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `CodexRewardId`.</summary>
        public bool ContainsCodexRewardId(string key) => _recordsByCodexRewardId.ContainsKey(key);
        #endregion // Indexing by `CodexRewardId`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "CodexReward.CodexRewardId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "CodexReward.CodexRewardId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._codexRewardId = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "CodexReward.CodexScope", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "CodexReward.CodexScope");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._codexScope = (global::Wildling.Data.CodexScope)value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "CodexReward.RegionId", TcbTable.KindScalar, true, TcbTable.ElementString);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "CodexReward.RegionId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._regionId_Region_index = value;
                                record._regionId = default(RegionTable.Record); // will be assigned.
                                record._regionId_F = false;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._regionIdHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._regionIdHasValue)
                                records[i]._regionId = default(RegionTable.Record);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "CodexReward.Threshold", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "CodexReward.Threshold");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._threshold = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "CodexReward.RewardGroupId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "CodexReward.RewardGroupId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rewardGroupId_RewardGroup_index = value;
                                record._rewardGroupId = default(RewardGroupTable.Record); // will be assigned.
                                record._rewardGroupId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByCodexRewardId = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsByCodexRewardId.Add(record.CodexRewardId, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByCodexRewardId = recordsByCodexRewardId;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
