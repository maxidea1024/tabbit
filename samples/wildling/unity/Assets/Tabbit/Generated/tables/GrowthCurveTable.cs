// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    [System.Serializable]
    public partial class GrowthCurveRecord
    {
        #region Values
        /// <summary>
        /// 등급마다 곡선이 다르다
        /// </summary>
        public global::Wildling.Data.Grade Grade => _grade;

        /// <summary>
        /// 레벨
        /// </summary>
        public int Level => _level;

        /// <summary>
        /// 체력 배수. 만분율
        /// </summary>
        public int HpFactor => _hpFactor;

        /// <summary>
        /// 공격 배수
        /// </summary>
        public int AttackFactor => _attackFactor;

        /// <summary>
        /// 방어 배수
        /// </summary>
        public int DefenseFactor => _defenseFactor;

        /// <summary>
        /// 구간 보너스. 없으면 비운다
        /// </summary>
        public int BonusFactor => _bonusFactor;
        /// <summary>Whether this row has a value for <see cref="BonusFactor"/>.</summary>
        public bool HasBonusFactor => _bonusFactorHasValue;

        /// <summary>
        /// 소모 재화
        /// </summary>
        public CostsEntry[] Costs => _costs;
        #endregion

        /// <summary>One element of <see cref="Costs"/>.</summary>
        [System.Serializable]
        public struct CostsEntry
        {
            /// 소모 재화
            public string CurrencyId;
            public CurrencyRecord CurrencyByCurrencyId;
            /// 첫째 소모 수량
            public int Amount;

            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"CurrencyId\":"); ToStringHelper.ToString(CurrencyId, sb);
                sb.Append(",\"Amount\":"); ToStringHelper.ToString(Amount, sb);
                sb.Append("}");
                return sb.ToString();
            }
        }

        #region Storage
        internal global::Wildling.Data.Grade _grade;
        internal int _level;
        internal int _hpFactor;
        internal int _attackFactor;
        internal int _defenseFactor;
        internal int _bonusFactor;
        internal bool _bonusFactorHasValue;
        internal CostsEntry[] _costs = System.Array.Empty<CostsEntry>();
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"Grade\":"); ToStringHelper.ToString(Grade, sb);
            sb.Append(",\"Level\":"); ToStringHelper.ToString(Level, sb);
            sb.Append(",\"HpFactor\":"); ToStringHelper.ToString(HpFactor, sb);
            sb.Append(",\"AttackFactor\":"); ToStringHelper.ToString(AttackFactor, sb);
            sb.Append(",\"DefenseFactor\":"); ToStringHelper.ToString(DefenseFactor, sb);
            sb.Append(",\"BonusFactor\":"); ToStringHelper.ToString(BonusFactor, sb);
            sb.Append(",\"Costs\":"); ToStringHelper.ToString(Costs, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 등급과 레벨별 능력치 배수와 소모 재화이다.
    /// </summary>
    [System.Serializable]
    public partial class GrowthCurveTable : IEnumerable<GrowthCurveRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Grade", "Level", "HpFactor", "AttackFactor", "DefenseFactor", "BonusFactor", "Costs" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Grade, r.Level, r.HpFactor, r.AttackFactor, r.DefenseFactor, r.BonusFactor, r.Costs });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<GrowthCurveRecord> Records => _records;
        private List<GrowthCurveRecord> _records = new List<GrowthCurveRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<GrowthCurveRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<GrowthCurveRecord> IEnumerable<GrowthCurveRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'Grade and Level'
        private Dictionary<string, GrowthCurveRecord> _recordsByGradeAndLevel = new Dictionary<string, GrowthCurveRecord>();

        /// <summary>Joins the columns of the `Grade and Level` key into the text the map is keyed by.</summary>
        private static string KeyOfGradeAndLevel(global::Wildling.Data.Grade gradeKey, int levelKey)
        {
            var parts = new string[]
            {
                ((long)gradeKey).ToString(System.Globalization.CultureInfo.InvariantCulture),
                levelKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `Grade and Level`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public GrowthCurveRecord FindByGradeAndLevel(global::Wildling.Data.Grade gradeKey, int levelKey)
            => _recordsByGradeAndLevel.TryGetValue(KeyOfGradeAndLevel(gradeKey, levelKey), out GrowthCurveRecord record) ? record : null;

        /// <summary>
        /// The row with this `Grade and Level`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public GrowthCurveRecord GetByGradeAndLevelOrThrow(global::Wildling.Data.Grade gradeKey, int levelKey)
        {
            if (!_recordsByGradeAndLevel.TryGetValue(KeyOfGradeAndLevel(gradeKey, levelKey), out GrowthCurveRecord record))
                throw new TabbitException($"There is no record in table `GrowthCurve` that corresponds to field `Grade and Level` value ({gradeKey}, {levelKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Grade and Level`.</summary>
        public bool ContainsGradeAndLevel(global::Wildling.Data.Grade gradeKey, int levelKey) => _recordsByGradeAndLevel.ContainsKey(KeyOfGradeAndLevel(gradeKey, levelKey));

        /// <summary>
        /// The row with this `Grade and Level`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// A key of several columns takes them in the order the sheet wrote them, which is
        /// the order `GetByGradeAndLevelOrThrow` takes them in too.
        /// spec/targets/table-collection-surface.md section 5.4.
        /// </remarks>
        public GrowthCurveRecord this[global::Wildling.Data.Grade gradeKey, int levelKey]
            => GetByGradeAndLevelOrThrow(gradeKey, levelKey);
        #endregion // Indexing by `Grade and Level`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<GrowthCurveRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new GrowthCurveRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "GrowthCurve.Grade", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "GrowthCurve.Grade");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._grade = (global::Wildling.Data.Grade)value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "GrowthCurve.Level", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "GrowthCurve.Level");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._level = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "GrowthCurve.HpFactor", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "GrowthCurve.HpFactor");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._hpFactor = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "GrowthCurve.AttackFactor", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "GrowthCurve.AttackFactor");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._attackFactor = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "GrowthCurve.DefenseFactor", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "GrowthCurve.DefenseFactor");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._defenseFactor = value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "GrowthCurve.BonusFactor", TcbTable.KindScalar, true, TcbTable.ElementI32, TcbTable.ElementVarint);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "GrowthCurve.BonusFactor");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._bonusFactor = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._bonusFactorHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._bonusFactorHasValue)
                                records[i]._bonusFactor = default(int);
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "GrowthCurve.Costs.CurrencyId", TcbTable.KindArray, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "GrowthCurve.Costs.CurrencyId");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._costs = new GrowthCurveRecord.CostsEntry[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._costs[j].CurrencyId = cursor.NextString();
                                record._costs[j].CurrencyByCurrencyId = default(CurrencyRecord); // will be assigned.
                            }
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "GrowthCurve.Costs.Amount", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "GrowthCurve.Costs.Amount");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._costs.Length != elementCount)
                            {
                                throw new TcbException(
                                    "GrowthCurve.Costs: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._costs.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._costs[j].Amount = cursor.NextI32();
                            }
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByGradeAndLevel = new Dictionary<string, GrowthCurveRecord>(count);
            foreach (var record in records)
                recordsByGradeAndLevel.Add(KeyOfGradeAndLevel(record.Grade, record.Level), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByGradeAndLevel = recordsByGradeAndLevel;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
