// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    /// <summary>
    /// 스테이지이다. 첫 키가 단일이므로 참조 대상이 된다.
    /// </summary>
    [System.Serializable]
    public partial class StageTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 식별자
            /// </summary>
            public string StageId => _stageId;

            /// <summary>
            /// 어느 지역인가
            /// </summary>
            public string RegionId => _regionId_Region_index;
            public RegionTable.Record RegionByRegionId => _regionId;

            /// <summary>
            /// 지역 안의 순번
            /// </summary>
            public int Index => _index;

            /// <summary>
            /// 일반 · 관측 · 수호자
            /// </summary>
            public global::Wildling.Data.StageKind StageKind => _stageKind;

            /// <summary>
            /// 등장 목록. 셀 안의 참조 배열
            /// </summary>
            public string[] WaveMonsterIds => _waveMonsterIds_Monster_index;
            public MonsterTable.Record[] MonsterByWaveMonsterIds => _waveMonsterIds;

            /// <summary>
            /// 각 등장의 레벨
            /// </summary>
            public int[] WaveLevels => _waveLevels;

            /// <summary>
            /// 클리어 보상
            /// </summary>
            public string RewardGroupId => _rewardGroupId_RewardGroup_index;
            public RewardGroupTable.Record RewardGroupByRewardGroupId => _rewardGroupId;
            #endregion

            #region Reference wiring
            public void SetReference_RegionId_INTERNAL(RegionTable.Record value) => _regionId = value;
            public void SetReference_WaveMonsterIds_INTERNAL(int index, MonsterTable.Record value) => _waveMonsterIds[index] = value;
            public void SetReference_RewardGroupId_INTERNAL(RewardGroupTable.Record value) => _rewardGroupId = value;
            #endregion

            #region Storage
            internal string _stageId = "";
            internal RegionTable.Record _regionId;
            internal string _regionId_Region_index;
            public bool _regionId_F = false;
            internal int _index;
            internal global::Wildling.Data.StageKind _stageKind;
            internal MonsterTable.Record[] _waveMonsterIds = System.Array.Empty<MonsterTable.Record>();
            internal string[] _waveMonsterIds_Monster_index = System.Array.Empty<string>();
            public bool[] _waveMonsterIds_F = System.Array.Empty<bool>();
            internal int[] _waveLevels = System.Array.Empty<int>();
            internal RewardGroupTable.Record _rewardGroupId;
            internal string _rewardGroupId_RewardGroup_index;
            public bool _rewardGroupId_F = false;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"StageId\":"); ToStringHelper.ToString(StageId, sb);
                sb.Append(",\"RegionId\":"); ToStringHelper.ToString(RegionId, sb);
                sb.Append(",\"Index\":"); ToStringHelper.ToString(Index, sb);
                sb.Append(",\"StageKind\":"); ToStringHelper.ToString(StageKind, sb);
                sb.Append(",\"WaveMonsterIds\":"); ToStringHelper.ToString(WaveMonsterIds, sb);
                sb.Append(",\"WaveLevels\":"); ToStringHelper.ToString(WaveLevels, sb);
                sb.Append(",\"RewardGroupId\":"); ToStringHelper.ToString(RewardGroupId, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "StageId", "RegionId", "Index", "StageKind", "WaveMonsterIds", "WaveLevels", "RewardGroupId" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.StageId, r.RegionId, r.Index, r.StageKind, r.WaveMonsterIds, r.WaveLevels, r.RewardGroupId });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'StageId'
        public Dictionary<string, Record> RecordsByStageId => _recordsByStageId;
        private Dictionary<string, Record> _recordsByStageId = new Dictionary<string, Record>();

        /// <summary>
        /// The row with this `StageId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByStageId(string key)
            => _recordsByStageId.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `StageId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByStageId(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByStageIdOrThrow(string key)
        {
            if (!_recordsByStageId.TryGetValue(key, out Record record))
                throw new TabbitException($"There is no record in table `Stage` that corresponds to field `StageId` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `StageId`.</summary>
        public bool ContainsStageId(string key) => _recordsByStageId.ContainsKey(key);
        #endregion // Indexing by `StageId`

        #region Indexing by 'RegionId and Index'
        private Dictionary<string, Record> _recordsByRegionIdAndIndex = new Dictionary<string, Record>();

        /// <summary>Joins the columns of the `RegionId and Index` key into the text the map is keyed by.</summary>
        private static string KeyOfRegionIdAndIndex(RegionTable.Record regionIdKey, int indexKey)
        {
            var parts = new string[]
            {
                regionIdKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
                indexKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `RegionId and Index`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByRegionIdAndIndex(RegionTable.Record regionIdKey, int indexKey)
            => _recordsByRegionIdAndIndex.TryGetValue(KeyOfRegionIdAndIndex(regionIdKey, indexKey), out Record record) ? record : null;

        /// <summary>
        /// The row with this `RegionId and Index`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public Record GetByRegionIdAndIndexOrThrow(RegionTable.Record regionIdKey, int indexKey)
        {
            if (!_recordsByRegionIdAndIndex.TryGetValue(KeyOfRegionIdAndIndex(regionIdKey, indexKey), out Record record))
                throw new TabbitException($"There is no record in table `Stage` that corresponds to field `RegionId and Index` value ({regionIdKey}, {indexKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `RegionId and Index`.</summary>
        public bool ContainsRegionIdAndIndex(RegionTable.Record regionIdKey, int indexKey) => _recordsByRegionIdAndIndex.ContainsKey(KeyOfRegionIdAndIndex(regionIdKey, indexKey));
        #endregion // Indexing by `RegionId and Index`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "Stage.StageId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Stage.StageId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._stageId = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "Stage.RegionId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Stage.RegionId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._regionId_Region_index = value;
                                record._regionId = default(RegionTable.Record); // will be assigned.
                                record._regionId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "Stage.Index", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Stage.Index");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._index = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "Stage.StageKind", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Stage.StageKind");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._stageKind = (global::Wildling.Data.StageKind)value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "Stage.WaveMonsterIds", TcbTable.KindArray, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Stage.WaveMonsterIds");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._waveMonsterIds = new MonsterTable.Record[elementCount];
                            record._waveMonsterIds_Monster_index = new string[elementCount];
                            record._waveMonsterIds_F = new bool[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._waveMonsterIds_Monster_index[j] = cursor.NextString();
                                record._waveMonsterIds[j] = default(MonsterTable.Record); // will be assigned.
                                record._waveMonsterIds_F[j] = false;
                            }
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "Stage.WaveLevels", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Stage.WaveLevels");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._waveLevels = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._waveLevels[j] = cursor.NextI32();
                            }
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "Stage.RewardGroupId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Stage.RewardGroupId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rewardGroupId_RewardGroup_index = value;
                                record._rewardGroupId = default(RewardGroupTable.Record); // will be assigned.
                                record._rewardGroupId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByStageId = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsByStageId.Add(record.StageId, record);
            var recordsByRegionIdAndIndex = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsByRegionIdAndIndex.Add(KeyOfRegionIdAndIndex(record.RegionId, record.Index), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByStageId = recordsByStageId;
            _recordsByRegionIdAndIndex = recordsByRegionIdAndIndex;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
