// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    /// <summary>
    /// 스킬 하나가 일으키는 것이다. 판별자가 그 행의 형태를 정한다.
    /// </summary>
    [System.Serializable]
    public partial class SkillEffectTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 어느 스킬인가
            /// </summary>
            public string SkillId => _skillId_Skill_index;
            public SkillTable.Record SkillBySkillId => _skillId;

            /// <summary>
            /// 적용 순서
            /// </summary>
            public int Order => _order;

            /// <summary>
            /// 이 행의 효과가 어떤 형태인가
            /// </summary>
            public Effect Effect
                => _effect_value ?? (_effect_value = BuildEffect());

            private Effect _effect_value;

            private Effect BuildEffect()
            {
                switch (_effect.Type)
                {
                    case 1:
                        return new DamageEffect
                        {
                            Chance = _effect.Chance,
                            Power = _effect.Power,
                        };
                    case 2:
                        return new HealEffect
                        {
                            Chance = _effect.Chance,
                            Power = _effect.Power,
                        };
                    case 3:
                        return new StatusEffect
                        {
                            Chance = _effect.Chance,
                            Status = _effect.Status,
                            Duration = _effect.Duration,
                        };
                    case 4:
                        return new BuffEffect
                        {
                            Chance = _effect.Chance,
                            Stat = _effect.Stat,
                            Ratio = _effect.Ratio,
                            Duration = _effect.Duration,
                        };
                }

                // A number no variant claims. The conversion refuses one, so reaching this
                // means the file was written by a build that had a variant this code does not
                // - the same shape as a column added after this code was generated.
                return null;
            }
            #endregion

            /// <summary>One element of <see cref="Effect"/>.</summary>
            [System.Serializable]
            public struct EffectEntry
            {
                /// 이 행의 효과가 어떤 형태인가
                public int Type;
                /// 발동 확률. 만분율
                public int Chance;
                /// 피해 또는 회복 배수. 만분율
                public int Power;
                /// 부여하는 상태
                public global::Wildling.Data.StatusKind Status;
                /// 변동시키는 능력치
                public global::Wildling.Data.StatKind Stat;
                /// 변동률. 만분율. 음수는 하락
                public int Ratio;
                /// 지속 턴
                public int Duration;

                public override string ToString()
                {
                    var sb = new StringBuilder("{");
                    sb.Append("\"Type\":"); ToStringHelper.ToString(Type, sb);
                    sb.Append(",\"Chance\":"); ToStringHelper.ToString(Chance, sb);
                    sb.Append(",\"Power\":"); ToStringHelper.ToString(Power, sb);
                    sb.Append(",\"Status\":"); ToStringHelper.ToString(Status, sb);
                    sb.Append(",\"Stat\":"); ToStringHelper.ToString(Stat, sb);
                    sb.Append(",\"Ratio\":"); ToStringHelper.ToString(Ratio, sb);
                    sb.Append(",\"Duration\":"); ToStringHelper.ToString(Duration, sb);
                    sb.Append("}");
                    return sb.ToString();
                }
            }

            #region Reference wiring
            public void SetReference_SkillId_INTERNAL(SkillTable.Record value) => _skillId = value;
            #endregion

            #region Storage
            internal SkillTable.Record _skillId;
            internal string _skillId_Skill_index;
            public bool _skillId_F = false;
            internal int _order;
            internal EffectEntry _effect;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"SkillId\":"); ToStringHelper.ToString(SkillId, sb);
                sb.Append(",\"Order\":"); ToStringHelper.ToString(Order, sb);
                sb.Append(",\"Effect\":"); ToStringHelper.ToString(Effect, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "SkillId", "Order", "Effect" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.SkillId, r.Order, r.Effect });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'SkillId and Order'
        private Dictionary<string, Record> _recordsBySkillIdAndOrder = new Dictionary<string, Record>();

        /// <summary>Joins the columns of the `SkillId and Order` key into the text the map is keyed by.</summary>
        private static string KeyOfSkillIdAndOrder(string skillIdKey, int orderKey)
        {
            var parts = new string[]
            {
                skillIdKey,
                orderKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `SkillId and Order`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindBySkillIdAndOrder(string skillIdKey, int orderKey)
            => _recordsBySkillIdAndOrder.TryGetValue(KeyOfSkillIdAndOrder(skillIdKey, orderKey), out Record record) ? record : null;

        /// <summary>
        /// The row with this `SkillId and Order`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public Record GetBySkillIdAndOrderOrThrow(string skillIdKey, int orderKey)
        {
            if (!_recordsBySkillIdAndOrder.TryGetValue(KeyOfSkillIdAndOrder(skillIdKey, orderKey), out Record record))
                throw new TabbitException($"There is no record in table `SkillEffect` that corresponds to field `SkillId and Order` value ({skillIdKey}, {orderKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `SkillId and Order`.</summary>
        public bool ContainsSkillIdAndOrder(string skillIdKey, int orderKey) => _recordsBySkillIdAndOrder.ContainsKey(KeyOfSkillIdAndOrder(skillIdKey, orderKey));
        #endregion // Indexing by `SkillId and Order`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "SkillEffect.SkillId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "SkillEffect.SkillId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._skillId_Skill_index = value;
                                record._skillId = default(SkillTable.Record); // will be assigned.
                                record._skillId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "SkillEffect.Order", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "SkillEffect.Order");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._order = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "SkillEffect.Effect.Type", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "SkillEffect.Effect.Type");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._effect.Type = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "SkillEffect.Effect.Chance", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "SkillEffect.Effect.Chance");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._effect.Chance = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "SkillEffect.Effect.Power", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "SkillEffect.Effect.Power");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._effect.Power = value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "SkillEffect.Effect.Status", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "SkillEffect.Effect.Status");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._effect.Status = (global::Wildling.Data.StatusKind)value;
                            } while (--n > 0);
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "SkillEffect.Effect.Stat", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "SkillEffect.Effect.Stat");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._effect.Stat = (global::Wildling.Data.StatKind)value;
                            } while (--n > 0);
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "SkillEffect.Effect.Ratio", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "SkillEffect.Effect.Ratio");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._effect.Ratio = value;
                            } while (--n > 0);
                        }
                        break;

                    case 9:
                        TcbTable.CheckColumn(column, "SkillEffect.Effect.Duration", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "SkillEffect.Effect.Duration");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._effect.Duration = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsBySkillIdAndOrder = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsBySkillIdAndOrder.Add(KeyOfSkillIdAndOrder(record.SkillId, record.Order), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsBySkillIdAndOrder = recordsBySkillIdAndOrder;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
