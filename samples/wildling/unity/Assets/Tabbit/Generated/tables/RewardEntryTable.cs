// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    /// <summary>
    /// 묶음 하나의 항목이다. 판별자가 형태를 정한다.
    /// </summary>
    [System.Serializable]
    public partial class RewardEntryTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 어느 묶음인가
            /// </summary>
            public string RewardGroupId => _rewardGroupId_RewardGroup_index;
            public RewardGroupTable.Record RewardGroupByRewardGroupId => _rewardGroupId;

            /// <summary>
            /// 표시 순서
            /// </summary>
            public int Order => _order;

            /// <summary>
            /// 이 행의 보상이 어떤 형태인가
            /// </summary>
            public Reward Reward
                => _reward_value ?? (_reward_value = BuildReward());

            private Reward _reward_value;

            private Reward BuildReward()
            {
                switch (_reward.Type)
                {
                    case 1:
                        return new ItemReward
                        {
                            Amount = _reward.Amount,
                            ItemId = _reward.ItemId,
                            ItemByItemId = _reward.ItemByItemId,
                        };
                    case 2:
                        return new CurrencyReward
                        {
                            Amount = _reward.Amount,
                            CurrencyId = _reward.CurrencyId,
                            CurrencyByCurrencyId = _reward.CurrencyByCurrencyId,
                        };
                    case 3:
                        return new MonsterReward
                        {
                            Amount = _reward.Amount,
                            MonsterId = _reward.MonsterId,
                            MonsterByMonsterId = _reward.MonsterByMonsterId,
                        };
                    case 4:
                        return new ShardReward
                        {
                            Amount = _reward.Amount,
                            MonsterId = _reward.MonsterId,
                            MonsterByMonsterId = _reward.MonsterByMonsterId,
                        };
                }

                // A number no variant claims. The conversion refuses one, so reaching this
                // means the file was written by a build that had a variant this code does not
                // - the same shape as a column added after this code was generated.
                return null;
            }

            /// <summary>
            /// 확률. 만분율
            /// </summary>
            public int Rate => _rate;

            /// <summary>
            /// 서버만 쓰는 가중치
            /// </summary>
            public int ServerWeight => _serverWeight;
            /// <summary>Whether this row has a value for <see cref="ServerWeight"/>.</summary>
            public bool HasServerWeight => _serverWeightHasValue;
            #endregion

            /// <summary>One element of <see cref="Reward"/>.</summary>
            [System.Serializable]
            public struct RewardEntry
            {
                /// 이 행의 보상이 어떤 형태인가
                public int Type;
                /// 수량
                public int Amount;
                /// 아이템 보상의 대상
                public string ItemId;
                public ItemTable.Record ItemByItemId;
                public bool ItemId_F;
                /// 재화 보상의 대상
                public string CurrencyId;
                public CurrencyTable.Record CurrencyByCurrencyId;
                public bool CurrencyId_F;
                /// 와일드링 또는 조각 보상의 대상
                public string MonsterId;
                public MonsterTable.Record MonsterByMonsterId;
                public bool MonsterId_F;

                public override string ToString()
                {
                    var sb = new StringBuilder("{");
                    sb.Append("\"Type\":"); ToStringHelper.ToString(Type, sb);
                    sb.Append(",\"Amount\":"); ToStringHelper.ToString(Amount, sb);
                    sb.Append(",\"ItemId\":"); ToStringHelper.ToString(ItemId, sb);
                    sb.Append(",\"CurrencyId\":"); ToStringHelper.ToString(CurrencyId, sb);
                    sb.Append(",\"MonsterId\":"); ToStringHelper.ToString(MonsterId, sb);
                    sb.Append("}");
                    return sb.ToString();
                }
            }

            #region Reference wiring
            public void SetReference_RewardGroupId_INTERNAL(RewardGroupTable.Record value) => _rewardGroupId = value;
            #endregion

            #region Storage
            internal RewardGroupTable.Record _rewardGroupId;
            internal string _rewardGroupId_RewardGroup_index;
            public bool _rewardGroupId_F = false;
            internal int _order;
            internal RewardEntry _reward;
            internal int _rate;
            internal int _serverWeight;
            internal bool _serverWeightHasValue;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"RewardGroupId\":"); ToStringHelper.ToString(RewardGroupId, sb);
                sb.Append(",\"Order\":"); ToStringHelper.ToString(Order, sb);
                sb.Append(",\"Reward\":"); ToStringHelper.ToString(Reward, sb);
                sb.Append(",\"Rate\":"); ToStringHelper.ToString(Rate, sb);
                sb.Append(",\"ServerWeight\":"); ToStringHelper.ToString(ServerWeight, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "RewardGroupId", "Order", "Reward", "Rate", "ServerWeight" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.RewardGroupId, r.Order, r.Reward, r.Rate, r.ServerWeight });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'RewardGroupId and Order'
        private Dictionary<string, Record> _recordsByRewardGroupIdAndOrder = new Dictionary<string, Record>();

        /// <summary>Joins the columns of the `RewardGroupId and Order` key into the text the map is keyed by.</summary>
        private static string KeyOfRewardGroupIdAndOrder(string rewardGroupIdKey, int orderKey)
        {
            var parts = new string[]
            {
                rewardGroupIdKey,
                orderKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `RewardGroupId and Order`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByRewardGroupIdAndOrder(string rewardGroupIdKey, int orderKey)
            => _recordsByRewardGroupIdAndOrder.TryGetValue(KeyOfRewardGroupIdAndOrder(rewardGroupIdKey, orderKey), out Record record) ? record : null;

        /// <summary>
        /// The row with this `RewardGroupId and Order`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public Record GetByRewardGroupIdAndOrderOrThrow(string rewardGroupIdKey, int orderKey)
        {
            if (!_recordsByRewardGroupIdAndOrder.TryGetValue(KeyOfRewardGroupIdAndOrder(rewardGroupIdKey, orderKey), out Record record))
                throw new TabbitException($"There is no record in table `RewardEntry` that corresponds to field `RewardGroupId and Order` value ({rewardGroupIdKey}, {orderKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `RewardGroupId and Order`.</summary>
        public bool ContainsRewardGroupIdAndOrder(string rewardGroupIdKey, int orderKey) => _recordsByRewardGroupIdAndOrder.ContainsKey(KeyOfRewardGroupIdAndOrder(rewardGroupIdKey, orderKey));
        #endregion // Indexing by `RewardGroupId and Order`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "RewardEntry.RewardGroupId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "RewardEntry.RewardGroupId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rewardGroupId_RewardGroup_index = value;
                                record._rewardGroupId = default(RewardGroupTable.Record); // will be assigned.
                                record._rewardGroupId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "RewardEntry.Order", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RewardEntry.Order");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._order = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "RewardEntry.Reward.Type", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RewardEntry.Reward.Type");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward.Type = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "RewardEntry.Reward.Amount", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RewardEntry.Reward.Amount");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward.Amount = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "RewardEntry.Reward.ItemId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "RewardEntry.Reward.ItemId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward.ItemId = value;
                                record._reward.ItemByItemId = default(ItemTable.Record); // will be assigned.
                                record._reward.ItemId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "RewardEntry.Reward.CurrencyId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "RewardEntry.Reward.CurrencyId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward.CurrencyId = value;
                                record._reward.CurrencyByCurrencyId = default(CurrencyTable.Record); // will be assigned.
                                record._reward.CurrencyId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "RewardEntry.Reward.MonsterId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "RewardEntry.Reward.MonsterId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward.MonsterId = value;
                                record._reward.MonsterByMonsterId = default(MonsterTable.Record); // will be assigned.
                                record._reward.MonsterId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "RewardEntry.Rate", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RewardEntry.Rate");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rate = value;
                            } while (--n > 0);
                        }
                        break;

                    case 9:
                        TcbTable.CheckColumn(column, "RewardEntry.ServerWeight", TcbTable.KindScalar, true, TcbTable.ElementI32, TcbTable.ElementVarint);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "RewardEntry.ServerWeight");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._serverWeight = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._serverWeightHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._serverWeightHasValue)
                                records[i]._serverWeight = default(int);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByRewardGroupIdAndOrder = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsByRewardGroupIdAndOrder.Add(KeyOfRewardGroupIdAndOrder(record.RewardGroupId, record.Order), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByRewardGroupIdAndOrder = recordsByRewardGroupIdAndOrder;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
