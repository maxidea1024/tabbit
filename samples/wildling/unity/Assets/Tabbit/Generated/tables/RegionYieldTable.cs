// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    /// <summary>
    /// 지역과 시간대별 산출이다. 뒤로 갈수록 줄어든다.
    /// </summary>
    [System.Serializable]
    public partial class RegionYieldTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 어느 지역인가
            /// </summary>
            public string RegionId => _regionId_Region_index;
            public RegionTable.Record RegionByRegionId => _regionId;

            /// <summary>
            /// 누적 시간대
            /// </summary>
            public int HourBand => _hourBand;

            /// <summary>
            /// 시간당 골드
            /// </summary>
            public int GoldPerHour => _goldPerHour;

            /// <summary>
            /// 시간당 먹이
            /// </summary>
            public int FoodPerHour => _foodPerHour;

            /// <summary>
            /// 재료 드랍 묶음
            /// </summary>
            public string RewardGroupId => _rewardGroupId_RewardGroup_index;
            public RewardGroupTable.Record RewardGroupByRewardGroupId => _rewardGroupId;
            #endregion

            #region Reference wiring
            public void SetReference_RegionId_INTERNAL(RegionTable.Record value) => _regionId = value;
            public void SetReference_RewardGroupId_INTERNAL(RewardGroupTable.Record value) => _rewardGroupId = value;
            #endregion

            #region Storage
            internal RegionTable.Record _regionId;
            internal string _regionId_Region_index;
            public bool _regionId_F = false;
            internal int _hourBand;
            internal int _goldPerHour;
            internal int _foodPerHour;
            internal RewardGroupTable.Record _rewardGroupId;
            internal string _rewardGroupId_RewardGroup_index;
            public bool _rewardGroupId_F = false;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"RegionId\":"); ToStringHelper.ToString(RegionId, sb);
                sb.Append(",\"HourBand\":"); ToStringHelper.ToString(HourBand, sb);
                sb.Append(",\"GoldPerHour\":"); ToStringHelper.ToString(GoldPerHour, sb);
                sb.Append(",\"FoodPerHour\":"); ToStringHelper.ToString(FoodPerHour, sb);
                sb.Append(",\"RewardGroupId\":"); ToStringHelper.ToString(RewardGroupId, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "RegionId", "HourBand", "GoldPerHour", "FoodPerHour", "RewardGroupId" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.RegionId, r.HourBand, r.GoldPerHour, r.FoodPerHour, r.RewardGroupId });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'RegionId and HourBand'
        private Dictionary<string, Record> _recordsByRegionIdAndHourBand = new Dictionary<string, Record>();

        /// <summary>Joins the columns of the `RegionId and HourBand` key into the text the map is keyed by.</summary>
        private static string KeyOfRegionIdAndHourBand(RegionTable.Record regionIdKey, int hourBandKey)
        {
            var parts = new string[]
            {
                regionIdKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
                hourBandKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `RegionId and HourBand`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByRegionIdAndHourBand(RegionTable.Record regionIdKey, int hourBandKey)
            => _recordsByRegionIdAndHourBand.TryGetValue(KeyOfRegionIdAndHourBand(regionIdKey, hourBandKey), out Record record) ? record : null;

        /// <summary>
        /// The row with this `RegionId and HourBand`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public Record GetByRegionIdAndHourBandOrThrow(RegionTable.Record regionIdKey, int hourBandKey)
        {
            if (!_recordsByRegionIdAndHourBand.TryGetValue(KeyOfRegionIdAndHourBand(regionIdKey, hourBandKey), out Record record))
                throw new TabbitException($"There is no record in table `RegionYield` that corresponds to field `RegionId and HourBand` value ({regionIdKey}, {hourBandKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `RegionId and HourBand`.</summary>
        public bool ContainsRegionIdAndHourBand(RegionTable.Record regionIdKey, int hourBandKey) => _recordsByRegionIdAndHourBand.ContainsKey(KeyOfRegionIdAndHourBand(regionIdKey, hourBandKey));
        #endregion // Indexing by `RegionId and HourBand`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "RegionYield.RegionId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "RegionYield.RegionId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._regionId_Region_index = value;
                                record._regionId = default(RegionTable.Record); // will be assigned.
                                record._regionId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "RegionYield.HourBand", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RegionYield.HourBand");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._hourBand = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "RegionYield.GoldPerHour", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RegionYield.GoldPerHour");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._goldPerHour = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "RegionYield.FoodPerHour", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RegionYield.FoodPerHour");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._foodPerHour = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "RegionYield.RewardGroupId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "RegionYield.RewardGroupId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rewardGroupId_RewardGroup_index = value;
                                record._rewardGroupId = default(RewardGroupTable.Record); // will be assigned.
                                record._rewardGroupId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByRegionIdAndHourBand = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsByRegionIdAndHourBand.Add(KeyOfRegionIdAndHourBand(record.RegionId, record.HourBand), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByRegionIdAndHourBand = recordsByRegionIdAndHourBand;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
