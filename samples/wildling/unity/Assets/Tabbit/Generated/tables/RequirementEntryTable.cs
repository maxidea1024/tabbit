// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    /// <summary>
    /// 묶음 하나의 조건이다.
    /// </summary>
    [System.Serializable]
    public partial class RequirementEntryTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 어느 묶음인가
            /// </summary>
            public string RequirementGroupId => _requirementGroupId_RequirementGroup_index;
            public RequirementGroupTable.Record RequirementGroupByRequirementGroupId => _requirementGroupId;

            /// <summary>
            /// 검사 순서
            /// </summary>
            public int Order => _order;

            /// <summary>
            /// 이 행의 조건이 어떤 형태인가
            /// </summary>
            public Requirement Req
                => _req_value ?? (_req_value = BuildReq());

            private Requirement _req_value;

            private Requirement BuildReq()
            {
                switch (_req.Type)
                {
                    case 1:
                        return new LevelRequirement
                        {
                            Level = _req.Level,
                        };
                    case 2:
                        return new CodexRequirement
                        {
                            CodexState = _req.CodexState,
                        };
                    case 3:
                        return new ItemRequirement
                        {
                            ItemId = _req.ItemId,
                            Amount = _req.Amount,
                        };
                    case 4:
                        return new StageRequirement
                        {
                            StageId = _req.StageId,
                        };
                }

                // A number no variant claims. The conversion refuses one, so reaching this
                // means the file was written by a build that had a variant this code does not
                // - the same shape as a column added after this code was generated.
                return null;
            }
            #endregion

            /// <summary>One element of <see cref="Req"/>.</summary>
            [System.Serializable]
            public struct ReqEntry
            {
                /// 이 행의 조건이 어떤 형태인가
                public int Type;
                public int Level;
                public global::Wildling.Data.CodexState CodexState;
                public string ItemId;
                public ItemTable.Record ItemByItemId;
                public bool ItemId_F;
                public int Amount;
                public string StageId;
                public StageTable.Record StageByStageId;
                public bool StageId_F;

                public override string ToString()
                {
                    var sb = new StringBuilder("{");
                    sb.Append("\"Type\":"); ToStringHelper.ToString(Type, sb);
                    sb.Append(",\"Level\":"); ToStringHelper.ToString(Level, sb);
                    sb.Append(",\"CodexState\":"); ToStringHelper.ToString(CodexState, sb);
                    sb.Append(",\"ItemId\":"); ToStringHelper.ToString(ItemId, sb);
                    sb.Append(",\"Amount\":"); ToStringHelper.ToString(Amount, sb);
                    sb.Append(",\"StageId\":"); ToStringHelper.ToString(StageId, sb);
                    sb.Append("}");
                    return sb.ToString();
                }
            }

            #region Reference wiring
            public void SetReference_RequirementGroupId_INTERNAL(RequirementGroupTable.Record value) => _requirementGroupId = value;
            #endregion

            #region Storage
            internal RequirementGroupTable.Record _requirementGroupId;
            internal string _requirementGroupId_RequirementGroup_index;
            public bool _requirementGroupId_F = false;
            internal int _order;
            internal ReqEntry _req;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"RequirementGroupId\":"); ToStringHelper.ToString(RequirementGroupId, sb);
                sb.Append(",\"Order\":"); ToStringHelper.ToString(Order, sb);
                sb.Append(",\"Req\":"); ToStringHelper.ToString(Req, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "RequirementGroupId", "Order", "Req" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.RequirementGroupId, r.Order, r.Req });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'RequirementGroupId and Order'
        private Dictionary<string, Record> _recordsByRequirementGroupIdAndOrder = new Dictionary<string, Record>();

        /// <summary>Joins the columns of the `RequirementGroupId and Order` key into the text the map is keyed by.</summary>
        private static string KeyOfRequirementGroupIdAndOrder(RequirementGroupTable.Record requirementGroupIdKey, int orderKey)
        {
            var parts = new string[]
            {
                requirementGroupIdKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
                orderKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `RequirementGroupId and Order`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByRequirementGroupIdAndOrder(RequirementGroupTable.Record requirementGroupIdKey, int orderKey)
            => _recordsByRequirementGroupIdAndOrder.TryGetValue(KeyOfRequirementGroupIdAndOrder(requirementGroupIdKey, orderKey), out Record record) ? record : null;

        /// <summary>
        /// The row with this `RequirementGroupId and Order`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public Record GetByRequirementGroupIdAndOrderOrThrow(RequirementGroupTable.Record requirementGroupIdKey, int orderKey)
        {
            if (!_recordsByRequirementGroupIdAndOrder.TryGetValue(KeyOfRequirementGroupIdAndOrder(requirementGroupIdKey, orderKey), out Record record))
                throw new TabbitException($"There is no record in table `RequirementEntry` that corresponds to field `RequirementGroupId and Order` value ({requirementGroupIdKey}, {orderKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `RequirementGroupId and Order`.</summary>
        public bool ContainsRequirementGroupIdAndOrder(RequirementGroupTable.Record requirementGroupIdKey, int orderKey) => _recordsByRequirementGroupIdAndOrder.ContainsKey(KeyOfRequirementGroupIdAndOrder(requirementGroupIdKey, orderKey));
        #endregion // Indexing by `RequirementGroupId and Order`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "RequirementEntry.RequirementGroupId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "RequirementEntry.RequirementGroupId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._requirementGroupId_RequirementGroup_index = value;
                                record._requirementGroupId = default(RequirementGroupTable.Record); // will be assigned.
                                record._requirementGroupId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "RequirementEntry.Order", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RequirementEntry.Order");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._order = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "RequirementEntry.Req.Type", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RequirementEntry.Req.Type");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._req.Type = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "RequirementEntry.Req.Level", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RequirementEntry.Req.Level");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._req.Level = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "RequirementEntry.Req.CodexState", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RequirementEntry.Req.CodexState");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._req.CodexState = (global::Wildling.Data.CodexState)value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "RequirementEntry.Req.ItemId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "RequirementEntry.Req.ItemId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._req.ItemId = value;
                                record._req.ItemByItemId = default(ItemTable.Record); // will be assigned.
                                record._req.ItemId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "RequirementEntry.Req.Amount", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "RequirementEntry.Req.Amount");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._req.Amount = value;
                            } while (--n > 0);
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "RequirementEntry.Req.StageId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "RequirementEntry.Req.StageId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._req.StageId = value;
                                record._req.StageByStageId = default(StageTable.Record); // will be assigned.
                                record._req.StageId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByRequirementGroupIdAndOrder = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsByRequirementGroupIdAndOrder.Add(KeyOfRequirementGroupIdAndOrder(record.RequirementGroupId, record.Order), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByRequirementGroupIdAndOrder = recordsByRequirementGroupIdAndOrder;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
