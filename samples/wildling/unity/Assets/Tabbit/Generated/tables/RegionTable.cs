// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    [System.Serializable]
    public partial class RegionRecord
    {
        #region Values
        /// <summary>
        /// 식별자
        /// </summary>
        public string RegionId => _regionId;

        /// <summary>
        /// 표시 이름
        /// </summary>
        public string Name => _name;

        /// <summary>
        /// 진행 순서
        /// </summary>
        public int Order => _order;

        /// <summary>
        /// 해금 상태의 초기값
        /// </summary>
        public global::Wildling.Data.RegionState State => _state;

        /// <summary>
        /// 그 지역에 많은 속성
        /// </summary>
        public global::Wildling.Data.Element ThemeElement => _themeElement;

        /// <summary>
        /// 배경
        /// </summary>
        public string Background => _background;

        /// <summary>
        /// 안개 색
        /// </summary>
        public FogColorEntry FogColor => _fogColor;

        /// <summary>
        /// 해금 조건. 첫 지역은 비운다
        /// </summary>
        public string RequirementGroupId => _requirementGroupId_RequirementGroup_index;
        public RequirementGroupRecord RequirementGroupByRequirementGroupId => _requirementGroupId;
        /// <summary>Whether this row has a value for <see cref="RequirementGroupId"/>.</summary>
        public bool HasRequirementGroupId => _requirementGroupIdHasValue;
        #endregion

        /// <summary>One element of <see cref="FogColor"/>.</summary>
        [System.Serializable]
        public struct FogColorEntry
        {
            /// 안개 색
            public float R;
            /// 안개 색
            public float G;
            /// 안개 색
            public float B;
            /// 안개 색
            public float A;

            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"R\":"); ToStringHelper.ToString(R, sb);
                sb.Append(",\"G\":"); ToStringHelper.ToString(G, sb);
                sb.Append(",\"B\":"); ToStringHelper.ToString(B, sb);
                sb.Append(",\"A\":"); ToStringHelper.ToString(A, sb);
                sb.Append("}");
                return sb.ToString();
            }
        }

        #region Reference wiring
        public void SetReference_RequirementGroupId_INTERNAL(RequirementGroupRecord value) => _requirementGroupId = value;
        #endregion

        #region Storage
        internal string _regionId = "";
        internal string _name = "";
        internal int _order;
        internal global::Wildling.Data.RegionState _state;
        internal global::Wildling.Data.Element _themeElement;
        internal string _background = "";
        internal FogColorEntry _fogColor;
        internal RequirementGroupRecord _requirementGroupId;
        internal string _requirementGroupId_RequirementGroup_index;
        internal bool _requirementGroupIdHasValue;
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"RegionId\":"); ToStringHelper.ToString(RegionId, sb);
            sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
            sb.Append(",\"Order\":"); ToStringHelper.ToString(Order, sb);
            sb.Append(",\"State\":"); ToStringHelper.ToString(State, sb);
            sb.Append(",\"ThemeElement\":"); ToStringHelper.ToString(ThemeElement, sb);
            sb.Append(",\"Background\":"); ToStringHelper.ToString(Background, sb);
            sb.Append(",\"FogColor\":"); ToStringHelper.ToString(FogColor, sb);
            sb.Append(",\"RequirementGroupId\":"); ToStringHelper.ToString(RequirementGroupId, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 지역이다. 장기 진행 구조를 만든다.
    /// </summary>
    [System.Serializable]
    public partial class RegionTable : IEnumerable<RegionRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "RegionId", "Name", "Order", "State", "ThemeElement", "Background", "FogColor", "RequirementGroupId" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.RegionId, r.Name, r.Order, r.State, r.ThemeElement, r.Background, r.FogColor, r.RequirementGroupId });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<RegionRecord> Records => _records;
        private List<RegionRecord> _records = new List<RegionRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<RegionRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<RegionRecord> IEnumerable<RegionRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'RegionId'
        public Dictionary<string, RegionRecord> RecordsByRegionId => _recordsByRegionId;
        private Dictionary<string, RegionRecord> _recordsByRegionId = new Dictionary<string, RegionRecord>();

        /// <summary>
        /// The row with this `RegionId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public RegionRecord FindByRegionId(string key)
            => _recordsByRegionId.TryGetValue(key, out RegionRecord record) ? record : null;

        /// <summary>
        /// The row with this `RegionId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByRegionId(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public RegionRecord GetByRegionIdOrThrow(string key)
        {
            if (!_recordsByRegionId.TryGetValue(key, out RegionRecord record))
                throw new TabbitException($"There is no record in table `Region` that corresponds to field `RegionId` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `RegionId`.</summary>
        public bool ContainsRegionId(string key) => _recordsByRegionId.ContainsKey(key);
        #endregion // Indexing by `RegionId`

        /// <summary>Each row with the `RegionId` it is keyed by.</summary>
        /// <remarks>
        /// `foreach (var (key, row) in table.Entries)`. What this saves a caller is not the
        /// key value - the row carries it - but having to know which column the key is:
        /// `RegionId` here, something else in the next table.
        ///
        /// A struct that enumerates itself, so the loop allocates nothing, and the rows in
        /// the order the file wrote them rather than the order a dictionary happens to hold
        /// them - which makes this and a plain `foreach` over the table agree.
        ///
        /// Only the primary key gets this. A table keyed by several columns together has no
        /// single key value to pair a row with.
        /// </remarks>
        public struct EntryEnumerator
        {
            private readonly List<RegionRecord> _rows;
            private int _at;

            internal EntryEnumerator(List<RegionRecord> rows)
            {
                _rows = rows;
                _at = -1;
            }

            public EntryEnumerator GetEnumerator() => this;

            public bool MoveNext() => ++_at < _rows.Count;

            public (string Key, RegionRecord Row) Current
                => (_rows[_at].RegionId, _rows[_at]);
        }

        /// <summary>Each row with the `RegionId` it is keyed by.</summary>
        public EntryEnumerator Entries => new EntryEnumerator(_records);

        /// <summary>
        /// The row with this `RegionId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// `GetByRegionIdOrThrow` under the spelling the language uses for a
        /// keyed collection. It throws because that is what `Dictionary` does here; a
        /// language whose own map answers with null generates a subscript that answers with
        /// null. spec/targets/table-collection-surface.md section 5.7.
        ///
        /// **This is the key, not the row's position.** `table[0]` is the row whose
        /// `RegionId` is 0, not the first row - the rows in order are
        /// `Records`. Nothing in the type says which, which is why the table itself is not
        /// subscriptable by position anywhere.
        ///
        /// It does not replace `FindByRegionId`: a key that may be absent
        /// wants the one whose name says a miss is an ordinary answer.
        /// </remarks>
        public RegionRecord this[string key] => GetByRegionIdOrThrow(key);

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<RegionRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new RegionRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "Region.RegionId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.RegionId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._regionId = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "Region.Name", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.Name");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._name = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "Region.Order", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.Order");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._order = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "Region.State", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.State");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._state = (global::Wildling.Data.RegionState)value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "Region.ThemeElement", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.ThemeElement");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._themeElement = (global::Wildling.Data.Element)value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "Region.Background", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.Background");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._background = value;
                            } while (--n > 0);
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "Region.FogColor.R", TcbTable.KindScalar, false, TcbTable.ElementF32);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.FogColor.R");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._fogColor.R = cursor.NextF32();
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "Region.FogColor.G", TcbTable.KindScalar, false, TcbTable.ElementF32);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.FogColor.G");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._fogColor.G = cursor.NextF32();
                        }
                        break;

                    case 9:
                        TcbTable.CheckColumn(column, "Region.FogColor.B", TcbTable.KindScalar, false, TcbTable.ElementF32);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.FogColor.B");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._fogColor.B = cursor.NextF32();
                        }
                        break;

                    case 10:
                        TcbTable.CheckColumn(column, "Region.FogColor.A", TcbTable.KindScalar, false, TcbTable.ElementF32);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.FogColor.A");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._fogColor.A = cursor.NextF32();
                        }
                        break;

                    case 11:
                        TcbTable.CheckColumn(column, "Region.RequirementGroupId", TcbTable.KindScalar, true, TcbTable.ElementString);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Region.RequirementGroupId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._requirementGroupId_RequirementGroup_index = value;
                                record._requirementGroupId = default(RequirementGroupRecord); // will be assigned.
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._requirementGroupIdHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._requirementGroupIdHasValue)
                                records[i]._requirementGroupId = default(RequirementGroupRecord);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByRegionId = new Dictionary<string, RegionRecord>(count);
            foreach (var record in records)
                recordsByRegionId.Add(record.RegionId, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByRegionId = recordsByRegionId;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
