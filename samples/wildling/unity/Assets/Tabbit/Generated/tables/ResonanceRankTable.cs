// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    [System.Serializable]
    public partial class ResonanceRankRecord
    {
        #region Values
        /// <summary>
        /// 등급
        /// </summary>
        public global::Wildling.Data.Grade Grade => _grade;

        /// <summary>
        /// 공명 등급
        /// </summary>
        public int Rank => _rank;

        /// <summary>
        /// 능력치 배수. 만분율
        /// </summary>
        public int StatFactor => _statFactor;

        /// <summary>
        /// 필요한 울림 조각
        /// </summary>
        public int ShardCost => _shardCost;

        /// <summary>
        /// 3 · 5등급의 추가 효과
        /// </summary>
        public string UnlockNote => _unlockNote;
        /// <summary>Whether this row has a value for <see cref="UnlockNote"/>.</summary>
        public bool HasUnlockNote => _unlockNoteHasValue;
        #endregion

        #region Storage
        internal global::Wildling.Data.Grade _grade;
        internal int _rank;
        internal int _statFactor;
        internal int _shardCost;
        internal string _unlockNote = "";
        internal bool _unlockNoteHasValue;
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"Grade\":"); ToStringHelper.ToString(Grade, sb);
            sb.Append(",\"Rank\":"); ToStringHelper.ToString(Rank, sb);
            sb.Append(",\"StatFactor\":"); ToStringHelper.ToString(StatFactor, sb);
            sb.Append(",\"ShardCost\":"); ToStringHelper.ToString(ShardCost, sb);
            sb.Append(",\"UnlockNote\":"); ToStringHelper.ToString(UnlockNote, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 공명 등급별 배수와 필요 조각이다.
    /// </summary>
    [System.Serializable]
    public partial class ResonanceRankTable : IEnumerable<ResonanceRankRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Grade", "Rank", "StatFactor", "ShardCost", "UnlockNote" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Grade, r.Rank, r.StatFactor, r.ShardCost, r.UnlockNote });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<ResonanceRankRecord> Records => _records;
        private List<ResonanceRankRecord> _records = new List<ResonanceRankRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<ResonanceRankRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<ResonanceRankRecord> IEnumerable<ResonanceRankRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'Grade and Rank'
        private Dictionary<string, ResonanceRankRecord> _recordsByGradeAndRank = new Dictionary<string, ResonanceRankRecord>();

        /// <summary>Joins the columns of the `Grade and Rank` key into the text the map is keyed by.</summary>
        private static string KeyOfGradeAndRank(global::Wildling.Data.Grade gradeKey, int rankKey)
        {
            var parts = new string[]
            {
                ((long)gradeKey).ToString(System.Globalization.CultureInfo.InvariantCulture),
                rankKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `Grade and Rank`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public ResonanceRankRecord FindByGradeAndRank(global::Wildling.Data.Grade gradeKey, int rankKey)
            => _recordsByGradeAndRank.TryGetValue(KeyOfGradeAndRank(gradeKey, rankKey), out ResonanceRankRecord record) ? record : null;

        /// <summary>
        /// The row with this `Grade and Rank`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public ResonanceRankRecord GetByGradeAndRankOrThrow(global::Wildling.Data.Grade gradeKey, int rankKey)
        {
            if (!_recordsByGradeAndRank.TryGetValue(KeyOfGradeAndRank(gradeKey, rankKey), out ResonanceRankRecord record))
                throw new TabbitException($"There is no record in table `ResonanceRank` that corresponds to field `Grade and Rank` value ({gradeKey}, {rankKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Grade and Rank`.</summary>
        public bool ContainsGradeAndRank(global::Wildling.Data.Grade gradeKey, int rankKey) => _recordsByGradeAndRank.ContainsKey(KeyOfGradeAndRank(gradeKey, rankKey));

        /// <summary>
        /// The row with this `Grade and Rank`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// A key of several columns takes them in the order the sheet wrote them, which is
        /// the order `GetByGradeAndRankOrThrow` takes them in too.
        /// spec/targets/table-collection-surface.md section 5.4.
        /// </remarks>
        public ResonanceRankRecord this[global::Wildling.Data.Grade gradeKey, int rankKey]
            => GetByGradeAndRankOrThrow(gradeKey, rankKey);
        #endregion // Indexing by `Grade and Rank`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<ResonanceRankRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new ResonanceRankRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "ResonanceRank.Grade", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "ResonanceRank.Grade");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._grade = (global::Wildling.Data.Grade)value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "ResonanceRank.Rank", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "ResonanceRank.Rank");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rank = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "ResonanceRank.StatFactor", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "ResonanceRank.StatFactor");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._statFactor = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "ResonanceRank.ShardCost", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "ResonanceRank.ShardCost");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._shardCost = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "ResonanceRank.UnlockNote", TcbTable.KindScalar, true, TcbTable.ElementString);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "ResonanceRank.UnlockNote");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._unlockNote = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._unlockNoteHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._unlockNoteHasValue)
                                records[i]._unlockNote = "";
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByGradeAndRank = new Dictionary<string, ResonanceRankRecord>(count);
            foreach (var record in records)
                recordsByGradeAndRank.Add(KeyOfGradeAndRank(record.Grade, record.Rank), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByGradeAndRank = recordsByGradeAndRank;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
