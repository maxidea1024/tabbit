// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    /// <summary>
    /// 단계별로 사용할 수 있는 스킬이다.
    /// </summary>
    [System.Serializable]
    public partial class MonsterSkillTable : IEnumerable<MonsterSkillTable.Record>
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 어느 단계인가
            /// </summary>
            public string MonsterId => _monsterId_Monster_index;
            public MonsterTable.Record MonsterByMonsterId => _monsterId;

            /// <summary>
            /// 어느 스킬인가
            /// </summary>
            public string SkillId => _skillId_Skill_index;
            public SkillTable.Record SkillBySkillId => _skillId;

            /// <summary>
            /// 액티브인지 패시브인지
            /// </summary>
            public global::Wildling.Data.SlotKind SlotKind => _slotKind;

            /// <summary>
            /// 이 단계부터 사용할 수 있다
            /// </summary>
            public int UnlockStage => _unlockStage;
            #endregion

            #region Reference wiring
            public void SetReference_MonsterId_INTERNAL(MonsterTable.Record value) => _monsterId = value;
            public void SetReference_SkillId_INTERNAL(SkillTable.Record value) => _skillId = value;
            #endregion

            #region Storage
            internal MonsterTable.Record _monsterId;
            internal string _monsterId_Monster_index;
            public bool _monsterId_F = false;
            internal SkillTable.Record _skillId;
            internal string _skillId_Skill_index;
            public bool _skillId_F = false;
            internal global::Wildling.Data.SlotKind _slotKind;
            internal int _unlockStage;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"MonsterId\":"); ToStringHelper.ToString(MonsterId, sb);
                sb.Append(",\"SkillId\":"); ToStringHelper.ToString(SkillId, sb);
                sb.Append(",\"SlotKind\":"); ToStringHelper.ToString(SlotKind, sb);
                sb.Append(",\"UnlockStage\":"); ToStringHelper.ToString(UnlockStage, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "MonsterId", "SkillId", "SlotKind", "UnlockStage" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.MonsterId, r.SkillId, r.SlotKind, r.UnlockStage });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<Record>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<Record> IEnumerable<Record>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'MonsterId and SkillId'
        private Dictionary<string, Record> _recordsByMonsterIdAndSkillId = new Dictionary<string, Record>();

        /// <summary>Joins the columns of the `MonsterId and SkillId` key into the text the map is keyed by.</summary>
        private static string KeyOfMonsterIdAndSkillId(string monsterIdKey, string skillIdKey)
        {
            var parts = new string[]
            {
                monsterIdKey,
                skillIdKey,
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `MonsterId and SkillId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByMonsterIdAndSkillId(string monsterIdKey, string skillIdKey)
            => _recordsByMonsterIdAndSkillId.TryGetValue(KeyOfMonsterIdAndSkillId(monsterIdKey, skillIdKey), out Record record) ? record : null;

        /// <summary>
        /// The row with this `MonsterId and SkillId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public Record GetByMonsterIdAndSkillIdOrThrow(string monsterIdKey, string skillIdKey)
        {
            if (!_recordsByMonsterIdAndSkillId.TryGetValue(KeyOfMonsterIdAndSkillId(monsterIdKey, skillIdKey), out Record record))
                throw new TabbitException($"There is no record in table `MonsterSkill` that corresponds to field `MonsterId and SkillId` value ({monsterIdKey}, {skillIdKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `MonsterId and SkillId`.</summary>
        public bool ContainsMonsterIdAndSkillId(string monsterIdKey, string skillIdKey) => _recordsByMonsterIdAndSkillId.ContainsKey(KeyOfMonsterIdAndSkillId(monsterIdKey, skillIdKey));

        /// <summary>
        /// The row with this `MonsterId and SkillId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// A key of several columns takes them in the order the sheet wrote them, which is
        /// the order `GetByMonsterIdAndSkillIdOrThrow` takes them in too.
        /// spec/targets/table-collection-surface.md section 5.4.
        /// </remarks>
        public Record this[string monsterIdKey, string skillIdKey]
            => GetByMonsterIdAndSkillIdOrThrow(monsterIdKey, skillIdKey);
        #endregion // Indexing by `MonsterId and SkillId`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "MonsterSkill.MonsterId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterSkill.MonsterId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._monsterId_Monster_index = value;
                                record._monsterId = default(MonsterTable.Record); // will be assigned.
                                record._monsterId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "MonsterSkill.SkillId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterSkill.SkillId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._skillId_Skill_index = value;
                                record._skillId = default(SkillTable.Record); // will be assigned.
                                record._skillId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "MonsterSkill.SlotKind", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterSkill.SlotKind");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._slotKind = (global::Wildling.Data.SlotKind)value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "MonsterSkill.UnlockStage", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterSkill.UnlockStage");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._unlockStage = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByMonsterIdAndSkillId = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsByMonsterIdAndSkillId.Add(KeyOfMonsterIdAndSkillId(record.MonsterId, record.SkillId), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByMonsterIdAndSkillId = recordsByMonsterIdAndSkillId;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
