// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    [System.Serializable]
    public partial class EncounterTableRecord
    {
        #region Values
        /// <summary>
        /// 식별자
        /// </summary>
        public string EncounterId => _encounterId;

        /// <summary>
        /// 어느 지역인가
        /// </summary>
        public string RegionId => _regionId_Region_index;
        public RegionRecord RegionByRegionId => _regionId;

        /// <summary>
        /// 은둔 슬롯의 조건
        /// </summary>
        public string RequirementGroupId => _requirementGroupId_RequirementGroup_index;
        public RequirementGroupRecord RequirementGroupByRequirementGroupId => _requirementGroupId;
        /// <summary>Whether this row has a value for <see cref="RequirementGroupId"/>.</summary>
        public bool HasRequirementGroupId => _requirementGroupIdHasValue;

        /// <summary>
        /// 어느 단계가 나오는가.
        /// </summary>
        public EntriesEntry[] Entries => _entries;
        #endregion

        /// <summary>One element of <see cref="Entries"/>.</summary>
        [System.Serializable]
        public struct EntriesEntry
        {
            /// 어느 단계가 나오는가.
            public string MonsterId;
            public MonsterRecord MonsterByMonsterId;
            public bool MonsterId_F;
            /// 가중치. 등급 기본값에 지역 계수를 적용한 값입니다.
            public int Weight;
            /// 어느 슬롯에서 나오는가.
            public global::Wildling.Data.EncounterSlot EncounterSlot;

            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"MonsterId\":"); ToStringHelper.ToString(MonsterId, sb);
                sb.Append(",\"Weight\":"); ToStringHelper.ToString(Weight, sb);
                sb.Append(",\"EncounterSlot\":"); ToStringHelper.ToString(EncounterSlot, sb);
                sb.Append("}");
                return sb.ToString();
            }
        }

        #region Reference wiring
        public void SetReference_RegionId_INTERNAL(RegionRecord value) => _regionId = value;
        public void SetReference_RequirementGroupId_INTERNAL(RequirementGroupRecord value) => _requirementGroupId = value;
        #endregion

        #region Storage
        internal string _encounterId = "";
        internal RegionRecord _regionId;
        internal string _regionId_Region_index;
        public bool _regionId_F = false;
        internal RequirementGroupRecord _requirementGroupId;
        internal string _requirementGroupId_RequirementGroup_index;
        public bool _requirementGroupId_F = false;
        internal bool _requirementGroupIdHasValue;
        internal EntriesEntry[] _entries = System.Array.Empty<EntriesEntry>();
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"EncounterId\":"); ToStringHelper.ToString(EncounterId, sb);
            sb.Append(",\"RegionId\":"); ToStringHelper.ToString(RegionId, sb);
            sb.Append(",\"RequirementGroupId\":"); ToStringHelper.ToString(RequirementGroupId, sb);
            sb.Append(",\"Entries\":"); ToStringHelper.ToString(Entries, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 지역별 출현 목록이다. 확률은 클라이언트가 알 필요가 없다.
    /// </summary>
    [System.Serializable]
    public partial class EncounterTableTable : IEnumerable<EncounterTableRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "EncounterId", "RegionId", "RequirementGroupId", "Entries" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.EncounterId, r.RegionId, r.RequirementGroupId, r.Entries });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<EncounterTableRecord> Records => _records;
        private List<EncounterTableRecord> _records = new List<EncounterTableRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<EncounterTableRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<EncounterTableRecord> IEnumerable<EncounterTableRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'EncounterId'
        public Dictionary<string, EncounterTableRecord> RecordsByEncounterId => _recordsByEncounterId;
        private Dictionary<string, EncounterTableRecord> _recordsByEncounterId = new Dictionary<string, EncounterTableRecord>();

        /// <summary>
        /// The row with this `EncounterId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public EncounterTableRecord FindByEncounterId(string key)
            => _recordsByEncounterId.TryGetValue(key, out EncounterTableRecord record) ? record : null;

        /// <summary>
        /// The row with this `EncounterId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByEncounterId(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public EncounterTableRecord GetByEncounterIdOrThrow(string key)
        {
            if (!_recordsByEncounterId.TryGetValue(key, out EncounterTableRecord record))
                throw new TabbitException($"There is no record in table `EncounterTable` that corresponds to field `EncounterId` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `EncounterId`.</summary>
        public bool ContainsEncounterId(string key) => _recordsByEncounterId.ContainsKey(key);
        #endregion // Indexing by `EncounterId`

        /// <summary>Each row with the `EncounterId` it is keyed by.</summary>
        /// <remarks>
        /// `foreach (var (key, row) in table.Entries)`. What this saves a caller is not the
        /// key value - the row carries it - but having to know which column the key is:
        /// `EncounterId` here, something else in the next table.
        ///
        /// A struct that enumerates itself, so the loop allocates nothing, and the rows in
        /// the order the file wrote them rather than the order a dictionary happens to hold
        /// them - which makes this and a plain `foreach` over the table agree.
        ///
        /// Only the primary key gets this. A table keyed by several columns together has no
        /// single key value to pair a row with.
        /// </remarks>
        public struct EntryEnumerator
        {
            private readonly List<EncounterTableRecord> _rows;
            private int _at;

            internal EntryEnumerator(List<EncounterTableRecord> rows)
            {
                _rows = rows;
                _at = -1;
            }

            public EntryEnumerator GetEnumerator() => this;

            public bool MoveNext() => ++_at < _rows.Count;

            public (string Key, EncounterTableRecord Row) Current
                => (_rows[_at].EncounterId, _rows[_at]);
        }

        /// <summary>Each row with the `EncounterId` it is keyed by.</summary>
        public EntryEnumerator Entries => new EntryEnumerator(_records);

        /// <summary>
        /// The row with this `EncounterId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// `GetByEncounterIdOrThrow` under the spelling the language uses for a
        /// keyed collection. It throws because that is what `Dictionary` does here; a
        /// language whose own map answers with null generates a subscript that answers with
        /// null. spec/targets/table-collection-surface.md section 5.7.
        ///
        /// **This is the key, not the row's position.** `table[0]` is the row whose
        /// `EncounterId` is 0, not the first row - the rows in order are
        /// `Records`. Nothing in the type says which, which is why the table itself is not
        /// subscriptable by position anywhere.
        ///
        /// It does not replace `FindByEncounterId`: a key that may be absent
        /// wants the one whose name says a miss is an ordinary answer.
        /// </remarks>
        public EncounterTableRecord this[string key] => GetByEncounterIdOrThrow(key);

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<EncounterTableRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new EncounterTableRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "EncounterTable.EncounterId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "EncounterTable.EncounterId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._encounterId = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "EncounterTable.RegionId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "EncounterTable.RegionId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._regionId_Region_index = value;
                                record._regionId = default(RegionRecord); // will be assigned.
                                record._regionId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "EncounterTable.RequirementGroupId", TcbTable.KindScalar, true, TcbTable.ElementString);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "EncounterTable.RequirementGroupId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._requirementGroupId_RequirementGroup_index = value;
                                record._requirementGroupId = default(RequirementGroupRecord); // will be assigned.
                                record._requirementGroupId_F = false;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._requirementGroupIdHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._requirementGroupIdHasValue)
                                records[i]._requirementGroupId = default(RequirementGroupRecord);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "EncounterTable.Entries.MonsterId", TcbTable.KindArray, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "EncounterTable.Entries.MonsterId");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._entries = new EncounterTableRecord.EntriesEntry[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._entries[j].MonsterId = cursor.NextString();
                                record._entries[j].MonsterByMonsterId = default(MonsterRecord); // will be assigned.
                                record._entries[j].MonsterId_F = false;
                            }
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "EncounterTable.Entries.Weight", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "EncounterTable.Entries.Weight");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._entries.Length != elementCount)
                            {
                                throw new TcbException(
                                    "EncounterTable.Entries: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._entries.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._entries[j].Weight = cursor.NextI32();
                            }
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "EncounterTable.Entries.EncounterSlot", TcbTable.KindArray, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "EncounterTable.Entries.EncounterSlot");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._entries.Length != elementCount)
                            {
                                throw new TcbException(
                                    "EncounterTable.Entries: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._entries.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._entries[j].EncounterSlot = (global::Wildling.Data.EncounterSlot)cursor.NextI32();
                            }
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByEncounterId = new Dictionary<string, EncounterTableRecord>(count);
            foreach (var record in records)
                recordsByEncounterId.Add(record.EncounterId, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByEncounterId = recordsByEncounterId;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
