// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    /// <summary>
    /// 속성 상성 배수이다. 조합이 키이므로 복합 기본 인덱스이다.
    /// </summary>
    [System.Serializable]
    public partial class ElementAffinityTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 공격 속성
            /// </summary>
            public global::Wildling.Data.Element Attacker => _attacker;

            /// <summary>
            /// 방어 속성
            /// </summary>
            public global::Wildling.Data.Element Defender => _defender;

            /// <summary>
            /// 피해 배수. 만분율
            /// </summary>
            public int Factor => _factor;
            #endregion

            #region Storage
            internal global::Wildling.Data.Element _attacker;
            internal global::Wildling.Data.Element _defender;
            internal int _factor;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Attacker\":"); ToStringHelper.ToString(Attacker, sb);
                sb.Append(",\"Defender\":"); ToStringHelper.ToString(Defender, sb);
                sb.Append(",\"Factor\":"); ToStringHelper.ToString(Factor, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Attacker", "Defender", "Factor" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Attacker, r.Defender, r.Factor });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Attacker and Defender'
        private Dictionary<string, Record> _recordsByAttackerAndDefender = new Dictionary<string, Record>();

        /// <summary>Joins the columns of the `Attacker and Defender` key into the text the map is keyed by.</summary>
        private static string KeyOfAttackerAndDefender(global::Wildling.Data.Element attackerKey, global::Wildling.Data.Element defenderKey)
        {
            var parts = new string[]
            {
                ((long)attackerKey).ToString(System.Globalization.CultureInfo.InvariantCulture),
                ((long)defenderKey).ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `Attacker and Defender`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByAttackerAndDefender(global::Wildling.Data.Element attackerKey, global::Wildling.Data.Element defenderKey)
            => _recordsByAttackerAndDefender.TryGetValue(KeyOfAttackerAndDefender(attackerKey, defenderKey), out Record record) ? record : null;

        /// <summary>
        /// The row with this `Attacker and Defender`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public Record GetByAttackerAndDefenderOrThrow(global::Wildling.Data.Element attackerKey, global::Wildling.Data.Element defenderKey)
        {
            if (!_recordsByAttackerAndDefender.TryGetValue(KeyOfAttackerAndDefender(attackerKey, defenderKey), out Record record))
                throw new TabbitException($"There is no record in table `ElementAffinity` that corresponds to field `Attacker and Defender` value ({attackerKey}, {defenderKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Attacker and Defender`.</summary>
        public bool ContainsAttackerAndDefender(global::Wildling.Data.Element attackerKey, global::Wildling.Data.Element defenderKey) => _recordsByAttackerAndDefender.ContainsKey(KeyOfAttackerAndDefender(attackerKey, defenderKey));
        #endregion // Indexing by `Attacker and Defender`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "ElementAffinity.Attacker", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "ElementAffinity.Attacker");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._attacker = (global::Wildling.Data.Element)value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "ElementAffinity.Defender", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "ElementAffinity.Defender");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._defender = (global::Wildling.Data.Element)value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "ElementAffinity.Factor", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "ElementAffinity.Factor");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._factor = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByAttackerAndDefender = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsByAttackerAndDefender.Add(KeyOfAttackerAndDefender(record.Attacker, record.Defender), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByAttackerAndDefender = recordsByAttackerAndDefender;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
