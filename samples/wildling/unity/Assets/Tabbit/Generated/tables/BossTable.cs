// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    [System.Serializable]
    public partial class BossRecord
    {
        #region Values
        /// <summary>
        /// 식별자
        /// </summary>
        public string BossId => _bossId;

        /// <summary>
        /// 외형과 기본 능력치의 출처
        /// </summary>
        public string MonsterId => _monsterId_Monster_index;
        public MonsterRecord MonsterByMonsterId => _monsterId;

        /// <summary>
        /// 계수. 만분율
        /// </summary>
        public StatFactorEntry StatFactor => _statFactor;

        /// <summary>
        /// 특수 능력
        /// </summary>
        public global::Wildling.Data.BossAbility Ability => _ability;

        /// <summary>
        /// 페이즈별 행동 순서
        /// </summary>
        public int[][] AbilityPattern => _abilityPattern;
        internal const int AbilityPattern_N = 3;

        /// <summary>
        /// 등장 연출의 회전
        /// </summary>
        public SpawnRotationEntry SpawnRotation => _spawnRotation;

        /// <summary>
        /// 실루엣 색
        /// </summary>
        public TintEntry Tint => _tint;

        /// <summary>
        /// 특수 능력의 효과. 원소가 행에서 온다
        /// </summary>
        public Effect[] Effects
            => _effects_value ?? (_effects_value = BuildEffects());

        private Effect[] _effects_value;

        private Effect[] BuildEffects()
        {
            var built = new Effect[_effects.Length];

            for (int at = 0; at < _effects.Length; at++)
                built[at] = BuildEffectsElement(_effects[at]);

            return built;
        }

        private static Effect BuildEffectsElement(
            EffectsEntry entry)
        {
            switch (entry.Type)
            {
                case 1:
                    return new DamageEffect
                    {
                        Chance = entry.Chance,
                        Power = entry.Power,
                    };
                case 2:
                    return new HealEffect
                    {
                        Chance = entry.Chance,
                        Power = entry.Power,
                    };
                case 3:
                    return new StatusEffect
                    {
                        Chance = entry.Chance,
                        Status = entry.Status,
                        Duration = entry.Duration,
                    };
                case 4:
                    return new BuffEffect
                    {
                        Chance = entry.Chance,
                        Stat = entry.Stat,
                        Ratio = entry.Ratio,
                        Duration = entry.Duration,
                    };
            }

            // A number no variant claims - a file written by a build that had one this code
            // does not. Unlike a column added later, which the reader skips, that makes the
            // element uninterpretable.
            throw new TcbException(
                "Effect: no variant is numbered " + entry.Type);
        }
        #endregion

        /// <summary>One element of <see cref="StatFactor"/>.</summary>
        [System.Serializable]
        public struct StatFactorEntry
        {
            /// 계수. 만분율
            public int Hp;
            /// 공격 계수
            public int Attack;
            /// 방어 계수
            public int Defense;
            /// 행동 순서 계수
            public int Speed;
            /// 치명타 확률 계수
            public int CritRate;
            /// 치명타 배수 계수
            public int CritPower;

            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Hp\":"); ToStringHelper.ToString(Hp, sb);
                sb.Append(",\"Attack\":"); ToStringHelper.ToString(Attack, sb);
                sb.Append(",\"Defense\":"); ToStringHelper.ToString(Defense, sb);
                sb.Append(",\"Speed\":"); ToStringHelper.ToString(Speed, sb);
                sb.Append(",\"CritRate\":"); ToStringHelper.ToString(CritRate, sb);
                sb.Append(",\"CritPower\":"); ToStringHelper.ToString(CritPower, sb);
                sb.Append("}");
                return sb.ToString();
            }
        }

        private static int[][] NewAbilityPattern()
        {
            var result = new int[AbilityPattern_N][];
            for (int i = 0; i < result.Length; i++)
                result[i] = System.Array.Empty<int>();
            return result;
        }

        /// <summary>One element of <see cref="SpawnRotation"/>.</summary>
        [System.Serializable]
        public struct SpawnRotationEntry
        {
            /// 등장 연출의 회전
            public float X;
            /// 등장 연출의 회전
            public float Y;
            /// 등장 연출의 회전
            public float Z;
            /// 등장 연출의 회전
            public float W;

            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"X\":"); ToStringHelper.ToString(X, sb);
                sb.Append(",\"Y\":"); ToStringHelper.ToString(Y, sb);
                sb.Append(",\"Z\":"); ToStringHelper.ToString(Z, sb);
                sb.Append(",\"W\":"); ToStringHelper.ToString(W, sb);
                sb.Append("}");
                return sb.ToString();
            }
        }

        /// <summary>One element of <see cref="Tint"/>.</summary>
        [System.Serializable]
        public struct TintEntry
        {
            /// 실루엣 색
            public int R;
            /// 실루엣 색
            public int G;
            /// 실루엣 색
            public int B;
            /// 실루엣 색
            public int A;

            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"R\":"); ToStringHelper.ToString(R, sb);
                sb.Append(",\"G\":"); ToStringHelper.ToString(G, sb);
                sb.Append(",\"B\":"); ToStringHelper.ToString(B, sb);
                sb.Append(",\"A\":"); ToStringHelper.ToString(A, sb);
                sb.Append("}");
                return sb.ToString();
            }
        }

        /// <summary>One element of <see cref="Effects"/>.</summary>
        [System.Serializable]
        public struct EffectsEntry
        {
            /// 특수 능력의 효과. 원소가 행에서 온다
            public int Type;
            /// 발동 확률. 만분율
            public int Chance;
            /// 피해 또는 회복 배수
            public int Power;
            /// 부여하는 상태
            public global::Wildling.Data.StatusKind Status;
            /// 변동시키는 능력치
            public global::Wildling.Data.StatKind Stat;
            /// 변동률. 만분율
            public int Ratio;
            /// 지속 턴
            public int Duration;

            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Type\":"); ToStringHelper.ToString(Type, sb);
                sb.Append(",\"Chance\":"); ToStringHelper.ToString(Chance, sb);
                sb.Append(",\"Power\":"); ToStringHelper.ToString(Power, sb);
                sb.Append(",\"Status\":"); ToStringHelper.ToString(Status, sb);
                sb.Append(",\"Stat\":"); ToStringHelper.ToString(Stat, sb);
                sb.Append(",\"Ratio\":"); ToStringHelper.ToString(Ratio, sb);
                sb.Append(",\"Duration\":"); ToStringHelper.ToString(Duration, sb);
                sb.Append("}");
                return sb.ToString();
            }
        }

        #region Reference wiring
        public void SetReference_MonsterId_INTERNAL(MonsterRecord value) => _monsterId = value;
        #endregion

        #region Storage
        internal string _bossId = "";
        internal MonsterRecord _monsterId;
        internal string _monsterId_Monster_index;
        public bool _monsterId_F = false;
        internal StatFactorEntry _statFactor;
        internal global::Wildling.Data.BossAbility _ability;
        internal int[][] _abilityPattern = NewAbilityPattern();
        internal SpawnRotationEntry _spawnRotation;
        internal TintEntry _tint;
        internal EffectsEntry[] _effects = System.Array.Empty<EffectsEntry>();
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"BossId\":"); ToStringHelper.ToString(BossId, sb);
            sb.Append(",\"MonsterId\":"); ToStringHelper.ToString(MonsterId, sb);
            sb.Append(",\"StatFactor\":"); ToStringHelper.ToString(StatFactor, sb);
            sb.Append(",\"Ability\":"); ToStringHelper.ToString(Ability, sb);
            sb.Append(",\"AbilityPattern\":"); ToStringHelper.ToString(AbilityPattern, sb);
            sb.Append(",\"SpawnRotation\":"); ToStringHelper.ToString(SpawnRotation, sb);
            sb.Append(",\"Tint\":"); ToStringHelper.ToString(Tint, sb);
            sb.Append(",\"Effects\":"); ToStringHelper.ToString(Effects, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 지역 수호자이다. 효과가 멀티 로우 다형 배열이므로 5단계를 기다린다.
    /// </summary>
    [System.Serializable]
    public partial class BossTable : IEnumerable<BossRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "BossId", "MonsterId", "StatFactor", "Ability", "AbilityPattern", "SpawnRotation", "Tint", "Effects" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.BossId, r.MonsterId, r.StatFactor, r.Ability, r.AbilityPattern, r.SpawnRotation, r.Tint, r.Effects });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<BossRecord> Records => _records;
        private List<BossRecord> _records = new List<BossRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<BossRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<BossRecord> IEnumerable<BossRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'BossId'
        public Dictionary<string, BossRecord> RecordsByBossId => _recordsByBossId;
        private Dictionary<string, BossRecord> _recordsByBossId = new Dictionary<string, BossRecord>();

        /// <summary>
        /// The row with this `BossId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public BossRecord FindByBossId(string key)
            => _recordsByBossId.TryGetValue(key, out BossRecord record) ? record : null;

        /// <summary>
        /// The row with this `BossId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByBossId(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public BossRecord GetByBossIdOrThrow(string key)
        {
            if (!_recordsByBossId.TryGetValue(key, out BossRecord record))
                throw new TabbitException($"There is no record in table `Boss` that corresponds to field `BossId` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `BossId`.</summary>
        public bool ContainsBossId(string key) => _recordsByBossId.ContainsKey(key);
        #endregion // Indexing by `BossId`

        /// <summary>Each row with the `BossId` it is keyed by.</summary>
        /// <remarks>
        /// `foreach (var (key, row) in table.Entries)`. What this saves a caller is not the
        /// key value - the row carries it - but having to know which column the key is:
        /// `BossId` here, something else in the next table.
        ///
        /// A struct that enumerates itself, so the loop allocates nothing, and the rows in
        /// the order the file wrote them rather than the order a dictionary happens to hold
        /// them - which makes this and a plain `foreach` over the table agree.
        ///
        /// Only the primary key gets this. A table keyed by several columns together has no
        /// single key value to pair a row with.
        /// </remarks>
        public struct EntryEnumerator
        {
            private readonly List<BossRecord> _rows;
            private int _at;

            internal EntryEnumerator(List<BossRecord> rows)
            {
                _rows = rows;
                _at = -1;
            }

            public EntryEnumerator GetEnumerator() => this;

            public bool MoveNext() => ++_at < _rows.Count;

            public (string Key, BossRecord Row) Current
                => (_rows[_at].BossId, _rows[_at]);
        }

        /// <summary>Each row with the `BossId` it is keyed by.</summary>
        public EntryEnumerator Entries => new EntryEnumerator(_records);

        /// <summary>
        /// The row with this `BossId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// `GetByBossIdOrThrow` under the spelling the language uses for a
        /// keyed collection. It throws because that is what `Dictionary` does here; a
        /// language whose own map answers with null generates a subscript that answers with
        /// null. spec/targets/table-collection-surface.md section 5.7.
        ///
        /// **This is the key, not the row's position.** `table[0]` is the row whose
        /// `BossId` is 0, not the first row - the rows in order are
        /// `Records`. Nothing in the type says which, which is why the table itself is not
        /// subscriptable by position anywhere.
        ///
        /// It does not replace `FindByBossId`: a key that may be absent
        /// wants the one whose name says a miss is an ordinary answer.
        /// </remarks>
        public BossRecord this[string key] => GetByBossIdOrThrow(key);

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<BossRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new BossRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "Boss.BossId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.BossId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._bossId = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "Boss.MonsterId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.MonsterId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._monsterId_Monster_index = value;
                                record._monsterId = default(MonsterRecord); // will be assigned.
                                record._monsterId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "Boss.StatFactor.Hp", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.StatFactor.Hp");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._statFactor.Hp = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "Boss.StatFactor.Attack", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.StatFactor.Attack");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._statFactor.Attack = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "Boss.StatFactor.Defense", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.StatFactor.Defense");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._statFactor.Defense = value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "Boss.StatFactor.Speed", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.StatFactor.Speed");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._statFactor.Speed = value;
                            } while (--n > 0);
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "Boss.StatFactor.CritRate", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.StatFactor.CritRate");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._statFactor.CritRate = value;
                            } while (--n > 0);
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "Boss.StatFactor.CritPower", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.StatFactor.CritPower");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._statFactor.CritPower = value;
                            } while (--n > 0);
                        }
                        break;

                    case 9:
                        TcbTable.CheckColumn(column, "Boss.Ability", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Ability");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._ability = (global::Wildling.Data.BossAbility)value;
                            } while (--n > 0);
                        }
                        break;

                    case 10:
                        TcbTable.CheckColumn(column, "Boss.AbilityPattern.0", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.AbilityPattern.0");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._abilityPattern[0] =
                                new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._abilityPattern[0][j] = cursor.NextI32();
                            }
                        }
                        break;

                    case 11:
                        TcbTable.CheckColumn(column, "Boss.AbilityPattern.1", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.AbilityPattern.1");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._abilityPattern[1] =
                                new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._abilityPattern[1][j] = cursor.NextI32();
                            }
                        }
                        break;

                    case 12:
                        TcbTable.CheckColumn(column, "Boss.AbilityPattern.2", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.AbilityPattern.2");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._abilityPattern[2] =
                                new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._abilityPattern[2][j] = cursor.NextI32();
                            }
                        }
                        break;

                    case 13:
                        TcbTable.CheckColumn(column, "Boss.SpawnRotation.X", TcbTable.KindScalar, false, TcbTable.ElementF32);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.SpawnRotation.X");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._spawnRotation.X = cursor.NextF32();
                        }
                        break;

                    case 14:
                        TcbTable.CheckColumn(column, "Boss.SpawnRotation.Y", TcbTable.KindScalar, false, TcbTable.ElementF32);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.SpawnRotation.Y");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._spawnRotation.Y = cursor.NextF32();
                        }
                        break;

                    case 15:
                        TcbTable.CheckColumn(column, "Boss.SpawnRotation.Z", TcbTable.KindScalar, false, TcbTable.ElementF32);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.SpawnRotation.Z");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._spawnRotation.Z = cursor.NextF32();
                        }
                        break;

                    case 16:
                        TcbTable.CheckColumn(column, "Boss.SpawnRotation.W", TcbTable.KindScalar, false, TcbTable.ElementF32);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.SpawnRotation.W");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._spawnRotation.W = cursor.NextF32();
                        }
                        break;

                    case 17:
                        TcbTable.CheckColumn(column, "Boss.Tint.R", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Tint.R");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._tint.R = value;
                            } while (--n > 0);
                        }
                        break;

                    case 18:
                        TcbTable.CheckColumn(column, "Boss.Tint.G", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Tint.G");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._tint.G = value;
                            } while (--n > 0);
                        }
                        break;

                    case 19:
                        TcbTable.CheckColumn(column, "Boss.Tint.B", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Tint.B");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._tint.B = value;
                            } while (--n > 0);
                        }
                        break;

                    case 20:
                        TcbTable.CheckColumn(column, "Boss.Tint.A", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Tint.A");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._tint.A = value;
                            } while (--n > 0);
                        }
                        break;

                    case 21:
                        TcbTable.CheckColumn(column, "Boss.Effects.Type", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Effects.Type");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._effects = new BossRecord.EffectsEntry[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._effects[j].Type = cursor.NextI32();
                            }
                        }
                        break;

                    case 22:
                        TcbTable.CheckColumn(column, "Boss.Effects.Chance", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Effects.Chance");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._effects.Length != elementCount)
                            {
                                throw new TcbException(
                                    "Boss.Effects: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._effects.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._effects[j].Chance = cursor.NextI32();
                            }
                        }
                        break;

                    case 23:
                        TcbTable.CheckColumn(column, "Boss.Effects.Power", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Effects.Power");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._effects.Length != elementCount)
                            {
                                throw new TcbException(
                                    "Boss.Effects: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._effects.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._effects[j].Power = cursor.NextI32();
                            }
                        }
                        break;

                    case 24:
                        TcbTable.CheckColumn(column, "Boss.Effects.Status", TcbTable.KindArray, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Effects.Status");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._effects.Length != elementCount)
                            {
                                throw new TcbException(
                                    "Boss.Effects: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._effects.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._effects[j].Status = (global::Wildling.Data.StatusKind)cursor.NextI32();
                            }
                        }
                        break;

                    case 25:
                        TcbTable.CheckColumn(column, "Boss.Effects.Stat", TcbTable.KindArray, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Effects.Stat");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._effects.Length != elementCount)
                            {
                                throw new TcbException(
                                    "Boss.Effects: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._effects.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._effects[j].Stat = (global::Wildling.Data.StatKind)cursor.NextI32();
                            }
                        }
                        break;

                    case 26:
                        TcbTable.CheckColumn(column, "Boss.Effects.Ratio", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Effects.Ratio");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._effects.Length != elementCount)
                            {
                                throw new TcbException(
                                    "Boss.Effects: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._effects.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._effects[j].Ratio = cursor.NextI32();
                            }
                        }
                        break;

                    case 27:
                        TcbTable.CheckColumn(column, "Boss.Effects.Duration", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Boss.Effects.Duration");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._effects.Length != elementCount)
                            {
                                throw new TcbException(
                                    "Boss.Effects: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._effects.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._effects[j].Duration = cursor.NextI32();
                            }
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByBossId = new Dictionary<string, BossRecord>(count);
            foreach (var record in records)
                recordsByBossId.Add(record.BossId, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByBossId = recordsByBossId;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
