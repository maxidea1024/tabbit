// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Wildling.Data
{
    /// <summary>
    /// 각성 관계이다. 연장 행은 소모 재화의 원소이다.
    /// </summary>
    [System.Serializable]
    public partial class MonsterAwakeningTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 각성 전
            /// </summary>
            public string FromMonsterId => _fromMonsterId_Monster_index;
            public MonsterTable.Record MonsterByFromMonsterId => _fromMonsterId;

            /// <summary>
            /// 각성 후
            /// </summary>
            public string ToMonsterId => _toMonsterId_Monster_index;
            public MonsterTable.Record MonsterByToMonsterId => _toMonsterId;

            /// <summary>
            /// 각성으로 더해지는 값
            /// </summary>
            public GainEntry Gain => _gain;

            /// <summary>
            /// 각성 조건
            /// </summary>
            public string RequirementGroupId => _requirementGroupId_RequirementGroup_index;
            public RequirementGroupTable.Record RequirementGroupByRequirementGroupId => _requirementGroupId;

            /// <summary>
            /// 어느 재화인가.
            /// </summary>
            public CostsEntry[] Costs => _costs;
            #endregion

            /// <summary>One element of <see cref="Gain"/>.</summary>
            [System.Serializable]
            public struct GainEntry
            {
                /// 각성으로 더해지는 값
                public int Hp;
                public int Attack;
                public int Defense;
                public int Speed;
                public int CritRate;
                public int CritPower;

                public override string ToString()
                {
                    var sb = new StringBuilder("{");
                    sb.Append("\"Hp\":"); ToStringHelper.ToString(Hp, sb);
                    sb.Append(",\"Attack\":"); ToStringHelper.ToString(Attack, sb);
                    sb.Append(",\"Defense\":"); ToStringHelper.ToString(Defense, sb);
                    sb.Append(",\"Speed\":"); ToStringHelper.ToString(Speed, sb);
                    sb.Append(",\"CritRate\":"); ToStringHelper.ToString(CritRate, sb);
                    sb.Append(",\"CritPower\":"); ToStringHelper.ToString(CritPower, sb);
                    sb.Append("}");
                    return sb.ToString();
                }
            }

            /// <summary>One element of <see cref="Costs"/>.</summary>
            [System.Serializable]
            public struct CostsEntry
            {
                /// 어느 재화인가.
                public string CurrencyId;
                public CurrencyTable.Record CurrencyByCurrencyId;
                public bool CurrencyId_F;
                /// 수량.
                public int Amount;

                public override string ToString()
                {
                    var sb = new StringBuilder("{");
                    sb.Append("\"CurrencyId\":"); ToStringHelper.ToString(CurrencyId, sb);
                    sb.Append(",\"Amount\":"); ToStringHelper.ToString(Amount, sb);
                    sb.Append("}");
                    return sb.ToString();
                }
            }

            #region Reference wiring
            public void SetReference_FromMonsterId_INTERNAL(MonsterTable.Record value) => _fromMonsterId = value;
            public void SetReference_ToMonsterId_INTERNAL(MonsterTable.Record value) => _toMonsterId = value;
            public void SetReference_RequirementGroupId_INTERNAL(RequirementGroupTable.Record value) => _requirementGroupId = value;
            #endregion

            #region Storage
            internal MonsterTable.Record _fromMonsterId;
            internal string _fromMonsterId_Monster_index;
            public bool _fromMonsterId_F = false;
            internal MonsterTable.Record _toMonsterId;
            internal string _toMonsterId_Monster_index;
            public bool _toMonsterId_F = false;
            internal GainEntry _gain;
            internal RequirementGroupTable.Record _requirementGroupId;
            internal string _requirementGroupId_RequirementGroup_index;
            public bool _requirementGroupId_F = false;
            internal CostsEntry[] _costs = System.Array.Empty<CostsEntry>();
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"FromMonsterId\":"); ToStringHelper.ToString(FromMonsterId, sb);
                sb.Append(",\"ToMonsterId\":"); ToStringHelper.ToString(ToMonsterId, sb);
                sb.Append(",\"Gain\":"); ToStringHelper.ToString(Gain, sb);
                sb.Append(",\"RequirementGroupId\":"); ToStringHelper.ToString(RequirementGroupId, sb);
                sb.Append(",\"Costs\":"); ToStringHelper.ToString(Costs, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "FromMonsterId", "ToMonsterId", "Gain", "RequirementGroupId", "Costs" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.FromMonsterId, r.ToMonsterId, r.Gain, r.RequirementGroupId, r.Costs });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'FromMonsterId'
        public Dictionary<MonsterTable.Record, Record> RecordsByFromMonsterId => _recordsByFromMonsterId;
        private Dictionary<MonsterTable.Record, Record> _recordsByFromMonsterId = new Dictionary<MonsterTable.Record, Record>();

        /// <summary>
        /// The row with this `FromMonsterId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByFromMonsterId(MonsterTable.Record key)
            => _recordsByFromMonsterId.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `FromMonsterId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByFromMonsterId(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByFromMonsterIdOrThrow(MonsterTable.Record key)
        {
            if (!_recordsByFromMonsterId.TryGetValue(key, out Record record))
                throw new TabbitException($"There is no record in table `MonsterAwakening` that corresponds to field `FromMonsterId` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `FromMonsterId`.</summary>
        public bool ContainsFromMonsterId(MonsterTable.Record key) => _recordsByFromMonsterId.ContainsKey(key);
        #endregion // Indexing by `FromMonsterId`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await WildlingData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `WildlingData.EncryptionKey` and `WildlingData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, WildlingData.EncryptionKey, WildlingData.MacKey, WildlingData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "MonsterAwakening.FromMonsterId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.FromMonsterId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._fromMonsterId_Monster_index = value;
                                record._fromMonsterId = default(MonsterTable.Record); // will be assigned.
                                record._fromMonsterId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "MonsterAwakening.ToMonsterId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.ToMonsterId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._toMonsterId_Monster_index = value;
                                record._toMonsterId = default(MonsterTable.Record); // will be assigned.
                                record._toMonsterId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "MonsterAwakening.Gain.Hp", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.Gain.Hp");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._gain.Hp = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "MonsterAwakening.Gain.Attack", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.Gain.Attack");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._gain.Attack = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "MonsterAwakening.Gain.Defense", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.Gain.Defense");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._gain.Defense = value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "MonsterAwakening.Gain.Speed", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.Gain.Speed");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._gain.Speed = value;
                            } while (--n > 0);
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "MonsterAwakening.Gain.CritRate", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.Gain.CritRate");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._gain.CritRate = value;
                            } while (--n > 0);
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "MonsterAwakening.Gain.CritPower", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.Gain.CritPower");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._gain.CritPower = value;
                            } while (--n > 0);
                        }
                        break;

                    case 9:
                        TcbTable.CheckColumn(column, "MonsterAwakening.RequirementGroupId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.RequirementGroupId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._requirementGroupId_RequirementGroup_index = value;
                                record._requirementGroupId = default(RequirementGroupTable.Record); // will be assigned.
                                record._requirementGroupId_F = false;
                            } while (--n > 0);
                        }
                        break;

                    case 10:
                        TcbTable.CheckColumn(column, "MonsterAwakening.Costs.CurrencyId", TcbTable.KindArray, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.Costs.CurrencyId");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            record._costs = new Record.CostsEntry[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._costs[j].CurrencyId = cursor.NextString();
                                record._costs[j].CurrencyByCurrencyId = default(CurrencyTable.Record); // will be assigned.
                                record._costs[j].CurrencyId_F = false;
                            }
                        }
                        break;

                    case 11:
                        TcbTable.CheckColumn(column, "MonsterAwakening.Costs.Amount", TcbTable.KindArray, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "MonsterAwakening.Costs.Amount");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            int elementCount;
                            elementCount = cursor.NextLength();
                            if (record._costs.Length != elementCount)
                            {
                                throw new TcbException(
                                    "MonsterAwakening.Costs: the file gives this row "
                                    + elementCount + " elements for one member of the record and "
                                    + record._costs.Length + " for another. Every member of a "
                                    + "record carries the same element count, so the file is damaged.");
                            }
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._costs[j].Amount = cursor.NextI32();
                            }
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByFromMonsterId = new Dictionary<MonsterTable.Record, Record>(count);
            foreach (var record in records)
                recordsByFromMonsterId.Add(record.FromMonsterId, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByFromMonsterId = recordsByFromMonsterId;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Wildling.Data
