// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Clover.Data
{
    [System.Serializable]
    public partial class TransitionRecord
    {
        #region Values
        /// <summary>
        /// 갈리는 자리
        /// </summary>
        public string TransitionId => _transitionId;

        /// <summary>
        /// 지우는 방법
        /// </summary>
        public global::Clover.Data.TransitionKind Kind => _kind;

        /// <summary>
        /// 지우는 시간. 밀리초
        /// </summary>
        public int OutMs => _outMs;

        /// <summary>
        /// 아무것도 보이지 않는 채로 머무는 시간
        /// </summary>
        public int HoldMs => _holdMs;

        /// <summary>
        /// 되돌리는 시간
        /// </summary>
        public int InMs => _inMs;

        /// <summary>
        /// 다 지워진 자리에 남는 색
        /// </summary>
        public string Ink => _ink;

        /// <summary>
        /// 다가오는가. 밀림과 옆으로가 이 값을 봅니다
        /// </summary>
        public bool Toward => _toward;

        /// <summary>
        /// 시작할 때 나는 소리. `SoundCue` 의 이름
        /// </summary>
        public string Cue => _cue;
        /// <summary>Whether this row has a value for <see cref="Cue"/>.</summary>
        public bool HasCue => _cueHasValue;

        /// <summary>
        /// 어느 자리인가
        /// </summary>
        public string Note => _note;
        #endregion

        #region Storage
        internal string _transitionId = "";
        internal global::Clover.Data.TransitionKind _kind;
        internal int _outMs;
        internal int _holdMs;
        internal int _inMs;
        internal string _ink = "";
        internal bool _toward;
        internal string _cue = "";
        internal bool _cueHasValue;
        internal string _note = "";
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"TransitionId\":"); ToStringHelper.ToString(TransitionId, sb);
            sb.Append(",\"Kind\":"); ToStringHelper.ToString(Kind, sb);
            sb.Append(",\"OutMs\":"); ToStringHelper.ToString(OutMs, sb);
            sb.Append(",\"HoldMs\":"); ToStringHelper.ToString(HoldMs, sb);
            sb.Append(",\"InMs\":"); ToStringHelper.ToString(InMs, sb);
            sb.Append(",\"Ink\":"); ToStringHelper.ToString(Ink, sb);
            sb.Append(",\"Toward\":"); ToStringHelper.ToString(Toward, sb);
            sb.Append(",\"Cue\":"); ToStringHelper.ToString(Cue, sb);
            sb.Append(",\"Note\":"); ToStringHelper.ToString(Note, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 씬이 갈릴 때 화면을 어떻게 지우는가입니다. **덮개를 그리는 것이 아니라 화면 자체를 처리합니다.**
    /// </summary>
    [System.Serializable]
    public partial class TransitionTable : IEnumerable<TransitionRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "TransitionId", "Kind", "OutMs", "HoldMs", "InMs", "Ink", "Toward", "Cue", "Note" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.TransitionId, r.Kind, r.OutMs, r.HoldMs, r.InMs, r.Ink, r.Toward, r.Cue, r.Note });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<TransitionRecord> Records => _records;
        private List<TransitionRecord> _records = new List<TransitionRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<TransitionRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<TransitionRecord> IEnumerable<TransitionRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'TransitionId'
        public Dictionary<string, TransitionRecord> RecordsByTransitionId => _recordsByTransitionId;
        private Dictionary<string, TransitionRecord> _recordsByTransitionId = new Dictionary<string, TransitionRecord>();

        /// <summary>
        /// The row with this `TransitionId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public TransitionRecord FindByTransitionId(string key)
            => _recordsByTransitionId.TryGetValue(key, out TransitionRecord record) ? record : null;

        /// <summary>
        /// The row with this `TransitionId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByTransitionId(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public TransitionRecord GetByTransitionIdOrThrow(string key)
        {
            if (!_recordsByTransitionId.TryGetValue(key, out TransitionRecord record))
                throw new TabbitException($"There is no record in table `Transition` that corresponds to field `TransitionId` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `TransitionId`.</summary>
        public bool ContainsTransitionId(string key) => _recordsByTransitionId.ContainsKey(key);
        #endregion // Indexing by `TransitionId`

        /// <summary>Each row with the `TransitionId` it is keyed by.</summary>
        /// <remarks>
        /// `foreach (var (key, row) in table.Entries)`. What this saves a caller is not the
        /// key value - the row carries it - but having to know which column the key is:
        /// `TransitionId` here, something else in the next table.
        ///
        /// A struct that enumerates itself, so the loop allocates nothing, and the rows in
        /// the order the file wrote them rather than the order a dictionary happens to hold
        /// them - which makes this and a plain `foreach` over the table agree.
        ///
        /// Only the primary key gets this. A table keyed by several columns together has no
        /// single key value to pair a row with.
        /// </remarks>
        public struct EntryEnumerator
        {
            private readonly List<TransitionRecord> _rows;
            private int _at;

            internal EntryEnumerator(List<TransitionRecord> rows)
            {
                _rows = rows;
                _at = -1;
            }

            public EntryEnumerator GetEnumerator() => this;

            public bool MoveNext() => ++_at < _rows.Count;

            public (string Key, TransitionRecord Row) Current
                => (_rows[_at].TransitionId, _rows[_at]);
        }

        /// <summary>Each row with the `TransitionId` it is keyed by.</summary>
        public EntryEnumerator Entries => new EntryEnumerator(_records);

        /// <summary>
        /// The row with this `TransitionId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// `GetByTransitionIdOrThrow` under the spelling the language uses for a
        /// keyed collection. It throws because that is what `Dictionary` does here; a
        /// language whose own map answers with null generates a subscript that answers with
        /// null. spec/targets/table-collection-surface.md section 5.7.
        ///
        /// **This is the key, not the row's position.** `table[0]` is the row whose
        /// `TransitionId` is 0, not the first row - the rows in order are
        /// `Records`. Nothing in the type says which, which is why the table itself is not
        /// subscriptable by position anywhere.
        ///
        /// It does not replace `FindByTransitionId`: a key that may be absent
        /// wants the one whose name says a miss is an ordinary answer.
        /// </remarks>
        public TransitionRecord this[string key] => GetByTransitionIdOrThrow(key);

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await CloverData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `CloverData.EncryptionKey` and `CloverData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, CloverData.EncryptionKey, CloverData.MacKey, CloverData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<TransitionRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new TransitionRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "Transition.TransitionId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Transition.TransitionId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._transitionId = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "Transition.Kind", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Transition.Kind");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._kind = (global::Clover.Data.TransitionKind)value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "Transition.OutMs", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Transition.OutMs");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._outMs = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "Transition.HoldMs", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Transition.HoldMs");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._holdMs = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "Transition.InMs", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Transition.InMs");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._inMs = value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "Transition.Ink", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Transition.Ink");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._ink = value;
                            } while (--n > 0);
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "Transition.Toward", TcbTable.KindScalar, false, TcbTable.ElementBool);
                        cursor = new TcbColumnCursor(reader, column, count, "Transition.Toward");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._toward = cursor.NextBool();
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "Transition.Cue", TcbTable.KindScalar, true, TcbTable.ElementString);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Transition.Cue");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._cue = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._cueHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._cueHasValue)
                                records[i]._cue = "";
                        }
                        break;

                    case 9:
                        TcbTable.CheckColumn(column, "Transition.Note", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Transition.Note");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._note = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByTransitionId = new Dictionary<string, TransitionRecord>(count);
            foreach (var record in records)
                recordsByTransitionId.Add(record.TransitionId, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByTransitionId = recordsByTransitionId;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Clover.Data
