// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Clover.Data
{
    [System.Serializable]
    public partial class LeaderboardRecord
    {
        #region Values
        /// <summary>
        /// 식별자
        /// </summary>
        public string BoardId => _boardId;

        /// <summary>
        /// 기획자가 읽는 이름. **화면의 이름은 지표와 축에서 조립됩니다**
        /// </summary>
        public string Name => _name;

        /// <summary>
        /// 순위를 매기는 값
        /// </summary>
        public global::Clover.Data.LeaderboardMetric Metric => _metric;

        /// <summary>
        /// 조커 풀. 「전부」가 없습니다
        /// </summary>
        public global::Clover.Data.PoolChoice Pool => _pool;

        /// <summary>
        /// 무엇으로 나누는가
        /// </summary>
        public global::Clover.Data.SplitKind Split => _split;

        /// <summary>
        /// 나누는 값. `split` 이 `None` 이면 비어 있습니다
        /// </summary>
        public string SplitRef => _splitRef;
        /// <summary>Whether this row has a value for <see cref="SplitRef"/>.</summary>
        public bool HasSplitRef => _splitRefHasValue;

        /// <summary>
        /// 판의 어느 탭
        /// </summary>
        public global::Clover.Data.BoardGroup Group => _group;

        /// <summary>
        /// 탭 안의 순서
        /// </summary>
        public int SortOrder => _sortOrder;
        #endregion

        #region Storage
        internal string _boardId = "";
        internal string _name = "";
        internal global::Clover.Data.LeaderboardMetric _metric;
        internal global::Clover.Data.PoolChoice _pool;
        internal global::Clover.Data.SplitKind _split;
        internal string _splitRef = "";
        internal bool _splitRefHasValue;
        internal global::Clover.Data.BoardGroup _group;
        internal int _sortOrder;
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"BoardId\":"); ToStringHelper.ToString(BoardId, sb);
            sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
            sb.Append(",\"Metric\":"); ToStringHelper.ToString(Metric, sb);
            sb.Append(",\"Pool\":"); ToStringHelper.ToString(Pool, sb);
            sb.Append(",\"Split\":"); ToStringHelper.ToString(Split, sb);
            sb.Append(",\"SplitRef\":"); ToStringHelper.ToString(SplitRef, sb);
            sb.Append(",\"Group\":"); ToStringHelper.ToString(Group, sb);
            sb.Append(",\"SortOrder\":"); ToStringHelper.ToString(SortOrder, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 리더보드 한 판입니다. **지표 하나를 축 하나로 나눈 것**이고, 보드를 더하는 데 코드가 바뀌지 않습니다. 화면에 적히는 이름은 지표와 축의 이름에서 조립하므로, 여기 `name` 은 기획자가 시트에서 읽는 것입니다.
    /// </summary>
    [System.Serializable]
    public partial class LeaderboardTable : IEnumerable<LeaderboardRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "BoardId", "Name", "Metric", "Pool", "Split", "SplitRef", "Group", "SortOrder" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.BoardId, r.Name, r.Metric, r.Pool, r.Split, r.SplitRef, r.Group, r.SortOrder });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<LeaderboardRecord> Records => _records;
        private List<LeaderboardRecord> _records = new List<LeaderboardRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<LeaderboardRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<LeaderboardRecord> IEnumerable<LeaderboardRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'BoardId'
        public Dictionary<string, LeaderboardRecord> RecordsByBoardId => _recordsByBoardId;
        private Dictionary<string, LeaderboardRecord> _recordsByBoardId = new Dictionary<string, LeaderboardRecord>();

        /// <summary>
        /// The row with this `BoardId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public LeaderboardRecord FindByBoardId(string key)
            => _recordsByBoardId.TryGetValue(key, out LeaderboardRecord record) ? record : null;

        /// <summary>
        /// The row with this `BoardId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByBoardId(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public LeaderboardRecord GetByBoardIdOrThrow(string key)
        {
            if (!_recordsByBoardId.TryGetValue(key, out LeaderboardRecord record))
                throw new TabbitException($"There is no record in table `Leaderboard` that corresponds to field `BoardId` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `BoardId`.</summary>
        public bool ContainsBoardId(string key) => _recordsByBoardId.ContainsKey(key);
        #endregion // Indexing by `BoardId`

        /// <summary>Each row with the `BoardId` it is keyed by.</summary>
        /// <remarks>
        /// `foreach (var (key, row) in table.Entries)`. What this saves a caller is not the
        /// key value - the row carries it - but having to know which column the key is:
        /// `BoardId` here, something else in the next table.
        ///
        /// A struct that enumerates itself, so the loop allocates nothing, and the rows in
        /// the order the file wrote them rather than the order a dictionary happens to hold
        /// them - which makes this and a plain `foreach` over the table agree.
        ///
        /// Only the primary key gets this. A table keyed by several columns together has no
        /// single key value to pair a row with.
        /// </remarks>
        public struct EntryEnumerator
        {
            private readonly List<LeaderboardRecord> _rows;
            private int _at;

            internal EntryEnumerator(List<LeaderboardRecord> rows)
            {
                _rows = rows;
                _at = -1;
            }

            public EntryEnumerator GetEnumerator() => this;

            public bool MoveNext() => ++_at < _rows.Count;

            public (string Key, LeaderboardRecord Row) Current
                => (_rows[_at].BoardId, _rows[_at]);
        }

        /// <summary>Each row with the `BoardId` it is keyed by.</summary>
        public EntryEnumerator Entries => new EntryEnumerator(_records);

        /// <summary>
        /// The row with this `BoardId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// `GetByBoardIdOrThrow` under the spelling the language uses for a
        /// keyed collection. It throws because that is what `Dictionary` does here; a
        /// language whose own map answers with null generates a subscript that answers with
        /// null. spec/targets/table-collection-surface.md section 5.7.
        ///
        /// **This is the key, not the row's position.** `table[0]` is the row whose
        /// `BoardId` is 0, not the first row - the rows in order are
        /// `Records`. Nothing in the type says which, which is why the table itself is not
        /// subscriptable by position anywhere.
        ///
        /// It does not replace `FindByBoardId`: a key that may be absent
        /// wants the one whose name says a miss is an ordinary answer.
        /// </remarks>
        public LeaderboardRecord this[string key] => GetByBoardIdOrThrow(key);

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await CloverData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `CloverData.EncryptionKey` and `CloverData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, CloverData.EncryptionKey, CloverData.MacKey, CloverData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<LeaderboardRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new LeaderboardRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "Leaderboard.BoardId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Leaderboard.BoardId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._boardId = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "Leaderboard.Name", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "Leaderboard.Name");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._name = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "Leaderboard.Metric", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Leaderboard.Metric");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._metric = (global::Clover.Data.LeaderboardMetric)value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "Leaderboard.Pool", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Leaderboard.Pool");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._pool = (global::Clover.Data.PoolChoice)value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "Leaderboard.Split", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Leaderboard.Split");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._split = (global::Clover.Data.SplitKind)value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "Leaderboard.SplitRef", TcbTable.KindScalar, true, TcbTable.ElementString);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "Leaderboard.SplitRef");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._splitRef = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._splitRefHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._splitRefHasValue)
                                records[i]._splitRef = "";
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "Leaderboard.Group", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Leaderboard.Group");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._group = (global::Clover.Data.BoardGroup)value;
                            } while (--n > 0);
                        }
                        break;

                    case 8:
                        TcbTable.CheckColumn(column, "Leaderboard.SortOrder", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "Leaderboard.SortOrder");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._sortOrder = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByBoardId = new Dictionary<string, LeaderboardRecord>(count);
            foreach (var record in records)
                recordsByBoardId.Add(record.BoardId, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByBoardId = recordsByBoardId;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Clover.Data
