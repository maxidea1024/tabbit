// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Clover.Data
{
    [System.Serializable]
    public partial class CardSetRecord
    {
        #region Values
        /// <summary>
        /// 식별자
        /// </summary>
        public string SetId => _setId;

        /// <summary>
        /// 표시 이름
        /// </summary>
        public string Name => _name;

        /// <summary>
        /// 고르는 화면에서의 순서
        /// </summary>
        public int SortOrder => _sortOrder;

        /// <summary>
        /// 그림이 있는 폴더. 비면 그립니다
        /// </summary>
        public string ArtDir => _artDir;
        /// <summary>Whether this row has a value for <see cref="ArtDir"/>.</summary>
        public bool HasArtDir => _artDirHasValue;

        /// <summary>
        /// 그림이 어디서 왔는가
        /// </summary>
        public string Credit => _credit;
        /// <summary>Whether this row has a value for <see cref="Credit"/>.</summary>
        public bool HasCredit => _creditHasValue;

        /// <summary>
        /// 그림에 모서리의 랭크와 무늬가 들어 있는가
        /// </summary>
        public bool ArtHasIndex => _artHasIndex;

        /// <summary>
        /// 종이의 색. **강화의 색과 겹치지 않아야 합니다** — 바탕에 색이 들면 강화로 읽힙니다
        /// </summary>
        public string Paper => _paper;
        #endregion

        #region Storage
        internal string _setId = "";
        internal string _name = "";
        internal int _sortOrder;
        internal string _artDir = "";
        internal bool _artDirHasValue;
        internal string _credit = "";
        internal bool _creditHasValue;
        internal bool _artHasIndex;
        internal string _paper = "";
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"SetId\":"); ToStringHelper.ToString(SetId, sb);
            sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
            sb.Append(",\"SortOrder\":"); ToStringHelper.ToString(SortOrder, sb);
            sb.Append(",\"ArtDir\":"); ToStringHelper.ToString(ArtDir, sb);
            sb.Append(",\"Credit\":"); ToStringHelper.ToString(Credit, sb);
            sb.Append(",\"ArtHasIndex\":"); ToStringHelper.ToString(ArtHasIndex, sb);
            sb.Append(",\"Paper\":"); ToStringHelper.ToString(Paper, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 트럼프 한 벌의 겉모습입니다. **덱과 다른 축입니다** — 덱은 런의 시작 조건이고 이것은 그 52장이 어떻게 보이는가입니다. 그림이 있으면 그림이 얼굴이고, 없으면 무늬와 랭크를 그립니다.
    /// </summary>
    [System.Serializable]
    public partial class CardSetTable : IEnumerable<CardSetRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "SetId", "Name", "SortOrder", "ArtDir", "Credit", "ArtHasIndex", "Paper" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.SetId, r.Name, r.SortOrder, r.ArtDir, r.Credit, r.ArtHasIndex, r.Paper });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<CardSetRecord> Records => _records;
        private List<CardSetRecord> _records = new List<CardSetRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<CardSetRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<CardSetRecord> IEnumerable<CardSetRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'SetId'
        public Dictionary<string, CardSetRecord> RecordsBySetId => _recordsBySetId;
        private Dictionary<string, CardSetRecord> _recordsBySetId = new Dictionary<string, CardSetRecord>();

        /// <summary>
        /// The row with this `SetId`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public CardSetRecord FindBySetId(string key)
            => _recordsBySetId.TryGetValue(key, out CardSetRecord record) ? record : null;

        /// <summary>
        /// The row with this `SetId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetBySetId(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public CardSetRecord GetBySetIdOrThrow(string key)
        {
            if (!_recordsBySetId.TryGetValue(key, out CardSetRecord record))
                throw new TabbitException($"There is no record in table `CardSet` that corresponds to field `SetId` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `SetId`.</summary>
        public bool ContainsSetId(string key) => _recordsBySetId.ContainsKey(key);
        #endregion // Indexing by `SetId`

        /// <summary>Each row with the `SetId` it is keyed by.</summary>
        /// <remarks>
        /// `foreach (var (key, row) in table.Entries)`. What this saves a caller is not the
        /// key value - the row carries it - but having to know which column the key is:
        /// `SetId` here, something else in the next table.
        ///
        /// A struct that enumerates itself, so the loop allocates nothing, and the rows in
        /// the order the file wrote them rather than the order a dictionary happens to hold
        /// them - which makes this and a plain `foreach` over the table agree.
        ///
        /// Only the primary key gets this. A table keyed by several columns together has no
        /// single key value to pair a row with.
        /// </remarks>
        public struct EntryEnumerator
        {
            private readonly List<CardSetRecord> _rows;
            private int _at;

            internal EntryEnumerator(List<CardSetRecord> rows)
            {
                _rows = rows;
                _at = -1;
            }

            public EntryEnumerator GetEnumerator() => this;

            public bool MoveNext() => ++_at < _rows.Count;

            public (string Key, CardSetRecord Row) Current
                => (_rows[_at].SetId, _rows[_at]);
        }

        /// <summary>Each row with the `SetId` it is keyed by.</summary>
        public EntryEnumerator Entries => new EntryEnumerator(_records);

        /// <summary>
        /// The row with this `SetId`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// `GetBySetIdOrThrow` under the spelling the language uses for a
        /// keyed collection. It throws because that is what `Dictionary` does here; a
        /// language whose own map answers with null generates a subscript that answers with
        /// null. spec/targets/table-collection-surface.md section 5.7.
        ///
        /// **This is the key, not the row's position.** `table[0]` is the row whose
        /// `SetId` is 0, not the first row - the rows in order are
        /// `Records`. Nothing in the type says which, which is why the table itself is not
        /// subscriptable by position anywhere.
        ///
        /// It does not replace `FindBySetId`: a key that may be absent
        /// wants the one whose name says a miss is an ordinary answer.
        /// </remarks>
        public CardSetRecord this[string key] => GetBySetIdOrThrow(key);

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await CloverData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `CloverData.EncryptionKey` and `CloverData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, CloverData.EncryptionKey, CloverData.MacKey, CloverData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;
            byte[] presence;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<CardSetRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new CardSetRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "CardSet.SetId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSet.SetId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._setId = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "CardSet.Name", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSet.Name");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._name = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "CardSet.SortOrder", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSet.SortOrder");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._sortOrder = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "CardSet.ArtDir", TcbTable.KindScalar, true, TcbTable.ElementString);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSet.ArtDir");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._artDir = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._artDirHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._artDirHasValue)
                                records[i]._artDir = "";
                        }
                        break;

                    case 5:
                        TcbTable.CheckColumn(column, "CardSet.Credit", TcbTable.KindScalar, true, TcbTable.ElementString);
                        presence = TcbTable.ReadPresence(reader, column, count);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSet.Credit");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._credit = value;
                            } while (--n > 0);
                        }
                        for (int i = 0; i < count; i++)
                        {
                            records[i]._creditHasValue = TcbTable.IsPresent(presence, i);

                            // The block carries a value for every row, so an absent one has
                            // just been given whatever was there. Putting the empty value
                            // back is what makes this path agree with the JSON one.
                            if (!records[i]._creditHasValue)
                                records[i]._credit = "";
                        }
                        break;

                    case 6:
                        TcbTable.CheckColumn(column, "CardSet.ArtHasIndex", TcbTable.KindScalar, false, TcbTable.ElementBool);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSet.ArtHasIndex");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._artHasIndex = cursor.NextBool();
                        }
                        break;

                    case 7:
                        TcbTable.CheckColumn(column, "CardSet.Paper", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSet.Paper");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._paper = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsBySetId = new Dictionary<string, CardSetRecord>(count);
            foreach (var record in records)
                recordsBySetId.Add(record.SetId, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsBySetId = recordsBySetId;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Clover.Data
