// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Clover.Data
{
    [System.Serializable]
    public partial class ChallengeBanRecord
    {
        #region Values
        /// <summary>
        /// 어느 챌린지
        /// </summary>
        public string Owner => _owner_Challenge_index;
        public ChallengeRecord ChallengeByOwner => _owner;

        /// <summary>
        /// 같은 소유자 안의 순서
        /// </summary>
        public int Order => _order;

        /// <summary>
        /// 무엇의 갈래
        /// </summary>
        public global::Clover.Data.BanKind Kind => _kind;

        /// <summary>
        /// 금지되는 것의 식별자
        /// </summary>
        public string RefId => _refId;
        #endregion

        #region Reference wiring
        public void SetReference_Owner_INTERNAL(ChallengeRecord value) => _owner = value;
        #endregion

        #region Storage
        internal ChallengeRecord _owner;
        internal string _owner_Challenge_index;
        internal int _order;
        internal global::Clover.Data.BanKind _kind;
        internal string _refId = "";
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"Owner\":"); ToStringHelper.ToString(Owner, sb);
            sb.Append(",\"Order\":"); ToStringHelper.ToString(Order, sb);
            sb.Append(",\"Kind\":"); ToStringHelper.ToString(Kind, sb);
            sb.Append(",\"RefId\":"); ToStringHelper.ToString(RefId, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 챌린지가 금지하는 것입니다. 금지된 것은 어느 경로로도 나오지 않습니다.
    /// </summary>
    [System.Serializable]
    public partial class ChallengeBanTable : IEnumerable<ChallengeBanRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Owner", "Order", "Kind", "RefId" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Owner, r.Order, r.Kind, r.RefId });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<ChallengeBanRecord> Records => _records;
        private List<ChallengeBanRecord> _records = new List<ChallengeBanRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<ChallengeBanRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<ChallengeBanRecord> IEnumerable<ChallengeBanRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'Owner and Order'
        private Dictionary<string, ChallengeBanRecord> _recordsByOwnerAndOrder = new Dictionary<string, ChallengeBanRecord>();

        /// <summary>Joins the columns of the `Owner and Order` key into the text the map is keyed by.</summary>
        private static string KeyOfOwnerAndOrder(string ownerKey, int orderKey)
        {
            var parts = new string[]
            {
                ownerKey,
                orderKey.ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `Owner and Order`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public ChallengeBanRecord FindByOwnerAndOrder(string ownerKey, int orderKey)
            => _recordsByOwnerAndOrder.TryGetValue(KeyOfOwnerAndOrder(ownerKey, orderKey), out ChallengeBanRecord record) ? record : null;

        /// <summary>
        /// The row with this `Owner and Order`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public ChallengeBanRecord GetByOwnerAndOrderOrThrow(string ownerKey, int orderKey)
        {
            if (!_recordsByOwnerAndOrder.TryGetValue(KeyOfOwnerAndOrder(ownerKey, orderKey), out ChallengeBanRecord record))
                throw new TabbitException($"There is no record in table `ChallengeBan` that corresponds to field `Owner and Order` value ({ownerKey}, {orderKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Owner and Order`.</summary>
        public bool ContainsOwnerAndOrder(string ownerKey, int orderKey) => _recordsByOwnerAndOrder.ContainsKey(KeyOfOwnerAndOrder(ownerKey, orderKey));

        /// <summary>
        /// The row with this `Owner and Order`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// A key of several columns takes them in the order the sheet wrote them, which is
        /// the order `GetByOwnerAndOrderOrThrow` takes them in too.
        /// spec/targets/table-collection-surface.md section 5.4.
        /// </remarks>
        public ChallengeBanRecord this[string ownerKey, int orderKey]
            => GetByOwnerAndOrderOrThrow(ownerKey, orderKey);
        #endregion // Indexing by `Owner and Order`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await CloverData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `CloverData.EncryptionKey` and `CloverData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, CloverData.EncryptionKey, CloverData.MacKey, CloverData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<ChallengeBanRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new ChallengeBanRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "ChallengeBan.Owner", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "ChallengeBan.Owner");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._owner_Challenge_index = value;
                                record._owner = default(ChallengeRecord); // will be assigned.
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "ChallengeBan.Order", TcbTable.KindScalar, false, TcbTable.ElementI32, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "ChallengeBan.Order");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._order = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "ChallengeBan.Kind", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "ChallengeBan.Kind");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._kind = (global::Clover.Data.BanKind)value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        TcbTable.CheckColumn(column, "ChallengeBan.RefId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "ChallengeBan.RefId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._refId = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsByOwnerAndOrder = new Dictionary<string, ChallengeBanRecord>(count);
            foreach (var record in records)
                recordsByOwnerAndOrder.Add(KeyOfOwnerAndOrder(record.Owner, record.Order), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByOwnerAndOrder = recordsByOwnerAndOrder;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Clover.Data
