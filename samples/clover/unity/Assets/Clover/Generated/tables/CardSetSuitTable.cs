// ------------------------------------------------------------------------------
// <auto-generated>
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections.Generic;
using System.Threading.Tasks;

// Tabbit's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using Tabbit.Binary;

namespace Clover.Data
{
    [System.Serializable]
    public partial class CardSetSuitRecord
    {
        #region Values
        /// <summary>
        /// 이 색을 가진 세트
        /// </summary>
        public string SetId => _setId_CardSet_index;
        public CardSetRecord CardSetBySetId => _setId;

        /// <summary>
        /// 무늬
        /// </summary>
        public global::Clover.Data.SuitKind Suit => _suit;

        /// <summary>
        /// 카드에 쓰는 색
        /// </summary>
        public string Ink => _ink;
        #endregion

        #region Reference wiring
        public void SetReference_SetId_INTERNAL(CardSetRecord value) => _setId = value;
        #endregion

        #region Storage
        internal CardSetRecord _setId;
        internal string _setId_CardSet_index;
        internal global::Clover.Data.SuitKind _suit;
        internal string _ink = "";
        #endregion

        #region ToString
        public override string ToString()
        {
            var sb = new StringBuilder("{");
            sb.Append("\"SetId\":"); ToStringHelper.ToString(SetId, sb);
            sb.Append(",\"Suit\":"); ToStringHelper.ToString(Suit, sb);
            sb.Append(",\"Ink\":"); ToStringHelper.ToString(Ink, sb);
            sb.Append("}");
            return sb.ToString();
        }
        #endregion
    }

    /// <summary>
    /// 세트가 무늬마다 쓰는 색입니다. **그림이 없는 세트에서만 보입니다** — 그림이 얼굴인 세트는 색이 그림 안에 있습니다. 네 색을 다 적는 것이 요점입니다. 빠진 것을 코드의 기본값으로 채우면 그 기본값이 어느 세트의 색인지가 코드에만 있게 됩니다.
    /// </summary>
    [System.Serializable]
    public partial class CardSetSuitTable : IEnumerable<CardSetSuitRecord>
    {
        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "SetId", "Suit", "Ink" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.SetId, r.Suit, r.Ink });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<CardSetSuitRecord> Records => _records;
        private List<CardSetSuitRecord> _records = new List<CardSetSuitRecord>();

        /// <summary>How many rows the table holds.</summary>
        public int Count => _records.Count;

        /// <summary>The rows, in the order the file wrote them.</summary>
        /// <remarks>
        /// A struct enumerator rather than the interface one, because `foreach` binds to this
        /// by name and a boxed enumerator allocates once per loop - which in a project that
        /// walks a table every frame is an allocation every frame. LINQ and anything holding
        /// the table as `IEnumerable` reach the explicit implementations below instead, and
        /// those box exactly as they always would have.
        ///
        /// The list reference is read once, here. A refresh replaces the reference rather than
        /// its contents, so a loop already running keeps the rows it started with - the same
        /// property `Records` documents above, reached without naming the list.
        /// </remarks>
        public List<CardSetSuitRecord>.Enumerator GetEnumerator() => _records.GetEnumerator();

        IEnumerator<CardSetSuitRecord> IEnumerable<CardSetSuitRecord>.GetEnumerator() => _records.GetEnumerator();

        System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()
            => _records.GetEnumerator();

        #region Indexing by 'SetId and Suit'
        private Dictionary<string, CardSetSuitRecord> _recordsBySetIdAndSuit = new Dictionary<string, CardSetSuitRecord>();

        /// <summary>Joins the columns of the `SetId and Suit` key into the text the map is keyed by.</summary>
        private static string KeyOfSetIdAndSuit(string setIdKey, global::Clover.Data.SuitKind suitKey)
        {
            var parts = new string[]
            {
                setIdKey,
                ((long)suitKey).ToString(System.Globalization.CultureInfo.InvariantCulture),
            };

            var text = new StringBuilder();

            foreach (var part in parts)
                text.Append(part.Length).Append(':').Append(part);

            return text.ToString();
        }

        /// <summary>
        /// The row with this `SetId and Suit`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language Tabbit generates has
        /// this one under the same name.
        /// </remarks>
        public CardSetSuitRecord FindBySetIdAndSuit(string setIdKey, global::Clover.Data.SuitKind suitKey)
            => _recordsBySetIdAndSuit.TryGetValue(KeyOfSetIdAndSuit(setIdKey, suitKey), out CardSetSuitRecord record) ? record : null;

        /// <summary>
        /// The row with this `SetId and Suit`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        public CardSetSuitRecord GetBySetIdAndSuitOrThrow(string setIdKey, global::Clover.Data.SuitKind suitKey)
        {
            if (!_recordsBySetIdAndSuit.TryGetValue(KeyOfSetIdAndSuit(setIdKey, suitKey), out CardSetSuitRecord record))
                throw new TabbitException($"There is no record in table `CardSetSuit` that corresponds to field `SetId and Suit` value ({setIdKey}, {suitKey})");

            return record;
        }

        /// <summary>Whether the table holds a row with this `SetId and Suit`.</summary>
        public bool ContainsSetIdAndSuit(string setIdKey, global::Clover.Data.SuitKind suitKey) => _recordsBySetIdAndSuit.ContainsKey(KeyOfSetIdAndSuit(setIdKey, suitKey));

        /// <summary>
        /// The row with this `SetId and Suit`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// A key of several columns takes them in the order the sheet wrote them, which is
        /// the order `GetBySetIdAndSuitOrThrow` takes them in too.
        /// spec/targets/table-collection-surface.md section 5.4.
        /// </remarks>
        public CardSetSuitRecord this[string setIdKey, global::Clover.Data.SuitKind suitKey]
            => GetBySetIdAndSuitOrThrow(setIdKey, suitKey);
        #endregion // Indexing by `SetId and Suit`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await CloverData.ReadAllBytesAsync(filename);

            // Unconditionally, and with whatever `CloverData.EncryptionKey` and `CloverData.MacKey`
            // hold - which is null unless the project set them. A file that is neither
            // encrypted nor signed comes back from this untouched, so the load path is the
            // same either way and there is no condition here that could be the wrong way
            // round. spec/wire/tcb-mac-and-signature.md.
            var reader = new TcbReader(TcbTable.Open(
                bytes, CloverData.EncryptionKey, CloverData.MacKey, CloverData.VerifyMac));

            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(TcbReader reader)
        {
            var columns = TcbTable.ReadHeader(reader, out int count);
            TcbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<CardSetSuitRecord>(count);

            for (int i = 0; i < count; i++)
                records.Add(new CardSetSuitRecord());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        TcbTable.CheckColumn(column, "CardSetSuit.SetId", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSetSuit.SetId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._setId_CardSet_index = value;
                                record._setId = default(CardSetRecord); // will be assigned.
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        TcbTable.CheckColumn(column, "CardSetSuit.Suit", TcbTable.KindScalar, false, TcbTable.ElementVarint);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSetSuit.Suit");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._suit = (global::Clover.Data.SuitKind)value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        TcbTable.CheckColumn(column, "CardSetSuit.Ink", TcbTable.KindScalar, false, TcbTable.ElementString);
                        cursor = new TcbColumnCursor(reader, column, count, "CardSetSuit.Ink");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._ink = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                TcbTable.CheckBlockEnd(reader, column, blockEnd);
            }
            var recordsBySetIdAndSuit = new Dictionary<string, CardSetSuitRecord>(count);
            foreach (var record in records)
                recordsBySetIdAndSuit.Add(KeyOfSetIdAndSuit(record.SetId, record.Suit), record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsBySetIdAndSuit = recordsBySetIdAndSuit;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }
} // namespace Clover.Data
