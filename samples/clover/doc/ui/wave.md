# 칩과 배수의 파형

> [왼쪽 판의 값 칸으로](../ui.md#왼쪽-판의-값-칸) · [화면의 문법으로](../ui.md) · [clover 문서 목록으로](../readme.md)

---

칩과 배수의 상자 바탕에 파형이 흐릅니다. `shader/wave.ts` 의 `ScoreWave` 하나이고, 그 파일을
지우면 상자는 파형이 없던 때의 모습으로 돌아갑니다.

**음악 플레이어의 비주얼과 같은 그로우 라인입니다.** 선 하나가 아니라 가는 흰 심과 그 바깥의
넓은 색 번짐 두 겹입니다 — 굵기를 늘린 선을 여러 겹 쌓는 방법으로는 밝기가 단으로 끊깁니다.

```
수가 0:      아무것도 없습니다
수가 있음:   가운데 줄 하나가 얕게 흐릅니다
값이 더해짐: 그 줄에 노이즈가 얹히고 상자 전체가 밝아집니다 → 0.42초로 잦아듭니다
배당이 큼:   잦아들지 않습니다. 주파수·진폭·빠르기가 배당을 따라 함께 오릅니다
값이 줄어듦: 아무것도 하지 않습니다
```

---

## 세기를 정하는 것 둘

`surge` 하나에 두 가지가 **더해집니다.**

|성분|어디서|어떻게 변하는가|
|--|--|--|
|얹히는 것|값이 더해진 순간(`Slot.surge`)|더해질 때마다 얹히고 0.42초로 잦아듭니다. 곱으로 줄어들므로 연달아 더해지는 동안 끊기지 않습니다|
|바닥|지금의 배당(`payoutLevel`)|잦아들지 않습니다. 상한이 0.55입니다|

**값이 줄어드는 동안에는 이 칸의 어느 층도 놓이지 않습니다.** `quietOnDrop` 이 `rolling` 만
막고 있었고 번쩍임과 튐과 글자 물결은 `ripple()` 이 세우므로, 판이 끝나 두 칸이 0으로
되돌아갈 때마다 상자가 파랑과 붉음으로 한 번 빛났습니다 — 그 대목에 필요한 것은 조용히
없어지는 것뿐입니다. 지금은 `set target` 이 그 방향을 먼저 봅니다.

**바닥의 상한이 0.55인 것은 얹히는 것이 들어갈 자리를 남기는 것입니다.** 1로 두면 고배당에서
이미 최대이므로 새로 더해진 것이 화면에 드러나지 않습니다.

얹는 크기는 「한 번 더해졌다」의 몫 0.28에 상대적인 크기를 더한 것이고 합쳐서 0.70까지입니다.
절대값으로 세면 칩이 다섯 자리인 대목에서만 세기가 오르고, 상대값만 쓰면 큰 수에 조금 더해진
것이 아무것도 아니게 됩니다 — 어느 쪽도 「지금 하나 더해졌다」를 내지 못합니다.

### 수가 0 인 칸

**아무것도 없습니다.** 0 은 아직 아무것도 쌓이지 않은 자리이고, 파형은 쌓이는 수의 모습이므로
쌓인 것이 없으면 그림도 없어야 합니다 — 0 에 줄 하나가 흐르고 있으면 그 줄이 무엇을
나타내는지가 없어집니다.

**끄지 않고 사라집니다.** 알파를 한 프레임에 0 으로 두면 그것은 꺼진 것이고, 판이 끝날 때마다
두 상자에서 무언가 사라지는 것이 눈에 걸립니다.

|무엇|값|
|--|--|
|나타나는 데|90밀리초. **그 순간이 곧 첫 수가 더해지는 순간이므로** 밝아지는 겹과 함께 와야 합니다|
|사라지는 데|200밀리초. 판이 끝나 정리되는 대목이라 조금 여유가 있습니다|

칸마다 따로입니다(`uAlive` 가 vec2 입니다). 판이 끝나면 배수가 먼저 0 이 되고 칩이 굴러
내려가므로, 그 사이에는 왼쪽 상자에만 파형이 남습니다.

**둘 다 사라지면 층이 화면에서 빠집니다** — `view.visible` 이 거짓이 됩니다. 알파 0 으로
버릴 조각을 셈하는 값이 작기는 하나, 판이 도는 시간의 대부분이 그 자리입니다. 화질 옵션이
아니라 지금 수가 있는가로만 갈리므로 다음 프레임에 수가 생기면 그대로 돌아옵니다.

### 줄어드는 동안

판이 끝나 0으로 되돌아가는 것은 알릴 일이 아닙니다. 판정은 칸이 합니다 — `Slot` 의 `surged`
는 `value > shown` 일 때만 얹히고, 셰이더는 받은 값으로 그림만 냅니다.

**`Slot.lit` 이 아닙니다.** 그것은 값이 바뀌기만 하면 서므로 0으로 되돌아가는 대목에도 0이
아닙니다. 바탕의 번쩍임(`Game.paintScoreFlash`)은 그것을 그대로 씁니다 — 걸쇠가 둘인 이유가
이것이고, 하나로 묶으면 둘 중 하나를 고칠 수 없게 됩니다.

### 바닥의 자

```
level = clamp(log10(chips × mult / 100) / log10(2,000,000 / 100), 0, 1)
```

**[환희](../presentation.md)의 사다리를 쓰지 않습니다.** 처음에 그것을 그대로 가져왔고, 그
자는 첫 단이 `chips × mult` 로 400,000입니다 — **그 아래가 전부 0으로 눌려서 보통 판에서
파형이 요동치는 일이 아예 없었습니다.** 안티 5~6에 가야 무언가 달라졌습니다.

환희는 「대단한 판」을 가리는 것이고 **파형은 「지금 쌓이고 있다」를 나타내는 것**이므로 자가
달라야 합니다. 이 자는 한 런에서 실제로 지나가는 범위에 걸쳐 있습니다.

|한 판의 곱|세기|
|--|--|
|60 (30 × 2, 초반 원페어)|0.00|
|240 (60 × 4)|0.09|
|4,500 (300 × 15, 중반)|0.38|
|30,000 (1,000 × 30)|0.58|
|2,000,000 (20,000 × 100) 이상|1.00|

**득점하는 동안 이 값이 오릅니다.** 칩과 배수가 쌓여 가므로 한 판 안에서 0.1에서 0.7로
지나가고, 그것이 파형이 점점 거칠어지는 모습입니다.

**읽는 것은 화면에 있는 수입니다**(`Slot.amount`). 상태의 값을 쓰면 숫자가 아직 굴러가는 동안
파형만 먼저 최대가 됩니다.

---

## 흐르는 쪽과 배당이 정하는 것 셋

**칩은 왼쪽으로, 배수는 오른쪽으로 흐릅니다.**

**배당이 크면 주파수·진폭·빠르기가 함께 오릅니다.** 하나만 올리면 그것은 요동치는 것이
아닙니다 — 빠르기만 올리면 같은 파형이 빨리 지나가는 것이고, 진폭만 올리면 큰 물결이 느리게
가는 것이고, 주파수만 올리면 잔 물결이 됩니다.

|무엇|조용한 자리|배당의 마지막 단|어디|
|--|--|--|--|
|한 자리를 스쳐 가는 주파수|초당 1.19회|초당 4.54회 — **3.8배**|`uPhase` 의 오르는 빠르기입니다. 초당 7.5 에서 28.5 라디안|
|무늬가 흐르는 빠르기|초당 57픽셀. 상자를 2.0초에 건넙니다|초당 87픽셀. 1.3초|위의 둘을 나눈 값입니다|
|한 화면에 보이는 산의 수|2.4개|6.0개 — 2.5배|`freq = 2.4 + uLevel × 3.6`|
|진폭|3.2픽셀|17.2픽셀 — 5.4배|`3.2 + surge × 5 + uLevel × 10`. **배당의 몫이 세기의 몫과 따로인 이유는 잦아들지 않아야 하기 때문입니다**|
|노이즈가 흐르는 자리|초당 1.30|초당 2.15|`uDrift`. **파형과 같은 빠르기로 맞춘 값입니다**|

**산의 수를 올리면 위상도 함께 올려야 합니다.** 같은 위상 빠르기에서 산이 촘촘해지면 무늬가
지나가는 것이 그만큼 느려 보입니다 — 촘촘하게만 만들고 위상을 두면 얌전해집니다.

### 항 셋이 같은 쪽으로 같은 빠르기로

**여기에 두 가지가 잘못 있었습니다.** 파형이 어느 쪽으로도 가지 않고 제자리에서 들썩였고,
2026-09-08에 재어 두 컷 사이의 옮김이 **0픽셀**임을 확인하였습니다.

|무엇|무슨 일이 되었는가|
|--|--|
|둘째 항의 위상이 부호가 반대|그 항이 거꾸로 흘렀습니다. 무게가 0.647이므로 첫 항(1.0)을 거의 상쇄합니다|
|위상 계수가 그 항의 주기와 무관|항마다 다른 빠르기로 흘렀습니다. 셋이 같이 가지 않으면 모습만 바뀌고 옮겨지지 않습니다|
|노이즈를 읽는 자리가 빼는 쪽|무늬가 파형과 반대로 흘렀습니다. **더해지는 동안 가장 크게 보이는 항이므로 그 대목의 흐름이 통째로 거꾸로였습니다**|

**위상 속도는 위상 계수를 그 항의 주기로 나눈 값입니다.** 셋이 같은 쪽으로 같은 빠르기로
가려면 계수가 주기에 비례해야 하고, 그래서 `k2/k1` 과 `k3/k1` 을 곱합니다.

**0.94와 1.06은 일부러 어긋낸 것입니다.** 정확히 같은 빠르기로 두면 셋이 한 덩어리로 굳어
그려 놓은 그림 하나가 미끄러지는 것이 되고, 6%를 어긋내면 흐르는 동안 모습이 천천히 바뀝니다.
되풀이가 보이지 않는 것은 부호가 아니라 **주기 1 · 2.3 · 4.7이 서로 나누어떨어지지 않는
것**에서 옵니다.

**잔 떨림(19주기)만 절반 빠르기입니다.** 115픽셀에서 한 주기가 6픽셀이고, 같은 빠르기로
흐르면 한 프레임에 0.26주기를 지나 되감기는 것으로 보입니다 — 그 항의 역할은 거칠기이지
흐름이 아닙니다.

**잔 항만 덜 올립니다.** 세 항을 다 같은 배로 올리면 마지막 항이 4.7 × 6.0 = 28주기가 되고,
그것은 115픽셀에서 한 주기가 4픽셀입니다 — 판이 작게 들어가는 화면에서 화면 픽셀 두어 개의
무늬가 되어 프레임마다 반짝입니다. `fine = 1 + (freq − 1) × 0.45` 로 올리면 마지막 단에서
7.5픽셀입니다.

**진폭 17.2픽셀은 상자를 거의 채웁니다.** 상자의 절반이 29픽셀이고 숫자가 34픽셀이므로
위아래로 남는 것이 12픽셀씩입니다 — 마지막 단에서는 파형이 글자를 통째로 지나고, 그것이 그
배당에서 의도한 모습입니다. 아래 「숫자와 부딪히는 자리」.

상한은 프레임에 맞춰 정한 것입니다. 가장 잔 항(19주기)이 마지막 단에서 초당 78픽셀이므로
60프레임에서 한 프레임이 1.3픽셀이고, 이보다 올리면 잔 무늬가 프레임마다 튑니다.

### 시간이 아니라 위상

`uv.x * f - uTime * speed` 로 적으면 `speed` 가 바뀌는 순간 `uTime` 이 이미 큰 값이므로 위상이
통째로 뛰고, **배당이 오를 때마다 파형이 한 번 끊깁니다.** 누적하면 빠르기만 바뀝니다.

**흐름은 `tick` 에서 실제 초로 잇습니다.** `step` 은 초당 60번 고정이므로 프레임이 그보다 적게
나오는 화면에서는 그 안에서 여러 번 돌고, 위상을 거기서 올리면 화면에 보이는 흐름이 프레임
수와 어긋납니다. 세기는 `step` 에서 읽습니다 — 얹히는 것이 단계당 비율로 줄어드는 값입니다.

---

## 상자 둘을 사각형 하나로

두 상자는 `splitX(block, [1, 사이, 1])` 로 나온 대칭이므로, 사각형 하나(264 × 58)를 덮고
**가운데에서 접습니다.**

```glsl
bool  right = vPx.x >= uHalf;
vec2  local = vec2(right ? uBlock.x - vPx.x : vPx.x, vPx.y);
float surge = right ? uSurge.y : uSurge.x;
vec3  ink   = right ? uMult : uChips;
```

접으면 오른쪽 상자의 가로 좌표가 오른쪽에서 왼쪽으로 자라므로, **방향이 하나인 식이 칩에서는
왼쪽으로 배수에서는 오른쪽으로 흐릅니다.** 방향 유니폼이 없습니다.

|얻는 것|대가|
|--|--|
|그리기 1회. 상자마다 하나면 2회이고 그 사이에 배치가 또 끊깁니다|**두 상자가 대칭이라는 것에 의존합니다.** `splitX` 의 그 한 줄이 바뀌면 이 셰이더가 어긋납니다|
|방향 유니폼이 없어집니다|배수 쪽 무늬가 칩 쪽의 좌우 뒤집힌 것입니다. 두 칸의 세기가 다르므로 같은 모습이 되는 것은 둘이 같은 세기일 때뿐입니다|
|접은 자리가 곱셈표가 서는 34픽셀의 사이입니다 — **접힘의 이음매는 잘려 나갑니다**|그 사이의 조각을 셈하고 버립니다. 덮는 사각형이 1.30% 대신 1.50% 입니다|

---

## 층 일곱

|층|무엇|
|--|--|
|파형|사인 3개의 합. 주기 1 · 2.3 · 4.7 이 서로 나누어떨어지지 않으므로 되풀이가 보이지 않습니다. **위상 계수는 그 항의 주기에 비례합니다** — 셋이 같은 쪽으로 같은 빠르기로 흐르는 조건입니다. **진폭이 평상시 3.2픽셀입니다** — 58픽셀 상자의 절반이 29픽셀이므로 이것은 가운데 줄이 얕게 굽이치는 정도입니다|
|노이즈|`soft` 그림 **읽기 1회**. 값이 오를 때만 얹힙니다. `n` 과 `jag` 의 곱이 요점입니다 — 잔 떨림의 진폭을 노이즈가 정하므로 **거친 자리가 가로로 뭉쳐서 옵니다.** `jag` 만 얹으면 상자 전체에 같은 굵기의 빗살이 서고, 그것은 파형이 아니라 무늬로 보입니다|
|기울기 보정|`dFdx` 하나. 세로 거리만 쓰면 **파형이 급한 자리에서 줄이 굵어 보입니다.** 노이즈 항의 해석적 미분은 구할 수 없고, 도함수 명령 하나가 두 항을 함께 처리합니다|
|그로우|심은 `exp(-d²·0.62)`, 번짐은 `1/(1+d²·0.045)`. 절반 밝기가 1.1픽셀과 4.7픽셀이고, 번짐은 15픽셀에서 이미 0.09이므로 상자의 위아래 변에 닿기 전에 잦아듭니다|
|더해진 순간의 겹|`1/(1+d²·0.004)` 에 평평한 몫 0.16. **상자 전체가 밝아집니다** — 절반 밝기가 15.8픽셀이므로 위아래 변까지 닿고, 평평한 몫이 파형에서 먼 네 귀퉁이를 채웁니다. **바닥이 아니라 얹히는 것만 봅니다**(`uPulse`) — 바닥까지 보면 고배당에서는 늘 밝아 있어 더해진 순간이 드러나지 않습니다|
|사라짐|`uAlive` 를 알파에 곱합니다. 칸마다 따로이고 90/200밀리초입니다|
|자르기|둥근 사각형 SDF 하나. **마스크 그림도 스텐실도 없습니다.** `fwidth` 로 나누므로 흐림이 화면 픽셀 1개이고, 판 좌표로 적으면 화면 배율이 2.7배인 자리에서 2.7픽셀이 됩니다|

섞기는 `add` 이고 알파를 미리 곱해 냅니다. **아래에 상자의 채움과 번쩍임이 있고 위에 숫자가
있습니다** — 위로 올리면 숫자를 덮고, 아래로 내리면 번쩍임이 덮습니다.

### 숫자와 부딪히는 자리

상자는 58픽셀이고 숫자는 34픽셀이므로 위아래로 남는 것이 12픽셀씩입니다. 파형의 가운데 줄은
숫자의 중간 높이와 같은 자리이고, 고배당에서 진폭이 17.2픽셀까지 오르면 글자를 통째로
지납니다.

숫자에는 검은 테가 있습니다 — 34 × 0.075 = **2.55픽셀**이고 색은 `0x0a0f18` 입니다. 흰 숫자 ·
검은 테 · 그 아래 빛의 순서이므로 읽히지만, **2.55픽셀은 두꺼운 테가 아니므로 번짐의 상한을
지켜야 합니다.** 번짐의 최고값이 0.85이고 심이 0.52이며, 그 이상으로 올리면 숫자의 테가 빛에
묻힙니다. 더해진 순간의 겹도 같은 이유로 0.50까지입니다 — 그것은 상자 전체에 고르게 얹히므로
숫자 뒤의 바탕을 통째로 올립니다.

---

## 값

|무엇|값|
|--|--|
|상자 하나|(264 − 34) / 2 × 58 = **115 × 58**|
|덮는 사각형|264 × 58 = 15,312 판픽셀 = 판 1,024,000의 **1.50%**|
|픽셀마다|그림 읽기 **1회**, 초월함수 5개(sin 4 · exp 1), 도함수 2개|
|그리기|**+2회.** 메시 하나와, 이 z 순서에서 피할 수 없는 배치 끊김 하나입니다. **두 칸이 다 0 인 동안에는 0회입니다**|
|FBO|**0.** 필터로 만들면 프레임마다 렌더 타깃 하나이고, 타일 기반 GPU 에서 타깃 전환은 픽셀 수에 비례하지 않는 값입니다|

**기준은 이미 돌고 있는 배경입니다.** `shader/background.ts` 의 `fbm` 은 그림 읽기 2회이고
도메인 워핑이 그것을 5번 부르므로 픽셀마다 10회, 그것을 화면 전체에 돌립니다. 그림 읽기로
견주면 `1.50% × 1 / 100% × 10` = **0.15%** 이고, ALU 까지 넣어도 배경 하나의 **1% 아래**입니다.

### 화질과의 관계

**옵션의 그래픽 품질이 이 층을 끄지 않습니다.** 칩과 배수는 판이 도는 내내 눈이 머무는
자리이고, 파형은 그 상자가 살아 있다는 표시입니다 — 화질을 낮춘 화면에서 그 자리만 죽은
상자가 되면 그것은 연출이 덜 보이는 것이 아니라 다른 게임입니다.

**값이 그것을 허락합니다.** 배경 셰이더 하나의 1% 아래이므로 화질로 아낄 것이 있는 자리가
아닙니다. 화질이 갈리는 것은 [재가 되는 전환](ash.md) 하나입니다.

기계로 갈리는 것은 하나뿐입니다 — **`coarsePointer()` 이면 `uJag` 가 0입니다.** 19주기가
115픽셀이므로 한 주기가 6픽셀이고, 판이 0.5배로 들어가는 화면에서는 화면 픽셀 3개입니다 —
그 크기의 무늬는 프레임마다 반짝입니다. 파형과 노이즈는 그대로 있습니다.

**핸드폰에서 그 밖에 줄일 것이 없습니다.** 1.50%는 그 기계에서도 1.50%이고, 프레임을 놓치는
자리는 [재가 되는 것](ash.md#모바일)입니다.

---

## 확인

|도구|무엇|
|--|--|
|`tools/shoot-wave.ts`|11컷을 오려 3배로 굽습니다 — 수가 0 인 자리 · 카드를 고른 뒤(값이 있고 조용한 자리) · 더해지는 동안 다섯 · 배당이 큰 두 자리 · 사라지는 도중과 사라진 뒤. 자리는 화면이 알린 것을 씁니다(`__clover.scoreWave().box`)|
|`__clover.scoreWave()`|지금의 세기 · 배당 · 위상 · 칸마다의 나타남. **세기와 빠르기와 사라짐은 값으로 봅니다**|
|`__clover.forceScore(chips, mult)`|칸에 수를 넣습니다. 배당의 마지막 단은 안티 5~6에서 나오고, 거기까지 판을 굴리는 것은 그 연출을 보려는 도구가 할 일이 아닙니다|
|`npm run check:wave-flow`|**흐르는 쪽을 잽니다.** 배당 셋에서 컷 둘을 견주어 칩이 음수(왼쪽)이고 배수가 양수(오른쪽)인지, 그 크기가 겨눈 값의 절반을 넘는지 봅니다|
|`__clover.holdWave(phase)`|위상을 그 값에 세우고 붙잡습니다|

**빠르기는 판의 시계로 나눕니다.** 손 시계로 나누면 값이 프레임 수에 흔들립니다 — 그 도구는
GPU 없이 돌아서 프레임이 60에 못 미치고, Pixi 의 티커가 `minFPS` 로 한 프레임의 길이를
100밀리초에서 끊으므로 그만큼의 시간이 누적되지 않습니다. **게다가 그 손실이 배당마다
다릅니다** — 요동치는 화면이 더 비싸므로 프레임이 더 적고, 그러면 빠른 자리가 느리게
측정됩니다(1.33 · 4.43 · 6.65 로 비가 5.00 대신 3.4 로 나온 자리입니다).

위상을 올리는 쪽도 같은 시계를 쓰므로 그 시계로 나눈 값은 겨눈 값과 같습니다. 2026-09-08에
실측한 값이 **7.50 · 19.59 · 28.50** 입니다.

### 흐르는 쪽의 측정

**시간으로는 잴 수 없습니다.** 그림 한 장을 굽는 데 1초쯤 들어서 두 컷 사이에 위상이 800라디안
넘게 가고, 그것은 파장의 여러 배이므로 상관의 어느 골을 골랐는지 알 수 없습니다 — 처음에
±24와 ±40으로 찾다가 0과 −39를 얻었고 둘 다 딴 골이었습니다.

그래서 `holdWave` 로 위상을 붙잡고 `TAU / 4` 만큼 손으로 옮깁니다. **어느 배당에서나 정확히
산의 4분의 1**이고, 찾는 범위를 파장의 0.45로 묶으면 옆의 골을 고르지 않습니다.

**흰 숫자는 색으로 걷어 냅니다.** 34픽셀 숫자가 파형보다 밝으므로 밝기로 산줄기를 뽑으면
숫자가 앉은 칸이 전부 숫자를 가리킵니다 — 숫자는 세 채널이 같고 파형은 파랑 또는 붉음이므로
채널을 빼면 숫자가 상쇄됩니다.

**그림으로 세기를 판정하지 않습니다.** 흐르는 것을 정지한 컷으로 판정하려면 컷마다 같은 무늬를
찾아 그 이동을 세야 하고, 파형에는 같은 무늬가 없습니다.
