// 씬.
//
// **이 게임에는 씬이 셋이고 순서가 있습니다** — 데이터를 읽고(`loading`), 시작을
// 기다리고(`title`), 판이 돕니다(`run`).
//
// 셋이 서로 다른 자리에 있습니다. `loading` 은 화면을 그리는 것이 서기 전이므로 진입점이
// DOM 한 줄로 가지고 있고, 나머지 둘은 `Game` 안에 있습니다 — 그래서 판을 접고 타이틀로
// 가는 데 페이지를 다시 읽을 일이 없습니다. **다시 읽으면 로딩이 한 번 더 보이는데, 판을
// 접는 것과 데이터를 읽는 것은 아무 관계가 없습니다.**

export type Scene = 'loading' | 'title' | 'run'
