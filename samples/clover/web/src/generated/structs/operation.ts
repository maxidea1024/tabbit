// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------


// The types this module's members name.
import { UnitKind } from '../enums/unit-kind'
import { PerUnitMode } from '../enums/per-unit-mode'
import { Rarity } from '../enums/rarity'
import { EnhancementKind } from '../enums/enhancement-kind'
import { CounterField } from '../enums/counter-field'
import { ResetKind } from '../enums/reset-kind'
import { HandPick } from '../enums/hand-pick'
import { CreateKind } from '../enums/create-kind'
import { EditionKind } from '../enums/edition-kind'
import { CardClass } from '../enums/card-class'
import { SealKind } from '../enums/seal-kind'
import { SuitKind } from '../enums/suit-kind'
import { ModifyKind } from '../enums/modify-kind'
import { JokerPick } from '../enums/joker-pick'
import { DebuffKind } from '../enums/debuff-kind'
import { RuleKind } from '../enums/rule-kind'
import { Duration } from '../enums/duration'
import { CardTrait } from '../enums/card-trait'

/**
 * One value of Operation, whichever shape it took.
 *
 * Narrow it on `kind`, which every variant declares as its own name.
 */
export type Operation =
  | OpAddChips
  | OpAddMult
  | OpMulMult
  | OpAddMoney
  | OpSetMoney
  | OpPerUnit
  | OpRandomRange
  | OpRetrigger
  | OpGrowSelf
  | OpResetSelf
  | OpGrowOthers
  | OpLevelUpHand
  | OpCreateCard
  | OpAddCard
  | OpDestroyCard
  | OpModifyCard
  | OpModifyJoker
  | OpDestroyJoker
  | OpCopyJoker
  | OpDebuff
  | OpDisableBoss
  | OpPreventLoss
  | OpChangeRule
  | OpChangeRuleByCounter
  | OpCardTrait
  | OpMulMoney
  | OpShopGift
  | OpDuplicateNextTag
  | OpRerollBoss
  | OpForceDiscard
  | OpDrawFaceDown
  | OpFlipJokers
  | OpDisableRandomJoker
  | OpGrant
  | OpNothing
  | OpCustom

/** What every Operation carries, whichever shape it is. */
export interface OperationBase {
}

/**
 * One shape of Operation.
 *
 * The file carries 1 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpAddChips extends OperationBase {
  readonly kind: 'OpAddChips'
  chips: number
}

/**
 * One shape of Operation.
 *
 * The file carries 2 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpAddMult extends OperationBase {
  readonly kind: 'OpAddMult'
  mult: number
}

/**
 * One shape of Operation.
 *
 * The file carries 3 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpMulMult extends OperationBase {
  readonly kind: 'OpMulMult'
  mult: number
}

/**
 * One shape of Operation.
 *
 * The file carries 4 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpAddMoney extends OperationBase {
  readonly kind: 'OpAddMoney'
  money: number
  /** 상한. 없으면 비웁니다. */
  cap: number
}

/**
 * One shape of Operation.
 *
 * The file carries 5 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpSetMoney extends OperationBase {
  readonly kind: 'OpSetMoney'
  money: number
}

/**
 * One shape of Operation.
 *
 * The file carries 6 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpPerUnit extends OperationBase {
  readonly kind: 'OpPerUnit'
  /** 상한. 없으면 비웁니다. */
  cap: number
  unit: UnitKind
  mode: PerUnitMode
  value: number
  baseValue: number
  /** `JokerRarityCount` 가 어느 희귀도를 세는가. */
  rarity: Rarity
  /** `DeckEnhancementCount` 가 어느 강화를 세는가. */
  enhancement: EnhancementKind
}

/**
 * One shape of Operation.
 *
 * The file carries 7 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpRandomRange extends OperationBase {
  readonly kind: 'OpRandomRange'
  mode: PerUnitMode
  min: number
  max: number
}

/**
 * One shape of Operation.
 *
 * The file carries 8 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpRetrigger extends OperationBase {
  readonly kind: 'OpRetrigger'
  times: number
}

/**
 * One shape of Operation.
 *
 * The file carries 9 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpGrowSelf extends OperationBase {
  readonly kind: 'OpGrowSelf'
  /** 상한. 없으면 비웁니다. */
  cap: number
  counter: CounterField
  step: number
  /** 시작값. 비우면 `MultMul` 은 10000 이고 나머지는 0 입니다. */
  init: number
  floor: number
  reset: ResetKind
}

/**
 * One shape of Operation.
 *
 * The file carries 10 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpResetSelf extends OperationBase {
  readonly kind: 'OpResetSelf'
  counter: CounterField
}

/**
 * One shape of Operation.
 *
 * The file carries 11 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpGrowOthers extends OperationBase {
  readonly kind: 'OpGrowOthers'
  counter: CounterField
  step: number
}

/**
 * One shape of Operation.
 *
 * The file carries 12 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpLevelUpHand extends OperationBase {
  readonly kind: 'OpLevelUpHand'
  handPick: HandPick
  levels: number
}

/**
 * One shape of Operation.
 *
 * The file carries 13 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpCreateCard extends OperationBase {
  readonly kind: 'OpCreateCard'
  handPick: HandPick
  create: CreateKind
  count: number
  edition: EditionKind
  /** `JokerRarityCount` 가 어느 희귀도를 세는가. */
  rarity: Rarity
  /** 정하지 않은 성질을 무작위로 채웁니다. */
  random: boolean
  /** 특정 행을 가리킬 때의 식별자. 팩과 바우처와 태그가 씁니다. */
  refId: string
}

/**
 * One shape of Operation.
 *
 * The file carries 14 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpAddCard extends OperationBase {
  readonly kind: 'OpAddCard'
  create: CreateKind
  count: number
  cardClass: CardClass
  /** `DeckEnhancementCount` 가 어느 강화를 세는가. */
  enhancement: EnhancementKind
  seal: SealKind
  /** 정하지 않은 성질을 무작위로 채웁니다. */
  random: boolean
}

/**
 * One shape of Operation.
 *
 * The file carries 15 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpDestroyCard extends OperationBase {
  readonly kind: 'OpDestroyCard'
  count: number
}

/**
 * One shape of Operation.
 *
 * The file carries 16 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpModifyCard extends OperationBase {
  readonly kind: 'OpModifyCard'
  value: number
  edition: EditionKind
  /** `DeckEnhancementCount` 가 어느 강화를 세는가. */
  enhancement: EnhancementKind
  seal: SealKind
  suit: SuitKind
  modify: ModifyKind
  /** 정하지 않은 성질을 무작위로 채웁니다. */
  random: boolean
}

/**
 * One shape of Operation.
 *
 * The file carries 17 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpModifyJoker extends OperationBase {
  readonly kind: 'OpModifyJoker'
  edition: EditionKind
  /** 정하지 않은 성질을 무작위로 채웁니다. */
  random: boolean
}

/**
 * One shape of Operation.
 *
 * The file carries 18 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpDestroyJoker extends OperationBase {
  readonly kind: 'OpDestroyJoker'
  pick: JokerPick
}

/**
 * One shape of Operation.
 *
 * The file carries 19 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpCopyJoker extends OperationBase {
  readonly kind: 'OpCopyJoker'
  pick: JokerPick
}

/**
 * One shape of Operation.
 *
 * The file carries 20 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpDebuff extends OperationBase {
  readonly kind: 'OpDebuff'
  suit: SuitKind
  debuff: DebuffKind
}

/**
 * One shape of Operation.
 *
 * The file carries 21 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpDisableBoss extends OperationBase {
  readonly kind: 'OpDisableBoss'
}

/**
 * One shape of Operation.
 *
 * The file carries 22 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpPreventLoss extends OperationBase {
  readonly kind: 'OpPreventLoss'
}

/**
 * One shape of Operation.
 *
 * The file carries 23 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpChangeRule extends OperationBase {
  readonly kind: 'OpChangeRule'
  value: number
  rule: RuleKind
  absolute: boolean
  duration: Duration
}

/**
 * One shape of Operation.
 *
 * The file carries 24 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpChangeRuleByCounter extends OperationBase {
  readonly kind: 'OpChangeRuleByCounter'
  counter: CounterField
  rule: RuleKind
}

/**
 * One shape of Operation.
 *
 * The file carries 25 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpCardTrait extends OperationBase {
  readonly kind: 'OpCardTrait'
  trait: CardTrait
}

/**
 * One shape of Operation.
 *
 * The file carries 26 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpMulMoney extends OperationBase {
  readonly kind: 'OpMulMoney'
  /** 상한. 없으면 비웁니다. */
  cap: number
  value: number
}

/**
 * One shape of Operation.
 *
 * The file carries 27 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpShopGift extends OperationBase {
  readonly kind: 'OpShopGift'
  create: CreateKind
  count: number
  edition: EditionKind
  /** `JokerRarityCount` 가 어느 희귀도를 세는가. */
  rarity: Rarity
  free: boolean
}

/**
 * One shape of Operation.
 *
 * The file carries 28 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpDuplicateNextTag extends OperationBase {
  readonly kind: 'OpDuplicateNextTag'
}

/**
 * One shape of Operation.
 *
 * The file carries 29 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpRerollBoss extends OperationBase {
  readonly kind: 'OpRerollBoss'
}

/**
 * One shape of Operation.
 *
 * The file carries 30 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpForceDiscard extends OperationBase {
  readonly kind: 'OpForceDiscard'
  count: number
}

/**
 * One shape of Operation.
 *
 * The file carries 31 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpDrawFaceDown extends OperationBase {
  readonly kind: 'OpDrawFaceDown'
  cardClass: CardClass
}

/**
 * One shape of Operation.
 *
 * The file carries 32 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpFlipJokers extends OperationBase {
  readonly kind: 'OpFlipJokers'
}

/**
 * One shape of Operation.
 *
 * The file carries 33 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpDisableRandomJoker extends OperationBase {
  readonly kind: 'OpDisableRandomJoker'
}

/**
 * One shape of Operation.
 *
 * The file carries 34 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpGrant extends OperationBase {
  readonly kind: 'OpGrant'
  create: CreateKind
  count: number
  /** 특정 행을 가리킬 때의 식별자. 팩과 바우처와 태그가 씁니다. */
  refId: string
}

/**
 * One shape of Operation.
 *
 * The file carries 35 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpNothing extends OperationBase {
  readonly kind: 'OpNothing'
}

/**
 * One shape of Operation.
 *
 * The file carries 36 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface OpCustom extends OperationBase {
  readonly kind: 'OpCustom'
  handler: string
}
