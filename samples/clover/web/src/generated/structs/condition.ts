// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY Tabbit.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------


// The types this module's members name.
import { PokerHandKind } from '../enums/poker-hand-kind'
import { SuitKind } from '../enums/suit-kind'
import { EnhancementKind } from '../enums/enhancement-kind'
import { SealKind } from '../enums/seal-kind'
import { EditionKind } from '../enums/edition-kind'
import { Compare } from '../enums/compare'
import { BlindKind } from '../enums/blind-kind'
import { CounterField } from '../enums/counter-field'
import { TargetKind } from '../enums/target-kind'
import { ConsumableKind } from '../enums/consumable-kind'

/**
 * One value of Condition, whichever shape it took.
 *
 * Narrow it on `kind`, which every variant declares as its own name.
 */
export type Condition =
  | CondAlways
  | CondHandContains
  | CondHandIs
  | CondCardSuit
  | CondCardRankSet
  | CondCardIsFace
  | CondCardEnhancement
  | CondCardSeal
  | CondCardEdition
  | CondCardCount
  | CondAllSuitsPresent
  | CondSuitPair
  | CondAllHeldSuit
  | CondBlindKind
  | CondMoney
  | CondDiscardsLeft
  | CondHandsLeft
  | CondDiscardsUnused
  | CondHandRepeated
  | CondFirstHand
  | CondLastHand
  | CondIsMostPlayedHand
  | CondFirstDiscard
  | CondEveryNHands
  | CondCounterAtLeast
  | CondChargeLeft
  | CondTargetMatch
  | CondDeckEnhancedAtLeast
  | CondBossTriggered
  | CondScoreRatioAtLeast
  | CondNoFaceScored
  | CondFaceScored
  | CondCounterAtMost
  | CondDiscardedFaceAtLeast
  | CondHandContainsRankAndHand
  | CondFirstHandSingleCard
  | CondFirstHandSingleRank
  | CondFirstDiscardSingleCard
  | CondNotMostPlayedHand
  | CondCardEnhanced
  | CondConsumableKind

/** What every Condition carries, whichever shape it is. */
export interface ConditionBase {
}

/**
 * One shape of Condition.
 *
 * The file carries 1 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondAlways extends ConditionBase {
  readonly kind: 'CondAlways'
}

/**
 * One shape of Condition.
 *
 * The file carries 2 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondHandContains extends ConditionBase {
  readonly kind: 'CondHandContains'
  hand: PokerHandKind
}

/**
 * One shape of Condition.
 *
 * The file carries 3 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondHandIs extends ConditionBase {
  readonly kind: 'CondHandIs'
  hand: PokerHandKind
}

/**
 * One shape of Condition.
 *
 * The file carries 4 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCardSuit extends ConditionBase {
  readonly kind: 'CondCardSuit'
  suit: SuitKind
}

/**
 * One shape of Condition.
 *
 * The file carries 5 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCardRankSet extends ConditionBase {
  readonly kind: 'CondCardRankSet'
}

/**
 * One shape of Condition.
 *
 * The file carries 6 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCardIsFace extends ConditionBase {
  readonly kind: 'CondCardIsFace'
}

/**
 * One shape of Condition.
 *
 * The file carries 7 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCardEnhancement extends ConditionBase {
  readonly kind: 'CondCardEnhancement'
  enhancement: EnhancementKind
}

/**
 * One shape of Condition.
 *
 * The file carries 8 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCardSeal extends ConditionBase {
  readonly kind: 'CondCardSeal'
  seal: SealKind
}

/**
 * One shape of Condition.
 *
 * The file carries 9 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCardEdition extends ConditionBase {
  readonly kind: 'CondCardEdition'
  edition: EditionKind
}

/**
 * One shape of Condition.
 *
 * The file carries 10 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCardCount extends ConditionBase {
  readonly kind: 'CondCardCount'
  n: number
  compare: Compare
}

/**
 * One shape of Condition.
 *
 * The file carries 11 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondAllSuitsPresent extends ConditionBase {
  readonly kind: 'CondAllSuitsPresent'
}

/**
 * One shape of Condition.
 *
 * The file carries 12 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondSuitPair extends ConditionBase {
  readonly kind: 'CondSuitPair'
  suit: SuitKind
}

/**
 * One shape of Condition.
 *
 * The file carries 13 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondAllHeldSuit extends ConditionBase {
  readonly kind: 'CondAllHeldSuit'
}

/**
 * One shape of Condition.
 *
 * The file carries 14 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondBlindKind extends ConditionBase {
  readonly kind: 'CondBlindKind'
  blind: BlindKind
}

/**
 * One shape of Condition.
 *
 * The file carries 15 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondMoney extends ConditionBase {
  readonly kind: 'CondMoney'
  n: number
  compare: Compare
}

/**
 * One shape of Condition.
 *
 * The file carries 16 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondDiscardsLeft extends ConditionBase {
  readonly kind: 'CondDiscardsLeft'
  n: number
  compare: Compare
}

/**
 * One shape of Condition.
 *
 * The file carries 17 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondHandsLeft extends ConditionBase {
  readonly kind: 'CondHandsLeft'
  n: number
  compare: Compare
}

/**
 * One shape of Condition.
 *
 * The file carries 18 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondDiscardsUnused extends ConditionBase {
  readonly kind: 'CondDiscardsUnused'
}

/**
 * One shape of Condition.
 *
 * The file carries 19 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondHandRepeated extends ConditionBase {
  readonly kind: 'CondHandRepeated'
}

/**
 * One shape of Condition.
 *
 * The file carries 20 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondFirstHand extends ConditionBase {
  readonly kind: 'CondFirstHand'
}

/**
 * One shape of Condition.
 *
 * The file carries 21 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondLastHand extends ConditionBase {
  readonly kind: 'CondLastHand'
}

/**
 * One shape of Condition.
 *
 * The file carries 22 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondIsMostPlayedHand extends ConditionBase {
  readonly kind: 'CondIsMostPlayedHand'
}

/**
 * One shape of Condition.
 *
 * The file carries 23 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondFirstDiscard extends ConditionBase {
  readonly kind: 'CondFirstDiscard'
}

/**
 * One shape of Condition.
 *
 * The file carries 24 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondEveryNHands extends ConditionBase {
  readonly kind: 'CondEveryNHands'
  n: number
}

/**
 * One shape of Condition.
 *
 * The file carries 25 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCounterAtLeast extends ConditionBase {
  readonly kind: 'CondCounterAtLeast'
  counter: CounterField
  consume: boolean
  n: number
}

/**
 * One shape of Condition.
 *
 * The file carries 26 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondChargeLeft extends ConditionBase {
  readonly kind: 'CondChargeLeft'
}

/**
 * One shape of Condition.
 *
 * The file carries 27 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondTargetMatch extends ConditionBase {
  readonly kind: 'CondTargetMatch'
  target: TargetKind
}

/**
 * One shape of Condition.
 *
 * The file carries 28 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondDeckEnhancedAtLeast extends ConditionBase {
  readonly kind: 'CondDeckEnhancedAtLeast'
  n: number
}

/**
 * One shape of Condition.
 *
 * The file carries 29 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondBossTriggered extends ConditionBase {
  readonly kind: 'CondBossTriggered'
}

/**
 * One shape of Condition.
 *
 * The file carries 30 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondScoreRatioAtLeast extends ConditionBase {
  readonly kind: 'CondScoreRatioAtLeast'
  num: number
  den: number
}

/**
 * One shape of Condition.
 *
 * The file carries 31 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondNoFaceScored extends ConditionBase {
  readonly kind: 'CondNoFaceScored'
}

/**
 * One shape of Condition.
 *
 * The file carries 32 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondFaceScored extends ConditionBase {
  readonly kind: 'CondFaceScored'
}

/**
 * One shape of Condition.
 *
 * The file carries 33 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCounterAtMost extends ConditionBase {
  readonly kind: 'CondCounterAtMost'
  counter: CounterField
  n: number
}

/**
 * One shape of Condition.
 *
 * The file carries 34 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondDiscardedFaceAtLeast extends ConditionBase {
  readonly kind: 'CondDiscardedFaceAtLeast'
  n: number
}

/**
 * One shape of Condition.
 *
 * The file carries 35 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondHandContainsRankAndHand extends ConditionBase {
  readonly kind: 'CondHandContainsRankAndHand'
  hand: PokerHandKind
}

/**
 * One shape of Condition.
 *
 * The file carries 36 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondFirstHandSingleCard extends ConditionBase {
  readonly kind: 'CondFirstHandSingleCard'
}

/**
 * One shape of Condition.
 *
 * The file carries 37 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondFirstHandSingleRank extends ConditionBase {
  readonly kind: 'CondFirstHandSingleRank'
}

/**
 * One shape of Condition.
 *
 * The file carries 38 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondFirstDiscardSingleCard extends ConditionBase {
  readonly kind: 'CondFirstDiscardSingleCard'
}

/**
 * One shape of Condition.
 *
 * The file carries 39 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondNotMostPlayedHand extends ConditionBase {
  readonly kind: 'CondNotMostPlayedHand'
}

/**
 * One shape of Condition.
 *
 * The file carries 40 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondCardEnhanced extends ConditionBase {
  readonly kind: 'CondCardEnhanced'
}

/**
 * One shape of Condition.
 *
 * The file carries 41 for it. That number rather than the name is what
 * lets this variant be renamed without a deployed reader reading a different one.
 */
export interface CondConsumableKind extends ConditionBase {
  readonly kind: 'CondConsumableKind'
  consumable: ConsumableKind
}
