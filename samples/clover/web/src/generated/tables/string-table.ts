// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { CloverData } from '../clover-data'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  stringId: string
  ko: string
  en: string
  ja: string
  zhHans: string
  zhHant: string
  de: string
}

// Generated from samples/clover/design-data/xlsx/Text.xlsx : StringTable : A1
/** 번역 대조본입니다. 시트의 표시 이름이 한국어이고 영어가 여기 있습니다. */
export class StringTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** 식별자 */
  public get stringId(): string { return this._stringId }

  /** 한국어 */
  public get ko(): string { return this._ko }

  /** 영어 */
  public get en(): string { return this._en }

  /** 일본어 */
  public get ja(): string { return this._ja }

  /** 중국어 간체 */
  public get zhHans(): string { return this._zhHans }

  /** 중국어 번체 */
  public get zhHant(): string { return this._zhHant }

  /** 독일어 */
  public get de(): string { return this._de }

  public _stringId: string = ''
  public _ko: string = ''
  public _en: string = ''
  public _ja: string = ''
  public _zhHans: string = ''
  public _zhHant: string = ''
  public _de: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._stringId = dataRow.stringId
    this._ko = dataRow.ko
    this._en = dataRow.en
    this._ja = dataRow.ja
    this._zhHans = dataRow.zhHans
    this._zhHant = dataRow.zhHant
    this._de = dataRow.de
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._stringId = dataRow[offset++]
    this._ko = dataRow[offset++]
    this._en = dataRow[offset++]
    this._ja = dataRow[offset++]
    this._zhHans = dataRow[offset++]
    this._zhHant = dataRow[offset++]
    this._de = dataRow[offset++]
  }
}

// Generated from samples/clover/design-data/xlsx/Text.xlsx : StringTable : A1
/** 번역 대조본입니다. 시트의 표시 이름이 한국어이고 영어가 여기 있습니다. */
export class StringTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): StringTableRecord[] { return this._records }
  private _records: StringTableRecord[] = []

  /** How many rows the table holds. */
  public get count(): number { return this._records.length }

  /**
   * The rows, in the order the file wrote them.
   *
   * The array reference is read once, here. A refresh replaces the reference rather
   * than its contents, so a loop already running keeps the rows it started with.
   */
  public [Symbol.iterator](): IterableIterator<StringTableRecord> {
    return this._records[Symbol.iterator]()
  }

  // Indexing by 'stringId'
  public get recordsByStringId(): Map<string, StringTableRecord> { return this._recordsByStringId }
  private _recordsByStringId: Map<string, StringTableRecord> = new Map<string, StringTableRecord>()

  /**
   * The row with this stringId, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByStringId(key: string): StringTableRecord | undefined {
    return this._recordsByStringId.get(key)
  }

  /**
   * The row with this stringId, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByStringId(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByStringIdOrThrow(key: string): StringTableRecord {
    const found = this._recordsByStringId.get(key)
    if (!found)
      throw new Error(`There is no record in table "StringTable" that corresponds to field "stringId" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this stringId. */
  public containsStringId(key: string): boolean {
    return this._recordsByStringId.has(key)
  }


  /**
   * Each row with the stringId it is keyed by -
   * `for (const [key, row] of table.entries())`.
   *
   * What this saves a caller is not the key value - the row carries it - but having to know
   * which column the key is: stringId here, something else in the next table.
   *
   * The rows come in the order the file wrote them rather than the order the map holds
   * them, which makes this and iterating the table agree. Only the primary key has this: a
   * table keyed by several columns together has no single key value to pair a row with.
   */
  public *entries(): IterableIterator<[string, StringTableRecord]> {
    for (const record of this._records) {
      yield [record.stringId, record]
    }
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: StringTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new StringTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new StringTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `CloverData.encryptionKey` and `CloverData.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      CloverData.encryptionKey, CloverData.macKey, CloverData.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: StringTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new StringTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'StringTable.StringId', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'StringTable.StringId')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._stringId = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'StringTable.Ko', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'StringTable.Ko')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._ko = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'StringTable.En', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'StringTable.En')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._en = value
          }
          break
        case 4:
          tabbit.checkColumn(column, 'StringTable.Ja', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'StringTable.Ja')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._ja = value
          }
          break
        case 5:
          tabbit.checkColumn(column, 'StringTable.ZhHans', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'StringTable.ZhHans')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._zhHans = value
          }
          break
        case 6:
          tabbit.checkColumn(column, 'StringTable.ZhHant', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'StringTable.ZhHant')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._zhHant = value
          }
          break
        case 7:
          tabbit.checkColumn(column, 'StringTable.De', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'StringTable.De')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._de = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: StringTableRecord[]): void {
    const recordsByStringId = new Map<string, StringTableRecord>()

    for (const record of records) {
      recordsByStringId.set(record.stringId, record)
    }

    this._records = records
    this._recordsByStringId = recordsByStringId
  }
}
