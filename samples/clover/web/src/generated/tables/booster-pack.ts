// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { CloverData } from '../clover-data'

// Automatically import to handle external type references.
import { PackKind } from '../enums/pack-kind'
import { PackSize } from '../enums/pack-size'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  packId: string
  kind: PackKind
  size: PackSize
  cost: number
  cards: number
  picks: number
}

// Generated from samples/clover/design-data/xlsx/Shop.xlsx : BoosterPack : A1
/** 팩 15종입니다. 갈래 5종과 크기 3종의 조합입니다. */
export class BoosterPackRecord {
  /** Default constructor */
  constructor() {
  }

  /** 식별자 */
  public get packId(): string { return this._packId }

  /** 갈래 */
  public get kind(): PackKind { return this._kind }

  /** 크기 */
  public get size(): PackSize { return this._size }

  /** 값 */
  public get cost(): number { return this._cost }

  /** 들어 있는 장수 */
  public get cards(): number { return this._cards }

  /** 고르는 장수 */
  public get picks(): number { return this._picks }

  public _packId: string = ''
  public _kind: PackKind = 0 as PackKind
  public _size: PackSize = 0 as PackSize
  public _cost: number = 0
  public _cards: number = 0
  public _picks: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._packId = dataRow.packId
    this._kind = dataRow.kind
    this._size = dataRow.size
    this._cost = dataRow.cost
    this._cards = dataRow.cards
    this._picks = dataRow.picks
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._packId = dataRow[offset++]
    this._kind = dataRow[offset++]
    this._size = dataRow[offset++]
    this._cost = dataRow[offset++]
    this._cards = dataRow[offset++]
    this._picks = dataRow[offset++]
  }
}

// Generated from samples/clover/design-data/xlsx/Shop.xlsx : BoosterPack : A1
/** 팩 15종입니다. 갈래 5종과 크기 3종의 조합입니다. */
export class BoosterPackTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): BoosterPackRecord[] { return this._records }
  private _records: BoosterPackRecord[] = []

  // Indexing by 'packId'
  public get recordsByPackId(): Map<string, BoosterPackRecord> { return this._recordsByPackId }
  private _recordsByPackId: Map<string, BoosterPackRecord> = new Map<string, BoosterPackRecord>()

  /**
   * The row with this packId, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByPackId(key: string): BoosterPackRecord | undefined {
    return this._recordsByPackId.get(key)
  }

  /**
   * The row with this packId, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByPackId(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByPackIdOrThrow(key: string): BoosterPackRecord {
    const found = this._recordsByPackId.get(key)
    if (!found)
      throw new Error(`There is no record in table "BoosterPack" that corresponds to field "packId" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this packId. */
  public containsPackId(key: string): boolean {
    return this._recordsByPackId.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: BoosterPackRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new BoosterPackRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new BoosterPackRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `CloverData.encryptionKey` and `CloverData.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      CloverData.encryptionKey, CloverData.macKey, CloverData.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: BoosterPackRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new BoosterPackRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'BoosterPack.PackId', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'BoosterPack.PackId')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._packId = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'BoosterPack.Kind', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'BoosterPack.Kind')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._kind = value as PackKind
          }
          break
        case 3:
          tabbit.checkColumn(column, 'BoosterPack.Size', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'BoosterPack.Size')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._size = value as PackSize
          }
          break
        case 4:
          tabbit.checkColumn(column, 'BoosterPack.Cost', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'BoosterPack.Cost')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._cost = value
          }
          break
        case 5:
          tabbit.checkColumn(column, 'BoosterPack.Cards', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'BoosterPack.Cards')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._cards = value
          }
          break
        case 6:
          tabbit.checkColumn(column, 'BoosterPack.Picks', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'BoosterPack.Picks')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._picks = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: BoosterPackRecord[]): void {
    const recordsByPackId = new Map<string, BoosterPackRecord>()

    for (const record of records) {
      recordsByPackId.set(record.packId, record)
    }

    this._records = records
    this._recordsByPackId = recordsByPackId
  }
}
