// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { CloverData } from '../clover-data'

// Automatically import to handle external type references.
import { BlindKind } from '../enums/blind-kind'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  blind: BlindKind
  name: string
  scoreMul: number
  reward: number
  skippable: boolean
}

// Generated from samples/clover/design-data/xlsx/Progression.xlsx : Blind : A1
/** 안테 하나의 세 라운드입니다. 보스는 배율을 `BossBlind` 가 덮어씁니다. */
export class BlindRecord {
  /** Default constructor */
  constructor() {
  }

  /** 블라인드 */
  public get blind(): BlindKind { return this._blind }

  /** 표시 이름 */
  public get name(): string { return this._name }

  /** 기준 점수의 배율. 만분율 */
  public get scoreMul(): number { return this._scoreMul }

  /** 격파 보상 */
  public get reward(): number { return this._reward }

  /** 스킵할 수 있는가 */
  public get skippable(): boolean { return this._skippable }

  public _blind: BlindKind = 0 as BlindKind
  public _name: string = ''
  public _scoreMul: number = 0
  public _reward: number = 0
  public _skippable: boolean = false

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._blind = dataRow.blind
    this._name = dataRow.name
    this._scoreMul = dataRow.scoreMul
    this._reward = dataRow.reward
    this._skippable = dataRow.skippable
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._blind = dataRow[offset++]
    this._name = dataRow[offset++]
    this._scoreMul = dataRow[offset++]
    this._reward = dataRow[offset++]
    this._skippable = dataRow[offset++]
  }
}

// Generated from samples/clover/design-data/xlsx/Progression.xlsx : Blind : A1
/** 안테 하나의 세 라운드입니다. 보스는 배율을 `BossBlind` 가 덮어씁니다. */
export class BlindTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): BlindRecord[] { return this._records }
  private _records: BlindRecord[] = []

  /** How many rows the table holds. */
  public get count(): number { return this._records.length }

  /**
   * The rows, in the order the file wrote them.
   *
   * The array reference is read once, here. A refresh replaces the reference rather
   * than its contents, so a loop already running keeps the rows it started with.
   */
  public [Symbol.iterator](): IterableIterator<BlindRecord> {
    return this._records[Symbol.iterator]()
  }

  // Indexing by 'blind'
  public get recordsByBlind(): Map<BlindKind, BlindRecord> { return this._recordsByBlind }
  private _recordsByBlind: Map<BlindKind, BlindRecord> = new Map<BlindKind, BlindRecord>()

  /**
   * The row with this blind, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByBlind(key: BlindKind): BlindRecord | undefined {
    return this._recordsByBlind.get(key)
  }

  /**
   * The row with this blind, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByBlind(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByBlindOrThrow(key: BlindKind): BlindRecord {
    const found = this._recordsByBlind.get(key)
    if (!found)
      throw new Error(`There is no record in table "Blind" that corresponds to field "blind" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this blind. */
  public containsBlind(key: BlindKind): boolean {
    return this._recordsByBlind.has(key)
  }


  /**
   * Each row with the blind it is keyed by -
   * `for (const [key, row] of table.entries())`.
   *
   * What this saves a caller is not the key value - the row carries it - but having to know
   * which column the key is: blind here, something else in the next table.
   *
   * The rows come in the order the file wrote them rather than the order the map holds
   * them, which makes this and iterating the table agree. Only the primary key has this: a
   * table keyed by several columns together has no single key value to pair a row with.
   */
  public *entries(): IterableIterator<[BlindKind, BlindRecord]> {
    for (const record of this._records) {
      yield [record.blind, record]
    }
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: BlindRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new BlindRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new BlindRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `CloverData.encryptionKey` and `CloverData.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      CloverData.encryptionKey, CloverData.macKey, CloverData.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: BlindRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new BlindRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'Blind.Blind', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Blind.Blind')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._blind = value as BlindKind
          }
          break
        case 2:
          tabbit.checkColumn(column, 'Blind.Name', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Blind.Name')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._name = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'Blind.ScoreMul', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Blind.ScoreMul')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._scoreMul = value
          }
          break
        case 4:
          tabbit.checkColumn(column, 'Blind.Reward', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Blind.Reward')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._reward = value
          }
          break
        case 5:
          tabbit.checkColumn(column, 'Blind.Skippable', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_BOOL])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Blind.Skippable')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._skippable = cursor.nextBool()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: BlindRecord[]): void {
    const recordsByBlind = new Map<BlindKind, BlindRecord>()

    for (const record of records) {
      recordsByBlind.set(record.blind, record)
    }

    this._records = records
    this._recordsByBlind = recordsByBlind
  }
}
