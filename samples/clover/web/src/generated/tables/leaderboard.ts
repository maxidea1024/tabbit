// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { CloverData } from '../clover-data'

// Automatically import to handle external type references.
import { LeaderboardMetric } from '../enums/leaderboard-metric'
import { PoolChoice } from '../enums/pool-choice'
import { SplitKind } from '../enums/split-kind'
import { BoardGroup } from '../enums/board-group'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  boardId: string
  name: string
  metric: LeaderboardMetric
  pool: PoolChoice
  split: SplitKind
  splitRef: string
  group: BoardGroup
  sortOrder: number
}

// Generated from samples/clover/design-data/xlsx/Meta.xlsx : Leaderboard : A1
/** 리더보드 한 판입니다. **지표 하나를 축 하나로 나눈 것**이고, 보드를 더하는 데 코드가 바뀌지 않습니다. 화면에 적히는 이름은 지표와 축의 이름에서 조립하므로, 여기 `name` 은 기획자가 시트에서 읽는 것입니다. */
export class LeaderboardRecord {
  /** Default constructor */
  constructor() {
  }

  /** 식별자 */
  public get boardId(): string { return this._boardId }

  /** 기획자가 읽는 이름. **화면의 이름은 지표와 축에서 조립됩니다** */
  public get name(): string { return this._name }

  /** 순위를 매기는 값 */
  public get metric(): LeaderboardMetric { return this._metric }

  /** 조커 풀. 「전부」가 없습니다 */
  public get pool(): PoolChoice { return this._pool }

  /** 무엇으로 나누는가 */
  public get split(): SplitKind { return this._split }

  /** 나누는 값. `split` 이 `None` 이면 비어 있습니다 */
  public get splitRef(): string { return this._splitRef }
  /** Whether this row has a value for `splitRef`. */
  public get hasSplitRef(): boolean { return this._splitRefHasValue }

  /** 판의 어느 탭 */
  public get group(): BoardGroup { return this._group }

  /** 탭 안의 순서 */
  public get sortOrder(): number { return this._sortOrder }

  public _boardId: string = ''
  public _name: string = ''
  public _metric: LeaderboardMetric = 0 as LeaderboardMetric
  public _pool: PoolChoice = 0 as PoolChoice
  public _split: SplitKind = 0 as SplitKind
  public _splitRef: string = ''
  public _splitRefHasValue: boolean = false
  public _group: BoardGroup = 0 as BoardGroup
  public _sortOrder: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._boardId = dataRow.boardId
    this._name = dataRow.name
    this._metric = dataRow.metric
    this._pool = dataRow.pool
    this._split = dataRow.split
    this._splitRefHasValue = dataRow.splitRef !== null && dataRow.splitRef !== undefined; if (this._splitRefHasValue) this._splitRef = dataRow.splitRef
    this._group = dataRow.group
    this._sortOrder = dataRow.sortOrder
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._boardId = dataRow[offset++]
    this._name = dataRow[offset++]
    this._metric = dataRow[offset++]
    this._pool = dataRow[offset++]
    this._split = dataRow[offset++]
    const _splitRef_raw = dataRow[offset++]
    this._splitRefHasValue = _splitRef_raw !== null && _splitRef_raw !== undefined
    if (this._splitRefHasValue) this._splitRef = _splitRef_raw
    this._group = dataRow[offset++]
    this._sortOrder = dataRow[offset++]
  }
}

// Generated from samples/clover/design-data/xlsx/Meta.xlsx : Leaderboard : A1
/** 리더보드 한 판입니다. **지표 하나를 축 하나로 나눈 것**이고, 보드를 더하는 데 코드가 바뀌지 않습니다. 화면에 적히는 이름은 지표와 축의 이름에서 조립하므로, 여기 `name` 은 기획자가 시트에서 읽는 것입니다. */
export class LeaderboardTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): LeaderboardRecord[] { return this._records }
  private _records: LeaderboardRecord[] = []

  /** How many rows the table holds. */
  public get count(): number { return this._records.length }

  /**
   * The rows, in the order the file wrote them.
   *
   * The array reference is read once, here. A refresh replaces the reference rather
   * than its contents, so a loop already running keeps the rows it started with.
   */
  public [Symbol.iterator](): IterableIterator<LeaderboardRecord> {
    return this._records[Symbol.iterator]()
  }

  // Indexing by 'boardId'
  public get recordsByBoardId(): Map<string, LeaderboardRecord> { return this._recordsByBoardId }
  private _recordsByBoardId: Map<string, LeaderboardRecord> = new Map<string, LeaderboardRecord>()

  /**
   * The row with this boardId, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByBoardId(key: string): LeaderboardRecord | undefined {
    return this._recordsByBoardId.get(key)
  }

  /**
   * The row with this boardId, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByBoardId(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByBoardIdOrThrow(key: string): LeaderboardRecord {
    const found = this._recordsByBoardId.get(key)
    if (!found)
      throw new Error(`There is no record in table "Leaderboard" that corresponds to field "boardId" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this boardId. */
  public containsBoardId(key: string): boolean {
    return this._recordsByBoardId.has(key)
  }


  /**
   * Each row with the boardId it is keyed by -
   * `for (const [key, row] of table.entries())`.
   *
   * What this saves a caller is not the key value - the row carries it - but having to know
   * which column the key is: boardId here, something else in the next table.
   *
   * The rows come in the order the file wrote them rather than the order the map holds
   * them, which makes this and iterating the table agree. Only the primary key has this: a
   * table keyed by several columns together has no single key value to pair a row with.
   */
  public *entries(): IterableIterator<[string, LeaderboardRecord]> {
    for (const record of this._records) {
      yield [record.boardId, record]
    }
  }

  /**
   * Read a table from the text of its JSON export.
   *
   * The text, not a path: where it comes from - a file, a fetch, an asset bundle - is the
   * caller's, so this module names no runtime module and loads wherever TypeScript runs.
   * Either row shape the exporter writes is accepted.
   */
  public readJsonFrom(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: LeaderboardRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new LeaderboardRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new LeaderboardRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /**
   * Read a table from the bytes of its binary export.
   *
   * The bytes as the file holds them; the envelope is opened here. Unconditionally, and with
   * whatever `CloverData.encryptionKey` and `CloverData.macKey` hold - which is null
   * unless the project set them. A file that is neither encrypted nor signed comes back from
   * that untouched, so the load path is the same either way and there is no condition here
   * that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
   *
   * Then column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(fileBytes: Uint8Array): void {
    const data = tabbit.open(
      fileBytes, CloverData.encryptionKey, CloverData.macKey, CloverData.verifyMac)
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor
    let presence: Uint8Array | null

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: LeaderboardRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new LeaderboardRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'Leaderboard.BoardId', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Leaderboard.BoardId')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._boardId = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'Leaderboard.Name', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Leaderboard.Name')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._name = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'Leaderboard.Metric', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Leaderboard.Metric')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._metric = value as LeaderboardMetric
          }
          break
        case 4:
          tabbit.checkColumn(column, 'Leaderboard.Pool', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Leaderboard.Pool')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._pool = value as PoolChoice
          }
          break
        case 5:
          tabbit.checkColumn(column, 'Leaderboard.Split', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Leaderboard.Split')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._split = value as SplitKind
          }
          break
        case 6:
          tabbit.checkColumn(column, 'Leaderboard.SplitRef', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_STRING])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Leaderboard.SplitRef')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._splitRef = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._splitRefHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._splitRefHasValue)
              records[i]._splitRef = ''
          }
          break
        case 7:
          tabbit.checkColumn(column, 'Leaderboard.Group', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Leaderboard.Group')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._group = value as BoardGroup
          }
          break
        case 8:
          tabbit.checkColumn(column, 'Leaderboard.SortOrder', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Leaderboard.SortOrder')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._sortOrder = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: LeaderboardRecord[]): void {
    const recordsByBoardId = new Map<string, LeaderboardRecord>()

    for (const record of records) {
      recordsByBoardId.set(record.boardId, record)
    }

    this._records = records
    this._recordsByBoardId = recordsByBoardId
  }
}
