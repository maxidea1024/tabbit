// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { CloverData } from '../clover-data'

// Automatically import to handle external type references.
import { TransitionKind } from '../enums/transition-kind'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  transitionId: string
  kind: TransitionKind
  outMs: number
  holdMs: number
  inMs: number
  ink: string
  toward: boolean
  cue: string
  note: string
}

// Generated from samples/clover/design-data/xlsx/Feel.xlsx : Transition : A1
/** 씬이 갈릴 때 화면을 어떻게 지우는가입니다. **덮개를 그리는 것이 아니라 화면 자체를 처리합니다.** */
export class TransitionRecord {
  /** Default constructor */
  constructor() {
  }

  /** 갈리는 자리 */
  public get transitionId(): string { return this._transitionId }

  /** 지우는 방법 */
  public get kind(): TransitionKind { return this._kind }

  /** 지우는 시간. 밀리초 */
  public get outMs(): number { return this._outMs }

  /** 아무것도 보이지 않는 채로 머무는 시간 */
  public get holdMs(): number { return this._holdMs }

  /** 되돌리는 시간 */
  public get inMs(): number { return this._inMs }

  /** 다 지워진 자리에 남는 색 */
  public get ink(): string { return this._ink }

  /** 다가오는가. 밀림과 옆으로가 이 값을 봅니다 */
  public get toward(): boolean { return this._toward }

  /** 시작할 때 나는 소리. `SoundCue` 의 이름 */
  public get cue(): string { return this._cue }
  /** Whether this row has a value for `cue`. */
  public get hasCue(): boolean { return this._cueHasValue }

  /** 어느 자리인가 */
  public get note(): string { return this._note }

  public _transitionId: string = ''
  public _kind: TransitionKind = 0 as TransitionKind
  public _outMs: number = 0
  public _holdMs: number = 0
  public _inMs: number = 0
  public _ink: string = ''
  public _toward: boolean = false
  public _cue: string = ''
  public _cueHasValue: boolean = false
  public _note: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._transitionId = dataRow.transitionId
    this._kind = dataRow.kind
    this._outMs = dataRow.outMs
    this._holdMs = dataRow.holdMs
    this._inMs = dataRow.inMs
    this._ink = dataRow.ink
    this._toward = dataRow.toward
    this._cueHasValue = dataRow.cue !== null && dataRow.cue !== undefined; if (this._cueHasValue) this._cue = dataRow.cue
    this._note = dataRow.note
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._transitionId = dataRow[offset++]
    this._kind = dataRow[offset++]
    this._outMs = dataRow[offset++]
    this._holdMs = dataRow[offset++]
    this._inMs = dataRow[offset++]
    this._ink = dataRow[offset++]
    this._toward = dataRow[offset++]
    const _cue_raw = dataRow[offset++]
    this._cueHasValue = _cue_raw !== null && _cue_raw !== undefined
    if (this._cueHasValue) this._cue = _cue_raw
    this._note = dataRow[offset++]
  }
}

// Generated from samples/clover/design-data/xlsx/Feel.xlsx : Transition : A1
/** 씬이 갈릴 때 화면을 어떻게 지우는가입니다. **덮개를 그리는 것이 아니라 화면 자체를 처리합니다.** */
export class TransitionTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): TransitionRecord[] { return this._records }
  private _records: TransitionRecord[] = []

  /** How many rows the table holds. */
  public get count(): number { return this._records.length }

  /**
   * The rows, in the order the file wrote them.
   *
   * The array reference is read once, here. A refresh replaces the reference rather
   * than its contents, so a loop already running keeps the rows it started with.
   */
  public [Symbol.iterator](): IterableIterator<TransitionRecord> {
    return this._records[Symbol.iterator]()
  }

  // Indexing by 'transitionId'
  public get recordsByTransitionId(): Map<string, TransitionRecord> { return this._recordsByTransitionId }
  private _recordsByTransitionId: Map<string, TransitionRecord> = new Map<string, TransitionRecord>()

  /**
   * The row with this transitionId, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByTransitionId(key: string): TransitionRecord | undefined {
    return this._recordsByTransitionId.get(key)
  }

  /**
   * The row with this transitionId, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByTransitionId(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByTransitionIdOrThrow(key: string): TransitionRecord {
    const found = this._recordsByTransitionId.get(key)
    if (!found)
      throw new Error(`There is no record in table "Transition" that corresponds to field "transitionId" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this transitionId. */
  public containsTransitionId(key: string): boolean {
    return this._recordsByTransitionId.has(key)
  }


  /**
   * Each row with the transitionId it is keyed by -
   * `for (const [key, row] of table.entries())`.
   *
   * What this saves a caller is not the key value - the row carries it - but having to know
   * which column the key is: transitionId here, something else in the next table.
   *
   * The rows come in the order the file wrote them rather than the order the map holds
   * them, which makes this and iterating the table agree. Only the primary key has this: a
   * table keyed by several columns together has no single key value to pair a row with.
   */
  public *entries(): IterableIterator<[string, TransitionRecord]> {
    for (const record of this._records) {
      yield [record.transitionId, record]
    }
  }

  /**
   * Read a table from the text of its JSON export.
   *
   * The text, not a path: where it comes from - a file, a fetch, an asset bundle - is the
   * caller's, so this module names no runtime module and loads wherever TypeScript runs.
   * Either row shape the exporter writes is accepted.
   */
  public readJsonFrom(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: TransitionRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new TransitionRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new TransitionRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /**
   * Read a table from the bytes of its binary export.
   *
   * The bytes as the file holds them; the envelope is opened here. Unconditionally, and with
   * whatever `CloverData.encryptionKey` and `CloverData.macKey` hold - which is null
   * unless the project set them. A file that is neither encrypted nor signed comes back from
   * that untouched, so the load path is the same either way and there is no condition here
   * that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
   *
   * Then column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(fileBytes: Uint8Array): void {
    const data = tabbit.open(
      fileBytes, CloverData.encryptionKey, CloverData.macKey, CloverData.verifyMac)
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor
    let presence: Uint8Array | null

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: TransitionRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new TransitionRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'Transition.TransitionId', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Transition.TransitionId')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._transitionId = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'Transition.Kind', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Transition.Kind')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._kind = value as TransitionKind
          }
          break
        case 3:
          tabbit.checkColumn(column, 'Transition.OutMs', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Transition.OutMs')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._outMs = value
          }
          break
        case 4:
          tabbit.checkColumn(column, 'Transition.HoldMs', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Transition.HoldMs')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._holdMs = value
          }
          break
        case 5:
          tabbit.checkColumn(column, 'Transition.InMs', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Transition.InMs')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._inMs = value
          }
          break
        case 6:
          tabbit.checkColumn(column, 'Transition.Ink', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Transition.Ink')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._ink = value
          }
          break
        case 7:
          tabbit.checkColumn(column, 'Transition.Toward', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_BOOL])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Transition.Toward')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._toward = cursor.nextBool()
          }
          break
        case 8:
          tabbit.checkColumn(column, 'Transition.Cue', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_STRING])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Transition.Cue')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._cue = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._cueHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._cueHasValue)
              records[i]._cue = ''
          }
          break
        case 9:
          tabbit.checkColumn(column, 'Transition.Note', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Transition.Note')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._note = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: TransitionRecord[]): void {
    const recordsByTransitionId = new Map<string, TransitionRecord>()

    for (const record of records) {
      recordsByTransitionId.set(record.transitionId, record)
    }

    this._records = records
    this._recordsByTransitionId = recordsByTransitionId
  }
}
