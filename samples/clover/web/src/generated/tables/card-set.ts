// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { CloverData } from '../clover-data'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  setId: string
  name: string
  sortOrder: number
  artDir: string
  credit: string
  artHasIndex: boolean
  paper: string
}

// Generated from samples/clover/design-data/xlsx/Feel.xlsx : CardSet : A1
/** 트럼프 한 벌의 겉모습입니다. **덱과 다른 축입니다** — 덱은 런의 시작 조건이고 이것은 그 52장이 어떻게 보이는가입니다. 그림이 있으면 그림이 얼굴이고, 없으면 무늬와 랭크를 그립니다. */
export class CardSetRecord {
  /** Default constructor */
  constructor() {
  }

  /** 식별자 */
  public get setId(): string { return this._setId }

  /** 표시 이름 */
  public get name(): string { return this._name }

  /** 고르는 화면에서의 순서 */
  public get sortOrder(): number { return this._sortOrder }

  /** 그림이 있는 폴더. 비면 그립니다 */
  public get artDir(): string { return this._artDir }
  /** Whether this row has a value for `artDir`. */
  public get hasArtDir(): boolean { return this._artDirHasValue }

  /** 그림이 어디서 왔는가 */
  public get credit(): string { return this._credit }
  /** Whether this row has a value for `credit`. */
  public get hasCredit(): boolean { return this._creditHasValue }

  /** 그림에 모서리의 랭크와 무늬가 들어 있는가 */
  public get artHasIndex(): boolean { return this._artHasIndex }

  /** 종이의 색. **강화의 색과 겹치지 않아야 합니다** — 바탕에 색이 들면 강화로 읽힙니다 */
  public get paper(): string { return this._paper }

  public _setId: string = ''
  public _name: string = ''
  public _sortOrder: number = 0
  public _artDir: string = ''
  public _artDirHasValue: boolean = false
  public _credit: string = ''
  public _creditHasValue: boolean = false
  public _artHasIndex: boolean = false
  public _paper: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._setId = dataRow.setId
    this._name = dataRow.name
    this._sortOrder = dataRow.sortOrder
    this._artDirHasValue = dataRow.artDir !== null && dataRow.artDir !== undefined; if (this._artDirHasValue) this._artDir = dataRow.artDir
    this._creditHasValue = dataRow.credit !== null && dataRow.credit !== undefined; if (this._creditHasValue) this._credit = dataRow.credit
    this._artHasIndex = dataRow.artHasIndex
    this._paper = dataRow.paper
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._setId = dataRow[offset++]
    this._name = dataRow[offset++]
    this._sortOrder = dataRow[offset++]
    const _artDir_raw = dataRow[offset++]
    this._artDirHasValue = _artDir_raw !== null && _artDir_raw !== undefined
    if (this._artDirHasValue) this._artDir = _artDir_raw
    const _credit_raw = dataRow[offset++]
    this._creditHasValue = _credit_raw !== null && _credit_raw !== undefined
    if (this._creditHasValue) this._credit = _credit_raw
    this._artHasIndex = dataRow[offset++]
    this._paper = dataRow[offset++]
  }
}

// Generated from samples/clover/design-data/xlsx/Feel.xlsx : CardSet : A1
/** 트럼프 한 벌의 겉모습입니다. **덱과 다른 축입니다** — 덱은 런의 시작 조건이고 이것은 그 52장이 어떻게 보이는가입니다. 그림이 있으면 그림이 얼굴이고, 없으면 무늬와 랭크를 그립니다. */
export class CardSetTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): CardSetRecord[] { return this._records }
  private _records: CardSetRecord[] = []

  /** How many rows the table holds. */
  public get count(): number { return this._records.length }

  /**
   * The rows, in the order the file wrote them.
   *
   * The array reference is read once, here. A refresh replaces the reference rather
   * than its contents, so a loop already running keeps the rows it started with.
   */
  public [Symbol.iterator](): IterableIterator<CardSetRecord> {
    return this._records[Symbol.iterator]()
  }

  // Indexing by 'setId'
  public get recordsBySetId(): Map<string, CardSetRecord> { return this._recordsBySetId }
  private _recordsBySetId: Map<string, CardSetRecord> = new Map<string, CardSetRecord>()

  /**
   * The row with this setId, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findBySetId(key: string): CardSetRecord | undefined {
    return this._recordsBySetId.get(key)
  }

  /**
   * The row with this setId, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getBySetId(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getBySetIdOrThrow(key: string): CardSetRecord {
    const found = this._recordsBySetId.get(key)
    if (!found)
      throw new Error(`There is no record in table "CardSet" that corresponds to field "setId" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this setId. */
  public containsSetId(key: string): boolean {
    return this._recordsBySetId.has(key)
  }


  /**
   * Each row with the setId it is keyed by -
   * `for (const [key, row] of table.entries())`.
   *
   * What this saves a caller is not the key value - the row carries it - but having to know
   * which column the key is: setId here, something else in the next table.
   *
   * The rows come in the order the file wrote them rather than the order the map holds
   * them, which makes this and iterating the table agree. Only the primary key has this: a
   * table keyed by several columns together has no single key value to pair a row with.
   */
  public *entries(): IterableIterator<[string, CardSetRecord]> {
    for (const record of this._records) {
      yield [record.setId, record]
    }
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: CardSetRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new CardSetRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new CardSetRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `CloverData.encryptionKey` and `CloverData.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      CloverData.encryptionKey, CloverData.macKey, CloverData.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor
    let presence: Uint8Array | null

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: CardSetRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new CardSetRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'CardSet.SetId', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CardSet.SetId')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._setId = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'CardSet.Name', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CardSet.Name')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._name = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'CardSet.SortOrder', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CardSet.SortOrder')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._sortOrder = value
          }
          break
        case 4:
          tabbit.checkColumn(column, 'CardSet.ArtDir', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_STRING])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CardSet.ArtDir')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._artDir = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._artDirHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._artDirHasValue)
              records[i]._artDir = ''
          }
          break
        case 5:
          tabbit.checkColumn(column, 'CardSet.Credit', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_STRING])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CardSet.Credit')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._credit = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._creditHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._creditHasValue)
              records[i]._credit = ''
          }
          break
        case 6:
          tabbit.checkColumn(column, 'CardSet.ArtHasIndex', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_BOOL])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CardSet.ArtHasIndex')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._artHasIndex = cursor.nextBool()
          }
          break
        case 7:
          tabbit.checkColumn(column, 'CardSet.Paper', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CardSet.Paper')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._paper = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: CardSetRecord[]): void {
    const recordsBySetId = new Map<string, CardSetRecord>()

    for (const record of records) {
      recordsBySetId.set(record.setId, record)
    }

    this._records = records
    this._recordsBySetId = recordsBySetId
  }
}
