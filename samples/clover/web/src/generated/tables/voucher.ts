// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { CloverData } from '../clover-data'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  voucherId: string
  name: string
  cost: number
  upgradesFrom: string
  sortOrder: number
}

// Generated from samples/clover/design-data/xlsx/Shop.xlsx : Voucher : A1
/** 바우처 32종입니다. 16쌍이고, 상위는 자기 하위를 산 뒤에만 상점에 나옵니다. */
export class VoucherRecord {
  /** Default constructor */
  constructor() {
  }

  /** 식별자 */
  public get voucherId(): string { return this._voucherId }

  /** 표시 이름 */
  public get name(): string { return this._name }

  /** 값 */
  public get cost(): number { return this._cost }

  /** 이것이 잇는 하위 바우처 */
  public get upgradesFrom(): string { return this._upgradesFrom_Voucher_index }
  public get voucherByUpgradesFrom(): VoucherRecord { return this._upgradesFrom }
  /** Whether this row has a value for `upgradesFrom`. */
  public get hasUpgradesFrom(): boolean { return this._upgradesFromHasValue }

  /** 수집 목록에서의 순서 */
  public get sortOrder(): number { return this._sortOrder }

  public setReference_upgradesFrom_INTERNAL(value: VoucherRecord) { this._upgradesFrom = value; }

  public _voucherId: string = ''
  public _name: string = ''
  public _cost: number = 0
  public _upgradesFrom: VoucherRecord
  public _upgradesFrom_Voucher_index: string = ""
  public _upgradesFromHasValue: boolean = false
  public _sortOrder: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._voucherId = dataRow.voucherId
    this._name = dataRow.name
    this._cost = dataRow.cost
    this._upgradesFrom_Voucher_index = dataRow.upgradesFrom
    this._sortOrder = dataRow.sortOrder
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._voucherId = dataRow[offset++]
    this._name = dataRow[offset++]
    this._cost = dataRow[offset++]
    this._upgradesFrom_Voucher_index = dataRow[offset++]
    this._sortOrder = dataRow[offset++]
  }
}

// Generated from samples/clover/design-data/xlsx/Shop.xlsx : Voucher : A1
/** 바우처 32종입니다. 16쌍이고, 상위는 자기 하위를 산 뒤에만 상점에 나옵니다. */
export class VoucherTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): VoucherRecord[] { return this._records }
  private _records: VoucherRecord[] = []

  /** How many rows the table holds. */
  public get count(): number { return this._records.length }

  /**
   * The rows, in the order the file wrote them.
   *
   * The array reference is read once, here. A refresh replaces the reference rather
   * than its contents, so a loop already running keeps the rows it started with.
   */
  public [Symbol.iterator](): IterableIterator<VoucherRecord> {
    return this._records[Symbol.iterator]()
  }

  // Indexing by 'voucherId'
  public get recordsByVoucherId(): Map<string, VoucherRecord> { return this._recordsByVoucherId }
  private _recordsByVoucherId: Map<string, VoucherRecord> = new Map<string, VoucherRecord>()

  /**
   * The row with this voucherId, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByVoucherId(key: string): VoucherRecord | undefined {
    return this._recordsByVoucherId.get(key)
  }

  /**
   * The row with this voucherId, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByVoucherId(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByVoucherIdOrThrow(key: string): VoucherRecord {
    const found = this._recordsByVoucherId.get(key)
    if (!found)
      throw new Error(`There is no record in table "Voucher" that corresponds to field "voucherId" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this voucherId. */
  public containsVoucherId(key: string): boolean {
    return this._recordsByVoucherId.has(key)
  }


  /**
   * Each row with the voucherId it is keyed by -
   * `for (const [key, row] of table.entries())`.
   *
   * What this saves a caller is not the key value - the row carries it - but having to know
   * which column the key is: voucherId here, something else in the next table.
   *
   * The rows come in the order the file wrote them rather than the order the map holds
   * them, which makes this and iterating the table agree. Only the primary key has this: a
   * table keyed by several columns together has no single key value to pair a row with.
   */
  public *entries(): IterableIterator<[string, VoucherRecord]> {
    for (const record of this._records) {
      yield [record.voucherId, record]
    }
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: VoucherRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new VoucherRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new VoucherRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `CloverData.encryptionKey` and `CloverData.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      CloverData.encryptionKey, CloverData.macKey, CloverData.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor
    let presence: Uint8Array | null

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: VoucherRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new VoucherRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'Voucher.VoucherId', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Voucher.VoucherId')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._voucherId = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'Voucher.Name', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Voucher.Name')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._name = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'Voucher.Cost', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Voucher.Cost')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._cost = value
          }
          break
        case 4:
          tabbit.checkColumn(column, 'Voucher.UpgradesFrom', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_STRING])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Voucher.UpgradesFrom')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._upgradesFrom_Voucher_index = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._upgradesFromHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._upgradesFromHasValue)
              records[i]._upgradesFrom = undefined
          }
          break
        case 5:
          tabbit.checkColumn(column, 'Voucher.SortOrder', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Voucher.SortOrder')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._sortOrder = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: VoucherRecord[]): void {
    const recordsByVoucherId = new Map<string, VoucherRecord>()

    for (const record of records) {
      recordsByVoucherId.set(record.voucherId, record)
    }

    this._records = records
    this._recordsByVoucherId = recordsByVoucherId
  }
}
