// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { CloverData } from '../clover-data'

// Automatically import to handle external type references.
import { JokerRecord } from './joker'
import { Trigger } from '../enums/trigger'
import { RankKind } from '../enums/rank-kind'
import { SuitKind } from '../enums/suit-kind'
import { Scope } from '../enums/scope'
import { PokerHandKind } from '../enums/poker-hand-kind'
import { EnhancementKind } from '../enums/enhancement-kind'
import { SealKind } from '../enums/seal-kind'
import { EditionKind } from '../enums/edition-kind'
import { BlindKind } from '../enums/blind-kind'
import { ConsumableKind } from '../enums/consumable-kind'
import { TargetKind } from '../enums/target-kind'
import { CounterField } from '../enums/counter-field'
import { Compare } from '../enums/compare'
import { UnitKind } from '../enums/unit-kind'
import { PerUnitMode } from '../enums/per-unit-mode'
import { ResetKind } from '../enums/reset-kind'
import { HandPick } from '../enums/hand-pick'
import { CreateKind } from '../enums/create-kind'
import { CardClass } from '../enums/card-class'
import { Rarity } from '../enums/rarity'
import { ModifyKind } from '../enums/modify-kind'
import { CardTrait } from '../enums/card-trait'
import { DebuffKind } from '../enums/debuff-kind'
import { JokerPick } from '../enums/joker-pick'
import { RuleKind } from '../enums/rule-kind'
import { Duration } from '../enums/duration'
import { Condition } from '../structs/condition'
import { Operation } from '../structs/operation'

/** One element of JokerEffectRecord.condition. */
export interface ConditionEntry {
  /** 무엇이 참이어야 하는가 */
  type: number
  hand: PokerHandKind
  suit: SuitKind
  enhancement: EnhancementKind
  seal: SealKind
  edition: EditionKind
  blind: BlindKind
  consumable: ConsumableKind
  target: TargetKind
  counter: CounterField
  consume: boolean
  n: number
  compare: Compare
  num: number
  den: number
}

function buildCondition(e: ConditionEntry): Condition {
  switch (e.type) {
    case 1:
      return {
        kind: 'CondAlways',
      }
    case 2:
      return {
        kind: 'CondHandContains',
        hand: e.hand,
      }
    case 3:
      return {
        kind: 'CondHandIs',
        hand: e.hand,
      }
    case 4:
      return {
        kind: 'CondCardSuit',
        suit: e.suit,
      }
    case 5:
      return {
        kind: 'CondCardRankSet',
      }
    case 6:
      return {
        kind: 'CondCardIsFace',
      }
    case 7:
      return {
        kind: 'CondCardEnhancement',
        enhancement: e.enhancement,
      }
    case 8:
      return {
        kind: 'CondCardSeal',
        seal: e.seal,
      }
    case 9:
      return {
        kind: 'CondCardEdition',
        edition: e.edition,
      }
    case 10:
      return {
        kind: 'CondCardCount',
        n: e.n,
        compare: e.compare,
      }
    case 11:
      return {
        kind: 'CondAllSuitsPresent',
      }
    case 12:
      return {
        kind: 'CondSuitPair',
        suit: e.suit,
      }
    case 13:
      return {
        kind: 'CondAllHeldSuit',
      }
    case 14:
      return {
        kind: 'CondBlindKind',
        blind: e.blind,
      }
    case 15:
      return {
        kind: 'CondMoney',
        n: e.n,
        compare: e.compare,
      }
    case 16:
      return {
        kind: 'CondDiscardsLeft',
        n: e.n,
        compare: e.compare,
      }
    case 17:
      return {
        kind: 'CondHandsLeft',
        n: e.n,
        compare: e.compare,
      }
    case 18:
      return {
        kind: 'CondDiscardsUnused',
      }
    case 19:
      return {
        kind: 'CondHandRepeated',
      }
    case 20:
      return {
        kind: 'CondFirstHand',
      }
    case 21:
      return {
        kind: 'CondLastHand',
      }
    case 22:
      return {
        kind: 'CondIsMostPlayedHand',
      }
    case 23:
      return {
        kind: 'CondFirstDiscard',
      }
    case 24:
      return {
        kind: 'CondEveryNHands',
        n: e.n,
      }
    case 25:
      return {
        kind: 'CondCounterAtLeast',
        counter: e.counter,
        consume: e.consume,
        n: e.n,
      }
    case 26:
      return {
        kind: 'CondChargeLeft',
      }
    case 27:
      return {
        kind: 'CondTargetMatch',
        target: e.target,
      }
    case 28:
      return {
        kind: 'CondDeckEnhancedAtLeast',
        n: e.n,
      }
    case 29:
      return {
        kind: 'CondBossTriggered',
      }
    case 30:
      return {
        kind: 'CondScoreRatioAtLeast',
        num: e.num,
        den: e.den,
      }
    case 31:
      return {
        kind: 'CondNoFaceScored',
      }
    case 32:
      return {
        kind: 'CondFaceScored',
      }
    case 33:
      return {
        kind: 'CondCounterAtMost',
        counter: e.counter,
        n: e.n,
      }
    case 34:
      return {
        kind: 'CondDiscardedFaceAtLeast',
        n: e.n,
      }
    case 35:
      return {
        kind: 'CondHandContainsRankAndHand',
        hand: e.hand,
      }
    case 36:
      return {
        kind: 'CondFirstHandSingleCard',
      }
    case 37:
      return {
        kind: 'CondFirstHandSingleRank',
      }
    case 38:
      return {
        kind: 'CondFirstDiscardSingleCard',
      }
    case 39:
      return {
        kind: 'CondNotMostPlayedHand',
      }
    case 40:
      return {
        kind: 'CondCardEnhanced',
      }
    case 41:
      return {
        kind: 'CondConsumableKind',
        consumable: e.consumable,
      }
  }

  throw new Error(`Condition: no variant is numbered ${e.type}. This build knows CondAlways, CondHandContains, CondHandIs, CondCardSuit, CondCardRankSet, CondCardIsFace, CondCardEnhancement, CondCardSeal, CondCardEdition, CondCardCount, CondAllSuitsPresent, CondSuitPair, CondAllHeldSuit, CondBlindKind, CondMoney, CondDiscardsLeft, CondHandsLeft, CondDiscardsUnused, CondHandRepeated, CondFirstHand, CondLastHand, CondIsMostPlayedHand, CondFirstDiscard, CondEveryNHands, CondCounterAtLeast, CondChargeLeft, CondTargetMatch, CondDeckEnhancedAtLeast, CondBossTriggered, CondScoreRatioAtLeast, CondNoFaceScored, CondFaceScored, CondCounterAtMost, CondDiscardedFaceAtLeast, CondHandContainsRankAndHand, CondFirstHandSingleCard, CondFirstHandSingleRank, CondFirstDiscardSingleCard, CondNotMostPlayedHand, CondCardEnhanced, CondConsumableKind.`)
}

/** ConditionEntry as the .json export carries it. */
interface ConditionEntryJson {
  type: number
  hand: PokerHandKind
  suit: SuitKind
  enhancement: EnhancementKind
  seal: SealKind
  edition: EditionKind
  blind: BlindKind
  consumable: ConsumableKind
  target: TargetKind
  counter: CounterField
  consume: boolean
  n: number
  compare: Compare
  num: number
  den: number
}

/** One element of JokerEffectRecord.operation. */
export interface OperationEntry {
  /** 무엇을 하는가 */
  type: number
  chips: number
  mult: number
  money: number
  /** 상한. 없으면 비웁니다. */
  cap: number
  unit: UnitKind
  mode: PerUnitMode
  value: number
  baseValue: number
  min: number
  max: number
  times: number
  counter: CounterField
  step: number
  /** 시작값. 비우면 `MultMul` 은 10000 이고 나머지는 0 입니다. */
  init: number
  floor: number
  reset: ResetKind
  handPick: HandPick
  levels: number
  create: CreateKind
  count: number
  cardClass: CardClass
  edition: EditionKind
  /** `JokerRarityCount` 가 어느 희귀도를 세는가. */
  rarity: Rarity
  /** `DeckEnhancementCount` 가 어느 강화를 세는가. */
  enhancement: EnhancementKind
  seal: SealKind
  suit: SuitKind
  modify: ModifyKind
  trait: CardTrait
  debuff: DebuffKind
  pick: JokerPick
  rule: RuleKind
  absolute: boolean
  duration: Duration
  free: boolean
  /** 정하지 않은 성질을 무작위로 채웁니다. */
  random: boolean
  /** 특정 행을 가리킬 때의 식별자. 팩과 바우처와 태그가 씁니다. */
  refId: string
  handler: string
}

function buildOperation(e: OperationEntry): Operation {
  switch (e.type) {
    case 1:
      return {
        kind: 'OpAddChips',
        chips: e.chips,
      }
    case 2:
      return {
        kind: 'OpAddMult',
        mult: e.mult,
      }
    case 3:
      return {
        kind: 'OpMulMult',
        mult: e.mult,
      }
    case 4:
      return {
        kind: 'OpAddMoney',
        money: e.money,
        cap: e.cap,
      }
    case 5:
      return {
        kind: 'OpSetMoney',
        money: e.money,
      }
    case 6:
      return {
        kind: 'OpPerUnit',
        cap: e.cap,
        unit: e.unit,
        mode: e.mode,
        value: e.value,
        baseValue: e.baseValue,
        rarity: e.rarity,
        enhancement: e.enhancement,
      }
    case 7:
      return {
        kind: 'OpRandomRange',
        mode: e.mode,
        min: e.min,
        max: e.max,
      }
    case 8:
      return {
        kind: 'OpRetrigger',
        times: e.times,
      }
    case 9:
      return {
        kind: 'OpGrowSelf',
        cap: e.cap,
        counter: e.counter,
        step: e.step,
        init: e.init,
        floor: e.floor,
        reset: e.reset,
      }
    case 10:
      return {
        kind: 'OpResetSelf',
        counter: e.counter,
      }
    case 11:
      return {
        kind: 'OpGrowOthers',
        counter: e.counter,
        step: e.step,
      }
    case 12:
      return {
        kind: 'OpLevelUpHand',
        handPick: e.handPick,
        levels: e.levels,
      }
    case 13:
      return {
        kind: 'OpCreateCard',
        handPick: e.handPick,
        create: e.create,
        count: e.count,
        edition: e.edition,
        rarity: e.rarity,
        random: e.random,
        refId: e.refId,
      }
    case 14:
      return {
        kind: 'OpAddCard',
        create: e.create,
        count: e.count,
        cardClass: e.cardClass,
        enhancement: e.enhancement,
        seal: e.seal,
        random: e.random,
      }
    case 15:
      return {
        kind: 'OpDestroyCard',
        count: e.count,
      }
    case 16:
      return {
        kind: 'OpModifyCard',
        value: e.value,
        edition: e.edition,
        enhancement: e.enhancement,
        seal: e.seal,
        suit: e.suit,
        modify: e.modify,
        random: e.random,
      }
    case 17:
      return {
        kind: 'OpModifyJoker',
        edition: e.edition,
        random: e.random,
      }
    case 18:
      return {
        kind: 'OpDestroyJoker',
        pick: e.pick,
      }
    case 19:
      return {
        kind: 'OpCopyJoker',
        pick: e.pick,
      }
    case 20:
      return {
        kind: 'OpDebuff',
        suit: e.suit,
        debuff: e.debuff,
      }
    case 21:
      return {
        kind: 'OpDisableBoss',
      }
    case 22:
      return {
        kind: 'OpPreventLoss',
      }
    case 23:
      return {
        kind: 'OpChangeRule',
        value: e.value,
        rule: e.rule,
        absolute: e.absolute,
        duration: e.duration,
      }
    case 24:
      return {
        kind: 'OpChangeRuleByCounter',
        counter: e.counter,
        rule: e.rule,
      }
    case 25:
      return {
        kind: 'OpCardTrait',
        trait: e.trait,
      }
    case 26:
      return {
        kind: 'OpMulMoney',
        cap: e.cap,
        value: e.value,
      }
    case 27:
      return {
        kind: 'OpShopGift',
        create: e.create,
        count: e.count,
        edition: e.edition,
        rarity: e.rarity,
        free: e.free,
      }
    case 28:
      return {
        kind: 'OpDuplicateNextTag',
      }
    case 29:
      return {
        kind: 'OpRerollBoss',
      }
    case 30:
      return {
        kind: 'OpForceDiscard',
        count: e.count,
      }
    case 31:
      return {
        kind: 'OpDrawFaceDown',
        cardClass: e.cardClass,
      }
    case 32:
      return {
        kind: 'OpFlipJokers',
      }
    case 33:
      return {
        kind: 'OpDisableRandomJoker',
      }
    case 34:
      return {
        kind: 'OpGrant',
        create: e.create,
        count: e.count,
        refId: e.refId,
      }
    case 35:
      return {
        kind: 'OpNothing',
      }
    case 36:
      return {
        kind: 'OpCustom',
        handler: e.handler,
      }
  }

  throw new Error(`Operation: no variant is numbered ${e.type}. This build knows OpAddChips, OpAddMult, OpMulMult, OpAddMoney, OpSetMoney, OpPerUnit, OpRandomRange, OpRetrigger, OpGrowSelf, OpResetSelf, OpGrowOthers, OpLevelUpHand, OpCreateCard, OpAddCard, OpDestroyCard, OpModifyCard, OpModifyJoker, OpDestroyJoker, OpCopyJoker, OpDebuff, OpDisableBoss, OpPreventLoss, OpChangeRule, OpChangeRuleByCounter, OpCardTrait, OpMulMoney, OpShopGift, OpDuplicateNextTag, OpRerollBoss, OpForceDiscard, OpDrawFaceDown, OpFlipJokers, OpDisableRandomJoker, OpGrant, OpNothing, OpCustom.`)
}

/** OperationEntry as the .json export carries it. */
interface OperationEntryJson {
  type: number
  chips: number
  mult: number
  money: number
  cap: number
  unit: UnitKind
  mode: PerUnitMode
  value: number
  baseValue: number
  min: number
  max: number
  times: number
  counter: CounterField
  step: number
  init: number
  floor: number
  reset: ResetKind
  handPick: HandPick
  levels: number
  create: CreateKind
  count: number
  cardClass: CardClass
  edition: EditionKind
  rarity: Rarity
  enhancement: EnhancementKind
  seal: SealKind
  suit: SuitKind
  modify: ModifyKind
  trait: CardTrait
  debuff: DebuffKind
  pick: JokerPick
  rule: RuleKind
  absolute: boolean
  duration: Duration
  free: boolean
  random: boolean
  refId: string
  handler: string
}

/** A type for handling rows when parsing .json. */
interface IDataRow {
  owner: string
  order: number
  trigger: Trigger
  chanceNum: number
  chanceDen: number
  firstOnly: boolean
  ranks: RankKind[]
  suits: SuitKind[]
  scope: Scope
  scopeCount: number
  condition: ConditionEntryJson
  operation: OperationEntryJson
}

// Generated from samples/clover/design-data/xlsx/Jokers.xlsx : JokerEffect : A1
/** 조커가 하는 일입니다. 조커 하나가 행 여러 개를 가집니다. */
export class JokerEffectRecord {
  /** Default constructor */
  constructor() {
  }

  /** 이 효과를 가진 것 */
  public get owner(): string { return this._owner_Joker_index }
  public get jokerByOwner(): JokerRecord { return this._owner }

  /** 같은 소유자 안의 순서 */
  public get order(): number { return this._order }

  /** 언제 보는가 */
  public get trigger(): Trigger { return this._trigger }

  /** 확률의 분자 */
  public get chanceNum(): number { return this._chanceNum }
  /** Whether this row has a value for `chanceNum`. */
  public get hasChanceNum(): boolean { return this._chanceNumHasValue }

  /** 확률의 분모 */
  public get chanceDen(): number { return this._chanceDen }
  /** Whether this row has a value for `chanceDen`. */
  public get hasChanceDen(): boolean { return this._chanceDenHasValue }

  /** 조건을 만족한 첫 대상만 */
  public get firstOnly(): boolean { return this._firstOnly }
  /** Whether this row has a value for `firstOnly`. */
  public get hasFirstOnly(): boolean { return this._firstOnlyHasValue }

  /** 어느 랭크들인가 */
  public get ranks(): RankKind[] { return this._ranks }
  /** Whether this row has a value for `ranks`. */
  public get hasRanks(): boolean { return this._ranksHasValue }

  /** 어느 무늬들인가 */
  public get suits(): SuitKind[] { return this._suits }
  /** Whether this row has a value for `suits`. */
  public get hasSuits(): boolean { return this._suitsHasValue }

  /** 누구에게 */
  public get scope(): Scope { return this._scope }

  /** 대상 개수 */
  public get scopeCount(): number { return this._scopeCount }
  /** Whether this row has a value for `scopeCount`. */
  public get hasScopeCount(): boolean { return this._scopeCountHasValue }

  /** 무엇이 참이어야 하는가 */
  public get condition(): Condition {
    return this._condition_value ?? (this._condition_value = buildCondition(this._condition))
  }

  private _condition_value: Condition | null = null

  /** 무엇을 하는가 */
  public get operation(): Operation {
    return this._operation_value ?? (this._operation_value = buildOperation(this._operation))
  }

  private _operation_value: Operation | null = null

  public setReference_owner_INTERNAL(value: JokerRecord) { this._owner = value; }

  public _owner: JokerRecord
  public _owner_Joker_index: string = ""
  public _owner_F: boolean = false
  public _order: number = 0
  public _trigger: Trigger = 0 as Trigger
  public _chanceNum: number = 0
  public _chanceNumHasValue: boolean = false
  public _chanceDen: number = 0
  public _chanceDenHasValue: boolean = false
  public _firstOnly: boolean = false
  public _firstOnlyHasValue: boolean = false
  public _ranks: RankKind[] = []
  public _ranksHasValue: boolean = false
  public _suits: SuitKind[] = []
  public _suitsHasValue: boolean = false
  public _scope: Scope = 0 as Scope
  public _scopeCount: number = 0
  public _scopeCountHasValue: boolean = false
  public _condition: ConditionEntry = { type: 0, hand: 0 as PokerHandKind, suit: 0 as SuitKind, enhancement: 0 as EnhancementKind, seal: 0 as SealKind, edition: 0 as EditionKind, blind: 0 as BlindKind, consumable: 0 as ConsumableKind, target: 0 as TargetKind, counter: 0 as CounterField, consume: false, n: 0, compare: 0 as Compare, num: 0, den: 0 }
  public _operation: OperationEntry = { type: 0, chips: 0, mult: 0, money: 0, cap: 0, unit: 0 as UnitKind, mode: 0 as PerUnitMode, value: 0, baseValue: 0, min: 0, max: 0, times: 0, counter: 0 as CounterField, step: 0, init: 0, floor: 0, reset: 0 as ResetKind, handPick: 0 as HandPick, levels: 0, create: 0 as CreateKind, count: 0, cardClass: 0 as CardClass, edition: 0 as EditionKind, rarity: 0 as Rarity, enhancement: 0 as EnhancementKind, seal: 0 as SealKind, suit: 0 as SuitKind, modify: 0 as ModifyKind, trait: 0 as CardTrait, debuff: 0 as DebuffKind, pick: 0 as JokerPick, rule: 0 as RuleKind, absolute: false, duration: 0 as Duration, free: false, random: false, refId: '', handler: '' }

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._owner_Joker_index = dataRow.owner
    this._order = dataRow.order
    this._trigger = dataRow.trigger
    this._chanceNumHasValue = dataRow.chanceNum !== null && dataRow.chanceNum !== undefined; if (this._chanceNumHasValue) this._chanceNum = dataRow.chanceNum
    this._chanceDenHasValue = dataRow.chanceDen !== null && dataRow.chanceDen !== undefined; if (this._chanceDenHasValue) this._chanceDen = dataRow.chanceDen
    this._firstOnlyHasValue = dataRow.firstOnly !== null && dataRow.firstOnly !== undefined; if (this._firstOnlyHasValue) this._firstOnly = dataRow.firstOnly
    this._ranksHasValue = dataRow.ranks !== null && dataRow.ranks !== undefined; if (this._ranksHasValue) this._ranks = dataRow.ranks
    this._suitsHasValue = dataRow.suits !== null && dataRow.suits !== undefined; if (this._suitsHasValue) this._suits = dataRow.suits
    this._scope = dataRow.scope
    this._scopeCountHasValue = dataRow.scopeCount !== null && dataRow.scopeCount !== undefined; if (this._scopeCountHasValue) this._scopeCount = dataRow.scopeCount
    this._condition = ((e: any) => ({ type: e.type, hand: e.hand, suit: e.suit, enhancement: e.enhancement, seal: e.seal, edition: e.edition, blind: e.blind, consumable: e.consumable, target: e.target, counter: e.counter, consume: e.consume, n: e.n, compare: e.compare, num: e.num, den: e.den }))(dataRow.condition)
    this._operation = ((e: any) => ({ type: e.type, chips: e.chips, mult: e.mult, money: e.money, cap: e.cap, unit: e.unit, mode: e.mode, value: e.value, baseValue: e.baseValue, min: e.min, max: e.max, times: e.times, counter: e.counter, step: e.step, init: e.init, floor: e.floor, reset: e.reset, handPick: e.handPick, levels: e.levels, create: e.create, count: e.count, cardClass: e.cardClass, edition: e.edition, rarity: e.rarity, enhancement: e.enhancement, seal: e.seal, suit: e.suit, modify: e.modify, trait: e.trait, debuff: e.debuff, pick: e.pick, rule: e.rule, absolute: e.absolute, duration: e.duration, free: e.free, random: e.random, refId: e.refId, handler: e.handler }))(dataRow.operation)
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._owner_Joker_index = dataRow[offset++]
    this._order = dataRow[offset++]
    this._trigger = dataRow[offset++]
    const _chanceNum_raw = dataRow[offset++]
    this._chanceNumHasValue = _chanceNum_raw !== null && _chanceNum_raw !== undefined
    if (this._chanceNumHasValue) this._chanceNum = _chanceNum_raw
    const _chanceDen_raw = dataRow[offset++]
    this._chanceDenHasValue = _chanceDen_raw !== null && _chanceDen_raw !== undefined
    if (this._chanceDenHasValue) this._chanceDen = _chanceDen_raw
    const _firstOnly_raw = dataRow[offset++]
    this._firstOnlyHasValue = _firstOnly_raw !== null && _firstOnly_raw !== undefined
    if (this._firstOnlyHasValue) this._firstOnly = _firstOnly_raw
    this._ranks = dataRow[offset++]
    this._suits = dataRow[offset++]
    this._scope = dataRow[offset++]
    const _scopeCount_raw = dataRow[offset++]
    this._scopeCountHasValue = _scopeCount_raw !== null && _scopeCount_raw !== undefined
    if (this._scopeCountHasValue) this._scopeCount = _scopeCount_raw
    this._condition = { type: dataRow[offset++], hand: dataRow[offset++], suit: dataRow[offset++], enhancement: dataRow[offset++], seal: dataRow[offset++], edition: dataRow[offset++], blind: dataRow[offset++], consumable: dataRow[offset++], target: dataRow[offset++], counter: dataRow[offset++], consume: dataRow[offset++], n: dataRow[offset++], compare: dataRow[offset++], num: dataRow[offset++], den: dataRow[offset++] }
    this._operation = { type: dataRow[offset++], chips: dataRow[offset++], mult: dataRow[offset++], money: dataRow[offset++], cap: dataRow[offset++], unit: dataRow[offset++], mode: dataRow[offset++], value: dataRow[offset++], baseValue: dataRow[offset++], min: dataRow[offset++], max: dataRow[offset++], times: dataRow[offset++], counter: dataRow[offset++], step: dataRow[offset++], init: dataRow[offset++], floor: dataRow[offset++], reset: dataRow[offset++], handPick: dataRow[offset++], levels: dataRow[offset++], create: dataRow[offset++], count: dataRow[offset++], cardClass: dataRow[offset++], edition: dataRow[offset++], rarity: dataRow[offset++], enhancement: dataRow[offset++], seal: dataRow[offset++], suit: dataRow[offset++], modify: dataRow[offset++], trait: dataRow[offset++], debuff: dataRow[offset++], pick: dataRow[offset++], rule: dataRow[offset++], absolute: dataRow[offset++], duration: dataRow[offset++], free: dataRow[offset++], random: dataRow[offset++], refId: dataRow[offset++], handler: dataRow[offset++] }
  }
}

// Generated from samples/clover/design-data/xlsx/Jokers.xlsx : JokerEffect : A1
/** 조커가 하는 일입니다. 조커 하나가 행 여러 개를 가집니다. */
export class JokerEffectTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): JokerEffectRecord[] { return this._records }
  private _records: JokerEffectRecord[] = []

  // Indexing by 'Owner and Order'
  private _recordsByOwnerAndOrder: Map<string, JokerEffectRecord> = new Map<string, JokerEffectRecord>()

  /** Joins the columns of the Owner and Order key into the text the map is keyed by. */
  private static keyOfOwnerAndOrder(ownerKey: string, orderKey: number): string {
    const parts: string[] = [
      ownerKey,
      String(orderKey),
    ]

    let text = ""

    for (const part of parts)
      text += `${part.length}:${part}`

    return text
  }

  /**
   * The row with this Owner and Order, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByOwnerAndOrder(ownerKey: string, orderKey: number): JokerEffectRecord | undefined {
    return this._recordsByOwnerAndOrder.get(JokerEffectTable.keyOfOwnerAndOrder(ownerKey, orderKey))
  }

  /**
   * The row with this Owner and Order, or a thrown Error naming what was missing.
   */
  public getByOwnerAndOrderOrThrow(ownerKey: string, orderKey: number): JokerEffectRecord {
    const found = this._recordsByOwnerAndOrder.get(JokerEffectTable.keyOfOwnerAndOrder(ownerKey, orderKey))
    if (!found)
      throw new Error(`There is no record in table "JokerEffect" that corresponds to field "Owner and Order" value (${ownerKey}, ${orderKey})`)

    return found
  }

  /** Whether the table holds a row with this Owner and Order. */
  public containsOwnerAndOrder(ownerKey: string, orderKey: number): boolean {
    return this._recordsByOwnerAndOrder.has(JokerEffectTable.keyOfOwnerAndOrder(ownerKey, orderKey))
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: JokerEffectRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new JokerEffectRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new JokerEffectRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `CloverData.encryptionKey` and `CloverData.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      CloverData.encryptionKey, CloverData.macKey, CloverData.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor
    let presence: Uint8Array | null

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: JokerEffectRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new JokerEffectRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'JokerEffect.Owner', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Owner')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._owner_Joker_index = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'JokerEffect.Order', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Order')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._order = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'JokerEffect.Trigger', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Trigger')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._trigger = value as Trigger
          }
          break
        case 4:
          tabbit.checkColumn(column, 'JokerEffect.ChanceNum', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.ChanceNum')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._chanceNum = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._chanceNumHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._chanceNumHasValue)
              records[i]._chanceNum = 0
          }
          break
        case 5:
          tabbit.checkColumn(column, 'JokerEffect.ChanceDen', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.ChanceDen')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._chanceDen = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._chanceDenHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._chanceDenHasValue)
              records[i]._chanceDen = 0
          }
          break
        case 6:
          tabbit.checkColumn(column, 'JokerEffect.FirstOnly', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_BOOL])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.FirstOnly')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._firstOnly = cursor.nextBool()
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._firstOnlyHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._firstOnlyHasValue)
              records[i]._firstOnly = false
          }
          break
        case 7:
          tabbit.checkColumn(column, 'JokerEffect.Ranks', tabbit.KIND_ARRAY, true, [tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Ranks')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = cursor.nextLength()
            record._ranks = []
            for (let j = 0; j < elementCount; ++j) {
              record._ranks.push(cursor.nextI32() as RankKind)
            }
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._ranksHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._ranksHasValue)
              records[i]._ranks = []
          }
          break
        case 8:
          tabbit.checkColumn(column, 'JokerEffect.Suits', tabbit.KIND_ARRAY, true, [tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Suits')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = cursor.nextLength()
            record._suits = []
            for (let j = 0; j < elementCount; ++j) {
              record._suits.push(cursor.nextI32() as SuitKind)
            }
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._suitsHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._suitsHasValue)
              records[i]._suits = []
          }
          break
        case 9:
          tabbit.checkColumn(column, 'JokerEffect.Scope', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Scope')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._scope = value as Scope
          }
          break
        case 10:
          tabbit.checkColumn(column, 'JokerEffect.ScopeCount', tabbit.KIND_SCALAR, true, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          presence = tabbit.readPresence(reader, column, rowCount)
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.ScopeCount')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._scopeCount = value
          }
          for (let i = 0; i < rowCount; ++i) {
            records[i]._scopeCountHasValue = tabbit.isPresent(presence, i)

            // The block carries a value for every row, so an absent one has just been
            // given whatever was there. Putting the empty value back is what makes this
            // path agree with the JSON one.
            if (!records[i]._scopeCountHasValue)
              records[i]._scopeCount = 0
          }
          break
        case 11:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Type', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Type')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.type = value
          }
          break
        case 12:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Hand', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Hand')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.hand = value as PokerHandKind
          }
          break
        case 13:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Suit', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Suit')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.suit = value as SuitKind
          }
          break
        case 14:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Enhancement', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Enhancement')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.enhancement = value as EnhancementKind
          }
          break
        case 15:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Seal', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Seal')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.seal = value as SealKind
          }
          break
        case 16:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Edition', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Edition')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.edition = value as EditionKind
          }
          break
        case 17:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Blind', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Blind')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.blind = value as BlindKind
          }
          break
        case 18:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Consumable', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Consumable')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.consumable = value as ConsumableKind
          }
          break
        case 19:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Target', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Target')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.target = value as TargetKind
          }
          break
        case 20:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Counter', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Counter')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.counter = value as CounterField
          }
          break
        case 21:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Consume', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_BOOL])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Consume')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._condition.consume = cursor.nextBool()
          }
          break
        case 22:
          tabbit.checkColumn(column, 'JokerEffect.Condition.N', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.N')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.n = value
          }
          break
        case 23:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Compare', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Compare')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.compare = value as Compare
          }
          break
        case 24:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Num', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Num')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.num = value
          }
          break
        case 25:
          tabbit.checkColumn(column, 'JokerEffect.Condition.Den', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Condition.Den')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._condition.den = value
          }
          break
        case 26:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Type', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Type')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.type = value
          }
          break
        case 27:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Chips', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Chips')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.chips = value
          }
          break
        case 28:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Mult', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Mult')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.mult = value
          }
          break
        case 29:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Money', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Money')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.money = value
          }
          break
        case 30:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Cap', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Cap')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.cap = value
          }
          break
        case 31:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Unit', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Unit')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.unit = value as UnitKind
          }
          break
        case 32:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Mode', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Mode')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.mode = value as PerUnitMode
          }
          break
        case 33:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Value', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Value')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.value = value
          }
          break
        case 34:
          tabbit.checkColumn(column, 'JokerEffect.Operation.BaseValue', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.BaseValue')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.baseValue = value
          }
          break
        case 35:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Min', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Min')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.min = value
          }
          break
        case 36:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Max', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Max')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.max = value
          }
          break
        case 37:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Times', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Times')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.times = value
          }
          break
        case 38:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Counter', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Counter')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.counter = value as CounterField
          }
          break
        case 39:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Step', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Step')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.step = value
          }
          break
        case 40:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Init', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Init')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.init = value
          }
          break
        case 41:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Floor', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Floor')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.floor = value
          }
          break
        case 42:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Reset', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Reset')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.reset = value as ResetKind
          }
          break
        case 43:
          tabbit.checkColumn(column, 'JokerEffect.Operation.HandPick', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.HandPick')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.handPick = value as HandPick
          }
          break
        case 44:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Levels', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Levels')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.levels = value
          }
          break
        case 45:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Create', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Create')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.create = value as CreateKind
          }
          break
        case 46:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Count', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Count')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.count = value
          }
          break
        case 47:
          tabbit.checkColumn(column, 'JokerEffect.Operation.CardClass', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.CardClass')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.cardClass = value as CardClass
          }
          break
        case 48:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Edition', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Edition')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.edition = value as EditionKind
          }
          break
        case 49:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Rarity', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Rarity')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.rarity = value as Rarity
          }
          break
        case 50:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Enhancement', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Enhancement')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.enhancement = value as EnhancementKind
          }
          break
        case 51:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Seal', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Seal')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.seal = value as SealKind
          }
          break
        case 52:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Suit', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Suit')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.suit = value as SuitKind
          }
          break
        case 53:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Modify', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Modify')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.modify = value as ModifyKind
          }
          break
        case 54:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Trait', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Trait')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.trait = value as CardTrait
          }
          break
        case 55:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Debuff', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Debuff')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.debuff = value as DebuffKind
          }
          break
        case 56:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Pick', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Pick')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.pick = value as JokerPick
          }
          break
        case 57:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Rule', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Rule')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.rule = value as RuleKind
          }
          break
        case 58:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Absolute', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_BOOL])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Absolute')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._operation.absolute = cursor.nextBool()
          }
          break
        case 59:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Duration', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Duration')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.duration = value as Duration
          }
          break
        case 60:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Free', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_BOOL])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Free')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._operation.free = cursor.nextBool()
          }
          break
        case 61:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Random', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_BOOL])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Random')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._operation.random = cursor.nextBool()
          }
          break
        case 62:
          tabbit.checkColumn(column, 'JokerEffect.Operation.RefId', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.RefId')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.refId = value
          }
          break
        case 63:
          tabbit.checkColumn(column, 'JokerEffect.Operation.Handler', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'JokerEffect.Operation.Handler')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._operation.handler = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: JokerEffectRecord[]): void {
    const recordsByOwnerAndOrder = new Map<string, JokerEffectRecord>()

    for (const record of records) {
      recordsByOwnerAndOrder.set(JokerEffectTable.keyOfOwnerAndOrder(record.owner, record.order), record)
    }

    this._records = records
    this._recordsByOwnerAndOrder = recordsByOwnerAndOrder
  }
}
