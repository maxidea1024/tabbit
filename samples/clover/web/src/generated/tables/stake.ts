// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { CloverData } from '../clover-data'

// Automatically import to handle external type references.
import { StakeKind } from '../enums/stake-kind'
import { StickerKind } from '../enums/sticker-kind'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  stake: StakeKind
  name: string
  anteColumn: number
  smallBlindReward: number
  discardsDelta: number
  sticker: StickerKind
  stickerPercent: number
}

// Generated from samples/clover/design-data/xlsx/Setup.xlsx : Stake : A1
/** 난이도입니다. **누적이므로 뒤의 것은 앞의 것을 전부 포함합니다** — 이 표의 값은 그 스테이크에서의 최종값입니다. */
export class StakeRecord {
  /** Default constructor */
  constructor() {
  }

  /** 스테이크 */
  public get stake(): StakeKind { return this._stake }

  /** 표시 이름 */
  public get name(): string { return this._name }

  /** `Ante` 의 어느 열을 읽는가 */
  public get anteColumn(): number { return this._anteColumn }

  /** 스몰 블라인드 격파 보상 */
  public get smallBlindReward(): number { return this._smallBlindReward }

  /** 버리기 증감 */
  public get discardsDelta(): number { return this._discardsDelta }

  /** 조커에 붙는 스티커 */
  public get sticker(): StickerKind { return this._sticker }

  /** 스티커가 붙을 확률 */
  public get stickerPercent(): number { return this._stickerPercent }

  public _stake: StakeKind = 0 as StakeKind
  public _name: string = ''
  public _anteColumn: number = 0
  public _smallBlindReward: number = 0
  public _discardsDelta: number = 0
  public _sticker: StickerKind = 0 as StickerKind
  public _stickerPercent: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._stake = dataRow.stake
    this._name = dataRow.name
    this._anteColumn = dataRow.anteColumn
    this._smallBlindReward = dataRow.smallBlindReward
    this._discardsDelta = dataRow.discardsDelta
    this._sticker = dataRow.sticker
    this._stickerPercent = dataRow.stickerPercent
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._stake = dataRow[offset++]
    this._name = dataRow[offset++]
    this._anteColumn = dataRow[offset++]
    this._smallBlindReward = dataRow[offset++]
    this._discardsDelta = dataRow[offset++]
    this._sticker = dataRow[offset++]
    this._stickerPercent = dataRow[offset++]
  }
}

// Generated from samples/clover/design-data/xlsx/Setup.xlsx : Stake : A1
/** 난이도입니다. **누적이므로 뒤의 것은 앞의 것을 전부 포함합니다** — 이 표의 값은 그 스테이크에서의 최종값입니다. */
export class StakeTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): StakeRecord[] { return this._records }
  private _records: StakeRecord[] = []

  /** How many rows the table holds. */
  public get count(): number { return this._records.length }

  /**
   * The rows, in the order the file wrote them.
   *
   * The array reference is read once, here. A refresh replaces the reference rather
   * than its contents, so a loop already running keeps the rows it started with.
   */
  public [Symbol.iterator](): IterableIterator<StakeRecord> {
    return this._records[Symbol.iterator]()
  }

  // Indexing by 'stake'
  public get recordsByStake(): Map<StakeKind, StakeRecord> { return this._recordsByStake }
  private _recordsByStake: Map<StakeKind, StakeRecord> = new Map<StakeKind, StakeRecord>()

  /**
   * The row with this stake, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByStake(key: StakeKind): StakeRecord | undefined {
    return this._recordsByStake.get(key)
  }

  /**
   * The row with this stake, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByStake(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByStakeOrThrow(key: StakeKind): StakeRecord {
    const found = this._recordsByStake.get(key)
    if (!found)
      throw new Error(`There is no record in table "Stake" that corresponds to field "stake" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this stake. */
  public containsStake(key: StakeKind): boolean {
    return this._recordsByStake.has(key)
  }


  /**
   * Each row with the stake it is keyed by -
   * `for (const [key, row] of table.entries())`.
   *
   * What this saves a caller is not the key value - the row carries it - but having to know
   * which column the key is: stake here, something else in the next table.
   *
   * The rows come in the order the file wrote them rather than the order the map holds
   * them, which makes this and iterating the table agree. Only the primary key has this: a
   * table keyed by several columns together has no single key value to pair a row with.
   */
  public *entries(): IterableIterator<[StakeKind, StakeRecord]> {
    for (const record of this._records) {
      yield [record.stake, record]
    }
  }

  /**
   * Read a table from the text of its JSON export.
   *
   * The text, not a path: where it comes from - a file, a fetch, an asset bundle - is the
   * caller's, so this module names no runtime module and loads wherever TypeScript runs.
   * Either row shape the exporter writes is accepted.
   */
  public readJsonFrom(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: StakeRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new StakeRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new StakeRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /**
   * Read a table from the bytes of its binary export.
   *
   * The bytes as the file holds them; the envelope is opened here. Unconditionally, and with
   * whatever `CloverData.encryptionKey` and `CloverData.macKey` hold - which is null
   * unless the project set them. A file that is neither encrypted nor signed comes back from
   * that untouched, so the load path is the same either way and there is no condition here
   * that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
   *
   * Then column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(fileBytes: Uint8Array): void {
    const data = tabbit.open(
      fileBytes, CloverData.encryptionKey, CloverData.macKey, CloverData.verifyMac)
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: StakeRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new StakeRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'Stake.Stake', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Stake.Stake')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._stake = value as StakeKind
          }
          break
        case 2:
          tabbit.checkColumn(column, 'Stake.Name', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_STRING])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Stake.Name')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameString(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._name = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'Stake.AnteColumn', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Stake.AnteColumn')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._anteColumn = value
          }
          break
        case 4:
          tabbit.checkColumn(column, 'Stake.SmallBlindReward', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Stake.SmallBlindReward')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._smallBlindReward = value
          }
          break
        case 5:
          tabbit.checkColumn(column, 'Stake.DiscardsDelta', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Stake.DiscardsDelta')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._discardsDelta = value
          }
          break
        case 6:
          tabbit.checkColumn(column, 'Stake.Sticker', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Stake.Sticker')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._sticker = value as StickerKind
          }
          break
        case 7:
          tabbit.checkColumn(column, 'Stake.StickerPercent', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'Stake.StickerPercent')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._stickerPercent = value
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: StakeRecord[]): void {
    const recordsByStake = new Map<StakeKind, StakeRecord>()

    for (const record of records) {
      recordsByStake.set(record.stake, record)
    }

    this._records = records
    this._recordsByStake = recordsByStake
  }
}
