// 화면의 자리와 시간.
//
// **여기 있는 것은 전부 수치입니다.** 무엇을 어디에 놓고 얼마나 오래 보이는가이고,
// 규칙은 하나도 없습니다. 판의 크기가 1280 × 800 하나로 고정되어 있으므로 좌표를
// 그대로 적습니다 — `layout` 이 창에 맞춰 전체를 배율로 맞춥니다.
//
// **검증 도구도 이 값을 씁니다.** `tools/harness.ts` 가 좌표를 손으로 다시 적어 두고
// 있었고, 배치를 고친 날부터 빈자리를 누르고 통과했습니다.

import { SIDE_PANEL, SIZE } from '../render/theme'
import { box, splitX, type Box } from '../ui/layout'
import { PANEL_BOTTOM } from '../ui/modal'

// 화면의 자리. **원작의 배치를 따릅니다** — 왼쪽에 판돈과 점수가 세로로 쌓이고, 위에 조커와
// 소모품이 나란히 서고, 가운데에서 카드를 내고, 아래에 패가 부챗살로 펴집니다.
export const LEFT = SIDE_PANEL.x

export const PANEL_W = SIDE_PANEL.width

/**
 * 왼쪽 판의 오른쪽 칸이 시작하는 자리.
 *
 * **두 칸이 판을 꽉 채웁니다.** 칸 하나가 124이고 사이가 16이면 둘이 264 — 판의 너비
 * 그대로입니다. 예전에는 오른쪽 칸을 `LEFT + 134` 에 두었고, 그러면 줄이 6픽셀 일찍
 * 끝나서 **오른쪽에만 여백이 남았습니다.**
 */
/** 판 안쪽의 여백. 칸과 자리는 이 안에 놓이고, 머리 판과 요구 점수는 판의 폭을 다 씁니다. */
export const IN_X = LEFT + 16
export const IN_W = PANEL_W - 32
/** 2×2 칸의 오른쪽 열. */
export const RIGHT_COL = IN_X + IN_W / 2
/** 칸 하나의 크기. 2×2 가 안쪽 폭을 다 씁니다. */
export const CELL_SLOT_W = IN_W / 2
export const CELL_SLOT_H = 48
/** 판 아래 단추 둘의 사이. */
export const PANEL_BTN_GAP = 8

/** 판이 놓이는 자리의 가운데. 왼쪽 패널을 뺀 나머지의 한가운데입니다. */
export const BOARD_X = (LEFT + PANEL_W + 20 + SIZE.width) / 2

/**
 * 판 위에 뜨는 것들의 가운데. **화면의 한가운데입니다.**
 *
 * 판의 한가운데에 두면 왼쪽 패널만큼 오른쪽으로 밀린 자리에 서고, 같은 화면에 뜨는
 * 정산·옵션·게임 방법은 화면 한가운데에 서므로 뜰 때마다 자리가 달라집니다.
 *
 * **넓이를 아는 자리는 `popupCenter(width)` 를 씁니다** — 가운데에 두었을 때 왼쪽 판을
 * 침범하는 판은 그만큼 오른쪽으로 밀어야 하고, 그 판정에는 넓이가 필요합니다.
 */
export const POPUP_X = SIZE.width / 2

/** 상점의 것들이 하나씩 서는 간격. */
/**
 * 세는 소리가 나는 가장 짧은 간격.
 *
 * **숫자가 바뀌는 것과 소리가 나는 것이 같은 일이 아닙니다.** 프레임마다 값이 바뀌는
 * 구간에서 그 둘을 묶으면 초당 예순 번이 됩니다.
 */
export const RANK_TICK = 0.055

export const REVEAL_STEP = 0.13

/** 하나가 다 서는 데 걸리는 시간. */
export const REVEAL_SPAN = 0.28

/** 설 때 아래에서 올라오는 거리. */
export const REVEAL_RISE = 14

/** 정산의 줄이 하나씩 서는 간격. */
export const PAYOUT_STEP = 0.16

/**
 * 판이 열리고 첫 줄이 서기까지.
 *
 * **그 사이에 「정산 중」 이 뜹니다.** 0.12초는 그 글을 읽을 수 없고, 그보다 길면 기다리는
 * 것이 됩니다.
 */
export const PAYOUT_WAIT = 0.52

/**
 * 합계가 `$` 낱개로 쌓이는 것.
 *
 * **받는 것은 돈이므로 낱개로 보입니다.** 수가 먼저 서면 그것은 셈의 결과이고, 낱개가
 * 쌓였다가 하나로 뭉치면 그 수가 방금 받은 그 돈이 됩니다.
 *
 * `COIN_MAX` 는 한 줄에 세우는 낱개의 한도입니다 — 그보다 많이 받는 판에서는 낱개가
 * 「많다」 만 알리고 정확한 수는 뭉친 뒤의 글이 알립니다.
 */
export const COIN_MAX = 28

export const COIN_STEP = 15

export const COIN_MERGE = 0.34

/** 받을 때 뭉쳐 있던 낱개가 다시 펼쳐지는 데 걸리는 시간. */
export const COIN_SPREAD = 0.22

/** 펼친 낱개가 하나씩 사라지는 간격. */
export const COIN_LAUNCH = 0.06

/**
 * 정산에서 뜨는 동전의 수 상한.
 *
 * **낱개는 28까지이지만 동전은 열둘까지입니다.** 그보다 많이 날면 하나씩 꽂히는 소리가
 * 뜻을 잃고(음이 순번마다 오릅니다) 금액 칸 앞이 동전으로 덮입니다 — `Coins.shares` 가
 * 한 자리에서 날릴 때 쓰는 상한과 같은 값입니다.
 */
export const COIN_FLIGHTS = 12

/**
 * 상점의 바닥이 서는 자리.
 *
 * **바닥이 고정이고 윗변이 움직입니다.** 다 사서 줄이 없어지면 판이 아래에서 줄어들지
 * 않고 위에서 줄어듭니다 — 단추가 있는 바닥이 움직이면 같은 단추를 누를 자리가 살 때마다
 * 달라집니다. 세 줄이 다 있을 때의 높이가 586 이므로 그때 윗변이 `y` 200 에 서고, 그것이
 * 조커와 소모품 줄(칸 수를 적은 글이 `y` 193 에서 끝납니다)의 바로 아래입니다.
 */
export const SHOP_BOTTOM = PANEL_BOTTOM

/** 상점 판이 아래에서 올라와 서는 데 걸리는 시간. 줄은 그 뒤부터 채워집니다. */
export const SHOP_RISE = 0.34

/**
 * 블라인드 고르기의 칸 셋이 올라오는 거리.
 *
 * **떠 있는 판보다 멉니다.** 판은 덮개가 짙어지는 것과 함께 서므로 58픽셀로도 「올라왔다」로
 * 읽히는데, 이 칸 셋은 덮개 없이 판 위에 그대로 서므로 그만큼으로는 자리에서 살짝 흔든
 * 것으로만 보입니다.
 */
export const BLIND_RISE = 58

/**
 * 머리띠와 첫 줄 사이.
 *
 * **6픽셀은 붙어 있는 것입니다.** 머리띠가 46픽셀이고 그 밑에 곧바로 단추가 서면 제목과
 * 단추가 한 덩어리로 보여서, 머리띠가 판의 머리가 아니라 첫 줄의 배경이 됩니다.
 */
export const MENU_PAD = 18

/**
 * 펼친 카드의 크기.
 *
 * **손패와 같습니다.** 팩에서 고르는 것은 판 위에서 손패를 한 번 더 치는 것과 같은
 * 몸짓이라, 손패 줄에 손패 크기로 깔립니다 — 1.55배로 키워 화면 가운데에 세우던 것은
 * 상점과 다른 화면 하나를 더 두는 것이었습니다.
 */
export const PACK_SCALE = 1

export const PACK_CARD_W = SIZE.jokerWidth * PACK_SCALE

export const PACK_CARD_H = SIZE.jokerHeight * PACK_SCALE

/** 펼친 팩에서 고른 한 장이 올라오는 거리. */
export const PACK_HOLD_RISE = 22

/** 산 것이 제자리에 닿기까지. 용수철이 그만큼에 잦아듭니다. */
/**
 * 산 것이 자리에 닿는 데까지.
 *
 * **오는 길이 보여야 합니다.** 사는 순간과 닿는 순간이 겹쳐 보이면 「울렁이다 · 오다 ·
 * 닿다」 셋 중 가운데가 없어집니다. 용수철의 빠르기(`Motion.drift`)와 같이 정해지는
 * 값이므로, 이것을 고치면 그쪽도 같은 비율로 고칩니다.
 *
 * **0.52초였습니다.** 사고 나서 물건이 칸에 들어가기까지가 그 앞의 박자(`BUY_LINGER`)와
 * 합쳐 1.67초였고, 상점에서 여러 개를 살 때 그 기다림이 사는 재미보다 길었습니다.
 */
export const LAND_AT = 0.26

/**
 * 바꿔 집을 때 파는 값이 뜨기까지.
 *
 * **덮개가 걷힌 뒤입니다.** 팩을 뜯은 채로 바꿔 집으면 파는 값이 그 덮개 뒤에서 뜹니다 —
 * 그리고 새 물건의 이름은 `LAND_AT` 이므로, 파는 것과 오는 것이 그 차례로 읽힙니다.
 */
export const SELL_WAIT = 0.24

/**
 * 값을 치르는 것을 보는 시간. 산 딱지가 그 자리에 남고, 그다음 일이 이 뒤에 옵니다.
 *
 * **값을 치르는 것은 그냥 지나가는 것이 아니라 하나의 사건입니다.** 값이 뜨고 · 동전이
 * 날아가 지갑에 꽂히고 · 잔액이 다 세어지는 것을 보고 난 뒤에 물건이 떠나거나 상점이
 * 내려갑니다 — 그 위에 다음 일을 얹으면 얼마를 냈는지가 지나가는 빛으로만 남습니다.
 * 동전은 마지막 것이 0.6초 뒤에 떠나 0.6초를 날므로 1.2초 안에 다 꽂힙니다.
 */
export const PAY_BEAT = 1.15

/**
 * 산 딱지가 그 자리에 남는 시간.
 *
 * **값을 치르는 박자의 절반입니다.** 동전은 그 뒤로도 지갑을 향해 날고 있고, 딱지는 값이
 * 그 위에 뜬 것을 보여 주는 몫만 하면 됩니다 — 박자 그대로(1.15초) 두었더니 누른 다음
 * 물건이 떠나기까지가 멈춰 있는 시간이 되었고, 그 뒤의 비행(`LAND_AT`)까지 합쳐 1.67초를
 * 기다렸습니다. 팩과 바우처의 딱지는 그대로 `PAY_BEAT` 입니다 — 그 둘은 그 자리에서
 * 뜯거나 규칙으로 들어가므로 기다리는 것이 곧 그 사건입니다.
 */
export const BUY_LINGER = PAY_BEAT / 2

/**
 * 조커와 소모품이 나란히 놓이는 줄의 가운데 높이.
 *
 * **자리의 윗변이 왼쪽 판의 윗변과 같습니다.** 위쪽에 셋이 놓여 있는데 그중 둘만 18픽셀
 * 내려와 있으면 그 셋이 한 줄로 읽히지 않습니다 — 가운데 높이가 아니라 윗변을 맞추는
 * 것이고, 그래서 이 값은 `판의 윗변 + 자리의 절반` 입니다.
 */
/**
 * 조커·소모품 줄의 가운데.
 *
 * **32픽셀에서 시작합니다.** 22였습니다 — 그 자리에서는 딱지와 단추가 함께 쓸 수 있는 것이
 * 152픽셀뿐이라, 상점의 칸과 같은 단추(32)를 세우면 고른 딱지의 윗변이 화면 밖으로
 * 나갔습니다. 10을 내리면 딱지 124 · 사이 4 · 단추 32 가 들어가고 고른 딱지의 윗변이 2에
 * 놓입니다 — **늘 조금씩 흔들리는 폭(±1.8)까지 화면 안입니다.**
 *
 * 아래로는 자리의 아랫변이 168, 칸 수를 적은 글이 174, 상점 판의 윗변이 200입니다.
 */
export const JOKER_Y = 32 + (SIZE.jokerHeight + 6 * 2) / 2

/**
 * 조커와 소모품의 자리.
 *
 * **칸을 하나씩 그리지 않습니다.** 칸 수는 규칙이 정하므로(덱·바우처·챌린지가 늘리고
 * 줄입니다) 칸마다 사각형을 그리면 줄의 너비가 규칙에 따라 달라집니다 — 조커가 8칸이면
 * 그 줄이 소모품 줄과 겹치고, 그것을 막을 것이 아무것도 없습니다.
 *
 * **그래서 자리는 고정된 사각형 둘이고, 몇 개든 그 안에서 배치됩니다.** 넘칠 만큼
 * 많아지면 서로 겹쳐 서고, 자리 밖으로는 나가지 않습니다.
 */
export const TRAY_LEFT = LEFT + PANEL_W + 20

export const TRAY_RIGHT = SIZE.width - LEFT

/** 두 자리 사이. */
export const TRAY_GAP = 20

/** 자리 안쪽의 좌우 여백. 카드가 테두리에 닿지 않을 만큼입니다. */
export const TRAY_PAD_X = 10

/**
 * 자리의 위아래 여백.
 *
 * **6픽셀입니다.** 자리의 아랫변이 176 이어야 하고, 상점 판의 윗변이 200 입니다 — 여백을
 * 더 두면 그 둘 사이가 없어집니다. 위로는 칸 수를 적은 글이 서므로 같은 값입니다.
 */
export const TRAY_PAD_Y = 6

/**
 * 두 자리의 너비 비율.
 *
 * **기본 칸 수입니다**(조커 5 · 소모품 2). 규칙이 칸을 늘려도 자리의 너비는 그대로이고,
 * 늘어난 것은 그 안에서 좁게 놓입니다 — 자리가 칸 수를 따라가면 고치려던 것이 그대로
 * 남습니다.
 */
export const TRAY_SHARE = [5, 2] as const

/** 카드 하나와 그 옆의 것 사이. 자리가 넉넉할 때의 간격입니다. */
export const SLOT_STEP = SIZE.jokerWidth + 12

export const TRAY_H = SIZE.jokerHeight + TRAY_PAD_Y * 2
export const [JOKER_TRAY, CONSUMABLE_TRAY] = splitX(
  box(TRAY_LEFT, JOKER_Y - TRAY_H / 2, TRAY_RIGHT - TRAY_LEFT, TRAY_H),
  TRAY_SHARE, TRAY_GAP)

/**
 * 자리 안에 `count` 개를 늘어놓습니다. 첫 장의 가운데와 그다음까지의 간격입니다.
 *
 * **넘치면 겹칩니다.** 자리에 맞는 간격까지 좁히고 그 뒤로는 서로 겹쳐 서므로, 몇 개가
 * 되어도 자리 밖으로 나가지 않습니다 — 손패가 같은 규칙입니다.
 *
 * **가운데에 모입니다.** 왼쪽부터 채우면 자리의 오른쪽이 늘 비어 보이고, 그 빈 자리가
 * 자리의 테두리와 함께 「아직 못 채운 칸」으로 읽힙니다.
 */
export function trayRow(tray: Box, count: number): { startX: number; spacing: number } {
  const room = tray.width - TRAY_PAD_X * 2
  const spacing = count > 1
    ? Math.min(SLOT_STEP, (room - SIZE.jokerWidth) / (count - 1))
    : SLOT_STEP
  // **왼쪽부터 채웁니다.** 가운데에 모으면 하나 얻을 때마다 이미 있던 것들이 옆으로
  // 밀립니다 — 조커는 왼쪽부터 차례로 발동하므로 그 순서가 자리의 순서여야 하고, 첫 칸이
  // 언제나 같은 자리에 있어야 몇 번째 것을 누르는지가 손에 익습니다.
  return { startX: tray.x + TRAY_PAD_X + SIZE.jokerWidth / 2, spacing }
}

/** 고른 것 아래의 단추 줄이 화면 끝에서 남기는 여백. */
export const HELD_EDGE = 10

/**
 * 고른 것 밑에 서는 단추의 높이.
 *
 * **단추의 아랫변이 그 물건이 서던 자리의 아랫변입니다.** 물건은 그만큼 위로 밀려
 * 올라가고, 단추는 바닥에 그대로 있습니다 — 고를 때마다 단추가 다른 높이에 서면 두 번째
 * 누름이 매번 다른 자리입니다.
 */
export const HELD_H = 36

/**
 * 고른 상점 칸이 밀려 올라가는 거리.
 *
 * **단추가 값보다 높은 만큼입니다.** 단추는 값이 있던 자리를 그대로 대신하므로, 그 차이만큼만
 * 물건이 비켜서면 됩니다.
 */
export const SHOP_LIFT = 14

/**
 * 딱지와 그 밑에 서는 단추 사이.
 *
 * **4픽셀입니다. 상점의 칸과 같습니다.**
 *
 * 줄에서는 2였습니다 — 그 자리에서는 딱지와 단추가 함께 쓸 수 있는 것이 152픽셀뿐이었고,
 * 4로 두면 고른 딱지의 윗변이 화면 밖으로 나갔습니다. 줄을 10픽셀 내려(`JOKER_Y`) 그
 * 자리를 만들었습니다 — **한 화면에 두 가지 높이의 단추가 서는 것보다 줄이 10픽셀 내려가는
 * 쪽이 낫습니다.**
 */
export const ROW_GAP = 4

/**
 * 줄에서 고른 조커 · 소모품이 밀려 올라가는 거리.
 *
 * **단추가 그 자리에 들어갈 만큼입니다.** 딱지는 위로 그만큼 비켜서고 단추의 아랫변이
 * 딱지가 놓이던 자리의 아랫변에 그대로 놓입니다 — 아래로 내려가는 것이 하나도 없습니다.
 * 상점의 칸과 같은 규칙이고, 다른 것은 여기에 칸의 테두리가 없다는 것뿐입니다.
 *
 * **12픽셀이었습니다.** 단추는 자리의 아랫변 아래에 세우고 딱지는 12만 들려서, 단추 줄이
 * 딱지가 서던 자리보다 18픽셀 아래로 내려가 있었고 딱지와 단추 사이는 22픽셀 벌어져
 * 있었습니다.
 *
 * **커서를 올려도 더 들리지 않습니다.** 고른 딱지는 이미 올라가 있고, 여기에 커서의
 * 10픽셀과 1.1배가 더 얹히면 윗변이 화면 밖으로 나갑니다 — `JokerView.held` 가 그것을
 * 막습니다.
 */
export const HELD_RISE = HELD_H + ROW_GAP

export const PLAY_Y = 366

/**
 * 딜러의 자리. 화면 오른쪽 위 밖입니다.
 *
 * **나간 카드는 여기로 가고, 돌아오는 카드는 여기서 옵니다.** 그래야 「딜러가 거둔 카드가
 * 덱으로 돌아온다」 로 읽힙니다. 덱(오른쪽 아래)과 같은 높이로 빠지면 나가는 것과 돌아오는
 * 것이 같은 자리에서 엇갈립니다.
 */
export const DEALER = { x: SIZE.width + 150, y: PLAY_Y - 190 }

/**
 * 마지막 카드가 덱에 닿고 나서 덱이 그 자리에 남는 시간.
 *
 * **닿자마자 빠지면 마무리가 덜 된 것으로 보입니다.** 돌아온 카드가 덱에 쌓인 것을
 * 보고 나서 덱이 물러나야 한 판이 닫힌 것이 됩니다 — 카드가 들어가는 것과 덱이 나가는
 * 것이 같은 순간이면 마지막 한 장이 어디로 갔는지가 남지 않습니다.
 */
export const DECK_LINGER = 0.5

/** 회수하는 카드가 한 장씩 떠나는 간격. */
export const RECALL_STEP = 0.018

/**
 * 내보낸 카드 한 장이 화면을 떠나기까지.
 *
 * **예약이 빈 것과 나가는 것이 끝난 것이 다르므로** 그 차이를 이 값으로 셉니다. 이 뒤에
 * 걷는 소리가 맺히고, 회수 소리는 그것을 기다립니다 — 그래서 짧습니다.
 */
export const RETIRE_TAIL = 0.18

/**
 * 카드를 다 거둔 뒤 정산이 서기까지의 한 박자.
 *
 * **끝난 것을 보는 시간입니다.** 마지막 한 장이 덱에 닿는 그 프레임에 판이 올라오면 거둔
 * 것과 다음 걸음이 한 동작으로 붙습니다.
 */
export const SWEEP_REST = 0.5

/**
 * 쓴 소모품의 네 마디.
 *
 * **눈이 따라갈 수 있는 길이여야 합니다.** 넷을 합쳐 1.5초 남짓이고, 그 사이에 판이 멈추지는
 * 않습니다 — 코어는 이미 처리했고 이것은 그 결과를 보이는 몫입니다.
 */
export const ITEM_WARP = 0.30

export const ITEM_ARRIVE = 0.62

export const ITEM_FLASH = 0.34

/**
 * 닿은 자리에서 떠는 것.
 *
 * **썼다는 몸짓입니다.** 닿자마자 타 없어지면 카드가 그냥 지워진 것으로 보입니다 — 조커가
 * 발동할 때 떠는 것과 같은 몸짓이고, 그것보다 짧습니다. 쓰는 것은 한 번뿐이므로 눈에
 * 남으면 되고, 길게 떨면 그다음의 정산이 그만큼 늦습니다.
 */
export const ITEM_SHAKE = 0.26

/** 떠는 크기. 도(°)입니다. */
export const ITEM_SHAKE_TILT = 7

export const ITEM_HOLD = 1.05

/** 상점의 팝 딱지 하나의 너버. **상점 카드의 158 과 다릅니다.** */
/**
 * 상점의 물건 칸.
 *
 * **물건 하나가 칸 하나입니다.** 카드(88 × 124)가 안에 서고 그 아래 한 줄이 값입니다. 팔린
 * 자리는 칸이 비어 남습니다 — 판이 줄어들지 않습니다.
 */
export const CELL_W = 104

export const CELL_H = 166

export const CELL_GAP = 12

/** 상품 · 팩 · 바우처 무리 사이. */
export const GROUP_GAP = 20

/** 물건이 칸에 내려와 앉는 거리. 위에서 옵니다 — 진열하는 손이 위에서 내려놓는 것입니다. */
export const STOCK_DROP = 22

/** 놓아 둔 것의 칩 크기. 카드의 오른쪽 위 모서리에 걸칩니다. */
export const GIFT_CHIP = 26

/**
 * 꾸욱 누르고 있으면 설명이 뜨는 데까지.
 *
 * **마우스에는 「올린다」가 있고 손가락에는 없습니다.** 손가락은 누르거나 안 누르거나
 * 둘뿐이라, 올리는 것으로 뜨는 설명은 손가락으로는 뜨게 할 방법이 없습니다 — 누르고
 * 기다리는 것이 그 자리를 대신합니다.
 *
 * 0.34초는 짧아서 고르려고 누른 것에도 뜨고, 0.7초는 길어서 뜨기 전에 손을 뗍니다.
 */
/**
 * 들린 카드가 제자리로 내려오는 데까지.
 *
 * **용수철의 시간입니다.** 내려오는 용수철이 8픽셀을 내려오는 데 그만큼 걸립니다 — 이보다
 * 짧으면 내려오는 도중에 나가고, 길면 다 내려온 카드가 가만히 기다립니다. 내려오는 것을
 * 두 배 빠르게 했으므로 이것도 절반입니다.
 */
export const ITEM_SETTLE = 0.11

/**
 * 다 내려온 카드가 나가기 전에 머무는 동안.
 *
 * **내려오자마자 나가면 내려온 것이 보이지 않습니다.** `ITEM_SETTLE` 이 내려오는 용수철의
 * 시간 그대로라 카드가 8픽셀을 내려온 그 프레임에 오른쪽으로 빠졌고, 그러면 올라갔다
 * 내려온 한 몸짓이 「올라갔다가 사라졌다」로 뭉개집니다 — 내려온 자리에 한 박자 머물러야
 * 그 카드가 제 할 일을 마치고 물러나는 것으로 보입니다.
 *
 * 길게 잡을 자리가 아닙니다. 이 뒤에 정산이 서므로, 여기서 끄는 만큼 그것이 늦습니다.
 */
export const ITEM_LINGER = 0.22

export const HAND_Y = 608

/**
 * 바뀌는 카드가 나와 서는 줄.
 *
 * **낸 카드의 줄과 손패 줄 사이입니다.** 카드가 바뀌는 것은 득점 도중에도 일어나므로
 * (`OnCardScored` · `OnHandPlayed`) 판 가운데에 두면 낸 카드와 겹칩니다.
 */
export const SHOW_Y = 486

/** 나온 카드는 손패 위에 있습니다. 판 위로 나온 것이므로 무엇에도 가리지 않습니다. */
export const SHOW_Z = 720

/** 나온 카드가 돌아가고 나서 지워지기까지. **타는 데 걸리는 시간이 그것입니다.** */
export const SHOW_CLEAR = 0.75

/** 타는 종이의 불빛. `dissolve.ts` 의 기본 불빛과 같은 색입니다. */
export const EMBER = 0xff8c2e

/**
 * 줄에 선 것들이 서로를 덮는 차례.
 *
 * **`addChild` 순서로는 정해지지 않습니다.** 판은 `zIndex` 로 정렬하므로 값이 같은 것들은
 * 들어온 순서로 남고, 그 순서는 깔린 순서입니다 — 정렬 단추가 배열의 차례를 바꾸어도
 * 겹침은 그대로였고, 그것이 「정렬했는데 겹침이 뒤죽박죽」의 원인이었습니다.
 *
 * **배열의 색인이 곧 이 값입니다.** 차례를 바꾸는 자리마다 겹침을 따로 되돌리지 않도록,
 * 다시 그릴 때 한 자리에서 정합니다.
 */
export const ROW_Z = 10

/** 끄는 동안 들어 올리는 자리. **줄의 어느 것보다 위, 낸 카드보다 아래입니다.** */
export const DRAG_Z = 90

/**
 * 커서의 기울기가 닿는 거리. 카드 한 장 너비를 1 로 센 값입니다.
 *
 * **이 밖은 0 입니다.** 값을 잘라 두기만 하면 멀리 있는 것까지 최대로 기운 채이고, 커서가
 * 지나갈 때 그 값이 한꺼번에 뒤집힙니다.
 */
export const TILT_REACH = 3

/**
 * 고른 것과 가리킨 것의 그리기 차례.
 *
 * **줄이 차면 겹칩니다.** 손패는 9장부터, 조커와 소모품은 자리가 좁아지면 겹치는데 그때
 * 겹치는 차례는 발동하는 차례입니다 — 가리킨 것과 고른 것은 그 위로 올라와야 무엇을
 * 가리키고 무엇을 고른 것인지 보이고, 떼면 줄의 차례로 돌아와야 합니다.
 *
 * **가리킨 것이 고른 것보다 위입니다.** 고른 것이 위에 굳어 있으면 그 뒤의 것을 가리켜도
 * 가려집니다 — 지금 보려는 것이 언제나 위입니다.
 */
export const PICK_Z = 60

export const HOVER_Z = 70

/**
 * 펼친 팩의 카드가 서는 자리.
 *
 * **손패가 서는 그 줄입니다.** 상점에서는 손패가 비어 있고, 뜯은 팩에서 고르는 것은 그
 * 판에 깔린 카드 중에서 고르는 것과 같은 몸짓입니다 — 화면 한가운데에 따로 세우면 카드가
 * 놓이는 자리가 국면마다 달라집니다. 가운데도 손패와 같이 판의 가운데입니다.
 */
export const PACK_CARDS_Y = HAND_Y

export const PACK_X = BOARD_X

/**
 * 뜯은 팩의 이름과 지시문이 서는 자리. 카드 위입니다.
 *
 * **고른 한 장이 올라와도 지시문에 닿지 않는 자리입니다.** 고른 카드는 22픽셀 올라오고
 * 1.07배로 커지므로 그만큼 위로 자라고, 그 윗변이 지시문 위로 올라가면 무엇을 하라는
 * 글이 카드에 덮입니다 — 하나를 고른 그 순간에 가려집니다.
 */
export const PACK_TITLE_Y = PACK_CARDS_Y - PACK_CARD_H / 2 - 108

/** 버튼 줄. **패 아래입니다** — 패와 겹치면 카드를 고를 수가 없습니다. */
/**
 * 판 아래 버튼 줄의 윗변.
 *
 * **손가락에 맞게 키웠습니다.** 46픽셀 높이는 마우스에는 넉넉하지만 손가락에는 빠듯해서,
 * 낸다와 버린다 사이의 취소를 잘못 누릅니다 — 키운 만큼 줄이 위로 올라오고, 손패와 지시문도
 * 그만큼 비켜섭니다.
 */
export const BUTTON_Y = 712

/**
 * 낸다·버린다의 크기.
 *
 * **높이 계단의 `xl` 입니다.** 판을 움직이는 단추라서 가장 큽니다 — 매번 누르는 것이고,
 * 화면에서 이 칸을 쓰는 것은 이 줄뿐입니다.
 */
export const PLAY_W = 214

/** 버린다는 낸다보다 좁습니다 — 나아가는 것이 더 큽니다. */
export const DISCARD_W = 180

export const PLAY_H = 72

/**
 * 정렬 단추 하나의 크기.
 *
 * **손가락으로 누를 수 있는 크기입니다.** 모바일에서 이것이 가장 작은 단추였습니다 —
 * 자리를 세는 쪽이 이 값을 읽으므로, 키워도 둘이 겹치지 않습니다.
 */
export const SORT_W = 72

/** 정렬 단추 둘의 사이. */
export const SORT_GAP = 12

/** 곁단추입니다 — 높이 계단의 `sm`. */
export const SORT_H = 36

/** 정렬 단추가 숨을 때 내려가는 거리. 화면 아래 밖까지입니다. */
export const SORT_HIDE = 120

/**
 * 블라인드를 고르는 동안 배경음을 낮추는 정도. 설정값에 대한 비율입니다.
 *
 * **곡을 끊지 않습니다.** 무엇과 붙을지 정하는 자리는 판이 도는 중이 아니므로 물러나야
 * 하지만, 조용해지면 판이 화면마다 토막나고 고르는 자리에서 분위기가 한 번 없어집니다 —
 * 절반이면 물러난 것으로 들리면서 그 곡이 이어지고 있는 것으로도 들립니다.
 */
export const BLIND_MUSIC_DIM = 0.5

/**
 * 왼쪽 판의 밑단에 서는 단추의 높이.
 *
 * **높이 계단의 기본 칸입니다.** 런 정보와 메뉴는 그 밖의 단추이고, 그 밖의 단추는 48
 * 입니다. 낸다·버린다(`xl`)와 같이 두었던 것을 걷었습니다 — 판을 움직이는 것과 판을
 * 읽는 것이 같은 크기이면 무엇이 매번 누르는 것인지가 크기로 드러나지 않습니다.
 */
export const FOOT_BTN_H = 48

/**
 * 상점 밑단의 단추 둘 — 리롤과 다음 블라인드.
 *
 * **나아가는 줄이므로 `lg` 입니다.** 그 줄의 단추는 갈래가 달라도 전부 같은 높이이고,
 * 금색은 그 안에 하나입니다.
 */
export const SHOP_FOOT_H = 60

/**
 * 왼쪽 판의 밑단에 서는 단추 둘 — 런 정보와 메뉴.
 *
 * **윗변은 판의 밑변에서 셉니다.** 판은 화면 아래 22픽셀까지 내려오고, 그 안쪽으로
 * 8픽셀을 둡니다 — 적어 두면 단추를 키운 날에 판 밖으로 밀려납니다.
 */
export const PANEL_BTN_W = (IN_W - PANEL_BTN_GAP) / 2

/**
 * 런 정보와 메뉴의 폭. **런 정보가 넓습니다** — 판 안에서 더 자주 여는 쪽이고, 둘과 그
 * 사이가 안쪽 폭을 다 씁니다.
 */
export const PANEL_INFO_W = 128
export const PANEL_MENU_W = IN_W - PANEL_BTN_GAP - PANEL_INFO_W

/** 판의 아랫변(788)에서 24 위입니다. */
export const PANEL_FOOT_Y = SIZE.height - 12 - 24 - FOOT_BTN_H

/** 취소. 가운데에 서고 그 둘보다 좁습니다. */
export const CLEAR_W = 76

/** 버튼 사이. **손가락 하나가 들어가야 합니다.** */
export const BUTTON_GAP = 8

/** 고른 카드에 도는 빛의 색. 셰이더가 0..1 로 받습니다. */
export const PICK_TINT: [number, number, number] = [0.45, 1.0, 0.68]

/** 칩 × 배수 상자의 윗변. 바로 위에 고른 족보의 이름이 놓입니다. */
/**
 * 떠오르는 차이 글의 개수와 목숨.
 *
 * **여덟이면 넉넉합니다.** 한 프레임에 바뀌는 칸은 많아야 셋(핸드·버리기·안테)이고,
 * 0.62초는 그 셋이 두 번 겹칠 만큼입니다.
 */
/** 새로 선 태그가 번쩍이는 동안. 부우 하고 나왔다가 잦아드는 데까지입니다. */
export const TAG_FLASH = 0.55

/** 건너뛴 카드의 태그 칩이 그 자리에서 커지는 시간. 그다음 머리띠로 날아갑니다. */
export const TAG_POP = 0.3

/** 태그가 발동할 때 켜졌다 잦아드는 데까지. */
export const TAG_FIRE = 0.6

/** 나오는 번쩍임이 잦아들기를 기다리는 동안. 그 자리에서 쓰이는 태그에만 걸립니다. */
export const TAG_FIRE_WAIT = 0.5

export const DELTA_POOL = 8

/**
 * ±N 글 하나가 화면에 있는 시간.
 *
 * **앞의 0.4는 제자리에 앉아 있습니다.** 칸의 숫자와 같은 크기로 뜨므로 읽을 시간이 있어야
 * 하고, 곧바로 떠오르면 크게 띄운 뜻이 없어집니다.
 */
export const DELTA_LIFE = 0.8

/**
 * 고정 단계의 길이. 밀리초.
 *
 * **60Hz 입니다.** 단계마다 난수를 뽑는 떨림들이 이 빠르기로 떨고, 단계당 비율로 줄어드는
 * 것들이 이 빠르기로 줄어듭니다. 화면 주사율과 무관합니다.
 */
export const STEP_MS = 1000 / 60

/** 떠오르는 글자가 사라지기까지. 밀리초. */
export const RISER_SPAN = 1_050

/**
 * 다 옅어지기 전에 온전히 남아 있는 동안. `RISER_SPAN` 에 대한 비율입니다.
 *
 * **읽을 시간입니다.** 뜨는 순간부터 옅어지기 시작하면 「무엇이 얼마나」 를 읽기 전에
 * 반쯤 사라지고, 조커가 이어서 발동하는 판에서는 그것이 지나가는 빛으로만 남습니다.
 */
export const RISER_HOLD = 0.52

/** 떠오르는 글이 올라가는 거리. 위에 자리가 없으면 그만큼만 올라갑니다. */
export const RISER_LIFT = 46

/**
 * 카드에서 나오는 글이 그 카드의 가운데에서 얼마나 위인가.
 *
 * **윗변에 살짝 걸칩니다.** 카드의 얼굴은 아래쪽이 무늬이고 위쪽은 랭크 한 글자이므로,
 * 걸쳐도 가리는 것이 적습니다 — 카드 밖으로 완전히 나가면 그것이 그 카드의 값이라는 것이
 * 흐려집니다.
 */
export const RISER_ON_CARD = SIZE.cardHeight / 2 - 12

/**
 * 떠오르는 글이 화면 변에서 남겨 두는 자리.
 *
 * **글은 자기가 어디에 뜨는지 정하지 않습니다.** 부르는 쪽이 넘겨주는 것은 그 물건의
 * 자리이고, 물건은 화면 변에 붙어 설 수 있습니다 — 위 줄은 화면 맨 위이고 블라인드 딱지는
 * 왼쪽 위 모서리라, 받은 자리에 그대로 두면 글과 그 번쩍임의 절반이 화면 밖입니다.
 */
export const RISER_MARGIN = 8

/** `half` 반지름의 것이 `span` 안에 온전히 들어가는, `at` 에서 가장 가까운 자리. */
export function within(at: number, half: number, span: number): number {
  const room = half + RISER_MARGIN
  // 판보다 넓은 것은 옮길 자리가 없습니다. 가운데에 둡니다.
  if (room * 2 > span) return span / 2
  return Math.max(room, Math.min(span - room, at))
}

export const CHIPS_Y = 344

/**
 * 왼쪽 판의 줄들이 서는 자리.
 *
 * **네 무리이고 무리 사이가 26픽셀입니다.** 낱개로 적어 두었더니 사이가 12·30·12로
 * 제각각이었고, 그러면 여섯 칸이 한 덩어리로 보입니다 — 어느 둘이 한 벌인지는 사이의
 * 넓이가 말하는 것이고, 그 넓이가 고르지 않으면 아무것도 말하지 않습니다.
 *
 * **첫 사이만 두 배입니다.** 족보 이름은 카드를 골랐을 때만 나오는 글이라 그 위가 대개
 * 비어 있고, 그 빈 자리까지가 26 + 26 입니다 — 줄은 그 한가운데인 278 에 놓입니다.
 *
 * |무리|담기는 것|
 * |--|--|
 * |블라인드|딱지 34–190 · 라운드 득점 200–252|
 * |이번 손|족보 이름 304–328 · 칩 × 배수 336–394|
 * |자원|핸드 · 버리기 420–472 · 소지금 · 안티 484–536|
 * |적용 중|562부터|
 *
 * 딱지와 칩 × 배수와 아래 버튼은 자리가 그대로입니다 — 움직인 것은 족보 이름과 아래 넷,
 * 그리고 적용 중입니다.
 *
 * **라운드 득점은 딱지에 붙습니다.** 그 둘은 한 무리이고 무리 안의 사이는 무리 사이보다
 * 좁아야 하는데, 20픽셀이면 아래의 무리 사이(26)와 구분되지 않아 여섯 칸이 고르게 늘어선
 * 것으로 보입니다 — 절반인 10픽셀입니다.
 */
export const PANEL_ROWS = {
  /** 라운드 점수 칸의 윗변. 딱지(32~244) 아래 24 입니다. */
  score: 268,
  /** 족보 이름은 판 안이 아니라 손패 위에 뜹니다. 남겨 둔 값은 옛 자리입니다. */
  handLabel: 304,
  /** 2×2 칸의 첫 줄. */
  hands: 416,
  money: 464,
  /** 적용 중 목록의 머리글. */
  active: 536,
} as const

/** 라운드 점수 칸의 높이. 이름과 값 한 줄, 그 아래 게이지입니다. */
export const SCORE_H = 64

/**
 * 무리를 가르는 줄들. **각 사이의 한가운데입니다.**
 *
 * 첫 줄이 291 이었습니다 — 라운드 득점의 밑변을 278 로 잡고 셋던 값인데 그 칸은 52픽셀
 * 높이라 262 에서 끝났고, 줄이 한가운데에서 13픽셀 비켜서 있었습니다.
 */
export const PANEL_GROOVES = [278, 407, 549] as const

/**
 * 칩 × 배수 덩어리.
 *
 * **판의 다른 칸 둘과 같은 한 벌입니다** — 핸드와 버리기가 그렇듯 같은 바탕의 상자 둘이
 * 나란히 놓이고, 그 사이에 곱셈표가 놓입니다. 파랑과 붉음은 값이 움직이는 동안에만 들고,
 * 조용할 때의 왼쪽 판은 같은 색의 칸들입니다.
 */
export const CHIPS_H = 60

/** 두 상자의 모서리. **0 입니다** — 화면에 둥근 모서리가 없습니다. */
export const CHIPS_R = 0

/** 두 상자 사이. **곱셈표가 그 사이에 놓입니다.** */
export const CHIPS_GAP = 12

/** 구분선 하나가 차지하는 높이. 줄은 그 한가운데입니다. */
export const RULE_H = 14

/**
 * 흐림의 반지름. **CSS 픽셀입니다.**
 *
 * 필터가 받는 값은 그 필터가 굽는 텍셀 단위이므로, 해상도가 달라지면 같은 값이 화면에서
 * 다른 크기가 됩니다 — 화면에서의 크기를 적어 두고 해상도를 곱합니다.
 */
export const BLUR_PX = 3

export const BLUR_BACK_PX = 2

export const DECK_X = SIZE.width - 62

/**
 * 카드를 거두는 동안 덱이 판 쪽으로 나오는 거리.
 *
 * **받는 것이 보여야 합니다.** 덱은 화면의 오른쪽 끝에 붙어 있어서, 그 자리에서 받으면
 * 카드가 화면 밖으로 나가는 것과 구분되지 않습니다.
 */
export const DECK_MEET = 54

export const DECK_Y = 608

/**
 * 자리를 비우는 글이 서는 자리. 조커와 소모품 줄 바로 아래입니다 — 자리의 아랫변(176)과
 * 칸 수를 적은 글(193) 아래입니다.
 */
export const FOCUS_Y = JOKER_TRAY.y + JOKER_TRAY.height + 36

/**
 * 자리를 비우는 글의 판 크기.
 *
 * **치른 값이 이 판의 가운데에 뜹니다.** 그래서 세우는 자리와 그 자리를 세는 자리 둘이
 * 이 값을 읽습니다 — 세우는 쪽의 지역 값이었고, 값이 뜨는 자리는 그것을 알 수 없어서
 * 판 옆의 작은 카드에서 났습니다.
 */
export const FOCUS_W = 600

export const FOCUS_H = 98

/**
 * 팩의 카드를 받으려고 덱이 나와 있는 시간.
 *
 * 카드가 닿는 데 `LAND_AT` 이고, 닿은 뒤에 덱이 눌리고 이름이 뜨는 것을 보는 데 나머지입니다.
 * **넉넉합니다.** 실시간 액션 게임이 아니므로, 일어난 일을 다 보고 나서 다음으로 갑니다.
 */
export const DECK_PEEK = 2.1

/**
 * 칸 수(`5 / 5`)가 산 직후에 강조되어 있는 시간.
 *
 * **산 것이 어디에 들어갔는지를 수로도 알립니다.** 물건이 줄에 닿는 것은 보이지만 그래서
 * 자리가 몇 남았는지는 그 순간에 눈이 가지 않습니다 — 닿는 그 자리에서 수가 커지고
 * 값의 색이 되어 잠깐 머물다가 돌아옵니다.
 */
export const COUNT_PULSE = 1.6

/**
 * 「적용 중」에 새로 들어온 줄이 밝게 남아 있는 시간.
 *
 * 바우처는 들어갈 칸이 없으므로 산 자리에서 이름이 뜨는 것이 유일한 표시였습니다 — 그것이
 * 규칙으로 들어갔다는 것은 왼쪽 판의 그 줄이 밝아지는 것으로 알립니다.
 */
export const ACTIVE_GLOW = 2.6

/**
 * 산 것이 자리에 닿은 뒤에 상점이 돌아오기까지.
 *
 * **닿는 것을 보고 나서입니다.** 팩이 닫히는 그 프레임에 상점이 올라오면 집은 카드가 아직
 * 줄로 가는 중인데 판이 그 밑에서 솟아, 어디에 들어간 것인지를 볼 틈이 없습니다 — 닿아서
 * 번쩍이고 이름이 뜨는 것까지 보고 그다음입니다.
 */
export const SHOP_RETURN_REST = 0.8

/**
 * 판을 적어 두는 사이의 가장 짧은 간격.
 *
 * **연출 하나보다 짧습니다.** 눌러서 다음 것을 누르기까지가 그보다 길므로, 사람이 손을
 * 멈추는 자리마다 적혀 있습니다.
 */
export const SAVE_GAP = 400
