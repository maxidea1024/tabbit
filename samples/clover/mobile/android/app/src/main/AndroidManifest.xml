<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        구글 백업에 담지 않습니다.

        이 앱이 저장소에 두는 것은 판 하나 · 옵션 · 도감, 그리고 **로그인한 계정의
        표딱지**입니다. 백업은 그것을 계정의 드라이브에 올리고 다른 기계에서 되돌리는데,
        표딱지가 그렇게 옮겨 가면 그 기계는 로그인한 적이 없는 채로 로그인되어 있습니다.

        판과 도감은 서버에 있고, 그 길로 옮겨 갑니다.
    -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            판이 1280 × 800 가로 하나에 맞춰 그려지므로 가로로 고정합니다. 세로로 돌리면
            좌우가 잘리거나 위아래에 배경만 남습니다.
        -->
        <activity
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                로그인을 마치고 돌아오는 길.

                **웹에서는 주소가 그대로 돌아오지만 앱에는 그 주소가 없습니다.**
                제공자는 시스템 브라우저에서 열리고, 그 브라우저가 `clover://auth#session=…`
                를 열면 안드로이드가 이 앱을 깨웁니다 — `launchMode="singleTask"` 이므로
                돌아오는 것은 지금 돌고 있는 그 앱입니다.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="clover" android:host="auth" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
