{
  "name": "clover-mobile",
  "version": "0.3.2",
  "private": true,
  "description": "clover — 안드로이드 포장. 웹 빌드를 그대로 담습니다.",
  "scripts": {
    "web": "cd ../web && npm run build",
    "sync": "cap sync android",
    "prepare:android": "npm run web && npm run sync",
    "apk": "npm run prepare:android && cd android && ./gradlew assembleRelease",
    "apk:win": "npm run prepare:android && cd android && .\\gradlew assembleRelease",
    "apk:debug": "npm run prepare:android && cd android && ./gradlew assembleDebug",
    "apk:debug:win": "npm run prepare:android && cd android && .\\gradlew assembleDebug",
    "open": "cap open android"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "dependencies": {
    "@capacitor/app": "^7.0.0",
    "@capacitor/haptics": "^7.0.5"
  }
}
