// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package sprout

import (
	"fmt"

	"sprout/tabbit"
)

// SettingRecord was generated from samples/sprout/xlsx/Setting.xlsx : SettingTable : A1.
type SettingRecord struct {
	// setting key
	SettingKey string
	// value as written
	Value string
	// description key
	DescKey string
}

// SettingTable holds every row of Setting.
type SettingTable struct {
	records []SettingRecord
	bySettingKey map[string]int
}

// Records returns every row, in the order the sheet declared them.
func (t *SettingTable) Records() []SettingRecord { return t.records }

// FindBySettingKey returns the row with this SettingKey, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *SettingTable) FindBySettingKey(key string) *SettingRecord {
	position, found := t.bySettingKey[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetBySettingKeyOrError returns the row with this SettingKey, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *SettingTable) GetBySettingKeyOrError(key string) (*SettingRecord, error) {
	position, found := t.bySettingKey[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Setting` that corresponds to field `SettingKey` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsSettingKey reports whether the table holds a row with this SettingKey.
func (t *SettingTable) ContainsSettingKey(key string) bool {
	_, found := t.bySettingKey[key]
	return found
}

// Read loads the table from a .tcb file written by Tabbit.
func (t *SettingTable) Read(filename string) error {
	data, err := tabbit.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	// Unconditionally, and with whatever EncryptionKey and MacKey hold - which is nil
	// unless the project set them. A file that is neither encrypted nor signed comes back
	// from this untouched, so the load path is the same either way and there is no
	// condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
	//
	// What comes back is a window onto data rather than a copy of it, which is why data
	// stays in scope for the whole read.
	opened, err := tabbit.Open(data, EncryptionKey, MacKey, VerifyMac)
	if err != nil {
		return err
	}

	reader := tabbit.NewReader(opened)
	count, columns := tabbit.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]SettingRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if tabbit.CheckColumn(reader, column, "Setting.SettingKey", tabbit.KindScalar, false, tabbit.ElementString) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Setting.SettingKey")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameString(count - i)
					for ; n > 0; n-- {
						records[i].SettingKey = value
						i++
					}
				}
			}
		case 2:
			if tabbit.CheckColumn(reader, column, "Setting.Value", tabbit.KindScalar, false, tabbit.ElementString) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Setting.Value")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameString(count - i)
					for ; n > 0; n-- {
						records[i].Value = value
						i++
					}
				}
			}
		case 3:
			if tabbit.CheckColumn(reader, column, "Setting.DescKey", tabbit.KindScalar, false, tabbit.ElementString) {
				cursor := tabbit.NewColumnCursor(reader, column, count, "Setting.DescKey")
				for i := int32(0); i < count; {
					n, value := cursor.NextSameString(count - i)
					for ; n > 0; n-- {
						records[i].DescKey = value
						i++
					}
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		tabbit.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Setting: %w", err)
	}

	bySettingKey := make(map[string]int, len(records))
	for i := range records {
		bySettingKey[records[i].SettingKey] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.bySettingKey = bySettingKey

	return nil
}
