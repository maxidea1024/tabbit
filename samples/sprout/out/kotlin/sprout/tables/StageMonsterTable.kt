// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package sprout

import java.io.File
import tabbit.TcbReader
import tabbit.RecordNotFoundException
import tabbit.Uuid
import tabbit.readAllBytes
import tabbit.open
import tabbit.readTableHeader
import tabbit.checkColumn
import tabbit.checkColumnWithElements
import tabbit.checkBlockEnd
import tabbit.readPresence
import tabbit.readElementPresence
import tabbit.isPresent
import tabbit.TcbException
import tabbit.ColumnCursor
import tabbit.ELEMENT_VARINT
import tabbit.ELEMENT_BOOL
import tabbit.ELEMENT_I32
import tabbit.ELEMENT_I64
import tabbit.ELEMENT_F32
import tabbit.ELEMENT_F64
import tabbit.ELEMENT_STRING
import tabbit.ELEMENT_UUID
import tabbit.KIND_SCALAR
import tabbit.KIND_ARRAY

// Generated from samples/sprout/Stage.xlsx : StageMonsterTable : A1
class StageMonsterRecord {
    /** primary index */
    var id: Int = 0
    /** name key */
    var nameKey: String = ""
    /** model asset */
    var model: String = ""
    /** element */
    var element: ElementType = ElementType.of(0)
    /** attack */
    var attack: Int = 0
    /** defense */
    var defense: Int = 0
    /** health */
    var health: Long = 0L
    /** skill ids */
    var skillIds: MutableList<Int> = ArrayList()

}

/** Every row of StageMonster. */
class StageMonsterTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<StageMonsterRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, StageMonsterRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): StageMonsterRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): StageMonsterRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `StageMonster` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .tcb file written by Tabbit. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        // Opened unconditionally, and with whatever Tables.encryptionKey and
        // Tables.macKey hold - which is null unless the project set them. A
        // file that is neither encrypted nor signed comes back from this untouched, so the
        // load path is the same either way and there is no condition here that could be the
        // wrong way round. spec/wire/tcb-mac-and-signature.md.
        val reader = TcbReader(open(
            readAllBytes(filename), Tables.encryptionKey,
            Tables.macKey, Tables.verifyMac))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<StageMonsterRecord>(count)
        val loadedById = HashMap<Int, StageMonsterRecord>(count * 2)

        repeat(count) { loaded.add(StageMonsterRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "StageMonster.Id", KIND_SCALAR, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "StageMonster.Id")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].id = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                2 -> {
                    checkColumn(column, "StageMonster.NameKey", KIND_SCALAR, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "StageMonster.NameKey")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].nameKey = cursor.runSameText
                            at++
                            n--
                        }
                    }
                }
                3 -> {
                    checkColumn(column, "StageMonster.Model", KIND_SCALAR, false, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "StageMonster.Model")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameString(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].model = cursor.runSameText
                            at++
                            n--
                        }
                    }
                }
                4 -> {
                    checkColumn(column, "StageMonster.Element", KIND_SCALAR, false, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "StageMonster.Element")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].element = ElementType.of(cursor.runSameValue)
                            at++
                            n--
                        }
                    }
                }
                5 -> {
                    checkColumn(column, "StageMonster.Attack", KIND_SCALAR, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "StageMonster.Attack")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].attack = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                6 -> {
                    checkColumn(column, "StageMonster.Defense", KIND_SCALAR, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "StageMonster.Defense")
                    var at = 0
                    while (at < count) {
                        var n = cursor.nextSameI32(count - at)
                        while (n > 0) {
                            val i = at
                            loaded[i].defense = cursor.runSameValue
                            at++
                            n--
                        }
                    }
                }
                7 -> {
                    checkColumn(column, "StageMonster.Health", KIND_SCALAR, false, ELEMENT_I64, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "StageMonster.Health")
                    for (record in loaded) {
                        record.health = cursor.nextI64()
                    }
                }
                8 -> {
                    checkColumn(column, "StageMonster.SkillIds", KIND_ARRAY, false, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "StageMonster.SkillIds")
                    for (record in loaded) {
                        val elementCount = cursor.nextLength()
                        record.skillIds = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.skillIds.add(cursor.nextI32()) }
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
