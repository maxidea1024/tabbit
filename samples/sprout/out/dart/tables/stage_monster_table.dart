// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from samples/sprout/xlsx/Stage.xlsx : StageMonsterTable : A1
class StageMonsterRecord {
  /// primary index
  int id = 0;
  /// name key
  String nameKey = '';
  /// model asset
  String model = '';
  /// element
  ElementType element = ElementType.of(0);
  /// attack
  int attack = 0;
  /// defense
  int defense = 0;
  /// health
  BigInt health = BigInt.zero;
  /// skill ids
  List<int> skillIds = [];

}

/// Every row of StageMonster.
class StageMonsterTable extends Iterable<StageMonsterRecord> {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<StageMonsterRecord> get records => _records;
  List<StageMonsterRecord> _records = [];

  /// How many rows the table holds.
  ///
  /// Overridden rather than inherited: `Iterable.length` counts by walking, and the rows
  /// are a list that already knows.
  @override
  int get length => _records.length;

  /// The rows, in the order the file wrote them. Extending `Iterable` on top of this is
  /// what gives the table `map` - `where` - `firstWhere` without any of them being
  /// written here.
  ///
  /// The list reference is read once, here. A refresh replaces the reference rather than
  /// its contents, so a loop already running keeps the rows it started with.
  @override
  Iterator<StageMonsterRecord> get iterator => _records.iterator;

  Map<int, StageMonsterRecord> _byId = {};

  /// The row with this Id, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  StageMonsterRecord? findById(int key) => _byId[key];

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getById(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  StageMonsterRecord getByIdOrThrow(int key) {
    final record = _byId[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `StageMonster` that corresponds to '
          'field `Id` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Id.
  bool containsId(int key) => _byId.containsKey(key);


  /// The row with this Id, or null when the table has none.
  ///
  /// `findById` under the spelling Dart uses for a keyed collection. It
  /// answers with null because that is what `Map` does here, and the type says so - a
  /// language whose own map throws generates a subscript that throws.
  /// spec/targets/table-collection-surface.md section 5.7.
  ///
  /// This is the key, not the row's position. `table[0]` is the row whose
  /// Id is 0, not the first row - the rows in order are `records`.
  ///
  /// Only a key of one column has this: Dart's subscript takes one argument.
  StageMonsterRecord? operator [](int key) => findById(key);


  /// Each row with the Id it is keyed by -
  /// `for (final entry in table.entries)`.
  ///
  /// What this saves a caller is not the key value - the row carries it - but having to
  /// know which column the key is: Id here, something else in the next
  /// table.
  ///
  /// The rows come in the order the file wrote them rather than the order the map holds
  /// them, which makes this and iterating the table agree. Only the primary key has this:
  /// a table keyed by several columns together has no single key value to pair a row with.
  Iterable<MapEntry<int, StageMonsterRecord>> get entries =>
      _records.map((record) => MapEntry(record.id, record));

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <StageMonsterRecord>[];
    final loadedById = <int, StageMonsterRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(StageMonsterRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'StageMonster.Id', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'StageMonster.Id');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].id = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'StageMonster.NameKey', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'StageMonster.NameKey');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].nameKey = value;
            }
          }
          break;
        case 3:
          checkColumn(column, 'StageMonster.Model', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'StageMonster.Model');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].model = value;
            }
          }
          break;
        case 4:
          checkColumn(column, 'StageMonster.Element', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'StageMonster.Element');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].element = ElementType.of(value);
            }
          }
          break;
        case 5:
          checkColumn(column, 'StageMonster.Attack', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'StageMonster.Attack');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].attack = value;
            }
          }
          break;
        case 6:
          checkColumn(column, 'StageMonster.Defense', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'StageMonster.Defense');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].defense = value;
            }
          }
          break;
        case 7:
          checkColumn(column, 'StageMonster.Health', kindScalar, false, [elementI64, elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'StageMonster.Health');
          for (final record in loaded) {
            record.health = cursor.nextI64();
          }
          break;
        case 8:
          checkColumn(column, 'StageMonster.SkillIds', kindArray, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'StageMonster.SkillIds');
          for (final record in loaded) {
            final elementCount = cursor.nextLength();
            record.skillIds = List.generate(elementCount, (_) => cursor.nextI32());
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedById[record.id] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byId = loadedById;
  }
}
