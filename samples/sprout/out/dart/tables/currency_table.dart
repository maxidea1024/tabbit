// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from samples/sprout/Item.xlsx : CurrencyTable : A1
class CurrencyRecord {
  /// primary index
  int id = 0;
  /// currency kind
  CurrencyType type = CurrencyType.of(0);
  /// name key
  String nameKey = '';
  /// icon asset
  String icon = '';
  /// hard cap, 0 for none
  BigInt cap = BigInt.zero;

}

/// Every row of Currency.
class CurrencyTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<CurrencyRecord> get records => _records;
  List<CurrencyRecord> _records = [];

  Map<int, CurrencyRecord> _byId = {};

  /// The row with this Id, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  CurrencyRecord? findById(int key) => _byId[key];

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getById(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  CurrencyRecord getByIdOrThrow(int key) {
    final record = _byId[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `Currency` that corresponds to '
          'field `Id` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Id.
  bool containsId(int key) => _byId.containsKey(key);

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <CurrencyRecord>[];
    final loadedById = <int, CurrencyRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(CurrencyRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'Currency.Id', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Currency.Id');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].id = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'Currency.Type', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Currency.Type');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].type = CurrencyType.of(value);
            }
          }
          break;
        case 3:
          checkColumn(column, 'Currency.NameKey', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'Currency.NameKey');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].nameKey = value;
            }
          }
          break;
        case 4:
          checkColumn(column, 'Currency.Icon', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'Currency.Icon');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].icon = value;
            }
          }
          break;
        case 5:
          checkColumn(column, 'Currency.Cap', kindScalar, false, [elementI64, elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Currency.Cap');
          for (final record in loaded) {
            record.cap = cursor.nextI64();
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedById[record.id] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byId = loadedById;
  }
}
