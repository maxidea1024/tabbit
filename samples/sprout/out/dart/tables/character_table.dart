// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from samples/sprout/xlsx/Character.xlsx : CharacterTable : A1
class CharacterRecord {
  /// primary index
  int id = 0;
  /// display name key
  String nameKey = '';
  /// portrait asset
  String portrait = '';
  /// combat role
  RoleType role = RoleType.of(0);
  /// element
  ElementType element = ElementType.of(0);
  /// initial grade
  GradeType grade = GradeType.of(0);
  /// base rarity
  RarityType rarity = RarityType.of(0);
  /// skill ids, ';' between
  List<int> skillIds = [];
  /// maximum level
  int maxLevel = 0;
  /// released
  bool released = false;

}

/// Every row of Character.
class CharacterTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<CharacterRecord> get records => _records;
  List<CharacterRecord> _records = [];

  Map<int, CharacterRecord> _byId = {};

  /// The row with this Id, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  CharacterRecord? findById(int key) => _byId[key];

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getById(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  CharacterRecord getByIdOrThrow(int key) {
    final record = _byId[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `Character` that corresponds to '
          'field `Id` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Id.
  bool containsId(int key) => _byId.containsKey(key);

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <CharacterRecord>[];
    final loadedById = <int, CharacterRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(CharacterRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'Character.Id', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.Id');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].id = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'Character.NameKey', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.NameKey');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].nameKey = value;
            }
          }
          break;
        case 3:
          checkColumn(column, 'Character.Portrait', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.Portrait');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].portrait = value;
            }
          }
          break;
        case 4:
          checkColumn(column, 'Character.Role', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.Role');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].role = RoleType.of(value);
            }
          }
          break;
        case 5:
          checkColumn(column, 'Character.Element', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.Element');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].element = ElementType.of(value);
            }
          }
          break;
        case 6:
          checkColumn(column, 'Character.Grade', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.Grade');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].grade = GradeType.of(value);
            }
          }
          break;
        case 7:
          checkColumn(column, 'Character.Rarity', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.Rarity');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].rarity = RarityType.of(value);
            }
          }
          break;
        case 8:
          checkColumn(column, 'Character.SkillIds', kindArray, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.SkillIds');
          for (final record in loaded) {
            final elementCount = cursor.nextLength();
            record.skillIds = List.generate(elementCount, (_) => cursor.nextI32());
          }
          break;
        case 9:
          checkColumn(column, 'Character.MaxLevel', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.MaxLevel');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].maxLevel = value;
            }
          }
          break;
        case 10:
          checkColumn(column, 'Character.Released', kindScalar, false, [elementBool]);
          cursor = TcbColumnCursor(reader, column, count, 'Character.Released');
          for (final record in loaded) {
            record.released = cursor.nextBool();
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedById[record.id] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byId = loadedById;
  }
}
