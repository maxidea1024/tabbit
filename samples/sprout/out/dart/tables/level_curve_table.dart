// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from samples/sprout/Growth.xlsx : LevelCurveTable : A1
class LevelCurveRecord {
  /// primary index
  int id = 0;
  /// curve tier
  String tierName = '';
  /// affected stat
  StatType curveStat = StatType.of(0);
  /// tick within the curve
  int tick = 0;
  /// value at the start of the step
  int baseValue = 0;
  /// increment per step
  int stepValue = 0;
  /// value at the end of the step
  BigInt endValue = BigInt.zero;
  /// ceiling for this curve
  BigInt capValue = BigInt.zero;
  /// balance revision
  int revision = 0;

}

/// Every row of LevelCurve.
class LevelCurveTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<LevelCurveRecord> get records => _records;
  List<LevelCurveRecord> _records = [];

  Map<int, LevelCurveRecord> _byId = {};

  /// The row with this Id, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  LevelCurveRecord? findById(int key) => _byId[key];

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getById(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  LevelCurveRecord getByIdOrThrow(int key) {
    final record = _byId[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `LevelCurve` that corresponds to '
          'field `Id` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Id.
  bool containsId(int key) => _byId.containsKey(key);

  /// Loads the table from a .tcb file written by Tabbit.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    // Opened unconditionally, and with whatever Tables.encryptionKey and Tables.macKey hold
    // - which is null unless the project set them. A file that is neither encrypted nor
    // signed comes back from this untouched, so the load path is the same either way and
    // there is no condition here that could be the wrong way round.
    // spec/wire/tcb-mac-and-signature.md.
    final reader = TcbReader(open(readAllBytes(filename), Tables.encryptionKey,
        Tables.macKey, Tables.verifyMac));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late TcbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <LevelCurveRecord>[];
    final loadedById = <int, LevelCurveRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(LevelCurveRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'LevelCurve.Id', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'LevelCurve.Id');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].id = value;
            }
          }
          break;
        case 2:
          checkColumn(column, 'LevelCurve.TierName', kindScalar, false, [elementString]);
          cursor = TcbColumnCursor(reader, column, count, 'LevelCurve.TierName');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameString(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].tierName = value;
            }
          }
          break;
        case 3:
          checkColumn(column, 'LevelCurve.CurveStat', kindScalar, false, [elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'LevelCurve.CurveStat');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].curveStat = StatType.of(value);
            }
          }
          break;
        case 4:
          checkColumn(column, 'LevelCurve.Tick', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'LevelCurve.Tick');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].tick = value;
            }
          }
          break;
        case 5:
          checkColumn(column, 'LevelCurve.BaseValue', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'LevelCurve.BaseValue');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].baseValue = value;
            }
          }
          break;
        case 6:
          checkColumn(column, 'LevelCurve.StepValue', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'LevelCurve.StepValue');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].stepValue = value;
            }
          }
          break;
        case 7:
          checkColumn(column, 'LevelCurve.EndValue', kindScalar, false, [elementI64, elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'LevelCurve.EndValue');
          for (final record in loaded) {
            record.endValue = cursor.nextI64();
          }
          break;
        case 8:
          checkColumn(column, 'LevelCurve.CapValue', kindScalar, false, [elementI64, elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'LevelCurve.CapValue');
          for (final record in loaded) {
            record.capValue = cursor.nextI64();
          }
          break;
        case 9:
          checkColumn(column, 'LevelCurve.Revision', kindScalar, false, [elementI32, elementVarint]);
          cursor = TcbColumnCursor(reader, column, count, 'LevelCurve.Revision');
          for (var i = 0; i < count; ) {
            final (n, value) = cursor.nextSameI32(count - i);
            for (var left = n; left > 0; left--, i++) {
              loaded[i].revision = value;
            }
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedById[record.id] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byId = loadedById;
  }
}
