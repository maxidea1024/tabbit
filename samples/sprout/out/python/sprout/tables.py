# ------------------------------------------------------------------------------
# Generated by Tabbit. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

import enum
import os

from . import tabbit
from .character_table import CharacterTable
from .character_stat_table import CharacterStatTable
from .character_ascend_table import CharacterAscendTable
from .character_skin_table import CharacterSkinTable
from .character_voice_table import CharacterVoiceTable
from .role_bonus_table import RoleBonusTable
from .element_affinity_table import ElementAffinityTable
from .codex_table import CodexTable
from .codex_entry_table import CodexEntryTable
from .codex_tier_table import CodexTierTable
from .dialogue_table import DialogueTable
from .dialogue_line_table import DialogueLineTable
from .cutscene_table import CutsceneTable
from .dungeon_table import DungeonTable
from .dungeon_floor_table import DungeonFloorTable
from .dungeon_reward_table import DungeonRewardTable
from .raid_table import RaidTable
from .equip_table import EquipTable
from .equip_level_table import EquipLevelTable
from .equip_grade_table import EquipGradeTable
from .equip_set_table import EquipSetTable
from .equip_set_bonus_table import EquipSetBonusTable
from .equip_option_table import EquipOptionTable
from .gacha_table import GachaTable
from .gacha_pool_table import GachaPoolTable
from .gacha_odds_table import GachaOddsTable
from .pity_table import PityTable
from .level_curve_table import LevelCurveTable
from .stat_base_table import StatBaseTable
from .ascend_curve_table import AscendCurveTable
from .power_score_table import PowerScoreTable
from .item_table import ItemTable
from .currency_table import CurrencyTable
from .material_table import MaterialTable
from .consumable_table import ConsumableTable
from .offer_table import OfferTable
from .offer_item_table import OfferItemTable
from .item_grade_table import ItemGradeTable
from .string_table import StringTable
from .offline_reward_table import OfflineRewardTable
from .idle_rate_table import IdleRateTable
from .afk_bonus_table import AfkBonusTable
from .quest_table import QuestTable
from .quest_step_table import QuestStepTable
from .daily_quest_table import DailyQuestTable
from .weekly_quest_table import WeeklyQuestTable
from .achievement_table import AchievementTable
from .reward_table import RewardTable
from .loot_group_table import LootGroupTable
from .loot_entry_table import LootEntryTable
from .mail_reward_table import MailRewardTable
from .setting_table import SettingTable
from .condition_table import ConditionTable
from .unlock_table import UnlockTable
from .table_table import TableTable
from .shop_table import ShopTable
from .shop_slot_table import ShopSlotTable
from .price_table import PriceTable
from .promo_table import PromoTable
from .skill_table import SkillTable
from .skill_level_table import SkillLevelTable
from .skill_effect_table import SkillEffectTable
from .buff_table import BuffTable
from .buff_pool_table import BuffPoolTable
from .passive_table import PassiveTable
from .stage_table import StageTable
from .stage_wave_table import StageWaveTable
from .stage_monster_table import StageMonsterTable
from .stage_reward_table import StageRewardTable
from .chapter_table import ChapterTable
from .world_table import WorldTable


class Tables:
    """Every table, loaded together so cross-table references can be resolved."""

    __slots__ = ("character", "character_stat", "character_ascend", "character_skin", "character_voice", "role_bonus", "element_affinity", "codex", "codex_entry", "codex_tier", "dialogue", "dialogue_line", "cutscene", "dungeon", "dungeon_floor", "dungeon_reward", "raid", "equip", "equip_level", "equip_grade", "equip_set", "equip_set_bonus", "equip_option", "gacha", "gacha_pool", "gacha_odds", "pity", "level_curve", "stat_base", "ascend_curve", "power_score", "item", "currency", "material", "consumable", "offer", "offer_item", "item_grade", "string", "offline_reward", "idle_rate", "afk_bonus", "quest", "quest_step", "daily_quest", "weekly_quest", "achievement", "reward", "loot_group", "loot_entry", "mail_reward", "setting", "condition", "unlock", "table", "shop", "shop_slot", "price", "promo", "skill", "skill_level", "skill_effect", "buff", "buff_pool", "passive", "stage", "stage_wave", "stage_monster", "stage_reward", "chapter", "world")

    #: The key the table files were sealed with, or None when they were not sealed.
    #:
    #: On the class rather than an argument to every read, because it is one decision the
    #: consuming project makes once, at start-up, before the first table is read - set it
    #: with `Tables.encryption_key = key`. A key threaded through every read signature would
    #: be the same value written out at every call site, and every call site is somewhere it
    #: could be wrong.
    #:
    #: Thirty-two bytes, the key the converter was given. None - the default - is a project
    #: that does not encrypt, and it costs that project nothing: the load path opens the
    #: envelope either way, and a file that is not encrypted comes back from that unchanged.
    #: A project that does encrypt and leaves this None gets the reader's own message saying
    #: the file is encrypted and no key was given, rather than a failure somewhere inside the
    #: format.
    #:
    #: Where the key comes from is the one thing about this layer worth being careful with.
    #: It is a secret only until it reaches a repository, a build log or an issue, and it
    #: cannot be taken back afterwards - so it is read at start-up from wherever this project
    #: keeps secrets, not written into a source file. What the layer is for, and therefore
    #: what is and is not worth defending, is spec/wire/tcb-v104-composed-encodings.md section 4.
    encryption_key = None

    #: The key the table files were signed with, or None when they were not signed.
    #:
    #: A different key from `encryption_key` and independent of it: a file can be signed
    #: without being encrypted, and encrypted without being signed.
    #:
    #: What it buys is the thing encryption does not. The cipher is a keystream XOR, so a bit
    #: flipped in the file is a bit flipped in the value it decrypts to - no key needed - and
    #: every structural check the format makes still passes, because a four-byte float
    #: accepts any four bytes. With this set, a file altered after export stops loading.
    #:
    #: Setting it makes the reader stricter in a second way: a file that carries no MAC is
    #: refused, because otherwise the check could be removed by zeroing sixteen bytes. So the
    #: order to turn this on in is data first, key second. spec/wire/tcb-mac-and-signature.md.
    mac_key = None

    #: Whether to check the MAC at all. True by default.
    #:
    #: For tools that read exported files without the key, and for measuring what the check
    #: costs at load. It is not a security boundary - anyone who can flip a flag in a shipped
    #: build can read `mac_key` out of the same build.
    verify_mac = True

    def __init__(self):
        self.character = CharacterTable()
        self.character_stat = CharacterStatTable()
        self.character_ascend = CharacterAscendTable()
        self.character_skin = CharacterSkinTable()
        self.character_voice = CharacterVoiceTable()
        self.role_bonus = RoleBonusTable()
        self.element_affinity = ElementAffinityTable()
        self.codex = CodexTable()
        self.codex_entry = CodexEntryTable()
        self.codex_tier = CodexTierTable()
        self.dialogue = DialogueTable()
        self.dialogue_line = DialogueLineTable()
        self.cutscene = CutsceneTable()
        self.dungeon = DungeonTable()
        self.dungeon_floor = DungeonFloorTable()
        self.dungeon_reward = DungeonRewardTable()
        self.raid = RaidTable()
        self.equip = EquipTable()
        self.equip_level = EquipLevelTable()
        self.equip_grade = EquipGradeTable()
        self.equip_set = EquipSetTable()
        self.equip_set_bonus = EquipSetBonusTable()
        self.equip_option = EquipOptionTable()
        self.gacha = GachaTable()
        self.gacha_pool = GachaPoolTable()
        self.gacha_odds = GachaOddsTable()
        self.pity = PityTable()
        self.level_curve = LevelCurveTable()
        self.stat_base = StatBaseTable()
        self.ascend_curve = AscendCurveTable()
        self.power_score = PowerScoreTable()
        self.item = ItemTable()
        self.currency = CurrencyTable()
        self.material = MaterialTable()
        self.consumable = ConsumableTable()
        self.offer = OfferTable()
        self.offer_item = OfferItemTable()
        self.item_grade = ItemGradeTable()
        self.string = StringTable()
        self.offline_reward = OfflineRewardTable()
        self.idle_rate = IdleRateTable()
        self.afk_bonus = AfkBonusTable()
        self.quest = QuestTable()
        self.quest_step = QuestStepTable()
        self.daily_quest = DailyQuestTable()
        self.weekly_quest = WeeklyQuestTable()
        self.achievement = AchievementTable()
        self.reward = RewardTable()
        self.loot_group = LootGroupTable()
        self.loot_entry = LootEntryTable()
        self.mail_reward = MailRewardTable()
        self.setting = SettingTable()
        self.condition = ConditionTable()
        self.unlock = UnlockTable()
        self.table = TableTable()
        self.shop = ShopTable()
        self.shop_slot = ShopSlotTable()
        self.price = PriceTable()
        self.promo = PromoTable()
        self.skill = SkillTable()
        self.skill_level = SkillLevelTable()
        self.skill_effect = SkillEffectTable()
        self.buff = BuffTable()
        self.buff_pool = BuffPoolTable()
        self.passive = PassiveTable()
        self.stage = StageTable()
        self.stage_wave = StageWaveTable()
        self.stage_monster = StageMonsterTable()
        self.stage_reward = StageRewardTable()
        self.chapter = ChapterTable()
        self.world = WorldTable()

    def read_all(self, base_path, file_extension=".tcb"):
        """Reads every table from base_path, then links the references between them.

        file_extension defaults to what the recipe told the exporter to write. Pass a
        different one when the data files were renamed after export.

        Safe to call on a loaded accessor. Every file is read into a table of its own and
        the references are linked among those, so a failure part way through leaves every
        table holding the load it already had, and no row points at a row from it.
        """
        loaded_character = CharacterTable()
        loaded_character.read(os.path.join(base_path, "Character" + file_extension))
        loaded_character_stat = CharacterStatTable()
        loaded_character_stat.read(os.path.join(base_path, "CharacterStat" + file_extension))
        loaded_character_ascend = CharacterAscendTable()
        loaded_character_ascend.read(os.path.join(base_path, "CharacterAscend" + file_extension))
        loaded_character_skin = CharacterSkinTable()
        loaded_character_skin.read(os.path.join(base_path, "CharacterSkin" + file_extension))
        loaded_character_voice = CharacterVoiceTable()
        loaded_character_voice.read(os.path.join(base_path, "CharacterVoice" + file_extension))
        loaded_role_bonus = RoleBonusTable()
        loaded_role_bonus.read(os.path.join(base_path, "RoleBonus" + file_extension))
        loaded_element_affinity = ElementAffinityTable()
        loaded_element_affinity.read(os.path.join(base_path, "ElementAffinity" + file_extension))
        loaded_codex = CodexTable()
        loaded_codex.read(os.path.join(base_path, "Codex" + file_extension))
        loaded_codex_entry = CodexEntryTable()
        loaded_codex_entry.read(os.path.join(base_path, "CodexEntry" + file_extension))
        loaded_codex_tier = CodexTierTable()
        loaded_codex_tier.read(os.path.join(base_path, "CodexTier" + file_extension))
        loaded_dialogue = DialogueTable()
        loaded_dialogue.read(os.path.join(base_path, "Dialogue" + file_extension))
        loaded_dialogue_line = DialogueLineTable()
        loaded_dialogue_line.read(os.path.join(base_path, "DialogueLine" + file_extension))
        loaded_cutscene = CutsceneTable()
        loaded_cutscene.read(os.path.join(base_path, "Cutscene" + file_extension))
        loaded_dungeon = DungeonTable()
        loaded_dungeon.read(os.path.join(base_path, "Dungeon" + file_extension))
        loaded_dungeon_floor = DungeonFloorTable()
        loaded_dungeon_floor.read(os.path.join(base_path, "DungeonFloor" + file_extension))
        loaded_dungeon_reward = DungeonRewardTable()
        loaded_dungeon_reward.read(os.path.join(base_path, "DungeonReward" + file_extension))
        loaded_raid = RaidTable()
        loaded_raid.read(os.path.join(base_path, "Raid" + file_extension))
        loaded_equip = EquipTable()
        loaded_equip.read(os.path.join(base_path, "Equip" + file_extension))
        loaded_equip_level = EquipLevelTable()
        loaded_equip_level.read(os.path.join(base_path, "EquipLevel" + file_extension))
        loaded_equip_grade = EquipGradeTable()
        loaded_equip_grade.read(os.path.join(base_path, "EquipGrade" + file_extension))
        loaded_equip_set = EquipSetTable()
        loaded_equip_set.read(os.path.join(base_path, "EquipSet" + file_extension))
        loaded_equip_set_bonus = EquipSetBonusTable()
        loaded_equip_set_bonus.read(os.path.join(base_path, "EquipSetBonus" + file_extension))
        loaded_equip_option = EquipOptionTable()
        loaded_equip_option.read(os.path.join(base_path, "EquipOption" + file_extension))
        loaded_gacha = GachaTable()
        loaded_gacha.read(os.path.join(base_path, "Gacha" + file_extension))
        loaded_gacha_pool = GachaPoolTable()
        loaded_gacha_pool.read(os.path.join(base_path, "GachaPool" + file_extension))
        loaded_gacha_odds = GachaOddsTable()
        loaded_gacha_odds.read(os.path.join(base_path, "GachaOdds" + file_extension))
        loaded_pity = PityTable()
        loaded_pity.read(os.path.join(base_path, "Pity" + file_extension))
        loaded_level_curve = LevelCurveTable()
        loaded_level_curve.read(os.path.join(base_path, "LevelCurve" + file_extension))
        loaded_stat_base = StatBaseTable()
        loaded_stat_base.read(os.path.join(base_path, "StatBase" + file_extension))
        loaded_ascend_curve = AscendCurveTable()
        loaded_ascend_curve.read(os.path.join(base_path, "AscendCurve" + file_extension))
        loaded_power_score = PowerScoreTable()
        loaded_power_score.read(os.path.join(base_path, "PowerScore" + file_extension))
        loaded_item = ItemTable()
        loaded_item.read(os.path.join(base_path, "Item" + file_extension))
        loaded_currency = CurrencyTable()
        loaded_currency.read(os.path.join(base_path, "Currency" + file_extension))
        loaded_material = MaterialTable()
        loaded_material.read(os.path.join(base_path, "Material" + file_extension))
        loaded_consumable = ConsumableTable()
        loaded_consumable.read(os.path.join(base_path, "Consumable" + file_extension))
        loaded_offer = OfferTable()
        loaded_offer.read(os.path.join(base_path, "Offer" + file_extension))
        loaded_offer_item = OfferItemTable()
        loaded_offer_item.read(os.path.join(base_path, "OfferItem" + file_extension))
        loaded_item_grade = ItemGradeTable()
        loaded_item_grade.read(os.path.join(base_path, "ItemGrade" + file_extension))
        loaded_string = StringTable()
        loaded_string.read(os.path.join(base_path, "String" + file_extension))
        loaded_offline_reward = OfflineRewardTable()
        loaded_offline_reward.read(os.path.join(base_path, "OfflineReward" + file_extension))
        loaded_idle_rate = IdleRateTable()
        loaded_idle_rate.read(os.path.join(base_path, "IdleRate" + file_extension))
        loaded_afk_bonus = AfkBonusTable()
        loaded_afk_bonus.read(os.path.join(base_path, "AfkBonus" + file_extension))
        loaded_quest = QuestTable()
        loaded_quest.read(os.path.join(base_path, "Quest" + file_extension))
        loaded_quest_step = QuestStepTable()
        loaded_quest_step.read(os.path.join(base_path, "QuestStep" + file_extension))
        loaded_daily_quest = DailyQuestTable()
        loaded_daily_quest.read(os.path.join(base_path, "DailyQuest" + file_extension))
        loaded_weekly_quest = WeeklyQuestTable()
        loaded_weekly_quest.read(os.path.join(base_path, "WeeklyQuest" + file_extension))
        loaded_achievement = AchievementTable()
        loaded_achievement.read(os.path.join(base_path, "Achievement" + file_extension))
        loaded_reward = RewardTable()
        loaded_reward.read(os.path.join(base_path, "Reward" + file_extension))
        loaded_loot_group = LootGroupTable()
        loaded_loot_group.read(os.path.join(base_path, "LootGroup" + file_extension))
        loaded_loot_entry = LootEntryTable()
        loaded_loot_entry.read(os.path.join(base_path, "LootEntry" + file_extension))
        loaded_mail_reward = MailRewardTable()
        loaded_mail_reward.read(os.path.join(base_path, "MailReward" + file_extension))
        loaded_setting = SettingTable()
        loaded_setting.read(os.path.join(base_path, "Setting" + file_extension))
        loaded_condition = ConditionTable()
        loaded_condition.read(os.path.join(base_path, "Condition" + file_extension))
        loaded_unlock = UnlockTable()
        loaded_unlock.read(os.path.join(base_path, "Unlock" + file_extension))
        loaded_table = TableTable()
        loaded_table.read(os.path.join(base_path, "Table" + file_extension))
        loaded_shop = ShopTable()
        loaded_shop.read(os.path.join(base_path, "Shop" + file_extension))
        loaded_shop_slot = ShopSlotTable()
        loaded_shop_slot.read(os.path.join(base_path, "ShopSlot" + file_extension))
        loaded_price = PriceTable()
        loaded_price.read(os.path.join(base_path, "Price" + file_extension))
        loaded_promo = PromoTable()
        loaded_promo.read(os.path.join(base_path, "Promo" + file_extension))
        loaded_skill = SkillTable()
        loaded_skill.read(os.path.join(base_path, "Skill" + file_extension))
        loaded_skill_level = SkillLevelTable()
        loaded_skill_level.read(os.path.join(base_path, "SkillLevel" + file_extension))
        loaded_skill_effect = SkillEffectTable()
        loaded_skill_effect.read(os.path.join(base_path, "SkillEffect" + file_extension))
        loaded_buff = BuffTable()
        loaded_buff.read(os.path.join(base_path, "Buff" + file_extension))
        loaded_buff_pool = BuffPoolTable()
        loaded_buff_pool.read(os.path.join(base_path, "BuffPool" + file_extension))
        loaded_passive = PassiveTable()
        loaded_passive.read(os.path.join(base_path, "Passive" + file_extension))
        loaded_stage = StageTable()
        loaded_stage.read(os.path.join(base_path, "Stage" + file_extension))
        loaded_stage_wave = StageWaveTable()
        loaded_stage_wave.read(os.path.join(base_path, "StageWave" + file_extension))
        loaded_stage_monster = StageMonsterTable()
        loaded_stage_monster.read(os.path.join(base_path, "StageMonster" + file_extension))
        loaded_stage_reward = StageRewardTable()
        loaded_stage_reward.read(os.path.join(base_path, "StageReward" + file_extension))
        loaded_chapter = ChapterTable()
        loaded_chapter.read(os.path.join(base_path, "Chapter" + file_extension))
        loaded_world = WorldTable()
        loaded_world.read(os.path.join(base_path, "World" + file_extension))

        self._solve_cross_references(loaded_character, loaded_character_stat, loaded_character_ascend, loaded_character_skin, loaded_character_voice, loaded_role_bonus, loaded_element_affinity, loaded_codex, loaded_codex_entry, loaded_codex_tier, loaded_dialogue, loaded_dialogue_line, loaded_cutscene, loaded_dungeon, loaded_dungeon_floor, loaded_dungeon_reward, loaded_raid, loaded_equip, loaded_equip_level, loaded_equip_grade, loaded_equip_set, loaded_equip_set_bonus, loaded_equip_option, loaded_gacha, loaded_gacha_pool, loaded_gacha_odds, loaded_pity, loaded_level_curve, loaded_stat_base, loaded_ascend_curve, loaded_power_score, loaded_item, loaded_currency, loaded_material, loaded_consumable, loaded_offer, loaded_offer_item, loaded_item_grade, loaded_string, loaded_offline_reward, loaded_idle_rate, loaded_afk_bonus, loaded_quest, loaded_quest_step, loaded_daily_quest, loaded_weekly_quest, loaded_achievement, loaded_reward, loaded_loot_group, loaded_loot_entry, loaded_mail_reward, loaded_setting, loaded_condition, loaded_unlock, loaded_table, loaded_shop, loaded_shop_slot, loaded_price, loaded_promo, loaded_skill, loaded_skill_level, loaded_skill_effect, loaded_buff, loaded_buff_pool, loaded_passive, loaded_stage, loaded_stage_wave, loaded_stage_monster, loaded_stage_reward, loaded_chapter, loaded_world)

        self.character = loaded_character
        self.character_stat = loaded_character_stat
        self.character_ascend = loaded_character_ascend
        self.character_skin = loaded_character_skin
        self.character_voice = loaded_character_voice
        self.role_bonus = loaded_role_bonus
        self.element_affinity = loaded_element_affinity
        self.codex = loaded_codex
        self.codex_entry = loaded_codex_entry
        self.codex_tier = loaded_codex_tier
        self.dialogue = loaded_dialogue
        self.dialogue_line = loaded_dialogue_line
        self.cutscene = loaded_cutscene
        self.dungeon = loaded_dungeon
        self.dungeon_floor = loaded_dungeon_floor
        self.dungeon_reward = loaded_dungeon_reward
        self.raid = loaded_raid
        self.equip = loaded_equip
        self.equip_level = loaded_equip_level
        self.equip_grade = loaded_equip_grade
        self.equip_set = loaded_equip_set
        self.equip_set_bonus = loaded_equip_set_bonus
        self.equip_option = loaded_equip_option
        self.gacha = loaded_gacha
        self.gacha_pool = loaded_gacha_pool
        self.gacha_odds = loaded_gacha_odds
        self.pity = loaded_pity
        self.level_curve = loaded_level_curve
        self.stat_base = loaded_stat_base
        self.ascend_curve = loaded_ascend_curve
        self.power_score = loaded_power_score
        self.item = loaded_item
        self.currency = loaded_currency
        self.material = loaded_material
        self.consumable = loaded_consumable
        self.offer = loaded_offer
        self.offer_item = loaded_offer_item
        self.item_grade = loaded_item_grade
        self.string = loaded_string
        self.offline_reward = loaded_offline_reward
        self.idle_rate = loaded_idle_rate
        self.afk_bonus = loaded_afk_bonus
        self.quest = loaded_quest
        self.quest_step = loaded_quest_step
        self.daily_quest = loaded_daily_quest
        self.weekly_quest = loaded_weekly_quest
        self.achievement = loaded_achievement
        self.reward = loaded_reward
        self.loot_group = loaded_loot_group
        self.loot_entry = loaded_loot_entry
        self.mail_reward = loaded_mail_reward
        self.setting = loaded_setting
        self.condition = loaded_condition
        self.unlock = loaded_unlock
        self.table = loaded_table
        self.shop = loaded_shop
        self.shop_slot = loaded_shop_slot
        self.price = loaded_price
        self.promo = loaded_promo
        self.skill = loaded_skill
        self.skill_level = loaded_skill_level
        self.skill_effect = loaded_skill_effect
        self.buff = loaded_buff
        self.buff_pool = loaded_buff_pool
        self.passive = loaded_passive
        self.stage = loaded_stage
        self.stage_wave = loaded_stage_wave
        self.stage_monster = loaded_stage_monster
        self.stage_reward = loaded_stage_reward
        self.chapter = loaded_chapter
        self.world = loaded_world

    def _solve_cross_references(self, character, character_stat, character_ascend, character_skin, character_voice, role_bonus, element_affinity, codex, codex_entry, codex_tier, dialogue, dialogue_line, cutscene, dungeon, dungeon_floor, dungeon_reward, raid, equip, equip_level, equip_grade, equip_set, equip_set_bonus, equip_option, gacha, gacha_pool, gacha_odds, pity, level_curve, stat_base, ascend_curve, power_score, item, currency, material, consumable, offer, offer_item, item_grade, string, offline_reward, idle_rate, afk_bonus, quest, quest_step, daily_quest, weekly_quest, achievement, reward, loot_group, loot_entry, mail_reward, setting, condition, unlock, table, shop, shop_slot, price, promo, skill, skill_level, skill_effect, buff, buff_pool, passive, stage, stage_wave, stage_monster, stage_reward, chapter, world):
        """Turns the stored indices into usable values, once every table is in memory.

        The tables arrive as arguments rather than off self, which is how this resolves the
        load being read rather than the one already published.
        """
        # No table references another.
        return
