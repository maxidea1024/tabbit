// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#pragma once

#include "CoreMinimal.h"
#include "UObject/ObjectMacros.h"
#include "Kismet/BlueprintFunctionLibrary.h"

#include "TabbitTcbReader.h"

// Must be the last include. UHT requires it, and moving it produces an error that
// names some other line entirely.
#include "FSproutData.generated.h"


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : A2
/** labels for GradeType */
UENUM(BlueprintType)
enum class EGradeType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Normal = 1 UMETA(DisplayName = "Normal"),
    Rare = 2 UMETA(DisplayName = "Rare"),
    Epic = 3 UMETA(DisplayName = "Epic"),
    Unique = 4 UMETA(DisplayName = "Unique"),
    Legend = 5 UMETA(DisplayName = "Legend"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : B2
/** labels for ElementType */
UENUM(BlueprintType)
enum class EElementType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Neutral = 1 UMETA(DisplayName = "Neutral"),
    Flame = 2 UMETA(DisplayName = "Flame"),
    Tide = 3 UMETA(DisplayName = "Tide"),
    Gale = 4 UMETA(DisplayName = "Gale"),
    Stone = 5 UMETA(DisplayName = "Stone"),
    Gloom = 6 UMETA(DisplayName = "Gloom"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : C2
/** labels for RoleType */
UENUM(BlueprintType)
enum class ERoleType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Vanguard = 1 UMETA(DisplayName = "Vanguard"),
    Skirmish = 2 UMETA(DisplayName = "Skirmish"),
    Support = 3 UMETA(DisplayName = "Support"),
    Arcane = 4 UMETA(DisplayName = "Arcane"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : D2
/** labels for RarityType */
UENUM(BlueprintType)
enum class ERarityType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    One = 1 UMETA(DisplayName = "One"),
    Two = 2 UMETA(DisplayName = "Two"),
    Three = 3 UMETA(DisplayName = "Three"),
    Four = 4 UMETA(DisplayName = "Four"),
    Five = 5 UMETA(DisplayName = "Five"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : E2
/** labels for CurrencyType */
UENUM(BlueprintType)
enum class ECurrencyType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Soft = 1 UMETA(DisplayName = "Soft"),
    Hard = 2 UMETA(DisplayName = "Hard"),
    Stamina = 3 UMETA(DisplayName = "Stamina"),
    Ticket = 4 UMETA(DisplayName = "Ticket"),
    Shard = 5 UMETA(DisplayName = "Shard"),
    Token = 6 UMETA(DisplayName = "Token"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : F2
/** labels for ItemType */
UENUM(BlueprintType)
enum class EItemType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Material = 1 UMETA(DisplayName = "Material"),
    Consumable = 2 UMETA(DisplayName = "Consumable"),
    Equip = 3 UMETA(DisplayName = "Equip"),
    Currency = 4 UMETA(DisplayName = "Currency"),
    Chest = 5 UMETA(DisplayName = "Chest"),
    Fragment = 6 UMETA(DisplayName = "Fragment"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : G2
/** labels for EquipSlot */
UENUM(BlueprintType)
enum class EEquipSlot : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Weapon = 1 UMETA(DisplayName = "Weapon"),
    Armor = 2 UMETA(DisplayName = "Armor"),
    Helm = 3 UMETA(DisplayName = "Helm"),
    Boots = 4 UMETA(DisplayName = "Boots"),
    Ring = 5 UMETA(DisplayName = "Ring"),
    Amulet = 6 UMETA(DisplayName = "Amulet"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : H2
/** labels for StatType */
UENUM(BlueprintType)
enum class EStatType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Attack = 1 UMETA(DisplayName = "Attack"),
    Defense = 2 UMETA(DisplayName = "Defense"),
    Health = 3 UMETA(DisplayName = "Health"),
    Crit = 4 UMETA(DisplayName = "Crit"),
    CritDamage = 5 UMETA(DisplayName = "CritDamage"),
    Speed = 6 UMETA(DisplayName = "Speed"),
    Evasion = 7 UMETA(DisplayName = "Evasion"),
    Accuracy = 8 UMETA(DisplayName = "Accuracy"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : I2
/** labels for TargetType */
UENUM(BlueprintType)
enum class ETargetType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    SelfOnly = 1 UMETA(DisplayName = "SelfOnly"),
    Ally = 2 UMETA(DisplayName = "Ally"),
    AllyAll = 3 UMETA(DisplayName = "AllyAll"),
    Enemy = 4 UMETA(DisplayName = "Enemy"),
    EnemyAll = 5 UMETA(DisplayName = "EnemyAll"),
    Random = 6 UMETA(DisplayName = "Random"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : J2
/** labels for ConditionType */
UENUM(BlueprintType)
enum class EConditionType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Level = 1 UMETA(DisplayName = "Level"),
    StageClear = 2 UMETA(DisplayName = "StageClear"),
    ItemOwned = 3 UMETA(DisplayName = "ItemOwned"),
    QuestDone = 4 UMETA(DisplayName = "QuestDone"),
    Collection = 5 UMETA(DisplayName = "Collection"),
    Login = 6 UMETA(DisplayName = "Login"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : K2
/** labels for RewardType */
UENUM(BlueprintType)
enum class ERewardType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Item = 1 UMETA(DisplayName = "Item"),
    Currency = 2 UMETA(DisplayName = "Currency"),
    Character = 3 UMETA(DisplayName = "Character"),
    Equip = 4 UMETA(DisplayName = "Equip"),
    Buff = 5 UMETA(DisplayName = "Buff"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : L2
/** labels for StageType */
UENUM(BlueprintType)
enum class EStageType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Story = 1 UMETA(DisplayName = "Story"),
    Elite = 2 UMETA(DisplayName = "Elite"),
    Event = 3 UMETA(DisplayName = "Event"),
    Trial = 4 UMETA(DisplayName = "Trial"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : M2
/** labels for DungeonType */
UENUM(BlueprintType)
enum class EDungeonType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Material = 1 UMETA(DisplayName = "Material"),
    Gold = 2 UMETA(DisplayName = "Gold"),
    Element = 3 UMETA(DisplayName = "Element"),
    Boss = 4 UMETA(DisplayName = "Boss"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : N2
/** labels for ShopType */
UENUM(BlueprintType)
enum class EShopType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Daily = 1 UMETA(DisplayName = "Daily"),
    Weekly = 2 UMETA(DisplayName = "Weekly"),
    Guild = 3 UMETA(DisplayName = "Guild"),
    Event = 4 UMETA(DisplayName = "Event"),
    Cash = 5 UMETA(DisplayName = "Cash"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : O2
/** labels for GachaType */
UENUM(BlueprintType)
enum class EGachaType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Standard = 1 UMETA(DisplayName = "Standard"),
    Limited = 2 UMETA(DisplayName = "Limited"),
    Beginner = 3 UMETA(DisplayName = "Beginner"),
    Step = 4 UMETA(DisplayName = "Step"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : P2
/** labels for QuestType */
UENUM(BlueprintType)
enum class EQuestType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Main = 1 UMETA(DisplayName = "Main"),
    Sub = 2 UMETA(DisplayName = "Sub"),
    Daily = 3 UMETA(DisplayName = "Daily"),
    Weekly = 4 UMETA(DisplayName = "Weekly"),
    Event = 5 UMETA(DisplayName = "Event"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : Q2
/** labels for DialogueSpeaker */
UENUM(BlueprintType)
enum class EDialogueSpeaker : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Narrator = 1 UMETA(DisplayName = "Narrator"),
    Hero = 2 UMETA(DisplayName = "Hero"),
    Ally = 3 UMETA(DisplayName = "Ally"),
    Villain = 4 UMETA(DisplayName = "Villain"),
    System = 5 UMETA(DisplayName = "System"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : R2
/** labels for UnlockType */
UENUM(BlueprintType)
enum class EUnlockType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Chapter = 1 UMETA(DisplayName = "Chapter"),
    Level = 2 UMETA(DisplayName = "Level"),
    Quest = 3 UMETA(DisplayName = "Quest"),
    Item = 4 UMETA(DisplayName = "Item"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : S2
/** labels for BuffKind */
UENUM(BlueprintType)
enum class EBuffKind : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Buff = 1 UMETA(DisplayName = "Buff"),
    Debuff = 2 UMETA(DisplayName = "Debuff"),
    Shield = 3 UMETA(DisplayName = "Shield"),
    Dot = 4 UMETA(DisplayName = "Dot"),
    Hot = 5 UMETA(DisplayName = "Hot"),
};


// Generated from samples/sprout/xlsx/Enums.xlsx : TableEnums : T2
/** labels for CollectionType */
UENUM(BlueprintType)
enum class ECollectionType : uint8
{
    /** None (automatically inserted by Tabbit) */
    None = 0 UMETA(DisplayName = "None"),
    Character = 1 UMETA(DisplayName = "Character"),
    Equip = 2 UMETA(DisplayName = "Equip"),
    Monster = 3 UMETA(DisplayName = "Monster"),
    Region = 4 UMETA(DisplayName = "Region"),
};


// Generated from samples/sprout/xlsx/Character.xlsx : CharacterTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCharacterRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    int32 Id = 0;

    /** display name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    FString NameKey;

    /** portrait asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    FString Portrait;

    /** combat role */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    ERoleType Role = static_cast<ERoleType>(0);

    /** element */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    EElementType Element = static_cast<EElementType>(0);

    /** initial grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** base rarity */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    ERarityType Rarity = static_cast<ERarityType>(0);

    /** skill ids, ';' between */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    TArray<int32> SkillIds;

    /** maximum level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    int32 MaxLevel = 0;

    /** released */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Character")
    bool bReleased = false;

};


// Generated from samples/sprout/xlsx/Character.xlsx : CharacterStatTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCharacterStatRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterStat")
    int32 Id = 0;

    /** owning character */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterStat")
    int32 CharacterId = 0;

    /** level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterStat")
    int32 Level = 0;

    /** required experience */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterStat")
    int64 RequiredExp = 0;

    /** attack at this level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterStat")
    int32 Attack = 0;

    /** defense at this level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterStat")
    int32 Defense = 0;

    /** health at this level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterStat")
    int32 Health = 0;

};


// Generated from samples/sprout/xlsx/Character.xlsx : CharacterAscendTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCharacterAscendRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterAscend")
    int32 Id = 0;

    /** owning character */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterAscend")
    int32 CharacterId = 0;

    /** ascension step */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterAscend")
    int32 Step = 0;

    /** material item ids */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterAscend")
    TArray<int32> MaterialIds;

    /** material counts */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterAscend")
    TArray<int32> MaterialCounts;

    /** soft currency cost */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterAscend")
    int64 GoldCost = 0;

    /** stat multiplier */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterAscend")
    float StatScale = 0.0f;

};


// Generated from samples/sprout/xlsx/Character.xlsx : CharacterSkinTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCharacterSkinRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterSkin")
    int32 Id = 0;

    /** owning character */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterSkin")
    int32 CharacterId = 0;

    /** skin name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterSkin")
    FString NameKey;

    /** model asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterSkin")
    FString Model;

    /** shop price */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterSkin")
    int32 Price = 0;

    /** default skin */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterSkin")
    bool bIsDefault = false;

};


// Generated from samples/sprout/xlsx/Character.xlsx : CharacterVoiceTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCharacterVoiceRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterVoice")
    int32 Id = 0;

    /** owning character */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterVoice")
    int32 CharacterId = 0;

    /** voice cue */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterVoice")
    FString Cue;

    /** audio asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterVoice")
    FString Clip;

    /** subtitle key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CharacterVoice")
    FString SubtitleKey;

};


// Generated from samples/sprout/xlsx/Character.xlsx : RoleBonusTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FRoleBonusRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "RoleBonus")
    int32 Id = 0;

    /** role */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "RoleBonus")
    ERoleType Role = static_cast<ERoleType>(0);

    /** stat */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "RoleBonus")
    EStatType Stat = static_cast<EStatType>(0);

    /** bonus in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "RoleBonus")
    int32 Bonus = 0;

};


// Generated from samples/sprout/xlsx/Character.xlsx : ElementAffinityTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FElementAffinityRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ElementAffinity")
    int32 Id = 0;

    /** attacking element */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ElementAffinity")
    EElementType Attacker = static_cast<EElementType>(0);

    /** defending element */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ElementAffinity")
    EElementType Defender = static_cast<EElementType>(0);

    /** damage rate in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ElementAffinity")
    int32 Rate = 0;

};


// Generated from samples/sprout/xlsx/Codex.xlsx : CodexTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCodexRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Codex")
    int32 Id = 0;

    /** collection kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Codex")
    ECollectionType Type = static_cast<ECollectionType>(0);

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Codex")
    FString NameKey;

    /** icon asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Codex")
    FString Icon;

    /** completion reward */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Codex")
    int32 RewardId = 0;

};


// Generated from samples/sprout/xlsx/Codex.xlsx : CodexEntryTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCodexEntryRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexEntry")
    int32 Id = 0;

    /** owning codex */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexEntry")
    int32 CodexId = 0;

    /** entry order */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexEntry")
    int32 EntryOrder = 0;

    /** registration condition */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexEntry")
    int32 ConditionId = 0;

    /** affected stat */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexEntry")
    EStatType Stat = static_cast<EStatType>(0);

    /** bonus in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexEntry")
    int32 Bonus = 0;

};


// Generated from samples/sprout/xlsx/Codex.xlsx : CodexTierTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCodexTierRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexTier")
    int32 Id = 0;

    /** owning codex */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexTier")
    int32 CodexId = 0;

    /** entries required */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexTier")
    int32 Required = 0;

    /** reward */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "CodexTier")
    int32 RewardId = 0;

};


// Generated from samples/sprout/xlsx/Dialogue.xlsx : DialogueTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FDialogueRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dialogue")
    int32 Id = 0;

    /** scene key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dialogue")
    FString SceneKey;

    /** cutscene, 0 for none */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dialogue")
    int32 CutsceneId = 0;

    /** skippable */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dialogue")
    bool bSkippable = false;

};


// Generated from samples/sprout/xlsx/Dialogue.xlsx : DialogueLineTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FDialogueLineRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DialogueLine")
    int32 Id = 0;

    /** owning dialogue */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DialogueLine")
    int32 DialogueId = 0;

    /** line order */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DialogueLine")
    int32 LineOrder = 0;

    /** speaker */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DialogueLine")
    EDialogueSpeaker Speaker = static_cast<EDialogueSpeaker>(0);

    /** portrait asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DialogueLine")
    FString Portrait;

    /** line key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DialogueLine")
    FString LineKey;

};


// Generated from samples/sprout/xlsx/Dialogue.xlsx : CutsceneTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCutsceneRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Cutscene")
    int32 Id = 0;

    /** timeline asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Cutscene")
    FString Timeline;

    /** length in milliseconds */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Cutscene")
    int32 LengthMs = 0;

    /** skippable after milliseconds */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Cutscene")
    int32 SkipAfterMs = 0;

};


// Generated from samples/sprout/xlsx/Dungeon.xlsx : DungeonTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FDungeonRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dungeon")
    int32 Id = 0;

    /** dungeon kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dungeon")
    EDungeonType Type = static_cast<EDungeonType>(0);

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dungeon")
    FString NameKey;

    /** element */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dungeon")
    EElementType Element = static_cast<EElementType>(0);

    /** daily entries */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dungeon")
    int32 DailyEntries = 0;

    /** unlock condition */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Dungeon")
    int32 UnlockId = 0;

};


// Generated from samples/sprout/xlsx/Dungeon.xlsx : DungeonFloorTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FDungeonFloorRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonFloor")
    int32 Id = 0;

    /** owning dungeon */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonFloor")
    int32 DungeonId = 0;

    /** floor */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonFloor")
    int32 Floor = 0;

    /** recommended power */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonFloor")
    int32 Power = 0;

    /** stamina cost */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonFloor")
    int32 Stamina = 0;

    /** reward */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonFloor")
    int32 RewardId = 0;

};


// Generated from samples/sprout/xlsx/Dungeon.xlsx : DungeonRewardTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FDungeonRewardRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonReward")
    int32 Id = 0;

    /** granted item */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonReward")
    int32 ItemId = 0;

    /** lowest count */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonReward")
    int32 MinCount = 0;

    /** highest count */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonReward")
    int32 MaxCount = 0;

    /** chance in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DungeonReward")
    int32 Chance = 0;

};


// Generated from samples/sprout/xlsx/Dungeon.xlsx : RaidTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FRaidRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Raid")
    int32 Id = 0;

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Raid")
    FString NameKey;

    /** boss monster */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Raid")
    int32 BossId = 0;

    /** health in millions */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Raid")
    int64 BossHealth = 0;

    /** turn limit */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Raid")
    int32 TurnLimit = 0;

    /** season length in days */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Raid")
    int32 SeasonDays = 0;

};


// Generated from samples/sprout/xlsx/Equipment.xlsx : EquipTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FEquipRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Equip")
    int32 Id = 0;

    /** equipment name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Equip")
    FString NameKey;

    /** slot */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Equip")
    EEquipSlot Slot = static_cast<EEquipSlot>(0);

    /** grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Equip")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** model asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Equip")
    FString Model;

    /** set, 0 for none */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Equip")
    int32 SetId = 0;

    /** base attack */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Equip")
    int32 Attack = 0;

    /** base defense */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Equip")
    int32 Defense = 0;

    /** maximum level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Equip")
    int32 MaxLevel = 0;

};


// Generated from samples/sprout/xlsx/Equipment.xlsx : EquipLevelTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FEquipLevelRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipLevel")
    int32 Id = 0;

    /** grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipLevel")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipLevel")
    int32 Level = 0;

    /** stat multiplier in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipLevel")
    int32 StatScale = 0;

    /** upgrade cost */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipLevel")
    int64 Cost = 0;

};


// Generated from samples/sprout/xlsx/Equipment.xlsx : EquipGradeTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FEquipGradeRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipGrade")
    int32 Id = 0;

    /** grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipGrade")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** option slots */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipGrade")
    int32 OptionSlots = 0;

    /** frame asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipGrade")
    FString Frame;

};


// Generated from samples/sprout/xlsx/Equipment.xlsx : EquipSetTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FEquipSetRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipSet")
    int32 Id = 0;

    /** set name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipSet")
    FString NameKey;

    /** member equipment ids */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipSet")
    TArray<int32> MemberIds;

};


// Generated from samples/sprout/xlsx/Equipment.xlsx : EquipSetBonusTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FEquipSetBonusRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipSetBonus")
    int32 Id = 0;

    /** owning set */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipSetBonus")
    int32 SetId = 0;

    /** pieces required */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipSetBonus")
    int32 Pieces = 0;

    /** affected stat */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipSetBonus")
    EStatType Stat = static_cast<EStatType>(0);

    /** bonus in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipSetBonus")
    int32 Bonus = 0;

};


// Generated from samples/sprout/xlsx/Equipment.xlsx : EquipOptionTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FEquipOptionRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipOption")
    int32 Id = 0;

    /** affected stat */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipOption")
    EStatType Stat = static_cast<EStatType>(0);

    /** lowest roll in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipOption")
    int32 MinRoll = 0;

    /** highest roll in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipOption")
    int32 MaxRoll = 0;

    /** weight */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "EquipOption")
    int32 Weight = 0;

};


// Generated from samples/sprout/xlsx/Gacha.xlsx : GachaTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FGachaRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Gacha")
    int32 Id = 0;

    /** gacha kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Gacha")
    EGachaType Type = static_cast<EGachaType>(0);

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Gacha")
    FString NameKey;

    /** banner asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Gacha")
    FString Banner;

    /** cost */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Gacha")
    int32 PriceId = 0;

    /** pity counter */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Gacha")
    int32 PityId = 0;

};


// Generated from samples/sprout/xlsx/Gacha.xlsx : GachaPoolTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FGachaPoolRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaPool")
    int32 Id = 0;

    /** owning gacha */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaPool")
    int32 GachaId = 0;

    /** granted character, 0 for none */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaPool")
    int32 CharacterId = 0;

    /** granted equipment, 0 for none */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaPool")
    int32 EquipId = 0;

    /** grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaPool")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** weight */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaPool")
    int32 Weight = 0;

};


// Generated from samples/sprout/xlsx/Gacha.xlsx : GachaOddsTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FGachaOddsRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaOdds")
    int32 Id = 0;

    /** owning gacha */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaOdds")
    int32 GachaId = 0;

    /** grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaOdds")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** rate in ten-thousandths */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "GachaOdds")
    int32 Rate = 0;

};


// Generated from samples/sprout/xlsx/Gacha.xlsx : PityTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FPityRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Pity")
    int32 Id = 0;

    /** draws before the guarantee */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Pity")
    int32 Threshold = 0;

    /** guaranteed grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Pity")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** resets on hit */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Pity")
    bool bResetOnHit = false;

};


// Generated from samples/sprout/xlsx/Growth.xlsx : LevelCurveTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FLevelCurveRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LevelCurve")
    int32 Id = 0;

    /** curve tier */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LevelCurve")
    FString TierName;

    /** affected stat */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LevelCurve")
    EStatType CurveStat = static_cast<EStatType>(0);

    /** tick within the curve */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LevelCurve")
    int32 Tick = 0;

    /** value at the start of the step */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LevelCurve")
    int32 BaseValue = 0;

    /** increment per step */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LevelCurve")
    int32 StepValue = 0;

    /** value at the end of the step */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LevelCurve")
    int64 EndValue = 0;

    /** ceiling for this curve */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LevelCurve")
    int64 CapValue = 0;

    /** balance revision */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LevelCurve")
    int32 Revision = 0;

};


// Generated from samples/sprout/xlsx/Growth.xlsx : StatBaseTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FStatBaseRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StatBase")
    int32 Id = 0;

    /** role */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StatBase")
    ERoleType Role = static_cast<ERoleType>(0);

    /** affected stat */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StatBase")
    EStatType Stat = static_cast<EStatType>(0);

    /** base value */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StatBase")
    int32 BaseValue = 0;

};


// Generated from samples/sprout/xlsx/Growth.xlsx : AscendCurveTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FAscendCurveRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "AscendCurve")
    int32 Id = 0;

    /** grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "AscendCurve")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** ascension step */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "AscendCurve")
    int32 Step = 0;

    /** multiplier in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "AscendCurve")
    int32 Scale = 0;

};


// Generated from samples/sprout/xlsx/Growth.xlsx : PowerScoreTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FPowerScoreRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "PowerScore")
    int32 Id = 0;

    /** affected stat */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "PowerScore")
    EStatType Stat = static_cast<EStatType>(0);

    /** weight in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "PowerScore")
    int32 Weight = 0;

};


// Generated from samples/sprout/xlsx/Item.xlsx : ItemTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FItemRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Item")
    int32 Id = 0;

    /** item name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Item")
    FString NameKey;

    /** item kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Item")
    EItemType Type = static_cast<EItemType>(0);

    /** grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Item")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** icon asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Item")
    FString Icon;

    /** stack ceiling */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Item")
    int32 StackLimit = 0;

    /** sell price */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Item")
    int32 SellPrice = 0;

    /** tradable */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Item")
    bool bTradable = false;

};


// Generated from samples/sprout/xlsx/Item.xlsx : CurrencyTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FCurrencyRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Currency")
    int32 Id = 0;

    /** currency kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Currency")
    ECurrencyType Type = static_cast<ECurrencyType>(0);

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Currency")
    FString NameKey;

    /** icon asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Currency")
    FString Icon;

    /** hard cap, 0 for none */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Currency")
    int64 Cap = 0;

};


// Generated from samples/sprout/xlsx/Item.xlsx : MaterialTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FMaterialRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Material")
    int32 Id = 0;

    /** backing item */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Material")
    int32 ItemId = 0;

    /** tier */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Material")
    int32 Tier = 0;

    /** element */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Material")
    EElementType Element = static_cast<EElementType>(0);

};


// Generated from samples/sprout/xlsx/Item.xlsx : ConsumableTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FConsumableRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Consumable")
    int32 Id = 0;

    /** backing item */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Consumable")
    int32 ItemId = 0;

    /** granted buff */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Consumable")
    int32 BuffId = 0;

    /** cooldown in seconds */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Consumable")
    int32 Cooldown = 0;

};


// Generated from samples/sprout/xlsx/Item.xlsx : OfferTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FOfferRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Offer")
    int32 Id = 0;

    /** package name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Offer")
    FString NameKey;

    /** shop kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Offer")
    EShopType Shop = static_cast<EShopType>(0);

    /** price in hard currency */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Offer")
    int32 Price = 0;

    /** purchase limit, 0 for none */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Offer")
    int32 Limit = 0;

};


// Generated from samples/sprout/xlsx/Item.xlsx : OfferItemTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FOfferItemRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "OfferItem")
    int32 Id = 0;

    /** owning offer */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "OfferItem")
    int32 OfferId = 0;

    /** granted item */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "OfferItem")
    int32 ItemId = 0;

    /** count */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "OfferItem")
    int32 Count = 0;

};


// Generated from samples/sprout/xlsx/Item.xlsx : ItemGradeTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FItemGradeRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ItemGrade")
    int32 Id = 0;

    /** grade */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ItemGrade")
    EGradeType Grade = static_cast<EGradeType>(0);

    /** frame asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ItemGrade")
    FString Frame;

    /** sell multiplier in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ItemGrade")
    int32 SellScale = 0;

};


// Generated from samples/sprout/xlsx/Localization.xlsx : StringTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FStringRow
{
    GENERATED_BODY()

    /** string key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "String")
    FString StringKey;

    /** English */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "String")
    FString En;

    /** Korean */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "String")
    FString Ko;

    /** Japanese */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "String")
    FString Ja;

};


// Generated from samples/sprout/xlsx/Offline.xlsx : OfflineRewardTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FOfflineRewardRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "OfflineReward")
    int32 Id = 0;

    /** account level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "OfflineReward")
    int32 AccountLevel = 0;

    /** soft currency per hour */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "OfflineReward")
    int64 GoldPerHour = 0;

    /** experience per hour */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "OfflineReward")
    int64 ExpPerHour = 0;

    /** drop group */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "OfflineReward")
    int32 LootGroupId = 0;

};


// Generated from samples/sprout/xlsx/Offline.xlsx : IdleRateTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FIdleRateRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "IdleRate")
    int32 Id = 0;

    /** highest stage cleared */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "IdleRate")
    int32 StageId = 0;

    /** rate in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "IdleRate")
    int32 Rate = 0;

    /** accumulation cap in hours */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "IdleRate")
    int32 CapHours = 0;

};


// Generated from samples/sprout/xlsx/Offline.xlsx : AfkBonusTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FAfkBonusRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "AfkBonus")
    int32 Id = 0;

    /** hours away */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "AfkBonus")
    int32 Hours = 0;

    /** bonus in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "AfkBonus")
    int32 Bonus = 0;

};


// Generated from samples/sprout/xlsx/Quest.xlsx : QuestTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FQuestRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Quest")
    int32 Id = 0;

    /** quest kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Quest")
    EQuestType Type = static_cast<EQuestType>(0);

    /** title key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Quest")
    FString TitleKey;

    /** summary key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Quest")
    FString SummaryKey;

    /** reward */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Quest")
    int32 RewardId = 0;

    /** unlock condition */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Quest")
    int32 UnlockId = 0;

    /** repeatable */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Quest")
    bool bRepeatable = false;

};


// Generated from samples/sprout/xlsx/Quest.xlsx : QuestStepTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FQuestStepRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "QuestStep")
    int32 Id = 0;

    /** owning quest */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "QuestStep")
    int32 QuestId = 0;

    /** step order */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "QuestStep")
    int32 StepOrder = 0;

    /** completion condition */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "QuestStep")
    int32 ConditionId = 0;

    /** target count */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "QuestStep")
    int32 TargetCount = 0;

    /** dialogue shown on completion */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "QuestStep")
    int32 DialogueId = 0;

};


// Generated from samples/sprout/xlsx/Quest.xlsx : DailyQuestTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FDailyQuestRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DailyQuest")
    int32 Id = 0;

    /** backing quest */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DailyQuest")
    int32 QuestId = 0;

    /** points awarded */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DailyQuest")
    int32 Points = 0;

    /** reset hour, server time */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "DailyQuest")
    int32 ResetHour = 0;

};


// Generated from samples/sprout/xlsx/Quest.xlsx : WeeklyQuestTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FWeeklyQuestRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "WeeklyQuest")
    int32 Id = 0;

    /** backing quest */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "WeeklyQuest")
    int32 QuestId = 0;

    /** points awarded */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "WeeklyQuest")
    int32 Points = 0;

    /** reset weekday, Monday is 0 */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "WeeklyQuest")
    int32 ResetWeekday = 0;

};


// Generated from samples/sprout/xlsx/Quest.xlsx : AchievementTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FAchievementRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Achievement")
    int32 Id = 0;

    /** title key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Achievement")
    FString TitleKey;

    /** completion condition */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Achievement")
    int32 ConditionId = 0;

    /** target count */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Achievement")
    int32 TargetCount = 0;

    /** reward */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Achievement")
    int32 RewardId = 0;

    /** badge asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Achievement")
    FString Badge;

};


// Generated from samples/sprout/xlsx/Reward.xlsx : RewardTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FRewardRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Reward")
    int32 Id = 0;

    /** reward kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Reward")
    ERewardType Type = static_cast<ERewardType>(0);

    /** drop group, 0 for a fixed reward */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Reward")
    int32 LootGroupId = 0;

    /** fixed item ids */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Reward")
    TArray<int32> ItemIds;

    /** fixed item counts */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Reward")
    TArray<int32> ItemCounts;

};


// Generated from samples/sprout/xlsx/Reward.xlsx : LootGroupTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FLootGroupRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootGroup")
    int32 Id = 0;

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootGroup")
    FString NameKey;

    /** draws per open */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootGroup")
    int32 Draws = 0;

    /** duplicates allowed */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootGroup")
    bool bAllowDuplicate = false;

};


// Generated from samples/sprout/xlsx/Reward.xlsx : LootEntryTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FLootEntryRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootEntry")
    int32 Id = 0;

    /** owning group */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootEntry")
    int32 LootGroupId = 0;

    /** granted item */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootEntry")
    int32 ItemId = 0;

    /** lowest count */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootEntry")
    int32 MinCount = 0;

    /** highest count */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootEntry")
    int32 MaxCount = 0;

    /** weight */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "LootEntry")
    int32 Weight = 0;

};


// Generated from samples/sprout/xlsx/Reward.xlsx : MailRewardTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FMailRewardRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "MailReward")
    int32 Id = 0;

    /** title key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "MailReward")
    FString TitleKey;

    /** body key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "MailReward")
    FString BodyKey;

    /** reward */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "MailReward")
    int32 RewardId = 0;

    /** expiry in days */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "MailReward")
    int32 ExpiryDays = 0;

};


// Generated from samples/sprout/xlsx/Setting.xlsx : SettingTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FSettingRow
{
    GENERATED_BODY()

    /** setting key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Setting")
    FString SettingKey;

    /** value as written */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Setting")
    FString Value;

    /** description key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Setting")
    FString DescKey;

};


// Generated from samples/sprout/xlsx/Setting.xlsx : ConditionTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FConditionRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Condition")
    int32 Id = 0;

    /** condition kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Condition")
    EConditionType Type = static_cast<EConditionType>(0);

    /** compared value */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Condition")
    int32 Value = 0;

    /** referenced table row, 0 for none */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Condition")
    int32 TargetId = 0;

    /** description key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Condition")
    FString DescKey;

};


// Generated from samples/sprout/xlsx/Setting.xlsx : UnlockTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FUnlockRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Unlock")
    int32 Id = 0;

    /** unlock kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Unlock")
    EUnlockType Type = static_cast<EUnlockType>(0);

    /** condition */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Unlock")
    int32 ConditionId = 0;

    /** locked message key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Unlock")
    FString MessageKey;

};


// Generated from samples/sprout/xlsx/Setting.xlsx : Table : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FTableRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Table")
    int32 Id = 0;

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Table")
    FString NameKey;

    /** value */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Table")
    int32 Value = 0;

};


// Generated from samples/sprout/xlsx/Shop.xlsx : ShopTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FShopRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Shop")
    int32 Id = 0;

    /** shop kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Shop")
    EShopType Type = static_cast<EShopType>(0);

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Shop")
    FString NameKey;

    /** refresh hours */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Shop")
    int32 RefreshHours = 0;

    /** free refreshes per day */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Shop")
    int32 FreeRefresh = 0;

};


// Generated from samples/sprout/xlsx/Shop.xlsx : ShopSlotTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FShopSlotRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopSlot")
    int32 Id = 0;

    /** owning shop */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopSlot")
    int32 ShopId = 0;

    /** slot order */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopSlot")
    int32 SlotOrder = 0;

    /** sold item */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopSlot")
    int32 ItemId = 0;

    /** count */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopSlot")
    int32 Count = 0;

    /** price */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopSlot")
    int32 PriceId = 0;

    /** appearance weight */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "ShopSlot")
    int32 Weight = 0;

};


// Generated from samples/sprout/xlsx/Shop.xlsx : PriceTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FPriceRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Price")
    int32 Id = 0;

    /** currency */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Price")
    ECurrencyType Currency = static_cast<ECurrencyType>(0);

    /** amount */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Price")
    int32 Amount = 0;

    /** discount in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Price")
    int32 Discount = 0;

};


// Generated from samples/sprout/xlsx/Shop.xlsx : PromoTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FPromoRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Promo")
    int32 Id = 0;

    /** backing offer */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Promo")
    int32 OfferId = 0;

    /** banner asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Promo")
    FString Banner;

    /** available days */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Promo")
    int32 AvailableDays = 0;

};


// Generated from samples/sprout/xlsx/Skill.xlsx : SkillTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FSkillRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Skill")
    int32 Id = 0;

    /** skill name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Skill")
    FString NameKey;

    /** description key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Skill")
    FString DescKey;

    /** icon asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Skill")
    FString Icon;

    /** target rule */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Skill")
    ETargetType Target = static_cast<ETargetType>(0);

    /** cooldown in turns */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Skill")
    int32 Cooldown = 0;

    /** effect ids */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Skill")
    TArray<int32> EffectIds;

    /** maximum skill level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Skill")
    int32 MaxLevel = 0;

};


// Generated from samples/sprout/xlsx/Skill.xlsx : SkillLevelTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FSkillLevelRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillLevel")
    int32 Id = 0;

    /** owning skill */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillLevel")
    int32 SkillId = 0;

    /** skill level */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillLevel")
    int32 Level = 0;

    /** power in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillLevel")
    int32 Power = 0;

    /** upgrade cost */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillLevel")
    int64 Cost = 0;

};


// Generated from samples/sprout/xlsx/Skill.xlsx : SkillEffectTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FSkillEffectRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillEffect")
    int32 Id = 0;

    /** effect kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillEffect")
    EBuffKind Kind = static_cast<EBuffKind>(0);

    /** affected stat */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillEffect")
    EStatType Stat = static_cast<EStatType>(0);

    /** magnitude in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillEffect")
    int32 Magnitude = 0;

    /** duration in turns */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillEffect")
    int32 Duration = 0;

    /** chance in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "SkillEffect")
    int32 Chance = 0;

};


// Generated from samples/sprout/xlsx/Skill.xlsx : BuffTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FBuffRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Buff")
    int32 Id = 0;

    /** buff name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Buff")
    FString NameKey;

    /** kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Buff")
    EBuffKind Kind = static_cast<EBuffKind>(0);

    /** icon asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Buff")
    FString Icon;

    /** stacks allowed */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Buff")
    int32 StackLimit = 0;

    /** dispellable */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Buff")
    bool bDispellable = false;

};


// Generated from samples/sprout/xlsx/Skill.xlsx : BuffPoolTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FBuffPoolRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "BuffPool")
    int32 Id = 0;

    /** candidate buffs */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "BuffPool")
    TArray<int32> BuffIds;

    /** weights, one per candidate */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "BuffPool")
    TArray<int32> Weights;

};


// Generated from samples/sprout/xlsx/Skill.xlsx : PassiveTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FPassiveRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Passive")
    int32 Id = 0;

    /** owning character */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Passive")
    int32 CharacterId = 0;

    /** unlock ascension step */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Passive")
    int32 UnlockStep = 0;

    /** affected stat */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Passive")
    EStatType Stat = static_cast<EStatType>(0);

    /** bonus in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Passive")
    int32 Bonus = 0;

};


// Generated from samples/sprout/xlsx/Stage.xlsx : StageTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FStageRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    int32 Id = 0;

    /** owning chapter */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    int32 ChapterId = 0;

    /** stage kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    EStageType Type = static_cast<EStageType>(0);

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    FString NameKey;

    /** background asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    FString Background;

    /** stamina cost */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    int32 Stamina = 0;

    /** recommended power */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    int32 Power = 0;

    /** first clear reward */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    int32 FirstReward = 0;

    /** repeat reward */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    int32 RepeatReward = 0;

    /** unlock condition */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Stage")
    int32 UnlockId = 0;

};


// Generated from samples/sprout/xlsx/Stage.xlsx : StageWaveTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FStageWaveRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageWave")
    int32 Id = 0;

    /** owning stage */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageWave")
    int32 StageId = 0;

    /** wave order */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageWave")
    int32 WaveOrder = 0;

    /** monster ids */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageWave")
    TArray<int32> MonsterIds;

    /** monster levels */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageWave")
    TArray<int32> MonsterLevels;

};


// Generated from samples/sprout/xlsx/Stage.xlsx : StageMonsterTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FStageMonsterRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageMonster")
    int32 Id = 0;

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageMonster")
    FString NameKey;

    /** model asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageMonster")
    FString Model;

    /** element */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageMonster")
    EElementType Element = static_cast<EElementType>(0);

    /** attack */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageMonster")
    int32 Attack = 0;

    /** defense */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageMonster")
    int32 Defense = 0;

    /** health */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageMonster")
    int64 Health = 0;

    /** skill ids */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageMonster")
    TArray<int32> SkillIds;

};


// Generated from samples/sprout/xlsx/Stage.xlsx : StageRewardTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FStageRewardRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    int32 Id = 0;

    /** owning stage */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    int32 StageId = 0;

    /** reward kind */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    ERewardType Type = static_cast<ERewardType>(0);

    /** granted item */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    int32 ItemId = 0;

    /** count */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    int32 Count = 0;

    /** chance in permille */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "StageReward")
    int32 Chance = 0;

};


// Generated from samples/sprout/xlsx/Stage.xlsx : ChapterTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FChapterRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Chapter")
    int32 Id = 0;

    /** owning world */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Chapter")
    int32 WorldId = 0;

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Chapter")
    FString NameKey;

    /** order */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Chapter")
    int32 SortOrder = 0;

    /** unlock condition */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Chapter")
    int32 UnlockId = 0;

};


// Generated from samples/sprout/xlsx/Stage.xlsx : WorldTable : A1
USTRUCT(BlueprintType)
struct SPROUTDATA_API FWorldRow
{
    GENERATED_BODY()

    /** primary index */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "World")
    int32 Id = 0;

    /** name key */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "World")
    FString NameKey;

    /** map asset */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "World")
    FString Map;

    /** theme colour */
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "World")
    FString ThemeColour;

};


/** Every row of Character. */
class SPROUTDATA_API FCharacterTable
{
public:
    const TArray<FCharacterRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCharacterRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCharacterRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of CharacterStat. */
class SPROUTDATA_API FCharacterStatTable
{
public:
    const TArray<FCharacterStatRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCharacterStatRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCharacterStatRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of CharacterAscend. */
class SPROUTDATA_API FCharacterAscendTable
{
public:
    const TArray<FCharacterAscendRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCharacterAscendRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCharacterAscendRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of CharacterSkin. */
class SPROUTDATA_API FCharacterSkinTable
{
public:
    const TArray<FCharacterSkinRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCharacterSkinRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCharacterSkinRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of CharacterVoice. */
class SPROUTDATA_API FCharacterVoiceTable
{
public:
    const TArray<FCharacterVoiceRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCharacterVoiceRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCharacterVoiceRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of RoleBonus. */
class SPROUTDATA_API FRoleBonusTable
{
public:
    const TArray<FRoleBonusRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FRoleBonusRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FRoleBonusRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of ElementAffinity. */
class SPROUTDATA_API FElementAffinityTable
{
public:
    const TArray<FElementAffinityRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FElementAffinityRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FElementAffinityRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Codex. */
class SPROUTDATA_API FCodexTable
{
public:
    const TArray<FCodexRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCodexRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCodexRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of CodexEntry. */
class SPROUTDATA_API FCodexEntryTable
{
public:
    const TArray<FCodexEntryRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCodexEntryRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCodexEntryRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of CodexTier. */
class SPROUTDATA_API FCodexTierTable
{
public:
    const TArray<FCodexTierRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCodexTierRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCodexTierRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Dialogue. */
class SPROUTDATA_API FDialogueTable
{
public:
    const TArray<FDialogueRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FDialogueRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FDialogueRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of DialogueLine. */
class SPROUTDATA_API FDialogueLineTable
{
public:
    const TArray<FDialogueLineRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FDialogueLineRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FDialogueLineRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Cutscene. */
class SPROUTDATA_API FCutsceneTable
{
public:
    const TArray<FCutsceneRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCutsceneRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCutsceneRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Dungeon. */
class SPROUTDATA_API FDungeonTable
{
public:
    const TArray<FDungeonRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FDungeonRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FDungeonRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of DungeonFloor. */
class SPROUTDATA_API FDungeonFloorTable
{
public:
    const TArray<FDungeonFloorRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FDungeonFloorRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FDungeonFloorRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of DungeonReward. */
class SPROUTDATA_API FDungeonRewardTable
{
public:
    const TArray<FDungeonRewardRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FDungeonRewardRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FDungeonRewardRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Raid. */
class SPROUTDATA_API FRaidTable
{
public:
    const TArray<FRaidRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FRaidRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FRaidRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Equip. */
class SPROUTDATA_API FEquipTable
{
public:
    const TArray<FEquipRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FEquipRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FEquipRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of EquipLevel. */
class SPROUTDATA_API FEquipLevelTable
{
public:
    const TArray<FEquipLevelRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FEquipLevelRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FEquipLevelRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of EquipGrade. */
class SPROUTDATA_API FEquipGradeTable
{
public:
    const TArray<FEquipGradeRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FEquipGradeRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FEquipGradeRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of EquipSet. */
class SPROUTDATA_API FEquipSetTable
{
public:
    const TArray<FEquipSetRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FEquipSetRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FEquipSetRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of EquipSetBonus. */
class SPROUTDATA_API FEquipSetBonusTable
{
public:
    const TArray<FEquipSetBonusRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FEquipSetBonusRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FEquipSetBonusRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of EquipOption. */
class SPROUTDATA_API FEquipOptionTable
{
public:
    const TArray<FEquipOptionRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FEquipOptionRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FEquipOptionRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Gacha. */
class SPROUTDATA_API FGachaTable
{
public:
    const TArray<FGachaRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FGachaRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FGachaRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of GachaPool. */
class SPROUTDATA_API FGachaPoolTable
{
public:
    const TArray<FGachaPoolRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FGachaPoolRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FGachaPoolRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of GachaOdds. */
class SPROUTDATA_API FGachaOddsTable
{
public:
    const TArray<FGachaOddsRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FGachaOddsRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FGachaOddsRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Pity. */
class SPROUTDATA_API FPityTable
{
public:
    const TArray<FPityRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FPityRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FPityRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of LevelCurve. */
class SPROUTDATA_API FLevelCurveTable
{
public:
    const TArray<FLevelCurveRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FLevelCurveRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FLevelCurveRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of StatBase. */
class SPROUTDATA_API FStatBaseTable
{
public:
    const TArray<FStatBaseRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FStatBaseRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FStatBaseRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of AscendCurve. */
class SPROUTDATA_API FAscendCurveTable
{
public:
    const TArray<FAscendCurveRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FAscendCurveRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FAscendCurveRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of PowerScore. */
class SPROUTDATA_API FPowerScoreTable
{
public:
    const TArray<FPowerScoreRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FPowerScoreRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FPowerScoreRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Item. */
class SPROUTDATA_API FItemTable
{
public:
    const TArray<FItemRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FItemRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FItemRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Currency. */
class SPROUTDATA_API FCurrencyTable
{
public:
    const TArray<FCurrencyRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FCurrencyRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FCurrencyRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Material. */
class SPROUTDATA_API FMaterialTable
{
public:
    const TArray<FMaterialRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FMaterialRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FMaterialRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Consumable. */
class SPROUTDATA_API FConsumableTable
{
public:
    const TArray<FConsumableRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FConsumableRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FConsumableRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Offer. */
class SPROUTDATA_API FOfferTable
{
public:
    const TArray<FOfferRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FOfferRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FOfferRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of OfferItem. */
class SPROUTDATA_API FOfferItemTable
{
public:
    const TArray<FOfferItemRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FOfferItemRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FOfferItemRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of ItemGrade. */
class SPROUTDATA_API FItemGradeTable
{
public:
    const TArray<FItemGradeRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FItemGradeRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FItemGradeRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of String. */
class SPROUTDATA_API FStringTable
{
public:
    const TArray<FStringRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this StringKey, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FStringRow* FindByStringKey(const FString& Key) const;

    /** Whether the table holds a row with this StringKey. */
    bool ContainsStringKey(const FString& Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FStringRow> RecordsStorage;
    TMap<FString, int32> ByStringKey;
};


/** Every row of OfflineReward. */
class SPROUTDATA_API FOfflineRewardTable
{
public:
    const TArray<FOfflineRewardRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FOfflineRewardRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FOfflineRewardRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of IdleRate. */
class SPROUTDATA_API FIdleRateTable
{
public:
    const TArray<FIdleRateRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FIdleRateRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FIdleRateRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of AfkBonus. */
class SPROUTDATA_API FAfkBonusTable
{
public:
    const TArray<FAfkBonusRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FAfkBonusRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FAfkBonusRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Quest. */
class SPROUTDATA_API FQuestTable
{
public:
    const TArray<FQuestRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FQuestRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FQuestRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of QuestStep. */
class SPROUTDATA_API FQuestStepTable
{
public:
    const TArray<FQuestStepRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FQuestStepRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FQuestStepRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of DailyQuest. */
class SPROUTDATA_API FDailyQuestTable
{
public:
    const TArray<FDailyQuestRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FDailyQuestRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FDailyQuestRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of WeeklyQuest. */
class SPROUTDATA_API FWeeklyQuestTable
{
public:
    const TArray<FWeeklyQuestRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FWeeklyQuestRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FWeeklyQuestRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Achievement. */
class SPROUTDATA_API FAchievementTable
{
public:
    const TArray<FAchievementRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FAchievementRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FAchievementRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Reward. */
class SPROUTDATA_API FRewardTable
{
public:
    const TArray<FRewardRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FRewardRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FRewardRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of LootGroup. */
class SPROUTDATA_API FLootGroupTable
{
public:
    const TArray<FLootGroupRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FLootGroupRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FLootGroupRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of LootEntry. */
class SPROUTDATA_API FLootEntryTable
{
public:
    const TArray<FLootEntryRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FLootEntryRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FLootEntryRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of MailReward. */
class SPROUTDATA_API FMailRewardTable
{
public:
    const TArray<FMailRewardRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FMailRewardRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FMailRewardRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Setting. */
class SPROUTDATA_API FSettingTable
{
public:
    const TArray<FSettingRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this SettingKey, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FSettingRow* FindBySettingKey(const FString& Key) const;

    /** Whether the table holds a row with this SettingKey. */
    bool ContainsSettingKey(const FString& Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FSettingRow> RecordsStorage;
    TMap<FString, int32> BySettingKey;
};


/** Every row of Condition. */
class SPROUTDATA_API FConditionTable
{
public:
    const TArray<FConditionRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FConditionRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FConditionRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Unlock. */
class SPROUTDATA_API FUnlockTable
{
public:
    const TArray<FUnlockRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FUnlockRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FUnlockRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Table. */
class SPROUTDATA_API FTableTable
{
public:
    const TArray<FTableRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FTableRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FTableRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Shop. */
class SPROUTDATA_API FShopTable
{
public:
    const TArray<FShopRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FShopRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FShopRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of ShopSlot. */
class SPROUTDATA_API FShopSlotTable
{
public:
    const TArray<FShopSlotRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FShopSlotRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FShopSlotRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Price. */
class SPROUTDATA_API FPriceTable
{
public:
    const TArray<FPriceRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FPriceRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FPriceRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Promo. */
class SPROUTDATA_API FPromoTable
{
public:
    const TArray<FPromoRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FPromoRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FPromoRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Skill. */
class SPROUTDATA_API FSkillTable
{
public:
    const TArray<FSkillRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FSkillRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FSkillRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of SkillLevel. */
class SPROUTDATA_API FSkillLevelTable
{
public:
    const TArray<FSkillLevelRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FSkillLevelRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FSkillLevelRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of SkillEffect. */
class SPROUTDATA_API FSkillEffectTable
{
public:
    const TArray<FSkillEffectRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FSkillEffectRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FSkillEffectRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Buff. */
class SPROUTDATA_API FBuffTable
{
public:
    const TArray<FBuffRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FBuffRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FBuffRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of BuffPool. */
class SPROUTDATA_API FBuffPoolTable
{
public:
    const TArray<FBuffPoolRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FBuffPoolRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FBuffPoolRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Passive. */
class SPROUTDATA_API FPassiveTable
{
public:
    const TArray<FPassiveRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FPassiveRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FPassiveRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Stage. */
class SPROUTDATA_API FStageTable
{
public:
    const TArray<FStageRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FStageRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FStageRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of StageWave. */
class SPROUTDATA_API FStageWaveTable
{
public:
    const TArray<FStageWaveRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FStageWaveRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FStageWaveRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of StageMonster. */
class SPROUTDATA_API FStageMonsterTable
{
public:
    const TArray<FStageMonsterRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FStageMonsterRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FStageMonsterRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of StageReward. */
class SPROUTDATA_API FStageRewardTable
{
public:
    const TArray<FStageRewardRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FStageRewardRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FStageRewardRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of Chapter. */
class SPROUTDATA_API FChapterTable
{
public:
    const TArray<FChapterRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FChapterRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FChapterRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/** Every row of World. */
class SPROUTDATA_API FWorldTable
{
public:
    const TArray<FWorldRow>& Records() const { return RecordsStorage; }

    /**
     * The row with this Id, or nullptr when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    const FWorldRow* FindById(int32 Key) const;

    /** Whether the table holds a row with this Id. */
    bool ContainsId(int32 Key) const;

    /** Loads the table from a .tcb file written by Tabbit. */
    bool Read(const FString& Filename);

private:
    TArray<FWorldRow> RecordsStorage;
    TMap<int32, int32> ById;
};


/**
 * Every table, loaded together.
 *
 * A static class rather than a UObject: the data outlives any world, is read once at
 * startup and never mutated, and nothing here needs reflection or garbage collection.
 */
class SPROUTDATA_API FSproutData
{
public:
    static const FCharacterTable& Character() { return CharacterStorage; }
    static const FCharacterStatTable& CharacterStat() { return CharacterStatStorage; }
    static const FCharacterAscendTable& CharacterAscend() { return CharacterAscendStorage; }
    static const FCharacterSkinTable& CharacterSkin() { return CharacterSkinStorage; }
    static const FCharacterVoiceTable& CharacterVoice() { return CharacterVoiceStorage; }
    static const FRoleBonusTable& RoleBonus() { return RoleBonusStorage; }
    static const FElementAffinityTable& ElementAffinity() { return ElementAffinityStorage; }
    static const FCodexTable& Codex() { return CodexStorage; }
    static const FCodexEntryTable& CodexEntry() { return CodexEntryStorage; }
    static const FCodexTierTable& CodexTier() { return CodexTierStorage; }
    static const FDialogueTable& Dialogue() { return DialogueStorage; }
    static const FDialogueLineTable& DialogueLine() { return DialogueLineStorage; }
    static const FCutsceneTable& Cutscene() { return CutsceneStorage; }
    static const FDungeonTable& Dungeon() { return DungeonStorage; }
    static const FDungeonFloorTable& DungeonFloor() { return DungeonFloorStorage; }
    static const FDungeonRewardTable& DungeonReward() { return DungeonRewardStorage; }
    static const FRaidTable& Raid() { return RaidStorage; }
    static const FEquipTable& Equip() { return EquipStorage; }
    static const FEquipLevelTable& EquipLevel() { return EquipLevelStorage; }
    static const FEquipGradeTable& EquipGrade() { return EquipGradeStorage; }
    static const FEquipSetTable& EquipSet() { return EquipSetStorage; }
    static const FEquipSetBonusTable& EquipSetBonus() { return EquipSetBonusStorage; }
    static const FEquipOptionTable& EquipOption() { return EquipOptionStorage; }
    static const FGachaTable& Gacha() { return GachaStorage; }
    static const FGachaPoolTable& GachaPool() { return GachaPoolStorage; }
    static const FGachaOddsTable& GachaOdds() { return GachaOddsStorage; }
    static const FPityTable& Pity() { return PityStorage; }
    static const FLevelCurveTable& LevelCurve() { return LevelCurveStorage; }
    static const FStatBaseTable& StatBase() { return StatBaseStorage; }
    static const FAscendCurveTable& AscendCurve() { return AscendCurveStorage; }
    static const FPowerScoreTable& PowerScore() { return PowerScoreStorage; }
    static const FItemTable& Item() { return ItemStorage; }
    static const FCurrencyTable& Currency() { return CurrencyStorage; }
    static const FMaterialTable& Material() { return MaterialStorage; }
    static const FConsumableTable& Consumable() { return ConsumableStorage; }
    static const FOfferTable& Offer() { return OfferStorage; }
    static const FOfferItemTable& OfferItem() { return OfferItemStorage; }
    static const FItemGradeTable& ItemGrade() { return ItemGradeStorage; }
    static const FStringTable& String() { return StringStorage; }
    static const FOfflineRewardTable& OfflineReward() { return OfflineRewardStorage; }
    static const FIdleRateTable& IdleRate() { return IdleRateStorage; }
    static const FAfkBonusTable& AfkBonus() { return AfkBonusStorage; }
    static const FQuestTable& Quest() { return QuestStorage; }
    static const FQuestStepTable& QuestStep() { return QuestStepStorage; }
    static const FDailyQuestTable& DailyQuest() { return DailyQuestStorage; }
    static const FWeeklyQuestTable& WeeklyQuest() { return WeeklyQuestStorage; }
    static const FAchievementTable& Achievement() { return AchievementStorage; }
    static const FRewardTable& Reward() { return RewardStorage; }
    static const FLootGroupTable& LootGroup() { return LootGroupStorage; }
    static const FLootEntryTable& LootEntry() { return LootEntryStorage; }
    static const FMailRewardTable& MailReward() { return MailRewardStorage; }
    static const FSettingTable& Setting() { return SettingStorage; }
    static const FConditionTable& Condition() { return ConditionStorage; }
    static const FUnlockTable& Unlock() { return UnlockStorage; }
    static const FTableTable& Table() { return TableStorage; }
    static const FShopTable& Shop() { return ShopStorage; }
    static const FShopSlotTable& ShopSlot() { return ShopSlotStorage; }
    static const FPriceTable& Price() { return PriceStorage; }
    static const FPromoTable& Promo() { return PromoStorage; }
    static const FSkillTable& Skill() { return SkillStorage; }
    static const FSkillLevelTable& SkillLevel() { return SkillLevelStorage; }
    static const FSkillEffectTable& SkillEffect() { return SkillEffectStorage; }
    static const FBuffTable& Buff() { return BuffStorage; }
    static const FBuffPoolTable& BuffPool() { return BuffPoolStorage; }
    static const FPassiveTable& Passive() { return PassiveStorage; }
    static const FStageTable& Stage() { return StageStorage; }
    static const FStageWaveTable& StageWave() { return StageWaveStorage; }
    static const FStageMonsterTable& StageMonster() { return StageMonsterStorage; }
    static const FStageRewardTable& StageReward() { return StageRewardStorage; }
    static const FChapterTable& Chapter() { return ChapterStorage; }
    static const FWorldTable& World() { return WorldStorage; }

    /**
     * Reads every table from BasePath. Returns false if any of them could not be read.
     *
     * FileExtension defaults to what the recipe told the exporter to write. Pass a different
     * one when the data files were renamed on the way into the package - which they often are,
     * since some platforms only cook what carries an extension they recognise.
     */
    static bool ReadAll(const FString& BasePath,
                        const FString& FileExtension = TEXT(".tcb"));

    /**
     * The key the table files were sealed with, or empty when they were not sealed.
     *
     * Beside ReadAll because it is the same kind of thing: one decision the project makes
     * once, at start-up, before the first table is read. A key threaded through every Read
     * signature would be the same value written out at every call site, and every call site
     * is somewhere it could be wrong.
     *
     * Thirty-two bytes, the key the converter was given. Empty - the default - is a project
     * that does not encrypt, and it costs that project nothing: the load path opens the
     * envelope either way, and a file that is not encrypted comes back from that unchanged.
     * A project that does encrypt and leaves this empty gets the reader's own message saying
     * the file is encrypted and no key was given, rather than a failure somewhere inside the
     * format.
     *
     * Where the key comes from is the one thing about this layer worth being careful with.
     * It is a secret only until it reaches a repository, a build log or an issue, and it
     * cannot be taken back afterwards - so it is read at start-up from wherever this project
     * keeps secrets, not written into a source file or a .ini the packaging step will cook.
     * What the layer is for, and therefore what is and is not worth defending, is
     * spec/wire/tcb-v104-composed-encodings.md section 4.
     */
    static TArray<uint8> EncryptionKey;

    /**
     * The key the table files were signed with, or empty when they were not signed.
     *
     * A different key from EncryptionKey and independent of it: a file can be signed without
     * being encrypted, and encrypted without being signed.
     *
     * What it buys is the thing encryption does not. The cipher is a keystream XOR, so a bit
     * flipped in the file is a bit flipped in the value it decrypts to - no key needed - and
     * every structural check the format makes still passes, because a four-byte float
     * accepts any four bytes. With this set, a file altered after export stops loading.
     *
     * Setting it makes the reader stricter in a second way: a file that carries no MAC is
     * refused, because otherwise the check could be removed by zeroing sixteen bytes. So the
     * order to turn this on in is data first, key second. spec/wire/tcb-mac-and-signature.md.
     */
    static TArray<uint8> MacKey;

    /**
     * Whether to check the MAC at all. True by default.
     *
     * For tools that read exported files without the key, and for measuring what the check
     * costs at load. It is not a security boundary - anyone who can flip a flag in a shipped
     * build can read MacKey out of the same build.
     */
    static bool bVerifyMac;

private:
    static FCharacterTable CharacterStorage;
    static FCharacterStatTable CharacterStatStorage;
    static FCharacterAscendTable CharacterAscendStorage;
    static FCharacterSkinTable CharacterSkinStorage;
    static FCharacterVoiceTable CharacterVoiceStorage;
    static FRoleBonusTable RoleBonusStorage;
    static FElementAffinityTable ElementAffinityStorage;
    static FCodexTable CodexStorage;
    static FCodexEntryTable CodexEntryStorage;
    static FCodexTierTable CodexTierStorage;
    static FDialogueTable DialogueStorage;
    static FDialogueLineTable DialogueLineStorage;
    static FCutsceneTable CutsceneStorage;
    static FDungeonTable DungeonStorage;
    static FDungeonFloorTable DungeonFloorStorage;
    static FDungeonRewardTable DungeonRewardStorage;
    static FRaidTable RaidStorage;
    static FEquipTable EquipStorage;
    static FEquipLevelTable EquipLevelStorage;
    static FEquipGradeTable EquipGradeStorage;
    static FEquipSetTable EquipSetStorage;
    static FEquipSetBonusTable EquipSetBonusStorage;
    static FEquipOptionTable EquipOptionStorage;
    static FGachaTable GachaStorage;
    static FGachaPoolTable GachaPoolStorage;
    static FGachaOddsTable GachaOddsStorage;
    static FPityTable PityStorage;
    static FLevelCurveTable LevelCurveStorage;
    static FStatBaseTable StatBaseStorage;
    static FAscendCurveTable AscendCurveStorage;
    static FPowerScoreTable PowerScoreStorage;
    static FItemTable ItemStorage;
    static FCurrencyTable CurrencyStorage;
    static FMaterialTable MaterialStorage;
    static FConsumableTable ConsumableStorage;
    static FOfferTable OfferStorage;
    static FOfferItemTable OfferItemStorage;
    static FItemGradeTable ItemGradeStorage;
    static FStringTable StringStorage;
    static FOfflineRewardTable OfflineRewardStorage;
    static FIdleRateTable IdleRateStorage;
    static FAfkBonusTable AfkBonusStorage;
    static FQuestTable QuestStorage;
    static FQuestStepTable QuestStepStorage;
    static FDailyQuestTable DailyQuestStorage;
    static FWeeklyQuestTable WeeklyQuestStorage;
    static FAchievementTable AchievementStorage;
    static FRewardTable RewardStorage;
    static FLootGroupTable LootGroupStorage;
    static FLootEntryTable LootEntryStorage;
    static FMailRewardTable MailRewardStorage;
    static FSettingTable SettingStorage;
    static FConditionTable ConditionStorage;
    static FUnlockTable UnlockStorage;
    static FTableTable TableStorage;
    static FShopTable ShopStorage;
    static FShopSlotTable ShopSlotStorage;
    static FPriceTable PriceStorage;
    static FPromoTable PromoStorage;
    static FSkillTable SkillStorage;
    static FSkillLevelTable SkillLevelStorage;
    static FSkillEffectTable SkillEffectStorage;
    static FBuffTable BuffStorage;
    static FBuffPoolTable BuffPoolStorage;
    static FPassiveTable PassiveStorage;
    static FStageTable StageStorage;
    static FStageWaveTable StageWaveStorage;
    static FStageMonsterTable StageMonsterStorage;
    static FStageRewardTable StageRewardStorage;
    static FChapterTable ChapterStorage;
    static FWorldTable WorldStorage;
};

/**
 * The same data, reachable from Blueprint.
 *
 * Every row above is a USTRUCT marked BlueprintType with BlueprintReadOnly properties,
 * which says the rows are meant to be used from Blueprint - and until this existed there
 * was no way to get one. A graph could declare a variable of a row type and had nothing to
 * obtain a value from, because the accessor is a plain C++ class and a static method on one
 * is not something Blueprint can call.
 *
 * A function library rather than making the accessor a UObject. The storage stays where it
 * is: this owns nothing, adds no garbage collection, and does not need a world. It is a
 * few UFUNCTIONs over the C++ that was already there.
 */
UCLASS()
class SPROUTDATA_API USproutDataLibrary : public UBlueprintFunctionLibrary
{
    GENERATED_BODY()

public:

    /**
     * The Character row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Character",
              meta = (DisplayName = "Get Character Row"))
    static FCharacterRow GetCharacterRow(int32 Key, bool& bFound);

    /** How many Character rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Character",
              meta = (DisplayName = "Get Character Row Count"))
    static int32 GetCharacterRowCount();

    /**
     * The Character row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Character",
              meta = (DisplayName = "Get Character Row At"))
    static FCharacterRow GetCharacterRowAt(int32 Position, bool& bFound);

    /**
     * The CharacterStat row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterStat",
              meta = (DisplayName = "Get CharacterStat Row"))
    static FCharacterStatRow GetCharacterStatRow(int32 Key, bool& bFound);

    /** How many CharacterStat rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterStat",
              meta = (DisplayName = "Get CharacterStat Row Count"))
    static int32 GetCharacterStatRowCount();

    /**
     * The CharacterStat row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterStat",
              meta = (DisplayName = "Get CharacterStat Row At"))
    static FCharacterStatRow GetCharacterStatRowAt(int32 Position, bool& bFound);

    /**
     * The CharacterAscend row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterAscend",
              meta = (DisplayName = "Get CharacterAscend Row"))
    static FCharacterAscendRow GetCharacterAscendRow(int32 Key, bool& bFound);

    /** How many CharacterAscend rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterAscend",
              meta = (DisplayName = "Get CharacterAscend Row Count"))
    static int32 GetCharacterAscendRowCount();

    /**
     * The CharacterAscend row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterAscend",
              meta = (DisplayName = "Get CharacterAscend Row At"))
    static FCharacterAscendRow GetCharacterAscendRowAt(int32 Position, bool& bFound);

    /**
     * The CharacterSkin row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterSkin",
              meta = (DisplayName = "Get CharacterSkin Row"))
    static FCharacterSkinRow GetCharacterSkinRow(int32 Key, bool& bFound);

    /** How many CharacterSkin rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterSkin",
              meta = (DisplayName = "Get CharacterSkin Row Count"))
    static int32 GetCharacterSkinRowCount();

    /**
     * The CharacterSkin row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterSkin",
              meta = (DisplayName = "Get CharacterSkin Row At"))
    static FCharacterSkinRow GetCharacterSkinRowAt(int32 Position, bool& bFound);

    /**
     * The CharacterVoice row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterVoice",
              meta = (DisplayName = "Get CharacterVoice Row"))
    static FCharacterVoiceRow GetCharacterVoiceRow(int32 Key, bool& bFound);

    /** How many CharacterVoice rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterVoice",
              meta = (DisplayName = "Get CharacterVoice Row Count"))
    static int32 GetCharacterVoiceRowCount();

    /**
     * The CharacterVoice row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CharacterVoice",
              meta = (DisplayName = "Get CharacterVoice Row At"))
    static FCharacterVoiceRow GetCharacterVoiceRowAt(int32 Position, bool& bFound);

    /**
     * The RoleBonus row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|RoleBonus",
              meta = (DisplayName = "Get RoleBonus Row"))
    static FRoleBonusRow GetRoleBonusRow(int32 Key, bool& bFound);

    /** How many RoleBonus rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|RoleBonus",
              meta = (DisplayName = "Get RoleBonus Row Count"))
    static int32 GetRoleBonusRowCount();

    /**
     * The RoleBonus row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|RoleBonus",
              meta = (DisplayName = "Get RoleBonus Row At"))
    static FRoleBonusRow GetRoleBonusRowAt(int32 Position, bool& bFound);

    /**
     * The ElementAffinity row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ElementAffinity",
              meta = (DisplayName = "Get ElementAffinity Row"))
    static FElementAffinityRow GetElementAffinityRow(int32 Key, bool& bFound);

    /** How many ElementAffinity rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ElementAffinity",
              meta = (DisplayName = "Get ElementAffinity Row Count"))
    static int32 GetElementAffinityRowCount();

    /**
     * The ElementAffinity row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ElementAffinity",
              meta = (DisplayName = "Get ElementAffinity Row At"))
    static FElementAffinityRow GetElementAffinityRowAt(int32 Position, bool& bFound);

    /**
     * The Codex row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Codex",
              meta = (DisplayName = "Get Codex Row"))
    static FCodexRow GetCodexRow(int32 Key, bool& bFound);

    /** How many Codex rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Codex",
              meta = (DisplayName = "Get Codex Row Count"))
    static int32 GetCodexRowCount();

    /**
     * The Codex row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Codex",
              meta = (DisplayName = "Get Codex Row At"))
    static FCodexRow GetCodexRowAt(int32 Position, bool& bFound);

    /**
     * The CodexEntry row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CodexEntry",
              meta = (DisplayName = "Get CodexEntry Row"))
    static FCodexEntryRow GetCodexEntryRow(int32 Key, bool& bFound);

    /** How many CodexEntry rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CodexEntry",
              meta = (DisplayName = "Get CodexEntry Row Count"))
    static int32 GetCodexEntryRowCount();

    /**
     * The CodexEntry row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CodexEntry",
              meta = (DisplayName = "Get CodexEntry Row At"))
    static FCodexEntryRow GetCodexEntryRowAt(int32 Position, bool& bFound);

    /**
     * The CodexTier row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CodexTier",
              meta = (DisplayName = "Get CodexTier Row"))
    static FCodexTierRow GetCodexTierRow(int32 Key, bool& bFound);

    /** How many CodexTier rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CodexTier",
              meta = (DisplayName = "Get CodexTier Row Count"))
    static int32 GetCodexTierRowCount();

    /**
     * The CodexTier row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|CodexTier",
              meta = (DisplayName = "Get CodexTier Row At"))
    static FCodexTierRow GetCodexTierRowAt(int32 Position, bool& bFound);

    /**
     * The Dialogue row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Dialogue",
              meta = (DisplayName = "Get Dialogue Row"))
    static FDialogueRow GetDialogueRow(int32 Key, bool& bFound);

    /** How many Dialogue rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Dialogue",
              meta = (DisplayName = "Get Dialogue Row Count"))
    static int32 GetDialogueRowCount();

    /**
     * The Dialogue row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Dialogue",
              meta = (DisplayName = "Get Dialogue Row At"))
    static FDialogueRow GetDialogueRowAt(int32 Position, bool& bFound);

    /**
     * The DialogueLine row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DialogueLine",
              meta = (DisplayName = "Get DialogueLine Row"))
    static FDialogueLineRow GetDialogueLineRow(int32 Key, bool& bFound);

    /** How many DialogueLine rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DialogueLine",
              meta = (DisplayName = "Get DialogueLine Row Count"))
    static int32 GetDialogueLineRowCount();

    /**
     * The DialogueLine row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DialogueLine",
              meta = (DisplayName = "Get DialogueLine Row At"))
    static FDialogueLineRow GetDialogueLineRowAt(int32 Position, bool& bFound);

    /**
     * The Cutscene row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Cutscene",
              meta = (DisplayName = "Get Cutscene Row"))
    static FCutsceneRow GetCutsceneRow(int32 Key, bool& bFound);

    /** How many Cutscene rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Cutscene",
              meta = (DisplayName = "Get Cutscene Row Count"))
    static int32 GetCutsceneRowCount();

    /**
     * The Cutscene row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Cutscene",
              meta = (DisplayName = "Get Cutscene Row At"))
    static FCutsceneRow GetCutsceneRowAt(int32 Position, bool& bFound);

    /**
     * The Dungeon row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Dungeon",
              meta = (DisplayName = "Get Dungeon Row"))
    static FDungeonRow GetDungeonRow(int32 Key, bool& bFound);

    /** How many Dungeon rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Dungeon",
              meta = (DisplayName = "Get Dungeon Row Count"))
    static int32 GetDungeonRowCount();

    /**
     * The Dungeon row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Dungeon",
              meta = (DisplayName = "Get Dungeon Row At"))
    static FDungeonRow GetDungeonRowAt(int32 Position, bool& bFound);

    /**
     * The DungeonFloor row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DungeonFloor",
              meta = (DisplayName = "Get DungeonFloor Row"))
    static FDungeonFloorRow GetDungeonFloorRow(int32 Key, bool& bFound);

    /** How many DungeonFloor rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DungeonFloor",
              meta = (DisplayName = "Get DungeonFloor Row Count"))
    static int32 GetDungeonFloorRowCount();

    /**
     * The DungeonFloor row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DungeonFloor",
              meta = (DisplayName = "Get DungeonFloor Row At"))
    static FDungeonFloorRow GetDungeonFloorRowAt(int32 Position, bool& bFound);

    /**
     * The DungeonReward row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DungeonReward",
              meta = (DisplayName = "Get DungeonReward Row"))
    static FDungeonRewardRow GetDungeonRewardRow(int32 Key, bool& bFound);

    /** How many DungeonReward rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DungeonReward",
              meta = (DisplayName = "Get DungeonReward Row Count"))
    static int32 GetDungeonRewardRowCount();

    /**
     * The DungeonReward row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DungeonReward",
              meta = (DisplayName = "Get DungeonReward Row At"))
    static FDungeonRewardRow GetDungeonRewardRowAt(int32 Position, bool& bFound);

    /**
     * The Raid row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Raid",
              meta = (DisplayName = "Get Raid Row"))
    static FRaidRow GetRaidRow(int32 Key, bool& bFound);

    /** How many Raid rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Raid",
              meta = (DisplayName = "Get Raid Row Count"))
    static int32 GetRaidRowCount();

    /**
     * The Raid row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Raid",
              meta = (DisplayName = "Get Raid Row At"))
    static FRaidRow GetRaidRowAt(int32 Position, bool& bFound);

    /**
     * The Equip row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Equip",
              meta = (DisplayName = "Get Equip Row"))
    static FEquipRow GetEquipRow(int32 Key, bool& bFound);

    /** How many Equip rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Equip",
              meta = (DisplayName = "Get Equip Row Count"))
    static int32 GetEquipRowCount();

    /**
     * The Equip row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Equip",
              meta = (DisplayName = "Get Equip Row At"))
    static FEquipRow GetEquipRowAt(int32 Position, bool& bFound);

    /**
     * The EquipLevel row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipLevel",
              meta = (DisplayName = "Get EquipLevel Row"))
    static FEquipLevelRow GetEquipLevelRow(int32 Key, bool& bFound);

    /** How many EquipLevel rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipLevel",
              meta = (DisplayName = "Get EquipLevel Row Count"))
    static int32 GetEquipLevelRowCount();

    /**
     * The EquipLevel row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipLevel",
              meta = (DisplayName = "Get EquipLevel Row At"))
    static FEquipLevelRow GetEquipLevelRowAt(int32 Position, bool& bFound);

    /**
     * The EquipGrade row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipGrade",
              meta = (DisplayName = "Get EquipGrade Row"))
    static FEquipGradeRow GetEquipGradeRow(int32 Key, bool& bFound);

    /** How many EquipGrade rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipGrade",
              meta = (DisplayName = "Get EquipGrade Row Count"))
    static int32 GetEquipGradeRowCount();

    /**
     * The EquipGrade row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipGrade",
              meta = (DisplayName = "Get EquipGrade Row At"))
    static FEquipGradeRow GetEquipGradeRowAt(int32 Position, bool& bFound);

    /**
     * The EquipSet row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipSet",
              meta = (DisplayName = "Get EquipSet Row"))
    static FEquipSetRow GetEquipSetRow(int32 Key, bool& bFound);

    /** How many EquipSet rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipSet",
              meta = (DisplayName = "Get EquipSet Row Count"))
    static int32 GetEquipSetRowCount();

    /**
     * The EquipSet row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipSet",
              meta = (DisplayName = "Get EquipSet Row At"))
    static FEquipSetRow GetEquipSetRowAt(int32 Position, bool& bFound);

    /**
     * The EquipSetBonus row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipSetBonus",
              meta = (DisplayName = "Get EquipSetBonus Row"))
    static FEquipSetBonusRow GetEquipSetBonusRow(int32 Key, bool& bFound);

    /** How many EquipSetBonus rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipSetBonus",
              meta = (DisplayName = "Get EquipSetBonus Row Count"))
    static int32 GetEquipSetBonusRowCount();

    /**
     * The EquipSetBonus row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipSetBonus",
              meta = (DisplayName = "Get EquipSetBonus Row At"))
    static FEquipSetBonusRow GetEquipSetBonusRowAt(int32 Position, bool& bFound);

    /**
     * The EquipOption row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipOption",
              meta = (DisplayName = "Get EquipOption Row"))
    static FEquipOptionRow GetEquipOptionRow(int32 Key, bool& bFound);

    /** How many EquipOption rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipOption",
              meta = (DisplayName = "Get EquipOption Row Count"))
    static int32 GetEquipOptionRowCount();

    /**
     * The EquipOption row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|EquipOption",
              meta = (DisplayName = "Get EquipOption Row At"))
    static FEquipOptionRow GetEquipOptionRowAt(int32 Position, bool& bFound);

    /**
     * The Gacha row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Gacha",
              meta = (DisplayName = "Get Gacha Row"))
    static FGachaRow GetGachaRow(int32 Key, bool& bFound);

    /** How many Gacha rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Gacha",
              meta = (DisplayName = "Get Gacha Row Count"))
    static int32 GetGachaRowCount();

    /**
     * The Gacha row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Gacha",
              meta = (DisplayName = "Get Gacha Row At"))
    static FGachaRow GetGachaRowAt(int32 Position, bool& bFound);

    /**
     * The GachaPool row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|GachaPool",
              meta = (DisplayName = "Get GachaPool Row"))
    static FGachaPoolRow GetGachaPoolRow(int32 Key, bool& bFound);

    /** How many GachaPool rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|GachaPool",
              meta = (DisplayName = "Get GachaPool Row Count"))
    static int32 GetGachaPoolRowCount();

    /**
     * The GachaPool row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|GachaPool",
              meta = (DisplayName = "Get GachaPool Row At"))
    static FGachaPoolRow GetGachaPoolRowAt(int32 Position, bool& bFound);

    /**
     * The GachaOdds row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|GachaOdds",
              meta = (DisplayName = "Get GachaOdds Row"))
    static FGachaOddsRow GetGachaOddsRow(int32 Key, bool& bFound);

    /** How many GachaOdds rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|GachaOdds",
              meta = (DisplayName = "Get GachaOdds Row Count"))
    static int32 GetGachaOddsRowCount();

    /**
     * The GachaOdds row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|GachaOdds",
              meta = (DisplayName = "Get GachaOdds Row At"))
    static FGachaOddsRow GetGachaOddsRowAt(int32 Position, bool& bFound);

    /**
     * The Pity row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Pity",
              meta = (DisplayName = "Get Pity Row"))
    static FPityRow GetPityRow(int32 Key, bool& bFound);

    /** How many Pity rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Pity",
              meta = (DisplayName = "Get Pity Row Count"))
    static int32 GetPityRowCount();

    /**
     * The Pity row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Pity",
              meta = (DisplayName = "Get Pity Row At"))
    static FPityRow GetPityRowAt(int32 Position, bool& bFound);

    /**
     * The LevelCurve row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|LevelCurve",
              meta = (DisplayName = "Get LevelCurve Row"))
    static FLevelCurveRow GetLevelCurveRow(int32 Key, bool& bFound);

    /** How many LevelCurve rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|LevelCurve",
              meta = (DisplayName = "Get LevelCurve Row Count"))
    static int32 GetLevelCurveRowCount();

    /**
     * The LevelCurve row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|LevelCurve",
              meta = (DisplayName = "Get LevelCurve Row At"))
    static FLevelCurveRow GetLevelCurveRowAt(int32 Position, bool& bFound);

    /**
     * The StatBase row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StatBase",
              meta = (DisplayName = "Get StatBase Row"))
    static FStatBaseRow GetStatBaseRow(int32 Key, bool& bFound);

    /** How many StatBase rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StatBase",
              meta = (DisplayName = "Get StatBase Row Count"))
    static int32 GetStatBaseRowCount();

    /**
     * The StatBase row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StatBase",
              meta = (DisplayName = "Get StatBase Row At"))
    static FStatBaseRow GetStatBaseRowAt(int32 Position, bool& bFound);

    /**
     * The AscendCurve row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|AscendCurve",
              meta = (DisplayName = "Get AscendCurve Row"))
    static FAscendCurveRow GetAscendCurveRow(int32 Key, bool& bFound);

    /** How many AscendCurve rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|AscendCurve",
              meta = (DisplayName = "Get AscendCurve Row Count"))
    static int32 GetAscendCurveRowCount();

    /**
     * The AscendCurve row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|AscendCurve",
              meta = (DisplayName = "Get AscendCurve Row At"))
    static FAscendCurveRow GetAscendCurveRowAt(int32 Position, bool& bFound);

    /**
     * The PowerScore row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|PowerScore",
              meta = (DisplayName = "Get PowerScore Row"))
    static FPowerScoreRow GetPowerScoreRow(int32 Key, bool& bFound);

    /** How many PowerScore rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|PowerScore",
              meta = (DisplayName = "Get PowerScore Row Count"))
    static int32 GetPowerScoreRowCount();

    /**
     * The PowerScore row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|PowerScore",
              meta = (DisplayName = "Get PowerScore Row At"))
    static FPowerScoreRow GetPowerScoreRowAt(int32 Position, bool& bFound);

    /**
     * The Item row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Item",
              meta = (DisplayName = "Get Item Row"))
    static FItemRow GetItemRow(int32 Key, bool& bFound);

    /** How many Item rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Item",
              meta = (DisplayName = "Get Item Row Count"))
    static int32 GetItemRowCount();

    /**
     * The Item row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Item",
              meta = (DisplayName = "Get Item Row At"))
    static FItemRow GetItemRowAt(int32 Position, bool& bFound);

    /**
     * The Currency row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Currency",
              meta = (DisplayName = "Get Currency Row"))
    static FCurrencyRow GetCurrencyRow(int32 Key, bool& bFound);

    /** How many Currency rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Currency",
              meta = (DisplayName = "Get Currency Row Count"))
    static int32 GetCurrencyRowCount();

    /**
     * The Currency row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Currency",
              meta = (DisplayName = "Get Currency Row At"))
    static FCurrencyRow GetCurrencyRowAt(int32 Position, bool& bFound);

    /**
     * The Material row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Material",
              meta = (DisplayName = "Get Material Row"))
    static FMaterialRow GetMaterialRow(int32 Key, bool& bFound);

    /** How many Material rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Material",
              meta = (DisplayName = "Get Material Row Count"))
    static int32 GetMaterialRowCount();

    /**
     * The Material row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Material",
              meta = (DisplayName = "Get Material Row At"))
    static FMaterialRow GetMaterialRowAt(int32 Position, bool& bFound);

    /**
     * The Consumable row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Consumable",
              meta = (DisplayName = "Get Consumable Row"))
    static FConsumableRow GetConsumableRow(int32 Key, bool& bFound);

    /** How many Consumable rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Consumable",
              meta = (DisplayName = "Get Consumable Row Count"))
    static int32 GetConsumableRowCount();

    /**
     * The Consumable row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Consumable",
              meta = (DisplayName = "Get Consumable Row At"))
    static FConsumableRow GetConsumableRowAt(int32 Position, bool& bFound);

    /**
     * The Offer row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Offer",
              meta = (DisplayName = "Get Offer Row"))
    static FOfferRow GetOfferRow(int32 Key, bool& bFound);

    /** How many Offer rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Offer",
              meta = (DisplayName = "Get Offer Row Count"))
    static int32 GetOfferRowCount();

    /**
     * The Offer row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Offer",
              meta = (DisplayName = "Get Offer Row At"))
    static FOfferRow GetOfferRowAt(int32 Position, bool& bFound);

    /**
     * The OfferItem row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|OfferItem",
              meta = (DisplayName = "Get OfferItem Row"))
    static FOfferItemRow GetOfferItemRow(int32 Key, bool& bFound);

    /** How many OfferItem rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|OfferItem",
              meta = (DisplayName = "Get OfferItem Row Count"))
    static int32 GetOfferItemRowCount();

    /**
     * The OfferItem row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|OfferItem",
              meta = (DisplayName = "Get OfferItem Row At"))
    static FOfferItemRow GetOfferItemRowAt(int32 Position, bool& bFound);

    /**
     * The ItemGrade row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ItemGrade",
              meta = (DisplayName = "Get ItemGrade Row"))
    static FItemGradeRow GetItemGradeRow(int32 Key, bool& bFound);

    /** How many ItemGrade rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ItemGrade",
              meta = (DisplayName = "Get ItemGrade Row Count"))
    static int32 GetItemGradeRowCount();

    /**
     * The ItemGrade row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ItemGrade",
              meta = (DisplayName = "Get ItemGrade Row At"))
    static FItemGradeRow GetItemGradeRowAt(int32 Position, bool& bFound);

    /**
     * The String row with the given StringKey.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|String",
              meta = (DisplayName = "Get String Row"))
    static FStringRow GetStringRow(const FString& Key, bool& bFound);

    /** How many String rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|String",
              meta = (DisplayName = "Get String Row Count"))
    static int32 GetStringRowCount();

    /**
     * The String row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|String",
              meta = (DisplayName = "Get String Row At"))
    static FStringRow GetStringRowAt(int32 Position, bool& bFound);

    /**
     * The OfflineReward row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|OfflineReward",
              meta = (DisplayName = "Get OfflineReward Row"))
    static FOfflineRewardRow GetOfflineRewardRow(int32 Key, bool& bFound);

    /** How many OfflineReward rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|OfflineReward",
              meta = (DisplayName = "Get OfflineReward Row Count"))
    static int32 GetOfflineRewardRowCount();

    /**
     * The OfflineReward row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|OfflineReward",
              meta = (DisplayName = "Get OfflineReward Row At"))
    static FOfflineRewardRow GetOfflineRewardRowAt(int32 Position, bool& bFound);

    /**
     * The IdleRate row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|IdleRate",
              meta = (DisplayName = "Get IdleRate Row"))
    static FIdleRateRow GetIdleRateRow(int32 Key, bool& bFound);

    /** How many IdleRate rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|IdleRate",
              meta = (DisplayName = "Get IdleRate Row Count"))
    static int32 GetIdleRateRowCount();

    /**
     * The IdleRate row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|IdleRate",
              meta = (DisplayName = "Get IdleRate Row At"))
    static FIdleRateRow GetIdleRateRowAt(int32 Position, bool& bFound);

    /**
     * The AfkBonus row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|AfkBonus",
              meta = (DisplayName = "Get AfkBonus Row"))
    static FAfkBonusRow GetAfkBonusRow(int32 Key, bool& bFound);

    /** How many AfkBonus rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|AfkBonus",
              meta = (DisplayName = "Get AfkBonus Row Count"))
    static int32 GetAfkBonusRowCount();

    /**
     * The AfkBonus row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|AfkBonus",
              meta = (DisplayName = "Get AfkBonus Row At"))
    static FAfkBonusRow GetAfkBonusRowAt(int32 Position, bool& bFound);

    /**
     * The Quest row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Quest",
              meta = (DisplayName = "Get Quest Row"))
    static FQuestRow GetQuestRow(int32 Key, bool& bFound);

    /** How many Quest rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Quest",
              meta = (DisplayName = "Get Quest Row Count"))
    static int32 GetQuestRowCount();

    /**
     * The Quest row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Quest",
              meta = (DisplayName = "Get Quest Row At"))
    static FQuestRow GetQuestRowAt(int32 Position, bool& bFound);

    /**
     * The QuestStep row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|QuestStep",
              meta = (DisplayName = "Get QuestStep Row"))
    static FQuestStepRow GetQuestStepRow(int32 Key, bool& bFound);

    /** How many QuestStep rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|QuestStep",
              meta = (DisplayName = "Get QuestStep Row Count"))
    static int32 GetQuestStepRowCount();

    /**
     * The QuestStep row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|QuestStep",
              meta = (DisplayName = "Get QuestStep Row At"))
    static FQuestStepRow GetQuestStepRowAt(int32 Position, bool& bFound);

    /**
     * The DailyQuest row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DailyQuest",
              meta = (DisplayName = "Get DailyQuest Row"))
    static FDailyQuestRow GetDailyQuestRow(int32 Key, bool& bFound);

    /** How many DailyQuest rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DailyQuest",
              meta = (DisplayName = "Get DailyQuest Row Count"))
    static int32 GetDailyQuestRowCount();

    /**
     * The DailyQuest row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|DailyQuest",
              meta = (DisplayName = "Get DailyQuest Row At"))
    static FDailyQuestRow GetDailyQuestRowAt(int32 Position, bool& bFound);

    /**
     * The WeeklyQuest row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|WeeklyQuest",
              meta = (DisplayName = "Get WeeklyQuest Row"))
    static FWeeklyQuestRow GetWeeklyQuestRow(int32 Key, bool& bFound);

    /** How many WeeklyQuest rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|WeeklyQuest",
              meta = (DisplayName = "Get WeeklyQuest Row Count"))
    static int32 GetWeeklyQuestRowCount();

    /**
     * The WeeklyQuest row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|WeeklyQuest",
              meta = (DisplayName = "Get WeeklyQuest Row At"))
    static FWeeklyQuestRow GetWeeklyQuestRowAt(int32 Position, bool& bFound);

    /**
     * The Achievement row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Achievement",
              meta = (DisplayName = "Get Achievement Row"))
    static FAchievementRow GetAchievementRow(int32 Key, bool& bFound);

    /** How many Achievement rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Achievement",
              meta = (DisplayName = "Get Achievement Row Count"))
    static int32 GetAchievementRowCount();

    /**
     * The Achievement row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Achievement",
              meta = (DisplayName = "Get Achievement Row At"))
    static FAchievementRow GetAchievementRowAt(int32 Position, bool& bFound);

    /**
     * The Reward row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Reward",
              meta = (DisplayName = "Get Reward Row"))
    static FRewardRow GetRewardRow(int32 Key, bool& bFound);

    /** How many Reward rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Reward",
              meta = (DisplayName = "Get Reward Row Count"))
    static int32 GetRewardRowCount();

    /**
     * The Reward row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Reward",
              meta = (DisplayName = "Get Reward Row At"))
    static FRewardRow GetRewardRowAt(int32 Position, bool& bFound);

    /**
     * The LootGroup row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|LootGroup",
              meta = (DisplayName = "Get LootGroup Row"))
    static FLootGroupRow GetLootGroupRow(int32 Key, bool& bFound);

    /** How many LootGroup rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|LootGroup",
              meta = (DisplayName = "Get LootGroup Row Count"))
    static int32 GetLootGroupRowCount();

    /**
     * The LootGroup row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|LootGroup",
              meta = (DisplayName = "Get LootGroup Row At"))
    static FLootGroupRow GetLootGroupRowAt(int32 Position, bool& bFound);

    /**
     * The LootEntry row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|LootEntry",
              meta = (DisplayName = "Get LootEntry Row"))
    static FLootEntryRow GetLootEntryRow(int32 Key, bool& bFound);

    /** How many LootEntry rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|LootEntry",
              meta = (DisplayName = "Get LootEntry Row Count"))
    static int32 GetLootEntryRowCount();

    /**
     * The LootEntry row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|LootEntry",
              meta = (DisplayName = "Get LootEntry Row At"))
    static FLootEntryRow GetLootEntryRowAt(int32 Position, bool& bFound);

    /**
     * The MailReward row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|MailReward",
              meta = (DisplayName = "Get MailReward Row"))
    static FMailRewardRow GetMailRewardRow(int32 Key, bool& bFound);

    /** How many MailReward rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|MailReward",
              meta = (DisplayName = "Get MailReward Row Count"))
    static int32 GetMailRewardRowCount();

    /**
     * The MailReward row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|MailReward",
              meta = (DisplayName = "Get MailReward Row At"))
    static FMailRewardRow GetMailRewardRowAt(int32 Position, bool& bFound);

    /**
     * The Setting row with the given SettingKey.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Setting",
              meta = (DisplayName = "Get Setting Row"))
    static FSettingRow GetSettingRow(const FString& Key, bool& bFound);

    /** How many Setting rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Setting",
              meta = (DisplayName = "Get Setting Row Count"))
    static int32 GetSettingRowCount();

    /**
     * The Setting row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Setting",
              meta = (DisplayName = "Get Setting Row At"))
    static FSettingRow GetSettingRowAt(int32 Position, bool& bFound);

    /**
     * The Condition row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Condition",
              meta = (DisplayName = "Get Condition Row"))
    static FConditionRow GetConditionRow(int32 Key, bool& bFound);

    /** How many Condition rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Condition",
              meta = (DisplayName = "Get Condition Row Count"))
    static int32 GetConditionRowCount();

    /**
     * The Condition row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Condition",
              meta = (DisplayName = "Get Condition Row At"))
    static FConditionRow GetConditionRowAt(int32 Position, bool& bFound);

    /**
     * The Unlock row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Unlock",
              meta = (DisplayName = "Get Unlock Row"))
    static FUnlockRow GetUnlockRow(int32 Key, bool& bFound);

    /** How many Unlock rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Unlock",
              meta = (DisplayName = "Get Unlock Row Count"))
    static int32 GetUnlockRowCount();

    /**
     * The Unlock row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Unlock",
              meta = (DisplayName = "Get Unlock Row At"))
    static FUnlockRow GetUnlockRowAt(int32 Position, bool& bFound);

    /**
     * The Table row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Table",
              meta = (DisplayName = "Get Table Row"))
    static FTableRow GetTableRow(int32 Key, bool& bFound);

    /** How many Table rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Table",
              meta = (DisplayName = "Get Table Row Count"))
    static int32 GetTableRowCount();

    /**
     * The Table row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Table",
              meta = (DisplayName = "Get Table Row At"))
    static FTableRow GetTableRowAt(int32 Position, bool& bFound);

    /**
     * The Shop row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Shop",
              meta = (DisplayName = "Get Shop Row"))
    static FShopRow GetShopRow(int32 Key, bool& bFound);

    /** How many Shop rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Shop",
              meta = (DisplayName = "Get Shop Row Count"))
    static int32 GetShopRowCount();

    /**
     * The Shop row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Shop",
              meta = (DisplayName = "Get Shop Row At"))
    static FShopRow GetShopRowAt(int32 Position, bool& bFound);

    /**
     * The ShopSlot row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ShopSlot",
              meta = (DisplayName = "Get ShopSlot Row"))
    static FShopSlotRow GetShopSlotRow(int32 Key, bool& bFound);

    /** How many ShopSlot rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ShopSlot",
              meta = (DisplayName = "Get ShopSlot Row Count"))
    static int32 GetShopSlotRowCount();

    /**
     * The ShopSlot row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|ShopSlot",
              meta = (DisplayName = "Get ShopSlot Row At"))
    static FShopSlotRow GetShopSlotRowAt(int32 Position, bool& bFound);

    /**
     * The Price row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Price",
              meta = (DisplayName = "Get Price Row"))
    static FPriceRow GetPriceRow(int32 Key, bool& bFound);

    /** How many Price rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Price",
              meta = (DisplayName = "Get Price Row Count"))
    static int32 GetPriceRowCount();

    /**
     * The Price row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Price",
              meta = (DisplayName = "Get Price Row At"))
    static FPriceRow GetPriceRowAt(int32 Position, bool& bFound);

    /**
     * The Promo row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Promo",
              meta = (DisplayName = "Get Promo Row"))
    static FPromoRow GetPromoRow(int32 Key, bool& bFound);

    /** How many Promo rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Promo",
              meta = (DisplayName = "Get Promo Row Count"))
    static int32 GetPromoRowCount();

    /**
     * The Promo row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Promo",
              meta = (DisplayName = "Get Promo Row At"))
    static FPromoRow GetPromoRowAt(int32 Position, bool& bFound);

    /**
     * The Skill row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Skill",
              meta = (DisplayName = "Get Skill Row"))
    static FSkillRow GetSkillRow(int32 Key, bool& bFound);

    /** How many Skill rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Skill",
              meta = (DisplayName = "Get Skill Row Count"))
    static int32 GetSkillRowCount();

    /**
     * The Skill row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Skill",
              meta = (DisplayName = "Get Skill Row At"))
    static FSkillRow GetSkillRowAt(int32 Position, bool& bFound);

    /**
     * The SkillLevel row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|SkillLevel",
              meta = (DisplayName = "Get SkillLevel Row"))
    static FSkillLevelRow GetSkillLevelRow(int32 Key, bool& bFound);

    /** How many SkillLevel rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|SkillLevel",
              meta = (DisplayName = "Get SkillLevel Row Count"))
    static int32 GetSkillLevelRowCount();

    /**
     * The SkillLevel row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|SkillLevel",
              meta = (DisplayName = "Get SkillLevel Row At"))
    static FSkillLevelRow GetSkillLevelRowAt(int32 Position, bool& bFound);

    /**
     * The SkillEffect row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|SkillEffect",
              meta = (DisplayName = "Get SkillEffect Row"))
    static FSkillEffectRow GetSkillEffectRow(int32 Key, bool& bFound);

    /** How many SkillEffect rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|SkillEffect",
              meta = (DisplayName = "Get SkillEffect Row Count"))
    static int32 GetSkillEffectRowCount();

    /**
     * The SkillEffect row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|SkillEffect",
              meta = (DisplayName = "Get SkillEffect Row At"))
    static FSkillEffectRow GetSkillEffectRowAt(int32 Position, bool& bFound);

    /**
     * The Buff row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Buff",
              meta = (DisplayName = "Get Buff Row"))
    static FBuffRow GetBuffRow(int32 Key, bool& bFound);

    /** How many Buff rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Buff",
              meta = (DisplayName = "Get Buff Row Count"))
    static int32 GetBuffRowCount();

    /**
     * The Buff row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Buff",
              meta = (DisplayName = "Get Buff Row At"))
    static FBuffRow GetBuffRowAt(int32 Position, bool& bFound);

    /**
     * The BuffPool row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|BuffPool",
              meta = (DisplayName = "Get BuffPool Row"))
    static FBuffPoolRow GetBuffPoolRow(int32 Key, bool& bFound);

    /** How many BuffPool rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|BuffPool",
              meta = (DisplayName = "Get BuffPool Row Count"))
    static int32 GetBuffPoolRowCount();

    /**
     * The BuffPool row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|BuffPool",
              meta = (DisplayName = "Get BuffPool Row At"))
    static FBuffPoolRow GetBuffPoolRowAt(int32 Position, bool& bFound);

    /**
     * The Passive row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Passive",
              meta = (DisplayName = "Get Passive Row"))
    static FPassiveRow GetPassiveRow(int32 Key, bool& bFound);

    /** How many Passive rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Passive",
              meta = (DisplayName = "Get Passive Row Count"))
    static int32 GetPassiveRowCount();

    /**
     * The Passive row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Passive",
              meta = (DisplayName = "Get Passive Row At"))
    static FPassiveRow GetPassiveRowAt(int32 Position, bool& bFound);

    /**
     * The Stage row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Stage",
              meta = (DisplayName = "Get Stage Row"))
    static FStageRow GetStageRow(int32 Key, bool& bFound);

    /** How many Stage rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Stage",
              meta = (DisplayName = "Get Stage Row Count"))
    static int32 GetStageRowCount();

    /**
     * The Stage row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Stage",
              meta = (DisplayName = "Get Stage Row At"))
    static FStageRow GetStageRowAt(int32 Position, bool& bFound);

    /**
     * The StageWave row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageWave",
              meta = (DisplayName = "Get StageWave Row"))
    static FStageWaveRow GetStageWaveRow(int32 Key, bool& bFound);

    /** How many StageWave rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageWave",
              meta = (DisplayName = "Get StageWave Row Count"))
    static int32 GetStageWaveRowCount();

    /**
     * The StageWave row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageWave",
              meta = (DisplayName = "Get StageWave Row At"))
    static FStageWaveRow GetStageWaveRowAt(int32 Position, bool& bFound);

    /**
     * The StageMonster row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageMonster",
              meta = (DisplayName = "Get StageMonster Row"))
    static FStageMonsterRow GetStageMonsterRow(int32 Key, bool& bFound);

    /** How many StageMonster rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageMonster",
              meta = (DisplayName = "Get StageMonster Row Count"))
    static int32 GetStageMonsterRowCount();

    /**
     * The StageMonster row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageMonster",
              meta = (DisplayName = "Get StageMonster Row At"))
    static FStageMonsterRow GetStageMonsterRowAt(int32 Position, bool& bFound);

    /**
     * The StageReward row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageReward",
              meta = (DisplayName = "Get StageReward Row"))
    static FStageRewardRow GetStageRewardRow(int32 Key, bool& bFound);

    /** How many StageReward rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageReward",
              meta = (DisplayName = "Get StageReward Row Count"))
    static int32 GetStageRewardRowCount();

    /**
     * The StageReward row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|StageReward",
              meta = (DisplayName = "Get StageReward Row At"))
    static FStageRewardRow GetStageRewardRowAt(int32 Position, bool& bFound);

    /**
     * The Chapter row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Chapter",
              meta = (DisplayName = "Get Chapter Row"))
    static FChapterRow GetChapterRow(int32 Key, bool& bFound);

    /** How many Chapter rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Chapter",
              meta = (DisplayName = "Get Chapter Row Count"))
    static int32 GetChapterRowCount();

    /**
     * The Chapter row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|Chapter",
              meta = (DisplayName = "Get Chapter Row At"))
    static FChapterRow GetChapterRowAt(int32 Position, bool& bFound);

    /**
     * The World row with the given Id.
     *
     * bFound rather than a pointer, because Blueprint has no null struct - a graph that
     * ignored a failure would otherwise carry a default row it could not tell apart from
     * a real one.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|World",
              meta = (DisplayName = "Get World Row"))
    static FWorldRow GetWorldRow(int32 Key, bool& bFound);

    /** How many World rows were loaded. */
    UFUNCTION(BlueprintPure, Category = "Tabbit|World",
              meta = (DisplayName = "Get World Row Count"))
    static int32 GetWorldRowCount();

    /**
     * The World row at a position, for walking the table in order.
     *
     * A position and a count rather than the whole array. Blueprint takes a return value
     * by value, so handing back a TArray would copy every row of the table on every call -
     * and a reference return is not something Unreal Header Tool accepts. With these two a
     * graph can loop over the table and copy one row per turn.
     */
    UFUNCTION(BlueprintPure, Category = "Tabbit|World",
              meta = (DisplayName = "Get World Row At"))
    static FWorldRow GetWorldRowAt(int32 Position, bool& bFound);

    /**
     * Reads every table from BasePath, as FSproutData::ReadAll does.
     *
     * BlueprintCallable rather than BlueprintPure: it does something.
     */
    UFUNCTION(BlueprintCallable, Category = "Tabbit",
              meta = (DisplayName = "Load All Tabbit Tables"))
    static bool ReadAll(const FString& BasePath,
                        const FString& FileExtension = TEXT(".tcb"));
};
