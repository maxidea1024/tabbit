// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import Foundation

/// Every table, loaded together so cross-table references can be resolved.
///
/// An instance rather than a set of static properties, which is the shape the other targets
/// grew into and the only shape available here: Swift's concurrency checking makes mutable
/// global state an error, so a `static var` table would not compile in a Swift 6 module. What
/// the instance buys is what it buys elsewhere - test isolation, two data versions open at
/// once, a hot reload that swaps one for the other. spec/targets/accessor-instances.md.
public final class Tables {

    public init() {}

    public private(set) var character: CharacterTable = CharacterTable()

    public private(set) var characterStat: CharacterStatTable = CharacterStatTable()

    public private(set) var characterAscend: CharacterAscendTable = CharacterAscendTable()

    public private(set) var characterSkin: CharacterSkinTable = CharacterSkinTable()

    public private(set) var characterVoice: CharacterVoiceTable = CharacterVoiceTable()

    public private(set) var roleBonus: RoleBonusTable = RoleBonusTable()

    public private(set) var elementAffinity: ElementAffinityTable = ElementAffinityTable()

    public private(set) var codex: CodexTable = CodexTable()

    public private(set) var codexEntry: CodexEntryTable = CodexEntryTable()

    public private(set) var codexTier: CodexTierTable = CodexTierTable()

    public private(set) var dialogue: DialogueTable = DialogueTable()

    public private(set) var dialogueLine: DialogueLineTable = DialogueLineTable()

    public private(set) var cutscene: CutsceneTable = CutsceneTable()

    public private(set) var dungeon: DungeonTable = DungeonTable()

    public private(set) var dungeonFloor: DungeonFloorTable = DungeonFloorTable()

    public private(set) var dungeonReward: DungeonRewardTable = DungeonRewardTable()

    public private(set) var raid: RaidTable = RaidTable()

    public private(set) var equip: EquipTable = EquipTable()

    public private(set) var equipLevel: EquipLevelTable = EquipLevelTable()

    public private(set) var equipGrade: EquipGradeTable = EquipGradeTable()

    public private(set) var equipSet: EquipSetTable = EquipSetTable()

    public private(set) var equipSetBonus: EquipSetBonusTable = EquipSetBonusTable()

    public private(set) var equipOption: EquipOptionTable = EquipOptionTable()

    public private(set) var gacha: GachaTable = GachaTable()

    public private(set) var gachaPool: GachaPoolTable = GachaPoolTable()

    public private(set) var gachaOdds: GachaOddsTable = GachaOddsTable()

    public private(set) var pity: PityTable = PityTable()

    public private(set) var levelCurve: LevelCurveTable = LevelCurveTable()

    public private(set) var statBase: StatBaseTable = StatBaseTable()

    public private(set) var ascendCurve: AscendCurveTable = AscendCurveTable()

    public private(set) var powerScore: PowerScoreTable = PowerScoreTable()

    public private(set) var item: ItemTable = ItemTable()

    public private(set) var currency: CurrencyTable = CurrencyTable()

    public private(set) var material: MaterialTable = MaterialTable()

    public private(set) var consumable: ConsumableTable = ConsumableTable()

    public private(set) var offer: OfferTable = OfferTable()

    public private(set) var offerItem: OfferItemTable = OfferItemTable()

    public private(set) var itemGrade: ItemGradeTable = ItemGradeTable()

    public private(set) var string: StringTable = StringTable()

    public private(set) var offlineReward: OfflineRewardTable = OfflineRewardTable()

    public private(set) var idleRate: IdleRateTable = IdleRateTable()

    public private(set) var afkBonus: AfkBonusTable = AfkBonusTable()

    public private(set) var quest: QuestTable = QuestTable()

    public private(set) var questStep: QuestStepTable = QuestStepTable()

    public private(set) var dailyQuest: DailyQuestTable = DailyQuestTable()

    public private(set) var weeklyQuest: WeeklyQuestTable = WeeklyQuestTable()

    public private(set) var achievement: AchievementTable = AchievementTable()

    public private(set) var reward: RewardTable = RewardTable()

    public private(set) var lootGroup: LootGroupTable = LootGroupTable()

    public private(set) var lootEntry: LootEntryTable = LootEntryTable()

    public private(set) var mailReward: MailRewardTable = MailRewardTable()

    public private(set) var setting: SettingTable = SettingTable()

    public private(set) var condition: ConditionTable = ConditionTable()

    public private(set) var unlock: UnlockTable = UnlockTable()

    public private(set) var table: TableTable = TableTable()

    public private(set) var shop: ShopTable = ShopTable()

    public private(set) var shopSlot: ShopSlotTable = ShopSlotTable()

    public private(set) var price: PriceTable = PriceTable()

    public private(set) var promo: PromoTable = PromoTable()

    public private(set) var skill: SkillTable = SkillTable()

    public private(set) var skillLevel: SkillLevelTable = SkillLevelTable()

    public private(set) var skillEffect: SkillEffectTable = SkillEffectTable()

    public private(set) var buff: BuffTable = BuffTable()

    public private(set) var buffPool: BuffPoolTable = BuffPoolTable()

    public private(set) var passive: PassiveTable = PassiveTable()

    public private(set) var stage: StageTable = StageTable()

    public private(set) var stageWave: StageWaveTable = StageWaveTable()

    public private(set) var stageMonster: StageMonsterTable = StageMonsterTable()

    public private(set) var stageReward: StageRewardTable = StageRewardTable()

    public private(set) var chapter: ChapterTable = ChapterTable()

    public private(set) var world: WorldTable = WorldTable()

    /// The key the table files were sealed with, or nil when they were not sealed.
    ///
    /// Beside `readAll` because it is the same kind of thing: one decision the consuming
    /// project makes once, at start-up, before the first table is read. A key threaded
    /// through every read signature would be the same value written out at every call site,
    /// and every call site is somewhere it could be wrong.
    ///
    /// Thirty-two bytes, the key the converter was given. Nil - the default - is a project
    /// that does not encrypt, and it costs that project nothing: the load path opens the
    /// envelope either way, and a file that is not encrypted comes back from that unchanged.
    /// A project that does encrypt and leaves this nil gets the reader's own message saying
    /// the file is encrypted and no key was given, rather than a failure somewhere inside the
    /// format.
    ///
    /// Where the key comes from is the one thing about this layer worth being careful with.
    /// It is a secret only until it reaches a repository, a build log or an issue, and it
    /// cannot be taken back afterwards - so it is read at start-up from wherever this project
    /// keeps secrets, not written into a source file and not left in a resource the app
    /// bundle carries. What the layer is for, and therefore what is and is not worth
    /// defending, is spec/wire/tcb-v104-composed-encodings.md section 4.
    public var encryptionKey: [UInt8]? = nil

    /// The key the table files were signed with, or nil when they were not signed.
    ///
    /// A different key from `encryptionKey` and independent of it: a file can be signed
    /// without being encrypted, and encrypted without being signed.
    ///
    /// What it buys is the thing encryption does not. The cipher is a keystream XOR, so a bit
    /// flipped in the file is a bit flipped in the value it decrypts to - no key needed - and
    /// every structural check the format makes still passes, because a four-byte float
    /// accepts any four bytes. With this set, a file altered after export stops loading.
    ///
    /// Setting it makes the reader stricter in a second way: a file that carries no MAC is
    /// refused, because otherwise the check could be removed by zeroing sixteen bytes. So the
    /// order to turn this on in is data first, key second. spec/wire/tcb-mac-and-signature.md.
    ///
    /// Verifying one needs CryptoKit or swift-crypto. On Apple platforms the first is already
    /// there; elsewhere the reader says which package to add.
    /// doc/languages/swift.md has both paths.
    public var macKey: [UInt8]? = nil

    /// Whether to check the MAC at all. True by default.
    ///
    /// For tools that read exported files without the key, and for measuring what the check
    /// costs at load. It is not a security boundary - anyone who can flip a flag in a shipped
    /// build can read `macKey` out of the same build.
    public var verifyMac: Bool = true

    /// Reads every table from basePath, then links the references between them.
    ///
    /// Safe to call on a loaded accessor. Every file is read into a table of its own and the
    /// references are linked among those, so nothing here is visible until all of it is: a
    /// failure part way through leaves the properties holding the load they already had, and
    /// no row ever points at a row from it.
    public func readAll(
        _ basePath: String,
        fileExtension: String = ".tcb"
    ) throws {
        let base = URL(fileURLWithPath: basePath)

        let loadedCharacterTable = CharacterTable()
        try loadedCharacterTable.read(
            base.appendingPathComponent("Character" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedCharacterStatTable = CharacterStatTable()
        try loadedCharacterStatTable.read(
            base.appendingPathComponent("CharacterStat" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedCharacterAscendTable = CharacterAscendTable()
        try loadedCharacterAscendTable.read(
            base.appendingPathComponent("CharacterAscend" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedCharacterSkinTable = CharacterSkinTable()
        try loadedCharacterSkinTable.read(
            base.appendingPathComponent("CharacterSkin" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedCharacterVoiceTable = CharacterVoiceTable()
        try loadedCharacterVoiceTable.read(
            base.appendingPathComponent("CharacterVoice" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedRoleBonusTable = RoleBonusTable()
        try loadedRoleBonusTable.read(
            base.appendingPathComponent("RoleBonus" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedElementAffinityTable = ElementAffinityTable()
        try loadedElementAffinityTable.read(
            base.appendingPathComponent("ElementAffinity" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedCodexTable = CodexTable()
        try loadedCodexTable.read(
            base.appendingPathComponent("Codex" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedCodexEntryTable = CodexEntryTable()
        try loadedCodexEntryTable.read(
            base.appendingPathComponent("CodexEntry" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedCodexTierTable = CodexTierTable()
        try loadedCodexTierTable.read(
            base.appendingPathComponent("CodexTier" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedDialogueTable = DialogueTable()
        try loadedDialogueTable.read(
            base.appendingPathComponent("Dialogue" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedDialogueLineTable = DialogueLineTable()
        try loadedDialogueLineTable.read(
            base.appendingPathComponent("DialogueLine" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedCutsceneTable = CutsceneTable()
        try loadedCutsceneTable.read(
            base.appendingPathComponent("Cutscene" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedDungeonTable = DungeonTable()
        try loadedDungeonTable.read(
            base.appendingPathComponent("Dungeon" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedDungeonFloorTable = DungeonFloorTable()
        try loadedDungeonFloorTable.read(
            base.appendingPathComponent("DungeonFloor" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedDungeonRewardTable = DungeonRewardTable()
        try loadedDungeonRewardTable.read(
            base.appendingPathComponent("DungeonReward" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedRaidTable = RaidTable()
        try loadedRaidTable.read(
            base.appendingPathComponent("Raid" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedEquipTable = EquipTable()
        try loadedEquipTable.read(
            base.appendingPathComponent("Equip" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedEquipLevelTable = EquipLevelTable()
        try loadedEquipLevelTable.read(
            base.appendingPathComponent("EquipLevel" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedEquipGradeTable = EquipGradeTable()
        try loadedEquipGradeTable.read(
            base.appendingPathComponent("EquipGrade" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedEquipSetTable = EquipSetTable()
        try loadedEquipSetTable.read(
            base.appendingPathComponent("EquipSet" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedEquipSetBonusTable = EquipSetBonusTable()
        try loadedEquipSetBonusTable.read(
            base.appendingPathComponent("EquipSetBonus" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedEquipOptionTable = EquipOptionTable()
        try loadedEquipOptionTable.read(
            base.appendingPathComponent("EquipOption" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedGachaTable = GachaTable()
        try loadedGachaTable.read(
            base.appendingPathComponent("Gacha" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedGachaPoolTable = GachaPoolTable()
        try loadedGachaPoolTable.read(
            base.appendingPathComponent("GachaPool" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedGachaOddsTable = GachaOddsTable()
        try loadedGachaOddsTable.read(
            base.appendingPathComponent("GachaOdds" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedPityTable = PityTable()
        try loadedPityTable.read(
            base.appendingPathComponent("Pity" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedLevelCurveTable = LevelCurveTable()
        try loadedLevelCurveTable.read(
            base.appendingPathComponent("LevelCurve" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedStatBaseTable = StatBaseTable()
        try loadedStatBaseTable.read(
            base.appendingPathComponent("StatBase" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedAscendCurveTable = AscendCurveTable()
        try loadedAscendCurveTable.read(
            base.appendingPathComponent("AscendCurve" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedPowerScoreTable = PowerScoreTable()
        try loadedPowerScoreTable.read(
            base.appendingPathComponent("PowerScore" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedItemTable = ItemTable()
        try loadedItemTable.read(
            base.appendingPathComponent("Item" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedCurrencyTable = CurrencyTable()
        try loadedCurrencyTable.read(
            base.appendingPathComponent("Currency" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedMaterialTable = MaterialTable()
        try loadedMaterialTable.read(
            base.appendingPathComponent("Material" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedConsumableTable = ConsumableTable()
        try loadedConsumableTable.read(
            base.appendingPathComponent("Consumable" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedOfferTable = OfferTable()
        try loadedOfferTable.read(
            base.appendingPathComponent("Offer" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedOfferItemTable = OfferItemTable()
        try loadedOfferItemTable.read(
            base.appendingPathComponent("OfferItem" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedItemGradeTable = ItemGradeTable()
        try loadedItemGradeTable.read(
            base.appendingPathComponent("ItemGrade" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedStringTable = StringTable()
        try loadedStringTable.read(
            base.appendingPathComponent("String" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedOfflineRewardTable = OfflineRewardTable()
        try loadedOfflineRewardTable.read(
            base.appendingPathComponent("OfflineReward" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedIdleRateTable = IdleRateTable()
        try loadedIdleRateTable.read(
            base.appendingPathComponent("IdleRate" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedAfkBonusTable = AfkBonusTable()
        try loadedAfkBonusTable.read(
            base.appendingPathComponent("AfkBonus" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedQuestTable = QuestTable()
        try loadedQuestTable.read(
            base.appendingPathComponent("Quest" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedQuestStepTable = QuestStepTable()
        try loadedQuestStepTable.read(
            base.appendingPathComponent("QuestStep" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedDailyQuestTable = DailyQuestTable()
        try loadedDailyQuestTable.read(
            base.appendingPathComponent("DailyQuest" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedWeeklyQuestTable = WeeklyQuestTable()
        try loadedWeeklyQuestTable.read(
            base.appendingPathComponent("WeeklyQuest" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedAchievementTable = AchievementTable()
        try loadedAchievementTable.read(
            base.appendingPathComponent("Achievement" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedRewardTable = RewardTable()
        try loadedRewardTable.read(
            base.appendingPathComponent("Reward" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedLootGroupTable = LootGroupTable()
        try loadedLootGroupTable.read(
            base.appendingPathComponent("LootGroup" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedLootEntryTable = LootEntryTable()
        try loadedLootEntryTable.read(
            base.appendingPathComponent("LootEntry" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedMailRewardTable = MailRewardTable()
        try loadedMailRewardTable.read(
            base.appendingPathComponent("MailReward" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedSettingTable = SettingTable()
        try loadedSettingTable.read(
            base.appendingPathComponent("Setting" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedConditionTable = ConditionTable()
        try loadedConditionTable.read(
            base.appendingPathComponent("Condition" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedUnlockTable = UnlockTable()
        try loadedUnlockTable.read(
            base.appendingPathComponent("Unlock" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedTableTable = TableTable()
        try loadedTableTable.read(
            base.appendingPathComponent("Table" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedShopTable = ShopTable()
        try loadedShopTable.read(
            base.appendingPathComponent("Shop" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedShopSlotTable = ShopSlotTable()
        try loadedShopSlotTable.read(
            base.appendingPathComponent("ShopSlot" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedPriceTable = PriceTable()
        try loadedPriceTable.read(
            base.appendingPathComponent("Price" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedPromoTable = PromoTable()
        try loadedPromoTable.read(
            base.appendingPathComponent("Promo" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedSkillTable = SkillTable()
        try loadedSkillTable.read(
            base.appendingPathComponent("Skill" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedSkillLevelTable = SkillLevelTable()
        try loadedSkillLevelTable.read(
            base.appendingPathComponent("SkillLevel" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedSkillEffectTable = SkillEffectTable()
        try loadedSkillEffectTable.read(
            base.appendingPathComponent("SkillEffect" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedBuffTable = BuffTable()
        try loadedBuffTable.read(
            base.appendingPathComponent("Buff" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedBuffPoolTable = BuffPoolTable()
        try loadedBuffPoolTable.read(
            base.appendingPathComponent("BuffPool" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedPassiveTable = PassiveTable()
        try loadedPassiveTable.read(
            base.appendingPathComponent("Passive" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedStageTable = StageTable()
        try loadedStageTable.read(
            base.appendingPathComponent("Stage" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedStageWaveTable = StageWaveTable()
        try loadedStageWaveTable.read(
            base.appendingPathComponent("StageWave" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedStageMonsterTable = StageMonsterTable()
        try loadedStageMonsterTable.read(
            base.appendingPathComponent("StageMonster" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedStageRewardTable = StageRewardTable()
        try loadedStageRewardTable.read(
            base.appendingPathComponent("StageReward" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedChapterTable = ChapterTable()
        try loadedChapterTable.read(
            base.appendingPathComponent("Chapter" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        let loadedWorldTable = WorldTable()
        try loadedWorldTable.read(
            base.appendingPathComponent("World" + fileExtension).path,
            key: encryptionKey, macKey: macKey, verifyMac: verifyMac)

        Tables.solveCrossReferences(character: loadedCharacterTable, characterStat: loadedCharacterStatTable, characterAscend: loadedCharacterAscendTable, characterSkin: loadedCharacterSkinTable, characterVoice: loadedCharacterVoiceTable, roleBonus: loadedRoleBonusTable, elementAffinity: loadedElementAffinityTable, codex: loadedCodexTable, codexEntry: loadedCodexEntryTable, codexTier: loadedCodexTierTable, dialogue: loadedDialogueTable, dialogueLine: loadedDialogueLineTable, cutscene: loadedCutsceneTable, dungeon: loadedDungeonTable, dungeonFloor: loadedDungeonFloorTable, dungeonReward: loadedDungeonRewardTable, raid: loadedRaidTable, equip: loadedEquipTable, equipLevel: loadedEquipLevelTable, equipGrade: loadedEquipGradeTable, equipSet: loadedEquipSetTable, equipSetBonus: loadedEquipSetBonusTable, equipOption: loadedEquipOptionTable, gacha: loadedGachaTable, gachaPool: loadedGachaPoolTable, gachaOdds: loadedGachaOddsTable, pity: loadedPityTable, levelCurve: loadedLevelCurveTable, statBase: loadedStatBaseTable, ascendCurve: loadedAscendCurveTable, powerScore: loadedPowerScoreTable, item: loadedItemTable, currency: loadedCurrencyTable, material: loadedMaterialTable, consumable: loadedConsumableTable, offer: loadedOfferTable, offerItem: loadedOfferItemTable, itemGrade: loadedItemGradeTable, string: loadedStringTable, offlineReward: loadedOfflineRewardTable, idleRate: loadedIdleRateTable, afkBonus: loadedAfkBonusTable, quest: loadedQuestTable, questStep: loadedQuestStepTable, dailyQuest: loadedDailyQuestTable, weeklyQuest: loadedWeeklyQuestTable, achievement: loadedAchievementTable, reward: loadedRewardTable, lootGroup: loadedLootGroupTable, lootEntry: loadedLootEntryTable, mailReward: loadedMailRewardTable, setting: loadedSettingTable, condition: loadedConditionTable, unlock: loadedUnlockTable, table: loadedTableTable, shop: loadedShopTable, shopSlot: loadedShopSlotTable, price: loadedPriceTable, promo: loadedPromoTable, skill: loadedSkillTable, skillLevel: loadedSkillLevelTable, skillEffect: loadedSkillEffectTable, buff: loadedBuffTable, buffPool: loadedBuffPoolTable, passive: loadedPassiveTable, stage: loadedStageTable, stageWave: loadedStageWaveTable, stageMonster: loadedStageMonsterTable, stageReward: loadedStageRewardTable, chapter: loadedChapterTable, world: loadedWorldTable)
        character = loadedCharacterTable
        characterStat = loadedCharacterStatTable
        characterAscend = loadedCharacterAscendTable
        characterSkin = loadedCharacterSkinTable
        characterVoice = loadedCharacterVoiceTable
        roleBonus = loadedRoleBonusTable
        elementAffinity = loadedElementAffinityTable
        codex = loadedCodexTable
        codexEntry = loadedCodexEntryTable
        codexTier = loadedCodexTierTable
        dialogue = loadedDialogueTable
        dialogueLine = loadedDialogueLineTable
        cutscene = loadedCutsceneTable
        dungeon = loadedDungeonTable
        dungeonFloor = loadedDungeonFloorTable
        dungeonReward = loadedDungeonRewardTable
        raid = loadedRaidTable
        equip = loadedEquipTable
        equipLevel = loadedEquipLevelTable
        equipGrade = loadedEquipGradeTable
        equipSet = loadedEquipSetTable
        equipSetBonus = loadedEquipSetBonusTable
        equipOption = loadedEquipOptionTable
        gacha = loadedGachaTable
        gachaPool = loadedGachaPoolTable
        gachaOdds = loadedGachaOddsTable
        pity = loadedPityTable
        levelCurve = loadedLevelCurveTable
        statBase = loadedStatBaseTable
        ascendCurve = loadedAscendCurveTable
        powerScore = loadedPowerScoreTable
        item = loadedItemTable
        currency = loadedCurrencyTable
        material = loadedMaterialTable
        consumable = loadedConsumableTable
        offer = loadedOfferTable
        offerItem = loadedOfferItemTable
        itemGrade = loadedItemGradeTable
        string = loadedStringTable
        offlineReward = loadedOfflineRewardTable
        idleRate = loadedIdleRateTable
        afkBonus = loadedAfkBonusTable
        quest = loadedQuestTable
        questStep = loadedQuestStepTable
        dailyQuest = loadedDailyQuestTable
        weeklyQuest = loadedWeeklyQuestTable
        achievement = loadedAchievementTable
        reward = loadedRewardTable
        lootGroup = loadedLootGroupTable
        lootEntry = loadedLootEntryTable
        mailReward = loadedMailRewardTable
        setting = loadedSettingTable
        condition = loadedConditionTable
        unlock = loadedUnlockTable
        table = loadedTableTable
        shop = loadedShopTable
        shopSlot = loadedShopSlotTable
        price = loadedPriceTable
        promo = loadedPromoTable
        skill = loadedSkillTable
        skillLevel = loadedSkillLevelTable
        skillEffect = loadedSkillEffectTable
        buff = loadedBuffTable
        buffPool = loadedBuffPoolTable
        passive = loadedPassiveTable
        stage = loadedStageTable
        stageWave = loadedStageWaveTable
        stageMonster = loadedStageMonsterTable
        stageReward = loadedStageRewardTable
        chapter = loadedChapterTable
        world = loadedWorldTable
    }

    /// Turns the stored keys into usable values, once every table is in memory.
    ///
    /// The tables arrive as arguments rather than being read off the instance, which is how
    /// this resolves the load being read rather than the one already published.
    private static func solveCrossReferences(character: CharacterTable, characterStat: CharacterStatTable, characterAscend: CharacterAscendTable, characterSkin: CharacterSkinTable, characterVoice: CharacterVoiceTable, roleBonus: RoleBonusTable, elementAffinity: ElementAffinityTable, codex: CodexTable, codexEntry: CodexEntryTable, codexTier: CodexTierTable, dialogue: DialogueTable, dialogueLine: DialogueLineTable, cutscene: CutsceneTable, dungeon: DungeonTable, dungeonFloor: DungeonFloorTable, dungeonReward: DungeonRewardTable, raid: RaidTable, equip: EquipTable, equipLevel: EquipLevelTable, equipGrade: EquipGradeTable, equipSet: EquipSetTable, equipSetBonus: EquipSetBonusTable, equipOption: EquipOptionTable, gacha: GachaTable, gachaPool: GachaPoolTable, gachaOdds: GachaOddsTable, pity: PityTable, levelCurve: LevelCurveTable, statBase: StatBaseTable, ascendCurve: AscendCurveTable, powerScore: PowerScoreTable, item: ItemTable, currency: CurrencyTable, material: MaterialTable, consumable: ConsumableTable, offer: OfferTable, offerItem: OfferItemTable, itemGrade: ItemGradeTable, string: StringTable, offlineReward: OfflineRewardTable, idleRate: IdleRateTable, afkBonus: AfkBonusTable, quest: QuestTable, questStep: QuestStepTable, dailyQuest: DailyQuestTable, weeklyQuest: WeeklyQuestTable, achievement: AchievementTable, reward: RewardTable, lootGroup: LootGroupTable, lootEntry: LootEntryTable, mailReward: MailRewardTable, setting: SettingTable, condition: ConditionTable, unlock: UnlockTable, table: TableTable, shop: ShopTable, shopSlot: ShopSlotTable, price: PriceTable, promo: PromoTable, skill: SkillTable, skillLevel: SkillLevelTable, skillEffect: SkillEffectTable, buff: BuffTable, buffPool: BuffPoolTable, passive: PassiveTable, stage: StageTable, stageWave: StageWaveTable, stageMonster: StageMonsterTable, stageReward: StageRewardTable, chapter: ChapterTable, world: WorldTable) {
        // No table references another.
    }
}
