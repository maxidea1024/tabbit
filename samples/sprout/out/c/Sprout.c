/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "Sprout.h"

#include <stdio.h>
#include <string.h>

/* The definitions for the declarations in the forward header. Beside the accessor's own
 * load because that is where a project answers the questions the generated code cannot. */
const uint8_t* Sprout_EncryptionKey = NULL;
int32_t Sprout_EncryptionKeyLength = 0;
const uint8_t* Sprout_MacKey = NULL;
int32_t Sprout_MacKeyLength = 0;
bool Sprout_VerifyMac = true;

bool Sprout_LoadAll(Sprout_t* data, const char* base_path,
                 char* error, size_t error_size) {
  return Sprout_LoadAllWithExtension(
    data, base_path, ".tcb", error, error_size);
}

bool Sprout_LoadAllWithExtension(Sprout_t* data, const char* base_path,
                 const char* file_extension, char* error, size_t error_size) {
  char path[1024];

  /* Loaded beside whatever `data` is already holding and swapped in at the end. Reading
   * again is a reload, and one that fails has to leave the caller's pointers valid: they
   * point into the arenas of the previous load, which is why this cannot start by zeroing
   * `data` - doing that dropped those arenas without freeing them. */
  Sprout_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Character", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CharacterLoad(&loaded.character, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "CharacterStat", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CharacterStatLoad(&loaded.character_stat, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "CharacterAscend", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CharacterAscendLoad(&loaded.character_ascend, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "CharacterSkin", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CharacterSkinLoad(&loaded.character_skin, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "CharacterVoice", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CharacterVoiceLoad(&loaded.character_voice, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "RoleBonus", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_RoleBonusLoad(&loaded.role_bonus, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "ElementAffinity", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_ElementAffinityLoad(&loaded.element_affinity, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Codex", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CodexLoad(&loaded.codex, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "CodexEntry", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CodexEntryLoad(&loaded.codex_entry, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "CodexTier", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CodexTierLoad(&loaded.codex_tier, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Dialogue", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_DialogueLoad(&loaded.dialogue, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "DialogueLine", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_DialogueLineLoad(&loaded.dialogue_line, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Cutscene", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CutsceneLoad(&loaded.cutscene, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Dungeon", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_DungeonLoad(&loaded.dungeon, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "DungeonFloor", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_DungeonFloorLoad(&loaded.dungeon_floor, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "DungeonReward", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_DungeonRewardLoad(&loaded.dungeon_reward, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Raid", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_RaidLoad(&loaded.raid, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Equip", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_EquipLoad(&loaded.equip, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "EquipLevel", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_EquipLevelLoad(&loaded.equip_level, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "EquipGrade", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_EquipGradeLoad(&loaded.equip_grade, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "EquipSet", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_EquipSetLoad(&loaded.equip_set, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "EquipSetBonus", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_EquipSetBonusLoad(&loaded.equip_set_bonus, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "EquipOption", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_EquipOptionLoad(&loaded.equip_option, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Gacha", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_GachaLoad(&loaded.gacha, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "GachaPool", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_GachaPoolLoad(&loaded.gacha_pool, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "GachaOdds", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_GachaOddsLoad(&loaded.gacha_odds, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Pity", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_PityLoad(&loaded.pity, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "LevelCurve", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_LevelCurveLoad(&loaded.level_curve, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "StatBase", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_StatBaseLoad(&loaded.stat_base, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "AscendCurve", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_AscendCurveLoad(&loaded.ascend_curve, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "PowerScore", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_PowerScoreLoad(&loaded.power_score, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Item", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_ItemLoad(&loaded.item, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Currency", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_CurrencyLoad(&loaded.currency, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Material", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_MaterialLoad(&loaded.material, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Consumable", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_ConsumableLoad(&loaded.consumable, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Offer", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_OfferLoad(&loaded.offer, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "OfferItem", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_OfferItemLoad(&loaded.offer_item, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "ItemGrade", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_ItemGradeLoad(&loaded.item_grade, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "String", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_StringLoad(&loaded.string, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "OfflineReward", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_OfflineRewardLoad(&loaded.offline_reward, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "IdleRate", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_IdleRateLoad(&loaded.idle_rate, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "AfkBonus", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_AfkBonusLoad(&loaded.afk_bonus, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Quest", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_QuestLoad(&loaded.quest, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "QuestStep", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_QuestStepLoad(&loaded.quest_step, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "DailyQuest", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_DailyQuestLoad(&loaded.daily_quest, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "WeeklyQuest", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_WeeklyQuestLoad(&loaded.weekly_quest, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Achievement", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_AchievementLoad(&loaded.achievement, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Reward", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_RewardLoad(&loaded.reward, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "LootGroup", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_LootGroupLoad(&loaded.loot_group, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "LootEntry", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_LootEntryLoad(&loaded.loot_entry, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "MailReward", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_MailRewardLoad(&loaded.mail_reward, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Setting", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_SettingLoad(&loaded.setting, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Condition", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_ConditionLoad(&loaded.condition, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Unlock", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_UnlockLoad(&loaded.unlock, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Table", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_TableLoad(&loaded.table, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Shop", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_ShopLoad(&loaded.shop, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "ShopSlot", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_ShopSlotLoad(&loaded.shop_slot, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Price", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_PriceLoad(&loaded.price, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Promo", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_PromoLoad(&loaded.promo, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Skill", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_SkillLoad(&loaded.skill, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "SkillLevel", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_SkillLevelLoad(&loaded.skill_level, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "SkillEffect", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_SkillEffectLoad(&loaded.skill_effect, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Buff", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_BuffLoad(&loaded.buff, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "BuffPool", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_BuffPoolLoad(&loaded.buff_pool, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Passive", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_PassiveLoad(&loaded.passive, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Stage", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_StageLoad(&loaded.stage, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "StageWave", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_StageWaveLoad(&loaded.stage_wave, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "StageMonster", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_StageMonsterLoad(&loaded.stage_monster, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "StageReward", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_StageRewardLoad(&loaded.stage_reward, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "Chapter", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_ChapterLoad(&loaded.chapter, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  if (snprintf(path, sizeof path, "%s/%s%s",
        base_path, "World", file_extension) >= (int)sizeof path) {
    tb_copy_error(error, error_size, base_path, "the path to a table file is too long");
    Sprout_Free(&loaded);
    return false;
  }

  if (!Sprout_WorldLoad(&loaded.world, path, error, error_size)) {
    /* Everything loaded so far goes too. A model missing one table is not one
     * a caller can use, and leaving it allocated makes that a leak as well. */
    Sprout_Free(&loaded);
    return false;
  }

  /* The previous load goes now, and not before. */
  Sprout_Free(data);
  *data = loaded;

  return true;
}

void Sprout_Free(Sprout_t* data) {
  Sprout_CharacterFree(&data->character);
  Sprout_CharacterStatFree(&data->character_stat);
  Sprout_CharacterAscendFree(&data->character_ascend);
  Sprout_CharacterSkinFree(&data->character_skin);
  Sprout_CharacterVoiceFree(&data->character_voice);
  Sprout_RoleBonusFree(&data->role_bonus);
  Sprout_ElementAffinityFree(&data->element_affinity);
  Sprout_CodexFree(&data->codex);
  Sprout_CodexEntryFree(&data->codex_entry);
  Sprout_CodexTierFree(&data->codex_tier);
  Sprout_DialogueFree(&data->dialogue);
  Sprout_DialogueLineFree(&data->dialogue_line);
  Sprout_CutsceneFree(&data->cutscene);
  Sprout_DungeonFree(&data->dungeon);
  Sprout_DungeonFloorFree(&data->dungeon_floor);
  Sprout_DungeonRewardFree(&data->dungeon_reward);
  Sprout_RaidFree(&data->raid);
  Sprout_EquipFree(&data->equip);
  Sprout_EquipLevelFree(&data->equip_level);
  Sprout_EquipGradeFree(&data->equip_grade);
  Sprout_EquipSetFree(&data->equip_set);
  Sprout_EquipSetBonusFree(&data->equip_set_bonus);
  Sprout_EquipOptionFree(&data->equip_option);
  Sprout_GachaFree(&data->gacha);
  Sprout_GachaPoolFree(&data->gacha_pool);
  Sprout_GachaOddsFree(&data->gacha_odds);
  Sprout_PityFree(&data->pity);
  Sprout_LevelCurveFree(&data->level_curve);
  Sprout_StatBaseFree(&data->stat_base);
  Sprout_AscendCurveFree(&data->ascend_curve);
  Sprout_PowerScoreFree(&data->power_score);
  Sprout_ItemFree(&data->item);
  Sprout_CurrencyFree(&data->currency);
  Sprout_MaterialFree(&data->material);
  Sprout_ConsumableFree(&data->consumable);
  Sprout_OfferFree(&data->offer);
  Sprout_OfferItemFree(&data->offer_item);
  Sprout_ItemGradeFree(&data->item_grade);
  Sprout_StringFree(&data->string);
  Sprout_OfflineRewardFree(&data->offline_reward);
  Sprout_IdleRateFree(&data->idle_rate);
  Sprout_AfkBonusFree(&data->afk_bonus);
  Sprout_QuestFree(&data->quest);
  Sprout_QuestStepFree(&data->quest_step);
  Sprout_DailyQuestFree(&data->daily_quest);
  Sprout_WeeklyQuestFree(&data->weekly_quest);
  Sprout_AchievementFree(&data->achievement);
  Sprout_RewardFree(&data->reward);
  Sprout_LootGroupFree(&data->loot_group);
  Sprout_LootEntryFree(&data->loot_entry);
  Sprout_MailRewardFree(&data->mail_reward);
  Sprout_SettingFree(&data->setting);
  Sprout_ConditionFree(&data->condition);
  Sprout_UnlockFree(&data->unlock);
  Sprout_TableFree(&data->table);
  Sprout_ShopFree(&data->shop);
  Sprout_ShopSlotFree(&data->shop_slot);
  Sprout_PriceFree(&data->price);
  Sprout_PromoFree(&data->promo);
  Sprout_SkillFree(&data->skill);
  Sprout_SkillLevelFree(&data->skill_level);
  Sprout_SkillEffectFree(&data->skill_effect);
  Sprout_BuffFree(&data->buff);
  Sprout_BuffPoolFree(&data->buff_pool);
  Sprout_PassiveFree(&data->passive);
  Sprout_StageFree(&data->stage);
  Sprout_StageWaveFree(&data->stage_wave);
  Sprout_StageMonsterFree(&data->stage_monster);
  Sprout_StageRewardFree(&data->stage_reward);
  Sprout_ChapterFree(&data->chapter);
  Sprout_WorldFree(&data->world);
}
