/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef SPROUT_FORWARD_H
#define SPROUT_FORWARD_H

#include <stdbool.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

typedef struct Sprout_CharacterRecord_t Sprout_CharacterRecord_t;
typedef struct Sprout_CharacterStatRecord_t Sprout_CharacterStatRecord_t;
typedef struct Sprout_CharacterAscendRecord_t Sprout_CharacterAscendRecord_t;
typedef struct Sprout_CharacterSkinRecord_t Sprout_CharacterSkinRecord_t;
typedef struct Sprout_CharacterVoiceRecord_t Sprout_CharacterVoiceRecord_t;
typedef struct Sprout_RoleBonusRecord_t Sprout_RoleBonusRecord_t;
typedef struct Sprout_ElementAffinityRecord_t Sprout_ElementAffinityRecord_t;
typedef struct Sprout_CodexRecord_t Sprout_CodexRecord_t;
typedef struct Sprout_CodexEntryRecord_t Sprout_CodexEntryRecord_t;
typedef struct Sprout_CodexTierRecord_t Sprout_CodexTierRecord_t;
typedef struct Sprout_DialogueRecord_t Sprout_DialogueRecord_t;
typedef struct Sprout_DialogueLineRecord_t Sprout_DialogueLineRecord_t;
typedef struct Sprout_CutsceneRecord_t Sprout_CutsceneRecord_t;
typedef struct Sprout_DungeonRecord_t Sprout_DungeonRecord_t;
typedef struct Sprout_DungeonFloorRecord_t Sprout_DungeonFloorRecord_t;
typedef struct Sprout_DungeonRewardRecord_t Sprout_DungeonRewardRecord_t;
typedef struct Sprout_RaidRecord_t Sprout_RaidRecord_t;
typedef struct Sprout_EquipRecord_t Sprout_EquipRecord_t;
typedef struct Sprout_EquipLevelRecord_t Sprout_EquipLevelRecord_t;
typedef struct Sprout_EquipGradeRecord_t Sprout_EquipGradeRecord_t;
typedef struct Sprout_EquipSetRecord_t Sprout_EquipSetRecord_t;
typedef struct Sprout_EquipSetBonusRecord_t Sprout_EquipSetBonusRecord_t;
typedef struct Sprout_EquipOptionRecord_t Sprout_EquipOptionRecord_t;
typedef struct Sprout_GachaRecord_t Sprout_GachaRecord_t;
typedef struct Sprout_GachaPoolRecord_t Sprout_GachaPoolRecord_t;
typedef struct Sprout_GachaOddsRecord_t Sprout_GachaOddsRecord_t;
typedef struct Sprout_PityRecord_t Sprout_PityRecord_t;
typedef struct Sprout_LevelCurveRecord_t Sprout_LevelCurveRecord_t;
typedef struct Sprout_StatBaseRecord_t Sprout_StatBaseRecord_t;
typedef struct Sprout_AscendCurveRecord_t Sprout_AscendCurveRecord_t;
typedef struct Sprout_PowerScoreRecord_t Sprout_PowerScoreRecord_t;
typedef struct Sprout_ItemRecord_t Sprout_ItemRecord_t;
typedef struct Sprout_CurrencyRecord_t Sprout_CurrencyRecord_t;
typedef struct Sprout_MaterialRecord_t Sprout_MaterialRecord_t;
typedef struct Sprout_ConsumableRecord_t Sprout_ConsumableRecord_t;
typedef struct Sprout_OfferRecord_t Sprout_OfferRecord_t;
typedef struct Sprout_OfferItemRecord_t Sprout_OfferItemRecord_t;
typedef struct Sprout_ItemGradeRecord_t Sprout_ItemGradeRecord_t;
typedef struct Sprout_StringRecord_t Sprout_StringRecord_t;
typedef struct Sprout_OfflineRewardRecord_t Sprout_OfflineRewardRecord_t;
typedef struct Sprout_IdleRateRecord_t Sprout_IdleRateRecord_t;
typedef struct Sprout_AfkBonusRecord_t Sprout_AfkBonusRecord_t;
typedef struct Sprout_QuestRecord_t Sprout_QuestRecord_t;
typedef struct Sprout_QuestStepRecord_t Sprout_QuestStepRecord_t;
typedef struct Sprout_DailyQuestRecord_t Sprout_DailyQuestRecord_t;
typedef struct Sprout_WeeklyQuestRecord_t Sprout_WeeklyQuestRecord_t;
typedef struct Sprout_AchievementRecord_t Sprout_AchievementRecord_t;
typedef struct Sprout_RewardRecord_t Sprout_RewardRecord_t;
typedef struct Sprout_LootGroupRecord_t Sprout_LootGroupRecord_t;
typedef struct Sprout_LootEntryRecord_t Sprout_LootEntryRecord_t;
typedef struct Sprout_MailRewardRecord_t Sprout_MailRewardRecord_t;
typedef struct Sprout_SettingRecord_t Sprout_SettingRecord_t;
typedef struct Sprout_ConditionRecord_t Sprout_ConditionRecord_t;
typedef struct Sprout_UnlockRecord_t Sprout_UnlockRecord_t;
typedef struct Sprout_TableRecord_t Sprout_TableRecord_t;
typedef struct Sprout_ShopRecord_t Sprout_ShopRecord_t;
typedef struct Sprout_ShopSlotRecord_t Sprout_ShopSlotRecord_t;
typedef struct Sprout_PriceRecord_t Sprout_PriceRecord_t;
typedef struct Sprout_PromoRecord_t Sprout_PromoRecord_t;
typedef struct Sprout_SkillRecord_t Sprout_SkillRecord_t;
typedef struct Sprout_SkillLevelRecord_t Sprout_SkillLevelRecord_t;
typedef struct Sprout_SkillEffectRecord_t Sprout_SkillEffectRecord_t;
typedef struct Sprout_BuffRecord_t Sprout_BuffRecord_t;
typedef struct Sprout_BuffPoolRecord_t Sprout_BuffPoolRecord_t;
typedef struct Sprout_PassiveRecord_t Sprout_PassiveRecord_t;
typedef struct Sprout_StageRecord_t Sprout_StageRecord_t;
typedef struct Sprout_StageWaveRecord_t Sprout_StageWaveRecord_t;
typedef struct Sprout_StageMonsterRecord_t Sprout_StageMonsterRecord_t;
typedef struct Sprout_StageRewardRecord_t Sprout_StageRewardRecord_t;
typedef struct Sprout_ChapterRecord_t Sprout_ChapterRecord_t;
typedef struct Sprout_WorldRecord_t Sprout_WorldRecord_t;

/* The key the table files were sealed with, and its length - NULL and 0 when they were not
 * sealed.
 *
 * Set once by the consuming project, at start-up, before the first table is read, rather
 * than passed to every Load: a key threaded through every load signature would be the same
 * value written out at every call site, and every call site is somewhere it could be wrong.
 * The bytes are not copied, so whatever they point at has to outlive the loading.
 *
 * Thirty-two bytes, the key the converter was given. NULL - the default - is a project that
 * does not encrypt, and it costs that project nothing: the load path opens the envelope
 * either way, and a file that is not encrypted comes back from that unchanged. A project
 * that does encrypt and leaves this NULL gets the reader's own message saying the file is
 * encrypted and no key was given, rather than a failure somewhere inside the format.
 *
 * Where the key comes from is the one thing about this layer worth being careful with. It is
 * a secret only until it reaches a repository, a build log or an issue, and it cannot be
 * taken back afterwards - so it is read at start-up from wherever this project keeps
 * secrets, not written into a source file as an array literal. What the layer is for, and
 * therefore what is and is not worth defending, is spec/wire/tcb-v104-composed-encodings.md
 * section 4. */
extern const uint8_t* Sprout_EncryptionKey;
extern int32_t Sprout_EncryptionKeyLength;

/* The key the table files were signed with, or NULL when they were not signed.
 *
 * A different key from the encryption one and independent of it: a file can be signed
 * without being encrypted, and encrypted without being signed.
 *
 * What it buys is the thing encryption does not. The cipher is a keystream XOR, so a bit
 * flipped in the file is a bit flipped in the value it decrypts to - no key needed - and
 * every structural check the format makes still passes, because a four-byte float accepts
 * any four bytes. With this set, a file altered after export stops loading instead.
 *
 * Setting it makes the reader stricter in a second way: a file that carries no MAC is
 * refused, because otherwise the check could be removed by zeroing sixteen bytes. So the
 * order to turn this on in is data first, key second. spec/wire/tcb-mac-and-signature.md. */
extern const uint8_t* Sprout_MacKey;
extern int32_t Sprout_MacKeyLength;

/* Whether to check the MAC at all. True by default.
 *
 * For tools that read exported files without the key, and for measuring what the check costs
 * at load. It is not a security boundary - anyone who can flip a flag in a shipped binary can
 * read the key out of the same binary. */
extern bool Sprout_VerifyMac;

#ifdef __cplusplus
}
#endif

#endif /* SPROUT_FORWARD_H */
