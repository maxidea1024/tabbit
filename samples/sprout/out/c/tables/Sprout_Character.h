/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef SPROUT_CHARACTER_H
#define SPROUT_CHARACTER_H

#include "tabbit/tabbit_tcb_reader.h"

#include "Sprout_Forward.h"
#include "enums/Sprout_EnumRoleType.h"
#include "enums/Sprout_EnumElementType.h"
#include "enums/Sprout_EnumGradeType.h"
#include "enums/Sprout_EnumRarityType.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from samples/sprout/Character.xlsx : CharacterTable : A1
 */
struct Sprout_CharacterRecord_t {
  /* primary index */
  int32_t id;
  /* display name key */
  const char* name_key;
  /* portrait asset */
  const char* portrait;
  /* combat role */
  Sprout_RoleType_t role;
  /* element */
  Sprout_ElementType_t element;
  /* initial grade */
  Sprout_GradeType_t grade;
  /* base rarity */
  Sprout_RarityType_t rarity;
  /* skill ids, ';' between */
  int32_t* skill_ids;
  int32_t skill_ids_count;
  /* maximum level */
  int32_t max_level;
  /* released */
  bool released;
};

/* Every row of Character.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Sprout_CharacterTable_t {
  Sprout_CharacterRecord_t* records;
  int32_t count;

  tb_index_entry* by_id;
  tb_arena arena;
} Sprout_CharacterTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Sprout_CharacterLoad(Sprout_CharacterTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Sprout_CharacterFree(Sprout_CharacterTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Sprout_CharacterRecord_t* Sprout_CharacterFindById(
  const Sprout_CharacterTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Sprout_CharacterContainsId(
  const Sprout_CharacterTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* SPROUT_CHARACTER_H */
