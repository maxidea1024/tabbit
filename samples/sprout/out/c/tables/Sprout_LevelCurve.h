/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef SPROUT_LEVEL_CURVE_H
#define SPROUT_LEVEL_CURVE_H

#include "tabbit/tabbit_tcb_reader.h"

#include "Sprout_Forward.h"
#include "enums/Sprout_EnumStatType.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from samples/sprout/Growth.xlsx : LevelCurveTable : A1
 */
struct Sprout_LevelCurveRecord_t {
  /* primary index */
  int32_t id;
  /* curve tier */
  const char* tier_name;
  /* affected stat */
  Sprout_StatType_t curve_stat;
  /* tick within the curve */
  int32_t tick;
  /* value at the start of the step */
  int32_t base_value;
  /* increment per step */
  int32_t step_value;
  /* value at the end of the step */
  int64_t end_value;
  /* ceiling for this curve */
  int64_t cap_value;
  /* balance revision */
  int32_t revision;
};

/* Every row of LevelCurve.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Sprout_LevelCurveTable_t {
  Sprout_LevelCurveRecord_t* records;
  int32_t count;

  tb_index_entry* by_id;
  tb_arena arena;
} Sprout_LevelCurveTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Sprout_LevelCurveLoad(Sprout_LevelCurveTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Sprout_LevelCurveFree(Sprout_LevelCurveTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Sprout_LevelCurveRecord_t* Sprout_LevelCurveFindById(
  const Sprout_LevelCurveTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Sprout_LevelCurveContainsId(
  const Sprout_LevelCurveTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* SPROUT_LEVEL_CURVE_H */
