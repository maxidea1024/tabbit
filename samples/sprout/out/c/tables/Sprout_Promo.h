/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef SPROUT_PROMO_H
#define SPROUT_PROMO_H

#include "tabbit/tabbit_tcb_reader.h"

#include "Sprout_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from samples/sprout/Shop.xlsx : PromoTable : A1
 */
struct Sprout_PromoRecord_t {
  /* primary index */
  int32_t id;
  /* backing offer */
  int32_t offer_id;
  /* banner asset */
  const char* banner;
  /* available days */
  int32_t available_days;
};

/* Every row of Promo.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Sprout_PromoTable_t {
  Sprout_PromoRecord_t* records;
  int32_t count;

  tb_index_entry* by_id;
  tb_arena arena;
} Sprout_PromoTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Sprout_PromoLoad(Sprout_PromoTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Sprout_PromoFree(Sprout_PromoTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Sprout_PromoRecord_t* Sprout_PromoFindById(
  const Sprout_PromoTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Sprout_PromoContainsId(
  const Sprout_PromoTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* SPROUT_PROMO_H */
