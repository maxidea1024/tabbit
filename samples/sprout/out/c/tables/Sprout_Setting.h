/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef SPROUT_SETTING_H
#define SPROUT_SETTING_H

#include "tabbit/tabbit_tcb_reader.h"

#include "Sprout_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from samples/sprout/xlsx/Setting.xlsx : SettingTable : A1
 */
struct Sprout_SettingRecord_t {
  /* setting key */
  const char* setting_key;
  /* value as written */
  const char* value;
  /* description key */
  const char* desc_key;
};

/* Every row of Setting.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Sprout_SettingTable_t {
  Sprout_SettingRecord_t* records;
  int32_t count;

  tb_string_index_entry* by_setting_key;
  tb_arena arena;
} Sprout_SettingTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Sprout_SettingLoad(Sprout_SettingTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Sprout_SettingFree(Sprout_SettingTable_t* table);

/* The row with the given SettingKey, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Sprout_SettingRecord_t* Sprout_SettingFindBySettingKey(
  const Sprout_SettingTable_t* table, const char* key);

/* Whether the table holds a row with this SettingKey. */
bool Sprout_SettingContainsSettingKey(
  const Sprout_SettingTable_t* table, const char* key);

#ifdef __cplusplus
}
#endif

#endif /* SPROUT_SETTING_H */
