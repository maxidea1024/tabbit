// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as tabbit from '../tabbit/tcb-reader'

// The accessor, for the encryption key it holds. `tables` imports this module back, and
// that cycle is only ever read from inside a method - by which point both modules have
// finished evaluating - so neither one is half built when the binding is used.
import { Tables } from '../tables'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  characterId: number
  step: number
  materialIds: number[]
  materialCounts: number[]
  goldCost: string
  statScale: number
}

// Generated from samples/sprout/xlsx/Character.xlsx : CharacterAscendTable : A1
export class CharacterAscendRecord {
  /** Default constructor */
  constructor() {
  }

  /** primary index */
  public get id(): number { return this._id }

  /** owning character */
  public get characterId(): number { return this._characterId }

  /** ascension step */
  public get step(): number { return this._step }

  /** material item ids */
  public get materialIds(): number[] { return this._materialIds }

  /** material counts */
  public get materialCounts(): number[] { return this._materialCounts }

  /** soft currency cost */
  public get goldCost(): bigint { return this._goldCost }

  /** stat multiplier */
  public get statScale(): number { return this._statScale }

  public _id: number = 0
  public _characterId: number = 0
  public _step: number = 0
  public _materialIds: number[] = []
  public _materialCounts: number[] = []
  public _goldCost: bigint = 0n
  public _statScale: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._characterId = dataRow.characterId
    this._step = dataRow.step
    this._materialIds = dataRow.materialIds
    this._materialCounts = dataRow.materialCounts
    this._goldCost = BigInt(dataRow.goldCost)
    this._statScale = Math.fround(dataRow.statScale)
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._characterId = dataRow[offset++]
    this._step = dataRow[offset++]
    this._materialIds = dataRow[offset++]
    this._materialCounts = dataRow[offset++]
    this._goldCost = BigInt(dataRow[offset++])
    this._statScale = Math.fround(dataRow[offset++])
  }
}

// Generated from samples/sprout/xlsx/Character.xlsx : CharacterAscendTable : A1
export class CharacterAscendTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): CharacterAscendRecord[] { return this._records }
  private _records: CharacterAscendRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, CharacterAscendRecord> { return this._recordsById }
  private _recordsById: Map<number, CharacterAscendRecord> = new Map<number, CharacterAscendRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): CharacterAscendRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): CharacterAscendRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "CharacterAscend" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: CharacterAscendRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new CharacterAscendRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new CharacterAscendRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .tcb file. */
  public readBinarySync(filename: string): void {
    // Unconditionally, and with whatever `Tables.encryptionKey` and `Tables.macKey` hold -
    // which is null unless the project set them. A file that is neither encrypted nor signed
    // comes back from this untouched, so the load path is the same either way and there is
    // no condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    this.readBinaryFrom(tabbit.open(
      tabbit.readAllBytes(filename),
      Tables.encryptionKey, Tables.macKey, Tables.verifyMac))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new tabbit.TcbReader(data)
    const { rowCount, columns } = tabbit.readTableHeader(reader)
    let cursor: tabbit.TcbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: CharacterAscendRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new CharacterAscendRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          tabbit.checkColumn(column, 'CharacterAscend.Id', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CharacterAscend.Id')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._id = value
          }
          break
        case 2:
          tabbit.checkColumn(column, 'CharacterAscend.CharacterId', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CharacterAscend.CharacterId')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._characterId = value
          }
          break
        case 3:
          tabbit.checkColumn(column, 'CharacterAscend.Step', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CharacterAscend.Step')
          for (let i = 0; i < rowCount; ) {
            const { n, value } = cursor.nextSameI32(rowCount - i)
            for (let left = n; left > 0; --left, ++i)
              records[i]._step = value
          }
          break
        case 4:
          tabbit.checkColumn(column, 'CharacterAscend.MaterialIds', tabbit.KIND_ARRAY, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CharacterAscend.MaterialIds')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = cursor.nextLength()
            record._materialIds = []
            for (let j = 0; j < elementCount; ++j) {
              record._materialIds.push(cursor.nextI32())
            }
          }
          break
        case 5:
          tabbit.checkColumn(column, 'CharacterAscend.MaterialCounts', tabbit.KIND_ARRAY, false, [tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CharacterAscend.MaterialCounts')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = cursor.nextLength()
            record._materialCounts = []
            for (let j = 0; j < elementCount; ++j) {
              record._materialCounts.push(cursor.nextI32())
            }
          }
          break
        case 6:
          tabbit.checkColumn(column, 'CharacterAscend.GoldCost', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_I64, tabbit.ELEMENT_I32, tabbit.ELEMENT_VARINT])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CharacterAscend.GoldCost')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._goldCost = cursor.nextI64()
          }
          break
        case 7:
          tabbit.checkColumn(column, 'CharacterAscend.StatScale', tabbit.KIND_SCALAR, false, [tabbit.ELEMENT_F32])
          cursor = new tabbit.TcbColumnCursor(reader, column, rowCount, 'CharacterAscend.StatScale')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._statScale = cursor.nextF32()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      tabbit.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: CharacterAscendRecord[]): void {
    const recordsById = new Map<number, CharacterAscendRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
