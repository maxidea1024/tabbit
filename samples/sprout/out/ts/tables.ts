// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

import * as path from 'path'

import { CharacterTable } from './tables/character'
import { CharacterStatTable } from './tables/character-stat'
import { CharacterAscendTable } from './tables/character-ascend'
import { CharacterSkinTable } from './tables/character-skin'
import { CharacterVoiceTable } from './tables/character-voice'
import { RoleBonusTable } from './tables/role-bonus'
import { ElementAffinityTable } from './tables/element-affinity'
import { CodexTable } from './tables/codex'
import { CodexEntryTable } from './tables/codex-entry'
import { CodexTierTable } from './tables/codex-tier'
import { DialogueTable } from './tables/dialogue'
import { DialogueLineTable } from './tables/dialogue-line'
import { CutsceneTable } from './tables/cutscene'
import { DungeonTable } from './tables/dungeon'
import { DungeonFloorTable } from './tables/dungeon-floor'
import { DungeonRewardTable } from './tables/dungeon-reward'
import { RaidTable } from './tables/raid'
import { EquipTable } from './tables/equip'
import { EquipLevelTable } from './tables/equip-level'
import { EquipGradeTable } from './tables/equip-grade'
import { EquipSetTable } from './tables/equip-set'
import { EquipSetBonusTable } from './tables/equip-set-bonus'
import { EquipOptionTable } from './tables/equip-option'
import { GachaTable } from './tables/gacha'
import { GachaPoolTable } from './tables/gacha-pool'
import { GachaOddsTable } from './tables/gacha-odds'
import { PityTable } from './tables/pity'
import { LevelCurveTable } from './tables/level-curve'
import { StatBaseTable } from './tables/stat-base'
import { AscendCurveTable } from './tables/ascend-curve'
import { PowerScoreTable } from './tables/power-score'
import { ItemTable } from './tables/item'
import { CurrencyTable } from './tables/currency'
import { MaterialTable } from './tables/material'
import { ConsumableTable } from './tables/consumable'
import { OfferTable } from './tables/offer'
import { OfferItemTable } from './tables/offer-item'
import { ItemGradeTable } from './tables/item-grade'
import { StringTable } from './tables/string'
import { OfflineRewardTable } from './tables/offline-reward'
import { IdleRateTable } from './tables/idle-rate'
import { AfkBonusTable } from './tables/afk-bonus'
import { QuestTable } from './tables/quest'
import { QuestStepTable } from './tables/quest-step'
import { DailyQuestTable } from './tables/daily-quest'
import { WeeklyQuestTable } from './tables/weekly-quest'
import { AchievementTable } from './tables/achievement'
import { RewardTable } from './tables/reward'
import { LootGroupTable } from './tables/loot-group'
import { LootEntryTable } from './tables/loot-entry'
import { MailRewardTable } from './tables/mail-reward'
import { SettingTable } from './tables/setting'
import { ConditionTable } from './tables/condition'
import { UnlockTable } from './tables/unlock'
import { TableTable } from './tables/table'
import { ShopTable } from './tables/shop'
import { ShopSlotTable } from './tables/shop-slot'
import { PriceTable } from './tables/price'
import { PromoTable } from './tables/promo'
import { SkillTable } from './tables/skill'
import { SkillLevelTable } from './tables/skill-level'
import { SkillEffectTable } from './tables/skill-effect'
import { BuffTable } from './tables/buff'
import { BuffPoolTable } from './tables/buff-pool'
import { PassiveTable } from './tables/passive'
import { StageTable } from './tables/stage'
import { StageWaveTable } from './tables/stage-wave'
import { StageMonsterTable } from './tables/stage-monster'
import { StageRewardTable } from './tables/stage-reward'
import { ChapterTable } from './tables/chapter'
import { WorldTable } from './tables/world'

/** Tables */
export class Tables {
  /**
   * The key the table files were sealed with, or null when they were not sealed.
   *
   * Beside the load calls because it is the same kind of thing: one decision the consuming
   * project makes once, at start-up, before the first table is read. A key threaded through
   * every read signature would be the same value written out at every call site, and every
   * call site is somewhere it could be wrong.
   *
   * Thirty-two bytes, the key the converter was given. Null - the default - is a project
   * that does not encrypt, and it costs that project nothing: the load path opens the
   * envelope either way, and a file that is not encrypted comes back from that unchanged. A
   * project that does encrypt and leaves this null gets the reader's own message saying the
   * file is encrypted and no key was given, rather than a failure somewhere inside the
   * format.
   *
   * Where the key comes from is the one thing about this layer worth being careful with. It
   * is a secret only until it reaches a repository, a build log or an issue, and it cannot
   * be taken back afterwards - so it is read at start-up from wherever this project keeps
   * secrets, not written into a source file and not bundled into anything a browser can
   * fetch. What the layer is for, and therefore what is and is not worth defending, is
   * spec/wire/tcb-v104-composed-encodings.md section 4.
   */
  public static encryptionKey: Uint8Array | null = null

  /**
   * The key the table files were signed with, or null when they were not signed.
   *
   * A different key from `encryptionKey` and independent of it: a file can be signed without
   * being encrypted, and encrypted without being signed.
   *
   * What it buys is the thing encryption does not. The cipher is a keystream XOR, so a bit
   * flipped in the file is a bit flipped in the value it decrypts to - no key needed - and
   * every structural check the format makes still passes, because a four-byte float accepts
   * any four bytes. With this set, a file altered after export stops loading instead.
   *
   * Setting it makes the reader stricter in a second way: a file that carries no MAC is
   * refused, because otherwise the check could be removed by zeroing sixteen bytes. So the
   * order to turn this on in is data first, key second.
   * spec/wire/tcb-mac-and-signature.md.
   */
  public static macKey: Uint8Array | null = null

  /**
   * Whether to check the MAC at all. True by default.
   *
   * For tools that read exported files without the key, and for measuring what the check
   * costs at load. It is not a security boundary - anyone who can flip a flag in a shipped
   * bundle can read `macKey` out of the same bundle.
   */
  public static verifyMac = true

  /** Peroperty for table Character */
  public get character(): CharacterTable { return this._character }
  private _character: CharacterTable = new CharacterTable()

  /** Peroperty for table CharacterStat */
  public get characterStat(): CharacterStatTable { return this._characterStat }
  private _characterStat: CharacterStatTable = new CharacterStatTable()

  /** Peroperty for table CharacterAscend */
  public get characterAscend(): CharacterAscendTable { return this._characterAscend }
  private _characterAscend: CharacterAscendTable = new CharacterAscendTable()

  /** Peroperty for table CharacterSkin */
  public get characterSkin(): CharacterSkinTable { return this._characterSkin }
  private _characterSkin: CharacterSkinTable = new CharacterSkinTable()

  /** Peroperty for table CharacterVoice */
  public get characterVoice(): CharacterVoiceTable { return this._characterVoice }
  private _characterVoice: CharacterVoiceTable = new CharacterVoiceTable()

  /** Peroperty for table RoleBonus */
  public get roleBonus(): RoleBonusTable { return this._roleBonus }
  private _roleBonus: RoleBonusTable = new RoleBonusTable()

  /** Peroperty for table ElementAffinity */
  public get elementAffinity(): ElementAffinityTable { return this._elementAffinity }
  private _elementAffinity: ElementAffinityTable = new ElementAffinityTable()

  /** Peroperty for table Codex */
  public get codex(): CodexTable { return this._codex }
  private _codex: CodexTable = new CodexTable()

  /** Peroperty for table CodexEntry */
  public get codexEntry(): CodexEntryTable { return this._codexEntry }
  private _codexEntry: CodexEntryTable = new CodexEntryTable()

  /** Peroperty for table CodexTier */
  public get codexTier(): CodexTierTable { return this._codexTier }
  private _codexTier: CodexTierTable = new CodexTierTable()

  /** Peroperty for table Dialogue */
  public get dialogue(): DialogueTable { return this._dialogue }
  private _dialogue: DialogueTable = new DialogueTable()

  /** Peroperty for table DialogueLine */
  public get dialogueLine(): DialogueLineTable { return this._dialogueLine }
  private _dialogueLine: DialogueLineTable = new DialogueLineTable()

  /** Peroperty for table Cutscene */
  public get cutscene(): CutsceneTable { return this._cutscene }
  private _cutscene: CutsceneTable = new CutsceneTable()

  /** Peroperty for table Dungeon */
  public get dungeon(): DungeonTable { return this._dungeon }
  private _dungeon: DungeonTable = new DungeonTable()

  /** Peroperty for table DungeonFloor */
  public get dungeonFloor(): DungeonFloorTable { return this._dungeonFloor }
  private _dungeonFloor: DungeonFloorTable = new DungeonFloorTable()

  /** Peroperty for table DungeonReward */
  public get dungeonReward(): DungeonRewardTable { return this._dungeonReward }
  private _dungeonReward: DungeonRewardTable = new DungeonRewardTable()

  /** Peroperty for table Raid */
  public get raid(): RaidTable { return this._raid }
  private _raid: RaidTable = new RaidTable()

  /** Peroperty for table Equip */
  public get equip(): EquipTable { return this._equip }
  private _equip: EquipTable = new EquipTable()

  /** Peroperty for table EquipLevel */
  public get equipLevel(): EquipLevelTable { return this._equipLevel }
  private _equipLevel: EquipLevelTable = new EquipLevelTable()

  /** Peroperty for table EquipGrade */
  public get equipGrade(): EquipGradeTable { return this._equipGrade }
  private _equipGrade: EquipGradeTable = new EquipGradeTable()

  /** Peroperty for table EquipSet */
  public get equipSet(): EquipSetTable { return this._equipSet }
  private _equipSet: EquipSetTable = new EquipSetTable()

  /** Peroperty for table EquipSetBonus */
  public get equipSetBonus(): EquipSetBonusTable { return this._equipSetBonus }
  private _equipSetBonus: EquipSetBonusTable = new EquipSetBonusTable()

  /** Peroperty for table EquipOption */
  public get equipOption(): EquipOptionTable { return this._equipOption }
  private _equipOption: EquipOptionTable = new EquipOptionTable()

  /** Peroperty for table Gacha */
  public get gacha(): GachaTable { return this._gacha }
  private _gacha: GachaTable = new GachaTable()

  /** Peroperty for table GachaPool */
  public get gachaPool(): GachaPoolTable { return this._gachaPool }
  private _gachaPool: GachaPoolTable = new GachaPoolTable()

  /** Peroperty for table GachaOdds */
  public get gachaOdds(): GachaOddsTable { return this._gachaOdds }
  private _gachaOdds: GachaOddsTable = new GachaOddsTable()

  /** Peroperty for table Pity */
  public get pity(): PityTable { return this._pity }
  private _pity: PityTable = new PityTable()

  /** Peroperty for table LevelCurve */
  public get levelCurve(): LevelCurveTable { return this._levelCurve }
  private _levelCurve: LevelCurveTable = new LevelCurveTable()

  /** Peroperty for table StatBase */
  public get statBase(): StatBaseTable { return this._statBase }
  private _statBase: StatBaseTable = new StatBaseTable()

  /** Peroperty for table AscendCurve */
  public get ascendCurve(): AscendCurveTable { return this._ascendCurve }
  private _ascendCurve: AscendCurveTable = new AscendCurveTable()

  /** Peroperty for table PowerScore */
  public get powerScore(): PowerScoreTable { return this._powerScore }
  private _powerScore: PowerScoreTable = new PowerScoreTable()

  /** Peroperty for table Item */
  public get item(): ItemTable { return this._item }
  private _item: ItemTable = new ItemTable()

  /** Peroperty for table Currency */
  public get currency(): CurrencyTable { return this._currency }
  private _currency: CurrencyTable = new CurrencyTable()

  /** Peroperty for table Material */
  public get material(): MaterialTable { return this._material }
  private _material: MaterialTable = new MaterialTable()

  /** Peroperty for table Consumable */
  public get consumable(): ConsumableTable { return this._consumable }
  private _consumable: ConsumableTable = new ConsumableTable()

  /** Peroperty for table Offer */
  public get offer(): OfferTable { return this._offer }
  private _offer: OfferTable = new OfferTable()

  /** Peroperty for table OfferItem */
  public get offerItem(): OfferItemTable { return this._offerItem }
  private _offerItem: OfferItemTable = new OfferItemTable()

  /** Peroperty for table ItemGrade */
  public get itemGrade(): ItemGradeTable { return this._itemGrade }
  private _itemGrade: ItemGradeTable = new ItemGradeTable()

  /** Peroperty for table String */
  public get string(): StringTable { return this._string }
  private _string: StringTable = new StringTable()

  /** Peroperty for table OfflineReward */
  public get offlineReward(): OfflineRewardTable { return this._offlineReward }
  private _offlineReward: OfflineRewardTable = new OfflineRewardTable()

  /** Peroperty for table IdleRate */
  public get idleRate(): IdleRateTable { return this._idleRate }
  private _idleRate: IdleRateTable = new IdleRateTable()

  /** Peroperty for table AfkBonus */
  public get afkBonus(): AfkBonusTable { return this._afkBonus }
  private _afkBonus: AfkBonusTable = new AfkBonusTable()

  /** Peroperty for table Quest */
  public get quest(): QuestTable { return this._quest }
  private _quest: QuestTable = new QuestTable()

  /** Peroperty for table QuestStep */
  public get questStep(): QuestStepTable { return this._questStep }
  private _questStep: QuestStepTable = new QuestStepTable()

  /** Peroperty for table DailyQuest */
  public get dailyQuest(): DailyQuestTable { return this._dailyQuest }
  private _dailyQuest: DailyQuestTable = new DailyQuestTable()

  /** Peroperty for table WeeklyQuest */
  public get weeklyQuest(): WeeklyQuestTable { return this._weeklyQuest }
  private _weeklyQuest: WeeklyQuestTable = new WeeklyQuestTable()

  /** Peroperty for table Achievement */
  public get achievement(): AchievementTable { return this._achievement }
  private _achievement: AchievementTable = new AchievementTable()

  /** Peroperty for table Reward */
  public get reward(): RewardTable { return this._reward }
  private _reward: RewardTable = new RewardTable()

  /** Peroperty for table LootGroup */
  public get lootGroup(): LootGroupTable { return this._lootGroup }
  private _lootGroup: LootGroupTable = new LootGroupTable()

  /** Peroperty for table LootEntry */
  public get lootEntry(): LootEntryTable { return this._lootEntry }
  private _lootEntry: LootEntryTable = new LootEntryTable()

  /** Peroperty for table MailReward */
  public get mailReward(): MailRewardTable { return this._mailReward }
  private _mailReward: MailRewardTable = new MailRewardTable()

  /** Peroperty for table Setting */
  public get setting(): SettingTable { return this._setting }
  private _setting: SettingTable = new SettingTable()

  /** Peroperty for table Condition */
  public get condition(): ConditionTable { return this._condition }
  private _condition: ConditionTable = new ConditionTable()

  /** Peroperty for table Unlock */
  public get unlock(): UnlockTable { return this._unlock }
  private _unlock: UnlockTable = new UnlockTable()

  /** Peroperty for table Table */
  public get table(): TableTable { return this._table }
  private _table: TableTable = new TableTable()

  /** Peroperty for table Shop */
  public get shop(): ShopTable { return this._shop }
  private _shop: ShopTable = new ShopTable()

  /** Peroperty for table ShopSlot */
  public get shopSlot(): ShopSlotTable { return this._shopSlot }
  private _shopSlot: ShopSlotTable = new ShopSlotTable()

  /** Peroperty for table Price */
  public get price(): PriceTable { return this._price }
  private _price: PriceTable = new PriceTable()

  /** Peroperty for table Promo */
  public get promo(): PromoTable { return this._promo }
  private _promo: PromoTable = new PromoTable()

  /** Peroperty for table Skill */
  public get skill(): SkillTable { return this._skill }
  private _skill: SkillTable = new SkillTable()

  /** Peroperty for table SkillLevel */
  public get skillLevel(): SkillLevelTable { return this._skillLevel }
  private _skillLevel: SkillLevelTable = new SkillLevelTable()

  /** Peroperty for table SkillEffect */
  public get skillEffect(): SkillEffectTable { return this._skillEffect }
  private _skillEffect: SkillEffectTable = new SkillEffectTable()

  /** Peroperty for table Buff */
  public get buff(): BuffTable { return this._buff }
  private _buff: BuffTable = new BuffTable()

  /** Peroperty for table BuffPool */
  public get buffPool(): BuffPoolTable { return this._buffPool }
  private _buffPool: BuffPoolTable = new BuffPoolTable()

  /** Peroperty for table Passive */
  public get passive(): PassiveTable { return this._passive }
  private _passive: PassiveTable = new PassiveTable()

  /** Peroperty for table Stage */
  public get stage(): StageTable { return this._stage }
  private _stage: StageTable = new StageTable()

  /** Peroperty for table StageWave */
  public get stageWave(): StageWaveTable { return this._stageWave }
  private _stageWave: StageWaveTable = new StageWaveTable()

  /** Peroperty for table StageMonster */
  public get stageMonster(): StageMonsterTable { return this._stageMonster }
  private _stageMonster: StageMonsterTable = new StageMonsterTable()

  /** Peroperty for table StageReward */
  public get stageReward(): StageRewardTable { return this._stageReward }
  private _stageReward: StageRewardTable = new StageRewardTable()

  /** Peroperty for table Chapter */
  public get chapter(): ChapterTable { return this._chapter }
  private _chapter: ChapterTable = new ChapterTable()

  /** Peroperty for table World */
  public get world(): WorldTable { return this._world }
  private _world: WorldTable = new WorldTable()

  /**
   * Read all tables asynchronously.
   *
   * `fileExtension` defaults to what the JSON exporter writes. Pass a different one when the
   * data files were renamed after export.
   */
  public async readAll(basePath: string, fileExtension: string = '.json'): Promise<void> {
    const character = new CharacterTable()
    await character.read(path.join(basePath, `Character${fileExtension}`))
    const characterStat = new CharacterStatTable()
    await characterStat.read(path.join(basePath, `CharacterStat${fileExtension}`))
    const characterAscend = new CharacterAscendTable()
    await characterAscend.read(path.join(basePath, `CharacterAscend${fileExtension}`))
    const characterSkin = new CharacterSkinTable()
    await characterSkin.read(path.join(basePath, `CharacterSkin${fileExtension}`))
    const characterVoice = new CharacterVoiceTable()
    await characterVoice.read(path.join(basePath, `CharacterVoice${fileExtension}`))
    const roleBonus = new RoleBonusTable()
    await roleBonus.read(path.join(basePath, `RoleBonus${fileExtension}`))
    const elementAffinity = new ElementAffinityTable()
    await elementAffinity.read(path.join(basePath, `ElementAffinity${fileExtension}`))
    const codex = new CodexTable()
    await codex.read(path.join(basePath, `Codex${fileExtension}`))
    const codexEntry = new CodexEntryTable()
    await codexEntry.read(path.join(basePath, `CodexEntry${fileExtension}`))
    const codexTier = new CodexTierTable()
    await codexTier.read(path.join(basePath, `CodexTier${fileExtension}`))
    const dialogue = new DialogueTable()
    await dialogue.read(path.join(basePath, `Dialogue${fileExtension}`))
    const dialogueLine = new DialogueLineTable()
    await dialogueLine.read(path.join(basePath, `DialogueLine${fileExtension}`))
    const cutscene = new CutsceneTable()
    await cutscene.read(path.join(basePath, `Cutscene${fileExtension}`))
    const dungeon = new DungeonTable()
    await dungeon.read(path.join(basePath, `Dungeon${fileExtension}`))
    const dungeonFloor = new DungeonFloorTable()
    await dungeonFloor.read(path.join(basePath, `DungeonFloor${fileExtension}`))
    const dungeonReward = new DungeonRewardTable()
    await dungeonReward.read(path.join(basePath, `DungeonReward${fileExtension}`))
    const raid = new RaidTable()
    await raid.read(path.join(basePath, `Raid${fileExtension}`))
    const equip = new EquipTable()
    await equip.read(path.join(basePath, `Equip${fileExtension}`))
    const equipLevel = new EquipLevelTable()
    await equipLevel.read(path.join(basePath, `EquipLevel${fileExtension}`))
    const equipGrade = new EquipGradeTable()
    await equipGrade.read(path.join(basePath, `EquipGrade${fileExtension}`))
    const equipSet = new EquipSetTable()
    await equipSet.read(path.join(basePath, `EquipSet${fileExtension}`))
    const equipSetBonus = new EquipSetBonusTable()
    await equipSetBonus.read(path.join(basePath, `EquipSetBonus${fileExtension}`))
    const equipOption = new EquipOptionTable()
    await equipOption.read(path.join(basePath, `EquipOption${fileExtension}`))
    const gacha = new GachaTable()
    await gacha.read(path.join(basePath, `Gacha${fileExtension}`))
    const gachaPool = new GachaPoolTable()
    await gachaPool.read(path.join(basePath, `GachaPool${fileExtension}`))
    const gachaOdds = new GachaOddsTable()
    await gachaOdds.read(path.join(basePath, `GachaOdds${fileExtension}`))
    const pity = new PityTable()
    await pity.read(path.join(basePath, `Pity${fileExtension}`))
    const levelCurve = new LevelCurveTable()
    await levelCurve.read(path.join(basePath, `LevelCurve${fileExtension}`))
    const statBase = new StatBaseTable()
    await statBase.read(path.join(basePath, `StatBase${fileExtension}`))
    const ascendCurve = new AscendCurveTable()
    await ascendCurve.read(path.join(basePath, `AscendCurve${fileExtension}`))
    const powerScore = new PowerScoreTable()
    await powerScore.read(path.join(basePath, `PowerScore${fileExtension}`))
    const item = new ItemTable()
    await item.read(path.join(basePath, `Item${fileExtension}`))
    const currency = new CurrencyTable()
    await currency.read(path.join(basePath, `Currency${fileExtension}`))
    const material = new MaterialTable()
    await material.read(path.join(basePath, `Material${fileExtension}`))
    const consumable = new ConsumableTable()
    await consumable.read(path.join(basePath, `Consumable${fileExtension}`))
    const offer = new OfferTable()
    await offer.read(path.join(basePath, `Offer${fileExtension}`))
    const offerItem = new OfferItemTable()
    await offerItem.read(path.join(basePath, `OfferItem${fileExtension}`))
    const itemGrade = new ItemGradeTable()
    await itemGrade.read(path.join(basePath, `ItemGrade${fileExtension}`))
    const string = new StringTable()
    await string.read(path.join(basePath, `String${fileExtension}`))
    const offlineReward = new OfflineRewardTable()
    await offlineReward.read(path.join(basePath, `OfflineReward${fileExtension}`))
    const idleRate = new IdleRateTable()
    await idleRate.read(path.join(basePath, `IdleRate${fileExtension}`))
    const afkBonus = new AfkBonusTable()
    await afkBonus.read(path.join(basePath, `AfkBonus${fileExtension}`))
    const quest = new QuestTable()
    await quest.read(path.join(basePath, `Quest${fileExtension}`))
    const questStep = new QuestStepTable()
    await questStep.read(path.join(basePath, `QuestStep${fileExtension}`))
    const dailyQuest = new DailyQuestTable()
    await dailyQuest.read(path.join(basePath, `DailyQuest${fileExtension}`))
    const weeklyQuest = new WeeklyQuestTable()
    await weeklyQuest.read(path.join(basePath, `WeeklyQuest${fileExtension}`))
    const achievement = new AchievementTable()
    await achievement.read(path.join(basePath, `Achievement${fileExtension}`))
    const reward = new RewardTable()
    await reward.read(path.join(basePath, `Reward${fileExtension}`))
    const lootGroup = new LootGroupTable()
    await lootGroup.read(path.join(basePath, `LootGroup${fileExtension}`))
    const lootEntry = new LootEntryTable()
    await lootEntry.read(path.join(basePath, `LootEntry${fileExtension}`))
    const mailReward = new MailRewardTable()
    await mailReward.read(path.join(basePath, `MailReward${fileExtension}`))
    const setting = new SettingTable()
    await setting.read(path.join(basePath, `Setting${fileExtension}`))
    const condition = new ConditionTable()
    await condition.read(path.join(basePath, `Condition${fileExtension}`))
    const unlock = new UnlockTable()
    await unlock.read(path.join(basePath, `Unlock${fileExtension}`))
    const table = new TableTable()
    await table.read(path.join(basePath, `Table${fileExtension}`))
    const shop = new ShopTable()
    await shop.read(path.join(basePath, `Shop${fileExtension}`))
    const shopSlot = new ShopSlotTable()
    await shopSlot.read(path.join(basePath, `ShopSlot${fileExtension}`))
    const price = new PriceTable()
    await price.read(path.join(basePath, `Price${fileExtension}`))
    const promo = new PromoTable()
    await promo.read(path.join(basePath, `Promo${fileExtension}`))
    const skill = new SkillTable()
    await skill.read(path.join(basePath, `Skill${fileExtension}`))
    const skillLevel = new SkillLevelTable()
    await skillLevel.read(path.join(basePath, `SkillLevel${fileExtension}`))
    const skillEffect = new SkillEffectTable()
    await skillEffect.read(path.join(basePath, `SkillEffect${fileExtension}`))
    const buff = new BuffTable()
    await buff.read(path.join(basePath, `Buff${fileExtension}`))
    const buffPool = new BuffPoolTable()
    await buffPool.read(path.join(basePath, `BuffPool${fileExtension}`))
    const passive = new PassiveTable()
    await passive.read(path.join(basePath, `Passive${fileExtension}`))
    const stage = new StageTable()
    await stage.read(path.join(basePath, `Stage${fileExtension}`))
    const stageWave = new StageWaveTable()
    await stageWave.read(path.join(basePath, `StageWave${fileExtension}`))
    const stageMonster = new StageMonsterTable()
    await stageMonster.read(path.join(basePath, `StageMonster${fileExtension}`))
    const stageReward = new StageRewardTable()
    await stageReward.read(path.join(basePath, `StageReward${fileExtension}`))
    const chapter = new ChapterTable()
    await chapter.read(path.join(basePath, `Chapter${fileExtension}`))
    const world = new WorldTable()
    await world.read(path.join(basePath, `World${fileExtension}`))

    this.publish(character, characterStat, characterAscend, characterSkin, characterVoice, roleBonus, elementAffinity, codex, codexEntry, codexTier, dialogue, dialogueLine, cutscene, dungeon, dungeonFloor, dungeonReward, raid, equip, equipLevel, equipGrade, equipSet, equipSetBonus, equipOption, gacha, gachaPool, gachaOdds, pity, levelCurve, statBase, ascendCurve, powerScore, item, currency, material, consumable, offer, offerItem, itemGrade, string, offlineReward, idleRate, afkBonus, quest, questStep, dailyQuest, weeklyQuest, achievement, reward, lootGroup, lootEntry, mailReward, setting, condition, unlock, table, shop, shopSlot, price, promo, skill, skillLevel, skillEffect, buff, buffPool, passive, stage, stageWave, stageMonster, stageReward, chapter, world)
  }

  /** Read all tables synchronously. */
  public readAllSync(basePath: string, fileExtension: string = '.json'): void {
    const character = new CharacterTable()
    character.readSync(path.join(basePath, `Character${fileExtension}`))
    const characterStat = new CharacterStatTable()
    characterStat.readSync(path.join(basePath, `CharacterStat${fileExtension}`))
    const characterAscend = new CharacterAscendTable()
    characterAscend.readSync(path.join(basePath, `CharacterAscend${fileExtension}`))
    const characterSkin = new CharacterSkinTable()
    characterSkin.readSync(path.join(basePath, `CharacterSkin${fileExtension}`))
    const characterVoice = new CharacterVoiceTable()
    characterVoice.readSync(path.join(basePath, `CharacterVoice${fileExtension}`))
    const roleBonus = new RoleBonusTable()
    roleBonus.readSync(path.join(basePath, `RoleBonus${fileExtension}`))
    const elementAffinity = new ElementAffinityTable()
    elementAffinity.readSync(path.join(basePath, `ElementAffinity${fileExtension}`))
    const codex = new CodexTable()
    codex.readSync(path.join(basePath, `Codex${fileExtension}`))
    const codexEntry = new CodexEntryTable()
    codexEntry.readSync(path.join(basePath, `CodexEntry${fileExtension}`))
    const codexTier = new CodexTierTable()
    codexTier.readSync(path.join(basePath, `CodexTier${fileExtension}`))
    const dialogue = new DialogueTable()
    dialogue.readSync(path.join(basePath, `Dialogue${fileExtension}`))
    const dialogueLine = new DialogueLineTable()
    dialogueLine.readSync(path.join(basePath, `DialogueLine${fileExtension}`))
    const cutscene = new CutsceneTable()
    cutscene.readSync(path.join(basePath, `Cutscene${fileExtension}`))
    const dungeon = new DungeonTable()
    dungeon.readSync(path.join(basePath, `Dungeon${fileExtension}`))
    const dungeonFloor = new DungeonFloorTable()
    dungeonFloor.readSync(path.join(basePath, `DungeonFloor${fileExtension}`))
    const dungeonReward = new DungeonRewardTable()
    dungeonReward.readSync(path.join(basePath, `DungeonReward${fileExtension}`))
    const raid = new RaidTable()
    raid.readSync(path.join(basePath, `Raid${fileExtension}`))
    const equip = new EquipTable()
    equip.readSync(path.join(basePath, `Equip${fileExtension}`))
    const equipLevel = new EquipLevelTable()
    equipLevel.readSync(path.join(basePath, `EquipLevel${fileExtension}`))
    const equipGrade = new EquipGradeTable()
    equipGrade.readSync(path.join(basePath, `EquipGrade${fileExtension}`))
    const equipSet = new EquipSetTable()
    equipSet.readSync(path.join(basePath, `EquipSet${fileExtension}`))
    const equipSetBonus = new EquipSetBonusTable()
    equipSetBonus.readSync(path.join(basePath, `EquipSetBonus${fileExtension}`))
    const equipOption = new EquipOptionTable()
    equipOption.readSync(path.join(basePath, `EquipOption${fileExtension}`))
    const gacha = new GachaTable()
    gacha.readSync(path.join(basePath, `Gacha${fileExtension}`))
    const gachaPool = new GachaPoolTable()
    gachaPool.readSync(path.join(basePath, `GachaPool${fileExtension}`))
    const gachaOdds = new GachaOddsTable()
    gachaOdds.readSync(path.join(basePath, `GachaOdds${fileExtension}`))
    const pity = new PityTable()
    pity.readSync(path.join(basePath, `Pity${fileExtension}`))
    const levelCurve = new LevelCurveTable()
    levelCurve.readSync(path.join(basePath, `LevelCurve${fileExtension}`))
    const statBase = new StatBaseTable()
    statBase.readSync(path.join(basePath, `StatBase${fileExtension}`))
    const ascendCurve = new AscendCurveTable()
    ascendCurve.readSync(path.join(basePath, `AscendCurve${fileExtension}`))
    const powerScore = new PowerScoreTable()
    powerScore.readSync(path.join(basePath, `PowerScore${fileExtension}`))
    const item = new ItemTable()
    item.readSync(path.join(basePath, `Item${fileExtension}`))
    const currency = new CurrencyTable()
    currency.readSync(path.join(basePath, `Currency${fileExtension}`))
    const material = new MaterialTable()
    material.readSync(path.join(basePath, `Material${fileExtension}`))
    const consumable = new ConsumableTable()
    consumable.readSync(path.join(basePath, `Consumable${fileExtension}`))
    const offer = new OfferTable()
    offer.readSync(path.join(basePath, `Offer${fileExtension}`))
    const offerItem = new OfferItemTable()
    offerItem.readSync(path.join(basePath, `OfferItem${fileExtension}`))
    const itemGrade = new ItemGradeTable()
    itemGrade.readSync(path.join(basePath, `ItemGrade${fileExtension}`))
    const string = new StringTable()
    string.readSync(path.join(basePath, `String${fileExtension}`))
    const offlineReward = new OfflineRewardTable()
    offlineReward.readSync(path.join(basePath, `OfflineReward${fileExtension}`))
    const idleRate = new IdleRateTable()
    idleRate.readSync(path.join(basePath, `IdleRate${fileExtension}`))
    const afkBonus = new AfkBonusTable()
    afkBonus.readSync(path.join(basePath, `AfkBonus${fileExtension}`))
    const quest = new QuestTable()
    quest.readSync(path.join(basePath, `Quest${fileExtension}`))
    const questStep = new QuestStepTable()
    questStep.readSync(path.join(basePath, `QuestStep${fileExtension}`))
    const dailyQuest = new DailyQuestTable()
    dailyQuest.readSync(path.join(basePath, `DailyQuest${fileExtension}`))
    const weeklyQuest = new WeeklyQuestTable()
    weeklyQuest.readSync(path.join(basePath, `WeeklyQuest${fileExtension}`))
    const achievement = new AchievementTable()
    achievement.readSync(path.join(basePath, `Achievement${fileExtension}`))
    const reward = new RewardTable()
    reward.readSync(path.join(basePath, `Reward${fileExtension}`))
    const lootGroup = new LootGroupTable()
    lootGroup.readSync(path.join(basePath, `LootGroup${fileExtension}`))
    const lootEntry = new LootEntryTable()
    lootEntry.readSync(path.join(basePath, `LootEntry${fileExtension}`))
    const mailReward = new MailRewardTable()
    mailReward.readSync(path.join(basePath, `MailReward${fileExtension}`))
    const setting = new SettingTable()
    setting.readSync(path.join(basePath, `Setting${fileExtension}`))
    const condition = new ConditionTable()
    condition.readSync(path.join(basePath, `Condition${fileExtension}`))
    const unlock = new UnlockTable()
    unlock.readSync(path.join(basePath, `Unlock${fileExtension}`))
    const table = new TableTable()
    table.readSync(path.join(basePath, `Table${fileExtension}`))
    const shop = new ShopTable()
    shop.readSync(path.join(basePath, `Shop${fileExtension}`))
    const shopSlot = new ShopSlotTable()
    shopSlot.readSync(path.join(basePath, `ShopSlot${fileExtension}`))
    const price = new PriceTable()
    price.readSync(path.join(basePath, `Price${fileExtension}`))
    const promo = new PromoTable()
    promo.readSync(path.join(basePath, `Promo${fileExtension}`))
    const skill = new SkillTable()
    skill.readSync(path.join(basePath, `Skill${fileExtension}`))
    const skillLevel = new SkillLevelTable()
    skillLevel.readSync(path.join(basePath, `SkillLevel${fileExtension}`))
    const skillEffect = new SkillEffectTable()
    skillEffect.readSync(path.join(basePath, `SkillEffect${fileExtension}`))
    const buff = new BuffTable()
    buff.readSync(path.join(basePath, `Buff${fileExtension}`))
    const buffPool = new BuffPoolTable()
    buffPool.readSync(path.join(basePath, `BuffPool${fileExtension}`))
    const passive = new PassiveTable()
    passive.readSync(path.join(basePath, `Passive${fileExtension}`))
    const stage = new StageTable()
    stage.readSync(path.join(basePath, `Stage${fileExtension}`))
    const stageWave = new StageWaveTable()
    stageWave.readSync(path.join(basePath, `StageWave${fileExtension}`))
    const stageMonster = new StageMonsterTable()
    stageMonster.readSync(path.join(basePath, `StageMonster${fileExtension}`))
    const stageReward = new StageRewardTable()
    stageReward.readSync(path.join(basePath, `StageReward${fileExtension}`))
    const chapter = new ChapterTable()
    chapter.readSync(path.join(basePath, `Chapter${fileExtension}`))
    const world = new WorldTable()
    world.readSync(path.join(basePath, `World${fileExtension}`))

    this.publish(character, characterStat, characterAscend, characterSkin, characterVoice, roleBonus, elementAffinity, codex, codexEntry, codexTier, dialogue, dialogueLine, cutscene, dungeon, dungeonFloor, dungeonReward, raid, equip, equipLevel, equipGrade, equipSet, equipSetBonus, equipOption, gacha, gachaPool, gachaOdds, pity, levelCurve, statBase, ascendCurve, powerScore, item, currency, material, consumable, offer, offerItem, itemGrade, string, offlineReward, idleRate, afkBonus, quest, questStep, dailyQuest, weeklyQuest, achievement, reward, lootGroup, lootEntry, mailReward, setting, condition, unlock, table, shop, shopSlot, price, promo, skill, skillLevel, skillEffect, buff, buffPool, passive, stage, stageWave, stageMonster, stageReward, chapter, world)
  }

  /**
   * Read all tables from the binary export.
   *
   * The counterpart of `readAllSync`, and the one to use when the data is binary: reading a
   * table on its own with `readBinarySync` leaves its references unlinked, because linking
   * needs every table. Both formats produce the same values.
   */
  public readAllBinarySync(basePath: string, fileExtension: string = '.tcb'): void {
    const character = new CharacterTable()
    character.readBinarySync(path.join(basePath, `Character${fileExtension}`))
    const characterStat = new CharacterStatTable()
    characterStat.readBinarySync(path.join(basePath, `CharacterStat${fileExtension}`))
    const characterAscend = new CharacterAscendTable()
    characterAscend.readBinarySync(path.join(basePath, `CharacterAscend${fileExtension}`))
    const characterSkin = new CharacterSkinTable()
    characterSkin.readBinarySync(path.join(basePath, `CharacterSkin${fileExtension}`))
    const characterVoice = new CharacterVoiceTable()
    characterVoice.readBinarySync(path.join(basePath, `CharacterVoice${fileExtension}`))
    const roleBonus = new RoleBonusTable()
    roleBonus.readBinarySync(path.join(basePath, `RoleBonus${fileExtension}`))
    const elementAffinity = new ElementAffinityTable()
    elementAffinity.readBinarySync(path.join(basePath, `ElementAffinity${fileExtension}`))
    const codex = new CodexTable()
    codex.readBinarySync(path.join(basePath, `Codex${fileExtension}`))
    const codexEntry = new CodexEntryTable()
    codexEntry.readBinarySync(path.join(basePath, `CodexEntry${fileExtension}`))
    const codexTier = new CodexTierTable()
    codexTier.readBinarySync(path.join(basePath, `CodexTier${fileExtension}`))
    const dialogue = new DialogueTable()
    dialogue.readBinarySync(path.join(basePath, `Dialogue${fileExtension}`))
    const dialogueLine = new DialogueLineTable()
    dialogueLine.readBinarySync(path.join(basePath, `DialogueLine${fileExtension}`))
    const cutscene = new CutsceneTable()
    cutscene.readBinarySync(path.join(basePath, `Cutscene${fileExtension}`))
    const dungeon = new DungeonTable()
    dungeon.readBinarySync(path.join(basePath, `Dungeon${fileExtension}`))
    const dungeonFloor = new DungeonFloorTable()
    dungeonFloor.readBinarySync(path.join(basePath, `DungeonFloor${fileExtension}`))
    const dungeonReward = new DungeonRewardTable()
    dungeonReward.readBinarySync(path.join(basePath, `DungeonReward${fileExtension}`))
    const raid = new RaidTable()
    raid.readBinarySync(path.join(basePath, `Raid${fileExtension}`))
    const equip = new EquipTable()
    equip.readBinarySync(path.join(basePath, `Equip${fileExtension}`))
    const equipLevel = new EquipLevelTable()
    equipLevel.readBinarySync(path.join(basePath, `EquipLevel${fileExtension}`))
    const equipGrade = new EquipGradeTable()
    equipGrade.readBinarySync(path.join(basePath, `EquipGrade${fileExtension}`))
    const equipSet = new EquipSetTable()
    equipSet.readBinarySync(path.join(basePath, `EquipSet${fileExtension}`))
    const equipSetBonus = new EquipSetBonusTable()
    equipSetBonus.readBinarySync(path.join(basePath, `EquipSetBonus${fileExtension}`))
    const equipOption = new EquipOptionTable()
    equipOption.readBinarySync(path.join(basePath, `EquipOption${fileExtension}`))
    const gacha = new GachaTable()
    gacha.readBinarySync(path.join(basePath, `Gacha${fileExtension}`))
    const gachaPool = new GachaPoolTable()
    gachaPool.readBinarySync(path.join(basePath, `GachaPool${fileExtension}`))
    const gachaOdds = new GachaOddsTable()
    gachaOdds.readBinarySync(path.join(basePath, `GachaOdds${fileExtension}`))
    const pity = new PityTable()
    pity.readBinarySync(path.join(basePath, `Pity${fileExtension}`))
    const levelCurve = new LevelCurveTable()
    levelCurve.readBinarySync(path.join(basePath, `LevelCurve${fileExtension}`))
    const statBase = new StatBaseTable()
    statBase.readBinarySync(path.join(basePath, `StatBase${fileExtension}`))
    const ascendCurve = new AscendCurveTable()
    ascendCurve.readBinarySync(path.join(basePath, `AscendCurve${fileExtension}`))
    const powerScore = new PowerScoreTable()
    powerScore.readBinarySync(path.join(basePath, `PowerScore${fileExtension}`))
    const item = new ItemTable()
    item.readBinarySync(path.join(basePath, `Item${fileExtension}`))
    const currency = new CurrencyTable()
    currency.readBinarySync(path.join(basePath, `Currency${fileExtension}`))
    const material = new MaterialTable()
    material.readBinarySync(path.join(basePath, `Material${fileExtension}`))
    const consumable = new ConsumableTable()
    consumable.readBinarySync(path.join(basePath, `Consumable${fileExtension}`))
    const offer = new OfferTable()
    offer.readBinarySync(path.join(basePath, `Offer${fileExtension}`))
    const offerItem = new OfferItemTable()
    offerItem.readBinarySync(path.join(basePath, `OfferItem${fileExtension}`))
    const itemGrade = new ItemGradeTable()
    itemGrade.readBinarySync(path.join(basePath, `ItemGrade${fileExtension}`))
    const string = new StringTable()
    string.readBinarySync(path.join(basePath, `String${fileExtension}`))
    const offlineReward = new OfflineRewardTable()
    offlineReward.readBinarySync(path.join(basePath, `OfflineReward${fileExtension}`))
    const idleRate = new IdleRateTable()
    idleRate.readBinarySync(path.join(basePath, `IdleRate${fileExtension}`))
    const afkBonus = new AfkBonusTable()
    afkBonus.readBinarySync(path.join(basePath, `AfkBonus${fileExtension}`))
    const quest = new QuestTable()
    quest.readBinarySync(path.join(basePath, `Quest${fileExtension}`))
    const questStep = new QuestStepTable()
    questStep.readBinarySync(path.join(basePath, `QuestStep${fileExtension}`))
    const dailyQuest = new DailyQuestTable()
    dailyQuest.readBinarySync(path.join(basePath, `DailyQuest${fileExtension}`))
    const weeklyQuest = new WeeklyQuestTable()
    weeklyQuest.readBinarySync(path.join(basePath, `WeeklyQuest${fileExtension}`))
    const achievement = new AchievementTable()
    achievement.readBinarySync(path.join(basePath, `Achievement${fileExtension}`))
    const reward = new RewardTable()
    reward.readBinarySync(path.join(basePath, `Reward${fileExtension}`))
    const lootGroup = new LootGroupTable()
    lootGroup.readBinarySync(path.join(basePath, `LootGroup${fileExtension}`))
    const lootEntry = new LootEntryTable()
    lootEntry.readBinarySync(path.join(basePath, `LootEntry${fileExtension}`))
    const mailReward = new MailRewardTable()
    mailReward.readBinarySync(path.join(basePath, `MailReward${fileExtension}`))
    const setting = new SettingTable()
    setting.readBinarySync(path.join(basePath, `Setting${fileExtension}`))
    const condition = new ConditionTable()
    condition.readBinarySync(path.join(basePath, `Condition${fileExtension}`))
    const unlock = new UnlockTable()
    unlock.readBinarySync(path.join(basePath, `Unlock${fileExtension}`))
    const table = new TableTable()
    table.readBinarySync(path.join(basePath, `Table${fileExtension}`))
    const shop = new ShopTable()
    shop.readBinarySync(path.join(basePath, `Shop${fileExtension}`))
    const shopSlot = new ShopSlotTable()
    shopSlot.readBinarySync(path.join(basePath, `ShopSlot${fileExtension}`))
    const price = new PriceTable()
    price.readBinarySync(path.join(basePath, `Price${fileExtension}`))
    const promo = new PromoTable()
    promo.readBinarySync(path.join(basePath, `Promo${fileExtension}`))
    const skill = new SkillTable()
    skill.readBinarySync(path.join(basePath, `Skill${fileExtension}`))
    const skillLevel = new SkillLevelTable()
    skillLevel.readBinarySync(path.join(basePath, `SkillLevel${fileExtension}`))
    const skillEffect = new SkillEffectTable()
    skillEffect.readBinarySync(path.join(basePath, `SkillEffect${fileExtension}`))
    const buff = new BuffTable()
    buff.readBinarySync(path.join(basePath, `Buff${fileExtension}`))
    const buffPool = new BuffPoolTable()
    buffPool.readBinarySync(path.join(basePath, `BuffPool${fileExtension}`))
    const passive = new PassiveTable()
    passive.readBinarySync(path.join(basePath, `Passive${fileExtension}`))
    const stage = new StageTable()
    stage.readBinarySync(path.join(basePath, `Stage${fileExtension}`))
    const stageWave = new StageWaveTable()
    stageWave.readBinarySync(path.join(basePath, `StageWave${fileExtension}`))
    const stageMonster = new StageMonsterTable()
    stageMonster.readBinarySync(path.join(basePath, `StageMonster${fileExtension}`))
    const stageReward = new StageRewardTable()
    stageReward.readBinarySync(path.join(basePath, `StageReward${fileExtension}`))
    const chapter = new ChapterTable()
    chapter.readBinarySync(path.join(basePath, `Chapter${fileExtension}`))
    const world = new WorldTable()
    world.readBinarySync(path.join(basePath, `World${fileExtension}`))

    this.publish(character, characterStat, characterAscend, characterSkin, characterVoice, roleBonus, elementAffinity, codex, codexEntry, codexTier, dialogue, dialogueLine, cutscene, dungeon, dungeonFloor, dungeonReward, raid, equip, equipLevel, equipGrade, equipSet, equipSetBonus, equipOption, gacha, gachaPool, gachaOdds, pity, levelCurve, statBase, ascendCurve, powerScore, item, currency, material, consumable, offer, offerItem, itemGrade, string, offlineReward, idleRate, afkBonus, quest, questStep, dailyQuest, weeklyQuest, achievement, reward, lootGroup, lootEntry, mailReward, setting, condition, unlock, table, shop, shopSlot, price, promo, skill, skillLevel, skillEffect, buff, buffPool, passive, stage, stageWave, stageMonster, stageReward, chapter, world)
  }

  /**
   * Publishes one whole load.
   *
   * Reading again - a refresh, a downloaded patch - loads into tables of its own and gets
   * here only once every file has been read. A failure anywhere leaves every table holding
   * what it held, which is the answer a running program wants: the data it already had, and
   * an exception saying why the new data was not taken.
   */
  private publish(character: CharacterTable, characterStat: CharacterStatTable, characterAscend: CharacterAscendTable, characterSkin: CharacterSkinTable, characterVoice: CharacterVoiceTable, roleBonus: RoleBonusTable, elementAffinity: ElementAffinityTable, codex: CodexTable, codexEntry: CodexEntryTable, codexTier: CodexTierTable, dialogue: DialogueTable, dialogueLine: DialogueLineTable, cutscene: CutsceneTable, dungeon: DungeonTable, dungeonFloor: DungeonFloorTable, dungeonReward: DungeonRewardTable, raid: RaidTable, equip: EquipTable, equipLevel: EquipLevelTable, equipGrade: EquipGradeTable, equipSet: EquipSetTable, equipSetBonus: EquipSetBonusTable, equipOption: EquipOptionTable, gacha: GachaTable, gachaPool: GachaPoolTable, gachaOdds: GachaOddsTable, pity: PityTable, levelCurve: LevelCurveTable, statBase: StatBaseTable, ascendCurve: AscendCurveTable, powerScore: PowerScoreTable, item: ItemTable, currency: CurrencyTable, material: MaterialTable, consumable: ConsumableTable, offer: OfferTable, offerItem: OfferItemTable, itemGrade: ItemGradeTable, string: StringTable, offlineReward: OfflineRewardTable, idleRate: IdleRateTable, afkBonus: AfkBonusTable, quest: QuestTable, questStep: QuestStepTable, dailyQuest: DailyQuestTable, weeklyQuest: WeeklyQuestTable, achievement: AchievementTable, reward: RewardTable, lootGroup: LootGroupTable, lootEntry: LootEntryTable, mailReward: MailRewardTable, setting: SettingTable, condition: ConditionTable, unlock: UnlockTable, table: TableTable, shop: ShopTable, shopSlot: ShopSlotTable, price: PriceTable, promo: PromoTable, skill: SkillTable, skillLevel: SkillLevelTable, skillEffect: SkillEffectTable, buff: BuffTable, buffPool: BuffPoolTable, passive: PassiveTable, stage: StageTable, stageWave: StageWaveTable, stageMonster: StageMonsterTable, stageReward: StageRewardTable, chapter: ChapterTable, world: WorldTable): void {
    this._character = character
    this._characterStat = characterStat
    this._characterAscend = characterAscend
    this._characterSkin = characterSkin
    this._characterVoice = characterVoice
    this._roleBonus = roleBonus
    this._elementAffinity = elementAffinity
    this._codex = codex
    this._codexEntry = codexEntry
    this._codexTier = codexTier
    this._dialogue = dialogue
    this._dialogueLine = dialogueLine
    this._cutscene = cutscene
    this._dungeon = dungeon
    this._dungeonFloor = dungeonFloor
    this._dungeonReward = dungeonReward
    this._raid = raid
    this._equip = equip
    this._equipLevel = equipLevel
    this._equipGrade = equipGrade
    this._equipSet = equipSet
    this._equipSetBonus = equipSetBonus
    this._equipOption = equipOption
    this._gacha = gacha
    this._gachaPool = gachaPool
    this._gachaOdds = gachaOdds
    this._pity = pity
    this._levelCurve = levelCurve
    this._statBase = statBase
    this._ascendCurve = ascendCurve
    this._powerScore = powerScore
    this._item = item
    this._currency = currency
    this._material = material
    this._consumable = consumable
    this._offer = offer
    this._offerItem = offerItem
    this._itemGrade = itemGrade
    this._string = string
    this._offlineReward = offlineReward
    this._idleRate = idleRate
    this._afkBonus = afkBonus
    this._quest = quest
    this._questStep = questStep
    this._dailyQuest = dailyQuest
    this._weeklyQuest = weeklyQuest
    this._achievement = achievement
    this._reward = reward
    this._lootGroup = lootGroup
    this._lootEntry = lootEntry
    this._mailReward = mailReward
    this._setting = setting
    this._condition = condition
    this._unlock = unlock
    this._table = table
    this._shop = shop
    this._shopSlot = shopSlot
    this._price = price
    this._promo = promo
    this._skill = skill
    this._skillLevel = skillLevel
    this._skillEffect = skillEffect
    this._buff = buff
    this._buffPool = buffPool
    this._passive = passive
    this._stage = stage
    this._stageWave = stageWave
    this._stageMonster = stageMonster
    this._stageReward = stageReward
    this._chapter = chapter
    this._world = world

    this.solveCrossReferences()
  }

  /**
   * Turns the stored keys into the rows they name, once every table is in memory.
   *
   * A `foreign` column travels as the target row's primary key, so a table read on its own
   * has the key and not the row - which is why this runs from `publish` and not from a
   * table's own read. A zero means the sheet left the cell empty and is left unresolved.
   */
  private solveCrossReferences(): void {
    // No table references another.
  }
}
