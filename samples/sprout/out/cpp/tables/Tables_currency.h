// ------------------------------------------------------------------------------
// Generated by Tabbit. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

#ifndef TABBIT_GENERATED_TABLES_CURRENCY_H
#define TABBIT_GENERATED_TABLES_CURRENCY_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "tabbit/tcb_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_currency_type.h"

namespace sprout {
// Generated from samples/sprout/xlsx/Item.xlsx : CurrencyTable : A1
struct CurrencyRecord {
  /// primary index
  std::int32_t id = 0;
  /// currency kind
  CurrencyType type = static_cast<CurrencyType>(0);
  /// name key
  std::string name_key;
  /// icon asset
  std::string icon;
  /// hard cap, 0 for none
  std::int64_t cap = 0;
};

class CurrencyTable {
 public:
  const std::vector<CurrencyRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const CurrencyRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const CurrencyRecord& get_by_id_or_throw(std::int32_t key) const {
    const CurrencyRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw tabbit::RecordNotFound(
        std::string("there is no record in table `Currency` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .tcb file written by Tabbit.
  void read(const std::string& filename) {
    // Not const, and it outlives the reader on purpose: opening the envelope decrypts in
    // place and hands back a window onto these same bytes rather than a copy of them.
    std::vector<std::uint8_t> buffer = tabbit::read_all_bytes(filename);

    // Unconditionally, and with whatever `encryption_key` and `mac_key` hold - which is
    // empty unless the project set them. A file that is neither encrypted nor signed comes
    // back from this untouched, so the load path is the same either way and there is no
    // condition here that could be the wrong way round. spec/wire/tcb-mac-and-signature.md.
    const std::pair<const std::uint8_t*, std::size_t> bytes =
      tabbit::open(buffer, encryption_key, mac_key, verify_mac);

    tabbit::TcbReader reader(bytes.first, bytes.second);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const tabbit::Header header = tabbit::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<CurrencyRecord> records;
    records.resize(row_count);

    for (const tabbit::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          tabbit::check_column(column, "Currency.Id", tabbit::kKindScalar, false, {tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Currency.Id");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].id = value;
            }
          }
          break;
        }
        case 2: {
          tabbit::check_column(column, "Currency.Type", tabbit::kKindScalar, false, {tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Currency.Type");
          std::int32_t value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_i32(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].type = static_cast<CurrencyType>(value);
            }
          }
          break;
        }
        case 3: {
          tabbit::check_column(column, "Currency.NameKey", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Currency.NameKey");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].name_key = value;
            }
          }
          break;
        }
        case 4: {
          tabbit::check_column(column, "Currency.Icon", tabbit::kKindScalar, false, {tabbit::kElementString});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Currency.Icon");
          std::string value{};
          for (std::size_t i = 0; i < row_count; ) {
            std::int32_t n = cursor.next_same_string(
                static_cast<std::int32_t>(row_count - i), value);
            for (; n > 0; --n, ++i) {
              records[i].icon = value;
            }
          }
          break;
        }
        case 5: {
          tabbit::check_column(column, "Currency.Cap", tabbit::kKindScalar, false, {tabbit::kElementI64, tabbit::kElementI32, tabbit::kElementVarint});
          tabbit::TcbColumnCursor cursor(reader, column, header.row_count, "Currency.Cap");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.cap = cursor.next_i64();
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      tabbit::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<CurrencyRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<CurrencyRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace sprout

#endif  // TABBIT_GENERATED_TABLES_CURRENCY_H
