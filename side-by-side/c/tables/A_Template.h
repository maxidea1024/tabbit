/* ------------------------------------------------------------------------------
 * Generated by Tabbit. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef A_TEMPLATE_H
#define A_TEMPLATE_H

#include "tabbit/tabbit_tcb_reader.h"

#include "A_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Generated from test/fixtures/xlsx/reserved-words/reserved-words.xlsx : ReservedWords : B2
 *
 * Named after a C++ keyword.
 */
struct A_TemplateRecord_t {
  /* primary index */
  int32_t index;
  /* class: keyword in C++ and C# */
  const char* class_;
  /* int: keyword in C++ and C# */
  int32_t int_;
  /* delete: keyword in C++ */
  bool delete_;
  /* operator: keyword in C++, and a secondary index */
  const char* operator_;
  /* namespace: keyword in C++ and C# */
  const char* namespace_;
  /* constructor: special member in TypeScript */
  const char* constructor;
  /* function: keyword in TypeScript */
  const char* function;
};

/* Every row of Template.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct A_TemplateTable_t {
  A_TemplateRecord_t* records;
  int32_t count;

  tb_index_entry* by_index;
  tb_string_index_entry* by_operator;
  tb_arena arena;
} A_TemplateTable_t;

/* Loads the table from a .tcb file written by Tabbit.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool A_TemplateLoad(A_TemplateTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void A_TemplateFree(A_TemplateTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const A_TemplateRecord_t* A_TemplateFindByIndex(
  const A_TemplateTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool A_TemplateContainsIndex(
  const A_TemplateTable_t* table, int32_t key);
/* The row with the given Operator, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const A_TemplateRecord_t* A_TemplateFindByOperator(
  const A_TemplateTable_t* table, const char* key);

/* Whether the table holds a row with this Operator. */
bool A_TemplateContainsOperator(
  const A_TemplateTable_t* table, const char* key);

#ifdef __cplusplus
}
#endif

#endif /* A_TEMPLATE_H */
