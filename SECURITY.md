# 보안 문제 신고

## 신고처

취약점은 **공개 이슈로 올리지 마세요.** GitHub의
[Security → Report a vulnerability](https://github.com/maxidea1024/Tabbit/security/advisories/new)로
비공개 신고를 열어 주세요. 그 화면이 보이지 않으면 저장소 관리자에게 직접 연락 주세요.

무엇을 어떻게 하면 재현되는지, 그리고 어떤 버전에서 봤는지를 적어 주시면 가장 빠릅니다.

## 취약점의 범위

이 도구가 다루는 것은 **신뢰할 수 없는 입력이 아닙니다.** 시트도 recipe도 그 프로젝트 사람이
쓴 것이고, 생성된 코드는 그 프로젝트가 컴파일합니다. 그래서 "이상한 엑셀을 넣으면 변환이
죽는다"는 버그이지 취약점이 아닙니다 — 그냥 이슈로 올려 주세요.

취약점으로 다루는 것은 이런 것들입니다.

- **비밀값이 새는 경우.** recipe의 `${환경변수}`로 받은 데이터베이스 비밀번호나 구글 OAuth
  토큰이 로그·생성물·히스토리 기록에 남는 경우.
- **의도하지 않은 곳에 쓰는 경우.** 시트나 recipe의 값이 출력 폴더 밖의 경로를 만들어내는
  경우(경로 탈출).
- **`--serve`가 노출되는 경우.** 히스토리 웹서버는 기본이 루프백이고 외부 바인딩에는 토큰을
  요구합니다. 그 두 가지를 우회할 수 있다면 취약점입니다.
- **생성된 리더가 손상된 데이터에 당하는 경우.** `.tcb` 파일은 CDN에서 내려받을 수 있으므로
  신뢰 경계 밖입니다. 13개 언어 리더 중 하나가 조작된 파일에 메모리 안전성을 잃는다면
  취약점입니다.
- 의존성의 알려진 취약점 중 **이 도구에서 실제로 닿는 경로**가 있는 것.

**`.tcb`의 암호화 키와 MAC 키가 클라이언트에서 추출된다는 것은 취약점이 아닙니다.** 두 키는
파일을 읽는 클라이언트에 실리고, 그것을 막는 형식은 없습니다 — 그 층이 무엇을 위한 것인지는
[형식 문서](doc/binary-format.md#이-두-layer가-하는-일과-하지-않는-일)에 적혀 있습니다. 다만
**키 없이** 변조된 파일이 검증을 통과하거나, 리더가 MAC 검사를 건너뛰는 경로가 있다면
취약점입니다.

## 지원 범위

최신 릴리즈만 고칩니다. 아직 1.0 이전이라 지난 버전으로 백포트하지 않습니다.
