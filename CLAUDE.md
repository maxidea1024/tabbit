# 이 저장소에서 지켜야 하는 것

`Tabbit`은 **범용 도구**입니다. 특정 게임·특정 팀의 시트를 읽는 것은 이 도구가 하는 역할의
한 가지 사례이지, 이 도구의 정체가 아닙니다.

---

## 코어에 프로젝트 이름 금지

**가장 중요한 규칙입니다.**

`samples/`의 프로젝트들(현재 `sprout`, `canopy`, `wildling`)은 **검증이 끝나면 폐기될 수 있습니다.**
그것들의 이름이 코어에 들어가면 두 가지 비용이 생깁니다 — 코어가 지저분해지고, **폐기 비용이
발생합니다.** 레이아웃 하나를 지우는데 recipe 스키마와 문서와 옵션 파싱을 함께 뒤져야 한다면,
그건 지울 수 없는 것이 된 것입니다.

### 코어의 범위

|위치|프로젝트 이름 허용|
|--|--|
|`src/Recipe/` · `src/Models/` · `src/Sources/` · `src/Importers/` · `src/Exporters/` · `src/CodeGeneration/` · `src/Targets/`|**금지**|
|`src/Cooking/Layouts/<X>LayoutParser.cs` 와 그 전용 헬퍼|허용 — **그 파일이 곧 그 프로젝트의 플러그인**|
|`samples/<X>/` · `doc/<X>-*.md`|허용|
|테스트 픽스처·레시피|허용|

### 대신 하는 것

**기능은 일반적으로 만들고, 이름은 플러그인 쪽에만 둡니다.**

|필요한 것|하는 방법|
|--|--|
|레이아웃 추가|`[TabbitLayout("id")]` 를 단 클래스 하나. 레지스트리가 어트리뷰트 스캔으로 찾으므로 **코어에 등록 코드가 없습니다**|
|**레이아웃 전용 설정**|`LayoutOptions` 자유 키/값 맵. 코어는 키를 모르고 검증하지 않습니다. 레이아웃이 `SheetLayout.Option(key)` 로 읽고, `RequireKnownOptions` 로 오타를 보고합니다|
|모든 레이아웃에 해당하는 설정|`SheetSourceRecipe` 의 정식 프로퍼티. `ArrayDelimiter`·`OnDuplicateIndex`·`OnFormulaError` 가 그런 것들입니다 — 어느 레이아웃인지 몰라도 찾을 수 있어야 하기 때문입니다|
|임포터가 레이아웃별로 다르게 읽어야 할 때|어트리뷰트에 선언하고 임포터가 확인합니다. `UsesNamedRanges` 가 그 예입니다 — 임포터는 「이 레이아웃이 정의된 이름을 쓰나?」만 확인하고, 어느 프로젝트인지는 모릅니다|

**판단 기준 하나:** 그 레이아웃 파일 하나를 지웠을 때 빌드가 깨지거나 코어에 흔적이 남으면,
설계가 잘못된 것입니다.

### 문서에도 동일 적용

코어 코드의 주석과 XML 문서에 프로젝트 이름을 쓰지 않습니다. 「어떤 레이아웃은」·「이 도구가
설계하지 않은 규칙으로 쓰인 시트는」처럼 일반적으로 적습니다. `doc/<프로젝트>-*.md` 는 예외이고,
거기서는 마음껏 구체적으로 씁니다.

> `LanguageProfile.cs` 와 `ruby-table.sbn` 의 `rescue` 는 **Ruby 키워드**입니다.
> 샘플 이름이 아니므로 그대로 둡니다.

---

## 샘플 데이터의 합성 원칙

`samples/` 의 워크북은 **전부 생성물**입니다. 손으로 만든 것도, 어디선가 가져온 것도 없습니다.

이 도구는 한동안 상용 프로젝트의 워크북으로 검증하였고, 그 데이터가 저장소에 있는 한 저장소를
공개할 수 없었습니다. 지금은 없고, **다시 들어오지 않게 하는 것이 이 규칙입니다.**

|하는 것|하지 않는 것|
|--|--|
|`samples/<X>/schema/*.tsv` 에 표기를 적고 생성기가 값을 만듭니다|실제 워크북을 복사해 오는 것. 시트 한 장이라도|
|규모와 분포를 재현합니다 — 행 수, 컬럼 타입, 값의 반복 성질|실제 값·밸런스 수치를 옮기는 것|
|이름은 새로 짓습니다|실제 테이블·컬럼·enum 이름을 옮기는 것|

**이름 판정은 둘로 갈립니다.** 테이블·enum 이름은 겹치면 갈아냅니다 — 단 어느 게임에나 있는
낱말 하나짜리(`Item`·`Quest`·`Shop`)는 예외입니다. 컬럼 이름은 일반어이거나 **일반어 둘을
뻔하게 붙인 것**(`itemId`·`sortOrder`·`maxHitPoints`)이면 통과입니다. 붙이는 방법이 여럿인
합성어는 우연히 겹치지 않으므로, 겹쳤다면 옮겨 적은 것입니다.

**픽스처도 같습니다.** 이진 파일은 문자열 게이트가 보지 못합니다 —
`tools/mabbit/test/fixtures/` 의 워크북이 실제로 그렇게 들어와 있었습니다.

게이트 셋이 [`.github/workflows/names.yml`](.github/workflows/names.yml) 에 있습니다.
경로·문자열·ASCII 경로를 봅니다. **이진 파일 안의 이름은 아무 게이트도 보지 못하므로 사람이
지킵니다.**

---

## 문서의 말투

**은유를 쓰지 않습니다.** 사물이 사람의 동사를 가져다 쓰면 그 문장은 정확하지 않게 됩니다.
판정은 하나입니다 — **주어를 사람이나 물건으로 바꿔 보고 자연스러우면 전용된 비유입니다.**

|쓰지 않습니다|씁니다|
|--|--|
|목록이 **썩습니다**|목록이 실제와 어긋납니다|
|게이트가 **섭니다** · 빌드가 **서지 않습니다**|게이트가 동작합니다 · 빌드가 실패합니다|
|행의 **신원**|그 행을 가리키는 것|
|코드에 **박혀** 있습니다|코드에 고정되어 있습니다|
|압축이 원본을 **이깁니다**|압축 결과가 원본보다 작습니다|
|게이트가 워크북에 **매달립니다**|게이트가 그 워크북 하나에 의존합니다|

`korean-report-style` 스킬의 검사기가 이것들을 검출합니다. 문서를 고쳤으면 돌리십시오.

```
python <스킬경로>/assets/lint.py <파일>
```

**표에 없는 것은 검사기가 검출하지 못합니다.** 새로 발견하면 스킬의
`references/substitutions.md` 에 한 줄 더합니다 — 그래야 다음에 검출됩니다.

그 밖의 규약은 스킬에 있습니다. 요지 셋만 여기 둡니다.

- **어체는 「합니다」체 하나입니다.** 평서체와 섞지 않습니다
- **제목은 명사구입니다.** 「무엇을 하는가」가 아니라 「하는 일」
- **개수는 숫자로.** 「열세 개」가 아니라 「13개」. 관형사(`세 가지`)는 그대로

---

## 그 외

- **커밋 메시지에는 conventional prefix를 붙입니다.** `feat:` · `fix:` · `refactor:` ·
  `chore:` · `docs:` · `test:` 형식입니다. 예: `fix: xlsb 짧은 행 복원에서 이름 범위 밖 셀 누락`.
- **전체 테스트는 아껴서.** `dotnet test` 전체는 **6분 40초 안팎**입니다 — 2026-08-28 실측
  4회, 1,829개. (한때 「10분」, 그다음 「22분 30초」, 그다음 「15분 53초」였습니다. **줄어든 것은
  시나리오를 테스트마다 다시 변환하던 것을 없애서**이지 게이트가 빠져서가 아닙니다 — 214곳에서
  변환하는데 시나리오는 73개뿐이었습니다.) 작업 중에는 `--filter` 로 관련 게이트만, 전체는
  푸쉬 전 한 번.
  **옵트인 게이트 둘은 그 시간에 없습니다** — `TABBIT_UE_ROOT` 를 걸어야 도는
  UnrealHeaderTool 과 `TABBIT_LUAJIT` 의 LuaJIT 모드입니다. 언리얼 템플릿을 건드렸으면
  UHT 를 따로 돌리십시오(전 언리얼 게이트가 50초).
- **테스트에서 변환은 시나리오당 한 번입니다.** `TabbitRunner.Convert(scenario)` 가 실행당
  한 번만 돌고 나머지는 그 답을 받습니다. **그래서 시나리오의 출력 트리는 공유 자원입니다** —
  거기에 파일을 쓰거나 그 위에 다시 변환하는 테스트는 **자기 시나리오를 가져야 합니다**
  (`sweep.json` 이 그런 예입니다). 남의 트리를 바꾸면 그것을 읽는 다른 테스트가 깨집니다.
- **생성 트리 안에서 빌드하지 않습니다.** 적합성 하네스는 `_lang/<시나리오>/<언어>` 로 복사해
  거기서 빌드합니다. 트리를 걷으며 모든 파일을 판정하는 테스트가 셋 있어서, 컴파일러의 작업
  파일이 트리에 남으면 그것을 산출물로 봅니다. 언어 하네스를 새로 만들면 `Generated()` 를
  거치십시오.
- **느린 것은 언어 툴체인 게이트입니다.** 모든 언어의 생성 코드를 실제로 컴파일하고 되읽는
  부분이 그 시간의 대부분입니다. **생성기·템플릿·와이어에 닿지 않는 변경이라면 그쪽에 도달할
  경로가 없고**, 그 사실은 골든으로 26초에 확인됩니다:
  `dotnet test --filter "FullyQualifiedName~ConversionGolden"`. 골든이 안 움직였는데 툴체인이
  깨질 수 있는 변경은 생성 코드를 건드린 변경뿐입니다.
- **골든이 판정 기준입니다.** 동작 보존 리팩터링이라면 골든이 한 바이트도 바뀌지 않아야 하고,
  바뀌었다면 그 diff가 리뷰 대상입니다.
- **픽스처 워크북을 더하거나 고쳤으면 `xlsx-reader.tsv`를 재기록합니다.** 워크북마다 「몇 장·몇
  행·몇 셀·해시」가 거기 적혀 있고, 새 워크북 하나가 그 게이트를 빨갛게 만듭니다. 골든과 같은
  스위치입니다 — `TABBIT_UPDATE_GOLDEN=1 dotnet test --filter "FullyQualifiedName~XlsxReaderGate"`.
  **`--filter`만 돌린 커밋에서는 이것이 보이지 않습니다.**
- **생성기·템플릿을 건드렸으면** 골든 재기록 → 전 언어 비교본 재생성 → **샘플 재생성** → 기록
  없이 재검증 순서로. 샘플을 빼먹어도 스위트는 통과하므로 — `samples/*/out/`은 커밋되어
  있지만 게이트가 없습니다 — 이 순서가 유일한 방어선입니다. 자세한 것은
  [아키텍처와 개발](doc/architecture.md#개발--테스트).
  **재생성 두 단계에는 `--full --force-output` 을 붙입니다.** 빌드 캐시는 레시피와 워크북을
  보고 생성기 버전은 보지 않으므로, 붙이지 않으면 **오류 없이 0.3초에 끝나고 아무것도 다시 쓰지
  않습니다.** 2026-08-28에 실제로 그렇게 지나갔고, 아무 게이트도 그것을 보지 않습니다.
